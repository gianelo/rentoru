# Tasks: MVP Rental Listings

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~5,600–8,250 total (greenfield, 8 capabilities) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR0a → PR0b → PR0c → PR1 → PR1b → PR2 → PR3 → PR4 → PR5 → PR6 → PR7 → PR8 → PR9 → PR10 → PR11 |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Session preflight collected 2026-08-16: execution mode `auto`, artifact store `hybrid`, delivery `auto-chain`, chain `stacked-to-main`, review budget 400 lines.

**Note on the 400-line budget.** It measures *authored* lines — what a human has to read. The native attempt runtime counts generated content too, so a slice that adds dependencies will report a much larger number because of `pnpm-lock.yaml`. Lockfiles are trusted, not reviewed. Where the two disagree, the authored count governs the review decision and the discrepancy is recorded rather than papered over.

### The forecasts below were wrong, and here is why

**Measured on 2026-08-16, after three consecutive slices overran:** these estimates forecast *implementation* and silently omitted *proof*. In a strict-TDD project that is not a rounding error — it is roughly half the diff.

PR1b-b is the clean measurement. Of its 508 authored insertions, **203 were the proof itself** — a 78-line WCAG contrast helper and a 125-line design-contract spec — against ~180 lines of actual components. The remainder was tokens and task bookkeeping. Six of twelve tasks consumed the entire 400-line budget, and the four tasks needing a real layout engine had not been started.

Two consequences, both applied:

1. **The estimates below are revised upward** for every slice not yet built. The old numbers were not pessimistic enough to be useful, and a forecast that is always wrong in the same direction stops being a forecast and becomes a ritual.
2. **A slice whose proof is expensive gets split on the proof boundary, not the feature boundary.** PR1b-b/PR1b-c is the worked example: the tasks provable by computation or static declaration shipped together; the four needing a real browser measurement became their own slice, because building that harness is itself ~130–155 lines before it proves anything.

This is the honest alternative to a third size exception. Granting one more would have made the budget advisory; correcting the forecast keeps it real.

### Per-slice estimate

| Slice | Est. lines | Risk |
|---|---|---|
| PR0a Toolchain + test harness + gate scripts | 250–320 | Low (config) |
| PR0b Persistence + auth scaffold | 200–280 | Medium |
| PR0c Deploy + CI pipeline | 180–280 | Medium |
| PR1 Identity + phone-verification port | 300–450 | Low |
| PR1b-a Tokens, root attributes, token contract, layout primitives | ~~200–300~~ **actual 734** | Shipped, `size:exception` |
| PR1b-b Atoms, button hierarchy, publisher badge, contrast proof | ~~250–350~~ **actual 526** | Shipped at reduced scope |
| PR1b-c Layout-measurement harness + the four measured bounds | 250–350 | Medium — the harness is ~130–155 before it proves anything |
| PR2 City/zone schema + seed + D5 proof | ~~400–600~~ **actual 258** | Shipped — split before a size:exception (see Phase 2) |
| PR2b Cascading city→zone select | ~~185~~ **actual 198** | Shipped 2026-08-16, within forecast — no exception |
| PR3 Publication core | 1200–1800 | **High — must be split into 3–4 slices before apply** |
| PR4 Trust: photo-hash dedup | 600–900 | Medium — split into 2 |
| PR5 Search | 700–1000 | Medium — split into 2–3 |
| PR6 Contact reveal | 600–900 | Medium — split into 2 |
| PR7 Lifecycle: reminder job | 1400–2000 | **High — split into 3–4** |
| PR8 Trust: reporting/auto-hide | 500–800 | Low — split into 2 |
| PR9 Broker bulk import | 1300–1900 | **High — split into 3–4** |
| PR10 Voluntary contribution | 250–400 | Low |
| PR11 Discovery & SEO surfaces + budget gates | 1100–1600 | Medium — split into 3 |

Revised figures apply a **~2× multiplier for proof** to the original implementation-only estimates, which is what the three measured slices actually cost. The total moves from ~5,600–8,250 to roughly **9,000–13,000 authored lines**, and the number of slices roughly doubles. That is not scope growth — it is the same product, forecast honestly for the first time.

PR9 depends only on PR3 (publication) and PR4 (trust), not on PR5–PR8. It is placed last to keep the stack linear, but it can be pulled forward if seed brokers need to load portfolios while the rest is still being built. PR10 depends on nothing beyond the app shell and can ship at any point — but see the D8 licensing decision in the design before shipping it.

### Suggested Work Units

| Unit | Goal | Likely PR | Focused test command | Runtime harness | Rollback boundary |
|---|---|---|---|---|---|
| 0a | Toolchain, test harness, gate scripts, config.yaml F1, F2 re-verify | PR0a | `pnpm test` (empty pass) | `pnpm lint`, `pnpm lint:tokens`, `pnpm tsc --noEmit` all clean | Revert repo to pre-scaffold state |
| 0b | Drizzle config, Neon client, Auth.js v5 + Google + Drizzle adapter | PR0b | `pnpm test` | Migrations apply to a Neon branch | `drizzle/**`, `src/shared/db/**`, auth config |
| 0c | Vercel deploy, CI pipeline, Postgres service container, gate-failure proof | PR0c | `pnpm test` | CI green on a throwaway PR; every gate proven to fail when violated | `.github/**`, `vercel.json` |
| 1 | Google sign-in, session guard, disabled phone-verification port | PR1 | `pnpm test:unit -- identity` | Manual Google OAuth sign-in on deployed preview | `src/modules/identity/**`, `app/(auth)/**` |
| 1b | Design tokens (`compacto` + `menta`), atoms, three-level buttons, result row, a11y baseline, D16 token contract | PR1b | `pnpm test -- design-system` | Render at 360px and 1280px on preview; measure the result row against its 96px height bound; flip `data-theme` in the inspector; contrast and focus audit | `src/styles/tokens.css`, `components/atoms/**` |
| 2 | City/zone schema, seed, cascading select | PR2 | `pnpm test:integration -- zone` | Seed script run against Neon branch | `drizzle/*_zones.sql`, seed script |
| 3 | Listing CRUD, publisher_type, min-content, city-FK, upload guard | PR3 | `pnpm test -- publication` | Publish flow on preview deploy | `src/modules/listing-publication/**` |
| 4 | dHash + publisher-excluding match port wired into publish | PR4 | `pnpm test:integration -- photo-hash` | Publish two accounts, same photo | `src/modules/listing-trust/**` (hash only) |
| 5 | Search port with required cityId, filters, city isolation | PR5 | `pnpm test:integration -- search` | Search UI on preview deploy | `src/modules/listing-search/**` |
| 6 | Reveal event + unique-pair view | PR6 | `pnpm test:integration -- reveal` | Reveal action on preview deploy | `src/modules/contact-reveal/**`, view migration |
| 7 | Expiry, reminder job, renewal token, cron route | PR7 | `pnpm test -- lifecycle` | `curl -X POST` job route with/without Bearer token | `src/modules/listing-lifecycle/**`, `app/api/jobs/**` |
| 8 | Reporting, auto-hide, operator restore | PR8 | `pnpm test -- trust-reporting` | Report flow x3 accounts on preview | `src/modules/listing-trust/**` (reporting only) |
| 9 | Operator-gated CSV import, whole-file validation, preview, drafts, idempotency | PR9 | `pnpm test -- bulk-import` | Import a real broker portfolio on preview deploy | `src/modules/broker-bulk-import/**`, draft-state migration |
| 10 | Dismissible contribution invitation + external destination page | PR10 | `pnpm test -- contribution` | Invitation renders and dismisses on preview | `src/modules/voluntary-contribution/**` |
| 11 | Zone landing pages, URL scheme, expired-listing retention page, sitemap, structured data | PR11 | `pnpm test -- discovery` | Crawl preview with JS disabled; budget check on preview | `app/(discovery)/**`, sitemap/robots routes |

## Phase 0: Bootstrap & Toolchain (PR0a → PR0b → PR0c)

**Split, per the 400-line review budget.** PR0 was forecast at 650–900 lines, so it ships as three stacked slices. The seam is not arbitrary — each slice is independently revertible and each ends with something that demonstrably runs:

| Slice | Tasks | Ends when |
|---|---|---|
| **PR0a** Toolchain & gate scripts | 0.1–0.4, 0.8b, 0.10 | `pnpm test`, `pnpm lint`, `pnpm lint:tokens`, `pnpm tsc --noEmit` all run and pass on an empty project |
| **PR0b** Persistence & auth scaffold | 0.5, 0.6 | Drizzle migrations apply to a Neon branch; the Auth.js tables exist |
| **PR0c** Deploy & CI pipeline | 0.7, 0.8, 0.9, 0.11 | A throwaway PR turns CI green, and every gate is proven to fail the build when violated |

PR0c is deliberately last: a pipeline that runs nothing is not worth reviewing, and the gates can only be proven to *fail* correctly once there is something for them to fail against.

**Note on the pnpm version ceiling (added during PR0c, after the founder's first Vercel deploy failed).** `package.json`'s `packageManager`/`devEngines` and `.github/workflows/ci.yml`'s `PNPM_VERSION` are pinned to **pnpm 10.x**, not the newer 11.x line used earlier in Phase 0. Vercel's build image does not recognize `packageManager: pnpm@11` — it falls back to npm, which then rejects the repo's `devEngines.packageManager` block outright (`EBADDEVENGINES`). The fix is a downgrade, not a Vercel environment variable (`ENABLE_EXPERIMENTAL_COREPACK` was considered and rejected — it would put an experimental flag on the critical path of every deploy). Whoever next bumps pnpm should re-check Vercel's supported package-manager versions before raising this ceiling past 10.x.

- [x] 0.1 `pnpm init`, add Next.js 15 + React 19, TS, `tsconfig.json`
- [x] 0.2 Configure Biome (`biome.json`), Vitest (`vitest.config.ts`), Playwright (`playwright.config.ts`)
- [x] 0.3 Apply follow-up F1: update `openspec/config.yaml` `testing:`/`context:`/`rules` blocks per design
- [x] 0.4 Follow-up F2: re-verify current Vercel/Neon/R2/Resend free-tier limits; record deltas vs design doc
- [x] 0.5 Drizzle config + `src/shared/db/client.ts` (Neon pooled endpoint)
- [x] 0.6 Auth.js v5 + Google provider + Drizzle adapter (`user`, `account`, `session` tables)
- [x] 0.7 Deploy skeleton to Vercel; confirm live Neon connection **Conciliada 2026-08-27: estaba hecha hace mucho y el plan mentía.** Verificado por la API de despliegues de GitHub y no por lectura del plan: `gh api repos/gianelo/rentas.com.ve/deployments?environment=Production` devuelve un despliegue de **producción** con `state: "success"` del 2026-08-27 sobre `aef3e98`, y el trabajo de `preview` del `ci.yml` (líneas 269–290) espera un despliegue de Vercel por PR y le pasa su URL al trabajo de e2e — o sea que el camino de despliegue no sólo existe, es de lo que depende la suite. La conexión viva a Neon la prueba el mismo despliegue sirviendo `/alquiler/**`, que consulta la base en cada carga. **No verificable desde este entorno**: `curl https://rentas.com.ve/` no resuelve el nombre porque el arenero no tiene DNS, así que la evidencia es la de la API y la del CI, no una respuesta HTTP del dominio.
- [x] 0.8 `.github/workflows/ci.yml`: lint, types, unit, coverage, `lint:tokens` and integration on every push; E2E, crawlability and both budget gates on pull requests only (Actions minutes are metered on a private repository)
- [x] 0.8b `pnpm lint:tokens` — fails on a colour literal, corner radius, thumbnail dimension or type size written as a value instead of a custom property in component styles (D16). Passes trivially until PR1b lands components; it exists from PR0 so no component is ever written without it
- [x] 0.9 Postgres service container in CI pinned to Neon's major version — the integration layer must not run against an emulator (pinned to `postgres:18`; the live Neon instance was verified at `server_version 18.4` on 2026-08-16, and the job asserts the container's version at runtime rather than trusting the image tag)
- [x] 0.10 Coverage gate: 90% floor on `src/modules/*/domain/` and `src/modules/*/application/`; no target on `infrastructure/` or `app/`
- [x] 0.11 Confirm every gate fails the build when violated — a gate that only warns is not a gate

**Gate-failure evidence (0.11), measured 2026-08-16.** Each gate was given a deliberate violation, the exit status recorded, the violation reverted, and the clean tree re-checked. A gate is only proven by the failing case; the passing case proves nothing on its own.

| Gate | Deliberate violation | Violated | Clean |
|---|---|---|---|
| `biome ci .` | badly formatted source file | exit 1 | exit 0 |
| `tsc --noEmit` | `const n: number = "not a number"` | exit 1 | exit 0 |
| `vitest run` | a test asserting `expect(1).toBe(2)` | exit 1 | exit 0 |
| `lint:tokens` | `.x { color: #272343; }` in `components/` | exit 1 | exit 0 |
| coverage floor | an uncovered file under `src/modules/*/domain/` | exit 1 | exit 0 |

The coverage failure names the scoped glob explicitly — `Coverage for lines (0%) does not meet "src/modules/*/domain/**" threshold (90%)` — which confirms the floor is the scoped policy from the design and not a repo-wide percentage in disguise.

**Two gates cannot be proven yet, and the workflow says so out loud.** E2E/crawlability has no specs and no preview URL; both budget gates have no script to run. Rather than letting an empty run look green, each emits a GitHub warning stating that it currently cannot fail and must not be read as a passing check. They are re-proven when PR11 lands their implementations.

## Phase 1: Identity + Phone Verification Port (PR1)

- [x] 1.1 RED: unauthenticated protected action redirects to sign-in
- [x] 1.2 GREEN: session-guard helper, `src/modules/identity/application/`
- [x] 1.3 RED: Google sign-in creates one account with only email + display name (implemented in PR0b — `toMinimalGoogleProfile` + `google-profile.test.ts`; verified against this spec scenario during PR1, no change needed)
- [x] 1.4 GREEN: Auth.js callback restricts captured profile fields (implemented in PR0b — wired as the Google provider's `profile()` callback in `auth.ts`; verified during PR1, no change needed)
- [x] 1.5 RED: expired session forces re-auth on protected action
- [x] 1.6 GREEN: expiry handling; sign-in UI at `app/(auth)/signin`
- [x] 1.7 RED: publish succeeds regardless of phone-verification status
- [x] 1.8 GREEN: `PhoneVerificationPort` contract + `DisabledPhoneVerificationAdapter` (`PHONE_VERIFICATION_ENABLED=false`, no domain branch)

## Phase 1b: Design Foundation (PR1b-a → PR1b-b)

Inserted between PR1 and PR2 because PR2 builds the first UI (the cascading city/zone select) and cannot be written before the visual language is settled. Carries D14, D15, and D16.

**Split, per the 400-line review budget.** Forecast at 400–600 lines across 18 tasks, so it ships as two stacked slices:

| Slice | Tasks | Ends when |
|---|---|---|
| **PR1b-a** The system and its guard | 1b.1–1b.4, 1b.13, 1b.17 | Tokens ship, the root carries `data-theme`/`data-layout`, `lint:tokens` rejects literals in real component styles, and a theme swap repaints everything |
| **PR1b-b** Provable atoms | 1b.6, 1b.7, 1b.9, 1b.15, 1b.16, 1b.18 | Button hierarchy, publisher badge, price typography, contrast, and focus visibility — every claim proved by real computation or a static declaration assertion, no fabricated layout proof |
| **PR1b-c** Result row, real layout | 1b.8, 1b.10–1b.12, 1b.14 | **Shipped 2026-08-16, 260 authored lines.** The result row is composed into a real route (`app/measure`) and measured with an actual Playwright layout engine at 360px/1280px — the four claims a stylesheet-content assertion cannot honestly prove. `1b.5`'s remaining atoms (chip, input, label, breadcrumb) stayed deferred — no task in this slice exercised them |

The order is not cosmetic. **The token contract has to land before the first component**, or components get written against literal values and the guard arrives to a codebase it must retroactively clean. A rule that arrives after the code it governs is a migration, not a guarantee.

**PR1b-a shipped at 734 authored lines against the 400 budget — `size:exception`, accepted by the founder on 2026-08-16.** The overage is recorded rather than absorbed silently. Its shape: `scripts/lint-tokens.mjs` 230 lines (real logic — the D16 guard, and where review effort actually belongs), `src/styles/tokens.css` 140 lines (transcription of three token sets from the design reference), five layout primitives ~290 lines across 15 near-identical small files, and ~90 lines of config and docs. The argument accepted was that review burden here is not proportional to line count. **This was the second consecutive exception** (PR0a was the first, 10 lines over on a generated lockfile). A third would mean the budget has become advisory, and the honest response then is to lower the forecast or split earlier — not to keep granting exceptions.

**PR1b-b split itself further, before a third exception could happen.** The original 12-task PR1b-b scope was re-forecast at apply time and found to not fit 400 authored lines even for its cheapest half: the six tasks provable by pure computation or a static-declaration assertion (button hierarchy, badge, price typography, contrast, focus, webfont/JS) alone cost ~490 authored lines (`components/atoms/*`, `components/contrast.ts` — a from-scratch WCAG luminance/contrast implementation kept dependency-free — and one consolidated `components/design-contract.test.tsx`). Rather than take a third exception, or bolt the real Playwright layout-measurement harness for 1b.10–1b.12/1b.14 onto an already-full diff, the remainder ships as **PR1b-c**, not yet started. Estimated cost of that harness alone (a `/measure` route serving real production CSS + a dedicated `playwright.measure.config.ts` + a multi-check spec) is ~130–155 lines before any 1b.5 atoms are added — its own budget, not a top-up on this one. `components/molecules/ResultRow.tsx` is built here structurally (grid, price/badge/title/metadata, correct DOM order — unit-tested) but its three measured claims (1b.10, 1b.11, 1b.12) are explicitly **not proven** and stay unchecked; a CSS-content assertion for `96px` would prove the stylesheet was written, not that the row renders within the bound, which is the entire point of those tasks.

**Source of truth:** `design/reference/sistema/SISTEMA.md` (system), `design/reference/sistema/tokens.css` (token sets), `design/reference/sistema/pantallas-compacto-menta.html` (six worked surfaces at 360px and 1280px). Combination: `data-theme="menta"` + `data-layout="compacto"`. The reference HTML is a prototype, not production code — its inline styles and `support.js` runtime are never ported. **CORREGIDA EL 2026-09-05: llamar «fuente de verdad» a estos tres archivos sin más es falso desde que llegaron las láminas, y así escrito hace que el próximo lector vuelva a litigar lo que ya está decidido.** `SISTEMA.md` se escribió al arrancar el proyecto, cuando no había ni una pantalla; las láminas llegaron el 2026-08-25, con el proyecto ya avanzado, y llegaron como una **corrección deliberada de UI/UX**. Estos tres archivos siguen siendo la referencia, pero **gobernados por el orden de autoridad que el fundador fijó el 2026-09-05** —texto ya shipeado y decidido, después la lámina para UI/UX, después el sistema para la redacción, después el sistema donde ninguna lámina dibuja—, escrito completo en el encabezado de la **Fase 22**.

- [x] 1b.1 Port the shipped token subset to `src/styles/tokens.css` — `[data-layout="compacto"]`, `[data-theme="menta"]`, plus one dark set behind `prefers-color-scheme` (D16). The remaining ten sets stay in the reference only
- [x] 1b.2 Set `data-theme` / `data-layout` on the root element in the app shell; no component reads either attribute directly
- [x] 1b.3 RED: **no component style contains a hex literal, a raw corner radius, a thumbnail dimension, or a literal type size** — every one resolves through a custom property (D16). Wire it as a lint rule, not a review habit
- [x] 1b.4 RED: swapping `data-theme` on the root element repaints every rendered atom and molecule, with no element retaining its previous colour (the inspector-flip criterion, automated)
- [ ] 1b.5 Átomos por diseño atómico, desde la anatomía de componentes del sistema: precio, título, metadato, placa, miniatura, ficha, botón, campo, etiqueta, miga de pan. **REESCRITA EL 2026-08-28, y no por un cambio de opinión: la lista de diez venía de una anatomía que ya no existe.** Las nueve láminas llegaron el 2026-08-25 y contestaron distinto; `SISTEMA.md` describía una FILA con miniatura al costado y el producto dibuja una CUADRÍCULA de tarjetas desde la 14.25. Los átomos se re-derivaron contra las láminas, **contando consumidores archivo por archivo y no de memoria**.

  **La cuenta anterior de esta misma tarea tenía dos celdas mal, verificadas una por una:**

  - `ListingStrip.tsx:74` **no es un título de aviso**: es `<h2 className={styles.title} id={headingId}>`, el encabezado de la SECCIÓN de una tira («Recientes en Caracas»). Papel tipográfico distinto, y meterlo en el átomo de título de lista es exactamente cómo el `<h1>` del inicio terminó agarrando `--fpb`.
  - la ficha `page.tsx:432` **no es la línea de metadatos**: es `<span className={styles.meta}>ID … · vence …</span>`, el pie con el identificador. Usa `--meta` (mono) y `--meta-fs-sm` (11), no `--sans`/`--meta-fs`/`--meta-fw`. Otro papel.

  **La cuenta re-derivada, con `file:line`:**

  | átomo | consumidores VIVOS | consumidor muerto | estado |
  |---|---|---|---|
  | **precio** | `ListingCard.tsx:70`, `app/mis-avisos/page.tsx:233` | `ResultRow.tsx:57` | **cerrado** (`components/atoms/Price.tsx`), y con el defecto de tamaño corregido en la 22.2 |
  | **metadato** | `ListingCard.module.css:72`, `mis-avisos.module.css:126` | `ResultRow.module.css:58` | **CERRADO 2026-08-28** — `components/atoms/ListingMeta.tsx`, tarea 22.3 |
  | **título de lista** | `ListingCard.module.css:56`, `mis-avisos.module.css:120` | `ResultRow.module.css:44` | **CERRADO 2026-08-28** — `components/atoms/ListingTitle.tsx`, tarea 22.4 |
  | **placa de publicador** | `ListingCard.tsx:66`, la ficha | — | **cerrado** (`components/atoms/PublisherBadge.tsx`). Pero ver la 21.15: las dos láminas la dibujan ENCIMA de la foto y el código la pone en el cuerpo |
  | **etiqueta** | los formularios | — | **cerrado** (`components/atoms/Label.tsx`) |
  | **botón** | todo el producto | — | **cerrado** (`components/atoms/buttons.tsx`), tres niveles y sin prop de variante |
  | **ficha de selección** | `home.module.css:69`, `mis-avisos.module.css:52`; `FilterChips.module.css:14` comparte el idioma pero **es otro papel** (siempre aplicada, siempre con ×) | — | **idioma unificado 2026-08-28** (22.5). El átomo como componente queda como **21.16** |
  | **miniatura** | `mis-avisos.module.css:94`, `photo-uploader.module.css:115` | `ResultRow.module.css:20` | **NO se promueve, y hay que decidir antes**: ninguna lámina nueva dibuja 44 × 34 y la de `/mis-avisos` dibuja 74 × 56 — es la **21.19**, y es una decisión antes que un átomo |
  | **campo** | **UNO**: `ContactBlock.tsx:186`. Publicar, editar, reportar, importar, entrar y los filtros escriben su propio control | — | **la pieza está cerrada y probada; la ADOPCIÓN sigue abierta** — ver el recuento del 2026-09-02 abajo |
  | **miga de pan** | `app/alquiler/[ciudad]/page.tsx:274` y `[zona]/page.tsx:286`, con `.breadcrumb/.crumbs/.crumb/.crumbLink` duplicadas en dos hojas | — | **NO es un átomo del sistema: ninguna lámina del 2026-08-25 dibuja una miga de pan.** Es duplicación de código y se resuelve extrayendo el ENCABEZADO entero, no la miga sola — es la **21.12** |

  **Lo que cierra esta barrida, con su archivo y su prueba nombrada:** el metadato (`components/atoms/ListingMeta.tsx` — `tests/measure/layout.spec.ts` → *«22.3: el metadato se dibuja igual en la tarjeta y en /mis-avisos»*) y el título de lista (`components/atoms/ListingTitle.tsx` — *«22.4: el título de lista se dibuja igual en la tarjeta y en /mis-avisos»*). Las dos son **mediciones de estilo calculado en un navegador**, no aserciones sobre el texto de una hoja: la lección de la 22.2 es que una hoja puede declarar lo correcto y la pantalla pintar otra cosa.

  **Lo que queda abierto en esta tarea:** el campo (`input`), y la ficha de selección como componente (21.16). La miga de pan **sale de la lista** y pasa a la 21.12 con su razón. La miniatura queda bloqueada por la 21.19.

  **RECUENTO DEL 2026-09-02, y esta tarea NO cierra. Las tres remisiones de arriba apuntan a números que no existen.** Verificado sobre el archivo entero: **no hay ninguna tarea 21.12, 21.16 ni 21.19** — la fase 21 llega hasta la 21.7 y ahí termina. La 1b.5 quedó apoyada en tres números inventados el 2026-08-28, así que sus tres diferimientos no estaban diferidos a ninguna parte: estaban perdidos. **Los tres tienen destino vivo en la fase 22 y se corrigen acá**, sin mover una sola decisión:

  - la ficha de selección como componente → **22.12** («La ficha de selección como átomo, que la 22.5 dejó a medio camino»), que es literalmente el mismo trabajo con el mismo diagnóstico;
  - la miga de pan → **22.6** («El encabezado de resultados, extraído»), que ya la nombra dentro de las ~146 líneas duplicadas al 100 % entre `ciudad.module.css` y `zona.module.css` — y que es exactamente lo que la 21.12 pedía: el encabezado entero, no la miga sola;
  - la miniatura → **22.15** («`/mis-avisos` viste la miniatura de una fila que ya no existe»), que es la decisión de 44 × 34 contra 74 × 56 que bloqueaba el átomo.

  **El campo, contado archivo por archivo el 2026-09-02, cae en dos mitades y sólo una está abierta.**

  **La pieza: hecha y afirmada, con archivo y prueba nombrada.** `components/molecules/Field.tsx` + `Field.module.css`, probados en `components/design-contract.test.tsx` (3.9) por siete afirmaciones: *«renders the glyph and the word, both inside the label»*, *«marks nothing when the field is optional»*, *«sets aria-invalid and points aria-describedby at a message that exists»*, *«adds nothing to announce when the field is valid»*, *«keeps the help text when an error appears, and puts the error first»*, *«uses the touch-target tokens rather than a bare 44px»* y *«declares the 2px error border the design specifies»*. No se escribió una prueba nueva para esto: ya estaba, y escribir la segunda habría sido la duplicación que la 14.42 prohíbe.

  **La adopción: UN consumidor vivo, y la cabecera de esa prueba decía tres.** `components/molecules/ContactBlock.tsx:186` y nadie más — el otro `import { Field }` del repositorio es el de la propia prueba. La cabecera del bloque 3.9 afirmaba que «la pantalla de renovación, el flujo de reporte y la vista previa de la importación» obtenían estas reglas componiendo `Field`; **los tres son falsos y se verificaron uno por uno**: la renovación no es una pantalla sino `app/renovar/[token]/route.ts`, un manejador de ruta sin formulario; `reportar/page.tsx` sólo lleva campos ocultos y sus propios controles; e `ImportarCartera.tsx` escribe su `<input type="file">` a mano. La cabecera se corrigió en esta entrega.

  **Lo que sí se construyó acá, porque era la mitad que nada guardaba:** *«el campo del sistema tiene quien lo dibuje (1b.5)»* en `components/design-contract.test.tsx`, con su guarda al lado. Es la lección de la 8.9 escrita como aserción en vez de como párrafo: `FieldRow` se entregó probado y sin que lo dibujara nadie, y su prueba estuvo verde hasta el día que se borró la pieza entera. `Field` está hoy **a un consumidor** de esa misma situación. Se cuenta recorriendo `components/` y `app/` y no de una lista, y a propósito en esa dirección: una lista de consumidores se pondría roja al AGREGAR uno, que es justo el movimiento que esta tarea quiere; ésta sólo se pone roja cuando desaparece el último. **Mutación**: aliasar el `Field` de `ContactBlock` a `Campo` —mismo render, mismos bytes servidos, las 19 pruebas de comportamiento de `ContactBlock` siguen verdes— deja **exactamente 1 roja, la nueva**; sin ella, la suite entera pasa con el sistema de diseño sin un solo dibujante de su campo.

  **Lo que falta y lo que cuesta.** El criterio de cierre que esta misma tabla usa para el metadato y el título (22.3/22.4) es la ADOPCIÓN: se cierran extrayendo la declaración duplicada, no declarando que la pieza existe. Bajo ese criterio el campo sigue abierto, y adoptarlo es reescribir el marcado de campo de `app/publicar/PublishStep.tsx` (~10 campos, con su propio `FieldError` y su propio esquema de `id`), `app/mis-avisos/[id]/editar/page.tsx` (~8), `reportar/page.tsx`, `ImportarCartera.tsx` y `(auth)/signin/page.tsx`. **Muy por encima del techo de 400 líneas**, y no es mecánico: `SearchFilters`/`SearchPanel`/`SearchPill` dibujan controles de FILTRO, que es otro papel —la misma distinción que esta tabla ya hizo entre la ficha de selección y `FilterChips`—, y decidir si entran o no es una decisión de diseño. Además toca dos pantallas contra las que la fase 22 ya tiene tareas abiertas (22.6, 22.15). **Va a decisión del fundador y número propio, no se hace de oficio acá**, que es el mismo camino que esta tabla le dio a la miniatura.

  — nota anterior, conservada: `Price` y `PublisherBadge` shipped as standalone atoms (PR1b-b); `Label` shipped as a standalone atom (PR2b, `components/atoms/Label.tsx`) — the cascading city/zone select was its first real consumer, **y ese consumidor se borró el 2026-08-28** (ver la corrección de la 2.5)

  **RECONTADA EL 2026-09-11 CONTRA EL CÓDIGO, y el recuento anterior sub-reportaba el avance.** La Fase 22 cerró 47 de 47 el 2026-09-07, **diez días después** del último recuento de esta tarea, y entregó átomos que este bloque seguía dando por abiertos. **Ocho de los diez están resueltos.**

  | átomo | dónde | quién lo cerró |
  | --- | --- | --- |
  | precio | `components/atoms/Price.tsx` | ya existía; **22.2** corrigió su tamaño |
  | metadato | `components/atoms/ListingMeta.tsx` | **22.3** |
  | título de lista | `components/atoms/ListingTitle.tsx` | **22.4** |
  | placa de publicador | `components/atoms/PublisherBadge.tsx` | ya existía; el defecto de posición se resolvió aparte en **22.10** |
  | etiqueta | `components/atoms/Label.tsx` | ya existía |
  | botón | `components/atoms/buttons.tsx` | ya existía |
  | ficha de selección | `components/atoms/SelectionChip.tsx` | **22.12** — **este bloque la daba ABIERTA** |
  | miga de pan | `components/organisms/SearchResultsHeader.tsx` | **22.6**, y cerrada **como decisión y no como átomo**, que es exactamente lo que este bloque ya había concluido: «ninguna lámina dibuja una miga de pan sola» |

  **Cómo se verificó la ficha de selección, porque el método importa más que el resultado.** La casilla de la 22.12 **no trae párrafo de cierre** en este archivo, así que leer la prosa habría dado «abierta». Lo que prueba el cierre es el código: `SelectionChip.tsx` lleva un doc-comment que cita «tasks.md 22.5, 22.12» y está **adoptado de verdad**, reemplazando las declaraciones locales en `app/page.tsx` y `app/mis-avisos/page.tsx`. **Se audita contra el código, nunca contra el texto de la tarea** — un bloque puede quedar viejo, y éste quedó viejo.

  **LAS DOS BRECHAS QUE SIGUEN ABIERTAS, y por eso la casilla NO se marca:**

  1. **La miniatura no llegó a átomo.** La **22.15** saldó la decisión de tamaño —de 44×34 a 74×56, más el `@media (min-width: 768px)` que faltaba— pero la dejó **inline en `app/mis-avisos/mis-avisos.module.css`** con sus tokens `--mis-avisos-thumb-w`/`-h`, mientras `app/publicar/fotos/photo-uploader.module.css` sigue declarando su propio `--twd` por separado. **Es la misma forma de deuda que el metadato y el título ya pagaron**: el mismo dato declarado dos veces en dos archivos. La decisión está tomada; falta la extracción.
  2. **El campo existe y casi nadie lo usa.** `components/molecules/Field.tsx` está hecho y probado, pero la adopción va en **dos de unos siete consumidores**: `ContactBlock.tsx` y —nuevo desde el recuento anterior— `app/ayuda/escribinos/page.tsx` (tarea 23.7, que no es de la Fase 22). Siguen sin importarlo `app/publicar/PublishStep.tsx`, `app/mis-avisos/[id]/editar/page.tsx`, `reportar/page.tsx`, `ImportarCartera.tsx` y `(auth)/signin/page.tsx`. **Esto ya estaba diferido a decisión del fundador y con número propio**, y esa decisión sigue en pie: la migración pasa del techo de 400 líneas y lleva adentro una decisión de diseño —si los controles de FILTRO entran o no—, así que no se hace de oficio acá.

- [x] 1b.6 GREEN: the three-level button hierarchy as distinct components — action (filled `--accent`), selection/state (`--tint` fill, `--accent` border and text), neutral (`--strong` border, no fill). They are not variants of one component with a free-form prop, because the levels must not be mixed. `components/atoms/buttons.tsx` + `Button.module.css`; proved by CSS-declaration assertion + a structural check that `Props` carries no `variant` field
- [x] 1b.7 RED: the `publisher_type` badge is distinguishable with colour removed — owner is filled (`--ink` on `--surface`), broker is outlined (`--strong` border, `--soft` text). Asserted against a greyscale render, not by reading the CSS. Proved two ways: structural (fill vs border, survives greyscale by construction) and computed (real WCAG relative-luminance/contrast on the shipped tokens, both themes) — `components/design-contract.test.tsx`, `components/contrast.ts`
- [x] 1b.8 Molecule: the result row — grid `[thumbnail] 1fr`; price and publisher badge share the first line via `space-between`; title clamped to two lines below; metadata (`zona · N hab · N m²`) below that. Price precedes title in DOM order. Built in PR1b-b (`components/molecules/ResultRow.tsx`, DOM-order unit test passing); now measured against its real acceptance criterion by PR1b-c's Playwright harness (1b.10/1b.11 pass — see below), so the task is complete rather than structurally-built-and-unchecked
- [x] 1b.9 RED: the price renders in the monospace system stack with `tabular-nums`, so prices align as a column across rows. Static-declaration assertion on `Price.module.css` (`font-family: var(--disp)`, `font-variant-numeric: tabular-nums`) — the claim IS about the declaration, so this is honest proof, not a proxy
- [x] 1b.10 RED: a result row's rendered height stays within 96px at 360px, including a title long enough to wrap to its two-line clamp. Density is enforced as a bound on the row, not as a count of rows above the fold — a count is a proxy that breaks on a font-metric difference and tells you nothing about what regressed. Proven by `tests/measure/layout.spec.ts` against a real 108-character wrapped title: measured **85.89px** (bound ≤96px). RED captured live by deliberately setting the row's padding to `40px 12px` — measured **151.89px**, reverted
- [x] 1b.11 RED: the result row renders with no horizontal overflow at a 360px viewport (`document.documentElement.scrollWidth <= clientWidth`). Measured **scrollWidth=360px, clientWidth=360px**. RED captured live by deliberately adding `min-width: 500px` to the row — measured **scrollWidth=548px vs clientWidth=360px**, reverted
- [x] 1b.12 RED: at 1280px, result rows and running text stay within the 1100px container rather than spanning the window; body copy is capped at a 520px reading width. Measured row width **1100px** (bound ≤1100px) and body-copy width **520px** (bound ≤520px), via a new `components/layout/ReadingWidth.tsx` primitive (520px cap, SISTEMA.md "Cuerpo"). RED captured live twice: `Container.module.css` max-width widened to 1400px → measured **1264px**; `ReadingWidth.module.css` max-width widened to 900px → measured **900px**; both reverted
- [x] 1b.13 GREEN: two-viewport layout primitives — 1100px container, 240px sticky filter sidebar (`grid: 240px 1fr`, gap 32), 600px single-column form shell, and the detail split (640px media + 420px sticky data column). **⚠️ LA BARRA LATERAL DE 240px QUE ESTA CASILLA DECLARA YA NO EXISTE (anotado 2026-08-28, no corregido).** `components/layout/SidebarLayout.{tsx,module.css,test.tsx}` se borró en `a02e1b1` (tarea 14.44), porque la 14.33 sacó la barra lateral de las dos pantallas de resultados **en todos los anchos** por decisión del fundador del 2026-08-26. Verificado: el único `grid-template-columns` con 240px que queda en el repositorio es `app/publicar/publish-steps.module.css:531`, que es el **riel de nueve pasos** de publicar y no una barra de filtros. Los otros tres primitivos —`Container`, `FormShell`, `DetailSplit`— siguen en pie y siguen medidos. Decidir si la casilla se reescribe con tres primitivos en vez de cuatro; no se tocó acá por la misma razón que la 2.5
- [x] 1b.14 RED: every interactive target is at least 44px in its smallest dimension on mobile and 36px on desktop. `--target-min`/`--target-min-desktop` tokens (added in PR1b-b, wired into `Button.module.css`) now proven by rendered geometry across all three button levels: mobile smallest dimension **44px** (bound ≥44px), desktop smallest dimension **36px** (bound ≥36px). RED captured live twice: `--target-min` set to 30px → measured **34px** on mobile; reverted, then `--target-min-desktop` set to 24px → measured **34px** on desktop; both reverted **CORRECCIÓN medida el 2026-08-28 (16.24): el «36px» de escritorio que esta tarea registra era falso.** Con `--target-min-desktop` en 36 el botón dibujado mide **39,25 px** — el `padding: 8px 16px` y el `line-height` ganan sobre el `min-height`, así que la cota `>= 36` tenía 3,25 px de holgura que nadie sabía que estaban ahí y ni siquiera estaba midiendo el token. Se descubrió mutando: al bajar el token para comprobar que la nueva aserción tenía dientes, lo medido no fue 36. Con el token en 44 el `min-height` sí manda y la medida es 44,00 exacta en las dos pantallas, que es lo que `toBe(44)` ahora fija.
- [x] 1b.15 RED: text contrast meets WCAG AA across every token pair in use, in both the shipped light and dark sets. Real computation (`components/contrast.ts`) over the shipped hex values for four pairs actually used by this slice's components, both `menta` and `oscuro` — `components/design-contract.test.tsx`
- [x] 1b.16 RED: keyboard focus is visibly indicated on every interactive atom. Static-declaration assertion — every button level's `:focus-visible` rule has a non-`none`, non-zero `outline` resolving through a token
- [x] 1b.17 GREEN: base layout, landmarks, and heading structure
- [x] 1b.18 Confirm the shipped read-path CSS carries no webfont request and no runtime JavaScript. Static scan: `tokens.css` has no `@font-face`/`url()`; no shipped atom/molecule declares `"use client"`

## Phase 2: City & Zone Data (PR2)

**Split at apply time, per the 400-line review budget.** The 400–600 forecast held for the whole slice, but not for a single PR under the 400-line cap: schema + migration + seed + the real-Postgres integration proof alone measured 258 authored lines, and the cascading select (2.4/2.5) — component, its own unit test, and its stylesheet — added another 185, for 443 total. Per this change's standing rule ("deliver the schema and its structural proof fully and defer the select rather than overrunning"), 2.4/2.5 are deferred to a follow-up slice (**PR2b**) rather than taking a `size:exception`. This is the same proof-boundary split PR1b-b/PR1b-c already established: the tasks provable by a database constraint ship together; UI that needs its own component, test, and stylesheet becomes its own slice.

- [x] 2.1 Schema: `city`, `zone` tables; `zone` `UNIQUE(id, city_id)` (D5)
- [x] 2.2 RED: integration test — cross-city zone reference violates uniqueness (`tests/integration/zone.test.ts`). **Proven against real Postgres 18.6**, first in CI's `integration` job and since locally via `pnpm db:test:up`. Postgres rejects the cross-city row with `23503`, and the same zone paired with its correct city is asserted to succeed — without that second half a typo in the table name would produce the same rejection and the test would pass for the wrong reason
- [x] 2.3 Seed script `src/shared/db/seed.ts` with the founder's not-yet-supplied taxonomy — ships PROVISIONAL Distrito Capital + Maracaibo lists, flagged as such in the file, idempotent via `ON CONFLICT`
- [x] 2.4 RED: zone selector offers only the selected city's zones — **PR2b, 2026-08-16.** `components/molecules/CityZoneSelect.test.tsx` written first against a deliberately unfiltered implementation (`zonesForSelectedCity = zones`). Real assertion failure captured live: `AssertionError: expected '<form class="_form_fbe3f4" method="ge…' not to contain 'Chacao'` (3/3 tests failed for the same reason — the selected city's zones weren't excluding the other city's). Fixed with `zones.filter((zone) => zone.cityId === selectedCityId)`; reran, 3/3 passed
- [x] 2.5 GREEN: cascading city→zone select component (`components/molecules/CityZoneSelect.tsx`) — **PR2b, 2026-08-16.** **⚠️ ESTA CASILLA DESCRIBE UN COMPONENTE QUE YA NO EXISTE (anotado 2026-08-28, no corregido).** `components/molecules/CityZoneSelect.{tsx,module.css,test.tsx}` se borró el 2026-08-28 en `6d299f1` («se van los tres que no dibujaba nadie»), junto con `PageShell` y `FieldRow`: lo superó `SearchFilters`. La casilla es correcta como **historia** —se entregó y funcionó— y engañosa como **estado**, que es la diferencia que AGENTS.md §5 nombra. Lo que sí sobrevive de esta tarea es el átomo `Label`, que sigue en pie con otros consumidores. Decidir si la casilla se reescribe en pasado o si el requisito se declara cubierto por `SearchFilters`: no se hizo acá porque cambia el significado de una entrega, no una redacción Server-rendered `<form method="get">`, two native `<select>` elements, a visible `ActionButton type="submit"`. No `onChange` handler, no client component directive — changing the city resubmits as a GET and the server rebuilds the zone `<select>` already filtered (design.md D13). The filter lives inside the component (D5's "guarantees live in the narrowest API"): it takes every curated zone plus the selected city id and filters internally, so a caller cannot pass a mismatched pair. Ships a new standalone `Label` atom (`components/atoms/Label.tsx`, promotes 1b.5) — both selects carry a real associated `<label htmlFor>`, not a placeholder. Verified no client-side JS: `components/design-contract.test.tsx`'s existing "no shipped atom/molecule declares a client directive" scan covers both new files by construction (it globs `components/atoms` and `components/molecules`)

  > **`listing-catalogue` module added 2026-08-21, and the reason is an architecture violation rather than a feature.** `design.md` states it in one line: "the Next.js `app/` tree is a thin delivery adapter that only translates HTTP/RSC into a use case call — no business rule lives there." Three files were writing raw Drizzle against `citiesTable`/`zonesTable` — the SAME `select` copied into `app/page.tsx` and `app/publicar/page.tsx`, plus a third in `app/publicar/actions.ts`. The duplication was the symptom; the cause was a missing port, because `ZoneCataloguePort` only answers "the zones of one city" and the read path needs every city plus the selected city's zones. `CataloguePort` + `DrizzleCatalogue` now serve all three, and `grep` for `shared/db/schema` under `app/` and `components/` comes back empty.
  >
  > **Two rules moved out of the front, at the founder's instruction (2026-08-21: "nunca mas coloques una regla de negocio en el front nunca").** `resolveSelectedCity` was `params.city ?? cities[0]?.id` inside `app/page.tsx` — which city a visitor sees before choosing one is a product decision, and it was being taken by whatever `ORDER BY name` returned first. `zonesForCity` was `zones.filter(...)` inline in TWO components, which is how two screens start disagreeing. Both now live in `listing-catalogue/domain`, where the 90% floor reaches them.
  >
  > **A real defect fixed on the way, not a refactor:** `buildSearchCriteria` never verified the city exists — it puts the value straight into `WHERE city_id = $1` — so `?city=cualquier-cosa` rendered an empty results page for a catalogue full of listings, indistinguishable to the visitor from "no hay avisos". `resolveSelectedCity` refuses an uncurated id and falls back. Same class of fix as `listingIdFromSlug`'s null.
  >
  > **The zone cascade the founder asked for (2026-08-21).** The search sidebar rendered the whole taxonomy in an `<optgroup>` per city, so choosing Maracaibo still offered Chacao. It now receives only the selected city's zones. The founder chose the SERVER cascade over client JS explicitly, with the tradeoff stated: the list narrows on reload, not on click, because a client-side narrow ships a bundle to the cheap phones D13 exists for. `SearchFilters.test.tsx`'s "offers every zone grouped by city" test was DELETED — it had encoded the bug as a requirement.
  >
  > Two mutations checked: accepting an uncurated city id turned 2 tests red, and making `zonesForCity` return the whole taxonomy for an absent city (the generous failure mode) turned 1 red. **Gap closed the same day: `tests/integration/catalogue.test.ts`** (5 specs, real Postgres). It proves the seam rather than re-proving the pure rules: that `listCities`' `ORDER BY name` is real — the site's root city for every visitor who has not chosen depends on it, and no unit test can see it — and that the rows feed `resolveSelectedCity`/`zonesForCity` unchanged. **The `innerJoin` against `city` was REMOVED rather than tested**: `zone_city_id_city_id_fk` is `ON DELETE cascade` (drizzle/0001), so the orphan zone it guarded cannot exist, and a join on every page load to defend an unreachable state is cost with no buyer. A spec now asserts the cascade against the running database, so weakening the constraint fails loudly instead of silently needing the join back. **One false green caught by mutation, and worth recording**: the cities fixture originally inserted Alfa before Beta, so insertion order and alphabetical order were the same sequence and the ordering assertion passed with the `ORDER BY` deleted. Reversed the insert; the mutation now turns it red. Same class of defect as PR11.1's killed-heredoc false green — a gate that cannot fail reports the guarantee as held.

## Phase 3: Listing Publication Core (PR3)

**Split into slices, as this phase's High risk required.** PR3a `feat/publish-validation` (the pure rules, #23); PR3b `feat/upload-guard` (the byte-level upload guard, #24); later slices carry the `listing_photo` schema, the `sharp` derivatives, the form, and the R2 adapter.

Both shipped slices are pure — no database, no R2, no network — which is why they could be built and reviewed in parallel. The cost of that parallelism showed up here: they both edited this block and conflicted. Worth remembering before splitting bookkeeping across concurrent slices again.

- [x] 3.1 Schema: `listing` table, `publisher_type` NOT NULL no default, composite FK `(zone_id, city_id) → zone(id, city_id)` (D5) — shipped in #20
- [x] 3.2 RED: integration test — cross-city listing insert fails FK constraint — shipped in #20. Proven RED by dropping the constraint against live Postgres: the cross-city row inserted (`promise resolved ... instead of rejecting`), and re-adding the constraint then failed until the orphan row was deleted
- [x] 3.3 RED: unit test — publish rejected without `publisher_type`, no default applied — proven twice, at both layers that can enforce it: `not_null_violation` from Postgres (#20) and `publisherType.required` from the validator, which also asserts explicitly that a missing type never silently becomes `owner`
- [x] 3.4 RED: unit test — publish rejected without photo / missing min content (title, description, price, city, zone)
- [x] 3.5 GREEN: `PublishListingUseCase` validation (publisher_type, USD price, city/zone, min content) — the rules shipped in #23 as `validatePublishableListing`, kept separate because the spec's "Uniform Validation Across Every Entry Path" means the broker importer (Phase 9) must be held to exactly them. `publishListing` is the use case that calls it: session gate → zone catalogue → validation → photo pipeline → write. **What this layer decides is the ORDER, and the order is the substance.** Validation runs before any photo, because it is a pure function over values already in memory while each photo costs a network read and a `sharp` decode — spending that on a draft that was never publishable burns a serverless invocation to reach an answer already known. Photos decode **one at a time**: six concurrent decodes against a serverless function's fixed memory ceiling is how this route dies under load, and it would die on the largest uploads, the ones a publisher cares most about. The publisher id comes from the session and never from the request body. Three of those were mutation-checked rather than asserted — parallelising the loop, reading the publisher id from the request, and moving validation after the photos each turned exactly one test red. **`present()` is the backstop for the defect this project already shipped**: after the #31 hole, every persisted column is read through a function that throws by name if the validator ever stops covering it, instead of writing a NULL into a column that refuses one
- [x] 3.13 Drizzle adapters for `ListingRepositoryPort` and `ZoneCataloguePort`, plus the integration test that proves one transaction. `save()` takes the listing and its photos in one call: a listing with no photo row violates a publish rule, so separate `saveListing`/`savePhoto` methods would let a caller create exactly that state whenever the second failed. **Building it surfaced a real infrastructure decision that D2 had deferred: `neon-http` cannot do transactions at all** — `db.transaction()` throws `No transactions support in neon-http driver`. It offers `batch()`, which Neon runs as one transaction, **but `batch()` exists on no other driver, so the integration test would have exercised a path production never runs** — a test reporting on code nobody ships, which is worse than no test. So `src/shared/db/transactional-client.ts` adds a second client on `neon-serverless` (the escalation path `client.ts`'s own comment already named for exactly this trigger), used **only** for the publish write. **Reads deliberately stay on `neon-http`**: D2's latency argument is about the read path on Venezuelan connections, and HTTP with no connection setup wins there, while a publish is a rare human-initiated action where a WebSocket handshake is invisible. Two clients chosen by access pattern beats one compromised for both. The handle is injected, so the deployment's Neon client and the test's `node-postgres` client run byte-identical adapter code. **The atomicity proof is the file's reason to exist**: two photos claiming position 0 make Postgres refuse the SECOND statement, after the listing row is already inserted — the test asserts the listing count is unchanged, which an in-memory fake could never establish, because a fake rolls back for having been written to rather than because a database made it. **Still open and unfixed**: if photo processing fails partway through a multi-photo publish, earlier derivatives are already promoted and their originals deleted — orphans in the bucket with no listing row, and nothing sweeps them
- [x] 3.6 RED: non-image / oversized upload rejected (MIME + magic-byte + size) — `inspectUploadedPhoto`, pure over bytes, 17 specs. Reads the file's own header rather than trusting `Content-Type`, which is a claim the uploader makes about their own bytes and which nothing verifies. SVG is refused outright even when honestly declared: it executes script, `image/svg+xml` is a legitimate image MIME type, and a public bucket serving it is script execution on the origin. **Two numbers here are chosen, not inherited, and both are recorded as such:** the 10 MB ceiling (design.md states only "a phone photo is 3–8 MB") and the exclusion of HEIC, which is the **iPhone camera default** — iOS normally transcodes to JPEG through a file input, but that is not guaranteed, and the fix if publishers hit it is a client-side transcode, never loosening the guard. The `sharp`-based pixel-bomb bound ships with 3.11, where the decoder already exists
- [x] 3.7 GREEN: `sharp`-based upload guard before persistence; R2 presigned PUT adapter — the adapter shipped in #27, the guard-before-persistence half here as `processUploadedPhoto`. **This step exists because the bytes never pass through this application on their way to R2** — that is what a presigned PUT means — so it is the first and only moment anything of ours can look at them, and three guarantees have nowhere else to live: the object belongs to the publisher claiming it, the bytes are the image they claim to be, and the original is discarded (D12). **The ownership check is the load-bearing one and it is not obvious**: the object key travels to the browser, so it is not a secret, and without comparing the publisher id a caller could submit `incoming/<someone-else>/<token>` and have this function download, derive, promote and then DELETE another account's pending photo. It is a structural re-derivation of the key rather than a `startsWith`, because a prefix test accepts `incoming/<publisher>/../elsewhere`. Both of those were mutation-checked rather than assumed — replacing the comparison with `startsWith` and removing the final delete each turned exactly one test red. Derivation is injected as a port so all of it is provable without a bucket, a credential or `sharp`; the encoder's byte budgets stay proven where they already were, against real noise fixtures
- [x] 3.8 Schema: `listing_photo` table — two derivative keys, two measured byte sizes, a display `position`, and **no column for the original file**, because D12 discards it and a nullable `original_key` would be a standing invitation to stop discarding it. **No `alt_text` column either**: the search spec requires alternative text on every photo but does not require a publisher to type it, and asking someone filling this form one-handed on a phone to describe six photographs produces empty fields rather than accessible ones — so it is composed at render time from the listing's title, zone and this row's position. The weaker-text tradeoff is recorded rather than glossed. `ON DELETE CASCADE` and `UNIQUE(listing_id, position)` proven against real Postgres, the cascade **proven RED** by removing it (`update or delete on table "listing" violates foreign key constraint`), plus a sanity test that two different listings may each hold a position 0 — a global unique on `position` alone would have passed the first test and broken the product on its second listing
- [x] 3.9 Publish form UI — shipped as four slices. (a) `violation-copy.ts`, one Spanish sentence per violation code, a `Record` over the union so a new code stops the build until someone writes it, with set equality asserted in **both** directions against codes the validator actually produced. (b) `PublishForm` + `/publicar`, server-rendered with a native POST. (c) **Rebuilt against the artboard after the first version differed in nine ways while passing every test** — the form had been written from the handoff prose, which states the rules but never the layout, and no test in the suite could see layout. The field pattern moved into `components/molecules/Field.tsx` so the next screen starts from the rules rather than from a copy, three form rules moved into `design-contract.test.tsx` where they hold for screens that do not exist yet, and four Playwright measurements at 360 and 1280 now defend the geometry. (d) The Server Action: validates, returns the publisher their words with each error under its own field, and hands a valid draft to step 2 in a short-lived base64url cookie — **base64url because the size test measured a maximal Spanish draft at 7,781 bytes percent-encoded against a ~4 KB ceiling**, a failure that would otherwise have arrived in production as a form that silently empties itself. **Two fields were added to the design's list with the founder's decision**: `habitaciones` and `metros²`, absent from screen 3 yet NOT NULL in the schema and rendered on the result row
- [x] 3.14 Step 2 — photo upload, the one screen where client JS is allowed (SISTEMA.md: "el único lugar donde se permite JS, para comprimir en el dispositivo antes de subir"). Shipped as three slices. **(a)** `validateUploadRequest` decides what is worth signing before the R2 adapter or the network are involved, and **refuses rather than truncates** an over-count request, because silently signing the first six of ten publishes a listing missing photos the publisher believes they attached. **(b)** The uploader, and **the order is the design rather than an implementation detail: compress → measure → sign → upload.** The presigned PUT pins `ContentLength` into its signature, so the exact byte count has to be known BEFORE the signature is requested — compressing afterwards would invalidate every URL just issued. 1600px on the longest edge, chosen against the pipeline that consumes it: the detail derivative is 1280, so uploading below that would make `sharp` upscale, inventing pixels for no saved bytes; the relationship is asserted against `DETAIL_MAX_EDGE` itself so moving either value fails and says which. `computeResize` is pure and separately tested because it is the only part that can ship wrong unnoticed — including the portrait case a width-only rule would leave full size, and the panorama that would otherwise reach a zero-pixel side. **(c)** `publishFromDraft` joins it: `processUploadedPhoto` per key, then `publishListing` in one transaction. **The composition root is where every port choice is finally paid for** — reads on `neon-http`, writes on the transactional client, derivation adapted so the application layer never mentions `Buffer`. Built per request, not at module load, because `createR2PhotoStorage` throws on any missing variable and at module scope that would take down every route that merely imports the file. A rejected draft returns to step 1 with its violations; anything else propagates, because a publisher cannot fix an unreachable bucket by editing a field. **The draft cookie is cleared only after the write succeeded** — clearing earlier loses someone's words to a failure they had no part in
- [x] 3.15 Render `publisher_type` visibly on the listing detail and the result card **Conciliada 2026-08-27: la superficie que faltaba existe desde hace tiempo y el plan seguía diciendo que no.** `PublisherBadge` se importa y se dibuja en los tres lugares que esta tarea pedía: la ficha (`app/alquiler/[ciudad]/[zona]/[slug]/page.tsx:329`), la tarjeta de la cuadrícula (`components/molecules/ListingCard.tsx:65`) y la fila (`components/molecules/ResultRow.tsx:58`). La prueba de que sobrevive en escala de grises —relleno contra borde, que es lo que la 14.25 conserva— sigue viva en `design-contract.test.tsx`. Lo que la tarea llamaba «lo que falta es la superficie» dejó de faltar con la ficha. — split out of 3.9 on 2026-08-18 because it needs the detail page, which does not exist. The badge atom and its greyscale-survival proof already shipped in PR1b-b; what is missing is the surface. ~~**Blocked on a design gap of the same shape as 3.9's**: artboard `2b` draws `2 HAB · 2 BAÑOS · 78 M² · 1 PUESTO`, and `baños` and `puesto` have no schema columns at all~~ — **gap closed 2026-08-20**: `listing.bathrooms` (NOT NULL) and `listing.parking_spots` (NOT NULL DEFAULT 0) shipped with the publish rules, form fields and copy. The founder chose the asymmetry: baños required because a blank cell beside three numbers reads as broken, puesto defaulted to 0 because zero parking is a fact and nobody should type a zero to publish an anexo. What still blocks 3.15 is only the detail page itself, which does not exist
- [x] 3.18 Publisher contact — **the design draws it and never asks for it.** Artboard 2b renders "Ver WhatsApp del dueño" and "El contacto se muestra a usuarios registrados", and no form in the system collected a value: the same shape of gap as `habitaciones`/`metros²` before it, found while wiring the reveal use case. **It is a METHOD plus a VALUE, not a phone number** (founder, 2026-08-18): "el valor que quiera mostrar la persona. Sea email, WhatsApp o número de teléfono." A column named `whatsapp` would have forced everyone who prefers email to lie in it, and **the reveal button's label must come from the method** — "Ver WhatsApp del dueño" is a promise the product does not keep for someone who chose email. **Lives on the account as a default and on the listing as the effective value, copied at publish time**: editing the account later must not rewrite adverts somebody has already seen, because a tenant who wrote to a number needs that advert to keep saying the number they wrote to. Validation is **shape, never verification** — nothing proves the line rings, phone verification is a disabled port (D9), and what the checks buy is the typo a publisher can fix while still on the form, which is the only moment it is cheap. Venezuelan numbers accept the separators people actually type. **The migration was hand-edited after generation**: `drizzle-kit` emitted `ADD COLUMN … NOT NULL` with no default, which Postgres refuses on a populated table (verified against 15 real rows, not assumed), so it adds nullable, backfills, then tightens — and the backfill writes a **visibly unusable** `sin-contacto` rather than a plausible number, because inventing one would hand a tenant a contact that goes nowhere. **Owed a design: `/mi-cuenta`**, where the account default would be edited; it blocks nothing, since a publisher with no default fills the field on the form
- [ ] 3.17 E2E: upload a real file to the real bucket. **REINTENTADA 2026-08-28 contra el arnés de la 11.22 y SIGUE ABIERTA: el arnés no ayuda acá, y decirlo es más honesto que escribir una especificación que se salta.** La 11.22 cambió el transporte de la BASE DE DATOS —`neonConfig.fetchEndpoint` apuntando a un proxy `pg` local— y no toca R2 en nada. Un origen S3 falso local tampoco sirve: mediría la política CORS del falso, que es exactamente el argumento con el que la 3.13 descartó probar contra `batch()`, «una prueba que informa sobre código que nadie despacha». **Lo que tendría que ser cierto, contado y no estimado:** (1) las credenciales de R2 como secretos del repositorio —hoy el job de e2e las omite A PROPÓSITO y su propio comentario lo dice, porque sus rutas fallan cerradas sin ellas y eso es lo que sus especificaciones miden—; (2) una sesión autenticada en el navegador, que la suite no sabe fabricar: `publish-access.spec.ts` prueba la NEGATIVA, y entrar exigiría un proveedor de credenciales de prueba o una cookie de sesión firmada con `AUTH_SECRET`; (3) la cookie de borrador del paso 1 para llegar al paso 2; (4) un barrendero para los objetos reales que cada corrida dejaría en el bucket — la 3.13 ya registra que los derivados huérfanos no los limpia nadie.

  **Y una corrección al texto de esta tarea, que es el hallazgo que importa: aun con los cuatro puntos, esta prueba NO habría atrapado el defecto del 2026-08-18.** La política que `docs/going-live.md` transcribe ya permite `http://localhost:3000` y `https://*.vercel.app` — o sea los dos orígenes desde los que puede correr CI. El defecto real fue que **el origen de PRODUCCIÓN** no estaba en la lista, y un navegador que sube desde un origen permitido contesta verde mientras ese hueco sigue abierto. Es la trampa número uno de este cambio: una prueba en verde bajo la mutación exacta que existía para atrapar. Lo que sí lo atraparía es afirmar la CONFIGURACIÓN del bucket (`GetBucketCors`) contra el origen de producción — sin navegador, sin sesión y sin subir nada. Queda como **3.19**
- [ ] 3.19 **(nueva, 2026-08-28 — sale de reintentar la 3.17.)** Afirmar la política CORS del bucket, no la subida: `GetBucketCors` sobre el bucket de producción y `expect(AllowedOrigins).toContain(<origen de producción>)`, más `AllowedMethods: ["PUT"]` y `AllowedHeaders: ["content-type"]`, que es lo que la firma presignada obliga a que el `OPTIONS` acepte. **Es la afirmación que el 2026-08-18 habría puesto en rojo y que un navegador corriendo desde un origen ya permitido no puede hacer.** No se construye todavía porque necesita las credenciales de R2 en CI, y shippearla sin ellas sería exactamente el defecto que la 8.9 acaba de barrer: código que no ejecuta nadie. Va con el trabajo de despliegue, junto a la 3.17 y `docs/going-live.md` **The only test that can catch a CORS failure**, and the reason it is its own task: photo upload shipped with 42 green specs across four files and was impossible in production, because the browser blocked every PUT before it left. `upload-request` and `compress` are pure, `r2-photo-storage` injects its S3 client, `process-uploaded-photo` uses fakes — **not one of them makes a real request**, and none of them can, because CORS is a browser policy that does not exist server-side. Needs the R2 credentials in CI and a session, so it belongs with the deploy work rather than before it. `docs/going-live.md` carries the configuration this depends on
- [x] 3.16 Design update of 2026-08-18, imported from the founder's Claude Design project (`Rentas - Sistema.dc.html`, artboard `2g`). **The colour half has shipped**: `--warn` and `--warn-bg` repainted from the palette itself — the mustard pair was the only colour on the publish screen belonging to no theme — plus `--meta`, a new token for monospaced metadata (counts, file sizes, step labels) declared separately from `--disp` because they share a value under `compacto` and mean different things. **`oscuro` was redrawn from menta's own palette**, `--surface` being menta's accent navy and `--accent` its mint, replacing an amber theme that shared nothing with it; the theme-contract gate needed narrowing for `--r`/`--rs`, which the new dark theme deliberately keeps identical because a radius is geometry rather than colour, and the narrowed rule was proven to still catch a real colour leak. **Artboard `2g` shipped too**, and it was a different screen from the one that had shipped: per-photo rows carrying a thumbnail, the file name and its compression result (`2,4 MB → 38 KB`), a `Portada` badge on the first, a per-photo remove button, per-photo states for compressing and for refusal (`✱ Es un video, no una foto`), a `4 de 6` counter, `+ Agregar más`, drag-to-reorder, an empty state, and the trust block that says publishing photos taken from another advert costs the account. **The design says "Hasta 8 fotos" and the founder chose to keep 6** (2026-08-18): eight would cost about a quarter of the free tier's catalogue capacity — ~5,900 listings against ~7,900 — measured against the stored derivatives rather than the discarded originals. The screen's copy therefore reads 6, and reads it **from `MAX_PHOTOS_PER_LISTING`** rather than repeating a number — a copy that states a limit it does not share with the validator is a copy that goes stale. Two tokens were added for it: `--meta-fs-sm` (11px, the step label and the counter) and `--r-thumb` (6px, because a 12px round on a 64×48 image eats the image). **The corner-radius gate needed fixing to accept it**: `border-radius: 0 var(--r-thumb) 0 var(--r-thumb)` was reported as a literal because the check read the whole value rather than each part, and the repaired check now also catches a literal hiding among tokens (`0 6px 0 var(--r)`), which the old one missed entirely

### A second forecast correction, measured 2026-08-17

The Phase 1b note above revised estimates upward once, for proof. **Phase 3's UI slices show the same error again in a different place: a form is not one unit of work per field.** `PublishForm` is 528 authored lines for eight fields — 241 component, 136 proof, 91 styles, 60 route — and that is *after* extracting a shared `Field` renderer that removed 36 lines of duplication. Nothing in it is padding: every field carries a real `<label>`, help text, a per-field error region, `aria-invalid`, `aria-describedby`, and the submitted value round-tripping so a phone user does not retype what they already wrote.

The lesson is not "grant another exception". It is that **a screen with N form fields costs roughly 55–65 authored lines per field including proof and styles**, so any remaining form surface must be forecast from its field count and split before it is built — never estimated as one slice and then rescued with an exception. Applied below to PR9's import preview and PR7's renewal surfaces.
- [x] 3.10 RED: after upload, only derivatives are persisted — the original file is not retained (D12) — `deriveListingPhoto` returns exactly two members and the source is not one of them. **This is the half this layer can enforce, not all of D12**: the R2 adapter (3.7) must still refuse to PUT the buffer it was handed, and that is a separate guarantee
- [x] 3.11 GREEN: `sharp` emits a row thumbnail at 128 × 96 (≤ 10 KB — covers the 44 × 34 mobile and 64 × 48 desktop row at 2× under `compacto`, D12/D14) and a detail image (≤ 200 KB) at upload; both stored in R2; platform on-demand image optimization is not used — WebP for both. **The byte budget is met by construction, not asserted afterwards**: quality steps down, and when quality alone cannot reach it the longest edge steps down too (1280 → 1024 → 800). That ladder exists because the first implementation threw instead, and the noise fixtures proved that was reachable — a maximally-detailed 1280-edge image lands at ~209 KB even at quality 40. Refusing a publisher's upload because their photograph carried too much detail is a broken product. Measured against incompressible noise: thumbnail **0.1–0.5 KB** (budget 10 KB), detail **167–190 KB** (budget 200 KB). **Recorded rather than quietly accepted: the thumbnail budget has ~20× headroom and therefore cannot fail — it is not a real constraint.** Storing the bytes in R2 belongs to 3.7
- [x] 3.12 RED: derivative dimensions and byte budgets hold for a portrait, a landscape, and an oversized source photo — plus the pixel-bomb bound promised in 3.6, landed here because it needs the decoder (40 MP; a 12 MP phone photo is 4032 × 3024). **The fixtures are deterministic random noise, and that is the load-bearing choice**: a solid-colour test image compresses to almost nothing, so a byte budget asserted against one passes however badly the encoder is configured — a gate that cannot fail, which is the failure this project has already shipped five times. A measured curiosity worth keeping: the 12 MP source fits at 1280px while a 1600 × 900 source falls back to 1024, because downscaling further averages the noise away and yields a *more* compressible image

## Phase 4: Trust — Photo-Hash Dedup (PR4)

**The pure half was pulled forward**, split across two slices — `trust/perceptual-hash-core` (dHash + Hamming, merged as #14) and `trust/perceptual-hash-adapter` (the port, the sharp boundary and the calibration harness). The reason for pulling it forward is the **uncalibrated `<= 8` threshold** (see Open Questions): the harness has to exist before real photographs can decide the number, and no amount of testing substitutes for that data. Tasks 4.1, 4.6, 4.7 and the `PublishListingUseCase` wiring all need `listing`, which is PR3.

- [x] 4.1 Schema: `listing_photo_hash` (`bit(64)`) — **`bit(64)`, not `bigint` and not `text`.** The similarity query is `bit_count(hash # $1) <= $2`, Postgres's own population count over an XOR, and that operator pair exists only for bit strings: storing this as a number would force every comparison into application code — the sequential-scan-in-TypeScript D4 exists to avoid — and storing it as text would make the same query a lie that happens to parse. Drizzle has no first-class `bit`, so the type is declared with `customType` rather than approximated with one that ships. `photo_id` is the primary key, because a photo with two hashes makes "is this a duplicate" depend on which row a scan reached first. **There is deliberately no `publisher_id` column**, even though the duplicate query filters on it: it is reachable by joining `listing_photo` to `listing`, and a copy here would be a second source of truth for who owns a listing — the exact fact D4's same-publisher exemption rests on. A denormalised copy that drifts turns "this is your own photo, republish freely" into a false accusation of duplication, which is the worst failure this feature has. **4.6's drift cross-check ships with the schema rather than after it**: the domain's own `KNOWN_HAMMING_DISTANCE_VECTORS` are replayed through a real `SELECT bit_count($1::bit(64) # $2::bit(64))` and asserted three ways — the declared value, what TypeScript computes, what Postgres computes
- [x] 4.2 RED: cross-publisher perceptually-matching photo rejects the listing. **Proved against `PhotoHashPort` with an in-memory fake, not the real adapter** (`application/ports/photo-hash.port.test.ts`) — the database behaviour is not proven until 4.6
- [x] 4.3 RED: same-publisher match (active/expired/other listing) is allowed — same fake, same caveat
- [x] 4.4 GREEN (hash computation only): `sharp` 9×8 grayscale dHash(64) — pure algorithm in `domain/dhash.ts`, sharp confined to `infrastructure/sharp-dhash.ts`. Wiring into `PublishListingUseCase` needs `listing` (PR3)
- [x] 4.5 GREEN: `PhotoHashPort` exposing only `findMatchesFromOtherPublishers(hash, excludePublisherId, maxDistance)` — no all-matches method (D4)
- [x] 4.6 GREEN: Drizzle/raw-SQL adapter using `bit_count` Hamming distance. `bit_count(a # b)` is Postgres's own population count over an XOR, so the scan runs where the rows are instead of pulling every hash into a serverless function to loop over. **The same-publisher exemption is not a filter this adapter remembers to apply**: the port exposes one query and `excludePublisherId` is required on it, so the exclusion reaches the SQL by construction — an owner republishing their own property after expiry passes by the shape of the API, not by anyone's discipline. `publisher_id` is **joined, never copied**, because a copy would be a second source of truth for who owns a listing and a drifted copy turns "this is your own photo" into a false accusation. **The drift cross-check landed with 4.1**: the domain's `KNOWN_HAMMING_DISTANCE_VECTORS` replayed through real `bit_count`, asserted three ways. The integration test proves what an in-memory fake structurally cannot — the fake filters because it was written to, this proves the SQL does — with Ana's expired listing deliberately holding the SAME hash as Bruno's, so a missing or misapplied exclusion returns two rows instead of one
- [x] 4.7 E2E: publish → duplicate photo rejected cross-account, accepted same publisher. **The task text understated the work, and this correction records why.** By the time this ran, PR3 had long shipped and 4.5/4.6 were both green, but grep across `src/` still found zero callers of `PhotoHashPort.findMatchesFromOtherPublishers` and zero writers of `listing_photo_hash` outside its own adapter test (`process-uploaded-photo.test.ts`, `attach-photo-to-draft.ts`'s own "known gap" docstring, and 9.20-9.23's own note). "E2E" alone would have proven a check that could structurally never fire, against a table nothing had ever written to. Actually closing the gap needed three things this line never named: (1) `PhotoHashPort.record(photoId, hash, recordedAt)` — the write half added beside the existing read method, deliberately NOT `publisherId`-bearing, since `listing_photo_hash` has no such column (schema.ts) and a copy would drift; (2) the hash computed and the cross-account check run inside `processUploadedPhoto` — the ONE choke point `publishListing` and `attachPhotoToDraft` both already call — fail-closed, so a photo whose hash cannot be computed is refused outright; (3) the actual `record()` call, placed in each caller AFTER its own listing/photo row exists, because `listing_photo_hash.photo_id` is a primary key referencing `listing_photo.id` — recording earlier is not just premature, it would risk poisoning the table against a publisher whose submission was then rejected for an unrelated reason. `ListingRepositoryPort.save` and `ListingPhotoAttachmentPort.attachPhoto` both widened their return types to hand back the real persisted photo id(s), in submission order, for exactly this reason. The Hamming threshold landed as `MAX_DUPLICATE_HAMMING_DISTANCE = 8` in the domain (hamming-distance.ts) — the design.md Open Question's own proposed, uncalibrated value, deliberately NOT marked resolved. Proven against real Postgres in `tests/integration/photo-duplicate-rejection.test.ts`: a photo matching another publisher's is rejected; the same publisher's own photo — on an active OR an expired listing — is accepted; a photo matching nothing is accepted and its hash is actually recorded (the one assertion that would have silently passed as false before this task, since the table was permanently empty)

## Phase 5: Listing Search (PR5)

- [x] 5.0 RED: a submitted `zone` that does not belong to the submitted `city` is ignored, not treated as a meaningful filter. **Found while building the cascading select in PR2b:** a GET form submits whatever its controls currently hold, so picking a new city without touching the zone sends the previous city's zone. Nothing is written, so D5's database constraint is not involved and cannot help — this one has to be handled where the query parameters are read, and the component cannot prevent it because it does not control what the browser posts — **the subtle one.** A GET form posts whatever its controls hold, so a stale city/zone pair reaches the server whenever someone changes the city without touching the zone. The zone is dropped by comparing it against the SUBMITTED city, not against the caller's list. **Open and unbuilt**: the drop is silent, so the 5.7 UI has no way to say "mostrando toda la ciudad" — a `droppedZone` flag is the obvious fix, left for whoever builds the reader
- [x] 5.1 RED: `ListingSearchPort.search(criteria)` — missing/nullable `cityId` rejected (D5)
- [x] 5.2 GREEN: search port signature with required non-nullable `cityId` — `search(criteria)` is the only method; there is no `searchAll` and no sentinel. **Stated at its real strength**: this holds while the interface has one method and nothing stops an adapter from ignoring `cityId` once inside, which is why the integration test exists. Mutation-checked: making `cityId` optional fails `tsc` with three unused-`@ts-expect-error` errors, so CI enforces it
- [x] 5.3 RED: integration — Maracaibo search excludes Distrito Capital listings (no-filter, wide-price-range, colliding-zone-name scenarios)
- [x] 5.4 GREEN: Drizzle search query — city/zone/price/characteristics filters — `city_id` and `status = 'active'` are appended before any caller-supplied filter, and `listing_city_status_idx` is `(city_id, status)`, so the pair every query starts with is also its access path
- [x] 5.5 RED: expired and auto-hidden listings excluded from search
- [x] 5.6 GREEN: active-only status filter — mutation-checked at the SQL level: deleting the predicate removes `status` from the generated `WHERE`
- [x] 5.7 Search results UI showing `publisher_type` per result — artboard 2a, and **it is one control set rendered two ways rather than two components**: a row of chips on a phone, a 240px sidebar at 1280, both the same `<form method="get">`. The GET is the whole mechanism (D11/D13): every change reloads with the state in the URL, which makes a filtered search **linkable, shareable and indexable** — and that matters more than usual here because listings circulate by WhatsApp, so a filter nobody can paste into a chat is a filter that does not travel. **The submit button is visible and required**: there is no `onChange` to auto-submit, and hiding it would leave the form unusable for exactly the visitors the no-JS rule exists for. Nothing is preselected, because a default city would silently answer D5's question for the visitor and they would never learn the catalogue has two cities. The last room step reads **`4+`, never a bare `4`** — a segmented control whose last step quietly meant "exactly four" would hide every larger apartment from the people most likely to want one. An empty result says what to change rather than leaving a blank column that reads as broken, and no city at all asks for one instead of showing a list that quietly crosses cities. Measured at both widths: every control is a real 44px target at 360, and the filter column never overflows — four room chips plus two price inputs in a row is the likeliest way that breaks, and a sideways-scrolling filter panel is one nobody finishes. **The results ARE the root** (founder, 2026-08-19): there is no separate home page, and the design supports that by omission — it has no home artboard and never mentions one. A landing page that only links onward spends the domain's strongest URL on a click, and in a classifieds product that URL belongs to the listings. `/alquiler/<ciudad>/<zona>/<slug>-<id>` stays the listing scheme. **Recorded as a cost, not discovered later**: there is now nowhere for a value proposition or a publish pitch beyond the bar's button. And an addition to the design, stated as one: artboard 2a gives this screen no heading at all, which is fine for a results page reached from elsewhere and wrong for a site root, so a visually hidden `<h1>` carries the document outline while the screen looks exactly as drawn

## Phase 6: Contact Reveal (PR6)

- [x] 6.1 Migration: `contact_reveal_event` table + `contact_reveal_unique_pair` VIEW (raw SQL, D6) — the table is append-only and **all four foreign keys are `ON DELETE restrict`, deliberately**. Cascading would delete the go/pivot evidence at exactly the moment a listing is taken down or an account closed. The cost is real and is the point: an account-erasure request now fails loudly until someone decides between anonymising and dropping these rows, which is design.md's own open question made visible instead of silently resolved the wrong way
- [x] 6.2 Hand-declare TS result type for the view query (flag as drift risk in code comment) — **the drift risk, recorded above `UniquePairRow`**: `drizzle-kit` neither creates nor diffs a view, and `db.execute` returns untyped rows, so the TS type is a hand declaration the compiler cannot check. Drift does not crash — a renamed column arrives as `undefined`, `Number(undefined)` is `NaN` — so the failure mode is a go/pivot report reading `NaN` unique pairs. The integration test is the only thing pinning it. `pgView` was considered and rejected: `DISTINCT ON` plus a window `count(*)` still needs an `.as(sql\`…\`)` escape hatch, which moves the unchecked string rather than removing it
- [x] 6.3 RED: anonymous visitor sees hidden/locked placeholder, no contact value
- [x] 6.4 RED: reveal creates exactly one event; repeat reveal by same tenant creates a second, non-deduplicated event
- [x] 6.5 GREEN: `RevealContactUseCase` — single insert, session-gated — session read **before** the listing is touched, mutation-checked: moving the gate after the read turns exactly that ordering test red
- [x] 6.6 RED: integration — after N repeat reveals of one pair, unique-pair view returns 1 row, `reveal_count=N`, `first_revealed_at`=earliest; raw event table still holds N rows — proven against real Postgres 18. **Verified independently rather than on report**: recreating the view without `DISTINCT ON` turns four tests red, including the one whose assertion the agent had itself tightened from `toBeGreaterThanOrEqual` to `toBeGreaterThan` after discovering the loose form passed against the broken view
- [x] 6.7 GREEN: view query wrapper
- [x] 6.8 Reveal UI: locked placeholder, reveal button, sign-in redirect. **Hecho 2026-08-22, PR #81.** `ContactBlock` con sus tres estados, `reveal-actions.ts` como acción de servidor (no un enlace: hay que registrar el evento que cuenta la métrica norte), `DrizzleRevealableListing`, y `view-listing-contact.ts` para el lado de lectura. El destino de vuelta pasó a ser una regla del dominio (`identity/domain/safe-return-destination.ts`) al descubrir que la comparación de prefijo dejaba la acción como un redirector abierto

  > **Correction landed 2026-08-21, ahead of 6.8.** The contact-reveal module modelled the publisher contact as a single `whatsapp: string`, written when no column stored one. Two columns ship now — `listing.contact_method` and `listing.contact_value`, both NOT NULL, copied at publish time — and `publishable-listing.ts` had already recorded the rule they exist for: "the reveal button's label comes from this, so a listing that says 'Ver WhatsApp' while holding an address is a promise the product does not keep." The method never reached the presentation layer, so 6.8's button would have said "Ver WhatsApp del dueño" over an email address for every publisher who chose one. Fixed in the domain rather than in the component, because the component is being redesigned and the rule is not: `ContactPresentation` now carries `method` in BOTH branches and `value` in the revealed branch only — **the method survives the lock, the value cannot be represented inside it**, which is the guarantee 6.3 was written for and it is unchanged. `contactChannelNoun` returns the channel noun alone ("WhatsApp" / "teléfono" / "email") so the sentence around it stays in the UI layer. Two mutations checked: making the locked branch carry the value turned 4 tests red including the `not.toContain` leak assertions, and hard-coding the noun to "WhatsApp" turned 1 red. **Open for the founder, four words wide**: SISTEMA.md writes the label as "Ver WhatsApp del dueño" and gives no wording for a broker or for the other two channels — the noun is derived, the rest of the sentence is not yet decided
- [x] 6.9 RED: an account holding **40 distinct listings inside a rolling 24-hour window** is refused the 41st, and the refusal writes no reveal event and discloses no contact value — the catalog must not be drainable by one registered account. **The unit is the listing, not the action**: a second reveal of a listing already counted inside the window must leave the allowance untouched, so the test that matters is the one where a tenant re-opens the same advert and is not charged for it. **Done 2026-08-24.** `evaluateRevealAllowance` unit RED confirmed missing module, then `revealContact` use-case RED confirmed (rate limit not enforced) before any implementation existed — see `reveal-rate-limit.test.ts` and the rate-limit cases in `reveal-contact.test.ts`
- [x] 6.10 GREEN: per-account reveal rate limit in the domain — **40 distinct listings / rolling 24h**, resolved by the founder 2026-08-24 (design.md Open Questions). The window rolls, it does not reset at midnight: an account refused at 18:00 recovers allowance as its oldest reveals age past 24 hours, never in a nightly batch. The rule lives in `contact-reveal/domain`, never in the component. **Done.** `reveal-rate-limit.ts` (`evaluateRevealAllowance`, pure — no `Date` at all, the rolling window lives in the new `RevealRateLimitPort` read query) wired into `revealContact`; `DrizzleContactRevealEvents` now also implements `RevealRateLimitPort` (`findRecentlyRevealedListingIds`) against the same table, no second repository. Integration-tested against real Postgres in `tests/integration/contact-reveal.test.ts`
- [x] 6.11 Migration: `contact_reveal_event.message` as **nullable `text`, plus a `NOT VALID` CHECK constraint** — not `NOT NULL DEFAULT ''`. Rows already exist from PR #81, and the two obvious migrations are both wrong here. A plain `NOT NULL` fails outright against them; `NOT NULL DEFAULT ''` succeeds by **writing a fact that never happened** into an append-only log, recording that those tenants submitted an empty message when the product had not yet asked them for one. This table is the go/pivot evidence and 6.1 already refused `ON DELETE cascade` for the same reason: history is not edited to make a schema convenient. **Done.** `drizzle/0012_icy_spectrum.sql` — the column comes from `drizzle-kit generate`; the `NOT VALID` CHECK is hand-appended (drizzle-kit does not emit `NOT VALID`) with the exact SQL below, plus the "never VALIDATE this" warning written into the migration file itself. Integration-tested: a `NULL`-message row (simulating a PR #81 row) still inserts; a blank/whitespace message is refused by Postgres

  ```sql
  ALTER TABLE contact_reveal_event ADD COLUMN message text;
  ALTER TABLE contact_reveal_event
    ADD CONSTRAINT contact_reveal_event_message_present
    CHECK (message IS NOT NULL AND length(btrim(message)) > 0) NOT VALID;
  ```

  **`NOT VALID` is the whole point and it is permanent, not a to-do.** Postgres skips the check for rows that already exist and enforces it on every insert and update from that moment on — which is exactly "the rule starts now". The constraint must never be `VALIDATE`d: validating it would fail against the historical rows, and passing would mean someone backfilled them. Record that in the migration itself, or the next person reads `NOT VALID` as unfinished work and finishes it.

  `NULL` therefore means one thing only: **this reveal predates the requirement.** It is not an empty message and cannot be confused with one, because `btrim` rejects blank and whitespace-only text at the database level, under the constraint and independently of the domain rule in 6.13. Every read of `message` handles `NULL` as the pre-rule case — including the abuse signal, which must not read a historical row as a suspicious blank submission. `contact_reveal_unique_pair` stays untouched: the message is carried, never aggregated
- [x] 6.12 RED: a reveal submitted without a message is refused — **no event recorded and no contact value returned**. Both halves matter: an event written for a refused reveal would inflate the north-star metric with contacts nobody made. **Done.** `reveal-message.test.ts` RED confirmed (module missing) before `requireRevealMessage` existed; `reveal-contact.test.ts`'s `it.each(["", "   ", undefined])` case RED-confirmed the use case still disclosed the contact before the wiring landed
- [x] 6.13 GREEN: the message is required, validated and stored on the reveal event by `revealContact`. **Stored at reveal time on purpose** — `wa.me` prefilled text is editable by the visitor and no messaging application reports back what was actually sent, so our row is the evidence and the prefill is only convenience. Trusting WhatsApp here would leave a decorative field with neither protection nor abuse signal. **Done.** `reveal-message.ts` (`requireRevealMessage`) checked via `btrim`-equivalent (matches the DB constraint's rule exactly) but returns the ORIGINAL untrimmed text — the spec calls it "the authoritative record of what the tenant wrote", so validation decides blank-or-not without rewriting what was submitted. Wired into `revealContact` right after the session gate, before the listing is touched (pure check, no I/O)
- [x] 6.14 GREEN: `contactActionHref` carries the tenant's own message instead of the fixed sentence hard-coded at `ContactBlock.tsx:181`. The domain already builds `https://wa.me/<intl>?text=<encoded>` and already normalises Venezuelan numbers (`0412…` → `58412…`), so this is a change of argument, not of mechanism. **Done end-to-end, closed 2026-08-24.** `ContactPresentation`'s revealed branch and `ContactViewer` carry an optional `message`; `presentContact` passes it through. The read-back gap flagged in the first pass is now closed: `view-listing-contact.ts` reads the tenant's message back via a new `RevealMessageHistoryPort.findLatestMessage(tenantUserId, listingId)` — kept alongside `RevealRateLimitPort` rather than weakening the write-only `ContactRevealEventPort`, implemented by `DrizzleContactRevealEvents` (same table, same class as the rate-limit read) with `ORDER BY revealed_at DESC, id DESC LIMIT 1`, because repeat reveals are never deduplicated (task 6.4) and the LATEST one is the one the tenant meant. Wired into `page.tsx` alongside the existing `reveals`/`listings` ports, fetched in parallel with the listing lookup — one extra read, paid only once `hasRevealed` is already known true. `NULL` (a reveal predating the migration) round-trips as `null` without crashing; `ContactBlock`'s existing `contact.message ?? defaultRevealMessage(listingTitle)` fallback still covers that case and the anonymous/never-revealed case. **Verified the locked branch leaks neither the key nor the read**: `ContactPresentation`'s `locked`/`expired` variants have no `message` field at the type level, and `view-listing-contact.test.ts` asserts `messages.findLatestMessage` is never called for an anonymous visitor or a signed-in visitor who has not revealed. Integration-tested against real Postgres: latest-of-two-messages wins, never-revealed returns `null`, and a historical `NULL` row (reproduced the same DROP/INSERT/ADD-CONSTRAINT-NOT-VALID way as the 6.11 constraint tests) returns `null` without error
- [x] 6.15 Reveal UI: message textarea **inside the `<form action={revealAction}>` that already exists** — it is a native POST that works without JavaScript, so the message needs no new endpoint, no client state and no new plumbing. The contact stays an `<ActionLink>` anchor rather than a scripted `window.open`, which is what makes the handoff survive a mobile popup blocker. **Done.** Reused the existing `Field`/textarea molecule (`components/molecules/Field.tsx`) inside the locked-state form, pre-filled with a sensible default the tenant can edit, `required` for the no-JS browser-level guard; `revealListingContact` reads `formData.get("message")` and passes it to `revealContact`, catching `MissingRevealMessageError`/`RevealRateLimitExceededError` the same way it already caught `ListingNotRevealableError` — a refusal re-renders the still-locked ficha instead of a broken error page

## Phase 7: Lifecycle — Expiry & Reminder Job (PR7)

- [x] 7.1 Schema: `listing.status`/`expires_at`; `listing_reminder` UNIQUE; `job_run`. **Reconciled 2026-08-24 — built, plan was stale.** All three live in `src/shared/db/schema.ts`. **Deviation from this task's own text, and it is a correction rather than a defect:** the unique key is `listing_reminder_cycle_unique` on **`(listing_id, kind, expires_at)`** — three columns, not two. `kind` had to enter the key because one cycle sends TWO distinct notices (expiry and photo purge); with the two-column key the purge notice would collide with the expiry notice of the same cycle and silently never send. Proven by `tests/integration/lifecycle.test.ts` — "la segunda reserva del mismo correo pierde" and "el correo de purga del mismo ciclo se reserva aparte"
- [x] 7.2 RED: listing expires 30 days after publish/last renewal, whichever later. **Reconciled 2026-08-24.** `domain/expiry.test.ts`: "vence 30 días después de publicar cuando nunca se renovó", "cuenta desde la renovación cuando es posterior a la publicación", "ignora una renovación anterior a la publicación" — the third is the one that pins *whichever later*
- [x] 7.3 GREEN: expiry calculation in domain. **Reconciled 2026-08-24.** `src/modules/listing-lifecycle/domain/expiry.ts`, with the listing's lifetime declared in one place ("declara la vida del aviso en un solo lugar") and the boundary pinned on both sides: not expired at the exact instant, expired one millisecond later
- [x] 7.4 RED: unauthenticated `POST /api/jobs/expiry-reminders` returns 401, `reminders_sent=0`. **Reconciled 2026-08-24.** `app/api/jobs/expiry-reminders/route.test.ts` covers three refusals, not one: no credential, wrong bearer, and **no `CRON_SECRET` on the server** — the last is the deploy that forgets the variable and would otherwise leave the route open to the world
- [x] 7.5 GREEN: constant-time Bearer `CRON_SECRET` check on job route. **Reconciled 2026-08-24.** `domain/cron-authorization.ts` — **in the domain, not the route**, so the second job route calls the same check instead of copy-pasting a `===` into it. Compares `timingSafeEqual` over the HMAC of each side, so the comparison never depends on the presented length, and **fails closed** when the server secret is missing. `app/api/jobs/photo-purge/route.ts` is the second caller that decision was made for
- [x] 7.6 RED: double-run of reminder job does not double-send (unique constraint). **Reconciled 2026-08-24.** `send-lifecycle-notices.test.ts` "una segunda corrida no vuelve a mandar el mismo correo" at the use-case level, and `tests/integration/lifecycle.test.ts` proves it against real Postgres — the constraint, not the application check, is what holds
- [x] 7.7 GREEN: `SendExpiryRemindersUseCase` — batched window select, insert `listing_reminder`, send email, record `job_run` (counts + failures). **Reconciled 2026-08-24**, shipped as `application/send-lifecycle-notices.ts`. Eight covering tests: the reservation is taken before the send and **returned on failure so the row can be retried** ("una falla del correo devuelve la reserva y no detiene los demás"), a publisher with no address counts as a failure rather than vanishing, listings are marked expired **before** deciding who to notify, and the run is recorded in `job_run` even when there was nothing to do. **Caveat, and it is the same one that blocks 7.11:** the only `LifecycleMailerPort` adapter is `console-lifecycle-mailer.ts`. The use case is complete and tested against a fake; nothing has ever left the machine
- [x] 7.8 RED: replayed renewal token rejected; GET on renewal link never mutates `expires_at`. **Reconciled 2026-08-24.** `renew-listing.test.ts` "rechaza el mismo token usado por segunda vez" and "muestra la confirmación sin mover nada"; an invalid token "no llega ni a leer el aviso", so a forged link costs no database read
- [x] 7.9 GREEN: HMAC-signed, listing-scoped, single-use, expiring token; GET renders confirmation, POST renews +30 days and burns token. **Reconciled 2026-08-24.** `domain/renewal-token.ts` with nine tests covering the attacks by name: a body rewritten to point at another listing, a token signed with a different secret, a signed body that is not JSON, and a signed listing id that is not a string. **An expired forged token is rejected by the signature, not by the date** — the order matters, because checking the date first would leak that the signature was valid. Two cycles of the same listing produce different tokens, which is what makes a token single-use
- [x] 7.10 RED: expired listing retained (not deleted), excluded from search, still renewable. **Reconciled 2026-08-24.** All three proven in `tests/integration/lifecycle.test.ts` against real Postgres: "marca vencido sin borrar la fila", "sale de la búsqueda", "sigue siendo renovable, y renovarlo lo devuelve a la búsqueda" — the round trip, not just the exclusion. A fourth test pins that a listing hidden by moderation is **not** touched by the expiry sweep
- [x] 7.11 Resend renewal notice; `vercel.json` cron schedule. **Closed 2026-08-24, the day the founder added the Resend key.** `infrastructure/resend-lifecycle-mailer.ts` replaces `ConsoleLifecycleMailer`, which is deleted — leaving a class whose own log line said "NO SE ENVIÓ ningún correo" would now be a lie. One instantiation site changed (`app/api/jobs/expiry-reminders/route.ts`).

  **No React Email template, and that is a decision rather than a shortcut.** The port had already ruled it out in writing: it takes a subject and a body that `composeNotice` built in the domain, because "un puerto que recibiera el aviso y el tipo de correo pondría la redacción del lado del proveedor". A template here would force one of two bad things — duplicate the copy, or move it out of the domain, which is the founder's permanent rule. What the adapter does instead is **presentation without wording**: `renderLifecycleHtml` splits the domain's body into paragraphs and escapes it, deciding not one word of what it says. Escaping is not cosmetic — the listing title was written by a person and travels inside that HTML, under our sending address.

  **Fails closed twice over.** `readMailerConfig` demands key AND sender together (a key without a sender bounces everything; a sender without a key sends nothing), and the route answers `500 mailer_not_configured` with `reminders_sent: 0` rather than starting a batch it cannot deliver — starting one would take each listing's ledger reservation, burn it with no mail sent, and the next run would find them reserved and never retry. It answers 500 and not 401 deliberately: the bearer was correct, so a 401 would send someone to rotate an unrelated secret.

  **`RESEND_API_KEY` alone is not enough**: `LIFECYCLE_MAIL_FROM` must be set and its domain verified in Resend, or every send bounces. Both are in `.env.example`. Four mutations checked red: removing the route guard, accepting a key without a sender, ignoring the error Resend RETURNS rather than throws, and dropping the HTML escape.

  **Proven to deliver, not just to compile — 2026-08-24.** A single real send through the configured adapter reached a Gmail inbox, in Primary and not in spam. **Stated at its real strength:** the sending domain is the founder's personal `gianbarboza.com` (tracked debt — `rentas.com.ve` is not owned yet), and mail from a domain you control to your own inbox is the easy case. It proves the wiring, the credentials and the HTML end to end; it does not prove deliverability to strangers, which only real volume will. The display name is set (`Rentas <avisos@…>`) so the recipient reads the product, not the domain — the mitigation that costs nothing while the address is provisional.
- [x] 7.12 E2E: reminder job → renewal link → renew flow. **CERRADA 2026-08-28: el camino feliz ya corre.** Lo que faltaba no era voluntad sino tres cosas que el repositorio no tenía: un aviso en una base alcanzable, su `expires_at` exacto —que es la cerradura del compare-and-swap— y el mismo `RENEWAL_TOKEN_SECRET` con el que firma el servidor. **La 11.22 puso las dos primeras** (`neonConfig.fetchEndpoint` contra `scripts/neon-http-proxy.mjs` más un Postgres sembrado) y el job de CI ya definía la tercera desde la 7.11; nadie lo había vuelto a mirar. `tests/e2e/renewal-link.spec.ts` → **«un enlace firmado renueva 30 días, y el mismo enlace no renueva dos veces»**, cuatro afirmaciones que sólo se pueden hacer con un aviso de verdad: el `GET` muestra el aviso LEÍDO (su título llega al HTML) y **no mueve la fila** —lo que la prueba de los dos `GET` no podía ver, porque sin base no había fila que mirar—; el `POST` suma 30 días contados desde AHORA y sube `expired` a `active`; y el mismo enlace contesta **409** la segunda vez sin mover la fila otro mes.

  **La fila la siembra y la borra la propia prueba, no `seed-e2e.ts`, y es una decisión y no una comodidad.** Renovar MUTA. Colgarse del catálogo compartido dejaría una suite que pasa según quién corrió antes; peor, `crawlability` corre en paralelo con `chromium` y encontraría el token ya quemado, leyendo «ya se usó» donde esperaba «renovado». Cada proyecto siembra **su propio id** (`idDePrueba(testInfo.project.name)`) y lo borra en el `finally`: sale igual con un reintento de CI, con los dos proyectos a la vez y corriendo dos veces seguidas. Ninguna fecha es literal — todas se derivan del reloj del momento, que es la trampa que ya mordió a este repositorio con una fixture de fecha escrita.

  **Tres mutaciones, y la tercera corrige lo que yo mismo iba a afirmar de más.** (a) `409` → `400` en la respuesta de «ya está renovado» de `app/renovar/[token]/route.ts`: **1.870 pruebas unitarias en verde** y sólo esta especificación en rojo, en los dos proyectos — la ruta entera (códigos, copia, encadenado) no la mide nadie más. (b) Quitar `eq(listings.expiresAt, request.expectedExpiresAt)` del `WHERE` de `DrizzleLifecycleListings.renew`, o sea convertir el token de un solo uso en uno infinito: **las 1.870 unitarias siguen verdes** y esto se pone rojo. (c) **Pero la integración TAMBIÉN atrapa la (b)** — `tests/integration/lifecycle.test.ts` falla 1 de 270 —, así que **no es cierto que esta especificación sea la única guardia del uso único**, y se anota en vez de dejar la frase bonita. Lo que sí es exclusivamente suyo es la (a): que la PANTALLA de renovación conteste lo que contesta.

  ~~**PARTIAL — el happy path sigue abierto**~~

  **What shipped, and why this half is the half a browser is for.** Four specs proving that a signed link arriving by email, opened the way a person opens it, **does not renew anything by being opened** and is not indexed: a token that does not parse and one with a plausible shape and a forged signature both refuse and never render the `<form method="post">` that is the only path to a renewal; the page carries `noindex, nofollow` on the error responses too, because those are the ones that circulate; and two consecutive raw `GET`s answer identically, which is what proves the open did not consume the token. Two mutations checked red: making the refusal render the form, and dropping the robots tag. **Runs everywhere with no `test.skip`** — an invalid token is rejected by the signature before the listing is ever read, so the whole path is exercised against the local fallback build pointed at an unroutable database.

  **What did NOT ship: running the reminder job end to end.** Since 7.11 wired `ResendLifecycleMailer`, running that job in CI would send **real email on every run** to whatever addresses are seeded — a scheduled sender, not a test. That half stays where it can be deterministic and offline: `send-lifecycle-notices.test.ts` and `tests/integration/lifecycle.test.ts`.

  **What remains, and what it needs.** The successful renewal in a browser — confirmation page, POST, listing back in search — needs two things Playwright cannot reach today: a listing seeded in the deployment's own database, and that deployment's `RENEWAL_TOKEN_SECRET` to mint a signature that verifies. Both mean new test-seeding infrastructure, which is its own work unit and not this one. Closing this task honestly is blocked on that, not on effort.

  `.github/workflows/ci.yml` gains a fake `RENEWAL_TOKEN_SECRET` for the e2e job — it signs nothing real and exists so the local fallback can REJECT a forged token rather than answer 500 "falta el secreto", which is the weaker half of what the spec is there to prove

## Phase 8: Trust — Reporting & Auto-Hide (PR8)

**El «6/6» de esta fase era falso, y así es como se veía.** Las seis tareas
originales (8.1-8.6) estaban hechas de verdad: el caso de uso, el dominio, el
esquema, los adaptadores y la ruta de operador. Lo que ninguna tarea nombraba
era la interfaz del inquilino, así que la fase se podía marcar completa con
`reportListing` **sin un solo llamador** — capacidad construida, probada contra
Postgres real e inalcanzable desde un navegador. La cuenta correcta de esta
fase es 8/8 y no 6/6, y las dos que faltaban son la 8.7 (hecha) y la 8.8
(abierta). AGENTS.md §5 lo dice con otras palabras: una casilla puede mentir.

- [x] 8.1 Schema: `listing_report`, `moderation_action`. **`drizzle/0013_giant_shriek.sql`.** `listing_report`: `UNIQUE(listing_id, reporter_id)` IS the "at most one report per account" guarantee — the same "constraint, not an `if`" reasoning `listing_reminder_cycle_unique` already established — so `count(*) WHERE listing_id = $1` is already a distinct-account count by construction. `moderation_action` has no `actor` column: this project has no operator-account model (the restore route is gated by a shared bearer secret, not a signed-in `user` row), so there is no identity to reference honestly. Both tables are `ON DELETE restrict` on every FK, same reasoning as `contact_reveal_event` (design.md D6): a report and a moderation action are both evidence, and `CASCADE` would erase exactly the rows most correlated with a listing or account someone was trying to make disappear.

  **DEVIATION-THAT-IS-A-CORRECTION, same shape as 7.1's UNIQUE columns.** This task's own text and design.md's Data Model prose both say `hidden_by_reports`. `src/shared/db/schema.ts:388` already ships `status: "active" | "expired" | "hidden"` with the comment "`hidden` is the auto-hide state from reports (Phase 8)", and both `markExpired`'s sweep and `DrizzleListingSearch` already filter on those three values. A fourth state would silently break both. This phase uses `hidden`, not `hidden_by_reports` — the schema was already right, the prose was stale.
- [x] 8.2 RED: unauthenticated visitor cannot report. **`src/modules/listing-trust/application/report-listing.test.ts`** — "refuses an anonymous visitor and records nothing" and "reads the session before touching the listing at all", written against `reportListing`/`./report-listing` before that module existed (`Cannot find module`). Same session-gate-first shape `revealContact` already carries. Mutation-checked: swapping the session read and the listing read turns the ordering test red (restored).
- [x] 8.3 RED: 3rd distinct authenticated account triggers auto-hide; repeat report from same account does not. **`src/modules/listing-trust/domain/report-threshold.test.ts`** (pure `resolveReportOutcome`, written before the module existed) plus `report-listing.test.ts`'s "hides the listing when the third distinct reporter arrives" / "records the report and does not hide the listing below the threshold". The distinct count itself is proven for real against Postgres in `tests/integration/listing-trust.test.ts` — the `listing_report_listing_reporter_unique` constraint, not application code, is what makes a repeat report from account A insert zero rows while accounts B and C each insert one.
- [x] 8.4 GREEN: `ReportListingUseCase` — distinct-account counting, `status → hidden` at 3. **`src/modules/listing-trust/application/report-listing.ts`** + `domain/report-threshold.ts`. Two decisions the spec leaves open, made here and recorded rather than left for the code to decide silently:

  **A report on an already-hidden or already-expired listing.** `resolveReportOutcome` only transitions `active → hidden`; a listing that is already `hidden` stays hidden (no "more hidden" state), and an `expired` listing stays `expired` — auto-hiding it would let it escape `markExpired`'s `WHERE status = 'active'` clause forever, which is `tests/integration/lifecycle.test.ts`'s own expiry guarantee breaking through a different module's write path. Both cases still record the report row (never refused, never silently dropped) — only the status transition is a no-op. Proven at the unit level (report-threshold.test.ts, report-listing.test.ts) and against real Postgres (an already-expired listing reported by 3 distinct accounts stays `expired`, `tests/integration/listing-trust.test.ts`).

  **Self-reporting is allowed.** Neither spec scenario exempts the publisher, and the threshold is 3 DISTINCT accounts regardless of who holds one of them — a publisher reporting their own listing spends one of the three seats it takes to hide it and gains nothing an attacker could not already get with a second real account. Covered by "allows the publisher to report their own listing".

  Mutation-checked: `>=` → `>` on the threshold turns both the domain and the application "hides at exactly 3" tests red; removing the `currentStatus !== "active"` guard turns "leaves an expired listing expired" red; removing `.onConflictDoNothing()` from the adapter turns the integration test's repeat-report assertion into a real `23505` unique-violation error instead of a silent no-op. All restored after confirming red.
- [x] 8.5 RED: operator restore returns listing to `active`. **`src/modules/listing-trust/application/restore-listing.test.ts`** (written before `restoreListing` existed) plus **`src/modules/listing-trust/domain/restore-outcome.test.ts`** for the pure decision, and **`src/modules/listing-trust/domain/operator-authorization.test.ts`** for the route's gate — the same seven malformed-header cases `cron-authorization.test.ts` already established, applied to a new `OPERATOR_SECRET` rather than reusing `CRON_SECRET` (two distinct guardians, no reason to share a rotation). All three referenced modules that did not exist yet at write time, confirmed to fail on `Cannot find module` before implementation.
- [x] 8.6 GREEN: `RestoreListingUseCase` + `moderation_action` record; minimal operator-only restore route. **`src/modules/listing-trust/application/restore-listing.ts`**, **`src/modules/listing-trust/infrastructure/drizzle-listing-moderation.ts`** (`DrizzleListingModeration`, `DrizzleListingReports`, `DrizzleModerationActions`), **`src/modules/listing-trust/infrastructure/moderation-config.ts`** (`readOperatorSecret`, same no-default/no-throw shape as `lifecycle-config.ts`), **`app/api/operator/restore-listing/route.ts`** + its `route.test.ts` (mirrors `expiry-reminders/route.test.ts`'s "import the real module, prove the 401/400 paths never touch the database" idiom).

  **"provided it has not also expired" (the spec's own words) is the second precondition, decided in `domain/restore-outcome.ts`.** A `hidden` listing is excluded from `markExpired`'s sweep, so its `expires_at` can sit in the past for as long as it stays hidden. Restoring it straight to `active` would resurrect a listing time has already retired; `resolveRestoreOutcome` instead returns `expired` in that case — still an operator action worth a `moderation_action` row, just not a return to `active`. Proven at the unit level and against real Postgres (`tests/integration/listing-trust.test.ts`, "returns to expired instead of active"). Only a `hidden` listing may be restored at all — `active` or `expired` is refused outright (`ListingNotHiddenError`), never silently no-op'd.

  **The route carries no business decision.** Same split as `sendLifecycleNotices`/`isAuthorizedJobRequest`: `isAuthorizedOperatorRequest` (domain, constant-time, fails closed with no `OPERATOR_SECRET`) gates the route; `restoreListing` itself takes no auth dependency, the same way `renew` and `sendLifecycleNotices` do not know about the cron secret that gates their own route.

  Mutation-checked: removing the `currentStatus !== "hidden"` guard in `resolveRestoreOutcome` turns both "refuses to restore a listing that is not hidden" tests red (domain and application); flipping the expiry comparison turns both "restores... to active"/"returns... to expired" tests red; forcing `isAuthorizedOperatorRequest` to always return `true` turns 3 of 10 `operator-authorization.test.ts` cases red. All restored after confirming red.

- [x] 8.7 **(nueva — encontrada al cerrar la 9.28, no planificada por nadie.)** **`reportListing` no lo llama ninguna ruta.** Es la QUINTA instancia de la misma forma de defecto en este trabajo, y la primera que ninguna tarea nombraba: `src/modules/listing-trust/application/report-listing.ts` está completo y probado (8.2, 8.3, 8.4), `resolveReportOutcome` decide el auto-ocultamiento al tercer reportante distinto, `listing_report` tiene su `UNIQUE(listing_id, reporter_id)` y `tests/integration/listing-trust.test.ts` lo prueba contra Postgres real — y `rg` sobre `app/` y `components/` devuelve **cero** llamadores. En un navegador, nadie puede reportar un aviso, así que el umbral de tres no puede dispararse nunca y el estado `hidden` sólo lo puede producir un `UPDATE` a mano. La lámina 14d ya dibuja la consecuencia («Oculta por reportes») de algo que hoy no puede ocurrir. La Fase 8 no tiene ninguna tarea de interfaz: `restoreListing` sí tiene su ruta de operador (8.6), pero el reporte del inquilino nunca tuvo una. Falta: el disparador en la ficha (`app/alquiler/[ciudad]/[zona]/[slug]/`), su acción de servidor sobre `reportListing`, y la copia del acuse. **No se construye en esta porción**: es Fase 8, no Fase 9, y meterla acá habría mezclado dos alcances en un mismo PR.

  **HECHA.** La puerta es una pantalla propia, `/alquiler/<ciudad>/<zona>/<slug>/reportar`:

  - **`src/modules/listing-trust/domain/report-screen.ts`** — qué se dibuja y qué se dice, decidido donde el suelo de cobertura del 90 % llega. Pruebas: `report-screen.test.ts`, «no tiene por dónde recibir el resultado del reporte» y «dice lo mismo haya ocultado el aviso o no, y no nombra el umbral».
  - **`app/alquiler/[ciudad]/[zona]/[slug]/reportar/page.tsx`** + `reportar.module.css` — el formulario. Prueba: `reportar.test.ts`, «entrega la acción al formulario sin ejecutarla», que afirma las dos mitades a la vez (la acción está cableada Y el render no la llamó).
  - **`app/alquiler/[ciudad]/[zona]/[slug]/reportar/actions.ts`** — la acción de servidor sobre `reportListing`. Pruebas: `actions.test.ts`, «manda al acuse cuando el reporte fue el que ocultó el aviso» y «no reporta nada y manda al inicio con otro origen escrito completo».
  - **`src/modules/identity/domain/safe-return-destination.ts`** gana `safeReturnPath`. Prueba: «rechaza una escapada por segmentos relativos».
  - **`app/alquiler/[ciudad]/[zona]/[slug]/page.tsx`** — el enlace del pie tenía destino y no llevaba a ningún lado: `href="#reportar"` era un ancla a una sección que la ficha nunca dibujó. Prueba: `ficha.test.ts`, «lleva a una ruta que existe de verdad», que afirma contra el sistema de archivos y no contra una cadena.
  - **`tests/integration/listing-report.test.ts`** — la cadena entera contra Postgres real, POR la acción de servidor y no alrededor de ella: «la tercera cuenta distinta oculta el aviso, y el acuse no lo dice». Es lo que faltaba: todo lo que había llamaba a `reportListing` directamente, que es exactamente por qué esta suite no vio el defecto. `vitest.integration.config.ts` gana el alias `@` para poder importar la acción.
  - **`tests/e2e/reportar-aviso.spec.ts`** — «un GET no reporta ni acusa recibo», con pedidos crudos, y corre además en el proyecto `crawlability` con el script apagado. Sin base de datos y sin `test.skip`: el `GET` de esta pantalla no consulta nada.

  **DOS DECISIONES QUE LA LÁMINA NO CONTESTA, tomadas acá y con su razón.**

  1. **Qué se ve después de reportar: un acuse constante.** Decir «este aviso quedó oculto» le entrega a quien ataca el único dato que le falta —cuántas cuentas más necesita—, y no decir nada no le enseña a nadie que el reporte llegó. La salida es la forma que AGENTS.md §7 ya usa para el contacto bloqueado: `ReportSentScreen` **no tiene ningún campo donde el resultado quepa** y `resolveReportScreen` **no tiene ningún parámetro por donde recibirlo**, así que un render no puede filtrarlo. La acción descarta `{ autoHidden }` sin guardarlo ni en una variable, y el destino de la redirección es idéntico en los dos casos.
  2. **El control se le muestra también a quien no tiene sesión.** La lámina dibuja «Reportar este aviso» al pie de una ficha que es anónima, y la spec de listing-trust describe al visitante sin sesión *intentando* reportar («the system blocks the action and requires sign-in first») — no se puede intentar lo que no se ve. Esconderlo haría que nadie descubriera que reportar existe, y reportar es la única señal antifraude que un inquilino puede dar. El muro está en la acción, no en el render, y devuelve a la pantalla de reportar y no a la ficha: quien entró para reportar sigue queriendo reportar.

  **DESVÍOS, registrados en vez de decididos en silencio.**

  - **Pantalla propia y no un bloque al pie de la ficha**, aunque el `href="#reportar"` que había sugería lo segundo. Sin JavaScript el acuse sólo puede llegar por una URL, así que reportar termina en una redirección; hacerla contra la ficha publicaría variantes con parámetro de la página más indexada del sitio para decir «gracias». La pantalla propia lleva `noindex` honesto.
  - **No arrastra el origen de la búsqueda** (`desde`, 16.9). Es un desvío sin salida cuya única puerta es volver al aviso, y el botón de atrás del navegador devuelve la URL de la ficha con su origen intacto; enhebrarlo además por el POST habría sido llevar estado a través de una redirección para un enlace al que se llega apretando atrás.
  - **`safeReturnPath` vive en `identity/domain` junto a `safeSignInDestination`** y comparte su origen inventado y su prefijo `/alquiler/`. Devuelve `null` en vez de un respaldo: acá el valor se concatena para armar `…/reportar?enviado`, y un respaldo silencioso convertiría una ruta hostil en un acuse dibujado sobre una pantalla que no es la nuestra.

  **CONTRADICCIÓN ENTRE PLAN Y CÓDIGO, no arreglada acá.** El texto de la 8.7 —y también el de la 8.3 y la 8.4— cita `tests/integration/listing-trust.test.ts`. Ese archivo no existe: las pruebas de integración están en `tests/integration/listing-report.test.ts` y `tests/integration/listing-restore.test.ts`. Se deja dicho para que la próxima persona no lo busque.

  **CONTRADICCIÓN ENTRE LÁMINA Y ESPECIFICACIÓN.** La especificación de la ficha (§2, paso 10) dice «Formulario de reporte», o sea una pantalla; las dos láminas de la ficha enlazan «Reportar este aviso» a **su propio `id`** (`#10a` en móvil, `#11d` en escritorio), así que la referencia declara el enlace y no dibuja la pantalla que abre. Ninguna lámina de reportar existe. Se derivó del sistema, como AGENTS.md §2 manda para las 16 superficies sin lámina: `FormShell` (una columna de 600px), la tipografía de la ficha, `ActionButton`, y ni un valor literal.

  **Mutaciones confirmadas, cada una con la prueba que puso en rojo** (aplicadas con un script que exige que el texto objetivo aparezca exactamente una vez y que el hash del archivo cambie — `sd` no reemplaza literales multilínea y sale 0):

  | Mutación | Prueba que se puso en rojo |
  |---|---|
  | `sentFlag === undefined ? FORM : SENT` → `sentFlag ? SENT : FORM` | «con el parámetro pelado, sin valor dibuja el acuse» |
  | `resolveReportScreen` gana un segundo parámetro | «no tiene por dónde recibir el resultado del reporte» |
  | el acuse dice «y el aviso quedó oculto» | «dice lo mismo haya ocultado el aviso o no…» |
  | `safeReturnPath` deja de mirar el origen | «rechaza otro origen escrito completo» (+2) |
  | el prefijo se compara sobre el texto crudo | «rechaza una escapada por segmentos relativos» |
  | se quita el `catch` de `safeReturnPath` | «rechaza un host que hace lanzar al parser» |
  | la redirección lleva `&oculto=${autoHidden}` | «manda al acuse…» (los dos casos) y «no distingue un caso del otro por el destino» |
  | la vuelta deja de pasar por `safeReturnPath` | «no reporta nada y manda al inicio…» (4 casos) |
  | sin sesión vuelve a la ficha en vez de a reportar | «manda a entrar con la vuelta a la pantalla de reportar» |
  | la página llama a la acción al dibujarse | «entrega la acción al formulario sin ejecutarla» |
  | el formulario se queda sin `action` | «entrega la acción al formulario sin ejecutarla» |
  | se quita la guarda `listingIdFromSlug` | «no dibuja el formulario» |
  | el enlace del pie apunta a `/reportes` | «lleva a una ruta que existe de verdad» |
  | la acción lee `formData.get("aviso")` | integración: «el primer reportante deja una fila…» (+3) |

  **Presupuesto excedido y dicho, no callado**: ~1.160 líneas de autoría (~430 de implementación, ~730 de pruebas y configuración) contra las 400 del presupuesto. No es divisible sin volver a crear el defecto: cualquier corte entrega o una pantalla sin acción o una acción sin pantalla, que es exactamente la forma de la 8.7.

- [ ] 8.8 **(nueva — consecuencia directa de la 8.7, no un defecto de ella.)** **Un aviso ocultado por reportes sólo se puede restaurar con `curl`.** `restoreListing` está completo (8.6) y su única puerta es `app/api/operator/restore-listing/route.ts`, detrás de un `OPERATOR_SECRET` de portador — sin pantalla, sin sesión de operador y sin modelo de cuenta que la sostenga (8.1 explica por qué `moderation_action` no tiene columna `actor`). Mientras nadie podía reportar, eso era teoría: el estado `hidden` no lo producía nada. **Desde la 8.7 sí lo produce**, y tres cuentas cualesquiera pueden ocultar un aviso legítimo, así que la restauración pasa a ser un camino que se va a recorrer. Falta decidir —es una decisión del fundador, no una técnica— si el operador tiene pantalla o si el `curl` alcanza para el MVP; y, si tiene pantalla, hace falta primero un modelo de cuenta de operador, que hoy no existe. `/mis-avisos` ya dibuja el estado («Ocultas», `publisher-listing-board.ts`), así que quien publica **sí** se entera; lo que no tiene es a quién pedirle la revisión.

  **Ampliada por el fundador (2026-08-27): no es un trabajo de operador, son DOS, y hoy se hacen de formas distintas.** Habilitar la carga masiva de una agencia es el otro: `user.bulk_import_enabled` se lee en cuatro lugares y **no lo escribe ni una línea de código**, así que prenderlo es un `UPDATE` a mano contra Neon. Restaurar, en cambio, es un `curl` con secreto de portador. Dos herramientas, dos maneras de equivocarse — y una asimetría que importa: restaurar deja constancia en `moderation_action`, el `UPDATE` no deja nada. Lo que se decida acá cubre las dos, para no construir una pantalla de restauración y descubrir después que falta la otra.

  **Lo que NO se toca, confirmado por el fundador el mismo día:** toda cuenta nueva nace con la bandera en `false`. Ya está garantizado en tres capas —`NOT NULL DEFAULT false` en el esquema, cero escrituras en el código, y el único `true` (`app/importar/page.tsx:63`) es para dibujar el menú DESPUÉS de que `isBulkImportAuthorized` dejó pasar, nunca para otorgar. Es la regla que el propio esquema escribe: **«operator-set, never self-service»**, sin ningún default que convierta un hueco en un permiso.

- [x] 8.9 **(nueva — encontrada barriendo en busca de una sexta instancia de la 8.7.)** **Cerrada 2026-08-28: los tres se borraron, y la prueba de que no dibujaban nada es el paquete.** Antes de tocar nada se estableció en positivo y no por `grep`: no hay archivo barril en `app/` ni en `components/`, los cuatro `import(` dinámicos del repositorio son `sharp-dhash` y tres `await import("./route")` de pruebas, y las cinco apariciones de `CityZoneSelect` fuera de sus dos archivos son **comentarios** (`catalogue.ts`, `seed.ts`, `search-criteria.ts` ×2). Medición del paquete con `pnpm build && pnpm budget:bundle` antes y después: **once de las doce rutas quedan byte a byte idénticas**; la única que se mueve es la ficha, 106.43 → 106.42 KB gzip, y esa diferencia **se aisló midiendo, no suponiendo** — restaurando sólo las reglas `.row` de `Field.module.css` la ficha vuelve a 106.43 exacto. O sea que los diez bytes son el mapa de clases de esa hoja (la ficha dibuja `ContactBlock`, que dibuja `Field`) y **los tres componentes borrados aportaban cero**. Por qué cada uno sobraba: `PageShell` nunca se cableó —su propio comentario decía que no entraba en `app/layout.tsx` para no anidar dos `<main>`— y cada pantalla resolvió sus landmarks a mano; `CityZoneSelect` quedó superado por `SearchFilters`, que ya lleva la regla D5 adentro (`zonesForCity`, misma función); y `FieldRow` existía para el par ciudad/zona del `PublishForm` que la 3.9(c) reemplazó por `PublishStep`, **que no pregunta la ciudad en ningún paso** (medido: `layout.spec.ts` → «3.9: la ciudad no se pregunta en ninguna parte del paso»), así que el par para el que existía no puede existir. Se van con ellos `CityZoneSelect.module.css`, las reglas `.row`/`.row > *` de `Field.module.css` y las dos suites de sus componentes; la aserción «pairs two fields on one row at every width» se borra **con** su sujeto y no se reapunta a otra hoja, por la misma razón que la 14.42 dejó escrita en `design-contract.test.tsx`. Verificado por `pnpm typecheck`, `pnpm lint` y `vitest run` (1.870 pruebas, 8 menos: las que medían lo borrado).

  **Contradicción anotada al pasar, no arreglada acá:** el texto de la 3.9 dice que «el patrón de campo se mudó a `components/molecules/Field.tsx`» para que la próxima pantalla partiera de las reglas. Hoy `Field` tiene **un solo** consumidor y no es el formulario de publicar: es `ContactBlock`. El `PublishStep` que reemplazó al `PublishForm` dibuja sus campos con `publish-steps.module.css` y no importa `Field`.

  ~~**(nueva — encontrada barriendo en busca de una sexta instancia de la 8.7.)**~~ **Tres componentes exportados que no dibuja nadie**: `PageShell` (`components/layout/PageShell.tsx`), `CityZoneSelect` (`components/molecules/CityZoneSelect.tsx`) y `FieldRow` (`components/molecules/Field.tsx`) no aparecen en ningún archivo servido de `app/` ni de `components/`. **No es la misma forma de defecto que la 8.7 y conviene no confundirlas**: un caso de uso inalcanzable es una capacidad del producto que no existe; un componente huérfano es una pantalla que dibujó lo suyo a mano en vez de componerlo, o código que sobró de un rediseño. `CityZoneSelect` es el interesante: publicar y buscar eligen ciudad y zona, así que o lo deberían estar usando o el molécula sobra. La barrida de `a02e1b1` («borrar lo que el nav y el modal dejaron sin quien los dibuje») ya hizo una pasada de esta clase; estos tres quedaron.

  **Lo que esa misma barrida SÍ descartó, y vale registrarlo**: no hay una sexta instancia de la 8.7. Los 22 casos de uso de `src/modules/*/application/` tienen llamador —`collect-curated-zones` y `process-uploaded-photo` llegan indirectamente, por `run-import-validation` y por `app/mis-avisos/actions.ts`— y las siete funciones de dominio exportadas que no aparecen fuera de su propio test (`parsePhotoNumber`, `photoNeighbours`, `safeResultsOrigin`, `completedSteps`, `pageCount`, `readSearchStep`, `acceptsZoneQuery`) las usa otra función del mismo archivo que sí tiene llamador. `reportListing` era el último.

## Phase 9: Broker Bulk Import (PR9)

Depends on PR3 (publication use cases) and PR4 (trust pipeline). Creates no write path of its own into `listing`.

- [x] 9.1 Schema: `user.bulk_import_enabled` (bool, default false); `listing.external_reference` (nullable); UNIQUE `(publisher_id, external_reference)`; `listing.status` gains `draft`; `bulk_import_batch` table. **`src/shared/db/schema.ts`, migration `drizzle/0014_fast_zaran.sql`.** Proven against real Postgres by `tests/integration/broker-bulk-import.test.ts` ("listing.status acepta 'draft'", "dos avisos del mismo publisher con la misma referencia externa violan la restricción única" — code `23505`, and the NULL-does-not-collide case). **Correction beyond the task text, recorded per AGENTS.md §5:** `bulk_import_batch` ships as a minimal scaffold (`id`, `publisher_id`, `uploaded_at`, `total_rows`, `valid_rows`, `created_drafts`) — the task only asked for the table to exist; its full shape belongs to `ValidateImportUseCase`/`ConfirmImportUseCase` (9.13-9.17), which no task before 9.1 has designed yet. **Also touched, and why it was safe:** `listing.status`'s TS union widened to include `draft`, which broke two *independent* narrower unions that read the same column — `ListingModerationStatus` (`src/modules/listing-trust/domain/report-threshold.ts`) and `RenewableListing.status` (`src/modules/listing-lifecycle/application/ports/lifecycle-listings.port.ts`). Both were widened alongside it with a comment explaining why no new branch is needed (each already fails closed via a `!== "active"`/`!== "hidden"` guard). `pnpm typecheck` was clean both before this widening (confirming the two holes above were the only ones) and after (confirming the fix). See the risk map in this apply's return summary for every place that reasons about `listing.status` and what `draft` means there today.
- [x] 9.2 RED: account with `bulk_import_enabled = false` POSTing the import endpoint directly returns 403 and creates no draft. **Tests written and confirmed failing before the guard existed:** `src/modules/broker-bulk-import/domain/bulk-import-authorization.test.ts`, `src/modules/broker-bulk-import/application/authorize-bulk-import.test.ts`, `app/api/bulk-import/route.test.ts` (all three failed on "Cannot find module" before the corresponding implementation files existed — see apply-progress for the transcript), and `tests/integration/broker-bulk-import.test.ts` against real Postgres.
- [x] 9.3 GREEN: server-side flag guard on every import endpoint — UI visibility is not the control. **`src/modules/broker-bulk-import/domain/bulk-import-authorization.ts`** (`isBulkImportAuthorized`, the one pure decision, same idiom as `operator-authorization.ts`) **→ `src/modules/broker-bulk-import/application/authorize-bulk-import.ts`** (`authorizeBulkImport`, the one function every import endpoint must call first: session gate via `requireAuthenticatedSession`, then the flag) **→ `src/modules/broker-bulk-import/infrastructure/drizzle-bulk-import-account.ts`** (`DrizzleBulkImportAccounts`) **→ `app/api/bulk-import/route.ts`** (the one endpoint this slice wires; 401 on no session, 403 on disabled flag, 501 `not_implemented` on an authorized request — the CSV pipeline itself is 9.4+). **Correction beyond the task text, recorded per AGENTS.md §5:** the task text implies a working import endpoint exists to guard; at this slice nothing past the guard is built (`ValidateImportUseCase`/`ConfirmImportUseCase` land in 9.15), so the authorized path intentionally answers `501` rather than performing an import — "creates no draft" is proven in the strongest available form: nothing in the codebase can create one yet. Every future import endpoint (CSV upload, confirm, photo attach — 9.7+) MUST call `authorizeBulkImport` first rather than re-checking the column itself.
- [x] 9.4 RED: file whose header omits a required column is rejected whole, naming the column, with no draft created. **`src/modules/broker-bulk-import/domain/csv-import-header.test.ts`** ("rejects the WHOLE file when precio_usd is missing, naming the column") — written before `csv-import-header.ts` existed ("Cannot find module" confirmed), then implemented. Also proven at the composed acceptance layer: **`src/modules/broker-bulk-import/application/parse-import-file.test.ts`** ("rejects the WHOLE file when precio_usd is missing... before any row is processed").
- [x] 9.5 RED: semicolon-delimited file carrying a UTF-8 BOM parses into correct columns, not one column. **`src/modules/broker-bulk-import/domain/csv-import-text.test.ts`** (`stripBom`, `sniffDelimiter`) + **`src/modules/broker-bulk-import/domain/csv-import-rows.test.ts`** ("splits a semicolon-delimited file into CORRECT columns, not one column"), composed and proven end-to-end in **`parse-import-file.test.ts`** ("parses a semicolon-delimited file carrying a UTF-8 BOM into CORRECT COLUMNS, not one column").
- [x] 9.6 RED: non-UTF-8 file rejected with a message telling the broker to re-export as CSV UTF-8. **`src/modules/broker-bulk-import/domain/decode-csv-bytes.test.ts`** (strict `TextDecoder({fatal:true})`, refuses an invalid byte sequence) + **`parse-import-file.test.ts`** ("rejects a file that cannot be decoded as UTF-8, telling the broker to re-export as CSV UTF-8" — asserts the thrown `ImportEncodingError`'s message matches `/CSV UTF-8/`).
- [x] 9.7 GREEN: CSV parser — delimiter sniffing (`,` / `;`), BOM strip, UTF-8 enforcement, streaming read. **`src/modules/broker-bulk-import/domain/csv-import-text.ts`** (BOM strip, delimiter sniff outside quoted regions) **+ `decode-csv-bytes.ts`** (strict UTF-8) **+ `csv-import-rows.ts`** (RFC4180-ish tokenizer: quoted fields, escaped `""`, CRLF tolerance) **→ `src/modules/broker-bulk-import/application/read-bounded-import-file.ts`** (the streaming half: reads `ImportFileSourcePort.chunks()` via `for await`, never buffers past a crossed bound) **→ `parse-import-file.ts`** (orchestrates all of it). **Correction beyond the task text, recorded per AGENTS.md §5:** "streaming read" is honest only up to the point bounds are enforced — once a file is proven ≤2 MB / ≤50 rows, the (necessarily small) remainder is decoded and tokenized in memory, which is standard and was never claimed to be streamed further. The genuinely streamed, no-buffer part is exactly where it matters: an oversized file is never read to completion (proven by `read-bounded-import-file.test.ts` asserting `consumedChunks()` stays far below the total chunk count on an ~8 MB adversarial input). **A deliberate simplification, stated rather than claimed away:** a CSV field may not contain a literal newline, even quoted — every `\n`/`\r\n` is a row separator. This is what keeps the streaming newline-count and the parser's real row count in exact agreement (see 9.8/9.9); a broker portfolio's `titulo`/`descripcion` cells are single-line copy, so this costs nothing real. Documented at the top of `csv-import-rows.test.ts`.
- [x] 9.8 RED: file exceeding the row or size limit is refused before its contents are parsed. **`src/modules/broker-bulk-import/domain/csv-import-bounds.test.ts`** + **`src/modules/broker-bulk-import/application/read-bounded-import-file.test.ts`** — every case proves the guarantee TWO ways: the right error is thrown, AND the fake source's `consumedChunks()` proves the stream was never read to completion (e.g. "refuses a file whose DECLARED size already exceeds the limit, WITHOUT reading a single chunk" — 0 chunks consumed; the ~8 MB adversarial-declared-size case aborts having consumed under half the chunks).
- [x] 9.9 GREEN: size and row bounds enforced pre-parse. **`src/modules/broker-bulk-import/domain/csv-import-bounds.ts`** (`MAX_IMPORT_FILE_SIZE_BYTES = 2 MB`, `MAX_IMPORT_ROW_COUNT = 50`, pure `checkFileSizeBound`/`checkRowCountBound` over numbers, no I/O) **→ `read-bounded-import-file.ts`** (streaming enforcement: `declaredByteLength` checked before a single chunk is read; actual bytes-read and newline-count both tracked live and abort the `for await` loop the instant either is CERTAIN to exceed the bound — defends against a lying declared size) **→ `parse-import-file.ts`** (an authoritative post-parse row-count check against the real parsed row count, for the boundary case a streaming newline-count alone cannot always resolve, e.g. exactly 51 rows with no trailing newline — proven by `parse-import-file.test.ts`'s dedicated boundary test). Both refusal messages name the limit (2 MB / 50 rows), matching the founder's requirement that a broker learns to split the file rather than stare at a generic error. **Mutation-checked**: disabling the size-bound check does not merely fail an assertion — it crashes the test worker with an out-of-memory error on the adversarial-declared-size test, which is itself strong proof the guard is load-bearing exactly where it matters.
- [x] 9.10 RED: extra columns (`publisher_type`, `status`, `expires_at`, `user_id`) are ignored; resulting drafts take publisher type from the account. **`src/modules/broker-bulk-import/domain/csv-import-rows.test.ts`** ("ignores publisher_type, status, expires_at, and user_id — never mapped, even if present") + **`parse-import-file.test.ts`** (same, at the composed layer). **Scope note recorded per AGENTS.md §5:** this slice proves the columns are dropped from the parsed row; "resulting drafts take publisher type from the account" is `ConfirmImportUseCase`'s job (tasks.md 9.15-9.17, not built yet) — there is no draft-creation code for this test to exercise yet, so the RED/GREEN pair here is scoped to what 9.4-9.11 actually builds: the parser's allowlist.
- [x] 9.11 GREEN: strict column allowlist — unrecognised columns dropped, never mapped. **`src/modules/broker-bulk-import/domain/csv-import-columns.ts`** (`IMPORT_COLUMN_ALLOWLIST` — the ONE source of the 16 columns, reused by both this parser and the future template generator, tasks.md 9.25) **→ `csv-import-header.ts`** (`validateImportHeader` builds `columnIndexByField` ONLY for allowlisted headers — an unrecognised header never gets an entry, so it is structurally unmappable, not filtered after the fact) **→ `csv-import-rows.ts`** (`mapImportRow` can only read through that map). **Founder decision beyond the spec's original 6, recorded per AGENTS.md §5:** the allowlist carries 16 columns, not 6 — the operator asked for every field the schema can already store (`tipo_inmueble`, `habitaciones`, `banos`, `metros2`, `estacionamientos`, and the five F6 booleans `planta_electrica`/`agua_regular`/`amoblado`/`vigilancia`/`linea_blanca`), all confirmed present on `listing` via `publishable-listing.ts`/`listing-repository.port.ts`. Contact columns are deliberately absent from the allowlist (`csv-import-columns.test.ts`, "does NOT accept contact columns"): `listing.contact_method`/`contact_value` are the account's default, copied at publish time, never the file's — a `contact_method` column in an uploaded file is dropped exactly like `publisher_type`. **Boolean cell vocabulary, decided and documented (founder ask, not in the original task text):** `si`/`no` (also `1`/`0`), case-insensitive, trimmed; empty means "not specified", not `false`. Executable in **`csv-import-boolean-cell.ts`** (`parseImportBooleanCell`, fully unit-tested) — **not yet wired into `mapImportRow`**, deliberately: this slice's row mapper stays string-in/string-out (no field has been validated or coerced), so a malformed boolean value becomes a row-level validation error with a reason in `ValidateImportUseCase` (9.12-9.15), not a silent coercion here. The vocabulary is decided and reusable now so that use case never re-invents it.
- [x] 9.12 RED: a row whose `zone` is not curated for its `city` is rejected by the same rule the single-listing flow applies. **`src/modules/broker-bulk-import/domain/import-row-validation.ts`** (`validateImportRows`, which calls `validatePublishableListing` verbatim rather than re-implementing the curated-zone check) proven by **`import-row-validation.test.ts`** ("rejects a row whose zone is not curated for its city, via the SAME rule the single-listing flow applies"). Mutation-checked: disabling `validatePublishableListing`'s `zoneId.notInCity` branch turns BOTH that test and `publishable-listing.test.ts`'s own zone tests red — one rule, one place it can break.
- [x] 9.13 RED: 38 valid + 2 invalid rows → preview reports both sets with row numbers and reasons; confirming creates exactly 38 drafts. **`import-row-validation.test.ts`** ("reports 38 valid rows and 2 invalid rows with their row numbers and reasons", pure/no I/O) and **`src/modules/broker-bulk-import/application/confirm-import.test.ts`** ("creates exactly the valid rows as drafts, and reports the invalid ones untouched" — asserts `createdCount === 38`, `save` called exactly 38 times, and 2 errors returned with row numbers/reasons).
- [x] 9.14 RED: previewing without confirming creates nothing. **`src/modules/broker-bulk-import/application/validate-import.ts`** (`validateImport`) proven by **`validate-import.test.ts`** ("NEVER calls listings.save — preview creates nothing" — asserts the spy directly, per the orchestrator's explicit ask, not only that a count stayed at zero). **Design choice, recorded per AGENTS.md §5:** `ValidateImportDependencies` includes `listings: ListingRepositoryPort` even though `validateImport` never calls `.save` on it — deliberately, so it shares the EXACT dependency shape `ConfirmImportDependencies` uses (one place a caller wires the ports) and so a test can spy on `.save` directly rather than only asserting a row count. Mutation-checked: inserting a stray `listings.save(...)` call into `validateImport` turns this exact test red.
- [x] 9.15 GREEN: `ValidateImportUseCase` (whole file, zero writes) + `ConfirmImportUseCase`. **`validate-import.ts`** + **`src/modules/broker-bulk-import/application/confirm-import.ts`** (`confirmImport`), both built on the shared **`run-import-validation.ts`** (`runImportValidation` — the ONE place both use cases validate, so "confirm creates exactly what the preview reported as valid" holds by construction) → **`import-row-validation.ts`** (domain) → **`collect-curated-zones.ts`** (gathers curated zones across every distinct city in the file, since `ZoneCataloguePort.listZonesForCity` is scoped to one city) → **`import-account-contact.ts`** + **`drizzle-import-account-contact.ts`** (the fail-closed account-contact guard below).

  **Correction beyond the task text, recorded per AGENTS.md §5 — this is the deviation the orchestrator's prompt flagged in advance.** The task says `ConfirmImportUseCase` "delegates to `PublishListingUseCase` validation." Taken literally this cannot work: `publishListing` always requires `photoCount >= 1` (`publishable-listing.ts`, `photos.required`) and an imported draft has zero photos by design (they attach afterwards, tasks.md 9.20-9.23) — every row would be rejected, and bulk import could never create a single draft. The resolution taken: **the photo requirement is a rule for ACTIVATION, not for a draft's existence** — the spec's own "Drafts Are Not Published Listings" requirement says so directly ("A draft MUST become active only once it satisfies minimum publishable content"). `validatePublishableListing` (`src/modules/listing-publication/domain/publishable-listing.ts`) now takes a third parameter, `stage: "draft" | "activation" = "activation"` — `"draft"` skips only the "at least one photo" half of the rule (the six-photo ceiling still applies); every other rule, including the curated-zone check, runs identically in both stages. This is **one validator, one rule set, one place the photo requirement lives** — not a second, photo-less copy of the function for imports to call instead. `publishListing` now passes `"activation"` explicitly (previously relied on there being no third parameter at all); the default preserves its exact prior behaviour, and `publishable-listing.test.ts`'s existing suite passes unchanged. New tests: `publishable-listing.test.ts` → `'stage "draft" — the photo requirement is deferred to activation'` (4 cases, including "defaults to activation stage when no stage is given, unchanged behaviour"). Mutation-checked (see 9.12 above) that the shared rule set still fires identically in both stages.

  **What "delegates to `PublishListingUseCase`" is honoured as, given the above:** not a call to `publishListing()` itself (which also hardcodes `status: "active"` and starts the 30-day expiry clock — both explicitly forbidden for a draft by "Drafts Are Not Published Listings" and by tasks.md 9.18's own scope), but reuse of the exact write path `publishListing` uses: `ListingRepositoryPort.save()`. `confirmImport` builds one `NewListing` per valid row with `status: "draft"` and calls the SAME port method, matching the phase header's own constraint verbatim: "Creates no write path of its own into `listing`." **Widened to make this possible, all backward-compatible:** `NewListing.status` (`listing-repository.port.ts`) from the literal `"active"` to `"active" | "draft"`; `NewListing` gained an optional `externalReference?: string` (`undefined` for the single-listing flow, unchanged); `DrizzleListingRepository.save()` (`drizzle-listing-repository.ts`) now writes `externalReference` and, **necessarily** (not a style choice), guards the photo-row insert with `if (photoRows.length > 0)` — `.values([])` is a Drizzle error, not a no-op, and every prior caller always had ≥1 photo (`photos.required`), so this path was simply never exercised before an imported draft's zero-photo array reached it. Proven by `tests/integration/broker-bulk-import-confirm.test.ts` ("creates a draft with zero photos and the external reference set").

  **`expiresAt` for a draft — a placeholder, stated rather than hidden:** `listing.expires_at` is NOT NULL at the schema level, so a draft row still has to satisfy it even though the spec says its real clock "starts at activation, not at import" (tasks.md 9.18, not this slice). `confirmImport` writes `expiresAt: publishedAt` (a zero-duration placeholder) — a draft is excluded from every expiry-driven query by its `status` alone, so this value carries no meaning before 9.18/9.19 recomputes both `publishedAt` and `expiresAt` from the real activation moment.

  **The account-contact guard the orchestrator's prompt asked for by name, built and fail-closed:** `listing.contact_method`/`contact_value` are NOT NULL; `user.contact_method`/`contact_value` (the CSV never carries contact — dropped like `publisher_type`, per "Accepted CSV Structure") are nullable. **`src/modules/broker-bulk-import/domain/import-account-contact.ts`** (`resolveImportAccountContact`, pure — `null` if the account, its method, or its value is missing/blank) → **`application/ports/import-account-contact.port.ts`** → **`infrastructure/drizzle-import-account-contact.ts`** (`DrizzleImportAccountContact`), checked ONCE per import inside `runImportValidation`, before a single byte of the file is parsed, throwing `ImportMissingAccountContactError` (`run-import-validation.ts`) — fifty rows that could never be activated is the worst available outcome, so the whole import is refused up front with an actionable message. Proven by `import-account-contact.test.ts`, `run-import-validation.test.ts`, `validate-import.test.ts`, `confirm-import.test.ts`, and `tests/integration/broker-bulk-import-confirm.test.ts` ("refuses the whole import when the account has no default contact configured" — asserts zero rows written).

  **Whole-file validation before any write: validate-all-then-write, not a database transaction — decided and stated, per the orchestrator's explicit ask.** `runImportValidation` decides every row's validity in memory BEFORE `confirmImport` writes a single one, so "a late invalid row does not leave earlier rows written" holds because row 40's validity is known before row 1 is ever saved — not because the writes share one transaction. A transaction across all rows was the alternative and was not taken: `ListingRepositoryPort.save` already owns its own transaction per listing (the row plus its photo rows), and each imported row is an independent draft — row 6 succeeding was never contingent on row 5 succeeding, so wrapping N independent listings in one bigger transaction would buy atomicity nothing here needs.

  Boolean-cell wiring (also this task, per the orchestrator's prompt): `parseImportBooleanCell` (built in 9.11, deliberately left unwired) is now called from `validateImportRows` for all five F6 columns — a value outside the `si`/`no`/`1`/`0` vocabulary becomes a row-level error (e.g. `hasPowerPlant.invalid`), never a silent coercion to `false`. Proven by `import-row-validation.test.ts`'s "boolean cells" describe block.

- [x] 9.16 RED: re-uploading an identical file creates no duplicates; a duplicate `referencia_externa` within one file rejects both rows. Within-file duplicate (pure, no DB): **`import-row-validation.ts`** proven by `import-row-validation.test.ts` ("rejects BOTH rows when referencia_externa is duplicated within the same file", "does not treat two blank external references as duplicates of each other") and end-to-end by `confirm-import.test.ts` ("creates no draft for either row when referencia_externa is duplicated within the file" — asserts `save` was never called). Cross-import duplicate (real Postgres, tasks.md 9.1's unique index): **`tests/integration/broker-bulk-import-confirm.test.ts`** ("creates no additional draft when the SAME external reference is confirmed again" — imports the same file twice against a real database and asserts exactly one row exists). Mutation-checked: disabling the within-file duplicate branch turns both the domain test and the confirm-import end-to-end test red.
- [x] 9.17 GREEN: idempotency enforced by the unique `(publisher_id, external_reference)` index, not by an application check. **`confirm-import.ts`** (`isUniqueViolation` — every valid row is simply inserted via `listings.save`, and Postgres' `23505` is caught per row rather than pre-checked with a SELECT). **A real, non-obvious defect found and fixed via this task's own integration test, recorded per AGENTS.md §5:** Drizzle's `node-postgres` driver wraps the raw `pg` error inside a `DrizzleQueryError`, which carries the original error — the one with `.code === "23505"` — on `.cause`, not on the error itself. A first version of `isUniqueViolation` checking only `error.code` (matching tasks.md 9.1's raw-`pool.query` precedent) passed every unit test against a fake but FAILED the integration test against real Postgres with the actual duplicate-key error escaping uncaught; `pgErrorCode` now checks both `error.code` and `error.cause.code`. Proven by `confirm-import.test.ts` ("treats a 23505 from the repository as an already-imported row, not a failure") and, decisively, by `tests/integration/broker-bulk-import-confirm.test.ts` against a real unique-constraint violation. Mutation-checked: forcing `isUniqueViolation` to always return `false` turns BOTH the fake-based unit test and the real-Postgres integration test red — the duplicate-key error escapes uncaught in both.
- [x] 9.18 RED: a draft is excluded from search, has no revealable contact, and has no expiry clock; its 30 days start at activation. **`tests/integration/listing-activation.test.ts`** against real Postgres ("is excluded from search, contact reveal, renewal, and moderation lookups"; "activates a draft with a photo: recomputes publishedAt/expiresAt from now, and it becomes visible") — a `WHERE` clause cannot be proven against a fake, so this is integration-only by design, matching design.md's own rule for D5/D6. Unit coverage for the DECISIONS around it (ownership, re-validation, timestamp recomputation) lives in **`src/modules/listing-publication/application/activate-listing.test.ts`**.

  **Correction beyond the task text, recorded per AGENTS.md §5 — an activation path was inferred work, not named by any task.** 9.18 itself says a draft's "30 days start at activation" and 9.27's e2e says "drafts activate", but no task before this one builds the transition — `stage: "draft"` (tasks.md 9.15) only ever let a draft be WRITTEN without photos; nothing re-validated it before it could become searchable. Without an activation use case, 9.18's own guarantee is unenforced and its scenario "Expiry clock starts at activation, not at import" is unwritable as a test. Built as **`activateListing`** — **`src/modules/listing-publication/application/activate-listing.ts`** (`ActivateListingNotFoundError`, `ActivateListingNotOwnedError`, `ActivateListingRejectedError`) + **`ports/listing-activation.port.ts`** (`ListingActivationPort` — a read port beside `ListingRepositoryPort`, per AGENTS.md §3, not a widening of it: `save` inserts, `findDraftById`/`activate` read-then-flip an existing row) + **`DrizzleListingActivation`** in `infrastructure/drizzle-listing-repository.ts`. Lives in `listing-publication` rather than `broker-bulk-import`: activation reuses `validatePublishableListing("activation")` verbatim, the same function `publishListing` already calls, and nothing about it is specific to how the draft was created — `broker-bulk-import` merely happens to be the only module that creates one today.

  **Ownership, same shape as 9.20's photo-ownership rule.** `ActivateListingNotOwnedError` is an explicit rejection (mirrors `process-uploaded-photo.ts`'s `key.notOwnedByPublisher`), checked BEFORE re-validation and BEFORE any write, so a stranger's draft never leaks whether it would pass or fail publish rules.

  **Timestamps recomputed, never restated.** `activateListing` calls `expiryFor({ publishedAt: now(), lastRenewedAt: null })` — `listing-lifecycle/domain/expiry.ts`'s own function, the same one `renewListing` uses — rather than a second `+ 30 days` literal. `DrizzleListingActivation.activate` is a compare-and-swap `UPDATE ... WHERE status = 'draft'` (same idiom as `renew`'s token burn), so a concurrent activation of the same draft cannot double-write; `activateListing` treats a lost race identically to "not found".

- [x] 9.19 GREEN: draft status handling across search, reveal, and lifecycle. Five paths were already safe by construction (slice A's own map, confirmed unchanged): `listing-search` (`status = 'active'`), `listing-discovery/drizzle-sitemap.ts`'s `activeListings`, `contact-reveal/drizzle-contact-reveal.ts`'s `findRevealable`, `listing-lifecycle`'s `markExpired`, and `noticeCandidates` (`inArray(status, ["active","expired"])`) — proven still true by `tests/integration/listing-activation.test.ts` rather than merely re-asserted.

  **The two soft spots `RenewableListing.status`'s own comment deferred to this task, both resolved as an explicit refusal.** `findRenewable` (`listing-lifecycle/infrastructure/drizzle-lifecycle.ts`) and `findModerated` (`listing-trust/infrastructure/drizzle-listing-moderation.ts`) previously looked a listing up by id with **no status filter at all** — reachable by nobody today (no renewal token is ever minted for a draft; no tenant is ever shown a draft's id to report), but unreachable by construction, not refused by a guard. Decided per AGENTS.md §7 ("Fail closed" — "prefer the shape where the failure mode is refusal"): both now carry `ne(status, 'draft')` in the `WHERE` itself, additive only — every existing case (`active`, `expired`, `hidden`) is untouched, proven by the full existing suites staying green (`renew-listing.test.ts`, `listing-report.test.ts`, `listing-restore.test.ts`, and their own integration tests). `findModerated`'s guard is belt-and-suspenders: `resolveRestoreOutcome` already fails closed on any non-`hidden` status, so this closes the path earlier (a draft now reads back as "not found" before reaching the domain decision) rather than being the only thing standing in the way. Mutation-checked: reverting either guard turns `listing-activation.test.ts`'s "excluded from search, contact reveal, renewal, and moderation lookups" red on exactly the two assertions it should (see apply-progress for the transcript).
- [x] 9.22 RED / 9.23 GREEN — **RECONCILED, not rebuilt (AGENTS.md §5: a checkbox is not evidence, and neither is its absence).** Both tasks were already fully delivered by an earlier slice, before 9.20/9.21 existed: **`src/modules/listing-publication/infrastructure/r2-photo-storage.ts`** + its test, `r2-photo-storage.test.ts`, carry every property these two tasks name, proven by test name: "derives the key from the publisher id and a random component" (server-derived key, per-account prefix — `deriveKey`, task 3.7's `PUBLISHER_ID_PATTERN` allowlist, never a caller-supplied path segment), "signs the configured bucket and the derived key, **never a caller-supplied one**" (9.22's first half — the client cannot choose where its bytes land, `UploadTarget`'s own docstring: "makes 'the client chose where its bytes land' unrepresentable instead of merely discouraged"), "pins the exact byte length when the caller declares one" and "refuses a declared length above the ceiling instead of signing it" (9.22's second half and 9.23's ceiling), "pins the content type into the signature" and "refuses a content type the derivative pipeline cannot decode" (fixed content-type), "expires the grant after the short TTL and says when" (`PRESIGNED_UPLOAD_TTL_SECONDS = 300`), "reaches no network to hand out a target" (every guarantee provable without R2, a bucket, or a credential). Confirmed by re-running the full 21-case suite in `r2-photo-storage.test.ts` before marking this — all pass unchanged; no second presign path was written.

  **One documented deviation from the task's literal wording, already recorded in the file itself and carried forward here rather than re-litigated:** the task says `content-length-range`, which is an S3 POST-policy field R2 does not implement (R2 has no `POST Object` at all). What ships instead — a SigV4 presigned PUT with `ContentLength` signed into `X-Amz-SignedHeaders` — is the file's own documented substitute: a body of any *other* declared length fails the signature at R2's edge, which is the same guarantee `content-length-range` would have provided, expressed the only way this transport allows. `read()` is the backstop for an object with no declared length. Both halves are separately test-proven ("pins the exact byte length..." / "refuses an oversized object without downloading it").

- [x] 9.20 RED / 9.21 GREEN: draft-ownership authorisation on photo attachment; broker B cannot attach a photo to broker A's draft. Genuinely new — no path existed to put a photo on a listing that already exists; `publishListing` only ever wrote photos once, bundled with the listing's own creation. **`src/modules/listing-publication/application/attach-photo-to-draft.ts`** (`attachPhotoToDraft`, `AttachPhotoToDraftNotFoundError`, `AttachPhotoToDraftNotOwnedError`, `AttachPhotoToDraftLimitReachedError`) + **`ports/listing-photo-attachment.port.ts`** (`ListingPhotoAttachmentPort` — a port beside `ListingRepositoryPort`, AGENTS.md §3, not a widening of it: `save` inserts a brand-new listing plus all its photos in one transaction; this appends ONE photo to a row that already exists) + **`DrizzleListingActivation.attachPhoto`** in `infrastructure/drizzle-listing-repository.ts` (the same class already implementing `ListingActivationPort` now also implements `ListingPhotoAttachmentPort` — one adapter, two narrow interfaces, no duplicated query). Proven by **`attach-photo-to-draft.test.ts`** (6 cases, fakes, including "throws AttachPhotoToDraftNotOwnedError for another broker's draft, and never touches storage" and "reuses the SAME upload guard the single-listing flow uses") and, because a fake proves the code path and not that Postgres actually refuses it (slice C's `isUniqueViolation` lesson, slice D's `WHERE`-clause lesson), **`tests/integration/listing-photo-attachment.test.ts`** against real Postgres — "refuses another broker's draft — broker B cannot attach a photo to broker A's draft (tasks.md 9.20)" seeds broker A's real imported draft via `confirmImport` and asserts broker B's session is refused and writes zero rows, plus the ceiling and success paths against a real table.

  **Reused, not restated, exactly as directed.** `createUploadTarget` (task 3.7) issues the upload target unchanged — no second presign path. `processUploadedPhoto` (task 3.7) is called VERBATIM for byte-level inspection, derivative generation and promotion — its own docstring had already anticipated this exact reuse: "a separate step... because the broker importer (Phase 9) uploads photos in a second phase with no publish form anywhere near it." `MAX_PHOTOS_PER_LISTING` is imported from `publishable-listing.ts`, not restated.

  **Decision recorded rather than made by accident, per the orchestrator's explicit ask.** This path accepts photos ONLY for a `draft` — `ListingActivationPort.findDraftById` is scoped to `status = 'draft'` in its own `WHERE`, so an `active` listing cannot reach this function at all. No task before 9.24, no spec scenario, and no design.md section asks for editing an already-live listing's photo set, and doing so would require deciding semantics (reordering, replacing, re-checking the ceiling against a searchable row) nothing here names. Widening this is a deliberate, reviewable change to `findDraftById`'s own `WHERE` later, not a silent gap now.

  **Mutation-checked**, both restored after confirming red: forcing the ownership comparison to `false` turns exactly "throws AttachPhotoToDraftNotOwnedError for another broker's draft..." red (unit); relaxing the ceiling from `>=` to `>` turns exactly "throws AttachPhotoToDraftLimitReachedError at the ceiling..." red (unit, and separately reproduced against real Postgres in the integration suite's ceiling test).

  **A genuine, pre-existing gap found by this task and NOT silently worked around (AGENTS.md §5).** The spec's own scenario "Duplicate photo rules still apply to imported drafts" names the cross-account perceptual-hash rule (design.md D4, `PhotoHashPort`) as something this path must reuse. It cannot be reused: **it is wired into ZERO listing flows in this codebase.** Task 4.7 ("E2E: publish → duplicate photo rejected cross-account... needs PR3") is still open — PR3 shipped, but 4.7 itself was never executed. `PhotoHashPort.findMatchesFromOtherPublishers` (task 4.5) and `DrizzlePhotoHash` (task 4.6) exist and are unit/integration-tested in isolation, but grep across `src/` finds zero callers from `publishListing`, zero callers from `processUploadedPhoto`, and — decisively — zero writers of `listing_photo_hash` anywhere outside that table's own adapter test. Wiring a duplicate check into this new path alone would compare every newly-attached photo against a table nothing has ever inserted into: a check that can structurally never find a match, which is not a guard, it is the appearance of one, and building it would have meant reporting a false "duplicate rules apply" here. Doing this honestly is task 4.7's own scope — a `PhotoHashPort` write method, hash computation wired into the one shared choke point both `publishListing` and this file already call (`processUploadedPhoto`), and dedicated tests proving it against real Postgres `bit_count` — genuinely separate, sizeable work left for that task rather than half-built silently inside this one. Flagged for the orchestrator to schedule. **Closed by 4.7 (see that entry)**: `processUploadedPhoto` now computes and checks the hash for every caller including this one, so the gap this note names no longer exists.
- [x] 9.24 RED / 9.25 GREEN: CSV writer neutralising leading `=`, `+`, `-`, `@`; template generator sharing the parser's column definition so the two cannot drift. **`src/modules/broker-bulk-import/domain/csv-output-writer.ts`** (`neutraliseCsvField`, `writeCsv` — the ONE place any downloadable CSV gets escaped, shared by the template today and by a future error report, per the spec's own "including the template and any error report") proven by **`csv-output-writer.test.ts`** ("prefixes a value beginning with '='/'+'/'-'/'@' with a single apostrophe", "does NOT over-neutralise: a trigger character in the MIDDLE of a value is left alone", RFC4180 quoting with doubled internal quotes). **`src/modules/broker-bulk-import/domain/build-import-template-rows.ts`** (`IMPORT_TEMPLATE_HEADER`, `buildImportTemplateRows` — reads `IMPORT_COLUMN_ALLOWLIST` verbatim, never restates it) → **`src/modules/broker-bulk-import/application/generate-import-template.ts`** (`generateImportTemplate`, composing the catalogue read with the pure row builder and the shared writer) proven by **`build-import-template-rows.test.ts`** and, decisively, **`generate-import-template.test.ts`**'s round trip: the generated template's example rows are fed byte-for-byte through the REAL `parseImportFile` (tasks.md 9.4-9.11) AND the REAL `validateImportRows` (tasks.md 9.12-9.15) — "the example rows are ACCEPTED, not merely parsed: zero validation errors through the real per-row validator".

  **Founder-level decision beyond the task text, recorded per AGENTS.md §5: one example row PER CITY, not per zone.** The spec asks the template to contain "the currently valid city and zone values", but `zone`'s own taxonomy can run to hundreds or thousands of rows across every estado/municipio/parroquia/elemento level (`schema.ts`'s own documented reality) — enumerating all of them in a downloadable file is neither practical nor what a broker filling a portfolio needs. Each generated row carries its city's REAL id and one of that city's REAL curated zone ids (`CataloguePort.listCities`/`listZones`, reused unchanged from `listing-catalogue` rather than widening `ZoneCataloguePort`, which is deliberately scoped to one city at a time). A city with zero curated zones is skipped rather than emitted with a certain-to-fail zona cell.

  **The trap named in the prompt — neutralisation vs. the example row — was not dodged.** Every generated example title deliberately BEGINS with `-` (`"-Amplio inmueble en <ciudad>"`, a plausible real listing title), which trips `neutraliseCsvField`'s leading-`-` trigger in the shipped template itself, not only in a synthetic test fixture. The round-trip test proves the composed pipeline still accepts it: `generate-import-template.test.ts` → "does NOT dodge the trap: the example title survives its own neutralisation and parses as text" asserts the re-parsed title is exactly `'-Amplio inmueble en Distrito Capital` — the apostrophe prefix plus the untouched original text.

  **What a neutralised value means coming back, decided and documented rather than left implicit:** the apostrophe is a single leading byte, never stripped back out on read. `csv-import-rows.ts` (tasks.md 9.11, shipped and out of THIS slice's scope) is deliberately left unchanged — undoing the prefix on read is a decision about every future upload, not only the template's own round trip, and belongs to whoever next touches the parser with that guarantee explicitly in view. A broker who re-uploads the template's example row completely unchanged stores that leading apostrophe as a literal character; a broker who replaces it with their own data (the documented, expected flow) never encounters it at all.

  **Beyond the spec's own four characters:** `neutraliseCsvField` also treats a leading TAB (`\t`) and CR (`\r`) as formula triggers, per the wider CSV-injection guidance the design.md threat-matrix row references — some spreadsheet import paths strip leading whitespace-like control bytes before deciding a cell's first "real" character, so a value stored as `\t=SUM(1,2)` could still be read as a formula once the tab is silently skipped. Included at zero cost: neither byte is meaningful content at the start of a text field.

  **Mutation-checked**, orchestrator's explicit ask: disabling each of the four spec-named trigger characters (`=`, `+`, `-`, `@`) individually in `FORMULA_TRIGGER_CHARS` turned exactly its own `csv-output-writer.test.ts` case red (and disabling `=` additionally broke the writer's own quoting-composition test), confirming no single character is redundantly covered by another. Hardcoding `IMPORT_TEMPLATE_HEADER` to drop `zona` (simulating one side drifting from `IMPORT_COLUMN_ALLOWLIST`) turned BOTH `build-import-template-rows.test.ts`'s own header-agreement test AND `generate-import-template.test.ts`'s round-trip tests red — proving the round trip catches drift at the composed level, not only at the array-equality level. All mutations reverted; the module's own 20 test files / 135 tests pass unchanged afterward.

  **Full-suite evidence:** `pnpm test:coverage` — 129 files / 1476 tests green, exit 0 (per-file `build-import-template-rows.ts` sits at 75% branch on its own — an unreachable "no example value for column" guard, same idiom as `present()`'s throw-by-name backstop from task 3.5 — but the aggregate `src/modules/*/domain/**` threshold the config actually checks stays well above 90%). `pnpm typecheck`, `pnpm lint`, `pnpm lint:tokens` all clean. `pnpm test:integration` — 24 files / 248 tests green (baseline unchanged; this slice touches no infrastructure/integration code).
- [x] 9.25b **(unplanned — not named by any Phase 9 task, the way 9.18/9.19's activation path was not.)** Bulk import resolves `ciudad`/`zona` by NAME instead of UUID. Slice C's own 9.12 entry flagged and deferred this verbatim: "`ciudad`/`zona` cells are treated as the same identifiers `CuratedZone` already uses; no name→id resolution layer built — deferred." Slice F then made the gap concrete: it emitted `city.id`/`zone.id` (raw UUIDs) into the downloadable template, so a broker filling the template by hand had to copy-paste two UUIDs into every one of fifty rows — every test passed and the feature was unusable. **`src/modules/broker-bulk-import/domain/resolve-import-locations.ts`** (`resolveImportRowLocation`/`resolveImportRowLocations`, `applyResolvedLocations`, `mergeLocationResolutionErrors`) runs BEFORE `validatePublishableListing` ever sees a row, scoped by the row's own resolved city (never a global zone search — «Centro» exists in both Maracaibo and Distrito Capital) and refusing rather than guessing when a name matches more than one place in one city (`zone.parent_id`'s own schema comment: "Chacao is a municipio AND a parroquia"). Everything past resolution — `import-row-validation.ts`, `confirmImport` — keeps working in ids, unchanged. **`build-import-template-rows.ts`** now emits `city.name`/`zone.name` instead of `city.id`/`zone.id`; **`generate-import-template.test.ts`**'s round-trip test (slice F) still passes, now proving the template's NAMES resolve back to real ids through the real resolver AND pass the real per-row validator, not merely that the template parses. Proven by **`resolve-import-locations.test.ts`** (29 cases: accent/case-insensitive matching, same-name-two-cities disambiguated by the row's own city, same-name-two-levels-one-city refused with both places named, unknown city naming which ones ARE valid, unknown zone naming the city, alias explicitly refused) and end-to-end by **`run-import-validation.test.ts`**'s five new cases.

  **Reuses `suggest-filters.ts`'s NFD-accent-stripping normalizer, not a second one** — that function's own comment warns against writing a second, so it was exported (`normalize`) and imported directly into `resolve-import-locations.ts`, the same cross-module domain-to-domain reuse this codebase already does for `slugify`.

  **Aliases decided, not left undecided (the orchestrator's explicit ask): NOT accepted.** The template already emits the canonical `zone.name`, so the documented happy path never needs one; the 3,547-entry alias index is IPOSTEL/OSM-sourced, not founder-curated, and accepting it here — with no human-in-the-loop confirmation step, unlike the publish form's fuzzy search — would widen, not narrow, the exact ambiguity problem this task closes.

  **`CatalogueZone` (listing-catalogue) widened, backward compatible:** `kind`/`category`/`parentName` added so an ambiguous match can be described ("el municipio Chacao y la parroquia Chacao") rather than merely flagged. `DrizzleCatalogue.listZones()` gained the same self-join `DrizzleZoneVocabulary.lookup` already runs. `zonesForCity` became generic over `{ cityId }` (matching `toSearchZones`'s existing shape) so `CityZoneSelect`, which only ever needed `id`/`name`/`cityId`, did not have to construct fields it never reads.

  **`ImportRowViolation` widened to include `string`, recorded as a deviation from the "stable, translatable codes" idiom `PublishViolation` documents:** unlike every other code in that union, WHICH city names are valid or WHICH places an ambiguous zone name matches is per-row data, not a fixed sentence a `Record<Code, Copy>` table could hold — and unlike `PublishViolation`, this pipeline has no consuming copy table yet (9.26 does not exist). The message renders once, in the domain, and travels as the reason itself.

  **Mutation-checked, all three confirmed and restored:** (1) disabling the city-name normalization call turned exactly the three accent/case-insensitive tests red; (2) raising the ambiguity threshold so two same-named zones no longer counted as ambiguous silently returned the FIRST match and turned red both the domain ambiguity tests and `run-import-validation.test.ts`'s end-to-end ambiguity case; (3) removing the `zone.cityId === city.id` scope from the zone filter (making it a global search) turned 10 tests red, including "never matches a same-named zone belonging to a different city" directly.

  **Full-suite evidence:** `pnpm typecheck` clean. `pnpm test:coverage` — 130 files / 1510 tests green, exit 0. `pnpm lint` / `pnpm lint:tokens` clean (9 pre-existing warnings + 2 infos untouched by this work, same baseline slice F documented). `pnpm test:integration` — 24 files / 248 tests green, unchanged baseline (three integration fixtures — `broker-bulk-import-confirm.test.ts`, `listing-activation.test.ts`, `listing-photo-attachment.test.ts` — updated to feed `confirmImport` real city/zone NAMES instead of raw ids, and wired `DrizzleCatalogue` as the new dependency).
- [x] 9.26 Import UI: upload, preview with per-row errors, confirm, draft list with photo attachment. **El motor entero ya estaba y no había puerta:** `src/modules/broker-bulk-import/` traía 25 archivos no-test —parseo, olfateo de delimitador, cotas de tamaño, validación fila por fila, resolución de ciudad/zona por nombre, idempotencia por índice único, confirmación, plantilla descargable, neutralización de fórmulas—, todos probados y **ninguno alcanzable desde un navegador**. Es la tercera vez en este trabajo (el anti-fraude de fotos y la pastilla de búsqueda ya lo fueron). **Entregado, con el archivo y la prueba de cada mitad:**
  - **La pantalla** — `app/importar/page.tsx` (lámina 14e), `app/importar/ImportarCartera.tsx`, `app/importar/VistaPrevia.tsx` (14g), `app/importar/importar.module.css`. Probado por **`app/importar/importar-contract.test.tsx`** — 9 casos con `renderToStaticMarkup`, o sea los bytes exactos que sale del servidor: *"la plantilla se baja con un enlace real: es el paso 1 y funciona sin JavaScript"*, *"el botón de revisar sale DESHABILITADO del servidor"*, *"y dice por qué, en vez de quedarse mudo"*, *"los dos límites se dicen ANTES de subir"*, *"nombra las seis columnas obligatorias con el nombre exacto que el parser espera"*, *"el botón de crear dice cuántas va a crear — no «Confirmar»"*, *"con cero filas listas NO hay botón de crear"*. Mutación: quitarle `archivo === null` a la condición del botón pone roja *"el botón de revisar sale DESHABILITADO del servidor"* y sólo ésa.
  - **La subida, la vista previa y el confirmar** — `app/api/bulk-import/route.ts` (reemplaza el `501` que la 9.3 dejó de andamio). Probado por **`app/api/bulk-import/route.test.ts`** — 10 casos: *"devuelve la vista previa con los errores por fila, y NUNCA confirma"*, *"confirma y devuelve cuántas se crearon y cuáles ya estaban"*, *"una acción que no es ninguna de las dos se rechaza sin tocar nada"*, *"sin archivo se rechaza sin tocar nada"*, más los tres rechazos de 14f (`columna-faltante`, `demasiadas-filas`, `cuenta-sin-contacto`) en 422 y la puerta en 401/403. Mutación: cambiar `listas: preview.validRows.length` por `preview.totalRows` pone roja *"devuelve la vista previa con los errores por fila, y NUNCA confirma"* y sólo ésa.
  - **La plantilla como paso 1** — `app/importar/plantilla/route.ts`, probada por **`app/importar/plantilla/route.test.ts`** (*"una cuenta habilitada se baja el CSV como archivo, no como texto en pantalla"*, *"con la bandera apagada responde 403 y NO genera la plantilla"*). Mutación: `attachment` → `inline` pone roja la primera y sólo ésa.
  - **La copia de los errores por fila** — `app/importar/import-copy.ts`, la contraparte que `import-row-validation.ts` nombró por escrito y no existía ("`app/publicar/violation-copy.ts` no tiene contraparte de importación; la UI de la 9.26 no existe todavía"). Probada por **`app/importar/import-copy.test.ts`**, incluida *"cubre TODOS los códigos que el publicador ya sabe traducir"*, que recorre en tiempo de ejecución las claves reales de `PUBLISH_VIOLATION_COPY` — `ImportRowViolation` incluye `string`, así que el compilador no puede exigir exhaustividad y lo hace esa prueba.
  - **La puerta, que no existía en ningún lado** — `src/modules/identity/domain/nav-account.ts` (`resolveAccountMenuItems`) + `components/organisms/Nav.tsx` + `app/mis-avisos/page.tsx`. **Defecto encontrado y corregido:** `canImportListings` se calculaba en el dominio, se probaba en el dominio y **no lo leía nadie** — `Nav.tsx` escribía su lista de filas a mano, así que "Importar cartera" era una fila que ningún dato podía encender, y la prueba «agencia y sesión dibujan la MISMA barra» seguía verde porque el panel del menú no se serializa en el servidor. Probado por **`src/modules/identity/domain/nav-account.test.ts`** (*"una cuenta de agencia recibe «Importar cartera» hacia /importar, después de «Mis avisos» (14b)"*, *"una cuenta de dueño NO recibe «Importar cartera»"*), **`components/organisms/Nav.test.tsx`** (*"le entrega al menú las filas que decide el dominio, «Importar cartera» incluida"*) y **`app/mis-avisos/puerta-de-importar.test.tsx`** (*"una cuenta habilitada ve «Importar cartera» como enlace real a /importar"*, *"una cuenta sin la bandera NO ve el enlace por ninguna parte"*, *"una cuenta que el adaptador no encuentra tampoco lo ve — falla cerrado"*). Mutaciones: `if (account.canImportListings)` → `if (true)` pone roja una sola prueba del dominio; reemplazar `items={resolveAccountMenuItems(...)}` por la lista escrita a mano pone roja una sola de `Nav`; sacarle `&& account.canImportListings` a `/mis-avisos` pone rojas las dos que niegan.
  - **Dónde cae la exención de JavaScript, y hasta dónde** (AGENTS.md §2 exime "bulk import (the file preview)"). La autorización **no** depende del script: `page.tsx` pregunta a `isBulkImportAuthorized` —la misma función de dominio que usa `authorizeBulkImport`— y devuelve `notFound()` si la cuenta no la tiene, y `/api/bulk-import` y `/importar/plantilla` la vuelven a exigir por su cuenta (esconder la UI no puede ser el único control). La plantilla es un `<a href>` real. Y el confirmar **falla cerrado**: el botón exige un archivo en memoria, que es lo único que sólo el script puede lograr, así que sin él nunca se habilita — y un `<noscript>` dice por qué, en vez de dejar la pantalla muda.
  - **DESVÍO 1, anotado y no resuelto en silencio (AGENTS.md §5): la lista de borradores con carga de fotos NO va en esta porción.** Es la mitad de la 9.26 que las láminas 14d y 14h dibujan, necesita un puerto de lectura de borradores por publicador y un disparador de activación que tampoco existen (`activateListing` y `attachPhotoToDraft` no los llama ninguna ruta), y sumarla habría llevado este PR bastante más allá de las 2.197 líneas a las que ya llegó. Queda como **tarea 9.28**, numerada y con alcance, no como una casilla que se marca sola.
  - **DESVÍO 2: la vista previa nombra la fila y el problema, no las celdas ofensoras.** La lámina 14g resalta referencia, precio, zona, habitaciones y título de cada fila con problema; `ImportRowError` sólo lleva `rowNumber` y `reasons`, y completarlas a ojo en la pantalla sería inventar el dato. Ensanchar el dominio para que las cargue queda como **tarea 9.29**.
  - **DESVÍO 3: el menú de cuenta dibuja tres filas y la lámina 14b dibuja cinco.** «Mis datos» y «Salir» son pantallas que no existen todavía, y una fila hacia un enlace roto es peor que una fila ausente (AGENTS.md §7).
- [x] 9.27 E2E: enabled broker imports → attaches photos → drafts activate → appear in city-scoped search. **Hasta acá cada eslabón estaba probado solo y ninguna prueba tocaba las juntas** — exactamente la clase de fallo que este repositorio ya pagó con el PR #103. Entregado en dos mitades, y se dice cuál es cuál:
  - **La mitad determinista, que corre en cada `push`** contra el Postgres dockerizado y no depende de ningún despliegue: **`tests/integration/bulk-import-to-search.test.ts`**, 4 casos que recorren la cadena por los casos de uso REALES (`confirmImport` → `attachPhotoToDraft` → `activateListing` → `DrizzleListingSearch.search`), nunca por `INSERT` a mano: *"la cadena entera: la que recibe foto y se activa aparece en la búsqueda de SU ciudad; la que no, no"*, *"un aviso importado y activado NO se filtra en la búsqueda de otra ciudad"* (que es donde se prueba que la ciudad deducida de un NOMBRE en el CSV es la misma por la que la búsqueda después filtra), *"un borrador importado SIN foto no se puede activar, y sigue sin aparecer"*, *"una cuenta sin la bandera no importa nada, ni siquiera para previsualizar"*. Mutaciones: `status: "draft"` → `"active"` en `confirmImport` pone rojas 3 de las 4; sacar `eq(listings.status, "active")` del adaptador de búsqueda pone rojas 3 de las 4; pasar `"draft"` en vez de `"activation"` a la re-validación de `activateListing` pone roja **exactamente una**, la de la foto.
  - **La mitad de navegador**: **`tests/e2e/import-access.spec.ts`**, 2 pruebas × 2 proyectos (`chromium` y `crawlability`), **4 pasadas y 0 saltadas** — corren en todas partes porque sin cookie de sesión `auth()` nunca toca la base, el mismo precedente de `publish-access.spec.ts` y `nav-account-control.spec.ts`: *"importar cartera es refusada sin sesión, y recuerda a dónde ibas"* y *"la plantilla no se baja sin sesión: responde 401, no un HTML disfrazado de CSV"*. Mutación: cambiarle el `callbackUrl` a `page.tsx` pone roja la primera en los dos proyectos. **Lo que NO se probó acá y no se finge:** el caso "una cuenta habilitada ve la pantalla" necesita una fila de sesión real en Postgres (`strategy: "database"`) y este runner no tiene credenciales de escritura contra la base de una vista previa; está cubierto de forma determinista por `importar-contract.test.tsx` y `puerta-de-importar.test.tsx`. Se prefiere probar menos acá que dejar un `test.skip` que se reporte verde sin poder pasar nunca — que es lo que le pasó a `publish-access.spec.ts` durante varios PRs.
- [x] 9.28 **(nueva — la mitad de la 9.26 que se difirió con nombre y alcance.)** La lista de borradores con carga de fotos y activación (láminas 14d y 14h). **Es la CUARTA vez que este trabajo encuentra la misma forma de defecto** —código completo, probado y sin un solo llamador de producción: el anti-fraude de fotos (4.7), la pastilla de búsqueda, el motor de importación (9.26) y `canImportListings` fueron las tres anteriores—. Verificado antes de tocar nada, no heredado del reporte anterior: `rg` sobre `app/` y `components/` devolvía CERO llamadores de `activateListing` y de `attachPhotoToDraft`. **Entregado, con el archivo y la prueba nombrada de cada mitad:**
  - **La consulta que la 20.9 dejó escrita como inexistente** («la lista real de avisos necesita una consulta que todavía no existe») — **`src/modules/listing-publication/application/ports/publisher-listings.port.ts`** (`PublisherListingsPort.listByPublisher`, un puerto de LECTURA al lado de los otros dos y no un ensanche de ninguno, AGENTS.md §3) + **`infrastructure/drizzle-publisher-listings.ts`**. Probada contra Postgres real, no con un doble: **`tests/integration/bulk-import-to-search.test.ts`** → *«el tablero de una inmobiliaria nunca trae los borradores de otra»*. Mutación: cambiar el `leftJoin` contra `listing_photo` por `innerJoin` —que escondería exactamente los 38 borradores sin fotos que la lámina pone arriba— pone roja *«la cadena por la pantalla…»* y sólo ésa; sacar `publisher_id` del `WHERE` pone rojas dos.
  - **Las reglas, en el dominio** — **`src/modules/listing-publication/domain/publisher-listing-board.ts`** (`buildPublisherListingBoard`, `parsePublisherListingFilter`): qué estado tiene cada aviso, cuáles van arriba, cuántos hay de cada clase y cuántos esperan fotos. Probado por **`publisher-listing-board.test.ts`** (15 casos), incluidos *«los borradores van primero, después lo urgente, y las vencidas al final»* (la anotación al pie de 14d: *"los borradores van arriba: son los que no se ven, y por eso los que importan"*), *«un aviso que la base todavía llama activo pero cuya fecha ya pasó se lee como vencido»* (porque `markExpired` corre en un cron con `WHERE status = 'active'` y entre el vencimiento y el trabajo la fila miente), *«las cuentas de las fichas NO cambian al filtrar: cuentan todo, no lo mostrado»* y *«cualquier otra cosa cae en «todos», nunca en una lista vacía»* (falla cerrado, §7). **Cuatro mutaciones, cada una una sola prueba roja:** anular el adelanto por fecha; contar las fichas sobre lo filtrado; sacarle `MIN_PHOTOS_FOR_ACTIVATION` al conteo de borradores sin fotos; anular el orden de estados.
  - **De quién son los avisos** — **`application/list-publisher-listings.ts`**, probado por **`list-publisher-listings.test.ts`** → *«lee los avisos de la cuenta de la sesión, y el pedido no puede nombrar otra»*. El id sale de la sesión y no hay ningún parámetro que acepte otro, que es más fuerte que validarlo.
  - **La firma de subida para un borrador** — **`application/request-draft-photo-upload.ts`**, probada por **`request-draft-photo-upload.test.ts`** → *«refusa el borrador de otra cuenta, y nunca firma un permiso para él»* y *«firma con el publicador de la sesión, el tipo pedido y el largo exacto»*. Existe porque `app/publicar/fotos/actions.ts` firma contra el borrador de una COOKIE del formulario de publicar, y un borrador importado no tiene cookie.
  - **Las dos llamadas que no existían** — **`app/mis-avisos/actions.ts`** (`activarBorrador`, `pedirDestinoDeFoto`, `adjuntarFotoAlBorrador`), probadas por **`app/mis-avisos/actions.test.ts`** → *«llama a activateListing con el id del formulario y vuelve a la lista»*, *«cuando el validador se niega, vuelve con el aviso y sus códigos, no con una frase»*, *«cualquier otro error sube, y no se convierte en una negativa dibujable»*, *«adjunta por attachPhotoToDraft y refresca la lista, que cuenta fotos»*. Mutaciones: tragarse cualquier error, no hacer viajar la negativa, y cruzar la fecha sin serializar — una prueba roja cada una.
  - **La pantalla** — **`app/mis-avisos/page.tsx`**, **`FotosDelBorrador.tsx`**, **`mis-avisos.module.css`**, probadas por **`app/mis-avisos/mis-avisos-contract.test.tsx`** (11 casos con `renderToStaticMarkup`, o sea los bytes exactos del servidor): *«dice cuántos avisos hay y cuántos no se ven todavía»*, *«un borrador sin fotos lo dice, y ofrece subirlas»*, *«cada borrador trae un formulario real para activarlo, con su id adentro»*, *«cuando la activación se negó, dice por qué junto al aviso que la pidió»*, *«no dibuja la negativa de un aviso que no la pidió»*, *«las fichas son enlaces reales con su cuenta, y funcionan sin JavaScript»*, *«una cuenta sin avisos lo dice, en vez de dibujar una lista vacía sin explicación»*.
  - **La cadena entera, por donde el corredor la recorre** — **`tests/integration/bulk-import-to-search.test.ts`** → *«la cadena por la pantalla: el borrador importado sale en «Mis avisos», recibe foto, se activa y cambia de estado ahí mismo»*, que recorre `confirmImport` → `listPublisherListings` → `requestDraftPhotoUpload` → `attachPhotoToDraft` → `activateListing` → `listPublisherListings` otra vez → `search`. Los dos eslabones del medio de la 9.27 dejan de estar probados sólo al nivel de caso de uso: ahora se ejercitan **por** lo que la pantalla llama, no alrededor.
  - **La regla de activación NO se reimplementó.** La pantalla dibuja el botón de activar para TODO borrador y `activateListing` contesta re-validando con `validatePublishableListing("activation")`; ningún `if` de `app/` decide un permiso mirando `photoCount`. Lo único que se compartió es el número: `MIN_PHOTOS_FOR_ACTIVATION` salió a `publishable-listing.ts` (donde estaba como un `1` suelto dentro de la propia función) para que la cuenta de «38 borradores esperando fotos» y el rechazo del validador no puedan discrepar.
  - **CORRECCIÓN, anotada y no resuelta en silencio (AGENTS.md §5): las láminas dibujan el RESULTADO de la activación y nunca su disparador.** 14d muestra «Borrador · faltan fotos» + «Subir fotos», y 14h muestra el mismo aviso ya como «Activa · 4 fotos», sin un solo control entre medio. Leerlo como «adjuntar una foto activa sola» habría hecho de la activación un efecto invisible de una subida: `activateListing` puede NEGARSE (re-valida veinte reglas, no una), y una negativa sin superficie deja a la inmobiliaria con «foto subida» y un aviso que sigue sin verse, sin nada que reintentar. Se entrega como un `<form>` explícito, que además funciona con el script apagado — la exención de JavaScript de `/mis-avisos` (AGENTS.md §2) cubre la COMPRESIÓN de fotos, no la activación.
  - **DESVÍO: la ficha no ofrece «Renovar 30 días» ni «Volver a publicar».** 14c y 14d las dibujan; la única renovación que existe hoy es `/renovar/[token]`, que llega por correo. Un botón hacia una ruta que no existe es un enlace roto (§7, la misma decisión que la 9.26 tomó con «Mis datos» y «Salir»). Queda como **tarea 9.31**.
  - **`byteLength` subió de `CreateUploadTargetInput` (infraestructura) a `CreateUploadTargetRequest` (el puerto).** Era una extensión que sólo el adaptador declaraba, y una `application/` no puede importar `infrastructure/` para pedirla. Aditivo y compatible: `CreateUploadTargetInput` queda como alias, y las 21 pruebas de `r2-photo-storage.test.ts` pasan sin tocarse.
- [x] 9.29 **(nueva.)** `ImportRowError` lleva las celdas de su fila. **`src/modules/broker-bulk-import/domain/import-row-cells.ts`** (`importRowCells`, `offendingCellsFor`) + `cells`/`offendingCells` en `ImportRowError`, probado por **`import-row-cells.test.ts`** (7 casos) y, decisivamente, por **`run-import-validation.test.ts`** → *«una fila cuya zona SÍ resolvió lleva igual el nombre del archivo en su celda, no el id resuelto»*.
  - **Las celdas salen de la fila CRUDA, y ésa es toda la dificultad de esta tarea.** `applyResolvedLocations` (9.25b) reemplaza `ciudad`/`zona` por sus ids antes de validar, así que para cuando `validateImportRows` ve la fila el nombre que la inmobiliaria escribió ya no está — y para una fila cuya zona no resolvió no está en ningún lado. `run-import-validation.ts` pasa `rows` (las del archivo) a `validateImportRows` y a `mergeLocationResolutionErrors` como argumento aparte; las reglas siguen corriendo sobre `preparedRows`, en ids, sin cambio. Mutación: pasar `preparedRows` en vez de `rows` pone roja exactamente esa prueba y sólo ésa.
  - **El resaltado** — `offendingCells`. Los códigos estables los clasifica una tabla; los mensajes de ubicación viajan como frase escrita (la 9.25b lo documenta) y por eso la celda la nombra `mergeLocationResolutionErrors`, que sí sabe de qué habla. Probado por *«una falla de ubicación lleva la celda de zona del ARCHIVO y la resalta»* y *«una falla de ubicación con OTRO problema independiente resalta las dos celdas, sin repetir»*. **`zone` y no `city`, anotado:** la tabla de 14g no dibuja columna de ciudad, así que las dos fallas de ubicación se leen en la única celda que muestra dónde queda la propiedad.
  - **El contador que `import-copy.ts` decía por escrito que no podía tener** («ese 24 sale de un `PublishCopyContext` que la importación no tiene»): `importRowReasonText(reason, cells)` escribe «La descripción tiene 61 caracteres, hacen falta 120», con `MIN_DESCRIPTION_CHARACTERS` leído del dominio y no un 120 copiado. Probado por *«cuenta la descripción cuando la fila le dice cuántos caracteres trae»* y *«sin celdas no inventa el número: vuelve a la frase sin contador»*.
  - **CORRECCIÓN al texto de la tarea, anotada (AGENTS.md §5):** las cinco celdas que nombra —referencia, precio, zona, habitaciones, título— no pueden producir el «61». `ImportRowCells` lleva además `descriptionLength`, contado con `characterCount`, que se EXPORTÓ de `publishable-listing.ts` en vez de copiarse: contar puntos de código de un lado y unidades UTF-16 del otro es exactamente cómo un contador termina en desacuerdo con la regla que cuenta. Probado por *«cuenta la descripción en puntos de código, igual que el validador»* (un emoji cuenta 1, no 2).
  - **La pantalla** — **`app/importar/preview.ts`** (`CeldasDeFila`), **`app/api/bulk-import/route.ts`** (traduce los nombres del dominio a los de la lámina, una sola vez) y **`app/importar/VistaPrevia.tsx`**, probados por **`route.test.ts`** → *«la frase de la descripción corta llega con su número, como la escribe 14g»* y por **`importar-contract.test.tsx`** → *«cada fila con problema muestra sus celdas: referencia, título, precio, zona y habitaciones»*, *«resalta la celda ofensora, y sólo ésa»*, *«una celda vacía no se dibuja, ni siquiera resaltada»*. El resaltado es un `<mark>`, no una clase de color: es una afirmación sobre el texto y existe también para quien no lo ve.
  - **DESVÍO: se entrega la TARJETA de 14g, no su tabla de escritorio.** La lámina dice al pie *"en móvil no se convierte en tabla angosta, se vuelve tarjetas de error más un resumen de las correctas"*; la tarjeta dice lo mismo que la fila de la tabla y sirve a los dos anchos. La tabla de 996 px sin scroll lateral y el resumen por zona de las filas correctas quedan como **tarea 9.30**.
- [ ] 9.30 **(nueva.)** **Confirmada abierta el 2026-08-28 y nombrada como el trabajo más grande que queda de la fase 9: es una pantalla, no un botón.** El dato viaja entero desde la 9.29, pero lo que falta es el SEGUNDO diseño de la misma información —una tabla de siete columnas más un resumen agrupado por zona—, y por el modelo de costo que este mismo archivo estableció («una pantalla con N campos cuesta 55–65 líneas autoradas por campo, prueba y estilos incluidos») no entra junto con nada más. Va sola, con su propio presupuesto. La vista previa de 14g a 1280: la tabla de 996 px sin scroll lateral (`Fila · Referencia · Precio · Zona · Hab · Título · Problema`) y, debajo, el resumen por zona de las filas correctas («Chacao 9 · Altamira 8 · La Castellana 6…») con su «Ver las 38». El dato ya viaja entero desde la 9.29; lo que falta es el segundo diseño de la misma información.
- [ ] 9.31 **(nueva.)** «Renovar 30 días» y «Volver a publicar» desde `/mis-avisos` (láminas 14c y 14d). **LA DECISIÓN QUE BLOQUEABA ESTA TAREA YA ESTÁ TOMADA — fundador, 2026-08-27 — y queda escrita acá completa para que la porción que la construya implemente en vez de volver a decidir. Falta el código, no el criterio.**

  **(a) Qué botón dibuja cada estado.** Las láminas dan dos de las tres filas y el fundador decidió la tercera. De la lámina, leída y no recordada: «Vence en 3 días» → **Renovar 30 días**; «Vencida el 2 de agosto» → **Volver a publicar**; un aviso con días de sobra **no dibuja ningún botón**. La decisión del fundador parte en dos el estado vencido, y su razón es la que manda: **cuando las fotos ya se purgaron el botón dice «Volver a publicar» y no «Renovar», porque sin fotos no hay portada y sin portada el aviso no existe en la cuadrícula — «Renovar» prometería algo que no pasa.** O sea que la regla completa es:

  | estado | `photoCount` | acción |
  |---|---|---|
  | `expiringSoon` | — | **Renovar 30 días** |
  | `expired` | > 0 (dentro de los 15 días de gracia) | **Renovar 30 días** — renovar SÍ lo devuelve con su portada, que es justo lo que el correo promete |
  | `expired` | 0 (purgadas) | **Volver a publicar** |
  | `active` | — | ninguno, la lámina no dibuja ninguno |
  | `draft` | — | «Activar», que ya ship*a* |
  | `hidden` | — | ninguno: `DrizzleLifecycleListings.renew` deja `hidden` como está a propósito, así que «Renovar» prometería lo mismo que no pasa |

  Es una **extensión** de la lámina y no un valor local inventado (SISTEMA.md), y queda anotada como tal: la lámina no distingue el estado de las fotos, el fundador sí.

  **(b) La autorización: por sesión, y NO se fabrica un token.** El fundador corrigió esto expresamente. El token firmado existe porque **quien abre ese correo no tiene sesión**; desde `/mis-avisos` la sesión ya identifica a la persona, y el aviso lo nombra el formulario. **Fabricar un token para alguien que ya está adentro es inventarle una cerradura a una puerta abierta.** El id del publicante sale de la sesión y de ningún parámetro, igual que en `listPublisherListings` y `activateListing`.

  **(c) Y aun así se conserva el `UPDATE` condicionado, que es la propiedad 3 de `renewal-token.ts` un nivel más arriba.** Sin token no hay `expectedExpiresAt` que firmar, pero el problema que ese `WHERE` resolvía —dos envíos que renuevan dos veces y regalan 60 días— sigue existiendo con un doble clic. La condición se saca del propio dato que la operación cambia, exactamente como allá: `WHERE id = ? AND publisher_id = ? AND status NOT IN ('draft','hidden') AND expires_at <= (now + EXPIRY_NOTICE_WINDOW_DAYS)`. El techo es la MISMA ventana de 5 días que `stateFor` usa para dibujar «Vence en N días» y que `noticeCandidates` usa para mandar el correo, así que el botón aparece exactamente cuando el producto ya está pidiendo renovar; y un segundo clic encuentra `expires_at = ahora + 30`, que ya pasó el techo, afecta cero filas y se lee «ya está renovado». **Ni tabla de tokens quemados, ni leer-y-después-escribir, ni ventana entre las dos.** El puerto devuelve `boolean` por la misma razón que `LifecycleListingsPort.renew`: la firma obliga a que sea un `UPDATE` condicionado y no una lectura seguida de una escritura.

  **(d) Lo que falta construir, y por qué no entró en la barrida del 2026-08-28.** Un campo derivado en `publisher-listing-board.ts` (la regla de (a) es dominio y no puede vivir en la pantalla, AGENTS.md §1), un caso de uso `renewOwnListing` con la sesión primero, un método de puerto con su adaptador Drizzle y su prueba de integración contra Postgres, la Server Action con `<form>` de verdad —`/mis-avisos` funciona con el script apagado— y los dos botones con su copia. Contra el modelo de costo de este archivo son ~400 líneas autoradas por sí sola, y la barrida ya llevaba 271. Va con la 9.30 en el PR que cierre la fase 9. Hoy la única renovación sigue siendo `/renovar/[token]`, que llega por correo.

## Phase 10: Voluntary Contribution (PR10)

**D8 is decided: the Vercel Pro migration happens last, and the product launches on Hobby without the contribution invitation.** This phase may therefore be built and merged, but the invitation must not be live before that migration. It ships behind `CONTRIBUTION_INVITATION_ENABLED`, default off — a switch, not a memo, because "remember not to deploy this" is not a guarantee.

- [ ] 10.0 GREEN: `CONTRIBUTION_INVITATION_ENABLED` flag, default off; the invitation renders nowhere when it is unset
- [ ] 10.0b RED: with the flag unset, no contribution invitation appears on any surface, and no contribution route is reachable
- [ ] 10.1 RED: a crafted destination parameter is ignored; only the configured destination is served
- [ ] 10.2 GREEN: destination resolved from server configuration only — never from query, path, or body
- [ ] 10.3 RED: contact reveal completes with no contribution prompt appearing in between
- [ ] 10.4 RED: dismissing the invitation keeps it hidden for the rest of the session
- [ ] 10.5 GREEN: dismissible, non-modal invitation component
- [ ] 10.6 RED: no capability is gated on contribution, and no contributor state is stored against a user
- [ ] 10.7 GREEN: contribution page — payment method and destination shown on-page, external link or code, no payment fields
- [ ] 10.8 Confirm go/pivot reporting still returns unique tenant-listing reveal pairs, unchanged by contribution data

## Phase 11: Discovery & SEO Surfaces (PR11)

Depends on PR5 (search) and PR7 (lifecycle). Carries D11 and the performance budget.

**11.17–11.20 pulled forward, shipped as their own slice (`perf/budget-gates`, off `main`).** CI's `budget` job has carried two gates that could not fail since PR0c — each emits a GitHub warning saying so out loud (`.github/workflows/ci.yml`, `budget` job). Real read-path pages are about to land (PR2b's city/zone select, then PR3's publish flow); a budget that starts measuring after the app has gained weight cannot tell you what added it. Landing the gates now, ahead of Phase 3/5/11's own pages, is what makes them able to catch the first regression rather than the fifth.

- [x] 11.1 GREEN: URL scheme `/alquiler/<ciudad>/<zona>/<slug>-<id>`; filters as query parameters. **Partial, 2026-08-20** — the scheme ships as `src/modules/listing-discovery/domain/listing-url.ts` (`slugify`, `buildListingPath`, `listingIdFromSlug`), pulled forward out of Phase 11 because the listing detail page needs it and nothing else can be built on top of a URL that does not exist yet. The filters half already holds: search is a `GET` form and its state is the query string (5.7). **~~What is deliberately NOT done is the route~~ — corregido 2026-08-27, la nota estaba vencida.** Las tres rutas del esquema existen y se sirven: `app/alquiler/[ciudad]/page.tsx`, `app/alquiler/[ciudad]/[zona]/page.tsx` y `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx`. Lo que sigue sin cerrarse es **el deber canónico que esta misma tarea describe abajo**, y se saca a su propia tarea (11.21) en vez de cerrarse en silencio junto con la ruta: cerrar la 11.1 con la ruta construida daría por probado un redirect que nadie probó. **The design decision worth carrying forward: only the id identifies a listing.** City, zone and slug are for a crawler and for a person deciding whether to tap a link pasted into a WhatsApp group; they carry no lookup power, so a retitled advert keeps its URL. The cost is a duty on the page — every path ending in the same id resolves to the same listing, so the page must rebuild the canonical path and redirect anything that differs, or one advert publishes unbounded duplicate URLs. **`listingIdFromSlug` returning `null` is the guard, not a convenience**: its result becomes a `WHERE id = $1`, so a segment that merely looks plausible is refused here rather than handed to the database. Four rules were mutation-checked rather than asserted and left at that — removing the regex's end anchor turned two tests red including the `… OR 1=1` segment, and dropping the lowercase, the NFD accent strip or the 60-character cap each turned exactly one red **Cerrada 2026-08-27**: lo único que quedaba abierto acá era el deber canónico, y se cerró en su propia tarea (11.21) con `app/alquiler/[ciudad]/[zona]/[slug]/ficha-vencida.test.tsx`, «un camino que no es el canónico redirige al canónico» y «es permanente: un rastreador tiene que poder mover el índice». El esquema de URL y los filtros en la query ya estaban desde la 11.2–11.6.
- [x] 11.2 RED: a copied filtered-search URL reopens with the same filter selection applied. **Cerrada 2026-08-27** por `app/alquiler/[ciudad]/busqueda-sin-javascript.test.tsx`, «una dirección con filtros reabre con esos mismos filtros puestos». Afirma las tres caras a la vez porque cada una sobrevive sola mientras las otras se pierden: las fichas de filtro puesto, los campos del panel con su valor escrito, y los resultados ya recortados. **Mutación**: `defaultValue={model.price.min}` → `defaultValue=""` en `components/organisms/SearchPanel.tsx` pone en rojo esta prueba **y sólo ésta**, 1 de 1791 — `components/organisms/SearchPanel.test.tsx` sigue verde, porque renderiza el panel con un modelo que arma la prueba y nunca preguntó si el valor de la dirección llega al campo
- [x] 11.3 RED: search results are present in the served response with scripting disabled. **Cerrada 2026-08-27** por el mismo archivo, «trae los resultados en el cuerpo de la respuesta». **`renderToStaticMarkup` es el punto entero**: devuelve el marcado del servidor sin marcas de hidratación y sin ejecutar nada del cliente, así que lo que se lee son los bytes que salen de la ruta — que es lo que ve un rastreador y lo que ve el navegador de WhatsApp cuando el bundle no llega. **Mutación**: `results.map(...)` → `[].map(...)` en `app/alquiler/[ciudad]/page.tsx` la pone en rojo (arrastra también a la 11.2 y la 11.4, que dibujan sobre las mismas tarjetas)
- [x] 11.4 GREEN: server-rendered search — no client-side filter or pagination layer. **Cerrada 2026-08-27** por dos pruebas del mismo archivo: «el recorte ocurre antes de la respuesta y no en una capa de cliente» y «pasar de página es seguir un enlace, con el script apagado». **Se afirma por el resultado observable y no por la ausencia de una cadena en un archivo**, y hay una razón medida para eso: la mutación que borra la paginación —`pagination.count > 1 && !pagination.beyondEnd` → `false`— deja **verde** a `pagination-contract.test.ts`, que es el archivo escrito para atrapar exactamente ese defecto, porque `aria-label="Paginación"` sigue estando en el fuente. La prueba nueva es la única de las 1791 que se pone roja. La otra mitad se prueba comparando dos cuerpos servidos: `?max=500` trae uno y `?min=1000` trae el otro, así que el recorte ocurrió antes de que la respuesta existiera. **Mutación**: `.search(criteria)` → `.search({ cityId: criteria.cityId })` pone en rojo esta prueba y la 11.2
- [x] 11.5 RED: a zone landing page's response body contains that zone's active listings without JavaScript execution. **Cerrada 2026-08-27** por `app/alquiler/[ciudad]/[zona]/zona-sin-javascript.test.tsx`, «trae los avisos activos de la zona en el cuerpo de la respuesta». **Mutación**: `results.map(...)` → `[].map(...)` en `app/alquiler/[ciudad]/[zona]/page.tsx` la pone en rojo, y con ella la guarda de la 11.6 — 2 de 1791, y **ninguna otra prueba del repositorio atrapaba que la página de zona dejara de dibujar sus avisos**
- [x] 11.6 RED: a `Maracaibo` zone landing page contains zero `Distrito Capital` listings. **Cerrada 2026-08-27** por el mismo archivo, «una zona de Maracaibo no trae ni un aviso de Distrito Capital». Afirma **cero de los de la otra ciudad**, no «algunos de los nuestros», y antes afirma que los nuestros están: dos listas vacías también son iguales, y sin esa guarda una página que no dibujara ningún aviso pasaría con las dos manos. Que el `WHERE` filtre lo prueba `tests/integration/listing-search.test.ts` contra Postgres real; lo que ésta cubre es el escalón que aquélla no alcanza — que la pantalla le pase al puerto la ciudad de SU ruta y dibuje la respuesta del puerto. **Mutación**: concatenar una segunda `search` con la ciudad hermana y sin zona —la fuga real que esta tarea existe para atrapar— pone en rojo **exactamente una prueba de 1791**, ésta. **Hallazgo arquitectónico anotado al pasar**: una fuga entre ciudades en esta pantalla sólo es alcanzable por el `cityId`, porque la página no tiene acceso a ningún conjunto de avisos que no sea la respuesta del puerto
- [x] 11.7 GREEN: statically generated landing page per curated (city, zone) pair. **Contradice al código, y hay que resolverlo antes de construirla (anotado 2026-08-27, fuera de la porción de la 11.2–11.6).** `app/alquiler/[ciudad]/[zona]/page.tsx` lee `searchParams`, así que Next la sirve por petición por construcción; y su propio comentario dice que la 14.24 borró el concepto de «página de zona» como vista aparte — la ruta de zona **es** la búsqueda de esa zona. Generarla estáticamente y a la vez aceptar filtros por query son dos cosas que no se sostienen juntas sin partir la ruta en dos. La decisión que falta es cuál de las dos gana **SUPERSEDIDA por la 14.24 — NO se construye, y ésta es la razón (resuelto 2026-08-27).** La contradicción anotada arriba se resuelve a favor del código: la 14.24 decidió que toda búsqueda vive en la ruta de su lugar y borró el concepto de «página de zona» como vista aparte. `app/alquiler/[ciudad]/[zona]/page.tsx` lee `searchParams`, así que Next la sirve por petición **por construcción**. Construir una página estática en paralelo daría una segunda dirección para contenido que ya tiene una — que es exactamente el contenido duplicado que la 14.24 quitó y que la 11.21 existe para impedir del lado de la ficha. Lo que la tarea quería comprar —que un rastreador vea los avisos de la zona sin ejecutar nada— lo compran la 11.5 (cuerpo servido) y la 11.16 (navegador con el script apagado), y lo compran mejor: miden el resultado en vez de suponer que el prerenderizado lo garantiza.
- [x] 11.8 RED: an expired listing URL returns a successful response stating expiry, with same-zone active listings shown **Cerrada 2026-08-27** por `app/alquiler/[ciudad]/[zona]/[slug]/ficha-vencida.test.tsx`: «responde con la pantalla del aviso y dice que venció» y «trae los avisos activos de la misma zona en el cuerpo de la respuesta». **La mitad de arriba ya estaba y el plan no lo decía**: el puerto devuelve el aviso vencido a propósito (`listing-detail.port.ts` lo documenta) y `ContactBlock` ya dibujaba el estado vencido con su fecha. Lo que faltaba —y lo que `design.md` llama la conversión que rescata al visitante más valioso que el sitio recibe— eran los avisos vivos DIBUJADOS ahí, no un enlace a otra pantalla. **Mutación**: `buildListingGrid(suggestions.listings, …)` → `buildListingGrid([], …)` pone en rojo 4 de 1828, todas de esta pantalla. Y se camina en un navegador con el script apagado: `tests/e2e/camino-de-lectura-sin-javascript.spec.ts`, «un aviso vencido responde 200, dice que venció y ofrece los activos de su zona»
- [x] 11.9 RED: the expired listing page carries `noindex` and its URL is absent from the sitemap. **Reconciled 2026-08-24 — built, plan was stale.** Both halves, and they live in different places on purpose. The page: `listing-discovery/domain/listing-structured-data.ts` decides indexing, tested by "saca del índice el aviso cuyo estado ya dice vencido" AND "saca del índice el que todavía dice active pero cuya fecha ya pasó" — the second is the one that matters, because a listing whose clock ran out is expired whether or not a job has relabelled it yet. The sitemap: `tests/integration/sitemap.test.ts` "deja fuera el vencido y el oculto" against real Postgres. A third test pins that `noindex` still lets links be followed — de-indexing a page is not the same as cutting it off.
- [x] 11.10 RED: a zone with no active listings widens to the same city only; a city with none shows no suggestions at all **Cerrada 2026-08-27** por `src/modules/listing-discovery/domain/listing-suggestions.test.ts` —«una zona sin avisos activos pide ampliar» y «una ciudad sin avisos activos no sugiere nada»— y por `application/suggest-active-listings.test.ts`, «amplía a la ciudad cuando la zona no tiene avisos activos» y «una ciudad sin activos no sugiere nada, y no consulta una tercera vez». **«Nunca más allá de la ciudad» no es una promesa del archivo: es el tipo.** `suggestFromZone` puede devolver `{ kind: "widen" }`; `suggestFromCity` devuelve un resultado y no tiene esa forma, así que un tercer paso no es una decisión que alguien pueda tomar mal — es una rama que no existe. En la pantalla lo mide `ficha-vencida.test.tsx`, «ni una sugerencia de Distrito Capital en una ficha de Maracaibo», que además afirma contra el puerto que las DOS consultas llevaron la ciudad de la ruta
- [x] 11.11 RED: an anonymous visitor sees no WhatsApp value on any suggested listing **Cerrada 2026-08-27** por `ficha-vencida.test.tsx`, «ningún valor de contacto viaja en la respuesta, ni del aviso ni de los sugeridos». **La garantía no es «no lo dibujamos»: es que no hay dónde dibujarlo**, que es el idiom que el §7 del `AGENTS.md` ya fija — el estado bloqueado no tiene propiedad `value`, `ListingSearchResult` no declara contacto y `GridCard` tampoco. Para que eso se MIDA en vez de suponerse, la prueba le pega un teléfono encima a la fila del detalle Y a cada fila sugerida. Del lado del caso de uso lo sostiene «no deja pasar un valor de contacto que venga pegado a la fila», y por eso `project()` copia campo por campo en vez de esparcir la fila: **mutación** — cambiar esa proyección por `{ ...listing }` pone en rojo exactamente esa prueba, 1 de 1828
- [x] 11.12 GREEN: `SuggestActiveListingsUseCase` — zone first, then city, never beyond the city **Cerrada 2026-08-27** como `src/modules/listing-discovery/application/suggest-active-listings.ts`, probada por «busca en la zona del aviso y no amplía cuando encuentra» y «nunca le pregunta al puerto por otra ciudad». **Reusa `ListingSearchPort` en vez de abrir un puerto propio**, y ésa es la decisión que vale la pena cargar: aquél ya filtra `status = 'active'` sin condiciones (5.5/5.6) y exige un `cityId` no anulable, así que el aislamiento no hay que volver a ganárselo —`tests/integration/listing-search.test.ts` ya lo prueba contra Postgres real— y la segunda consulta se hace SÓLO si la primera no alcanzó. El catálogo se lee únicamente en la rama ampliada, que es la rara: nombrar las otras zonas de la ciudad es lo único que la zona propia no puede contestar sola. **Mutación**: quitarle la zona a la primera consulta, o volver incondicional la ampliación, pone en rojo esas pruebas y ninguna otra
- [x] 11.13 GREEN: dynamic sitemap over active listings and zone pages; `robots` route; expired URLs dropped on expiry. **Shipped 2026-08-22.** `src/modules/listing-discovery/domain/sitemap.ts` (`buildSitemap`), `application/ports/sitemap.port.ts`, `infrastructure/drizzle-sitemap.ts` and `site-base-url.ts`; routes at `app/sitemap.ts` and `app/robots.ts`; `tests/integration/sitemap.test.ts` against real Postgres. **Three decisions worth carrying forward.** (1) **Zone pages are DERIVED from the active listings, never queried separately**, so `SitemapPort` has a single method and there is no `allZones()` — a zone page with no active listing is not expressible in the sitemap. Two queries could disagree, and the failure is us hand-delivering Google a URL that answers "todavía no hay avisos publicados acá". (2) **The freshness filter is two conditions, not one**: `status = 'active'` AND `expires_at > now()`. The status is moved by a scheduled job and a scheduled job runs late; in that window the row still says `active` while the ficha already draws itself as expired. Mutation-checked — dropping the date turns two integration specs red. (3) **`export const dynamic = "force-dynamic"` is load-bearing and was proven by mutation**: without it Next prerenders the sitemap at build time, and CI's `build` job runs with a deliberately unroutable `DATABASE_URL`, so removing the line fails the build with `getaddrinfo ENOTFOUND`. It is also simply correct — a sitemap baked at build is stale the moment the next listing is published. **`robots.txt` deliberately does NOT disallow the refined searches**: a URL blocked in robots.txt is never crawled, so Google never reads its `noindex` and an already-indexed page stays indexed forever. The `noindex` on filtered zone routes (14.24) is the mechanism; robots.txt only blocks what makes no sense to crawl at all (`/signin`, `/measure`, `/publicar`)
- [x] 11.14 GREEN: schema.org structured data on the listing detail page. **Reconciled 2026-08-24 — built, plan was stale.** `listing-structured-data.ts` emits the `RealEstateListing` JSON-LD at the listing's canonical address ("describe la página como un aviso inmobiliario en su dirección canónica"). Property types are translated to their schema.org accommodation ("traduce cada tipo de inmueble al alojamiento que le corresponde"), an unknown type degrades to the generic one rather than emitting a lie, and only declared attributes are listed — absent ones are omitted, never guessed. It throws without a site base URL, the same way the sitemap does: a canonical URL built on a missing origin is worse than none.
- [x] 11.15 RED: a listing below the minimum content threshold carries `noindex` (thin-content guard for bulk-imported portfolios). **Reconciled 2026-08-24 — built, plan was stale.** Same module, three tests pinning the boundary on both sides: "saca del índice un aviso por debajo del umbral de contenido", "indexa una descripción justo en el umbral", and "no cuenta el relleno de espacios como contenido" — whitespace padding is exactly how a bulk-imported portfolio would slip past a naive length check. Precedence is pinned too: "el vencimiento manda sobre el contenido delgado", so a listing that is both is de-indexed for the reason that will not change.
- [x] 11.16 RED: a zone landing page and the search results contain listings with JavaScript execution disabled (Playwright, scripting off). **Sigue abierta a propósito, y bloqueada por el arnés y no por la prueba (2026-08-27).** La mitad determinista la cubren la 11.3 y la 11.5 sobre el cuerpo servido y corren en cada `push`; lo que falta acá es el navegador de verdad con el script apagado, y hoy no puede correr: `/alquiler/**` consulta el catálogo antes de dibujar, y el respaldo local del `playwright.config.ts` compila contra una `DATABASE_URL` deliberadamente inalcanzable, así que sólo puede contestar 500. Escribir la prueba igual sería un séptimo `test.skip` — y una prueba que se salta ya se pudrió en silencio acá una vez (`publish-access.spec.ts` afirmaba un `testid` que se había mudado de ruta varias PRs antes y reportó verde todo el tiempo). **El camino existe y está verificado**: 11.22. **Y hay un segundo bloqueo que la 11.22 no cubre**: el job `e2e` de `.github/workflows/ci.yml` está condicionado a `github.event_name == 'pull_request'`, así que aun con el arnés esto no correría «en cada push» sin mover el job — y los minutos de Actions están medidos en este repositorio, de modo que moverlo es una decisión de costo del fundador y no una consecuencia técnica **Cerrada 2026-08-27** por `tests/e2e/camino-de-lectura-sin-javascript.spec.ts` en el proyecto `crawlability` (`javaScriptEnabled: false`): «la página de zona trae sus avisos y sus enlaces», «los resultados de la ciudad traen avisos» y «una zona de Maracaibo no dibuja un aviso de Distrito Capital». El bloqueo del arnés lo levantó la 11.22. **El segundo bloqueo se resolvió en vez de gastarse**: corre por PR y no por push (decisión del fundador, 2026-08-27), porque el job `e2e` está condicionado a `pull_request` y eso alcanza —lo que tiene que morder es la fusión— mientras la mitad determinista (11.3, 11.5) sigue corriendo en cada push. **Y no puede pudrirse detrás de un `test.skip`**: sin arnés y con `CI` puesto, el archivo FALLA en vez de saltarse. **Mutación**: `cards.map(...)` → `[].map(...)` en la página de zona pone en rojo 2 de 23 en un navegador de verdad
- [x] 11.17 GREEN: `scripts/budget-bundle.ts` reads `.next/app-build-manifest.json`, gzips every JS file for each read-path route (a manifest key ending `/page`, excluding `(auth)`/`/measure`/`_not-found`), and exits non-zero when the worst one exceeds 30 KB. No read-path route exists yet, so it falls back to the shared framework/runtime baseline (the floor every future one inherits) — measured **99.91 KB gzip against the 30 KB budget: FAIL today**, before a single line of application code exists. This is not "passes because the app is empty" — it is the opposite finding: Next.js 15 + React 19's own App Router client shell (`chunks/424-*.js` 45.8 kB + `chunks/b5b3cec6-*.js` 54.2 kB, gzip, present on literally every route including a bare route handler) already costs more than 3× the budget by itself. Flagged as a risk for the founder/design owner — see apply-progress
- [x] 11.18 RED: real conversion captured live. A temporary route (`app/budget-proof-temp/page.tsx`, never committed) measured **100.04 KB gzip** as a pure server component; adding one `"use client"` child (`useState` counter) measured **100.18 KB gzip** — a real, reproducible increase attributable to exactly that route, proving the script's differential detection (not just the absolute-threshold check). Reverted; `app/` carries zero net diff (`git status --porcelain -- app/` empty)
- [x] 11.19 GREEN: `lighthouserc.json` — mobile, simulated throttling matching Lighthouse's documented `mobileSlow4G` profile (RTT 150 ms, ~1.6 Mbps down — the profile historically named "3G" before Lighthouse 6's terminology update). `largest-contentful-paint` asserted globally at ≤2500 ms; `total-byte-weight` asserted via `assertMatrix` at ≤512000 B (500 KB) for detail-shaped URLs (`/alquiler/<ciudad>/<zona>/<slug>-<id>`) and ≤153600 B (150 KB) for search/zone-landing-shaped URLs (`/alquiler/<ciudad>[/<zona>]`) — both patterns are forward-looking assumptions about Phase 5/11's URL scheme (design.md D11, tasks.md 11.1) and match no URL today, since neither page exists. Cannot be verified locally (needs a deployment) — this config's first real execution is CI, exactly as the D5 integration test was handled
- [x] 11.20 GREEN: `.github/workflows/ci.yml`'s `budget` job now runs `pnpm build && pnpm run budget:bundle` and `pnpm run budget:lighthouse` unconditionally — the two "cannot fail yet" warning branches are removed. Both DATABASE_URL/AUTH_SECRET placeholders (matching the `build` job) were added to the `budget` job, which previously never actually ran `pnpm build` (the warning branch always fired, since neither script existed) and so never needed them

- [x] 11.21 **El deber canónico de la 11.1, como tarea propia (2026-08-27).** La 11.1 lo describe y lo deja escrito: «sólo el id identifica un aviso», así que **toda dirección terminada en el mismo id resuelve al mismo aviso**, y la página tiene que rearmar el camino canónico y redirigir lo que difiera — o un solo aviso publica direcciones duplicadas sin techo, que es justo lo que el rastreador castiga. Se saca acá porque la ruta ya existe (`app/alquiler/[ciudad]/[zona]/[slug]/page.tsx`) y el deber es de la página, no del esquema: cerrarlo dentro de la 11.1 daría por probado un redirect que nadie probó. Lo que hay que fijar antes de escribirlo: si el redirect es `308` (permanente, que es lo que un rastreador necesita) o `307`, y qué pasa cuando la ciudad o la zona del camino nombran un lugar que existe pero no es el del aviso — redirigir y no 404, porque el id sigue siendo válido **Cerrada 2026-08-27, y la mitad ya estaba construida — el plan estaba vencido.** `resolveListingRoute` (`domain/listing-detail-route.ts`) y su llamada en la ficha existían con diez pruebas de dominio. Lo que faltaba era la pregunta abierta de esta misma tarea, y se resolvió: **308 y no 307**. Un 307 es temporal, o sea que le pide al rastreador que CONSERVE la dirección vieja en el índice — exactamente el problema que esta redirección existe para resolver. `redirect()` de Next da 307; `permanentRedirect()` da 308. Probado en la pantalla por `ficha-vencida.test.tsx`: «un camino que no es el canónico redirige al canónico» y «es permanente: un rastreador tiene que poder mover el índice» — la segunda afirma que `redirect` NO se llamó, que es la única forma de que la diferencia entre los dos códigos se mida. La otra mitad de la pregunta (qué pasa si la ciudad o la zona nombran un lugar que existe pero no es el del aviso) ya estaba contestada y probada: redirige, no 404, porque el id sigue siendo válido. **Mutación**: volver a `redirect` pone en rojo esas dos y ninguna otra. En un navegador lo camina `camino-de-lectura-sin-javascript.spec.ts`, «un camino no canónico redirige permanentemente al canónico»
- [x] 11.22 **El arnés que le devuelve los dientes al proyecto `crawlability`, y el camino está verificado, no supuesto (2026-08-27).** Hoy siete especificaciones de `tests/e2e/` se saltan sin `PLAYWRIGHT_BASE_URL`, así que la afirmación del `AGENTS.md` §2 —«lo que convierte "anda sin JavaScript" de afirmación en medición»— no mide nada. La causa es estructural: la aplicación habla con Neon por HTTP (`@neondatabase/serverless` detrás de `assertPooledConnectionString`), y el Postgres del `docker-compose.yml` no puede hacerse pasar por eso. **Comprobado el 2026-08-27, corriendo de verdad**: `neonConfig.fetchEndpoint` acepta un endpoint propio, el protocolo HTTP de Neon es un `POST` con `{query, params}` y las cabeceras `Neon-Raw-Text-Output`/`Neon-Array-Mode` que devuelve `{command, rowCount, rows, fields, rowAsArray}` con las filas como texto crudo, y `pg` —que ya es dependencia de desarrollo— lo sirve entero con `rowMode: "array"` más un `getTypeParser` identidad. Un proxy de unas cuarenta líneas contra el contenedor del `docker-compose.yml` respondió `drizzle-orm/neon-http` sin tocar una línea de la aplicación. **Y la guarda del endpoint agrupado NO hay que debilitarla**: la cadena falsa puede llevar `-pooler.` en el nombre de host mientras el ruteo real viaja por `fetchEndpoint`, así que `assertPooledConnectionString` sigue rechazando un endpoint directo de Neon en producción exactamente igual que hoy. Lo que falta es el trabajo, no la respuesta: la costura en `src/shared/db/client.ts` (que debe aceptar el endpoint **sólo** si es de bucle local, o deja de ser una costura de prueba y pasa a ser una variable de entorno capaz de redirigir la base), el script del proxy, una semilla determinista de dos ciudades con fotos, el cableado del `playwright.config.ts` y el del job de CI. Es su propia porción: sumada a esta PR se lleva el presupuesto de 400 líneas por delante **Cerrada 2026-08-27, y el camino verificado resultó ser el camino.** Lo que se construyó: `scripts/neon-http-proxy.mjs` (traduce el HTTP de Neon a `pg`, incluidas las transacciones por lote), `src/shared/db/local-fetch-endpoint.ts` con su prueba, la costura de una línea en `src/shared/db/client.ts`, `scripts/seed-e2e.ts` (dos ciudades, cuatro zonas, seis avisos con portada y uno vencido), el cableado de `playwright.config.ts` como segundo `webServer`, y el contenedor de Postgres 18 más los pasos de migración y siembra en el job `e2e` de CI. **`assertPooledConnectionString` NO se debilitó**: `playwright.config.ts` le mete `-pooler.` al nombre de host de la cadena de prueba —`localhost-pooler.rentas.invalid`, que además no resuelve a ninguna parte— mientras el ruteo real viaja por `NEON_FETCH_ENDPOINT`, que sólo se acepta hacia el bucle local. **El resultado medido: de 7 especificaciones saltadas a 0.** Corriendo los dos proyectos, 46 pasan y ninguna se salta. **Y destapó una prueba podrida**, que es la segunda de este repositorio: `filtros-sin-javascript.spec.ts` afirmaba `getByText("Precio")` dentro del panel, que desde la 14.36 alcanza también al botón «Usar este precio» y caía por ambigüedad. Se saltaba en cada corrida desde que se escribió, así que nadie la vio. Corregida a `getByRole("heading")`. **Dos hallazgos del camino**: los ids de la semilla tienen que ser UUID porque `listingIdFromSlug` sólo reconoce esa forma —sembrar `e2e-mcbo-1` devolvía 404, la guarda de la 11.1 funcionando exactamente como está escrita—, y la semilla de e2e es un archivo aparte de `src/shared/db/seed.ts` a propósito: aquélla corre en cada despliegue de vista previa y sembrar fotos ahí pondría imágenes rotas en el único entorno que alguien mira

- [x] 11.23 **La ficha decide que un aviso venció mirando sólo `status`, y el índice lo decide mirando el reloj (anotado 2026-08-27, encontrado cerrando la 11.8).** `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` escribe `availability = detail.status === "active" ? "available" : "expired"`, mientras `resolveListingIndexing` saca del índice «el que todavía dice active pero cuya fecha ya pasó» — y la 11.9 dejó anotado que ésa es *la* prueba que importa, porque el `status` lo mueve un trabajo programado y un trabajo programado corre tarde. En esa ventana las dos mitades de la misma pantalla se contradicen: el `<head>` pide `noindex` por vencido y el cuerpo dibuja el bloque de contacto con llave, ofrece revelar y no muestra ni una salida. Quien llega desde un enlace de WhatsApp en esa ventana revela el contacto de un aviso que ya no está — que es justo el mensaje desperdiciado que la 5.5 evita del lado de la búsqueda. La corrección es una: que la disponibilidad salga del mismo dominio que ya sabe leer el reloj, no de un ternario en la página. Se saca aparte y no se cuela acá porque toca `contact-reveal` y porque el `WHERE` del sitemap (11.13) ya usa las dos condiciones — son tres lugares que tienen que decir lo mismo **Cerrada 2026-08-28, y los tres lugares resultaron ser cuatro.** La regla vive ahora en `src/modules/listing-discovery/domain/listing-availability.ts` (`resolveListingAvailability(listing, now)`), y el ternario de la página se borró en vez de corregirse: `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` la llama y no vuelve a mirar `status`. La prueba que lo fija es `ficha-vencida.test.tsx`, «un aviso que todavía dice active pero cuya fecha ya pasó se dibuja vencido», sobre los bytes servidos por `renderToStaticMarkup`; y su guarda obligatoria al lado, «un aviso active cuya fecha no pasó sigue ofreciendo el contacto», sin la cual una página que dibujara siempre el estado vencido pasaría la primera con las dos manos. **La ventana está medida y no supuesta**: `vercel.json` agenda `/api/jobs/expiry-reminders` con `0 13 * * *` —una vez al día—, `markExpired` corre adentro de ese trabajo (`send-lifecycle-notices.ts`) con `WHERE status = 'active' AND expires_at < now()` y nada más mueve el rótulo; como el aviso vence a los 30 días de la HORA de publicación, el hueco entre «vencido por reloj» y «vencido en la base» va de 0 a casi 24 horas. **Mutación**: devolver el ternario a la página pone en rojo esas dos y ninguna otra; romper `resolveListingAvailability` a mirar sólo el rótulo pone en rojo seis, en las cuatro superficies a la vez. **Y el barrido encontró un CUARTO lugar, que estaba mal y se corrigió acá**: `buildListingStructuredData` calculaba `const live = listing.status === "active"` para `offers.availability`, así que en esa misma ventana el JSON-LD declaraba `InStock` junto a un `validThrough` ya pasado — se contradecía a sí mismo en el único formato que un agregador lee sin abrir la página. Ahora recibe `now` como parámetro suelto, igual que `resolveListingIndexing`, y lo prueba «tampoco el que todavía dice active pero cuya fecha ya pasó». **Y un QUINTO, que NO se construyó y es una decisión pendiente del fundador**: `DrizzleListingSearch.search` filtra `eq(listings.status, "active")` y nada más, así que durante la misma ventana un aviso vencido sigue apareciendo en los resultados y un inquilino le escribe — exactamente el mensaje desperdiciado que la 5.5/5.6 dice evitar, corrido de la ficha a la lista. No se toca acá por tres razones: el propio comentario del archivo dice que `listing_city_status_idx` es `(city_id, status)` y que ese par «es también el camino de acceso», así que sumar `expires_at > now()` a la consulta más caliente del producto es una decisión de índice —y el índice es esquema, que esta porción tiene prohibido tocar—; necesita su prueba de integración contra Postgres real; y queda fuera del alcance escrito de esta tarea. **El SEO no se tocó** (11.8/11.9): la ficha vencida sigue respondiendo 200 con `noindex, follow`.
- [x] 11.24 **Un puerto propio para las sugerencias, si alguna vez se nota (anotado 2026-08-27, deliberadamente NO construido).** `suggestActiveListings` reusa `ListingSearchPort` y, sólo en la rama ampliada, `CataloguePort.listZones()` — que trae las casi 5.800 zonas del árbol territorial para nombrar hasta cuatro. Se aceptó a ojos abiertos: es la rama rara de la pantalla rara, la página de zona ya paga esa misma lectura en CADA búsqueda, y un puerto nuevo tendría que volver a ganarse contra Postgres real el aislamiento que `tests/integration/listing-search.test.ts` ya prueba. Si la latencia de la ficha vencida alguna vez aparece en una medición, la respuesta es un `findActive({ cityId, zoneId?, limit })` en `listing-discovery` que devuelva las filas ya unidas con el nombre de su zona — un viaje en vez de tres. Anotado para que sea una decisión y no un descubrimiento **Cerrada 2026-08-28 como DECISIÓN DE NO CONSTRUIR, no como trabajo hecho.** No existe `findActive` y no se escribió una línea de él: lo que se cierra es la deliberación, con su condición de reapertura escrita para que nadie tenga que volver a deducirla. **El disparador, textual: «si la latencia de la ficha vencida alguna vez aparece en una medición».** Hasta que eso pase, la respuesta es la de hoy. Se aceptó a ojos abiertos por tres razones que el fundador confirmó: es la rama rara de la pantalla rara; la página de zona ya paga esa MISMA lectura en CADA búsqueda, así que no es una clase de costo nueva sino la que el producto ya sostiene en su camino más transitado; y un puerto nuevo tendría que volver a ganarse contra Postgres real el aislamiento que `tests/integration/listing-search.test.ts` ya prueba. Si el disparador se cumple, la respuesta ya está escrita arriba: `findActive({ cityId, zoneId?, limit })` en `listing-discovery`, con las filas ya unidas al nombre de su zona — un viaje en vez de tres.

## Phase 11b: Operability — the schema arrives with the code, and a failure is legible (PR11b)

**Added 2026-08-20, after the fact rather than before it, and that is the point.** None of the 163 tasks above mentioned error handling, logging, observability or a deploy that touches the database — the words do not appear once. The cost of the omission was measured rather than imagined: **production ran four migrations behind from 2026-08-17 to 2026-08-20**. `build` was `next build` and nothing else, so no deploy had ever migrated anything. Search kept working, because it selects none of the missing columns. Signing in failed on `column "contact_method" does not exist`. Publishing was impossible — `listing_photo` did not exist at all. Nothing reported any of it, and the founder found it by trying to log in to his own product.

The honest ordering argument, recorded because it decides what ships first: **a logger would not have caught this.** The error was written; Vercel captured it; nobody reads logs on a pre-launch product. What closes the hole is making the schema arrive with the code that expects it.

- [x] 11b.1 `scripts/deploy-migrate.mjs` + `vercel-build`: apply pending migrations before the build that needs them. **Production only** — preview deployments share the production database until each gets its own Neon branch, and a preview build of an unmerged branch would apply that branch's migrations to live data. The skip is printed, never silent. A production build with no `DATABASE_URL` exits non-zero rather than deploying against an unknown schema
- [x] 11b.2 **El bloqueo se levantó derivando, no inventando (2026-08-28).** Existían tres huecos y ninguna lámina los dibuja: hoy un fallo de servidor muestra la pantalla por defecto de Next —*«Application error: a server-side exception has occurred»* más un hash, en inglés y sin una salida—. Ninguna lámina va a dibujarlos, así que se derivan del sistema, que es lo que `SISTEMA.md` manda: *«si una pantalla necesita un valor que el sistema no define, se extiende el sistema; no se inventa un valor local»*. **De dónde sale cada pieza, para que nadie lo lea como invención**: el título es el papel «Título de página» (20/700/1.25); el cuerpo es «Cuerpo» (15/400) dentro del **ancho de lectura de 520px** que ese mismo apartado fija (`ReadingWidth`); el contenedor es el de **1100px** de «Layout de escritorio»; la salida es el **nivel 1 «Acción»** de «Jerarquía de botones» y es la ÚNICA, porque un fallo no ofrece tres caminos; y el código va en `--mono`, que es el tipo que el sistema reserva para cifras que se leen carácter por carácter. La hoja no aporta un solo valor propio y `lint:tokens` lo verifica. `app/error.tsx`, `app/global-error.tsx` y `components/organisms/FailureScreen.tsx` + `.module.css`. **Pruebas nombradas**, `app/pantallas-de-fallo.test.tsx` sobre los bytes servidos (`renderToStaticMarkup`): «dice en castellano qué pasó, y no la frase por defecto de Next», «muestra el código que el visitante puede citarnos», «no filtra el mensaje ni la pila del error», «ofrece una salida que funciona con el script apagado» y —para la frontera global, que reemplaza el documento entero— «vuelve a poner los atributos de la raíz que el layout ya no está para poner». **Sin `reset()` a propósito**: es la única pieza que necesitaría JavaScript, y un botón que no responde con el script apagado es peor que no tenerlo. **Sin `Nav`**: el nav lee la sesión, y una pantalla de fallo no puede depender de lo que quizás falló. **Mutaciones**: quitarle `data-theme` a `global-error` pone en rojo su prueba y sólo esa; cambiar el enlace de salida por un `<button>` pone en rojo LAS TRES pantallas; dibujar `error.message` pone en rojo «no filtra el mensaje»
- [x] 11b.3 **Hecho, con una mitad medida que NO cierra y queda numerada (22.16).** `app/not-found.tsx`, derivado del mismo sistema que la 11b.2 y compartiendo su componente. Dice «No encontramos esa página» **sin decir por qué**, que es la 16.20: dado de baja, oculto por reportes o nunca existido se contestan igual, porque distinguirlos le entrega a quien sondea direcciones el dato exacto que le falta. Lleva `noindex, follow` escrito como etiqueta en el cuerpo —Next no exporta `metadata` desde este archivo y React 19 la iza al `<head>`—. **Pruebas nombradas**: `app/pantallas-de-fallo.test.tsx` → «dice que no encontramos la página, sin decir por qué», «ofrece una salida que funciona con el script apagado», «sale del índice»; y en un navegador con el script apagado, `tests/e2e/camino-de-lectura-sin-javascript.spec.ts` → «una dirección que no coincide con ninguna ruta responde 404 con pantalla y salida». **Lo que NO cierra, medido y no supuesto**: el `notFound()` que lanza una ruta dinámica —la ficha— lo dibuja Next del lado del CLIENTE, así que con el script apagado esa dirección sigue quedando en blanco (234 bytes de cuerpo, un límite de suspenso vacío y el contenido sólo en la carga de Flight). No lo causó este archivo: antes era la pantalla por defecto de Next por el mismo camino. Es la tarea **22.16**
- [x] 11b.4 **Hecho, y la juntura es el digest.** Una línea de JSON por fallo con la ruta, el digest que se le mostró al visitante y la causa. **Una sola costura y no tres**: `instrumentation.ts` exporta `onRequestError`, que es la única frontera que Next llama para las tres que pueden fallar —componentes de servidor, manejadores de ruta y acciones—, y llega con el MISMO digest que la frontera de error acaba de dibujar. **La decisión vive en el dominio y no en `app/`**: dar formato a una línea no es una regla de negocio, pero **qué puede aparecer en ella sí lo es**, con consecuencia de privacidad, y el suelo de cobertura del 90 % llega a `src/modules/` y no llega a `app/`. `src/modules/operability/domain/failure-report.ts`, al 100 %. **Cinco guardas, cada una con su prueba nombrada** en `failure-report.test.ts`: «lleva EXACTAMENTE el mismo código que la pantalla le mostró al visitante» (la juntura, probada como tal); «no deja pasar ninguna clave que no sea de las seis» —la forma cerrada, que es el idioma del estado de contacto sin propiedad `value`—; «no escribe la pila del error»; «tapa el correo y el teléfono que un mensaje de error haya arrastrado»; «de un valor lanzado que no es Error anota su tipo y nunca su contenido», porque `throw { token }` serializado entero pondría el token en el registro. Y en `instrumentation.test.ts`, la que justifica el archivo: «registra el PATRÓN de ruta y nunca la URL con el token adentro» — `/renovar/<token>` lleva **en el camino** la llave que renueva el aviso de otra persona, así que la ruta sale de `context.routePath` y no de `request.path`. Sin marca de tiempo: Vercel le pone la suya a cada línea. **Mutaciones**: cambiar `routePath` por `request.path`, quitar el tapado, serializar el valor no-`Error` y aceptar cualquier digest ponen en rojo una prueba cada una, la suya
- [x] 11b.5 **Hecho, y se probó construyendo el fallo en vez de afirmando que no está.** `scripts/schema-smoke.ts` compara lo que el código nombra —sacado de `schema.ts` con `getTableConfig`, no de una lista escrita a mano— contra `information_schema.columns`. **La decisión no es «comparar dos listas» sino en qué dirección**, y por eso vive en `src/modules/operability/domain/schema-drift.ts`: falta una columna que el código lee → despliegue roto; **sobra** una que el código todavía no lee → correcto, es el paso 1 del `añadir anulable → rellenar → NOT NULL` que este proyecto exige porque «no podemos borrar data real» (14.2, 11b.6), y un chequeo que fallara ahí refutaría cada migración en tres pasos. **No es un segundo guardián solapado**: lo invoca `scripts/deploy-migrate.mjs` justo después de migrar, así que hay un punto de entrada y dos preguntas —¿se puede migrar? y ¿quedó la base como el código la espera?—. **Prueba nombrada, contra Postgres real**, `tests/integration/schema-smoke.test.ts`: «no reporta deriva contra la base que las migraciones acaban de dejar», «caza un select que nombra una columna que la base no tiene» y «caza una tabla entera que la base no tiene» — las dos últimas afirman primero el mensaje **de Postgres** (`column "…" does not exist`, `relation "…" does not exist`, leído de `cause` y no del envoltorio de Drizzle) y después que el chequeo nombra lo mismo. **Y se corrió el script contra un esquema derivado a propósito**: renombrando `listing.contact_method` y soltando `listing_photo_derivative` en el contenedor desechable, `pnpm smoke:schema` salió con código 1 diciendo `listing: contact_method` y `listing_photo_derivative: LA TABLA NO EXISTE`, que es exactamente el fallo del 2026-08-17. **Mutaciones**: quitarle el orden estable pone en rojo su prueba; hacer que nada falte nunca pone en rojo tres; quitar el filtro de «sólo lo que falta» pone en rojo cuatro
- [x] 11b.6 **A migration must never destroy data**, as a gate rather than a promise (founder, 2026-08-20: "no podemos borrar data real"). `scripts/deploy-migrate.mjs` scans every migration file for `DROP TABLE|COLUMN|SCHEMA|DATABASE`, `TRUNCATE` and `DELETE FROM`, and refuses the deploy if it finds one. All eight migrations audited clean when this shipped, so the first destructive statement anyone generates stops a deploy instead of running against the catalogue — and `drizzle-kit` needs no instruction to be destructive, since removing a field from `schema.ts` is enough for it to emit `DROP COLUMN`. Comments are stripped before scanning, because several migrations *describe* a destructive statement they deliberately avoided, and a guard that fired on prose is a guard everyone learns to bypass. `ALLOW_DESTRUCTIVE_MIGRATION=1` is the deliberate, one-deploy escape hatch

## Phase 13: Not yet designed — logger and backups (founder, 2026-08-20)

Parked here on purpose rather than started. Both are real, both were requested, and neither has an answer yet that is worth committing to code.

- [ ] 13.1 **Logger.** The question is not "write failures down" — Vercel already captures stdout, and the four-day outage was written down the whole time. The question is what makes a failure *reach a person* on a product with no on-call and no traffic yet. Options to weigh before writing anything: structured JSON lines plus a Vercel log drain; an error-tracking service with an alert (Sentry's free tier covers this scale); or the cheapest honest thing, a scheduled check that pings the founder when the site stops answering. **The trap to avoid is building the part that feels like progress — a pretty logger — and still having nobody watching**
- [ ] 13.2 **Backups.** Nothing in this repository backs anything up today, and the catalogue is the product. Neon's own point-in-time restore is the obvious first answer and needs checking rather than assuming: what the free tier's retention window actually is, whether it survives a branch delete, and how long a restore takes. Then the question this phase exists for — is that enough on its own, or does a periodic dump belong somewhere the database provider cannot lose along with the database
- [ ] 13.3 Whatever 13.2 concludes, prove a restore once. **An untested backup is not a backup**, and the moment to discover that is not the moment it is needed



## Qué del código actual sobrevive al rediseño, y qué no (2026-08-22)

Medido sobre lo construido, no estimado. El fundador preguntó lo correcto: *"con el nuevo diseño hay cosas que ya posiblemente hicimos y que debemos hacer de nuevo"*. Sí — y esta lista existe para que nadie rehaga lo que no hace falta.

### Se rehace: la capa de entrega

| Pieza | Por qué |
|---|---|
| `components/molecules/ResultRow` | La fila se vuelve tarjeta de cuadrícula (14.25) |
| `components/molecules/SearchFilters` | Pasa a acordeón de 4 pasos con conteo en vivo |
| `components/layout/SidebarLayout` | La barra lateral de 240 px cambia por el panel de 3 columnas |
| `app/page.tsx` | Deja de ser los resultados y pasa a ser el inicio (14.21, 14.24) |
| `app/publicar/*` | Un formulario de dos pasos se vuelve nueve pantallas (Fase 18) |
| `app/(auth)/signin` | Gana el enlace por correo y sus dos presentaciones (Fase 15) |

### Sobrevive intacto, y es donde está el trabajo difícil

**Todo `src/modules/`**: las reglas de publicación con sus 27 códigos de violación, los puertos, los adaptadores de Drizzle y R2, el pipeline de fotos, la detección de duplicados por hash perceptual, el contacto con llave, el esquema de URL, el catálogo, el árbol territorial y los 3.547 alias. Más el esquema de base de datos entero y sus nueve migraciones.

**Y los átomos, que es lo que sorprende:** `Price`, `PublisherBadge`, `Label`, los tres botones y `Container` **no se tocan**. La paleta del diseño nuevo es idéntica a la que ya ship*a* — once de once valores, verificados uno por uno — y `design-contract.test.tsx` ya prueba estructuralmente lo que el diseño nuevo sigue pidiendo: el publicador distinguible en escala de grises, el precio con `tabular-nums`, el contraste AA en los dos temas, y ningún texto atenuado con `opacity`.

`DetailSplit` también se queda: sus `640px 420px` son exactamente lo que la ficha pide.

### La cuenta honesta

Se rehace la capa de entrega, que es cerca de un tercio del código. Se conservan las reglas, los datos y el contrato visual.

**Y no fue tiempo perdido.** Contrastar el diseño contra el código construido es lo que destapó cinco huecos de datos **antes** de escribir una pantalla: el tipo de propiedad, los cinco atributos, la unicidad de zonas que hacía imposible importar la taxonomía real, las seis zonas de Caracas archivadas en el estado equivocado, y los 179 topónimos enterrados. Las dos veces anteriores que este proyecto encontró un hueco así, fue **después** de construir.

## Phase 14: The search flow the founder specified (2026-08-21)

**Where this came from.** The founder delivered a functional specification for the mobile search flow — F1 to F15, with edge cases and ten acceptance criteria — plus a redesign of the list and filters (mobile and desktop). This phase is that document turned into work, checked line by line against what the code already does. It is deliberately written as *functionality*, not screens: the visual half arrives separately and must not be able to silently change a rule recorded here.

**What already holds, so nobody rebuilds it:** city isolation (F2, criterion 8) is guaranteed twice — the search port cannot express an unscoped query, and `listing_zone_city_fk` makes a cross-city row physically impossible; search state already lives in the URL as a `GET` with no client JS (F12, F14, criteria 5 and 7); publisher type is already visible and greyscale-distinguishable, with price above title (criteria 2 and 3); results already order by `published_at` descending (F9); the contact-reveal module already exists and is proven (F13's engine — only its screen is missing).

**The three contradictions, two of them RESOLVED by the founder on 2026-08-22:**

1. **RESOLVED — the grid wins, and `SISTEMA.md`'s row rule is retired.** That document states as a hard rule "Lista de filas, nunca grilla de tarjetas, en ningún ancho"; the founder overrode it deliberately after being shown the conflict. **The original argument was weight**, not taste: a row with a small thumbnail fits the 150 KB budget for 20 listings. The founder's own document reports today's results page at ~128 KB, so there is headroom — but a grid of 158 px photos spends it, and 14.5 (the budget gate) is what will say whether it fits. Two columns on mobile, three on desktop.

2. **RESOLVED — `/` becomes a home with four strips.** ~~results move to `/buscar`~~ — **corregido 2026-08-22**: la 14.24 borró `/buscar` y los resultados viven en `/alquiler/<ciudad>` y `/alquiler/<ciudad>/<zona>`. El documento maestro del fundador lo confirma en §7ter. `app/page.tsx` currently records the opposite as a deliberate decision ("there is no separate home page, and that is a decision rather than an omission"), and that comment must be rewritten rather than left contradicting the code beside it. What the old decision bought — the domain's strongest URL showing listings — is given up for what the strips buy: someone who arrives without knowing what they want sees supply immediately instead of an empty filter column.
3. **F13 says the number is "parcialmente oculto".** The shipped guarantee is stronger and deliberate: in the locked state the contact value is *unrepresentable* — it never reaches the browser at all, so there is nothing to un-hide in the HTML. Showing real digits is a different promise and has to be chosen, not slid into.

**A scheduling consequence of the grid decision, and it moves work earlier.** A card without an image reads as broken, so **the grid requires cover photos** — and today the search query never touches `listing_photo` while `ResultRow` renders a CSS placeholder. Phase 19's photo pipeline (four sizes, re-derivation, backfill) stops being a ficha concern and becomes a **prerequisite for the list itself**. Whoever plans the order should read 19a before starting 14.

### 14a. Data the product does not store yet

- [x] 14.1 `listing.property_type`, `NOT NULL` with **no default** — following `publisher_type`'s precedent in the schema, where the note records that a default is the silent failure mode. Taxonomy fixed by the founder (2026-08-21): `apartamento`, `casa`, `quinta`, `anexo`, `habitacion`. **`local comercial` was proposed and withdrawn**, and the withdrawal is load-bearing: it keeps the product residential, keeps `listing.rooms` `NOT NULL` without forcing a meaningless number onto a commercial unit, and keeps the detail page's four-cell stat strip (whose schema comment says it "draws four identical cells and has no empty state for one of them"). Do not re-add it without re-opening all three. **This is the THIRD instance of the same recurring gap** — `habitaciones`/`metros²`, then `baños`/`puesto` (3.15), now this — and the first one caught before building rather than after. **Hecho, migración `0008`.**
- [x] 14.2 Migration for 14.1 in three steps, because live rows exist and "no podemos borrar data real" is a gate (`scripts/deploy-migrate.mjs`, 11b.6): add nullable → backfill → set `NOT NULL`. **Hecho.** Tres pasos: nullable → backfill → `NOT NULL`, escrito a mano porque la salida de drizzle-kit habría fallado
- [x] 14.3 The six attributes of F6 — planta eléctrica, agua regular, amoblado, vigilancia 24 h, línea blanca (puesto de estacionamiento already ships as `parking_spots`). **As six boolean columns, not an attribute table**, and the reason is F6's own wording: attributes combine with AND ("piden todos, no cualquiera") and each must report how many of the current results have it. Six columns make that `COUNT(*) FILTER (WHERE amoblado)` — one pass, indexable. An attribute table makes it `GROUP BY … HAVING COUNT(*) = N` on every search: complexity paid on every query to save a migration done once a year. **Hecho.** Cinco columnas booleanas; `parking_spots` ya existía
- [x] 14.4 **DECIDIDA Y MOVIDA A LA FASE 25 el 2026-09-03: sale de la fase 14, que es la cuadrícula y el filtro, y no tenía nada que ver.** El texto original decía: *«A `pending_moderation` listing status. F1 excludes it from every collection; the enum today is only `active | expired | hidden`»* — y **ya estaba desactualizado**: el enum tiene CUATRO estados (`active | expired | hidden | draft`), no tres.

  **La decisión del fundador, y es al revés de lo que la tarea suponía.** La tarea daba a entender moderación previa para todos. **No**: los avisos se publican **en automático**. `pending_moderation` sólo alcanza a los avisos de un publicador **ya marcado**, y lo que marca a un publicador es que **le hayan ocultado un aviso por el umbral de tres reportantes distintos** (`AUTO_HIDE_REPORT_THRESHOLD`), no un reporte suelto.

  **Por qué tres y no uno, que fue la corrección del día.** El fundador lo planteó como «si un aviso es reportado». Con un solo reporte, **cualquiera hunde a un competidor con un clic**: `listing_report` garantiza un reporte por persona por aviso, así que nadie reporta dos veces — pero una cuenta nueva es gratis, y una inmobiliaria podía dejar a la de al lado con todas sus publicaciones futuras en una cola. Con el umbral de tres, la señal es «a esta persona le tumbaron un aviso», que es la misma que el sistema ya considera suficiente para ocultar, y no le regala a nadie un botón para hundir al vecino.
- [x] 14.5 **Cover photo in search results.** Today the search query never touches `listing_photo` and `ResultRow` renders a CSS placeholder — there is not a single real photograph anywhere in the results. F9 also requires that a listing with no photo never reaches the grid; the publish form already refuses one, but the broker bulk import (Phase 9) does not. **Hecho, PR #77/#83.** `coversFor` (plural, sin singular al lado) + `buildListingGrid`, en la zona, la ciudad y el inicio

### 14b. The search engine

- [x] 14.6 Multiple zones combined with OR (F4). `SearchCriteria` carries a single optional `zoneId`. **Hecho, PR #84.** `zoneIds` reemplaza a `zoneId`; una zona que ya no existe se descarta y el resto de la búsqueda sobrevive
- [x] 14.7 A `publisherType` criterion — "solo de dueños" (F6). The column exists; the filter does not. **Hecho, PR #84.**
- [x] 14.8 A `propertyType` criterion, from 14.1. **Hecho, PR #84.**
- [x] 14.9 Attribute criteria, AND-combined, from 14.3. **Hecho, PR #84.** Y sólo se puede pedir el `true`: `false` significa «no lo declaró», nunca «no lo tiene»
- [x] 14.10 Pagination (F10). The query has no `LIMIT` and no `OFFSET` — it returns the whole catalogue. **Hecho, PR #84.** 24 por página, porque divide a las dos cuadrículas (2 columnas en teléfono, 3 en escritorio) y la última fila nunca queda coja
- [x] 14.11 **Counts, and this is the heaviest requirement in the entire document.** F3, F4, F6 and F7 all demand that *every filter option shows its number before you choose it*, and F7 demands the confirm button state the exact result count at every step. That turns each filter into a faceted aggregation. **The cost is round trips, not Postgres.** Neon is serverless over HTTP: one query for the rows plus six for the facets is seven network round trips. The requirement is therefore ONE query returning rows and every facet count together, not a cache. **Hecho, PR #79.** Una sola consulta con `COUNT(*) FILTER`, y cada faceta se cuenta SIN su propio filtro para que las alternativas no den cero. Un test envuelve el driver y afirma `queries === 1`
- [x] 14.12 Price histogram over the selected zones (F5), plus the "la mayoría está entre $380 y $620" summary. **Rebanada A de cuatro, hecha — SOLO el dominio, PR #178. La casilla NO se marca**: sin la B, la C y la D no hay un pixel de histograma en ninguna pantalla. `listing-search/domain/price-histogram.ts`: `priceHistogram(tally)` y `pricePositionInBand(band, precio)`, más `PRICE_HISTOGRAM_BUCKETS` y `MIN_LISTINGS_FOR_PRICE_HISTOGRAM`. **Vive en `listing-search` y no en `listing-publication` a propósito**: publicar es el SEGUNDO consumidor (18.9), y dos histogramas —uno por módulo— serían dos formas de contar lo mismo que terminan dando dos números distintos para la misma zona. **Cuatro decisiones cerradas, con su razón**: (a) **los extremos del eje salen de los avisos, no de una escala fija** —el `$200` y el `$1000` de la lámina son el más barato y el más caro que se encontró—, y como repartir en cubos necesita los extremos y los extremos necesitan los datos, el dominio recibe la cuenta YA repartida y no deriva nada de una base: es lo único que deja la 14.11 en un solo viaje de red; (b) **el ancho es `tally.length` y no un parámetro**, con `PRICE_HISTOGRAM_BUCKETS = 8` como lo que hay que pedirle a la consulta — un cubo dibuja el histograma entero y más cubos que precios distintos deja cubos vacíos que siguen en la lista, porque el eje no se recorta; (c) **«la mayoría» es la franja contigua MÁS ANGOSTA cuyo conteo pasa estrictamente de la mitad** (`sum * 2 > total`, nunca `>=`: 16 de 32 no es la mayoría de nada y la regla transversal 3 no admite una etiqueta que lo diga), desempatada por la más cargada y después por la más barata, y **rotulada con el precio más barato y el más caro que hay ADENTRO** —avisos reales, no bordes calculados—; (d) **la altura de cada barra se mide contra la barra más alta y no contra el total**, que es lo que las láminas dibujan y lo único legible con ocho cubos. **El piso, que es una decisión de producto y el fundador puede moverla**: `MIN_LISTINGS_FOR_PRICE_HISTOGRAM = RESULTS_PER_PAGE / 2` = 12, derivado y no escrito a mano (mismo criterio que `LAST_ROOM_STEP`); por debajo devuelve `kind: "insufficient"` **con el total real**, que es un estado propio y no un histograma vacío — para una pantalla los dos son cero barras y significan lo contrario. Falla cerrado también con datos rotos: un cubo que dice tener avisos y no sabe entre qué precios están no se dibuja (AGENTS.md §7). **Pruebas nombradas** en `price-histogram.test.ts`: *«deriva el piso de la página de resultados en vez de escribirlo a mano»*, *«justo en el piso ya se dibuja: el límite se cumple, no se supera»*, *«un cubo que dice tener avisos pero no sabe a qué precio no se dibuja»*, *«el $200 y el $1000 de la lámina son el precio más barato y el más caro»*, *«ignora los cubos vacíos de las puntas al nombrar los extremos»*, *«se mide contra la barra más alta y no contra el total»*, *«es más de la mitad de verdad, y no la mitad justa»*, *«rotula la franja con precios que alguien pidió de verdad»*, *«elige la franja más angosta que alcanza la mayoría, no la primera»*, *«con dos franjas igual de angostas se queda con la que tiene más avisos»* y *«es la franja de la mayoría y no el eje entero lo que decide»*. **16 mutaciones, todas restauradas y verificadas por sha256**, cada una corrida contra la suite entera con y sin el archivo nuevo: las 16 ponen roja al menos una prueba con él y **ninguna pone roja nada sin él** (0/16), o sea que no hay una sola que ya estuviera cubierta. **Contradicción registrada entre las láminas**: el «ocho barras contra seis» NO es búsqueda contra publicar — Lista Teléfono, Lista Escritorio y Publicar Escritorio dibujan **ocho**, y sólo **Publicar Teléfono** dibuja **seis**. Se unifica en ocho porque el número de cubos se decide antes de la consulta y dos anchos serían dos consultas para la misma pregunta. **Segunda contradicción, sin resolver acá**: `$380` y `$620` **no son borde de cubo** de la escala `$200–$1000` que las mismas láminas dibujan, ni con 8 cubos ($100 cada uno) ni con 6 ($133,3) — por eso la franja se rotula con precios reales de adentro, que es la única lectura que los hace derivables. **Falta, y es lo que deja la casilla abierta**: la faceta del puerto y su única consulta —rebanada B, ver abajo qué tiene que devolver—, la pantalla de resultados con el marcado de barras dentro/fuera del rango elegido (rebanada C; es la línea 88 de `design/especificaciones/Rentas - UX movil.md` y **no está construida**, y tiene una decisión propia sin tomar: qué hacer con la barra que queda a caballo del borde, que es justamente lo que la lámina de Lista Escritorio dibuja mal al pintar en `--accent` el cubo 700–800 con el máximo en $700) y el paso 3 de publicar (rebanada D, la 18.9). **Lo que la rebanada B tiene que devolver para que el viaje siga siendo uno**, dicho concretamente para no re-derivarlo: `FacetedSearchPort` suma una faceta de precio con un arreglo de 8 `PriceBucketTally` en orden ascendente, cada uno con `count` entero y —cuando `count > 0`— el `lowestUsd` y el `highestUsd` **reales** de los avisos que cayeron adentro; nada más, porque el total, los dos extremos del eje y la franja los deriva el dominio. **Cabe en una sola sentencia** —un CTE con `min(price_usd)`/`max(price_usd)` sobre lo que matcheó y otro con `width_bucket(price_usd, lo, hi, 8)` encima, agregando `count(*)`, `min` y `max` por cubo—, así que `tests/integration/faceted-search.test.ts` y su `queries === 1` no se tocan. **El `+ 1` de `width_bucket` es la trampa**: el precio máximo cae en el cubo N+1 y hay que plegarlo al N, o el aviso más caro de la zona desaparece justo del histograma que dice cuál es el más caro. **Rebanada B de cuatro, hecha — la faceta y su consulta, PR #180. La casilla SIGUE ABIERTA**: ya hay de dónde sacar los números, pero ninguna pantalla dibuja todavía una barra (falta la C, la lista, y la D, el paso 3 de publicar). `FacetCounts.byPriceBucket` suma los ocho `PriceBucketTally` **obligatorios y no opcionales** —al revés que `withWidenedPrice`, porque salen de las filas que la consulta ya recorre y un campo opcional invitaría a una segunda llamada «sólo para el histograma», que es el viaje que no se paga—, y `drizzle-faceted-search.ts` los calcula **en la misma sentencia**: `queries === 1` sigue en verde y ahora afirma también que los ocho cubos viajaron con ella. **Cinco decisiones, con su razón**: (a) **el precio NO se cuenta contra su propio filtro**, misma regla de la casa que las otras seis facetas, y acá la más decisiva de todas — el histograma existe para que alguien ELIJA un rango, así que medido contra el rango ya elegido las barras de afuera caen a cero justo cuando se lo mira para moverse; (b) **la trampa del `+ 1` era real y se probó**: el $900 de la fixture es el máximo y `width_bucket` lo mandaba al cubo nueve, así que el último cubo se pide con `>=` y no con `=` — plegarlo con `least(…, 8)` sería un error peor, porque `least` **ignora los nulos** y volvería un ocho el cubo nulo de una búsqueda sin filas; (c) **con `lo === hi` `width_bucket` aborta la consulta entera** («lower bound cannot equal upper bound»), que es el caso común de una zona chica con avisos idénticos, así que el borde de arriba se ensancha en uno y todo cae en el primer cubo; sin filas los dos extremos son nulos, `width_bucket` devuelve nulo y los ocho ceros son la respuesta exacta —no una aproximación—, porque el histograma mira un subconjunto de lo que mira la consulta de afuera; (d) **por debajo del piso de doce la consulta reparte igual**: saltearlo exigiría saber el total ANTES, o sea otro viaje de red para ahorrarse unas columnas de una sentencia que ya recorre esas filas — negarse a dibujar es del dominio, no del adaptador; (e) **los cubos se agregan en su propia subconsulta y no en el `GROUP BY` de zona**, porque allá un cubo saldría partido por zona y habría que rejuntarlo en JavaScript sumando conteos pero COMPARANDO precios, una rama que sólo se equivoca cuando dos zonas caen en el mismo cubo — el defecto que ninguna fixture con precios repartidos llega a ver. **`price_usd` es `integer NOT NULL`**, así que un precio nulo no es expresible y no hay rama que escribirle. **Fixture nueva: una tercera ciudad de doce avisos en dos zonas**, con tres de $400 idénticos, porque los cinco de Maracaibo caen uno por cubo — sin un cubo con dos adentro, confundir `min` con `max` no se ve, y sin doce el dominio nunca dibuja. Es una ciudad aparte y no avisos sumados a Maracaibo justamente para no mover ningún número de las pruebas que ya estaban. **Nueve mutaciones, todas restauradas y verificadas por sha256**, cada una corrida contra la suite de integración entera **con y sin** las pruebas nuevas en el árbol: el pliegue del cubo N+1 (9 / 0), el ensanche de `lo === hi` (21 / 20), el precio contado contra sí mismo (1 / 0), los extremos sobre otro conjunto que los cubos (2 / 0), los cubos sobre otro conjunto que los extremos (2 / 0), los ocho ceros sin filas (1 / 0), el cubo vacío nombrando un precio (2 / 0), el `min` del cubo vuelto `max` (2 / 0) y seis cubos en vez de ocho (4 / 0). **Ocho de las nueve las atrapan SÓLO las pruebas nuevas**; la excepción es el ensanche, y tiene razón: sin él `lo === hi` hace **errar la consulta entera**, y Distrito Capital ya tenía un solo aviso, así que 20 pruebas viejas se caen con ella — la novena que suma es la que dice en qué cubo queda todo. **Falta**: la pantalla de resultados (rebanada C) y el paso 3 de publicar (rebanada D, la 18.9). **La contradicción de la barra a caballo del borde sigue abierta y es de la rebanada C**: la lámina de Lista Escritorio pinta en `--accent` el cubo 700–800 con el máximo puesto en $700, y nadie decidió todavía qué hace una barra que queda partida por el borde del rango elegido — la B no la toca porque el puerto no sabe qué rango se eligió al dibujar **Rebanada C de cuatro, hecha — el histograma dibujado en la pantalla de resultados, PR #181 (el dominio y la decisión) y PR #182 (la pantalla), en cadena porque juntas dan 613 líneas y el presupuesto de revisión son 400. La casilla SIGUE ABIERTA**: falta la D, el paso 3 de publicar (18.9). `listing-search/domain/price-histogram-panel.ts` traduce el histograma del dominio a lo que el panel dibuja y `PriceForm.histogram` lo lleva hasta `SearchPanel`; el componente no decide nada, y `PanelCounts` suma `byPriceBucket` sin tocar el caso de uso, porque `buildFilterPanel` ya le pasaba el `FacetCounts` entero. **La contradicción de la barra a caballo del borde, verificada contra las láminas y resuelta**: las DOS láminas de Lista —Escritorio y Teléfono, no sólo Escritorio como decía la anotación anterior— dibujan las mismas ocho barras (`30% tint · 55% accent · 100% accent · 78% accent · 62% accent · 40% accent · 22% tint · 12% tint`), el mismo eje `$200`–`$1000` y los mismos campos `$250`/`$700`, así que no es un desliz de una lámina sino una decisión repetida. Con ocho cubos de $100 se probaron las cuatro reglas candidatas y **una sola explica las ocho barras: «el borde izquierdo del cubo cae en el rango»** («cubo entero adentro» falla sólo en el cubo 6; «algún solape» falla sólo en el cubo 1; «el punto medio adentro» falla en los dos). Esa regla es una fórmula, no una coherencia: lo que significa es **estricta abajo y laxa arriba** — abajo exige el cubo completo, arriba se conforma con tocarlo. **Y el detalle que la anotación anterior tenía mal**: el cubo 6 no *contiene* el máximo de $700, **empieza** en $700 (el cubo 5 es $600–$700), así que no está a caballo de nada — su único punto adentro del rango es el borde mismo. La asimetría es entonces más filosa de lo que se creía: **la barra con la MITAD de su ancho adentro se pinta afuera, y la que no tiene ni un dólar de ancho adentro se pinta adentro.** **Decisión, y es una desviación de la lámina que se documenta en vez de hacerse callada (AGENTS.md §2)**: se decide con los **precios reales** del cubo —el `lowestUsd`/`highestUsd` que la rebanada B ya trae— y no con el borde nominal, lo que disuelve la pregunta en vez de elegir uno de sus dos lados; misma comparación en las dos puntas, y **la garantía corre para un solo lado**: una barra marcada AFUERA no tiene un solo aviso que el rango admita, mientras que una marcada adentro tiene precios que ALCANZAN el rango sin que dos extremos puedan probar que hay uno en el medio — que es el sentido correcto de fallar cerrado (§7), porque la afirmación segura es justo la que escondería oferta que la búsqueda ya está entregando. Sobre los números de la propia lámina la desviación cae en las dos barras marcadas: el cubo 1 pasa a **adentro** si tiene algún aviso de $250 o más, y el cubo 6 pasa a **afuera** salvo que tenga uno de exactamente $700. **Las otras tres decisiones de la rebanada**: (a) **la frase nombra las zonas elegidas y la ciudad cuando no hay ninguna** —«En Chacao, la mayoría…», «En Chacao y Altamira, la mayoría…», «En Chacao, Altamira y Las Mercedes, la mayoría…», «En Caracas, la mayoría…»—, unidas con «y» y no con la coma de `searchHeadline`, porque aquél es el rótulo terso de al lado de la pastilla y ésta es prosa; **sin ningún nombre la frase entera se calla** en vez de servir «En , la mayoría…», que es el defecto que se embarca; (b) **por debajo del piso de doce no se dibuja un histograma vacío**: se lee «Con 7 avisos no alcanza para mostrar cómo se reparten los precios», con el total REAL adentro, y el cero se dice distinto («Sin avisos todavía no hay precios que repartir») porque cero barras y pocas barras significan lo contrario; el formulario de precio sigue entero debajo; (c) **una fila de barras es una imagen de datos**, así que va `role="img"` con el dibujo dicho en palabras en su `aria-label` («Precios de $200 a $1000 en 8 barras: 4 dentro del rango elegido y 4 afuera»), y el marcado dentro/fuera **no descansa sólo en el color** —`--accent` contra `--tint` más un borde, que el modo de alto contraste sí conserva—, porque el color solo es lo que `violation-copy.ts` llama «invisible para quien no distingue colores». **Se sirve desde el servidor y no cuesta un byte de cliente**: `busqueda-sin-javascript.test.tsx` afirma las ocho barras, los dos rótulos del eje y la frase sobre el cuerpo que devuelve la ruta de verdad, y que el trozo del dibujo no trae ni script ni lienzo. El alto de cada barra es el único valor inline, porque es dato medido y no un tamaño del sistema — `lint:tokens` sigue en verde. **15 mutaciones, todas restauradas por copia de bytes y verificadas por sha256**, cada una corrida contra la suite entera con y sin las pruebas nuevas en el árbol. **Falta**: el paso 3 de publicar (rebanada D, la 18.9). **Rebanada D de cuatro, hecha — el paso 3 de publicar, y la casilla SE MARCA: no queda nada de la 14.12 sin afirmar.** `listing-search/domain/price-histogram-step.ts` es el hermano de `price-histogram-panel.ts` —un archivo por pantalla, las dos traduciendo el MISMO motor— y `page.tsx` del paso 3 lo sirve. Ver la 18.9 para las decisiones de la rebanada; lo que cierra ESTA tarea es que el histograma de F5 ya está dibujado sobre las zonas elegidas, con su frase «la mayoría está entre $380 y $620», en las dos pantallas que lo piden.
- [x] 14.13 Swap min and max when inverted instead of erroring; clamp out-of-range values to the real extremes and say so (F5) **El «y se dice» entregado el 2026-09-02 para las dos mitades. La casilla SIGUE ABIERTA por una sola decisión, que es del fundador y está al final.** **CIERRA el 2026-09-02 con la salida A, elegida por el fundador.** Las dos salidas quedaron presupuestadas al construir la corrección: **(A)** decir el hecho sin tocar la búsqueda, que es lo que se embarcó y cuesta cero; **(B)** redirigir a la dirección ajustada, que mantiene un viaje por pedido y hace que la URL coincida con lo mostrado (F12), **al precio de un pedido más y de reescribirle el enlace a quien lo compartió**. Se eligió A por eso último: una dirección que alguien mandó por WhatsApp no se le cambia por debajo. Lo que la 14.13 pedía —intercambiar en vez de errar, acotar a los extremos reales, **y decirlo**— está entero: el intercambio ya estaba y estaba afirmado en las dos direcciones, el acotado salió **sin una consulta nueva** (los extremos ya viajan en `byPriceBucket`, que no se cuenta contra su propio filtro, así que con `min=5000` la lista vuelve vacía y los cubos siguen describiendo el mercado), y las tres frases viven en `price-correction.ts`, bajo el piso de cobertura. **La asimetría es la decisión y está fijada por su negativa**: un tope suelto se anuncia como ajuste porque no recorta nada, y uno que se pasó de todo NO se llama ajuste, porque anunciar un ajuste que no ocurrió es la misma mentira que corregir en silencio, vista del otro lado. **La salida contada de la 14.15 sigue siendo la que ofrece rehacer la búsqueda.**

  **Lo que ya era verdad, verificado antes de diseñar nada.** El intercambio estaba construido y **estaba afirmado**: `orderPrices` en `search-criteria.ts` desde la 14.6, con cuatro casos con nombre en `search-criteria.test.ts` —*«intercambia los extremos en vez de dar error»*, *«los deja como están cuando ya están en orden»*, *«iguales no se tocan: es un precio exacto, no un error»* y *«con un solo extremo no hay nada que intercambiar»*—, o sea las dos direcciones ya pinchadas. **El clamp no existía en ninguna forma**: `rg clamp` sobre `src/` no devolvía una sola línea de precio. Y **las tres últimas palabras del enunciado no las cumplía ninguna de las dos mitades**: la corrección pasaba en silencio, que es una pantalla mintiendo sobre lo que hizo — alguien pide de $900 a $300 y mira los resultados de $300 a $900 sin una palabra.

  **Los «extremos reales» NO costaron una pregunta nueva, y era la pregunta que había que contestar antes de diseñar.** `FacetCounts.byPriceBucket` ya trae `lowestUsd`/`highestUsd` por cubo desde la 14.12 rebanada B, y —por la decisión (a) de esa rebanada— **el precio no se cuenta contra su propio filtro**. Eso es lo que lo hace servir justo cuando hace falta: con un mínimo de $5000 la lista vuelve vacía y los cubos siguen describiendo el mercado entero, así que «el más caro cuesta $900» sale de filas que la consulta ya recorrió. `priceExtremes(tally)` en `price-histogram.ts` los deriva reusando el mismo `bandOver` que rotula el eje, **sin el piso de los doce avisos**: dibujar sobre tres es ruido vestido de dato, pero «el más caro cuesta $900» es cierto con tres y con trescientos. `tests/integration/faceted-search.test.ts` y su `queries === 1` **no se tocaron**, y `build-filter-panel.test.ts` afirma además que la frase sale de esa única llamada.

  **Dónde vive la frase**: `listing-search/domain/price-correction.ts` — `priceRangeNotices(asked, matched)` decide, y `priceCorrectionNotices(query, tally)` la compone sobre lo que la pantalla tiene a mano. Va en el dominio y no en las dos páginas por la regla permanente del fundador y por la mecánica de siempre: el suelo del 90 % llega acá y no a `app/`. `buildFilterPanel` la devuelve como `priceNotices` —fuera del panel, porque el panel está cerrado por defecto y la corrección es sobre los resultados que se están mirando— y las dos pantallas de resultados la dibujan.

  **Las tres frases, y por qué son tres.** (a) *«Pediste de $900 a $300, al revés: se buscó de $300 a $900.»* — con los números TAL COMO se pidieron, que es lo único que explica por qué el formulario muestra otros dos. (b) *«El máximo de $5000 se ajustó a $900: no hay nada más caro.»* — el extremo FLOJO. (c) *«Ningún alquiler llega a $5000: el más caro cuesta $900.»* — el extremo que **pasó de largo**.

  **La asimetría entre (b) y (c) es la decisión del archivo, y es de honestidad.** Un extremo flojo no recorta nada, así que ajustarlo a la punta real no cambia una fila: el ajuste es exacto y se anuncia como ajuste. Uno que pasó de largo **sí cambiaría los resultados**, y las filas ya se trajeron con el rango pedido — anunciar ahí un ajuste que no ocurrió es la misma mentira que corregir en silencio, vista del otro lado. Se dice el hecho, y la salida con su conteo la sigue ofreciendo `search-exits.ts` (14.15): «Quitar el precio y ver 2». Está pinchado por su negativo: *«y NO lo llama un ajuste»*.

  **Fixture corregida de paso, y es la trampa que habría dejado todo esto sin medir.** `app/alquiler/catalogo-de-prueba.ts` repartía `byPriceBucket` contra `matching(criteria)`, o sea **contra el propio filtro de precio** — al revés que el adaptador real. Con ella, un mínimo de $5000 devolvía ocho ceros y la corrección no tenía contra qué medirse: una mutación apuntada a una entrada que la fixture nunca produce no mide nada.

  **Pruebas nombradas.** `price-correction.test.ts` (12 casos): *«dice que los dos números venían al revés, con los dos adentro»* y su otro lado *«un rango en orden no anuncia ninguna corrección»*; *«un extremo flojo se ajusta al precio real y lo nombra, de los dos lados»* contra *«un extremo que ya está adentro del rango real no se ajusta»* (que incluye el borde exacto, `min === lowest`); *«nombra el precio real de la punta que se pasó, y NO lo llama un ajuste»*; *«pasarse de largo se dice UNA vez: el otro extremo ya no recorta nada»*; *«sin precios reales no se inventa ninguno»* (falla cerrado, §7). Sobre el cuerpo servido, en las DOS pantallas —`busqueda-sin-javascript.test.tsx` y `zona-sin-javascript.test.tsx`— porque son dos archivos y el que se olvidara del cable dejaría media mitad del producto corrigiendo en silencio: *«dice que intercambió los dos extremos, con los números que se pidieron»*, *«nombra el precio real cuando el mínimo pedido no lo alcanza ninguno»* y *«una búsqueda que no se corrigió no dice nada»*.

  **Nueve mutaciones**, cada una con el texto presente exactamente una vez, sha256 antes ≠ después, restaurada por copia de bytes con sha256 igual al original y `git status` limpio; cada una corrida contra la suite entera **con** las pruebas nuevas y **con ellas fuera del árbol**: el intercambio nunca anunciado (6 / 0), el ajuste flojo anunciado también en el borde exacto (3 / 0), la punta que pasó de largo fingiendo un ajuste (2 / 0), el ajuste de máximo nunca anunciado (4 / 0), los extremos devueltos al revés (5 / 0), la dirección no leída (5 / 0), la aplicación sin pasar la corrección (4 / 0), la pantalla de ciudad sin dibujarla (2 / 0) y la de zona sin dibujarla (1 / 0). **Las nueve valen cero sin las pruebas nuevas**: no hay una sola que ya estuviera cubierta.

  **LO QUE DEJA LA CASILLA ABIERTA, y es una decisión de producto, no trabajo pendiente.** El clamp de la punta que pasó de largo **no se aplica a la consulta**, y no se puede aplicar sin un segundo viaje de red: los extremos reales llegan CON la respuesta, así que corregir el criterio con ellos exige preguntar de nuevo — y `buildFilterPanel` tiene su cota de un solo viaje afirmada en `tests/integration/faceted-search.test.ts` («el panel entero cuesta UN viaje de red», 14.50). Las dos salidas honestas, con su costo:

  1. **Como está hoy**: se dice el hecho y la 14.15 ofrece la salida contada. Cuesta cero. Quien pidió desde $5000 no recibe avisos de $900 que no pidió.
  2. **Redirigir**: la pantalla detecta el desborde con su única consulta y responde un `redirect` a la dirección con el extremo ya ajustado. Cada petición sigue costando una consulta y **la dirección pasa a coincidir con lo que se muestra**, que es lo que la F12 pide; el costo es una segunda petición de latencia y que reescribe el enlace que alguien compartió. La decisión la toma el fundador; el dominio ya devuelve el número contra el que habría que ajustar.
- [x] 14.14 The relaxation proposal (F10, F11): evaluate each active filter and offer the single change that adds the most results, with its number. "Ninguna pantalla termina en un vacío sin salida" (criterion 9). **Reconciled 2026-08-24 — built, plan was stale.** `listing-search/domain/search-exits.ts`. Tested by "dice que están todos y propone la relajación que más suma" and "sumar la zona vecina promete los que hay MÁS los que suma" — the count is the point, and it is additive rather than a replacement. "sin un filtro que soltar no inventa un botón" pins the negative case, and "a mitad de la lista no cierra nada: todavía faltan avisos" keeps the proposal from appearing before the results actually run out.
- [x] 14.15 Zero-results diagnosis (F11): name the filter causing the emptiness, offer up to three concrete exits each with its count. **Never suggest another city** (criterion 8). **Reconciled 2026-08-24 — built, plan was stale.** Same module. It names the culprit rather than apologising ("dice cuál es el filtro, no un mensaje genérico"), says so honestly when no single filter is to blame, and offers up to three exits each with its count. The criterion this task marks as critical is pinned by its own test: **"nunca ofrece otra ciudad: toda salida se queda dentro de ésta"**. And "ninguna salida lleva a otro vacío" — an exit that lands on a second empty screen is worse than no exit.
- [x] 14.16 **SACADA DE LA FASE 14 el 2026-09-03 por decisión del fundador: pasa a la Fase 24, entera.** Búsquedas guardadas, que la F11 promete («avisarme cuando aparezca algo así»). No se hizo nada: se movió. La razón está escrita en la Fase 24 y se resume en que esto no es una casilla, es un producto — y estaba inflando la cuenta de una fase con la que no comparte una línea.

### 14c. The suggestion box (founder, 2026-08-21 — "como hace Airbnb")

- [x] 14.17 **A translator, not a search.** Typing "arriendo maracaibo" must suggest the Maracaibo *filter*. This does NOT contradict the document's own "búsqueda de texto libre" exclusion, and the distinction is the whole design: it never reads listing titles or descriptions, so it cannot return empty on a thin catalogue — the reason free text was excluded. It matches a controlled vocabulary (cities, zones, prices, rooms, property types, attributes, publisher type) and emits filters. F4's "sólo autocompleta zonas conocidas" is this rule, widened. **Reconciled 2026-08-24 — built, plan was stale.** `listing-catalogue/domain/suggest-filters.ts` matches text to FILTERS, not to a text search: "reconoce una ciudad y descarta la palabra de relleno" is the translator behaviour this task describes. It reads a maximum price the way people actually type one, reads rooms and property type, and combines several signals from one phrase. It ignores accents and case, because nobody types either on a phone, and returns empty for text it does not recognise rather than guessing.
- [x] 14.18 **A suggestion is a (filter, value) pair, never a word.** `Centro` is a zone in BOTH Maracaibo and Distrito Capital — it is in the seed and `tests/integration/listing-search.test.ts` covers it as the colliding-name case. A bare "Centro" would apply the wrong city's filter and return zero results under the isolation rule, with the visitor unable to see why. Suggestions carry their kind and their scope: `Centro · Zona · Maracaibo`. **Reconciled 2026-08-24 — built, plan was stale.** Same module, and the test that proves the pair is the disambiguation one: "desambigua un nombre repetido con su padre" — `Centro` exists in BOTH Maracaibo and Distrito Capital, which is precisely the case this task names, and a suggestion that were only the word would be ambiguous between two cities. Aliases resolve by prefix as well as whole ("encuentra Altamira escribiendo «alta»"), a single letter does not autocomplete, and the same suggestion is never repeated twice.
- [x] 14.19 Pure-domain parser: text in, suggested filters out. No database, no network, no client — testable whole, and it keeps the founder's standing rule (a business rule never lives in the front). Reuse the NFD accent strip already shipped in `listing-discovery/domain/listing-url.ts` so "maracaybo" and "Maracaibo" agree. **Hecho.** `listing-catalogue/domain/suggest-filters.ts`. Falta cablearlo a una pantalla (14.17/14.18/14.20)
- [x] 14.20 Works with JavaScript off: the box is a `GET` form; the server translates and redirects to the canonical filtered URL. Live suggestions while typing are an enhancement on top, never the mechanism (F14). **Reconciled 2026-08-24 — built, plan was stale.** `components/molecules/SearchBar.tsx:54` is a literal `<form method="get">` and `listing-catalogue/domain/search-destination.ts` resolves where it lands. Three tests hold it: "es un GET que vuelve al servidor, no un enlace", "devuelve lo escrito para que el campo no se vacíe al volver del servidor", and **"no declara JavaScript de cliente"** — that last one is what keeps live suggestions an enhancement instead of a requirement.

### 14d. Surfaces

### 14f. El inicio y la cuadrícula (founder, 2026-08-22)

**Las dos decisiones de arriba rehacen la superficie de lectura entera**, y hasta ahora el plan sólo tenía una tarea bloqueada donde debería haber una sección. Esto es lo que realmente cambia.

- [x] 14.21 **El inicio, en `/`, con cuatro tiras de 5 avisos** (F1): recientes, cada ciudad, y hasta $400. Cada una declara su **total real** ("Ver los 23"), una colección vacía **no se renderiza** — la tira desaparece, no queda un hueco — y una con menos de 5 muestra los que haya sin la placa "Ver todos". Sin ningún aviso activo, el inicio muestra la barra de búsqueda y una invitación a publicar. **Hecho, PR #83.**
- [x] 14.22 **Las cuatro consultas, con su conteo total.** No es un `LIMIT 5` cuatro veces: cada tira necesita sus 5 filas **y** el total de la colección, y "Ver los 23" tiene que decir 23 de verdad (regla transversal 3). Ni vencidos, ni ocultos, ni pendientes de moderación. **Hecho, PR #83.** Una sola consulta para las cuatro tiras: `JOIN` contra un `VALUES` de colecciones, `count(*) over (partition by clave)` para el total y `row_number()` para el recorte
- [x] 14.23 **La misma propiedad puede salir en dos tiras** — reciente y barata a la vez — y eso es correcto, no un duplicado a deduplicar. **Hecho, PR #83.** Es la forma de la consulta, no una regla que alguien tenga que acordarse de no romper
- [x] 14.24 **RESUELTO 2026-08-22: `/buscar` no existe. Toda búsqueda vive en la ruta de su lugar.**
  >
  > La F12 escribía `/buscar?ciudad=distrito-capital&zona=chacao,altamira&…`, y el mapa de pantallas dibujaba BUSCAR como una caja propia. Lo destapó el fundador mirando una URL de Airbnb: `/s/Bocagrande/homes?checkin=…` — **el lugar va en la RUTA y los filtros volátiles en la query**, y el prefijo `/s/` no es una palabra legible porque no vale nada; el valor está en *Bocagrande*.
  >
  > **La pregunta que eso abre es mejor: ¿hace falta esa ruta?** No. `ListingSearchPort` garantiza a nivel de tipo que toda búsqueda lleva un `cityId` obligatorio y no nulable — "no hay `searchAll` ni un valor comodín que signifique «en todas partes»". Una ciudad es un lugar, así que **toda búsqueda posible ya cae en una ruta de lugar**:
  >
  > ```
  > /alquiler/caracas                        sólo ciudad
  > /alquiler/caracas?min=250&hab=2          ciudad + filtros
  > /alquiler/caracas/chacao                 ciudad + una zona   ← indexable
  > /alquiler/caracas/chacao?min=250         refinada
  > /alquiler/caracas?zona=chacao,altamira   varias zonas (F4, OR)
  > ```
  >
  > **Lo que se gana, en orden de peso.** (1) Una URL por contenido: con dos rutas los mismos avisos vivían en dos direcciones, que es el contenido duplicado que Google castiga sobre el dominio entero. (2) La regla de indexación queda **mecánica** — con parámetros `noindex`, sin parámetros se indexa — y no una condición que se rompe en silencio. (3) **Borra trabajo**: la página de zona de la Fase 11 (11.5–11.7) deja de ser una pantalla aparte, *es la misma sin filtros*.
  >
  > **La ficha anida dentro de su zona**, lo que se lee solo: `/alquiler/caracas/chacao/apto-2-hab-<id>`. La ambigüedad ya está resuelta desde la 11.1 — dos segmentos son una zona, tres son un aviso, y un tercer segmento que no termina en un id hace que `listingIdFromSlug` devuelva `null`.
  >
  > **Pendiente de corregir en los documentos del fundador**, no en el código: la F12 y el mapa de pantallas siguen escribiendo `/buscar`.
- [x] 14.30 **Reescribir el comentario de `app/page.tsx`**, que dice que no hay inicio separado "y eso es una decisión antes que una omisión". Ahora es falso, y quedaría contradiciendo al código de al lado. **Hecho, PR #83.**
- [x] 14.25 **La tarjeta de cuadrícula, que reemplaza a `ResultRow`.** Dos columnas de 158 px en móvil, tres de 254 px en escritorio. Conserva lo que la fila ya garantizaba y está probado en `design-contract.test.tsx`: el publicador visible y distinguible **en escala de grises** (relleno contra borde), y el precio antes del título en orden de documento y en peso. **Hecho, PR #77.**
- [x] 14.26 **La tira del inicio cambia de mecanismo entre anchos**: scroll horizontal con 5 más la placa "Ver todos" en móvil, filas fijas de 5 con el total y una flecha en el encabezado en escritorio. **Un solo componente con puntos de quiebre, nunca dos** — la misma razón que `SearchFilters` ya documenta: dos implementaciones se separan, y una búsqueda filtrada terminaría dando URLs distintas según el ancho. **Hecho, PR #83.** Un solo componente con puntos de quiebre
- [x] 14.27 **El presupuesto es lo que decide si la cuadrícula sobrevive — y la cuadrícula entra. CERRADA 2026-09-02, midiendo en vez de heredar el número.** El enunciado mezcla dos medidas distintas, y ahí estaba el nudo: **los ~128 KB contra 150 KB son *transferencia total de la página*, no el paquete de JavaScript.** Son dos gates distintos y sólo uno de los dos decide esto.

  **Cuál manda, dicho con el archivo que lo afirma.** El techo de la página de resultados es `lighthouserc.json` → `total-byte-weight ≤ 235520 B` sobre `^…/alquiler(/[^/]+){0,2}/?…`, o sea **230 KB**. El 150 del documento del fundador **ya estaba revisado**: `design.md` («Performance Budget», revisión del 2026-08-16) lo subió a 230 y escribió por qué —«102 KB de JavaScript más HTML, CSS y diez miniaturas no entran en 150 KB, así que ese número tampoco era alcanzable»—. **El enunciado heredó el 150 de un documento anterior a esa revisión.**

  **Lo que `pnpm budget:bundle` mide es otra cosa**: primer JavaScript del camino de lectura, 130 KB gzip. Medido hoy sobre el build de este repositorio: `/alquiler/[ciudad]/page` y `/alquiler/[ciudad]/[zona]/page` — **las dos que dibujan la cuadrícula — 106.43 KB gzip cada una**, 23.57 KB por debajo del tope; la peor ruta del camino de lectura es `/publicar/paso/[paso]/page` con 109.38 KB y no es ninguna de las dos. **La cuadrícula no agrega un solo byte de cliente**: `ListingCard` no lleva `"use client"` y el cambio de tamaño lo resuelve `<picture>` con un `media`, que el navegador evalúa antes de pedir bytes.

  **Lo que la cuadrícula sí gasta son fotos, y su techo está declarado y probado**: `DERIVATIVE_BUDGETS` fija `thumb` ≤ 8 KB (la tarjeta del teléfono) y `card` ≤ 16 KB (la del escritorio), con la prueba que suma los cinco tamaños («suma 246 KB por foto»). **Y no se piden las veinticuatro**: `ListingCard` dibuja `loading="lazy"`, así que la transferencia fría trae las que están sobre el pliegue — hoy 2 en el teléfono (≤16 KB) y 4 en el escritorio (≤64 KB), medido en la 14.29. La aritmética «20 avisos ≈ 128 KB» nunca aplicó tal cual: una página trae 24 (`RESULTS_PER_PAGE`) y el resto no se pide hasta que se baja.

  **Lo que NO se verificó desde acá, dicho en vez de escondido.** `pnpm budget:lighthouse` exige `PREVIEW_BASE_URL` y un despliegue, así que los 230 KB los afirma CI contra el preview y no esta corrida. Y **«el inicio tiene su propio techo: ~80 KB» no tiene gate detrás**: el patrón comodín de `lighthouserc.json` le da a `/` los mismos 230 KB, no 80. Si ese 80 es una decisión y no un apunte, es una entrada nueva en `assertMatrix` — no está puesta
- [x] 14.28 **Sin foto de portada la cuadrícula no existe — y eso ya está construido. CERRADA 2026-09-02, y con dos correcciones de referencia.** Una tarjeta sin imagen se lee como rota, y la regla que lo impide vive en el dominio desde la 14.5: `buildListingGrid` declara `REQUIRED_SIZES = ["thumb", "card"]` y descarta al aviso cuya portada no tenga las dos. Probado y nombrado: `src/modules/listing-discovery/domain/listing-grid.test.ts` → *«deja fuera al aviso cuya portada no tiene los dos tamaños que la tarjeta usa»* y *«deja fuera al aviso que no tiene portada»*. Una foto a medio derivar no dibuja media tarjeta: **no dibuja ninguna**.

  **Corrección 1 — el puntero está mal.** «La 19a» no es el pipeline de fotos: la sección **19a es «Photo captions: removed before they were built»**. Lo que el enunciado nombra es la sección **16a** («The photo pipeline no longer fits the design»), y está hecha: **16.1** re-derivar desde la de 1280 y no desde el original, **16.2** la migración `0010` con `listing_photo_derivative`, **16.3** el rellenado.

  **Corrección 2 — y ésta cambia de fase, no de número.** El «rellenado» de la 16.3 movió **claves**, no bytes. La migración `0010` lo dice dentro de sí misma: *«Las otras tres —card, strip y detail— NO existen para esas fotos y hay que re-derivarlas desde la de 1280, que es su propia tarea»*. O sea que **el rederivado masivo nunca fue prerrequisito de la lista**: la lista ya está protegida por `REQUIRED_SIZES`. Lo que ese rederivado desbloquea es que **los avisos anteriores al 2026-08-22 vuelvan a ser visibles** en la cuadrícula, que es inventario y no maquetación. Queda abierto como **19.18**, con la forma que la 19.3 ya le había escrito. **No se absorbe acá**: una tarea de la Fase 19 cerrada por su número dentro de la 14 es exactamente la deuda de numeración que la 14.24b y la 20.13 vinieron a pagar
- [x] 14.29 **CERRADA 2026-09-02 con el criterio REVISADO POR EL FUNDADOR de 4/8 a 2/4 — y esto no es una cota bajada para que dé, es una decisión con su razón y su fecha; ver el apartado del cierre al final de esta entrada.** Criterio de aceptación 1, y es medible: 4 avisos completos sobre el pliegue a 360 px, ~~6~~ 8 a 1280. MEDIDO EL 2026-09-02: entran 2 y 4. La medición existe y está nombrada — `tests/measure/lista.spec.ts` sobre el arnés `app/measure/lista/page.tsx`, atado a las dos pantallas de resultados por `app/measure/lista-medida.test.ts`. ~~La casilla queda abierta porque el criterio es el número, y el número no da~~ — **corregido el 2026-09-02 al cerrarse: el número no cambió, cambió el criterio, y lo cambió el fundador con su razón escrita.** Ver el apartado del cierre.

  **CORREGIDO 2026-09-02: «6 a 1280» es de antes de la 14.33.** La lámina 7c lo escribe entero —*«cuatro columnas de 254: 8 avisos sobre el pliegue, contra 6 antes»*— y el 6 es el de la barra lateral de 240 px que el fundador sacó el 2026-08-26. El objetivo de escritorio es **8**. El 4 del teléfono sí sobrevive: la lámina 6c lo anota con esas palabras, *«tarjeta de 195 px · 4 avisos completos»*, sobre un pliegue de 640.

  **Lo medido, con el pliegue de cada lámina** (360×640 y 1280×800, Chromium real):

  | | lámina | medido | dónde se va |
  |---|---|---|---|
  | 360×640 | 4 | **2** | la cuadrícula arranca a **373 px**; la 6c la arranca a ~74 (barra de 60 + relleno) |
  | 1280×800 | 8 | **4** | la cuadrícula arranca a **233 px** contra ~200 de la 7c, y la tarjeta mide **321 px** contra ~285 |

  **Dónde están los 373 px del teléfono, uno por uno**: barra 101, miga de pan 52, título 25, conteo 17, **fichas quitables 154** — y la tarjeta mide 249 px contra los 195 de la lámina. **Cuatro de esos cinco bloques no existen en la lámina 6c**: entre la barra y la cuadrícula no dibuja nada. Las fichas solas se llevan 154 px porque a 360 se pliegan a cuatro líneas, y la 6c pone ese dato en el «4» de la pastilla en vez de en una fila propia.

  **Lo que hay que mover es una decisión de diseño y no se toma acá** (qué se va del encabezado del teléfono, y de dónde salen los 36 px de más de la tarjeta de escritorio): queda abierta como **14.53**, con el presupuesto en píxeles ya contado.

  **REMEDIDO EL 2026-09-02 con la 14.53 construida, y el número no se movió: siguen entrando 2 y 4.** Las dos decisiones del fundador se llevaron 181 px del teléfono (154 de fichas quitables, 27 de la placa por tarjeta) y 54 del escritorio, y aun así la segunda fila termina **35 px** pasada del pliegue en el teléfono y **33 px** en el escritorio. La cuadrícula del teléfono arranca ahora a **219 px** contra los 373 de ayer y los ~74 de la lámina.

  | | lámina | antes | ahora | lo que sobra |
  |---|---|---|---|---|
  | 360×640 | 4 | 2 | **2** | 35 px |
  | 1280×800 | 8 | 4 | **4** | 33 px |

  **Lo único que queda comiéndose el pliegue es el encabezado de tres líneas** —miga de pan (52), `<h1>` (25) y conteo (17+14 de margen)— contra la única línea que dibujan 6c y 7c. Cualquiera de los tres alcanza para cerrar los dos números, y ninguno se puede sacar de oficio: **la miga de pan es la salida que la 14.41 dejó puesta al borrar la `SearchSummaryBar`**, y el `<h1>` es el encabezado que nombra la zona. Es una decisión del fundador y queda abierta con el presupuesto ya contado al píxel. La casilla sigue abierta porque **el criterio es el número**.

  ---

  ### El cierre, y la decisión que lo cierra (fundador, 2026-09-02)

  **El criterio pasa de 4/8 a 2/4 por decisión del fundador, y la distinción con «bajar la cota para que dé» es lo primero que hay que dejar escrito.** No se cerró porque el número diera: se cerró porque el fundador miró qué costaba llegar al número y decidió no pagarlo.

  **Preguntó lo correcto antes de decidir: *«pero los avisos pueden ser con scroll, no?»*.** Y la respuesta es que sí — nada está roto, la lista se baja y los 24 avisos de la página siguen ahí. Lo que el criterio mide es **la primera pantalla**, y el argumento es de su propio documento: la densidad es lo que hace que un catálogo chico se lea como un mercado y no como un vacío. Sabiendo eso, eligió.

  **Lo que eligió: volver, en un teléfono, vale más que un aviso y medio.** Lo único que quedaba por sacar para llegar a 4 y 8 era el encabezado de tres líneas, y sus tres bloques tienen dueño: la **miga de pan** es la salida que la 14.41 dejó puesta al borrarse la `SearchSummaryBar` —quitarla del teléfono se la quita a quien llegó a una zona filtrada desde Google o desde un chat—, el **`<h1>`** nombra la zona y el **conteo** dice cuántos hay. **Se quedan los tres. El encabezado no se toca.**

  **Lo que cuesta, dicho y no rodeado**: las láminas 6c y 7c dibujan **4 y 8**, el producto sirve **2 y 4**, y la diferencia es exactamente ese encabezado de tres líneas que las láminas no dibujan — 35 px en el teléfono y 33 en el escritorio, medidos. Queda registrado acá para que nadie lo lea como una lámina cumplida.

  **Lo que se entrega como prueba** es el conteo exacto en las dos pantallas: `tests/measure/lista.spec.ts` → *«a 360×640 entran 2 avisos completos, que es el criterio del fundador»* y *«a 1280×800 entran 4 avisos completos, …»*, más la guardia del encabezado (*«se come 219 px…»*) para que si creciera se sepa.

  ### Y un hallazgo que vale para toda la suite de medición, no sólo para acá

  La primera versión de este cierre acotaba en píxeles lo que le faltaba a la segunda fila para caber. **Sobre el mismo commit, macOS medía 35 px y el Linux de CI 69** — 34 px de diferencia, y la prueba se puso roja en CI. La causa se midió y **no es la que parecía**: el encabezado mide **219 px en las dos** máquinas y la tarjeta de **escritorio 294 en las dos**; la del teléfono mide **222 en una y 239 en la otra**, y la diferencia es **exactamente una caja de línea del metadato** (12 px × 1,4 = 16,8).

  El porqué: la línea `Chacao · 2 hab · 78 m²` ocupa **131,3 px de los 136 disponibles** en la tarjeta del teléfono —**4,7 px de holgura, un 3,5 %**— y `system-ui` no resuelve a la misma fuente en macOS que en Linux. Un 3,6 % más ancha y se pliega: +16,8 px por tarjeta, ×2 filas = +33,6. En escritorio la misma frase tiene **100,7 px de holgura** y por eso allá las dos máquinas coinciden al píxel. **La aritmética descarta la otra causa posible**: si en Linux se angostara la columna (una barra de desplazamiento clásica) la segunda fila caería en 707; si la columna sigue en 158 y sólo se pliega el texto, cae en **709**, y CI mide 709. Reproducido además en macOS: con la columna intacta y `letter-spacing: 0,3px` sobre el metadato, la pantalla mide 458/709 y la tarjeta 239 — los números de CI, clavados.

  **Dos consecuencias, y las dos son más grandes que esta tarea.** (1) **Una cota en píxeles sobre esta pantalla mide la máquina**; el conteo, no — las dos plataformas cuentan 2 y 4 con tarjetas de alturas distintas, que es justamente por qué el criterio se expresa como conteo. La cota de píxeles se **retiró**, no se ensanchó: con el criterio revisado ya no había presupuesto que medir, y una cota holgada de 80 no habría atrapado nada. (2) **El teléfono dibuja sus tarjetas a un pelo de plegar el metadato**, así que un aparato con una fuente de sistema un poco más ancha —Android contra iOS— ve cada tarjeta 17 px más alta. Eso queda medido como prueba propia (*«el metadato del teléfono va a un pelo de plegarse, y el del escritorio no»*), afirmado como **proporción y nunca en píxeles**: menos del 10 % de holgura en el teléfono, más del 25 % en escritorio.

  **Y una nota sobre la forma de la cota, porque contradice a la 1b.10 a propósito.** Aquella tarea decidió medir densidad como *cota sobre la fila* y no como *conteo sobre el pliegue*, con el argumento de que un conteo se rompe por una diferencia de métrica de fuente. La 22.1 ya movió el criterio de evaluación 1 en la dirección contraria, y esta medición es la que lo cierra: el conteo es lo que el fundador puso como criterio, la fila que la 1b.10 acotaba **ya no está en el camino de lectura**, y el riesgo que aquélla temía se contuvo con margen — la segunda fila del teléfono termina a 883 px contra un pliegue de 640, y la del escritorio a 887 contra 800
- [x] 14.22b "Limpiar todo" (F8), which resets everything except the city — "la ciudad no es un filtro, es el contexto". Renumerada a 14.22b: el plan tenía DOS tareas 14.22, y dos tareas con el mismo número es una que alguien va a dar por hecha mirando la otra. **CERRADA 2026-09-02, y el hallazgo es que la regla estaba entera: lo que faltaba era la prueba del cable.** `clearAllHref` (`search-query.ts`) la decide desde la 14.6 con cinco casos con nombre —*«vuelve a la ruta de la ciudad y suelta todos los filtros»*, *«la ciudad sobrevive: es el contexto de la búsqueda, no un filtro»*, *«suelta la zona de la RUTA, porque la zona es un filtro y la ciudad no»*, *«deja el acordeón donde estaba: limpiar no es cerrar»* y *«no se lleva por delante los parámetros ajenos»*—, y `buildSearchPanel` dos más. **Lo que ninguna prueba miraba es qué ruta le pasa cada página como `cityPath`.** Medido: pasarle `basePath` en `app/alquiler/[ciudad]/[zona]/page.tsx` —la ruta que YA nombra la zona— deja «Limpiar todo» conservando la zona y **no pone roja ni una sola de las 2595 unitarias**. Es el modo de fallo de la 16.9: el dominio entero escrito y probado, y el único llamador que importa sin cablear. La afirmación que faltaba se lee sobre el cuerpo servido, en `zona-sin-javascript.test.tsx`: *«suelta la zona del camino y todos los filtros, y deja la ciudad»*, con las DOS direcciones —la de la ciudad presente y la de la zona ausente—, porque un enlace a la ciudad pelada podría ser cualquiera de la miga de pan. Con ella, la misma mutación pone roja **1**. **Y la 20.6 no la contradice**: su corrección del 2026-09-02 dejó `homeCityChips` como el único selector de ciudad que el producto sirve, y compone `/?ciudad=…` **desde cero**, así que «la ciudad es el contexto» sigue siendo verdad de otra manera — cambiar de ciudad es empezar otra búsqueda desde el inicio, no limpiar ésta
- [x] 14.23b **CORREGIDA 2026-08-22 con el documento maestro del fundador en el repositorio.** Escribía `/buscar?ciudad=…`, que la 14.24 ya había descartado. El propio `design/especificaciones/Rentas - Flujos y funcionalidades.md` §7ter lo cierra desde el otro lado: *«Esto reemplaza los ejemplos anteriores del documento (`/buscar?ciudad=…` y `/aviso/84512`)»*, y fija `/alquiler/<ciudad>?zona=…&min=…&max=…&hab=…&tipo=…&pag=…`. **La deuda que la 14.24 dejaba abierta —«pendiente de corregir en los documentos del fundador»— está saldada.** Lo que queda de esta tarea es sólo lo que no era la ruta: un parámetro inválido se ignora con un aviso en vez de romper la página, y una zona que ya no existe se descarta dejando viva el resto de la búsqueda (esto último ya ships, PR #84). Renumerada a 14.23b porque el plan tenía DOS tareas 14.23. **CERRADA 2026-09-02, y lo que faltaba era lo mismo que en la 14.22b: que la frase llegue a la pantalla.** La regla y su texto estaban desde la resolución de ubicación (ver su anotación más abajo): `resolveSearchLocation` saca el parámetro de la query y devuelve `ZONE_QUERY_NOT_ALLOWED_NOTICE`, con tres casos nombrados en `search-location.test.ts`, y la otra mitad —un `?filtros=` que nombra un grupo que ya no existe— la cubre `STALE_FILTER_GROUP_NOTICE`. **Medido: apagar el bloque que dibuja el aviso en `app/alquiler/[ciudad]/[zona]/page.tsx` no ponía roja ninguna de las 2595 unitarias.** El dominio podía devolver el aviso perfecto y la página no dibujarlo, sin que nada se viera roto. `zona-sin-javascript.test.tsx` lo ata ahora sobre el cuerpo servido: *«sale el aviso, y el parámetro no se arrastra a ningún enlace»* —las dos mitades juntas, porque «ignorado» es falso si el parámetro sigue viajando— y su negativo, *«sin el parámetro no hay aviso: no se avisa de lo que nadie pidió»*. Con ellas, la misma mutación pone roja **1**
- [x] 14.24b An expired listing reached by direct link shows the ficha marked expired plus active listings from the same zone (F12) — needs `not-found`/expired handling, which is 11b.3. **CERRADA 2026-08-27 como duplicado, no como trabajo: es la 11.8, y se cierra nombrándola en vez de dos veces.** Palabra por palabra es el mismo requisito, y la 11.8 lo cerró hoy con sus pruebas nombradas —`app/alquiler/[ciudad]/[zona]/[slug]/ficha-vencida.test.tsx`: «responde con la pantalla del aviso y dice que venció» y «trae los avisos activos de la misma zona en el cuerpo de la respuesta»— más el camino caminado con el script apagado en `tests/e2e/camino-de-lectura-sin-javascript.spec.ts`. **Un requisito con dos números es peor que uno sin ninguno**: el segundo se cierra por su cuenta y el plan cuenta dos entregas donde hubo una. Éste queda cerrado POR la 11.8 y sin código propio; la deuda de numeración que la 14.44 destapó era ésta, y ésta es su otra mitad. **Renumerada a 14.24b el 2026-08-26: había DOS tareas 14.24** — la otra es la que resolvió que `/buscar` no existe, y es la que todo el plan cita por su número. Dos tareas con el mismo número es una que alguien da por hecha mirando la otra, que es el mismo defecto que la 20.13 ya corrigió para 14.22/14.23. **No la creó este PR**: venía de antes, y se destapó contando los números al agregar la 14.44

### 14e. Deliberately NOT doing

Recorded so nobody adds them as an improvement: map of results, address autocomplete, free-text search over listings, relevance ordering, favourites without an account. Each has a functional reason in the founder's document, not a scheduling one.

**Redis is not in this phase, and that is a decision with a date on it.** The founder proposed it for search speed (2026-08-21). It was declined *for search* on the product's own terms: F7 requires the **exact** count, and a cache returns a stale one — a button reading "Ver 47 avisos" over a list of 44 breaks the only thing that button is for, and every publish, expiry, renewal and report invalidates it. The catalogue is also small by the founder's own measure, who set "varios cientos" as the horizon at which free-text search reopens. The engine ships behind a port and gets **measured**; if real data proves it slow, Redis enters as an adapter without touching the domain. Where it is already expected to earn its place is task 6.10's per-account reveal limit, which needs a counter shared across serverless instances — a database is the wrong tool for that, and a cache is the right one.


### 14f. Lo que las láminas de Lista y Filtros pidieron y no está (2026-08-25)

Del proyecto de diseño `Rentas - Lista y Filtros - Desktop/Mobile.dc.html`, cruzado contra el código. Ninguna es una pantalla nueva: son comportamientos que las pantallas ya construidas todavía no tienen.

- [x] 14.30b **La pastilla de búsqueda como UN componente, presente en todas las pantallas menos la ficha** (acotado por el fundador el 2026-08-25 — ver la acotación al final de esta tarea). Su propia sección del diseño (14i) la define: tres piezas dentro de un mismo borde y sin divisores — texto, filtro y lupa. **El filtro no aparece cuando no hay zona elegida** ("sin búsqueda no hay nada que filtrar"), que es su estado en `/mis-avisos` e importar. Con zona muestra el nombre y el conteo en la segunda línea; con filtros la etiqueta pasa a "3 filtros" en acento, **sin badge — la palabra dice el estado**. **Renumerada a 14.30b: ya existía una 14.30** (línea de arriba, "reescribir el comentario de `app/page.tsx`") — dos tareas con el mismo número es una que alguien da por hecha mirando la otra (mismo defecto que 20.13 ya corrigió para 14.22/14.23). **Hecho, y desde la 14.39/14.40/14.41 la frase «en todas las pantallas» es verdad de verdad**: el inicio, la ficha, las dos pantallas de resultados y `/mis-avisos` la dibujan. Cuando se marcó esta casilla la pastilla existía como pieza y estaba cableada en una sola pantalla — lo decía su propia última línea, y la 14g existe por eso. `src/modules/listing-catalogue/domain/search-pill.ts` (`resolveSearchPill`, `formatListingCount`) decide el estado — 8 casos en `search-pill.test.ts`, incluida la cadena en blanco y el `filterCount` negativo. `components/molecules/SearchPill.tsx` + `.module.css` sólo traducen ese estado a marcado — 11 casos en `SearchPill.test.tsx`, con `renderToStaticMarkup` (mismo mecanismo que `SearchBar.test.tsx`): el filtro está AUSENTE (no vacío) sin zona, "Filtros" sin acento sin filtros aplicados, "N filtros" en acento con filtros, nunca un badge. **ACOTACIÓN de la 14i, decidida por el fundador el 2026-08-25 ("seguí el diseño, que fue lo que se decidió acá"): «presente en todas las pantallas» es «en todas MENOS la ficha».** Ninguna de las dos láminas de la ficha dibuja la pastilla — la de escritorio gasta el slot central en la marca y la de móvil dibuja dos hijos y ya. Una ficha no es una búsqueda: no hay nada que filtrar ni conteo que resumir. Queda cableada en **cuatro** pantallas: vacía en `/` y `/mis-avisos`, `selected` en `/alquiler/<ciudad>[/<zona>]`. Y lo sostiene el tipo y no un comentario: `NavProps` es una unión de dos formas y `pill` junto a `back` no compila (ver 14.40). `SearchBar` y `SearchSummaryBar` quedaron **huérfanas** al hacerlo y se borran en la 14.42.
- [x] 14.31 **En móvil el filtro de la pastilla pierde la palabra, nunca el número.** "A 360 px la palabra le robaba 44 px al nombre de la zona, y el nombre es contenido". **Hecho.** `SearchPill.module.css`, `@media (max-width: 480px)`: `.filterWord { display: none }` / `.filterCount { display: block }`. Probado por selector (no por cercanía en el texto — la primera versión de la prueba no distinguía qué regla apagaba qué, y no se ponía roja si se invertían las dos) en `SearchPill.test.tsx`. Mutación: invertir las dos declaraciones pone roja exactamente esta prueba; restaurado.

### 14g. UI-A: el nav y la pastilla, wiring pendiente (2026-08-25)

`14.30b`/`14.31` construyen la pastilla como pieza aislada, y `20.4` construye el nav que la contiene — los dos con su propio piso de pruebas. **Ninguno de los dos está cableado todavía en `/`, `/alquiler/<ciudad>[/<zona>]` ni la ficha** — cada una de esas pantallas sigue con su encabezado ad hoc (`app/page.tsx`'s `styles.bar`, `SearchSummaryBar`), y reemplazarlos es una porción propia con su propio riesgo de regresión sobre pantallas ya probadas. Requerido antes de dar 14.30b/20.4 por "presente en todas las pantallas" de verdad, tal como pide su propio texto.

**Esta sección no tenía casillas propias para el cableado**, que es la forma en que un trabajo pendiente desaparece: la prosa lo nombraba y ninguna tarea lo pedía. Van numeradas desde la 14.39 — la 14.30b ya tuvo que renumerarse una vez por chocar con otra 14.30, y la 20.13 corrigió dos choques más, así que acá se sigue después del último número usado y no se reabre uno intermedio.

- [x] 14.39 **El inicio (`/`) deja su encabezado propio y dibuja el `Nav`.** `app/page.tsx` tenía marca + `SearchBar` + "Publicar" con su propio CSS (`home.module.css`: `.bar`, `.barInner`, `.brand`, `.search`, `.publish`), y dos encabezados arrancan idénticos y se separan en el primer arreglo apurado. **Hecho.** `app/page.tsx` dibuja `<Nav account publish pill signInHref="/signin">`; el estado de la barra sale de `resolveNavAccount`/`resolveNavPublish` y el de la pastilla es `{ kind: "empty" }` (14i: sin zona no hay filtro). Las reglas dead del `home.module.css` se borraron.

  **El riesgo real era el mecanismo de búsqueda, no el pixel**, y quedó atado por dos lados: `components/molecules/SearchPill.test.tsx` → "carga el mecanismo del inicio sin degradarlo" renderiza la pastilla con el `homeSearchForm()` **real** y comprueba `method="get"`, el `action`, el nombre del campo contra `HOME_SEARCH_PARAM` y un `type="submit"` de verdad; `app/inicio-contract.test.ts` → "arma la pastilla con el MISMO formulario que el servidor traduce" comprueba que la página no escriba un `action` ni un `name` propios. Mutación: escribir `name="zona"` en `SearchPill.tsx` (el literal que la lámina 14i dibuja, o sea el error realista) pone roja exactamente la primera; escribir `action: "/"` en `app/page.tsx` pone roja exactamente la segunda. Las dos restauradas y verificadas en verde.

  Las ramas de `choices` y de «no entendí» del buscador **no se tocaron** — atadas por "sigue dibujando las opciones y el «no entendí» del buscador" en el mismo archivo.
- [x] 14.40 **La ficha dibuja el `Nav`, con el enlace de vuelta y sin pastilla** — y la marca cede el lugar **sólo en móvil** (ver la corrección de abajo: el texto de la 14.38 decía que cede siempre, y las láminas dicen que no). **Hecho.** `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` dibuja `<Nav … back={{ href: back.href, label: back.label }}>` **fuera del `<main>`** —adentro sería la cabecera de la región de contenido y no la del documento—, y borra su `styles.bar`/`styles.back`. Probado en `nav.test.ts` (nuevo, al lado de `volver-a-resultados.test.ts` y con el mismo mecanismo: se lee el archivo, porque lo que puede fallar es una relación entre la página y el dominio).

  **CORRECCIÓN sobre el texto de la 14.38, con su razón.** La 20.4 dejó el `back` del `Nav` dibujando un `←` mudo con la etiqueta escondida en un `aria-label`. Cablearlo destapó que eso es incorrecto: `resultsLink` (16.9) devuelve **dos acciones distintas** — «← Resultados» cuando hay una búsqueda a la que volver, y «Ver avisos en Chacao» cuando no la hay, con su propio comentario diciendo que en ese caso *"su texto no dice volver"*. Un `←` pelado dibuja las dos iguales y le promete una vuelta a quien llegó desde Google. Ahora el `Nav` **dibuja la etiqueta del dominio**, flecha incluida cuando corresponde, que además es lo que las dos láminas de la ficha dibujan («← Resultados» con texto a la vista). `Nav.test.tsx`: "dibuja el texto de la vuelta, que es el que decide el dominio", comprobado por contenido (`>texto<`) y no por `toContain` suelto — la primera versión pasaba con el texto escondido dentro del `aria-label`, exactamente el falso verde que la 14.31 ya había atrapado una vez.

  **Una sola lectura de sesión por petición, y es nueva.** La ficha ya leía la sesión para el bloque de contacto; la barra la necesita para el control de cuenta. Con `strategy: "database"` eso serían dos viajes HTTP a Neon en la pantalla más visitada del sitio. `app/_lib/session.ts` las deduplica con el mismo `cache` de React que esta página ya usaba para `findDetail`. Probado por "lee la sesión una sola vez, por el puerto memoizado".

  **CORRECCIÓN — RESUELTO por el fundador, 2026-08-25: "seguí el diseño, que fue lo que se decidió acá".** La primera versión de esta tarea siguió el texto de la 14.38 por encima de las láminas y dejó la discrepancia anotada. El fundador la cerró al revés: **ganan las láminas**, y leídas de cerca dicen más que "la marca cede".

  - **`Rentas - Ficha - Desktop.dc.html` línea 45** — encabezado de 72 px, contenedor de 1100, `justify-content:space-between`, **tres** hijos: `← Resultados` · `rentas` · `Publicar gratis`. **La marca NO cede en escritorio**: el `←` toma un tercer slot y la marca se corre al centro.
  - **`Rentas - Ficha - Mobile.dc.html` líneas 93-95** — encabezado de 56 px, `space-between`, **dos** hijos: `← Resultados` y la placa `Dueño`. Acá sí cede, porque a 360 px no caben tres.

  **Las dos láminas tienen razón: describen anchos distintos.** El texto de la 14.38 (*"la marca cede su lugar al ←"*) vale sólo para el móvil. Es el mismo desdoble móvil/escritorio que la 14.36 ya resolvió una vez para el panel de filtros, con una diferencia: allá una lámina estaba desactualizada, y acá **ninguna lo está**. Resuelto como **un solo encabezado con punto de quiebre, nunca dos implementaciones** — el argumento que `SearchFilters` y `app/home.module.css` ya dejaron escrito: `Nav.module.css`, `.brandCentre` con `display: none` en móvil y `display: flex` en el bloque de 768 px.

  **Medido, no leído.** `tests/measure/layout.spec.ts` monta el `Nav` real en el arnés de `app/measure/page.tsx` y lee geometría renderizada: *"en la ficha a 1280 la marca no cede, se corre al centro"* (el `←` a la izquierda de la marca, y el centro de la marca a ±4 px del centro de la barra) y *"en la ficha a 360 la marca no se dibuja"*. Mutaciones, cada una con su única prueba roja: sacar la marca del marcado → *"la marca sigue en el marcado"*; mostrarla en móvil → *"en móvil la marca no se dibuja"*; esconderla en escritorio → *"en escritorio la marca vuelve"*. Las tres restauradas.

  **La ficha NO lleva pastilla, y el tipo lo hace inexpresable.** Ninguna de las dos láminas la dibuja — la de escritorio gasta el slot central en la marca. `NavProps` pasó de cinco campos con dos opcionales a una **unión de dos formas**: `NavWithSearch` (pastilla, `back?: never`) y `NavWithReturn` (vuelta, `pill?: never`). Así una ficha con pastilla y una pantalla de resultados con flecha de vuelta **no compilan**, en vez de compilar en silencio — misma forma que `ListingSearchPort` («no hay `searchAll` ni un valor comodín») y lo que AGENTS.md §7 llama preferir el modo de fallo que es el rechazo. Lo comprueba `tsc` y no el runtime: un `@ts-expect-error` en `Nav.test.tsx` queda sin usar y `pnpm typecheck` falla si la unión vuelve a admitirlas juntas (mutación corrida: `TS2578`).

  **Se mantiene la marca como enlace y no como el `<span>` que exporta la lámina.** El `.dc.html` es una referencia, no código a copiar (AGENTS.md §2), y una marca sin destino le quita a la ficha su único camino al inicio.

  **La marca lleva punto, aunque esta lámina lo omita.** `Ficha - Desktop` escribe `rentas` sin punto, igual que `Entrar` y `Publicar - Desktop`; `Sistema`, `Cuenta e Importar` y `Lista y Filtros` lo escriben con punto. Manda `SISTEMA.md`, que es el sistema de registro y lo dice textual: *"no hay logotipo: la marca es la palabra «rentas.»"*. Queda una sola copia de la cadena en `Nav.tsx` (`WORDMARK`), porque escrita dos veces una de las dos se retipea de memoria.

  **Divergencias que quedan ABIERTAS y no se tocaron** (requerida confirmación del fundador): ninguna lámina de la ficha dibuja `Entrar`, y la de escritorio dibuja `Publicar gratis` como única acción mientras la de móvil no dibuja ninguna. Se conservan el control de cuenta y `Entrar` porque **ninguna de las dos láminas dibuja la ficha CON sesión**, y sacarlos dejaría a quien entró sin camino a `/mis-avisos` desde la pantalla más visitada del sitio. Tampoco se adoptaron las alturas de la lámina (72/56) sobre los tokens `--nav-h`/`--nav-h-desktop` (60/68) que fijó el artboard 14a: es la misma barra, y no puede medir distinto según la pantalla en la que esté.
- [x] 14.41 **`/alquiler/<ciudad>[/<zona>]` reemplazan `SearchSummaryBar` por el `Nav` con la pastilla en estado `selected`.** **Hecho.** Las dos páginas dibujan `<Nav>` con `resolveSearchPill({ zoneLabel: panel.headline, resultCount: total, filterCount: panel.pillFilters })`. Probado en `app/alquiler/[ciudad]/nav-contract.test.ts`, que corre las MISMAS aserciones sobre los dos archivos — dos pantallas cableadas por separado es cómo empiezan a discrepar, y la de zona es la de ciudad con un segmento más.

  **La contradicción que el cableado destapó, y no se resolvió en la página.** `countActiveFilters` cuenta la zona como filtro, porque el engranaje abría un acordeón que TENÍA paso de zona. La 14.36 lo sacó («la ubicación pasa a vivir SOLO en la ruta») y la 14i lo dice desde el otro lado («ciudad y zona no están ahí: eso lo resuelve el texto»); la lámina 7c lo dibuja — con dos zonas, precio, habitaciones y dueños puestos, la pastilla dice **«3 filtros»**, no 4. Pasarle `activeFilters` a la pastilla habría dibujado un número que no abre lo que abre, sin poner roja ninguna prueba. Se agregó `countPillFilters` en `search-accordion.ts` (6 casos, incluida la invariante «es el del engranaje menos la zona, nunca otra cosa») y `pillFilters` en `SearchPanelModel` (3 casos en `search-panel.test.ts`), **en el dominio y no como una resta en `app/`**. Mutación: hacer que `countPillFilters` cuente la zona pone rojas 6 pruebas; escribir `filterCount: panel.activeFilters` en la página pone roja exactamente la del contrato. Restauradas las dos.

  **Lo que se pierde y con qué se cubre, dicho antes de que alguien lo note usando el sitio.** La `SearchSummaryBar` sólo existía bajo 768 px y llevaba tres cosas: el `←` a la ciudad o al inicio, `panel.summary` («9 avisos · $250 – $700 · 2 hab · dueños») y el engranaje. El engranaje pasa a ser el filtro de la pastilla, con la misma dirección `…#filtros`. La flecha la cubre la miga de pan, que ya estaba y se dibuja siempre — atado por "las dos conservan la miga de pan". `panel.summary` **no se dibuja en ninguna parte ahora**: la lámina 6c pone «Chacao, Altamira / 9 avisos» y el detalle de los filtros pasa a las fichas quitables de la 14.33, que es tarea aparte. Queda como dato del modelo sin quien lo pida.

  **Desvío de lámina, anotado igual que el de la 20.4.** El artboard móvil 6c dibuja la fila de resultados con un `←` en lugar de la marca; el de escritorio 7b/7c dibuja `rentas.`. Se siguió el escritorio, que es la misma resolución que la 14.36 ya fijó para esas dos láminas («el móvil es anterior a la pastilla y no se actualizó»), y además el `Nav` elige marca o vuelta en el marcado y no por ancho. Requerida al fundador la confirmación.
- [x] 14.43 **CERRADA 2026-09-03 por la 14.54, que la resolvió borrando su premisa en vez de eligiendo una de sus tres salidas — ver la nota del fundador al final de esta entrada. La placa del publicador se muda al encabezado en móvil, y sólo en móvil.** `Rentas - Ficha - Mobile.dc.html` líneas 93-95 dibuja el encabezado de 56 px con dos hijos: `← Resultados` y la placa `Dueño` —`background: var(--ink)`, `color: var(--surface)`, versalita de 10 px con `letter-spacing: .04em`—, que es exactamente `.owner` de `PublisherBadge`. **Nadie lo construyó y ninguna tarea lo nombraba**; se destapó leyendo las láminas para la 14.40.

  **No es "dibujarla también arriba": es que se MUEVE.** Cada lámina dibuja la placa **una sola vez** — a 360 px en el encabezado, a 1280 px en la columna de datos, que es donde el código la pone hoy. O sea que el mismo elemento cambia de sitio con el ancho, y eso choca de frente con el comentario que la ficha ya tiene escrito al lado de `<PublisherBadge>`: *"Va acá y no en la barra porque en escritorio encabeza la columna de datos, y dibujarlo dos veces sería tener dos fichas otra vez."* Hay que elegir entre dibujarla dos veces con una escondida por ancho —lo que ese comentario desaconseja— o moverla con CSS desde un solo sitio del DOM, que es difícil porque los dos sitios viven en subárboles distintos (`Nav` y `DetailSplit`).

  **Lo que NO bloquea, verificado:** `design-contract.test.tsx` (1b.7) fija sólo la construcción de la placa —`.owner` con relleno, `.broker` con borde, y el contraste de `--ink` sobre `--surface`— y **nunca dónde se dibuja**; `ficha.test.ts` ordena `gallery → summary → body → contact` y tampoco la ancla. Mudarla no rompe ninguna de las dos.

  **Lo que sí hay que resolver antes:** el `Nav` lo comparten cinco pantallas y la placa es un dato de una sola. Meterla en `NavProps` le agregaría a la barra del inicio un campo que ahí no significa nada — probablemente sea una tercera forma de la unión (`NavWithReturn` más el publicador) y no un opcional más

  **NOTA DEL 2026-09-02, y es al revés de lo que parece: la 14.53 NO la absorbe ni la abarata en código.** Aquélla movió la placa de la TARJETA de la cuadrícula, encima de la portada; ésta la mueve en la FICHA, al encabezado de 56 px. Son dos superficies, dos subárboles y dos láminas distintas, y no comparten una línea. **Lo que sí queda resuelto es su duda de método**: la 14.53 tuvo que elegir entre esconder por ancho y dejar de dibujar, y la razón que decidió vale igual acá — **el servidor no sabe el ancho de la pantalla**, así que dibujar en un sitio a 360 y en otro a 1280 se resuelve con CSS o no se resuelve sin romper el piso de AGENTS.md §2 o la cacheabilidad de una sola respuesta. O sea que de las dos salidas que esta tarea plantea, la de «dibujarla dos veces con una escondida por ancho» deja de ser la desaconsejada por defecto y pasa a ser la única que el servidor puede sostener; lo que queda por resolver sigue siendo el `NavProps`, que es lo de arriba

  **CONSTRUIDA Y MEDIDA EL 2026-09-02, y NO se marca hecha: le falta UNA decisión del fundador, y es de las suyas.** La mudanza está construida, probada y con su contraste rehecho; lo que queda abierto es lo que la mudanza destapó al medirla, que es la divergencia que la 14.40 ya había dejado escrita.

  **Lo que se construyó.** `NavProps` gana una **tercera forma**, `NavWithListing` = vuelta + publicador, y la tarea lo había previsto bien. El primer intento puso `publisher` obligatorio en `NavWithReturn` con la razón escrita de que «`back` tiene exactamente una pantalla, la ficha»; **`pnpm typecheck` lo contradijo en la primera corrida**: `/reportar` también vuelve, y no muestra ningún aviso. Por eso la distinción es de forma y no un opcional — fuera de la ficha el publicador es **inexpresable**, que es lo que la tarea pedía resolver («meterla en `NavProps` le agregaría a la barra del inicio un campo que ahí no significa nada»). Se anota como corrección de una corrección, con el gate que la destapó.

  **La duda de método se resolvió con el argumento que la 14.53 dejó escrito, y no se volvió a discutir.** El servidor no sabe el ancho de la pantalla: no hay ancho en una petición HTTP, husmear el `User-Agent` rompe una sola respuesta cacheable para todos los anchos y decidirlo en el cliente contradice el piso de AGENTS.md §2. Así que la placa se dibuja en los dos sitios y cada hoja esconde la que no toca — `Nav.module.css` la apaga desde 768 px, `ficha.module.css` la apaga por debajo. `display: none` y no una atenuación: además la saca del árbol de accesibilidad, así que un lector de pantalla no anuncia «Dueño» dos veces.

  **Que se vea EXACTAMENTE una a cada ancho no lo puede afirmar ninguna de las dos hojas.** Viven en archivos que no se conocen, y las dos pueden estar verdes con las dos placas visibles o con ninguna. Lo cuenta `tests/measure/ficha.spec.ts` en Chromium, contando cajas dibujadas y **filtrando por estructura y no por nombre de clase** —las clases son hashes de compilación—: a 360 encabezado=1 / columna de datos=0, a 1280 encabezado=0 / columna de datos=1, y lo mismo con «Inmobiliaria», que es la palabra larga y la mitad de los avisos.

  **La garantía de la 14.25 NO se heredó: se volvió a medir sobre la superficie nueva.** El encabezado tiene su propio fondo y su borde inferior, así que se lee el color que el navegador **pintó** debajo de cada placa y se rehace la cuenta ahí, con la misma aritmética que la 14.53 usó sobre la portada: barra `rgb(255,255,255)` con alfa **1**, fondo dueño `30,32,34` (L=0,014) contra fondo inmobiliaria `255,255,255` (L=1,000), **separación 0,986** (la 1b.7 pide > 0,3), contrastes **16,34** y **6,40**, y el borde de la inmobiliaria a **3,96** con 1 px. Idénticos a los de la tarjeta, que es la consecuencia de que la placa no se repinte: el envoltorio esconde y nada más, y hay una prueba que lo exige (`no repinta`).

  **EL HALLAZGO, y es lo que va a decisión: a 360 px el encabezado se parte en dos renglones.** Medido: 60 px → **96 px**, igual con «Dueño» (60 px de ancho) que con «Inmobiliaria» (106 px). La cuenta: a 360 el interior útil son 336 px, la vuelta ocupa 89 y las acciones 203, así que **quedan 36 px libres** y la placa más corta necesita 68. La lámina dibuja ese encabezado con **dos** hijos —`← Resultados` y la placa— y la barra servida lleva **cuatro**, porque la 14.40 conservó «Publicar gratis» y «Entrar» con su razón escrita: *«ninguna de las dos láminas dibuja la ficha CON sesión»*, y sacarlos dejaría a quien entró sin camino a `/mis-avisos` desde la pantalla más visitada.

  **Las tres salidas, con lo que cada una contradice, y ninguna es de quien mide** (misma forma que la 14.53 usó antes de que se decidiera):
  1. **Se va «Publicar gratis» del encabezado de la ficha en móvil.** Es la única que hace entrar las dos palabras (libera ~136 px de los 78 que faltan para «Inmobiliaria»). Contradice la 14.38, que dice textualmente que sin sesión **NUNCA** se esconde — *«es cuando hay que provocar»*.
  2. **Se va «Entrar».** Libera ~67 px: alcanza para «Dueño» y **no** para «Inmobiliaria», o sea que arregla la mitad de los avisos y deja la otra mitad partida. Y contradice la divergencia que la 14.40 dejó abierta a propósito.
  3. **Se acepta el encabezado de dos renglones en la ficha en móvil.** No contradice ninguna decisión escrita, pero es un cambio visible de 36 px en la pantalla más visitada del sitio que ninguna tarea pidió — la clase de cambio que la 22.15 mandó a decisión del fundador en vez de hacerlo de oficio.

  **La medición ya está puesta y se mueve sola**: la prueba fija con igualdad exacta que las acciones arrancan por debajo de donde termina la vuelta —una relación entre cajas y no una cota en píxeles, porque las cotas en píxeles dependen de la máquina—, así que el primer cambio que resuelva esto la pone roja diciendo que se resolvió.

  **Seis mutaciones, cada una con el texto objetivo presente exactamente una vez, sha256 antes ≠ después, restauración por copia de bytes con sha256 idéntica y `git status` limpio; cada una corrida DOS veces —con las pruebas nuevas en el árbol y con ellas aparcadas fuera—**: el encabezado deja de dibujar la placa (1 / 0 unidad, **4 / 0 medición**), la del encabezado no se apaga en escritorio (1 / 0 unidad, **3 / 1 medición**), la de la columna de datos no se apaga en móvil (1 / 0 unidad, **2 / 0 medición**), la ficha escribe el publicador a mano en vez de leerlo del aviso (1 / 0), el envoltorio repinta la placa (1 / 0) y **el dueño pierde su relleno, que es la garantía de la 14.25** (1 / 1 unidad, **3 / 2 medición**). Las columnas aparcadas que no dan cero son las que ya existían: `design-contract.test.tsx` y el arnés de la 14.53, que protegen al átomo y no a esta mudanza.

  **Costo en el paquete**: `pnpm budget:bundle` antes → después, la ficha **109.27 → 109.31 KB** gzip y `/` 108.11 → 108.14; son las reglas de las dos hojas y nada más. La peor ruta sigue siendo `/importar` con 110.60 contra un tope de 130.

  **DECISIÓN DEL FUNDADOR, 2026-09-03: la tarea se resuelve borrando su premisa, y pasa a la 14.54.** No se eligió ninguna de las tres salidas. El fundador preguntó si la ficha ya decía adentro lo de dueño o inmobiliaria — **lo dice**: `ContactBlock.tsx:149` dibuja «publica como dueño» / «publica como inmobiliaria» al lado del nombre, construido y sirviéndose. Y la lámina móvil dibuja las **dos**, arriba la placa y abajo las palabras, así que la repetición era del diseño. La decisión se queda con la de abajo y **la placa sale del encabezado**, que era lo único que necesitaba los 11 px. La mudanza construida acá se revierte con la 14.54; lo que NO se revierte y queda como saldo es el hallazgo medido —el encabezado de la ficha llevaba 203 px de acciones que ninguna lámina móvil pide— y la tercera forma de `NavProps`, que la 14.54 borra junto con la segunda.

- [x] 14.42 **Borrar `SearchBar` y `SearchSummaryBar` cuando dejen de tener quien las dibuje.** Las dos quedan huérfanas al terminar la 14.39 y la 14.41 — con su hoja de estilos y su archivo de pruebas, o sea con un gate en verde que no protege nada que se sirva. **No va en el mismo PR a propósito**: son ~380 líneas de borrado y el presupuesto de revisión son 400 sobre código revisable, así que meterlas adentro empujaría fuera del techo el cableado que sí hay que leer con cuidado. `design-contract.test.tsx` lee `SearchBar.module.css` y hay que actualizarlo en el mismo movimiento

  **Hecho.** Seis archivos borrados enteros: `components/molecules/SearchBar.{tsx,module.css,test.tsx}` (320 líneas) y `components/organisms/SearchSummaryBar.{tsx,module.css,test.tsx}` (197). Más `panel.summary`, que era la segunda línea de esa barra.

  **Cómo se probó que nadie las alcanzaba, que es lo único que puede fallar en un borrado.** No por «grep no encontró nada» —un `grep` de nombre lo engaña un re-export, un barril, un import dinámico o un nombre que es subcadena de otro—, sino por ausencia positiva de las cuatro vías que existen: (1) `components/` **no tiene un solo archivo barril** (`find components -name "index.ts*"` vacío), así que no hay re-export posible; (2) el único `import` de cada pieza era **su propio archivo de pruebas**, buscado por RUTA y no por nombre (`from "./SearchBar"`), que es lo que atrapa un renombre; (3) no hay `next/dynamic`, `React.lazy` ni `import()` de ninguna de las dos en todo el repositorio; (4) `pnpm budget:bundle` da los **mismos bytes exactos** ruta por ruta antes y después —108.09 KB gzip la peor— y eso es la prueba positiva de que ya no viajaban en ningún bundle. Las menciones a `SearchBar` en `SearchPill.tsx:61` y `AccountMenu.tsx:35` son **prosa en comentarios**, verificadas una por una, no imports.

  **`design-contract.test.tsx`: se borran las aserciones, no se remiendan.** Tres leían `SearchBar.module.css` —`.bar` con `--searchbar-h`, `.label` con `--searchbar-fs`, y el `it` entero de «la sombra es un token y no la línea del borde»—. Su sujeto era esa hoja y se fue con la pieza. **No se reapuntaron a `SearchPill.module.css`**, aunque esa hoja también usa `--shadow-raised`: una aserción mudada de sujeto dice seguir protegiendo lo de antes y protege otra cosa, que es peor que no tenerla. Sobrevive intacta la que mide `tokens.css` (50 ≥ 44 táctil), porque su sujeto no se borró.

  **HALLAZGO — `--searchbar-h` y `--searchbar-fs` quedan sin un solo uso.** Eran de `SearchBar.module.css` y de ninguna otra hoja. **No se borraron acá**: sacar un token es un cambio al conjunto (SISTEMA.md) y no un uso de él, y `lint:tokens` no lo pide porque verifica paridad de temas y literales, nunca si alguien lo usa — o sea que este hueco no pone nada en rojo, igual que el `--fpb` del `<h1>` que ese mismo archivo documenta. Queda como **14.48**.

  **DESVÍO — `summariseSearch` se borró y la tarea no lo nombraba, y lo destapó la cobertura y no una lectura.** Al sacar `panel.summary`, su único consumidor de producción, la función quedó sin quien la llame. Medido: su bucle de atributos pasó a ser la **única sentencia sin cubrir** de todo `search-accordion.ts` (línea 303) — sus tres pruebas directas nunca le pasaban atributos, así que esa rama la ejercía el panel y nadie más. Una función sin consumidor cuyo cuerpo sólo cubre a medias su archivo es exactamente el gate en verde que esta tarea nombra, así que se fue con sus tres pruebas. Cascada cero: `priceSummary`, `roomsSummary`, `PUBLISHER_SUMMARY` y `ATTRIBUTE_SUMMARY` los siguen usando `resolveSearchSteps` y `describeFilter`.

  **HALLAZGO no resuelto — `model.activeFilters` quedó huérfano por este mismo borrado y la tarea no lo nombra.** Era el número del engranaje, o sea `SearchSummaryBarProps.activeFilters`: la misma barra, la misma tarea. Hoy sólo lo leen sus propias pruebas de dominio. Se deja en pie porque la lista de la tarea nombraba `panel.summary` y no éste, y borrarlo arrastra `countActiveFilters`, que sí tiene seis casos propios. Queda como **14.49**.

  **Evidencia.** `pnpm test:coverage` 141 archivos / 1654 pruebas en verde, salida 0, sin una sola aserción reescrita. Las líneas sin cubrir de `search-panel.ts`, `search-options.ts` y `search-accordion.ts` son **el mismo conjunto exacto que antes**, sólo renumeradas (comparado con el reporte JSON de v8 contra la base: 597→567, 216→206, 361→339, 537→507, 560→530 en `search-panel.ts`; 162→110 y 214→162 en `search-options.ts`), o sea cero huecos nuevos. `typecheck`, `lint`, `lint:tokens`, `test:e2e` (14 pasadas, 10 saltadas), `test:measure` (25) y `build` en verde.

- [x] 14.32 **El panel de escritorio deja de ser por pasos.** El diseño lo llama por su nombre: "la secuencia del móvil existe porque no cabe nada más, no porque sea mejor". En escritorio van precio, habitaciones, quién publica y atributos **a la vez**. **Hecho.** `search-accordion.ts`: `SEARCH_STEPS` pasa de `["ciudad","zona","precio","habitaciones"]` a `["precio","habitaciones","publica","atributos"]` —la 14.36 saca la ubicación, y el paso «habitaciones» se parte en los tres encabezados que la lámina 7b dibuja—; `SearchPanel.module.css` abre los cuatro en el bloque de 768 px. Probado **midiendo cajas renderizadas** en `tests/measure/layout.spec.ts`: *"14.32: a 1280 los cuatro grupos se ven a la vez — no hay secuencia"* y *"14.32: a 360 sigue siendo un acordeón — sólo el grupo abierto se dibuja"*. En dominio, `search-accordion.test.ts` → *"son precio, habitaciones, quién publica y atributos — ciudad y zona ya no están"*; en marcado, `SearchPanel.test.tsx` → *"cada grupo se abre con un enlace, así que el servidor decide cuál queda abierto"*.

  **CORRECCIÓN al texto de la tarea, verificada antes de escribir (AGENTS.md §5).** Decía que *"`SearchPanel.module.css` sólo cambia el área táctil a 768px"*, y era **falso**: la hoja ya traía un bloque `.step::details-content { content-visibility: visible }` con su comentario, o sea que alguien ya había intentado esta tarea en CSS. No se puede saber si funcionaba, y ése es el problema: **ninguna prueba lo miraba**, y en un navegador que no entiende `::details-content` el escritorio dibujaba el acordeón del teléfono con la hoja en verde. Es la misma clase de defecto que la 20.4 —`grid-template-columns` cierto y ciego—, y por eso lo que ahora lo sostiene es el arnés de medición y no una afirmación sobre la hoja.

  **Desvío deliberado: los grupos dejan de ser `<details>`.** El acordeón exclusivo del navegador (`<details name>`) resolvía el teléfono sin una línea de JavaScript, y era correcto mientras el escritorio dibujara lo mismo. Deja de servir en cuanto el escritorio tiene que mostrar los cuatro abiertos: **ninguna hoja de estilos puede volver a abrir de forma confiable un `<details>` cerrado** en los navegadores que este producto atiende —el interior de WhatsApp incluido—, así que sostenerlo costaba dos implementaciones, que es exactamente lo que la regla del fundador prohíbe (*"un solo componente con puntos de quiebre, nunca dos implementaciones"*, la misma que resolvió la 14.40). Lo que se pierde y se dice: **en el teléfono cambiar de grupo cuesta una vuelta al servidor** en vez de ser instantáneo. Lo que se gana: un solo marcado, y el mismo mecanismo con el script apagado. La 14.34 es donde el JavaScript vuelve como mejora.

  **Desvío de nombre: «tamaño» es «Habitaciones».** El fundador escribió *"precio, tamaño, quién publica y atributos"* y la lámina 7b dibuja los encabezados **Precio mensual · Habitaciones · Baños · Quién publica · La propiedad tiene**. No hay filtro de baños ni de m² en el modelo de datos —`FacetedSearchPort` no cuenta ni una cosa ni la otra—, así que el grupo se llama **Habitaciones**, que es el que existe. Inventar el control sin el conteo detrás sería un filtro que no filtra. Queda como **14.45**.
- [x] 14.33 **La pantalla de resultados pierde la barra lateral: los filtros viven sólo en el modal.** **RESUELTA por el fundador el 2026-08-26: en TODOS los anchos**, no sólo tras haber buscado. **Hecho.** Las dos páginas dejaron de dibujar `<SidebarLayout sidebar={<SearchPanel …>}>` y ahora dibujan `<SearchPanel model={panel} />` suelto y primero en el documento; el panel decide por su cuenta si se dibuja. Probado en `app/alquiler/[ciudad]/filtros-contract.test.ts`, que corre las MISMAS aserciones sobre los dos archivos: *"ya no dibuja la barra lateral, en ningún ancho"* y *"el panel se dibuja igual, y quién lo abre lo decide el dominio"*.

  **Que esté abierto es una lectura de la DIRECCIÓN, y ése es el piso sin JavaScript.** Al perder la barra, los filtros llegan por un solo camino: el control de filtro de la pastilla, que ya era `filtersHref` — *"la misma URL con el panel abierto desde el servidor"* (14i). Antes, que ese camino fuera un enlace era una comodidad; ahora es **la única forma de filtrar que existe**, así que un manejador de clic dejaría sin filtros a quien se quedó sin bundle. `resolveFilterPanel` (`search-accordion.ts`) lo decide, con `PANEL_OPEN_TOKEN = "todos"` para «abrilo y elegí vos el grupo» — 6 casos en `search-accordion.test.ts`, incluido *"presente pero vacío tampoco lo abre: es un campo que nadie llenó"*. En el marcado, `SearchPanel.test.tsx` → *"cerrado no dibuja nada: no hay filtros escondidos ocupando lugar"* y *"abierto es un diálogo con nombre, y con una salida visible"*. Y `tests/e2e/filtros-sin-javascript.spec.ts` lo corre con el script apagado en el proyecto `crawlability` — se salta sin despliegue, por la misma razón escrita en `busqueda-inicio.spec.ts`.

  **Las fichas quitables, que la 14g ya había anotado como pendientes de acá.** La 14.41 dejó dicho que `panel.summary` *"no se dibuja en ninguna parte ahora"* y que el detalle de los filtros pasaba a estas fichas. La lámina 7c las dibuja —«Chacao × Altamira × $250 – $700 × 2 habitaciones × Solo de dueños ×» y «Limpiar todo»— con la razón al lado: *"se saca un filtro sin abrir nada"*. `filterChips` en `search-panel.ts` las arma (6 casos en `search-panel.test.ts`) y `components/molecules/FilterChips.tsx` sólo las escribe (5 casos). **Una ficha por zona y no un bloque**, que es la diferencia con `describeFilter`: quitar «Chacao» tiene que dejar Altamira viva y devolver a `/alquiler/<ciudad>/altamira`, que es la dirección indexable — probado en *"quitar una zona deja viva a la otra, y la devuelve a su ruta canónica"*.

  **Y la cuadrícula cobra lo que la barra costaba.** Cuatro columnas en vez de tres (`ListingCard.module.css`): *"cuatro columnas de 254: 8 avisos sobre el pliegue, contra 6 antes"* (lámina 7c). El token `--card-w-desktop` ya valía 254 desde la 5.7; lo único que faltaba era el ancho. Medido y no leído, en `tests/measure/layout.spec.ts` → *"14.33: la cuadrícula gana el ancho de la barra lateral — cuatro columnas a 1280"*, que cuenta cuántas tarjetas comparten el borde superior. La prueba de hoja que había —`repeat(3, …)`— se actualizó y se anotó adentro que es de las que pueden ser ciertas y ciegas.

  **`components/layout/SidebarLayout` queda huérfano** al terminar esto, igual que `SearchBar` con la 14.39. Se borra en la **14.44**, no acá: este PR ya está muy por encima del techo de revisión.
- [x] 14.34 **El botón cuenta los resultados en vivo** — "baja de 70 a 9 mientras se filtra". Requiere JavaScript de cliente y `SearchPanel` es hoy explícitamente sin él (su propio comentario lo dice, y `SearchPanel.test.tsx` lo afirma). Depende de la decisión sobre la regla del camino de lectura: el conteo es mejora, el `<form method="get">` sigue siendo el piso. **Hecho 2026-08-27, y es el primer JavaScript de cliente que este cambio pone en el camino de lectura a propósito.**

  **El número NO costó un viaje de red más, y ése era el requisito duro.** La 14.11 dejó una sola consulta que devuelve las filas y todas las facetas, y `drizzle-faceted-search.ts` cuenta **cada faceta ignorando su propio filtro y respetando todos los demás**. Leído al derecho, eso significa que el conteo de una opción **es** el total resultante de elegirla — no una estimación. Y la misma consulta ya traía `withoutFilter` (las nueve relajaciones) y `cityTotal`, que son el mismo número al revés: cuántos quedan al SOLTAR. O sea que para los **diez enlaces del panel, en los dos sentidos, y para «Limpiar todo»**, el total ya viajaba con la página. No se agregó una consulta, ni un `fetch`, ni un `route handler`.

  **Qué se construyó.** `listing-search/domain/search-preview.ts` (`previewConfirmLabel`) decide qué número corresponde a qué opción; `search-confirm.ts` expone `confirmCountLabel` para que el servidor y el adelanto escriban **la misma etiqueta con la misma función** —dos formateos separados son dos que se separan, y el número parpadearía al llegar la respuesta—; `buildSearchPanel` cuelga un `previewLabel` de cada opción y un `clearAllPreviewLabel` del modelo; `SearchPanel.tsx` lo escribe como `data-preview` **desde el servidor**; y `components/organisms/LiveResultCount.tsx` —el único `"use client"`— lee ese atributo con **un solo oyente delegado** sobre el panel y cambia el texto del botón. Ni una regla en el frente: el componente lee un atributo y lo pinta.

  **Probado.** Dominio: `search-preview.test.ts` (8 casos: los dos sentidos de habitaciones, atributos y quién publica, «Limpiar todo», el cero con las palabras del servidor, y *"un número que la página no mandó NO se inventa: contesta null"*); `search-panel.test.ts` → *"el escalón elegido adelanta lo que queda al soltarlo, no lo que ya hay"* y *"una opción apagada no adelanta nada: no se puede tocar"*; `search-confirm.test.ts` → *"es la misma función que usa el botón del servidor"*. Marcado: `SearchPanel.test.tsx` → *"los adelantos son exactamente los diez enlaces que se pueden tocar"* —cuenta 9 ocurrencias y 2 opciones apagadas, en vez de un `not.toContain` que sigue verde si el atributo desapareció de todas—. **En un navegador de verdad**: `tests/measure/layout.spec.ts` → *"14.34: el botón baja de 16 a 9 al tocar el filtro, sin esperar al servidor"*, que **deja la navegación colgada** con `page.route` para volver determinista el medio segundo que la mejora existe para cubrir, y camina además el teclado (`Enter` sobre el enlace) y el borrado de la vista previa al tocar un encabezado.

  **El piso, medido y no afirmado.** `SearchPanel.tsx` **sigue sin ser un componente de cliente** —la aserción de la 14.33 sigue viva y sigue en verde— y el conteo del servidor viaja en el marcado: `SearchPanel.test.tsx` → *"el botón sale del servidor con su número escrito, sin ejecutar una línea de script"*, más `tests/measure/layout.spec.ts` → *"14.34: con el script apagado el botón sigue diciendo el número del servidor"*, que abre un contexto con `javaScriptEnabled: false` y verifica además que la opción sigue siendo un enlace con su `href`. **La mutación que lo prueba**: borrar el `label` del servidor del marcado (`{preview ?? label}` → `{preview}`) pone en rojo 3 pruebas, y desenganchar el oyente pone en rojo la de medición dejando la del piso en verde — que es exactamente la forma que tiene que tener una mejora.

  **Por qué la prueba de interacción vive en `tests/measure/` y no en `tests/e2e/`.** Aquella suite corre los MISMOS archivos en el proyecto `crawlability`, con el script apagado, donde una mejora de cliente no puede existir; ponerla ahí obligaba a un `test.skip`, y un `skip` es un gate en verde que no mide nada. **Hoy hay cero y este PR no agrega uno**: `pnpm test:e2e` corre 46 pruebas en los dos proyectos, todas pasadas, ninguna saltada.

  **El costo, ruta por ruta.** `pnpm budget:bundle` antes → después: `/alquiler/[ciudad]` 105.98 → **106.26 KB** y `/alquiler/[ciudad]/[zona]` 105.98 → **106.26 KB** (+0.28 KB gzip cada una); las otras diez rutas se movieron 0.01–0.02 KB, que es el ruido del hash de los trozos compartidos. La peor ruta del camino de lectura sigue siendo `/publicar/paso/[paso]` con 108.38 KB contra un tope de 130. **Y no se filtró a un trozo común**: `data-search-panel` aparece en exactamente tres archivos de `.next/static/chunks/` —los dos trozos propios de esas rutas y el del arnés `/measure`, que en producción devuelve 404—, o sea que las otras 23 entradas del `app-build-manifest.json` no lo cargan.

  **DESVÍO anotado — el precio no adelanta nada, y su ausencia es la decisión.** Es un rango escrito a mano en dos campos, no una opción de una lista, así que no tiene faceta que lo cuente. Adivinarlo habría sido el único número mentiroso de la pantalla, contra la regla transversal 3 que este botón existe para cumplir. Falla cerrado: sin número, el botón se queda con lo que el servidor escribió.

  **CORRECCIÓN al texto de la tarea, verificada antes de escribir (AGENTS.md §5).** Decía que el conteo *"requiere JavaScript de cliente"* para cambiar. **Ya cambiaba sin una línea nueva**: `AppLink` es `next/link` desde la 14.42, así que tocar un filtro ya era una navegación de cliente y el número ya bajaba de 70 a 9 sin recargar el documento. Lo que faltaba no era que cambiara, era **cuándo**: hoy cambia en el instante del toque en vez de cuando Neon contesta desde Venezuela. La tarea describía el problema con el vocabulario de antes de que `AppLink` existiera.
- [x] 14.35 **CERRADA 2026-09-02 POR SUSTITUCIÓN, no por haberse construido como estaba escrita.** La forma que pedía —la taxonomía entera filtrada en el cliente— está medida y no entra; la que sí entra está construida en la **14.51**, y lo que le falta al inicio queda vivo y con su costo escrito en la **14.52**. **Panel de sugerencias al escribir, filtrado en el cliente.** "Sin servicio externo: son ~2 KB filtrados en el cliente, así que aparece sin esperar red. Cada sugerencia trae municipio, ciudad y su conteo." `suggestFilters` ya existe y ya lo usa el servidor (14.17/14.20) — **la misma función alimenta las dos**, no una segunda copia en el cliente. **NO se construyó en la 14.34, y la razón es un número medido, no una preferencia (2026-08-27).**

  **«~2 KB» es falso para los datos que este repositorio tiene, y por un factor de quince.** Medido corriendo el parser real (`readTerritoryDocuments` + `readTerritoryToponyms` sobre `docs/territorio/`, 888 KB en 9 archivos): **10 municipios, 81 parroquias y 4.710 topónimos**. Serializado entero son 916.555 bytes crudos / **89,8 KB gzip**. Recortado al hueso —sólo el alias y a qué parroquia apunta, sin ids, sin ciudades y sin conteos— siguen siendo **30,1 KB gzip**. El margen del camino de lectura después de la 14.34 es de unos **21 KB** (peor ruta 108,38 sobre un tope de 130). **No entra ni en su forma mínima**, y su forma mínima además no serviría: sin id de zona ni ciudad, una sugerencia deja de ser un par (filtro, valor) con su ámbito, que es exactamente la regla de la 14.18 —`Centro` existe en Maracaibo y en Distrito Capital—.

  **Y el repositorio ya lo sabía.** `SearchVocabularyPort` existe con esa razón escrita adentro: *"la taxonomía real son miles de zonas más 3.547 alias; traerla entera en cada búsqueda es un recorrido secuencial por consulta"*. `DrizzleSearchVocabulary` estrecha con `ILIKE` a `LOOKUP_LIMIT = 60` filas **por lo escrito**. Un panel de cliente sobre el vocabulario entero es justo lo que ese puerto existe para no hacer. (De paso: el comentario dice 3.547 alias y hoy son 4.710 — creció y nadie actualizó el número.)

  **La segunda mitad de la tarea tampoco tiene de dónde salir.** *"Cada sugerencia trae municipio, ciudad y su conteo"*: `FilterSuggestion` y `SearchChoice` no llevan conteo, y la consulta del vocabulario no cuenta nada. El conteo por zona existe en OTRA consulta —`FacetCounts.byZone`, la de la 14.11— y sólo dentro de una ciudad ya elegida.

  **Las tres formas posibles, con lo que cuesta cada una.** (1) Mandar la taxonomía entera: 89,8 KB gzip, se lleva el tope de 130 por delante. (2) Un `fetch` por tecla: contradice la frase de la propia tarea —*"aparece sin esperar red"*— y es un viaje real por pulsación desde Venezuela, que es lo que la 14.11 existe para no pagar. (3) **Un vocabulario ACOTADO: sólo las zonas que tienen avisos activos, con su conteo.** Son decenas y no miles, y sugerir una zona vacía manda a una pantalla sin salida (regla transversal 4), así que recortar por ahí no es una degradación sino la respuesta correcta. **Esa es la propuesta, y es una decisión de producto**: con el script cargado se ofrecerían menos sugerencias que las que el servidor encuentra al enviar, a cambio de que ninguna lleve a un vacío. Queda como **14.51**, con su prerrequisito de datos en la **14.52**.

- [x] 14.51 **HECHA 2026-09-02, con la decisión del fundador tomada: pidió las tres tareas.** **El panel de sugerencias sobre un vocabulario acotado, que es la 14.35 con la forma que sí entra.**

  **Lo que se construyó.** `listing-catalogue/domain/bounded-vocabulary.ts` arma el vocabulario acotado a partir de lo que la pantalla ya tiene —el catálogo de ciudades y zonas que la ruta ya leyó, más `counts.byZone`, que la 14.11 trae en la MISMA consulta que las filas y las facetas—; `searchChoices` se **saca** de `resolveSearchDestination` (que ahora lo llama y colapsa) para que la lista exista sin colapsar, porque con una sola coincidencia `route` lleva la dirección y no la etiqueta; `SearchChoice` gana `countLabel`; y `components/client/SearchSuggestions.tsx` —el único `"use client"` que agrega este trabajo— cuelga del campo de la pastilla y dibuja lo que el dominio devolvió. **Ni una regla en el frente**: el componente tiene un `map` y un `if` sobre si la lista está vacía.

  **La misma función de las dos partes, y eso es literal.** El panel llama a `searchChoices`, y `resolveSearchDestination` —lo que corre en el servidor al enviar— llama a `searchChoices`. No hay una segunda copia del criterio en el cliente, que es lo que la 14.35 prohíbe y lo que mantiene «la etiqueta dice 9» y «la lista trae 9» como la misma pregunta.

  **`components/client/` y no `components/molecules/`, y lo destapó el gate.** `components/design-contract.test.tsx` prohíbe `"use client"` en átomos y moléculas: no es orden, es lo que mantiene el camino de lectura sin runtime. `CopyContact` ya había abierto ese directorio con el razonamiento escrito adentro. El archivo nació en `molecules/` y la suite lo puso en rojo.

  **El piso, medido y no afirmado.** `tests/e2e/sugerencias-sin-javascript.spec.ts` corre en los dos proyectos —`crawlability` con `javaScriptEnabled: false`— contra la aplicación compilada y el Postgres del arnés: la pastilla de `/alquiler/<ciudad>` busca con un `GET` del navegador y llega a `/alquiler/distrito-capital/altamira`, y el campo conserva dónde se está buscando (que es lo que se pierde si alguien vuelve controlado el `<input>` para leerlo desde el cliente). En el arnés de medición, además, con el script apagado el panel no existe y el formulario sigue siendo un `GET` con su botón. **No se agrega un solo `test.skip`**: los dos que la suite salta son los de `renewal-link` y ya estaban.

  **Y la mejora, en un navegador de verdad**: `tests/measure/sugerencias.spec.ts` (5 pruebas). Vive en `tests/measure/` y no en `tests/e2e/` por la misma razón que la 14.34 — aquella suite corre los mismos archivos con el script apagado, y ponerla ahí obligaba a un `skip`. Mide: «alta» ofrece Altamira con su ámbito y su conteo; «centro» ofrece **dos** opciones, una por ciudad (la regla de la 14.18 viva del lado del cliente); una zona con cero avisos **no** se ofrece y una con nueve **sí** —la pareja de la negativa, sin la cual un panel roto pasa igual—; y Escape cierra sin borrar lo escrito.

  **Un defecto encontrado midiendo, no revisando.** Chromium **vacía** un `<input type=\"search\"` entero al apretar Escape. Con la lista abierta, Escape significa «cerrá esto» y no «tirá lo que escribí», así que se hace `preventDefault` mientras hay lista y el segundo Escape se lo deja al navegador. Lo encontró la prueba, no la lectura del código.

  **Otro defecto encontrado midiendo: el vocabulario le mandaba al navegador campos que nadie usa.** Un `...zone` copiaba `kind` y `category` del catálogo —«elemento», «urbanizacion»— en CADA zona serializada. Se vio leyendo el marcado servido de `/alquiler/distrito-capital` con la aplicación compilada. El tipo no alcanzaba: declara cuatro campos y en ejecución acepta de más. Hoy se escribe campo por campo, con una prueba que cuenta las claves.

  **El costo, ruta por ruta y medido dos veces.** `pnpm budget:bundle`, antes → después: `/alquiler/[ciudad]` y `/alquiler/[ciudad]/[zona]` **106.46 → 108.97 KB** gzip; la ficha 106.73 → 109.27; `/` 105.69 → 108.11; `/mis-avisos` 106.65 → 109.16; `/importar` 107.99 → 110.53; `/reportar` 105.24 → 107.67; `/mis-avisos/[id]/editar` 107.58 → 110.11. Las cinco rutas de publicar y el visor de fotos **no se mueven**: no dibujan el `Nav`. O sea **+2,5 KB gzip en ocho rutas**, y siete de ellas no lo usan todavía — la isla entra en el primer paquete de toda ruta que importa la pastilla. La peor ruta del camino de lectura pasa a ser `/importar` con **110.53 KB contra un tope de 130**. **`next/dynamic` está medido y descartado**: partir la isla para que sólo la pidan las pantallas con vocabulario **sube** el número en las trece rutas (+0,5 KB más cada una), porque el cargador perezoso pesa más que lo que evita y se cuela en el trozo compartido.

  **Y el vocabulario en sí, que `budget:bundle` NO cuenta porque no es JavaScript sino datos del marcado.** Medido sobre la aplicación compilada contra el Postgres del arnés, en `/alquiler/distrito-capital` con dos zonas con avisos: **372 bytes crudos / 97 bytes gzip**. Proyectado con UUID reales sobre esa misma página —sustituyendo el segmento y midiendo el gzip en su contexto—: 10 zonas 1.916/841, 20 zonas 3.629/1.497, **30 zonas 5.380/2.121**, 60 zonas 10.605/3.863, 120 zonas 21.083/7.762. O sea **~65 bytes gzip por zona con avisos**, y **crece con el catálogo igual que el panel de filtros que ya está en la página** (aquél ya dibuja un enlace por zona con avisos). **La palanca escrita para cuando haga falta, con su disparador**: cerca de la mitad de esos bytes son los dos UUID de cada entrada (`id` y `cityId`), que el cliente sólo usa como clave de un `Map`; si alguna medición pone el marcado de la pantalla de resultados como un problema, la respuesta es numerar el vocabulario acotado con su propia clave —la ruta canónica de la zona, que ya es única entre ciudades— en vez de mandar los ids de la base. Hasta que eso pase, la respuesta es la de hoy.

  **Lo que se pierde y se dice, que era la decisión de producto.** El vocabulario acotado **no lleva los 4.710 alias** del «Índice de topónimos»: traerlos costaría una consulta más en la ruta más transitada del producto. Con el script cargado se sugiere entonces MENOS que lo que el servidor encuentra al enviar —escribir «Bella Vista» no autocompleta, pero enviar sí llega—, a cambio de que ninguna sugerencia lleve a una pantalla vacía. Y el conjunto ofrecido es el de las zonas con avisos **bajo los filtros que ya están puestos**, porque `byZone` se cuenta así; eso sólo puede ofrecer de menos, nunca un vacío.

  **`queries === 1` sobrevive**: esta tarea no agrega una consulta ni un `fetch`. `tests/integration/faceted-search.test.ts` sigue en verde (347 pruebas de integración, todas pasadas).

  **Nueve mutaciones, cada una con el texto objetivo presente exactamente una vez, sha256 antes ≠ después, restauración por copia de bytes con sha256 idéntica al original y `git diff --stat` limpio; cada una corrida contra la suite entera DOS veces —con las pruebas nuevas en el árbol y con ellas aparcadas fuera—**: la zona vacía vuelve a ofrecerse (2 / 0), la sugerencia pierde su conteo (2 / 0), el «0» vuelve a escribirse al lado (1 / 0), vuelve el `...zone` con `kind` y `category` (1 / 0), la página recorta a mano en vez de pedírselo al dominio (1 / 0), la pastilla deja de dibujar la isla (1 / 0), el oyente escucha otro evento (4 / 0, suite de medición), Escape deja de proteger lo escrito (1 / 0, suite de medición) y **el piso: la lupa deja de enviar el formulario (4 / 2, suite e2e)**. Las dos de la columna aparcada en la última son `busqueda-inicio`, que ya cubría el piso **en `/`**; lo que agrega este trabajo es el mismo piso **en la pantalla de resultados**, que nadie medía

  ---

  Se ofrecen **sólo las zonas con avisos activos, con su conteo**, y `suggestFilters` sigue siendo **la misma función de las dos partes** — nunca una segunda copia en el cliente. En las dos rutas de búsqueda ese conjunto **ya está en la página**: `buildFilterPanel` filtra `request.zones` por `counts.byZone` y `model.zones` lleva el `count`, así que ahí no cuesta un byte de datos nuevos. **Requiere una decisión del fundador antes de construirse**, y es la de arriba: con el script cargado se sugiere menos que lo que el servidor encuentra al enviar, a cambio de no ofrecer nunca una zona vacía. El piso no se toca: la pastilla sigue siendo `<form method="get">` y `resolveSearchDestination` sigue resolviendo en el servidor. Medir el costo ruta por ruta antes de darlo por bueno; el margen es de unos 21 KB
- [x] 14.52 **CERRADA 2026-09-02, y la cadena que temía no hizo falta: la 14.51 ya está en `dev`, así que esto salió como PR independiente.** La 14.51 se entregó sin ella y la razón es de forma, no de ganas: el inicio necesita un puerto de lectura nuevo con su adaptador y su prueba de integración contra Postgres real, y eso **depende** del componente que la 14.51 acaba de construir — o sea que no puede ser un PR independiente contra `dev`, sólo el segundo eslabón de una cadena. **Presupuesto**: `ActiveZonesPort.listActiveZones()` en `listing-discovery` (unas 25 líneas con su razón escrita), su `Drizzle…` con un `GROUP BY` sobre avisos activos unidos a su zona y su parroquia (~45), la prueba de integración que gane contra Postgres el aislamiento de ciudad que `listing-search.test.ts` ya prueba para la búsqueda (~60), el cableado del inicio (~10) y su prueba de contrato (~15): **~155 líneas** más el costo de marcado, que en `/` es el de las zonas con avisos **de las dos ciudades** y no de una, o sea el doble de la proyección medida en la 14.51 (~65 bytes gzip por zona). **Lo que cuesta no hacerla, dicho**: la sugerencia al escribir vive sólo en las dos rutas de búsqueda; en `/` la pastilla sigue siendo exactamente lo que era —un `<form method="get">` que el servidor traduce y redirige—, así que no hay una regresión, hay una mejora que todavía no llega a la portada. **El inicio ya paga los +2,5 KB gzip de la isla** (105.69 → 108.11), porque el `Nav` la importa: cuando la 14.52 se construya, el JavaScript ya está ahí y lo único nuevo son los datos. **El inicio no tiene de dónde sacar ese vocabulario acotado, y por eso la 14.51 no lo cubre.** En `/` no hay ciudad elegida y no hay conteo por zona: el inicio dibuja cuatro tiras de avisos y nada más. Hace falta **un puerto de lectura al lado del que ya existe** (AGENTS.md §3: se agrega un puerto de lectura, no se ensancha el de escritura) que devuelva las zonas con avisos activos de las dos ciudades con su conteo — una consulta, cacheable en la respuesta, no una por tecla. Sin esto la sugerencia al escribir vive sólo en las rutas de búsqueda, que es donde hoy están los datos

  **Lo que se construyó.** `ActiveZonesPort.listActiveZones()` en `listing-discovery` —sin argumentos, y eso es la decisión: ni una consulta por tecla ni una por ciudad, una sola respuesta cacheable con la página—, su `DrizzleActiveZones` con un `GROUP BY` sobre avisos vivos unido a su zona y al auto-join de su parroquia, `boundedVocabularyOf` en el dominio del catálogo, y el cableado del inicio. **Ni una regla en el frente**: la página no tiene un `.filter()` ni compone el conteo, y `app/inicio-contract.test.ts` lo ata con `expect(CODE).not.toContain("Object.fromEntries")`.

  **La misma función de las dos entradas, que era lo que podía separarse.** La pantalla de resultados llega con la taxonomía y un `Record` de facetas; el inicio, con las zonas ya contadas por su puerto. Las dos pasan por `boundedVocabularyOf`, así que «qué zona entra, con qué campos viaja y qué se hace con un cero» se decide **una sola vez**. `boundedVocabulary` es hoy esa función con el conteo buscado en el `Record`, y por eso una mutación del cero pone rojas las pruebas de las dos.

  **El predicado es el de la BÚSQUEDA y no el de las tiras, y es una decisión con consecuencia medible.** `status = 'active'` y `expires_at > now()`, lo mismo que `DrizzleListingSearch` y `DrizzleFacetedSearch`; **no** la portada completa que `DrizzleHomeCollections` exige. El destino de una sugerencia es `/alquiler/<ciudad>/<zona>`, que cuenta con ese predicado: copiar el de las tiras habría ofrecido «7» sobre una pantalla que dice 9. Regla transversal 3.

  **Las vueltas de red, contadas con el log de sentencias de Postgres y no afirmadas.** Antes: **3 consultas en 3 tandas** (catálogo → colecciones → portadas). Ahora: **4 consultas en las mismas 3 tandas**, porque el catálogo y las zonas con avisos no dependen uno del otro y van en el mismo `Promise.all`. Verificado con `log_statement='all'` sobre un `GET /` de la aplicación compilada: las dos primeras salen por **backends distintos** (pid 2058 y 2059) con 3 ms de diferencia, o sea concurrentes de verdad. Una mutación que las parta en dos `await` pone roja exactamente la prueba de contrato que lo exige.

  **`queries === 1` de la búsqueda facetada sobrevive intacta**: esta tarea no toca `countFacets` ni agrega una consulta a las rutas de búsqueda. 352 pruebas de integración en verde.

  **El costo en el marcado servido, medido y no proyectado desde la 14.51.** Sobre la aplicación compilada contra el Postgres del arnés, en `/` con las dos ciudades y sus 4 zonas con avisos: **635 bytes crudos / 132 bytes gzip en contexto** (la página entera pasa de 4.990 a 5.122 gzip). La 14.51 medía 372/97 con **2** zonas en una sola ciudad, así que la duplicación que esta tarea presupuestaba se cumple y con el margen del lado bueno. **Proyectado sustituyendo en su mismo contexto, con UUID reales y nombres tomados del corpus de `docs/territorio/`** (5.705 topónimos, muestreados espaciados para que no se parezcan entre sí y no inflen la compresión): 10 zonas 1.936/667, 20 3.730/1.127, 30 5.517/1.585, 60 10.772/3.002, 120 22.034/6.023 — o sea **~50 bytes gzip por zona con avisos** de 30 zonas en adelante, **no ~65**. La cifra de la 14.51 estaba proyectada sobre otra página y sale más barata acá.

  **La palanca de los UUID: medida, y NO se tira.** La 14.51 la dejó escrita con su disparador —«si alguna medición pone el marcado como un problema»— estimando que «cerca de la mitad» de los bytes son los dos UUID de cada entrada. Medido: **39–43 %**, no la mitad. Y lo que ahorraría hoy es **~50 bytes gzip**, porque el catálogo tiene 4 zonas con avisos; recién a 30 zonas ahorra 605 B y a 60, 1.295 B. Tirarla cuesta cambiar qué significa `zones[].id` en `SuggestionVocabulary`, que es el mismo tipo que `DrizzleSearchVocabulary` llena del lado del servidor y por el que `suggestFilters` une los alias: dos adaptadores y un dominio movidos para ganar 50 bytes. **El disparador sigue sin dispararse, ahora con el número de las dos pantallas y no de una.**

  **El bundle no se mueve, y era la predicción de la 14.51.** `pnpm budget:bundle` antes → después: **`/` 108.11 → 108.11 KB gzip**, y las trece rutas idénticas al byte. El JavaScript ya estaba pago desde la 14.51 —el `Nav` importa la isla— y lo único nuevo son datos del marcado, que ese gate no cuenta. La peor ruta del camino de lectura sigue siendo `/importar` con 110.53 contra un tope de 130.

  **El piso, afirmado en positivo y no por ausencia.** `tests/e2e/busqueda-inicio.spec.ts` corre en los dos proyectos: con el script apagado en `/` **no existe** la lista de sugerencias y la pastilla sigue siendo un `<form method="get">` que la lupa envía y que llega a `/alquiler/<ciudad>/chacao`. Y con el script cargado —una prueba saltada a propósito en `crawlability`, como las de `renewal-link`— escribir «tierra» ofrece **Tierra Negra con 2**, no con 3: la siembra pone tres avisos en esa zona y uno está vencido. El número es la aserción entera.

  **Nueve mutaciones, cada una con el texto objetivo presente exactamente una vez, sha256 antes ≠ después, restauración por copia de bytes con sha256 idéntica al original y `git status` limpio; cada una corrida DOS veces —con las pruebas nuevas en el árbol y con ellas aparcadas fuera—**: la zona en cero vuelve a ofrecerse (3 / 2), la parroquia deja de viajar (2 / 1), se cae el reloj (2 / 0), se cae el estado (2 / 0), la parroquia pasa a `innerJoin` y la zona sin padre desaparece (3 / 0), se cae `mapWith(Number)` y el conteo llega como el string del `bigint` (2 / 0), el inicio deja de pasarle el vocabulario a la pastilla (1 / 0 unidad, **1 / 0 e2e**), las dos lecturas dejan de ir en la misma espera (1 / 0) y el adaptador deja de filtrar del todo (2 / 0 integración, **1 / 0 e2e**).

  **Y una medición que salió en cero y se dice igual, porque enseña dónde no llega la siembra**: quitar **una sola** de las dos condiciones de frescura no pone roja ni una prueba e2e (0 / 0 las dos). El aviso vencido de `seed-e2e.ts` está vencido por estado **y** por reloj, así que cualquiera de las dos lo sigue dejando afuera — una mutación contra una entrada que la fixture no produce no mide nada. Lo que sí las separa es la fixture de integración, que trae un aviso `active` con la fecha pasada: ahí las dos caen por separado.

  **HALLAZGO — `src/vigencia-del-catalogo.test.ts` se puso roja sola, que es exactamente para lo que existe.** `drizzle-active-zones.ts` es el **sexto** lector del predicado del reloj, y la 21.7 dejó escrito que la lista es la decisión del fundador y que un lector más la reabre. No se unificó —el ayudante compartido cruzaría el límite entre capacidades, que es el criterio con el que se descartó la unificación el 2026-09-01— y el renglón se agregó con su razón adentro. **La decisión de si seis ya son demasiadas es del fundador y este trabajo no la toma: la reporta.**

- [x] 14.53 **CERRADA 2026-09-02 con las tres decisiones del fundador tomadas: las dos suyas construidas acá, y la tercera —el encabezado se queda— cerrando la 14.29 con el criterio revisado a 2/4.** (nueva — abierta por la 14.29, con el hueco medido y sin decisión tomada.) El criterio de aceptación 1 no da: entran **2 de 4** en el teléfono y **4 de 8** en el escritorio, y el presupuesto está contado al píxel. **No se toca nada hasta que el fundador decida qué se va**, porque las tres salidas posibles contradicen decisiones ya tomadas y ninguna es de quien mide.

  **Teléfono — faltan 243 px, y la lámina 6c dice de dónde.** Entre la barra y la cuadrícula la 6c **no dibuja nada**, y la pantalla servida dibuja cuatro bloques: miga de pan (52), título (25), conteo (17) y fichas quitables (**154**, porque a 360 se pliegan a cuatro líneas). Cada uno tiene dueño: la miga de pan la puso la 14.41 como reemplazo de la flecha de vuelta de la `SearchSummaryBar` —*«la cubre la miga de pan, que ya estaba y se dibuja siempre»*—, así que borrarla reabre esa salida; y las fichas quitables son de la lámina **7c**, que es la de **escritorio**. **La 6c no las dibuja: pone el número de filtros en la pastilla.** Si eso es un desvío de la 14.33 o una decisión, hay que decirlo.

  **Escritorio — faltan 87 px, y ~50 de ellos son la insignia.** La tarjeta mide 321 px contra los ~285 de la lámina. Las láminas 6c y 7c dibujan «Dueño» **encima de la foto** (`position:absolute; left:8px; top:8px`), y `ListingCard` la dibuja **en el flujo del cuerpo, arriba del precio**: ~25 px por tarjeta, dos filas, ~50 px. Mover `PublisherBadge` a la foto es lo que las láminas dibujan, pero toca una garantía probada —la 14.25 exige que el publicador siga siendo distinguible **en escala de grises**, `components/design-contract.test.tsx`— así que no es un movimiento de CSS suelto. Los ~33 restantes están en el encabezado: la 7c pone **una** línea («9 avisos en Chacao y Altamira»), la pantalla pone miga de pan, `<h1>` y conteo en tres.

  **Cuando se decida, la medición ya está puesta y va a moverse sola**: `tests/measure/lista.spec.ts` fija 2 y 4 con igualdad exacta, así que el primer cambio que mueva el pliegue la pone roja diciendo cuánto se movió — y ahí se actualiza a 4 y 8

  **Decisión 1, del fundador y mirando la pantalla real: «sí quítalos, ocupan mucho espacio».** Las fichas quitables se van del teléfono y **se quedan en escritorio**, donde entran en un renglón y no cuestan nada. `FilterChips.module.css` las apaga en el ancho base y las enciende en `@media (min-width: 768px)` — el mismo quiebre en el que la cuadrícula pasa a cuatro columnas, porque la 7c es la lámina de los dos. Pruebas: `FilterChips.test.tsx` («no se dibujan en el ancho base» / «vuelven en escritorio») y, dibujado, `tests/measure/lista.spec.ts` a los dos anchos.

  **Se ESCONDEN con CSS y no se dejan de dibujar, y no es comodidad: el servidor no sabe el ancho de la pantalla.** No hay ancho en una petición HTTP. Decidirlo allá pediría husmear el `User-Agent` —poco fiable, y además rompe una sola respuesta cacheable para todos los anchos— o decidirlo en el cliente, que contradice el piso de AGENTS.md §2. `display: none` saca además al enlace del orden de tabulación y del árbol de accesibilidad, así que no queda una parada invisible al tabular (medido: `enlaces de ficha alcanzables=0`); lo que sí se sigue pagando son sus bytes, y ése es el precio elegido. **Este argumento es el que la 14.43 necesita** para su propia mudanza por ancho, que hoy está escrita como una duda entre dos formas.

  **Lo que el visitante pierde en el teléfono, dicho y no rodeado**: ya no ve de un vistazo cuáles filtros están puestos ni puede sacar uno desde esa pantalla — abre el panel. Lo único que le queda diciendo que la búsqueda está filtrada es **el número dentro de la pastilla**, que la 14.31 ya había cableado y que acá pasa a ser carga: la medición lo comprueba a 360 («la pastilla dice 3 filtros») y una mutación que apague `.filterCount` pone roja exactamente esa prueba.

  **Decisión 2, del fundador: «súbela a la foto porque es lo que dice el último diseño que hicimos».** `PublisherBadge` deja el flujo del cuerpo y se dibuja encima de la portada, que es lo que dibujan 6c y 7c. **La placa no se repinta** —la garantía de la 14.25 es del átomo y sigue siendo suya—: lo que se agrega es **un piso opaco de `--surface` del tamaño exacto de la placa** debajo de ella. Sin ese piso la garantía se cae sola encima de una foto, porque la portada la sube quien publica y un borde sin relleno sobre una foto oscura desaparece.

  **Probado en escala de grises sobre una foto clara y una oscura de verdad, y cada eslabón medido y no afirmado** (`tests/measure/layout.spec.ts`, arnés `placa-sobre-foto`): la portada clara da L=0,902 y la oscura L=0,006 **leídas de un `<canvas>`**; el piso mide alfa **1** en las cuatro combinaciones; la placa cae entera dentro del piso y el piso dentro de la portada; y con ese fondo la separación de luminancia entre dueño e inmobiliaria es **0,986** (la 1b.7 pide > 0,3) con contrastes de **16,34** y **6,40** y el borde de la inmobiliaria a **3,96** — **idénticos con las dos fotos**, que es la consecuencia de que el piso sea opaco escrita como comparación.

  **Lo que se movió, medido** (mismo arnés, Chromium real):

  | | antes | ahora | de dónde |
  |---|---|---|---|
  | arranque de la cuadrícula a 360 | 373 px | **219 px** | −154 de fichas |
  | tarjeta a 360 | 249 px | **222 px** | −27 de la placa |
  | tarjeta a 1280 | 321 px | **294 px** | −27 de la placa |
  | 2ª fila a 360×640 | 883 px | **675 px** | pliegue 640 |
  | 2ª fila a 1280×800 | 887 px | **833 px** | pliegue 800 |

  **Con las dos decisiones puestas siguen entrando 2 y 4**, y faltaban **35 px** en el teléfono y **33** en el escritorio: el encabezado de tres líneas —miga de pan, `<h1>` y conteo— contra la única que dibujan 6c («9 avisos en Chacao») y 7c. Es exactamente el resto que esta misma tarea había contado («los ~33 restantes están en el encabezado»).

  **Y hubo una TERCERA decisión ese mismo día, que es la que cierra la 14.29 y por eso esta tarea no queda con un pendiente colgando.** Preguntado por esos 35 px, el fundador preguntó primero *«pero los avisos pueden ser con scroll, no?»* —sí: nada está roto y la lista se baja— y con eso decidió que **el encabezado se queda entero**: la miga de pan es la salida que la 14.41 dejó puesta al borrarse la `SearchSummaryBar`, y **volver, en un teléfono, vale más que un aviso y medio**. El criterio de aceptación 1 se revisó de 4/8 a **2/4** y la 14.29 cerró con esa decisión escrita, su fecha y lo que cuesta. **No queda nada abierto de esta tarea: no hay 35 px que buscar.**
- [x] 14.37 **RESUELTO por el fundador, 2026-08-25: se admiten los SVG en línea y se actualiza el sistema.** `SISTEMA.md` decía "no hay iconos"; las láminas nuevas traían dos SVG en la pastilla. Gana la lámina, y el sistema lo dice ahora con sus condiciones: **conjunto CERRADO de dos** —las tres rayas del filtro y la lupa—, **en línea y nunca un paquete de iconos**, con `aria-hidden` y su etiqueta accesible al lado, y `stroke="currentColor"` para que hereden el color como lo haría un carácter.

  Los glifos de texto siguen siendo el default para todo lo demás. La razón de la excepción está escrita: no hay carácter que signifique "filtro" sin ambigüedad, y `◎` se lee como un ojo y no como una lupa. **Un tercer icono no se agrega: se discute** — la tabla de `SISTEMA.md` es la lista completa, y ampliarla es cambiar el sistema, no usarlo. Registrado también en `AGENTS.md` §2
- [x] 14.38 **RESUELTO por el fundador, 2026-08-25: vale la propuesta del diseño — Publicar depende de la sesión.** Sin sesión queda **afuera y en acento**, que es cuando hay que provocar. Con sesión se muda a la **primera fila del menú de cuenta**, con relleno de acento, como "Publicar una propiedad". Y la marca cede su lugar al `←` en la ficha, *"porque en una ficha volver vale más que ir al inicio"*.

  Esto ajusta —no contradice— la regla que el fundador fijó al armar el nav. Esa regla existe para provocar al que todavía no publicó, y ése es exactamente el usuario **sin sesión**, donde el botón sigue afuera. Con sesión el botón ya hizo su trabajo. **Se descarta la alternativa** de mantenerlo afuera siempre: costaba dos filas de cromo —120 px antes de la primera foto en vez de 60— en la pantalla de móvil donde la foto es lo que vende
- [x] 14.36 **RESUELTO por el fundador, 2026-08-25: la lámina de móvil tenía el bug. El panel de filtros NO lleva ciudad ni zona.** Las dos láminas se contradecían — el escritorio decía *"este panel ya no repite ciudad ni zona: eso lo resuelve el buscador de la pastilla, y tenerlo en los dos lugares era el problema"*, y el móvil describía el acordeón con "los cuatro estados completos: ciudad, zona, precio y habitaciones". El móvil es anterior a la pastilla y no se actualizó. **Vale el escritorio.**

  **Consecuencia, y es más grande que un tilde:** `search-panel.ts` produce hoy `step: "zona"` y `zoneSearch`, y `search-accordion.ts` los secuencia. Sacar ciudad y zona del panel deja el acordeón de móvil con lo que queda —precio, tamaño, quién publica, atributos— y quita al escritorio la razón de ser secuencial. **La ubicación pasa a vivir SOLO en la ruta** (`/alquiler/<ciudad>/<zona>`), resuelta por el texto de la pastilla vía `resolveSearchDestination`; **los filtros viven SOLO en la query**. Un dato, un lugar.

  Ojo al mover esto: `buildSearchHref`, `SEARCH_QUERY_NAMES` y `search-exits.ts` razonan sobre la zona hoy — la salida de cero resultados ofrece "sumar la zona vecina" (14.14), que es una sugerencia de UBICACIÓN emitida desde el lado de los filtros. Decidir si esa salida ahora navega en vez de filtrar


- [x] 14.44 **Borrar lo que la 14.32 y la 14.33 dejaron huérfano — PARCIAL: dos de las tres piezas.** Tres piezas, y ninguna la dibuja ya nadie: `components/layout/SidebarLayout` (con su hoja y su archivo de pruebas), los pasos de ciudad y zona del panel —`model.cities`, `model.zones`, `model.zoneSearch` se siguen **armando** en `buildSearchPanel` y no los consume ningún componente— y lo que cuelga de ellos: `resolveZoneOptions`, `narrowZoneOptions`, `zoneIdsFromSuggestions` y `toCityChoice`. **Ojo antes de tirar del hilo**: `zoneHref` NO es huérfano —lo usa la salida `add-zone` de `search-exits.ts`— y `relaxableFilters`/`reliefHref` tampoco, que ahora también alimentan las fichas quitables. **No va en el mismo PR a propósito**, por la misma razón que la 14.42: es borrado, y el cableado que sí hay que leer con cuidado ya empujó este PR muy por encima del techo de 400. Un gate en verde que no protege nada que se sirva es exactamente lo que la 14.42 nombra

  **Hecho lo que era huérfano de verdad.** `components/layout/SidebarLayout.{tsx,module.css,test.tsx}` (66 líneas) borrado entero — mismo mecanismo de prueba que la 14.42: sin barril en `components/`, único import el de su propia prueba buscado por ruta, sin import dinámico, y `filtros-contract.test.ts` sigue afirmando `not.toContain("SidebarLayout")` sobre el código de las dos páginas, que es un guardia que sigue significando algo. Y `model.zoneSearch` con todo lo suyo: `ZoneSearchForm`, `zoneIdsFromSuggestions`, `narrowZoneOptions`, `ZoneSuggestion` y `SearchPanelInput.zoneSuggestions`. Cero consumidores de los cuatro, y `zoneSuggestions` **ningún llamador se lo pasaba nunca** —ni `build-filter-panel.ts` ni `app/measure/page.tsx`—, así que `zoneIdsFromSuggestions` corría siempre contra la lista vacía. `SEARCH_QUERY_NAMES.zoneSearch` («busca») **no se toca**: sigue en `FILTER_KEYS` y lo afirma `indexing-contract.test.ts`, que es la regla de no-indexar.

  **CORRECCIÓN, verificada antes de borrar (AGENTS.md §5): `model.cities`, `model.zones`, `resolveZoneOptions` y `toCityChoice` NO son huérfanos, y el código queda en pie.** La tarea los da por huérfanos porque ningún componente los dibuja, y eso es cierto y no alcanza. Son la **única superficie por la que se observa una conducta de aplicación que se sigue ejecutando en cada carga**: `buildFilterPanel` dispara una consulta por CADA otra ciudad (`Promise.all` sobre `request.cities`) para llenar `PanelCity.count`, que es F3, y filtra las zonas ofrecidas contra el conteo (`request.zones.filter(zone => zone.id in counts.byZone)`). Nueve aserciones de integración contra Postgres real las miden por ahí —«cada ciudad lleva SU número», «el conteo de la otra ciudad se calcula SIN las zonas de ésta», «se ofrecen las zonas que el conteo nombra, no la taxonomía entera»— más tres unitarias en `build-filter-panel.test.ts`. Borrar los campos deja dos salidas y las dos están prohibidas: reescribir esas aserciones, o borrar además el abanico de consultas, que es **un cambio de conducta** y no un borrado. Que nadie las dibuje no las hace muertas: las hace un costo que se paga y no se muestra.

  **La pregunta que esto abre y que no es de esta tarea:** ¿se sigue pagando N-1 viajes a Neon por búsqueda para un conteo por ciudad que ninguna pantalla dibuja desde la 14.36? Si la respuesta es no, se borra el abanico, el campo y sus pruebas **juntos** y en una tarea que se llame por su nombre. Queda como **14.50**.

  **Confirmado el aviso de la tarea, no dado por bueno:** `zoneHref` lo usa la salida `add-zone` de `search-exits.ts` **y además** `filterChips` en `search-panel.ts`; `relaxableFilters` y `reliefHref` los usan `search-exits.ts` y las fichas quitables. Los tres siguen en pie.

  **Evidencia.** La misma corrida de la 14.42: 141 archivos / 1654 pruebas, `typecheck`, `lint`, `lint:tokens`, `e2e`, `measure`, `build` y `budget:bundle` en verde, sin una aserción reescrita y con el mismo conjunto exacto de líneas sin cubrir.
- [x] 14.45 **CERRADA 2026-09-04 con la rebanada B, y con ella cierra la fase 14 entera. DECIDIDA POR EL FUNDADOR EL 2026-09-03. Baños y metros², que la lámina 7b dibuja y el modelo no tiene.** El panel de escritorio dibuja **Baños · 1 · 2 · 3+** entre Habitaciones y Quién publica, y el fundador nombró el grupo «tamaño». No existe ninguna de las dos cosas del lado de los datos: `SearchCriteria` tiene `minAreaM2` sin un solo control ni conteo, `FacetedSearchPort` no cuenta baños, y `listing.bathrooms` sí está en el esquema (el paso 4 de publicar lo pide). O sea: **el conteo es el trabajo, no el control** — un filtro sin su número al lado rompe la regla transversal 3, y por eso la 14.32 se negó a inventarlo. La lámina 7b también dibuja **«Puesto de estacionamiento»** entre los atributos, que es un sexto atributo que `LISTING_ATTRIBUTES` no tiene (son cinco): decidir si entra o si la lámina se corrige

  **LAS DOS DECISIONES DEL FUNDADOR, 2026-09-03.** (1) **El estacionamiento SÍ entra como sexta opción del filtro.** (2) **El filtro de baños se construye.** Los metros² quedan dentro del mismo grupo «tamaño» que el fundador ya había nombrado.

  **PERO el estacionamiento NO se guarda como un sexto booleano, y la razón es «un dato, un lugar».** Verificado antes de anotar la decisión: `listing.parking_spots` **ya existe** (`src/shared/db/schema.ts:473`, `integer NOT NULL DEFAULT 0`) y el paso 4 de publicar ya lo escribe. Los cinco de `LISTING_ATTRIBUTES` son booleanos del aviso (`hasPowerPlant`, `hasRegularWater`, `isFurnished`, `hasSecurity`, `hasAppliances`); agregar `hasParking` al lado guardaría **el mismo hecho dos veces**, y dos copias del mismo hecho se contradicen: `parking_spots = 0` con `hasParking = true` es una fila que el esquema permitiría y nadie podría arbitrar. Es exactamente el argumento con el que la 14.36 mató el paso de ciudad y la 14.50 el abanico de conteos.

  **Entonces: sexta opción DERIVADA de `parking_spots > 0`.** Lo que ve una persona es idéntico a lo que pidió el fundador —«Puesto de estacionamiento» con su ✓ en el filtro y en la lista «La propiedad tiene» de la ficha— y **no puede desincronizarse del número**, porque es el mismo dato leído de dos maneras. La tira numérica de la lámina («1 Puesto») y la lista de atributos salen de la misma columna. Lo que cambia respecto de los otros cinco es sólo la consulta del conteo en `DrizzleFacetedSearch`: `count(*) FILTER (WHERE parking_spots > 0)` en vez de un booleano.

  **El trabajo de verdad sigue siendo el conteo, no el control.** `grep -rn bathroom` da **cero** aciertos fuera del esquema: `listing.bathrooms` existe porque el paso 4 lo escribe, y **nada lo cuenta**. Dibujar «Baños · 1 · 2 · 3+» es media hora; que cada botón sepa cuántos avisos tiene detrás es la tarea, y sin ese número el filtro rompe la regla transversal 3 («todo conteo es real»), que es por lo que la 14.32 se negó a inventarlo.

  **Lo que hay que mirar antes de escribir** (no decidido, es de quien construya): `SearchCriteria` ya tiene `minAreaM2` **sin un solo control ni conteo** —o sea que la mitad de los metros² está escrita y muerta desde hace tiempo—, y `LISTING_ATTRIBUTES` es hoy una lista de claves booleanas del aviso; meterle una derivada obliga a decidir si la lista pasa a ser de opciones de filtro (y no de columnas) o si la derivada vive al lado. **Esa elección se escribe en el código con su razón**, no se resuelve en silencio.

  **REBANADA A HECHA (2026-09-03): el filtro de baños con su conteo real.** La 14.45 **sigue abierta**: falta la B (metros²) y la C (el estacionamiento derivado). Se partió en tres porque la nota de la 14.46 ya había medido que entero no entra bajo el techo de 400 líneas.

  **REBANADA B HECHA (2026-09-04): los metros² como CAMPO LIBRE. Con ella la 14.45 se cierra, y la fase 14 se queda sin una sola tarea abierta.** Lo construido, con el archivo y la prueba que lo sostienen:

  - **No es un eje nuevo: es el cableado de `SearchCriteria.minAreaM2`**, que existía desde hace tiempo, se parseaba y se aplicaba en las dos consultas **sin un solo control ni nombre en la dirección**. La decisión del fundador —un número escrito, no escalones— resultó ser exactamente la forma que ese criterio siempre esperó.
  - **`metros` en `SEARCH_QUERY_NAMES` y en `FILTER_KEYS`**, sin «²» por la misma razón que `banos` va sin «ñ»: `m%C2%B2` deja de leerse en el chat donde se pega (F12). Las dos guardas de clase se pusieron rojas solas apenas el nombre existió — `indexing-contract.test.ts` por `FILTER_KEYS` y `filtros-contract.test.ts` por las dos páginas, que es la prueba que la rebanada C dejó escrita **para este caso exacto**.
  - **La validación, que es lo que un campo libre trae y los escalones no tenían** (`readMinAreaM2` en `search-criteria.ts`, cuatro casos en `search-criteria.test.ts`). `readCount` **no alcanzaba**, y por dos motivos distintos: (1) acepta el **cero**, que acá es un filtro presente que no filtra —`area_m2` es positivo en todo aviso publicable— y que aun así dibujaría su ficha quitable y sumaría uno a la pastilla; (2) acepta **`1e21`**, que pasa `Number.isInteger` y **no entra en la columna `integer`**, así que `?metros=1e21` sería un 500 y no una pantalla. El techo es el de la columna (`2147483647`) y no un límite de producto inventado. Lo que no es entero positivo —incluidos los `72,5` del fundador— se cae **campo por campo**, que es la regla que este traductor ya aplicaba: la columna es `integer` y un mínimo fraccionario no se puede devolver escrito en el campo, así que se suelta en vez de redondear en silencio. Un mínimo enorme que sí cabe se acepta: es una búsqueda vacía legítima y el vacío ya tiene su salida contada, igual que el precio.
  - **NO se construyó faceta `byMinAreaM2`, y ésa es la decisión.** Con un campo libre no hay opciones que contar. `FacetCounts` y `PanelCounts` **no ganan un campo requerido**, que era justo lo que había vuelto imposible partir la rebanada A: los doce fixtures no se tocaron. La regla transversal 3 se cumple del otro lado — el número real es el total, que el botón ya dice.
  - **Sí gana `RelaxableFilter`** (`area`, entre los baños y el precio), con su ficha quitable, su parte del resumen del renglón cerrado —«2 hab · 2 baños · Desde 90 m²»— y su salida del vacío con número. Eso costó una clave más en los diecinueve `withoutFilter` del repositorio, y el `Record` completo las nombró todas al compilar.
  - **El área sale del `WHERE` compartido de `DrizzleFacetedSearch` y pasa a ser un eje**, exactamente el movimiento que el precio hizo con F10/F11: desde el `WHERE` un filtro no puede contar cuántos habría sin él. Una columna `count(*) filter` más en la MISMA sentencia; `queries === 1` sigue afirmado y en verde sin tocarlo.
  - **Un defecto preexistente que el movimiento destapó y arregla: `cityTotal` se quedaba con los metros² puestos.** Es el número de «Limpiar todo», o sea "la ciudad sin un solo filtro del panel", y con el área en `shared` prometía la ciudad **ya recortada**. Vivía escondido porque el criterio era inalcanzable desde una pantalla; en cuanto el campo existe, se dibuja mal en cada búsqueda que lo use. Medido en `tests/integration/faceted-search.test.ts` («Limpiar todo» no se queda con los metros² puestos).
  - **El control es un `<form method="get">` con su botón**, como el precio y por la misma razón: un campo suelto no envía nada con el script apagado (D13, F14). Va **dentro** del grupo «tamaño», debajo de las dos tiras de escalones, que es donde la lámina 7b pone el tamaño. Es **el único control del panel sin conteo al lado**, y eso está escrito con su razón.
  - **Un falso verde evitado en la siembra de e2e**: los seis avisos medían 80 m², así que `>= 70`, `> 45` y `count(*)` daban el mismo número y el filtro habría pasado en verde sin recortar nada. El de Altamira quedó en **45 m²**, con la razón al lado — es la misma corrección que la rebanada C tuvo que hacerle a `parking_spots`.
  - **La prueba sin JavaScript envía con `Enter` y no tocando el botón**, y se dice por qué: con el grupo del tamaño ya lleno el botón queda a veces bajo el pie pegajoso del panel a media altura de scroll y Playwright lo reporta interceptado. El envío implícito es el mismo `<form method="get">`; que el botón se dibuje lo afirma la prueba de componente.

  **Seis mutaciones, cada una dos veces: 6/6 mueren con las pruebas nuevas y 5/6 sobreviven con ellas aparcadas.** La que cae igual (`gte` → `gt` en el filtro de área) la mata una guarda preexistente —«el total coincide con las filas de la búsqueda equivalente: por área»—, que ya medía el criterio aunque ninguna pantalla pudiera pedirlo. Cada una con su sha256 antes ≠ después y restaurada por copia de bytes con sha256 idéntica; la columna «sin» corrió acotada a los archivos afectados. Las otras cinco: el cero aceptado, el techo de la columna levantado, el eje del área nunca excluido de su propia relajación, `relaxableFilters` sin `area`, y el nombre corto propio viajando además como campo escondido.

  **Medida: 550 líneas en bruto** (531 agregadas, 19 borradas) en 26 archivos, plan afuera; **254 descontando comentarios y líneas en blanco**, que es la cuenta con la que la rebanada A dio 407 y la C 237. **Bajo el techo de 400 de AGENTS.md §6**, y sin el precedente acotado de la A.

  **Evidencia.** `typecheck`, `lint`, `lint:tokens`, `build` en verde; 221 archivos / 2688 pruebas unitarias; 37 archivos / 374 de integración (80 en `faceted-search`); `test:measure` 69 pasadas; `test:e2e` entero en los DOS proyectos — `chromium` 36 pasadas / 1 salteada, `crawlability` 35 pasadas / 2 salteadas. `budget:bundle` 109.11 → 109.13 KB gzip en las dos rutas de búsqueda (el peor camino de lectura, 110.59 → 110.60 KB, techo 130): el campo se dibuja en el servidor y no manda una línea al navegador.

  **REBANADA C HECHA (2026-09-03): el puesto de estacionamiento, derivado y con su conteo real.** La 14.45 **sigue abierta y le queda sólo la B**. Lo construido, con el archivo y la prueba que lo sostienen:

  - **La forma que la tarea dejaba abierta, resuelta y escrita: `LISTING_ATTRIBUTES` pasa a ser la lista de OPCIONES DEL FILTRO —seis— y las cinco columnas booleanas se quedan con su propio tipo, `DeclaredAttribute`** (`search-criteria.ts`). El argumento es mecánico y está en el código: todo lo que el filtro necesita saber de sus opciones vive como `Record<ListingAttribute, …>` —la etiqueta, el nombre corto de la dirección, el nombre y el orden de la relajación, el resumen del renglón cerrado y el conteo— y a un `Record` completo le falta una clave y **no compila**. Con la sexta adentro del tipo, `pnpm typecheck` nombró los cinco lugares que faltaban; **con la derivada viviendo al lado habrían sido cinco agregados a mano que nada comprueba**, el mismo hueco silencioso que la 14.48 documenta en `lint:tokens`. Lo único que se pierde al ensanchar la lista —que cada clave tenga columna booleana— se recupera entero en `DeclaredAttribute`, que es lo que el mapa `Record<DeclaredAttribute, PgColumn>` sigue exigiendo: `parking_spots` es `integer` y ahí adentro no entra.
  - **`src/modules/listing-search/infrastructure/listing-attribute-sql.ts`** (nuevo) — la derivación escrita UNA vez, `parking_spots > 0`, y los dos adaptadores la comparten. Antes cada uno tenía su `ATTRIBUTE_COLUMNS` copiado con el comentario «igual que en `DrizzleListingSearch`»; mientras las opciones fueran `columna = true` la copia era barata, pero **el umbral es una regla**, y con dos copias el botón puede prometer un número que la lista no entrega. `«el filtro del puesto recorta la lista igual que recorta el total»` mide las dos consultas contra las mismas filas.
  - **El conteo en la MISMA sentencia** (`drizzle-faceted-search.ts`): una columna `count(*) filter (where parking_spots > 0)` más adentro del `select` que ya trae total, facetas y las nueve relajaciones, con su propio filtro apagado; y `withoutParking` para la ficha quitable y la salida del vacío. **`queries === 1` sigue afirmado y sigue en verde sin tocarlo.**
  - **`puesto` sin «ñ» ni tilde** y en `FILTER_KEYS`, por la misma razón que `banos` (F12). `puesto` y no `estacionamiento` porque es la palabra que la tira de datos de la ficha ya usa: un nombre corto que hay que traducir mentalmente no es corto.
  - **El puesto va CUARTO en la lista**, entre amoblado y vigilancia, porque ahí lo pone «Rentas - UX movil.md» F6. Al final por ser el último construido habría sido inventar una pantalla (AGENTS.md §2).
  - **En la ficha, con su ✓ en «La propiedad tiene» y FUERA del «no se declaró»**, y la asimetría tiene una sola razón: el paso 4 de publicar pide el número siempre —«Puestos permite 0»—, así que acá el cero es un cero declarado y no un silencio. Nombrarlo entre lo no declarado diría que nadie contestó sobre una respuesta que sí existe, y la tira de arriba ya la escribe como «0 Puestos».
  - **Una prueba de clase, no de caso** (`filtros-contract.test.ts`): las dos páginas tienen que leer **cada** nombre corto que el dominio declare. Todos los campos de `RawSearchParams` son opcionales —tienen que serlo—, así que olvidarse de pasar uno compila, pasa el `typecheck` y deja el filtro inalcanzable desde una pantalla. **Es exactamente lo que le pasó a `minAreaM2` y esta tarea ya lo tenía anotado**; ahora el próximo no puede quedarse a mitad de camino en silencio.

  **Dos falsos verdes destapados al construirla, los dos de la clase que la rebanada A ya había pagado.** (1) El arnés de integración escribía `parking_spots` a mano en **1** para todas las filas: con los cinco avisos en uno, `parking_spots > 0`, `> -1` y `count(*)` dan el mismo número y la derivación quedaba **sin una sola prueba que la pudiera poner en rojo**. Se volvió campo del fixture y A1 y A3 quedaron en cero. (2) La primera prueba de la ficha afirmaba «Puesto de estacionamiento» sobre el HTML entero y pasaba en verde **sin que la lista lo dibujara**: la descripción del aviso falso dice «Puesto de estacionamiento techado». Se recortó a la sección `declared-features`.

  **Un mutante equivalente, anotado en vez de disimulado.** «La faceta del puesto se cuenta contra sí misma» **no se puede matar**, y no es un hueco de las pruebas: para un atributo su filtro y su faceta son la MISMA condición, así que excluirla no cambia el número. El comentario de `others()` en `drizzle-faceted-search.ts` ya lo decía antes de esta rebanada. La propiedad que sí se puede medir —que la faceta respete **los demás** filtros— es la que quedó como M3.

  **Nueve mutaciones, cada una dos veces: 9/9 mueren con las pruebas nuevas y 6/9 sobreviven con ellas aparcadas.** Las tres que caen igual son guardas preexistentes: la lista cerrada de nombres cortos (M6), el contrato de indexado (M7) y «la ficha no dibuja la sección cuando no se declaró ninguno» (M8). Cada una con su sha256 antes ≠ después y restaurada idéntica.

  **Medida: 555 líneas en bruto** (506 agregadas, 49 borradas) en 35 archivos, plan afuera; **237 descontando comentarios y líneas en blanco**, que es la cuenta con la que la rebanada A dio 407. **Bajo el techo de 400 de AGENTS.md §6**, y el precedente acotado de la rebanada A no hizo falta.

  **POR QUÉ LA B NO VA EN ESTE PR, con el número y no con una estimación.** Los metros² son el análogo exacto de los baños: un eje nuevo de escalones con su conteo. La rebanada A **midió** ese trabajo en **407 líneas revisables** con la misma cuenta, y la B arrastra lo mismo —`area-steps.ts` y su prueba, el resolvedor, el nombre corto, `FILTER_KEYS`, las dos páginas, N columnas `count(*) filter` en `drizzle-faceted-search.ts`, `byMinAreaM2` como campo **requerido** de `FacetCounts` y de `PanelCounts` (que es lo que volvió a la A imposible de partir), `RelaxableFilter`, la ficha quitable, la salida del vacío, el paso del panel y los ~20 fixtures que el `Record` completo obliga a tocar—. 237 + ~400 pasa el techo en un 50 %, así que se entrega sola.

  **LOS CORTES DE m², ELEGIDOS Y CON SU PROCEDENCIA — para la B, no construidos acá.** No hay lámina que los dibuje (la 7b dibuja el grupo «tamaño» y ningún número), así que salen de la distribución. **El catálogo de producción no es visible desde acá**, y se dice en vez de fingirlo: lo que hay son los **diez avisos de `src/shared/db/seed.ts`** —34, 38, 52, 68, 78, 84, 92, 96, 120, 145 m²— y los **veintidós valores que las láminas dibujan** en fichas y tarjetas —38, 42, 52, 55, 68, 70, 74, 76, 78, 80, 82, 84, 88, 94, 96, 99, 105, 115, 120, 128, 165, 240—. Diez filas escritas a mano no son un mercado; veintidós valores de diseño tampoco. Lo que sí se puede afirmar de los dos juntos es dónde están los huecos: nada entre 55 y 68, nada entre 42 y 52, y la masa entre 68 y 99.

  **DESCARTADO POR EL FUNDADOR EL 2026-09-04: los metros² NO son escalones, son un campo donde se escribe el número.** Su razón, textual: *«hay casas que tienen 72,5 o 84 y así no puede ser preseleccionado»*. Un escalón es una lista cerrada y la superficie es un continuo: cualquier corte deja a alguien afuera de su propia casa. Los escalones de abajo quedan escritos **como el análisis descartado y no como el plan**, porque su procedencia sigue siendo el único inventario de superficies que existe.

  **Lo que la decisión cambia, y no es sólo el control.** `SearchCriteria` ya tiene `minAreaM2?: number` desde hace tiempo, escrito y sin un solo control ni conteo — o sea que **la decisión del fundador es exactamente la forma que ese criterio siempre esperó**: un mínimo suelto, «desde X m²», no una lista. La rebanada B deja de ser el análogo de baños y pasa a ser el cableado de algo que ya estaba a medio hacer.

  **Y desaparece el conteo por opción, que es lo que hay que decir en voz alta.** La regla transversal 3 («todo conteo es real») se cumplía mostrando el número al lado de cada opción; con un campo libre **no hay opciones que contar**. No la rompe: el número real pasa a ser el total de resultados, que es el que la pantalla ya muestra. Pero significa que **la faceta `byMinAreaM2` NO se construye**, y con ella se va la razón por la que esta rebanada no entraba en el techo — sin campo requerido nuevo en `FacetCounts` ni en `PanelCounts`, ya no arrastra los doce fixtures.

  **RESUELTO POR EL FUNDADOR EL 2026-09-04: la columna NO se toca. `area_m2` se queda en `integer`.** Su respuesta, textual: *«el filtro alcanza, exageré con el decimal»*. O sea que la rebanada B es **sólo el control y su cableado**: ni migración, ni paso 4 de publicar, ni ficha, ni importación masiva, ni lector de CSV. Quien tenga 72,5 escribe 72 o 73 al publicar, y el filtro —que es un mínimo— lo encuentra igual desde cualquiera de los dos. Queda escrito el análisis de abajo para que nadie reabra la migración creyendo que no se miró.

  **El detalle que quedó sin resolver hasta esa respuesta: `listing.area_m2` es `integer NOT NULL` (`src/shared/db/schema.ts:461`).** Los 72,5 de su ejemplo **hoy no se pueden guardar**: la columna no admite decimales. Son dos preguntas distintas y conviene no confundirlas — el **filtro** es un mínimo y medio metro nunca cambia un resultado («desde 70» encuentra la de 72,5 con la columna que hay); el **dato del aviso** sí se redondea, y la ficha diría «72 m²» donde el dueño escribió 72,5. Cambiar la columna a decimal es una migración sobre datos que la regla del fundador manda tratar como producción, y toca el paso 4 de publicar, la ficha, la importación masiva y el lector de CSV.

  **Cuatro escalones, como mínimos y con el «+» en el último, igual que habitaciones: `40 · 60 · 90 · 120+` — DESCARTADOS, ver arriba.** El 40 deja los estudios (34, 38) por debajo del primer escalón, así que el primero no es «todo»; el 60 cae en el hueco 55–68, que es el que separa un ambiente de dos; el 90 cae en la cola de la masa y separa el de dos habitaciones del grande; el 120 es el piso de casa y quinta (120, 128, 145, 165, 240). **Ninguno de los cuatro queda vacío en ninguno de los dos conjuntos**, que es la única propiedad que se puede comprobar hoy — la regla transversal 4 pide que ninguna opción lleve a un vacío, y con un catálogo de producción chico eso hay que **volver a medir antes de entregar la B**, no dar por bueno acá.

  **EL TECHO SE PASÓ POR SIETE LÍNEAS, Y EL FUNDADOR LO ACEPTÓ (2026-09-03, PR #223 mergeado).** La nota original de esta rebanada decía «382 revisables» y **era falsa**; el agente que la cerró recontó con la misma cuenta que usa el resto de este archivo: **653** en bruto (623/30, 32 archivos, plan afuera) y **407** descontando comentarios y blancos, contra el techo de 400 de AGENTS.md §6.

  **Por qué no se partió, que es lo que hace que el desvío sea defendible y no comodidad.** `byMinBathrooms` es **requerido** en `FacetCounts` y en `PanelCounts`, así que la mitad de los datos no compila hasta que el panel y sus doce fixtures la conocen: partir acá no es mover código de PR, es rehacer el diseño. El corte barato que existía —sacar el acordeón y la salida del vacío (`search-accordion.ts` + `search-confirm.ts` + `search-preview.ts` y sus pruebas, ~120 revisables) a una rebanada A2— habría entregado el botón **sin resumen y sin ficha quitable**, o sea un filtro a medias en producción para ganar siete líneas de revisión.

  **Queda como precedente acotado y no como permiso.** Siete líneas sobre cuatrocientas es 1,75 %; el techo sigue siendo 400 y la próxima vez que alguien lo pase hay que volver a pedirlo, con el número recontado y no con el que dice una nota.

  **Tres fixtures que mentían, encontrados al cerrar y arreglados en el mismo PR** (`77b3ac0`): `app/alquiler/catalogo-de-prueba.ts` declaraba `byMinBathrooms: { 1: 16, 2: 7, 3: 0 }` sobre un catálogo de **cuatro** avisos —con la regla «conteos consistentes con `matching`» escrita arriba en el mismo archivo—, y los fixtures `EMPTY` y `VACIO` tenían todo en cero **menos** los baños. Son copias del arnés de `/measure` pegadas donde no van. El JSDoc de la prueba de integración además decía «A5 tiene tres» cuando el arreglo del dato lo había subido a cuatro, que es justo lo que hace que el «3+» esté medido por algo.

  **Nueve mutaciones, cada una dos veces: 9/9 mueren con las pruebas nuevas y 7/9 sobreviven sin ellas.** Las dos que mueren igual son contratos preexistentes (el nombre sin «ñ» en el contrato de indexado, y la cuenta de adelantos del escalón en cero). M1 (`= 3` en vez de `>= 3`) y M3 (la lista filtra baños exactos) **sólo mueren gracias al arreglo del dato**: con el catálogo viejo de tres baños máximo, las dos pasaban en verde.

  **Medida corregida al cerrarla: 653 líneas revisables** (623 agregadas, 30 borradas) en 32 archivos, sin contar este plan. **Sobre las 400 de AGENTS.md §6, y se dice en vez de recortar una prueba.** La nota original decía 382 y ese número no se reproduce con ninguna cuenta: 653 es la del resto del archivo —agregadas más borradas, plan afuera—, y aun descontando comentarios y líneas en blanco quedan 407, que ya se pasa. **Por qué no se partió igual**: `byMinBathrooms` es un campo requerido de `FacetCounts` y de `PanelCounts`, así que la mitad de los datos no compila sin que el panel y sus doce fixtures la conozcan; partirla obligaba a rehacer el diseño de la rebanada, no a moverlo de PR.

  Lo construido, con el archivo y la prueba que lo sostienen:

  - **`src/modules/listing-search/domain/bathroom-steps.ts`** — los tres escalones y el «+» del último (`bathroom-steps.test.ts`: «marca el último con un «+», porque «3» es tres o más y no exactamente tres»). Archivo aparte de `room-steps.ts` y no una lista compartida: lo que difiere es el dato —la lámina 7b dibuja **tres** botones para baños y **cuatro** para habitaciones—, y lo que se repite son dos líneas de etiqueta.
  - **El «3+» se escribe como MÍNIMO en la dirección y en el criterio.** `?banos=3` es `minBathrooms: 3` y la consulta es `bathrooms >= 3`, igual que `minRooms`. **`banos` sin «ñ»**: con «ñ» la dirección viaja `ba%C3%B1os` y deja de leerse en el chat donde se pega (F12). Entra en `FILTER_KEYS`, y `indexing-contract.test.ts` se puso rojo solo apenas el nombre existió — la mitad del contrato que la 14.6 ya había dejado armada.
  - **El conteo en la MISMA sentencia** (`drizzle-faceted-search.ts`): tres columnas `count(*) filter (where bathrooms >= n)` dentro del `select` que ya trae total, facetas y las nueve relajaciones — no una consulta aparte, igual que hizo el histograma de la 14.12. `queries === 1` sigue afirmado y sigue en verde sin tocarlo.
  - **La faceta no se cuenta contra sí misma** y `bathrooms` entra en `RelaxableFilter`, así que hay ficha quitable y salida del vacío con su número (`tests/integration/faceted-search.test.ts`: «el conteo de baños ignora el filtro de baños, y el último escalón es «o más»», «soltar los baños promete lo que soltarlos de verdad devuelve»).
  - **Los baños van DENTRO del grupo de habitaciones, que es el «tamaño» que el fundador nombró**, y no en un quinto grupo. La lámina 7b los dibuja en una sola columna, uno debajo del otro; el código ya convierte cada encabezado de columna en un grupo, y meter un quinto habría movido las tres columnas de la 14.32 y sus mediciones. El resumen del renglón cerrado nombra los dos —«2 hab · 2 baños»—: con sólo la mitad, el acordeón del teléfono esconde el filtro que alguien acaba de poner.

  **Corrección al enunciado, verificada: el estacionamiento NO era de esta rebanada** y queda entero para la C; los metros² para la B. Nada de lo escrito acá los toca.

  **Una cosa que la prueba de mutación destapó y que estaba mal.** El primer juego de datos de integración daba a lo sumo **tres** baños, así que `bathrooms >= 3` y `bathrooms = 3` devolvían el mismo número: la mutación «el «3+» cuenta exactamente tres» pasaba en verde, o sea que la decisión central de esta rebanada no estaba medida por nada. Se subió el aviso más grande a **cuatro** baños y la mutación pasó a matar dos casos. Es exactamente la clase de verde que la 14.42 nombra: un gate que no protege lo que dice proteger.

  **Evidencia.** `typecheck`, `lint`, `lint:tokens`, `build` en verde; 221 archivos / 2664 pruebas unitarias; 37 archivos / 367 de integración (73 en `faceted-search`); `test:e2e` entero en los DOS proyectos — `chromium` 34 pasadas / 1 salteada, `crawlability` 33 pasadas / 2 salteadas. `budget:bundle` 109.09 → 109.10 KB gzip en las dos rutas de búsqueda (el peor camino de lectura, 110.57 → 110.58 KB, techo 130): el panel se dibuja en el servidor y no manda una línea de más al navegador. Nueve mutaciones, cada una con su sha256 antes ≠ después y restaurada idéntica; las nueve mueren con las pruebas nuevas puestas y **seis sobreviven sin ellas** — las tres que igual caen lo hacen por guardas estructurales que ya existían (el contrato de indexado, la lista cerrada de nombres y el conteo de enlaces con adelanto).

  **Medida el 2026-09-02 y NO construida, con el costo dicho en vez de estimado.** Tres correcciones al enunciado, las tres verificadas contra el código y contra la lámina:

  1. **Baños no necesita migración.** `listing.bathrooms` ya está en el esquema (`src/shared/db/schema.ts:465`, `integer NOT NULL`, sin `default`) — lo escribe el paso 4 de publicar. Lo que no existe es todo lo de arriba: `grep -rn bathroom` da **cero** aciertos en `src/modules/listing-search/`. No está en `SearchCriteria`, ni en `RawSearchParams`, ni en `SearchQueryField`/`SEARCH_QUERY_NAMES`, ni en `FILTER_KEYS`, ni en `DrizzleListingSearch`, ni en `FacetCounts`, ni en `SearchPanelModel`, ni en el panel.
  2. **`minAreaM2` no es «criterio sin control»: es criterio sin dirección.** Existe (`search-criteria.ts:100`), se parsea (`:273`) y se aplica en las DOS consultas —`gte(listings.areaM2, …)` en `drizzle-listing-search.ts:100` y en el `WHERE` compartido de `drizzle-faceted-search.ts:125-127`, donde está a propósito **fuera** de las facetas—. Lo que no tiene es nombre corto de URL ni entrada en `FILTER_KEYS`, y **ninguno de los dos `page.tsx` lo pasa**. Desde una pantalla es inalcanzable.
  3. **Ninguna lámina dibuja un control de m².** Los encabezados que la 7b dibuja son **Precio mensual · Habitaciones · Baños · Quién publica · La propiedad tiene** — leídos del marcado de la lámina. La mitad «metros²» del título de esta tarea **no tiene diseño detrás**, así que construirla sería inventar una pantalla (AGENTS.md §2). Necesita lámina o decisión del fundador antes que código.

  **Lo que cuesta la mitad que sí tiene lámina.** Un filtro de baños con su conteo es el análogo exacto de habitaciones, y habitaciones hoy se menciona en **233 líneas repartidas en 31 archivos**: `room-steps.ts` entero (49 líneas) más su prueba (50), el resolvedor de `search-options.ts`, el criterio, el nombre corto, `FILTER_KEYS`, los dos `page.tsx`, el eje y las columnas `count(*) filter` de `drizzle-faceted-search.ts`, `FacetCounts`, `PanelCounts`, `RelaxableFilter` —que arrastra `RELAXATION_ORDER`, `RELAXATION_NAMES` y `withoutFilter` en dos archivos—, `PreviewChange`/`totalAfter`, `SearchStepId`/`SEARCH_STEPS`/`STEP_COPY`, el componente del paso y la hoja. **Con las pruebas que este repositorio escribe, no entra bajo el techo de 400 líneas**: es su propio PR y probablemente dos. Por eso no va con la 14.46.

  **`queries === 1` sobrevive, y no por suerte:** los conteos de baños son columnas `count(*) filter (where …)` más en el mismo `select` con `GROUP BY zone_id`, no un viaje nuevo. Lo que sí conviene anotar es que el caso de `tests/integration/faceted-search.test.ts:654-695` **no pasa `minAreaM2`**, así que hoy una regresión de área no la vería nadie ahí.

  **El sexto atributo tampoco necesita migración.** `listing.parking_spots` existe (`schema.ts:473`, `integer NOT NULL DEFAULT 0`) y `LISTING_ATTRIBUTES` tiene cinco booleanos (`search-criteria.ts:164-170`). «Puesto de estacionamiento» sería `parking_spots > 0` sobre una columna que ya está. Sigue siendo decisión del fundador: es vocabulario de producto, no cableado
- [x] 14.46 **El fondo del modal, que hoy es una hoja opaca.** `SearchPanel.module.css` tapa el viewport con `background: var(--surface)` porque **no existe un token de velo** y `lint:tokens` prohíbe el literal — un `rgba(0,0,0,.5)` escrito a mano falla el gate, y con razón. La lámina 7b dibuja el modal sobre la lista, no reemplazándola. Es «se extiende el sistema, no se inventa un valor local» (SISTEMA.md): un `--scrim` en `tokens.css` con su par claro/oscuro, en `design/reference/sistema/tokens.css` y en la tabla de SISTEMA.md. Chico, pero es un cambio al sistema y no un uso de él. **Hecho 2026-09-02.**

  **No se declaró un token nuevo: se generalizó el que ya había.** El velo existía con otro nombre desde la 15.8 —`--door-veil`, la puerta de entrar— y con el mismo valor exacto. Declarar `--scrim` al lado habría sido dos nombres para un valor, que es lo que la 16.22 dejó **prohibido con un gate**: `scripts/lint-tokens.mjs` rechaza `--rule` y `--acc-ink` por esa razón. Así que `--door-veil` pasó a llamarse `--scrim`, la puerta usa el mismo token, y el nombre viejo entra en `TOKEN_ALIASES` — `var(--door-veil)` sin declarar no pinta nada y por lo tanto no da síntoma, que es exactamente cuándo hace falta un gate y no una costumbre.

  **La lámina 7b no dibuja ningún velo, y hay que decirlo.** El enunciado de arriba dice que dibuja el modal sobre la lista; lo que dibuja es el panel como **tarjeta de 1100 sobre una banda de `var(--bg)`**, con borde, `--r` y sombra, y sin nada translúcido en ninguna parte — verificado leyendo el marcado de la lámina, no su descripción. El único velo que el diseño dibuja en todo el conjunto es el de la lámina 9b (`rgba(30,32,34,.55)`), y es el que se toma. La lámina queda corregida por la 14.46 igual que la 16.24 corrigió su `min-height:40px`, y la fila está anotada en la tabla de cierre de `SISTEMA.md`.

  **Par claro/oscuro, y la razón es una medición.** `--viewer-scrim` y `--door-veil` vivían en `:root` con el argumento de que un velo «no es un color de la paleta, es una sombra sobre ella». Ese argumento es falso para este velo y el número lo dice: compuesto sobre `--bg`, el velo de `menta` separa el fondo velado de la lámina **3,98:1** contra 1,10:1 sin velo; ese **mismo** velo en `oscuro` da **1,13:1 contra el 1,15:1 que el tema ya tenía sin nada** — o sea deja la pantalla peor que no poniéndolo, porque oscurecer un fondo ya oscuro no separa. Por eso `--scrim` va en los dos bloques de tema (y `--dark-scrim` es negro puro: la paleta oscura no tiene ningún color más oscuro que su propio fondo), y por eso el contrato de tema de `lint:tokens` ahora lo cubre.

  **Un token no tiene comportamiento, así que se mide lo que produce.** `lint:tokens` prueba que ninguna hoja escribe un literal; **no prueba qué color sale**, y este defecto es la demostración: `.panel` tapaba el viewport con `var(--surface)` —un token, gate en verde— y lo que se dibujaba era una pantalla opaca. Dos capas, y rompen por separado: `components/design-contract.test.tsx` → *«menta/oscuro: el velo deja ver la lista — no es una lámina opaca»*, *«menta/oscuro: el velo aleja el fondo de la lámina, no lo acerca»* (la cota no es un número inventado: es lo que el tema ya separa sin velo), *«el par existe de verdad: los dos temas no repiten el mismo velo»*, *«el modal de filtros se dibuja sobre el velo y su hoja conserva --surface»* y *«la puerta usa el mismo velo: un valor, un nombre (16.22)»*; y en un navegador de verdad, `tests/measure/layout.spec.ts` → *«14.46: a 1280 el velo deja pasar la lista y la hoja no lo tapa entero»* (lee del compositor `rgba(30, 32, 34, 0.55)`, alfa 0,55, y comprueba que la hoja mide 800 de 1280 y arranca 8 px más abajo), *«14.46: el velo se repinta con el tema — el par claro/oscuro es real»* (pasa a `rgba(0, 0, 0, 0.66)`) y *«14.46: a 360 la hoja ocupa el ancho entero — la lámina 6b dibuja pantalla, no tarjeta»*.

  **`design/reference/sistema/tokens.css` NO se tocó, y es a propósito.** Ese archivo es el prototipo de 9 paletas × 4 estructuras, y no lleva ni `--viewer-scrim` ni llevaba `--door-veil`: meter un velo ahí obliga a inventarle un valor a siete paletas que nadie dibujó. Se registra donde el conjunto registra este tipo de extensión, que es `SISTEMA.md` — el mismo lugar y el mismo criterio que usó la 15.8.

  **Destapado de paso y NO corregido acá: `.sheet` lee mal la lámina.** `max-inline-size: 800px` cita *«el panel entero entra en 800 px» (lámina 7b)*, pero el 800 de la lámina es el **pliegue**: es un `<div class="fold">` con `top:800px`, la misma línea horizontal que la nota de la 7c llama «sobre el pliegue» al contar 8 avisos. El panel de la lámina mide **1100 de ancho**, que es el contenedor. O sea la hoja está midiendo un alto como si fuera un ancho. No se cambia acá porque mover el ancho del panel mueve las tres columnas de la 14.32 y sus mediciones, y eso es otra tarea
- [x] 14.47 **«Recientes ▾», el orden de la lista.** La lámina 7c lo dibuja al lado del conteo —«9 avisos en Chacao y Altamira · Recientes ▾»— y **ninguna tarea lo nombraba**; se destapó leyendo la lámina para la 14.33. Hoy el orden lo fija `DrizzleListingSearch` y no se puede cambiar desde la pantalla. Qué opciones hay es una decisión de producto (¿precio? ¿superficie?), y cuál es la de por defecto también — con la salvedad de que el orden es parte de la dirección o no se puede compartir, y si es parte de la dirección entra en `FILTER_KEYS`, porque la misma lista en otro orden es la misma página

  **LA DECISIÓN, 2026-09-03.** Tres opciones y ni una más: **Recientes** (la de por defecto), **Precio: menor a mayor** y **Precio: mayor a menor**.

  **Por qué «Recientes» sigue de por defecto, dicho como decisión y no como inercia.** Hoy `DrizzleListingSearch` ordena `ORDER BY published_at DESC, id ASC` y no es una elección: es lo que quedó. Ahora sí lo es, y la razón es del catálogo — un aviso viejo en un mercado de alquileres suele estar tomado, así que el más nuevo arriba es el que tiene más chance de existir todavía.

  **Por qué NO se ofrece orden por superficie, que era la otra candidata obvia.** `area_m2` no tiene control ni conteo en el filtro (ésa es la 14.45) y **una parte de los avisos puede no declararla**. Ordenar por un campo que falta ordena mal y **en silencio**: los avisos sin metros se van todos juntos a una punta de la lista sin que nadie pueda ver por qué. Un orden que miente es peor que un orden que no existe. Si la 14.45 se construye y el dato pasa a ser obligatorio, esta decisión se puede reabrir con el número de avisos sin metros a la vista.

  **El segundo criterio de desempate no se toca.** `asc(listings.id)` está ahí por una razón escrita arriba —sin un orden total, dos páginas de la misma búsqueda repiten un aviso y se saltan otro porque `OFFSET` corta sobre el orden que Postgres haya elegido esta vez—, así que los dos órdenes de precio lo conservan: `ORDER BY price_usd ASC|DESC, id ASC`.

  **Lo que queda para quien la construya, y es sólo técnico:** (a) el orden va en la dirección o no se puede compartir un enlace; (b) yendo en la dirección entra en `FILTER_KEYS` (`src/modules/listing-search/domain/search-query.ts`), y ahí está la trampa — **la misma lista en otro orden es la MISMA página para Google**, así que `?orden=…` no puede indexarse aparte o se publica el catálogo entero tres veces como contenido duplicado; (c) la lámina lo dibuja **sólo en escritorio**, y qué pasa en móvil es una pregunta que el tablero no contesta.

  **CONSTRUIDA 2026-09-03.** El vocabulario y las tres direcciones los arma `src/modules/listing-search/domain/search-order.ts` (`search-order.test.ts`), la consulta los ordena en `ORDER_BY` de `drizzle-listing-search.ts` (`tests/integration/listing-search.test.ts` → *«el orden de la lista (14.47)»*), y el control es `components/molecules/OrderMenu.tsx` (`OrderMenu.test.tsx` + `tests/e2e/camino-de-lectura-sin-javascript.spec.ts` → *«cambiar el orden de la lista funciona sin una línea de JavaScript»*, con el script apagado).

  **Lo de Google se resolvió con las DOS mitades, y la segunda es la que se podía errar en silencio.** `orden` entra en `FILTER_KEYS`, así que `?orden=…` sale del índice — el mismo trato y la misma razón que `filtros` y `busca`. Y **«Recientes» viaja como AUSENCIA del parámetro** (`SEARCH_ORDER_TOKENS.recent === null`): con un `?orden=recientes` explícito, la línea anterior habría sacado del índice justo la página en el orden por defecto, que es el riesgo que el enunciado nombra. No hizo falta inventar la primera mitad: `indexing-contract.test.ts` ya exigía que todo nombre de `SEARCH_QUERY_NAMES` estuviera en `FILTER_KEYS`, y se puso rojo solo al agregar `orden`.

  **En móvil se DIBUJA, y la lámina sólo lo dibuja en escritorio.** Esconderlo por debajo de 768 px no escondería una decoración: borraría la única salida de un estado que la dirección igual puede traer. Un enlace armado en escritorio con `?orden=precio-desc` y pegado en un chat abre en el teléfono en ese orden, y sin el control ahí no hay forma de volver a «Recientes» que no sea editar la URL a mano. El precedente de esconder por ancho (14.53, *«el servidor no sabe el ancho de la pantalla»*) resuelve DÓNDE dibujar algo que existe en los dos lados; no autoriza a quitar una función en uno.

  **Tres desvíos anotados, los tres con su razón.** (a) Es un `<details>` nativo con tres enlaces adentro, no un `<select onchange>`: el segundo se dibuja igual y no navega con el script apagado. El argumento de la 14.32 contra `<details>` no aplica —aquél era que ninguna hoja puede REABRIR uno cerrado, y acá los dos anchos quieren el mismo estado—, y si algún navegador no lo soportara las tres opciones quedan a la vista, que es la única degradación que no borra una función. (b) `min-height` es `--target-min` (44) y no los 40 px de la lámina, porque el control existe también en el teléfono y 40 queda por debajo del blanco de toque que el sistema declara. (c) El tamaño es `--meta-fs-sm` (11) y no los 11,5 de la lámina: el único 11,5 declarado es `--nav-avatar-fs-desktop`, cuyo significado son las iniciales del avatar, y estrenar un token por medio píxel es un cambio al conjunto que decide el fundador.

  **CORRECCIÓN 2026-09-03, y la destapó CI: el desplegable quedaba abierto sobre la lista con JavaScript.** `open` es estado del DOM y no de React. Sin script cada elección recarga el documento y el `<details>` vuelve cerrado solo; con `next/link` la navegación es de cliente, React reusa el mismo elemento y el menú quedaba **colgado sobre los primeros avisos** justo después de elegir. Se arregla keyeando el `<details>` por el orden puesto —*un orden distinto es un desplegable distinto*, así que React monta uno nuevo y nace cerrado—, para lo cual `SearchOrderMenu` estrena el campo `order`, que es la identidad del menú y no una etiqueta.

  **Lo que enseña el defecto es sobre las pruebas, y por eso queda escrito.** La prueba de punta a punta corre en los DOS proyectos y **no se acotó a `crawlability`**, aunque su nombre hablara del script apagado. Ahí estuvo el valor: `crawlability` pasaba en verde —la recarga tapaba el defecto— y fue `chromium` el que lo encontró. Es el reparto que ese archivo entero ya usaba sin decirlo: `crawlability` prueba que el piso existe y `chromium` que la mejora no lo rompe, que son las dos mitades del D13. La prueba pasó a llamarse por lo que afirma en los dos —*«cambiar el orden de la lista es un enlace, y el menú se cierra al elegir»*— y estrena la aserción que sólo `chromium` puede medir. Verificado rompiéndolo: sin el `key`, `crawlability` sigue verde y `chromium` se pone rojo.

  **Una mutación sobrevivió y obligó a corregir una fixture, que es el hallazgo del PR.** Cambiar el orden por defecto de «Recientes» a «Precio: menor a mayor» no ponía nada en rojo: la ciudad paginada sembraba `priceUsd: 100 + index` con el índice ascendente = más viejo, así que el precio ascendente y la fecha descendente daban EXACTAMENTE la misma lista y la prueba pasaba midiendo una casualidad. La fixture pasó a `100 + ((index * 7 + 3) % PAGINADOS)` —coprimos, así que cada aviso sigue con un precio distinto— y los tres órdenes encabezan ahora con tres avisos distintos.

  **No se tomó el 2026-09-02, y la razón no es el techo de líneas.** El enunciado mismo dice que **qué opciones hay y cuál es la de por defecto son decisiones de producto**, y no hay lámina que las fije: la 7c dibuja la etiqueta «Recientes ▾» y nada más — ni el desplegable abierto, ni las otras opciones. Construirlo obliga a inventar el vocabulario, que es exactamente lo que la 14.32 se negó a hacer con baños. Tres cosas más, medidas mientras se descartaba, para que quien la tome no las vuelva a derivar:

  - El orden de hoy es `desc(published_at), asc(id)` (`drizzle-listing-search.ts:144`), y **el `id` es lo que hace estable la paginación**: cualquier orden nuevo necesita su propio desempate o el mismo aviso aparece en dos páginas.
  - Si el orden viaja en la dirección —y tiene que viajar, o no se puede compartir— entra en `SearchQueryField`, `SEARCH_QUERY_NAMES` y `FILTER_KEYS`.
  - Y ahí hay una decisión escondida: `FILTER_KEYS` es lo que `isFilteredZoneRoute` usa para decidir `noindex` (`zone-route.ts:130-132`). **La misma lista en otro orden es la misma página**, así que probablemente NO deba contar como filtro para indexación — pero eso es una decisión, no un detalle de implementación, y meterla sin nombrarla saca de Google la página en el orden por defecto o mete dos veces la misma

- [x] 14.48 **`--searchbar-h` y `--searchbar-fs`, dos tokens sin un solo uso.** Los destapó la 14.42 al borrar `SearchBar.module.css`, que era la única hoja que los leía. **`lint:tokens` no los ve**, y ése es el punto: ese gate verifica que un valor SEA una propiedad personalizada y que los dos temas la declaren, nunca que alguien la use — el mismo hueco que `tokens.css` ya documenta con el `<h1>` agarrando `--fpb`. Sacarlos es un cambio al conjunto y no un uso de él (SISTEMA.md), así que van con su fila en la tabla de SISTEMA.md y en `design/reference/sistema/tokens.css`. Antes de tirar: `design-contract.test.tsx` todavía afirma `--searchbar-h ≥ --target-min` y `--searchbar-fs ≠ --control-fs`, y esas dos aserciones se van con los tokens, no antes. Decidir también si `lint:tokens` debería contar usos, que es la causa y no el síntoma

  **Cerrada 2026-09-02, y lo que cierra de verdad es la causa: `lint:tokens` cuenta usos.** Los dos tokens se tiran, sus dos aserciones se van con ellos, y el gate estrena un tercer chequeo — *cada token declarado tiene un lector*.

  **La decisión pedida, y el argumento.** El síntoma valía dos líneas; la causa era que el gate **no puede ver lo que no aparece**: verifica que un valor SEA una propiedad personalizada (1b.3) y que los dos temas la declaren (1b.4), y un token huérfano no aparece en ninguna hoja. Este repositorio ya lo encontró **tres veces a mano** —14.48, 16.37 (`--fpb`), 22.13 (`--ft`/`--tclamp`)— siempre barriendo por otra cosa. Tres instancias de la misma forma es una causa, no una coincidencia.

  **Lo que decidió el caso fue medir el falso positivo antes de escribir el gate, no discutirlo.** De los **134 tokens declarados**, **128 tienen lector** y **6 no**. Ése es el costo real de la regla: seis, no treinta. Y de los seis, **dos no eran peso muerto sino defectos vivos con síntoma en pantalla** que nadie había visto:

  - **`--pill-text-fs-desktop` (14px) no lo leía ninguna hoja.** `.input` escribía `--pill-text-fs` en la base **y otra vez** dentro del `@media (max-width: 480px)` — o sea 13,5 en todos los anchos y una declaración que no hacía nada. `.filter`, doce líneas más abajo en la misma hoja, tiene la forma correcta (el de escritorio en la base, el del teléfono en el `@media`), así que la corrección no inventa un valor: usa el que el conjunto ya declara del lado que le corresponde.
  - **`--nav-avatar-fs` (13px, teléfono) tampoco.** `.avatarInitials` usaba el de escritorio **sin ningún `@media`**, así que las iniciales medían 11,5 también a 360.

  Un gate cuya primera corrida encuentra dos defectos en producción **se paga solo en esa corrida**. Y ninguno de los dos podía verse antes: `lint:tokens` no mira qué píxel sale, y el par declarado se leía perfecto en `tokens.css`.

  **Los dos quedan dibujados y medidos en un navegador de verdad**, porque un token no tiene comportamiento y se mide lo que produce (mismo criterio que la 14.46): `tests/measure/layout.spec.ts` → *«14.48: el texto de la pastilla es 13,5 en el teléfono y 14 en escritorio»* y *«14.48: las iniciales del avatar son 13 en el teléfono y 11,5 en escritorio»*, leyendo `getComputedStyle` a 360 y a 1280, con la aserción de que **no son el mismo número** — que es lo que había. `app/measure` estrena el arnés de barra **con sesión** que faltaba: el menú de cuenta no se dibujaba en ninguna parte medible, y por eso ahí vivía un token muerto.

  **El falso positivo legítimo existe y NO se resuelve apagando el gate.** Un token DEL SISTEMA declarado para una pantalla que este producto todavía no dibuja es correcto: retirarlo del subconjunto que ship*a* es un cambio al conjunto y lo decide el fundador, que es exactamente cómo la 16.37 cerró `--fpb`. `--ft` y `--tclamp` son ése caso y son de la **22.13**, así que quedan con una excepción **nombrada, con su motivo y su tarea dueña** — la misma forma que `TOKEN_ALIASES` ya usa en ese archivo. Y **la excepción caduca sola**: si un token exceptuado vuelve a usarse el gate falla igual, por la excepción vencida. Una lista de perdones que nadie limpia es cómo un gate deja de significar algo.

  **Los archivos se recorren, no se enumeran** — misma lección que `cron-wiring.test.ts`: una lista escrita a mano de hojas a mirar es un gate que no ve la siguiente hoja que alguien agregue.

  **Y `scripts/` estrena un lugar donde probarse.** `scripts/lint-tokens.test.ts` **ejecuta** el gate contra un árbol de mentira (no importa sus funciones: `tsconfig` tiene `allowJs: false`, que es la misma razón por la que `components/contrast.ts` duplica sus ayudantes). Un gate sin prueba propia puede dejar de comprobar en silencio, y esa ausencia es parte de por qué este hueco duró: mutar el chequeo a que devuelva siempre vacío deja `pnpm lint:tokens` **en verde** y sólo esa prueba en rojo.

  **CORRECCIÓN al enunciado, verificada antes de tocar (AGENTS.md §5).** Decía que los dos tokens *«van con su fila en la tabla de SISTEMA.md y en `design/reference/sistema/tokens.css`»*. **No están en ninguno de los dos** — comprobado por `grep`: son medidas que este proyecto inventó para `SearchBar.module.css`, así que sacarlos no toca el sistema. Es la distinción que la 16.37 ya había dejado escrita («aquéllos son medidas que este proyecto inventó y puede tirar»), y este enunciado es anterior a ella.

- [x] 14.49 **`model.activeFilters`, huérfano desde que se fue la barra resumen.** Era el número del engranaje de `SearchSummaryBar` —su prop `activeFilters`—, o sea que la 14.41 lo dejó sin pantalla y la 14.42 sin componente. Hoy lo leen tres aserciones de `search-panel.test.ts` y nadie más; ninguna página ni `SearchPanel.tsx` lo tocan (las pastillas usan `pillFilters`, que sí se dibuja). No se borró con la 14.42 porque la lista de esa tarea nombraba `panel.summary` y no éste, y arrastra `countActiveFilters`, que tiene seis casos propios en `search-accordion.test.ts` incluida la resta contra `countPillFilters` que documenta por qué la zona cuenta en uno y no en el otro. O se borran los tres juntos, o se dice qué pantalla va a volver a mostrar ese número

  **Cerrada 2026-09-02: se borraron los tres juntos, y ninguna pantalla los va a volver a mostrar.** `model.activeFilters`, `countActiveFilters` y las nueve aserciones que los medían (tres en `search-panel.test.ts`, seis en `search-accordion.test.ts`). La verificación de que estaba muerto NO fue una corrida en verde —eso es lo que también da un borrado que nadie probaba— sino el compilador: al sacar el campo, `pnpm typecheck` enumeró **todos** sus lectores, y los seis eran pruebas o arneses de medición; ninguna página, ningún componente. Y se estableció en positivo lo que un `grep` no puede: no hay archivo barril en `app/` ni en `components/`, y ningún `import(` dinámico alcanza estos módulos, así que no hay lectura por cadena que el tipo no vea.

  **Lo que habría estado rojo si siguiera en uso**: cualquier `panel.activeFilters` en una página o un componente deja de compilar — es exactamente lo que pasó con los seis sitios que sí lo leían.

  **Las seis de `search-accordion.test.ts` no se borraron todas, y la que sobrevive cambió de forma.** Cinco medían `countActiveFilters` y se van con él. La sexta era la invariante —«es el conteo del engranaje menos la zona, nunca otra cosa», escrita como `countActiveFilters(x) - countPillFilters(x) === 1`— y esa resta se quedó sin minuendo. Lo que protegía sí se conserva, dicho de la única forma que queda honesta: **agregar una zona no mueve `countPillFilters`**. Mutación: hacer que `countPillFilters` cuente la zona pone rojas 3 de `search-accordion.test.ts` y 2 de `search-panel.test.ts`; con las nuevas afuera siguen rojas 2, o sea que el positivo estaba protegido antes y sigue estándolo.

  **Y se borró media aserción de `nav-contract.test.ts`**, que afirmaba `not.toMatch(/filterCount:\s*panel\.activeFilters/)` sobre el texto de las dos páginas. Lo que la reemplaza es más fuerte: el campo no existe, así que escribirlo **no compila**. Dejarla habría sido una aserción incapaz de fallar, que es la trampa 1 de este plan.

- [x] 14.50 **El abanico de consultas por ciudad: ¿se sigue pagando un conteo que nadie dibuja?** Lo destapó la 14.44 al negarse a borrar `model.cities`. `buildFilterPanel` dispara **una consulta por cada otra ciudad** en cada carga de resultados (`Promise.all` sobre `request.cities`) sólo para llenar `PanelCity.count`. Es F3 y estaba bien mientras el panel dibujaba el paso de ciudad; la 14.36 lo sacó («un dato, un lugar»: la ubicación vive en la ruta) y desde entonces el número se calcula, viaja y no se muestra. **No es un borrado y por eso no entró en la 14.44**: es una decisión de producto con un costo medible detrás — hoy son 2 ciudades, o sea un viaje extra por búsqueda, y crece con el catálogo. Si la respuesta es que no se muestra más, se borran **juntos** el abanico, `PanelCity.count`, `model.cities`, `toCityChoice` y las doce aserciones que lo miden (nueve de integración contra Postgres real en `faceted-search.test.ts`, tres en `build-filter-panel.test.ts`) — y `planCityChange` queda entonces sin llamador de producción, con siete casos propios en `search-query.test.ts`, así que entra en la misma decisión. Si la respuesta es que vuelve a una pantalla, se dice a cuál y la tarea se cierra sin tocar nada

  **Cerrada 2026-09-02. La respuesta es que no se muestra más, y el costo se midió antes de decidir en vez de estimarse.**

  **Lo que un resultado cuesta hoy, contado de verdad y no leído del código.** Se instrumentó el proxy HTTP de Neon del arnés de la 11.22 —cada `POST /sql` es exactamente un viaje de red— y se cargó la pantalla contra la aplicación compilada, con la semilla de e2e (dos ciudades):

  | Pantalla | Antes | Después |
  |---|---|---|
  | `/alquiler/distrito-capital` | **6** | **5** |
  | `/alquiler/distrito-capital/chacao` | **6** | **5** |
  | `/alquiler/distrito-capital?min=250&hab=2` | **6** | **5** |

  Los seis eran: 2 del catálogo (`listCities` + `listZones`, compartidos con `generateMetadata` por `cache`), 1 de la lista, 1 de las portadas, 1 de `countFacets` y **1 por cada otra ciudad**. O sea **4 + N**, que con N=2 son 6 y con el catálogo del país entero son 4 + N. Ahora son **5 fijos**.

  **La 14.11 tenía razón y su cota estaba en el lugar equivocado.** `tests/integration/faceted-search.test.ts` afirma `queries === 1` desde el PR #79, y esa afirmación envuelve `countFacets` — el adaptador. El abanico vivía una capa más arriba, en `buildFilterPanel`, así que **la promesa de un viaje único viajaba debajo de un bucle que nadie contaba**. Medido con el mismo arnés antes de tocar nada: `buildFilterPanel` con dos ciudades en el catálogo, **2 consultas**. La cota nueva está donde faltaba: *«el panel entero cuesta UN viaje de red (14.50)»* en integración contra Postgres real, y *«le hace UNA sola pregunta al puerto»* contra el doble en `build-filter-panel.test.ts`, que es rápida y corre en cada `pnpm test:unit`.

  **Por qué no se muestra más, verificado y no supuesto.** `SearchPanel.tsx` no menciona `cities` en ninguna línea; los únicos consumidores de `model.cities` eran dos fixtures de prueba y el arnés de `app/measure`. Y el producto **sí tiene** un selector de ciudad — las fichas del inicio, `homeCityChips` en `listing-discovery` — pero es otro módulo, se alimenta de otro puerto y **no lleva conteo**: la lámina no lo dibuja con número. O sea que el número no está esperando una pantalla; la pantalla que lo pedía la sacó la 14.36 por decisión del fundador.

  **Se fueron juntos**, como la tarea pedía: el abanico, `PanelCity.count`, `PanelCity` entero, `model.cities`, `CityChoice`, `toCityChoice`, `planCityChange` con su `CityChangePlan`, y las pruebas que los medían: los **tres casos de integración** contra Postgres real de «el conteo por ciudad (F3)» (ocho aserciones), **dos** de `build-filter-panel.test.ts` (tres aserciones), los **seis** de «paso 1 · la ciudad» en `search-panel.test.ts` y los **siete** de `planCityChange` en `search-query.test.ts`. Ninguna se reapuntó a otro sujeto, por la razón que la 14.42 dejó escrita en `design-contract.test.tsx`: una aserción mudada de sujeto dice seguir protegiendo lo de antes y protege otra cosa.

  **Y la petición dejó de llevar el catálogo para sacarle un nombre.** `FilterPanelRequest.cityId` + `cities` pasaron a ser un `cityName: string`, que es el único dato que el panel leía de esa lista: el encabezado. Llevar la lista entera **era lo que hacía expresable el abanico**; con un nombre no hay sobre qué iterar. Es la misma forma de la 14.11 aplicada a la firma en vez de al SQL.

  **Lo que habría estado rojo si el conteo siguiera dibujándose:** `pnpm typecheck` nombra a cada lector de `model.cities` al borrarlo, y nombró seis — dos fixtures, el arnés de medición y tres bloques de prueba. Un componente que lo dibujara habría salido en esa lista y el borrado no habría compilado. La corrida en verde **no** es la prueba y no se ofrece como tal.

  **Destapado por la mutación y corregido acá: el encabezado de la búsqueda no se medía en el dominio.** Ignorar `input.cityName` en `buildSearchPanel` ponía roja UNA prueba, y era de `app/` — donde el suelo de cobertura del 90 % no llega. `model.headline` es lo que la pastilla dibuja cuando no hay zona elegida, y era exactamente el dato que el catálogo de ciudades servía. Se agregaron dos casos en `search-panel.test.ts` y uno en `build-filter-panel.test.ts`. **Segunda mutación, segundo hueco**: poner `cityName: ""` en la llamada de la página de **zona** pasaba en verde, porque ahí el encabezado lo pone la zona y el nombre de la ciudad sólo sale en la causa del vacío. `nav-contract.test.ts` lo afirma ahora sobre las dos páginas — **contando las dos apariciones y no buscando una**, porque la primera versión de esa aserción también pasaba en verde: cada página pasa el nombre en dos lugares (la cuadrícula y el panel) y encontrar uno alcanzaba.

  **La 20.6 queda anotada y NO se da por entregada en silencio** (ver su casilla): `planCityChange` era su única implementación.

- **Mudada acá el 2026-09-01, desde la fase 18.** Nació ahí porque la 18.9 la midió mientras construía el paso 3, pero lo que pide tocar es `countFacets` y su `width_bucket` —el motor del histograma, que es trabajo de la 14.12— y no tiene nada que ver con «Publicar — nueve pasos». **Conserva el número 18.28 a propósito**: la numeración de esta fase no es contigua ni ordenada (va de la 14.32 a la 14.52 salteada), así que nada obliga a renumerarla, y el enunciado de la 18.9 la nombra por número — renumerarla dejaría esa referencia apuntando a nada.

- [x] 18.28 **(nueva — la deuda que la 18.9 dejó medida en vez de esconder.) Desprender el reparto en cubos a una función que los dos adaptadores llamen.** Hoy `DrizzleZonePriceTally` compone `DrizzleFacetedSearch`, así que hay UN `width_bucket` en el repositorio —que es lo que la 18.9 pedía— pero el paso 3 paga por debajo las treinta agregaciones del panel de búsqueda para leer ocho cubos. **No es un viaje de red de más ni un recorrido de filas de más**, que es el costo dominante; son agregaciones filtradas sobre filas que la consulta ya tiene en memoria. **Lo que costaría**: sacar el CTE de extremos y las ocho celdas `jsonb` de `countFacets` a una función que reciba el `WHERE` y devuelva la subconsulta, y que los dos adaptadores la llamen. **Lo que exige**: `tests/integration/faceted-search.test.ts` y su `queries === 1` tienen que seguir en verde sin tocarse, y hace falta una prueba de integración propia del puerto angosto — hoy no tiene ninguna. **Por qué no se hizo en la 18.9**: partía un archivo embarcado tres PR antes dentro de una rebanada que ya estaba en el techo de revisión, y el beneficio es CPU medible, no un defecto

  **Cerrada 2026-09-02: NO se desprende, y la razón no es el techo de líneas — es que lo que ahorra y lo que arriesga no están en la misma escala.**

  **Lo que ahorra, con el número del propio enunciado.** No es un viaje de red ni un recorrido de filas de más: son ~30 agregaciones filtradas sobre filas que la consulta ya tiene en memoria, en una pantalla **con sesión** (`/publicar/paso/3`) y de una sola zona. El costo dominante de este producto es el viaje de red — la 14.11 lo estableció y la **14.50 acaba de mostrar cuánto pesa cuando es real: 6 → 5 viajes por resultado, medidos**. Esto no mueve ninguno de los dos números que sí duelen.

  **Lo que arriesga, y acá está el hallazgo.** La pregunta de la tarea era *«¿extraer arriesga dos implementaciones?»*. Del `width_bucket` **no**: una función que devuelva la subconsulta sigue siendo una sola. **El riesgo está una capa antes, en el `WHERE`.** El enunciado pide *«una función que reciba el `WHERE` y devuelva la subconsulta»*, y recibir el `WHERE` es **sacarlo del único lugar donde vive hoy**. La 21.1 ya dejó escrito por qué eso importa, en el propio `drizzle-faceted-search.ts`: *«Que vivan en el WHERE compartido es la parte que importa: es el mismo lugar del que sale el total, cada faceta, `cityTotal` y las nueve relajaciones, así que ningún número puede quedarse con la mitad de la regla.»* La frescura son **dos** condiciones (`status = 'active'` **y** `expires_at > now()`), y son las que un segundo llamador puede pasar a medias.

  **Y dos `width_bucket` sería el problema menor.** Dos reparticiones dan dos «la mayoría pide entre $X y $Y» distintos, que se nota comparando. Dos `WHERE` dan **un histograma que cuenta avisos vencidos**, y eso no se nota nunca: la frase sale igual de plausible. Medido, no supuesto: sacar `gt(expiresAt, now())` del `WHERE` compartido pone rojas 8 pruebas de integración — con la prueba nueva del puerto angosto y sin ella, porque esa capa ya estaba cubierta. Y desviar el `WHERE` **del puerto angosto** (que es exactamente lo que la extracción haría posible) pone rojas 5 **sólo si la prueba nueva existe**; sin ella, todo verde.

  **La versión segura del refactor existe** —extraer también el constructor del `WHERE` desde el criterio, no recibirlo hecho— pero es más grande que la que el enunciado describe, parte el mismo archivo que la 18.9 se negó a partir, y su beneficio sigue siendo CPU en una pantalla con sesión. Queda dicho por si alguien la retoma: **la línea es «recibe el criterio», nunca «recibe el `WHERE`»**.

  **Lo que SÍ se hizo, que es la otra mitad del enunciado y no dependía de la extracción.** *«Hace falta una prueba de integración propia del puerto angosto — hoy no tiene ninguna.»* Era cierto y era el hueco caro: `drizzle-zone-price-tally.ts` tenía **cobertura 0** y sus ocho cubos son la frase que ve quien publica. `tests/integration/zone-price-tally.test.ts` (nuevo, 5 casos contra Postgres real) lo cubre: el reparto de los doce avisos con sus extremos y su cubo vacío sin nombres, el aislamiento de zona, el de ciudad con dos zonas homónimas (D5), y el vencido por reloj (21.1).

  **Y una de las cinco es la invariante de la 18.9 convertida en aserción**: *«devuelve exactamente los mismos cubos que la faceta de la búsqueda»*. Hoy es trivialmente cierta —el adaptador compone al otro— y por eso cuesta nada dejarla escrita. **Es lo que se pone rojo el día que alguien desprenda el reparto y las dos ramas dejen de coincidir.** Sin ella, «un solo `width_bucket` en el repositorio» era una propiedad de cómo estaba escrito el código y no algo que se pudiera romper y notar. Esa es la entrega real de esta tarea: la extracción no se hace, pero deja de ser peligrosa.

  **`tests/integration/faceted-search.test.ts` y su `queries === 1` no se tocaron**, que es lo que la tarea exigía.


### La resolución de ubicación del fundador (2026-08-26) — «un dato, un lugar»

**Corrige lo que la 14.6 entregó, y por eso se anota como corrección y no como tarea nueva.** La 14.6 (PR #84) hizo `SearchCriteria.zoneIds` **plural** y combinado con O, y la salida de cero resultados `add-zone` suma una zona vecina *sin soltar la elegida* (`search-exits.ts`: *"Las zonas se combinan con O"*). La 14.36 dijo que la ubicación vive **sólo en la ruta**. **Las dos no podían valer**: una ruta tiene un solo segmento de zona. La 14.36 ya lo había marcado —*"ojo al mover esto: `buildSearchHref`, `SEARCH_QUERY_NAMES` y `search-exits.ts` razonan sobre la zona hoy"*— y lo dejó sin decidir.

**Lo decidido: una sola forma de URL por búsqueda.**

| Búsqueda | Dirección | Índice |
|---|---|---|
| una zona | `/alquiler/<ciudad>/<zona>` | indexada, canónica |
| varias zonas | `/alquiler/<ciudad>?zona=a,b` | **`noindex`** |
| la ciudad entera | `/alquiler/<ciudad>` | indexada, canónica |

O sea: **la ruta de zona rechaza el parámetro `zona`, y la de ciudad es la única que lo admite.** La ubicación nunca aparece dos veces adentro de una dirección — que es lo que la 14.36 protegía con «un dato, un lugar», **sin borrar una capacidad real**. La selección múltiple se conserva a propósito: «Chacao o Altamira» es una búsqueda que la gente hace y «todo Caracas» es demasiado ancho. Lo que se le quita es el índice, porque una combinación es **una pantalla de trabajo y no una de aterrizaje**: con cuatro zonas en Maracaibo son quince direcciones para contenido que ya vive en cinco páginas, Google lo lee como duplicado, y la búsqueda orgánica es el canal de adquisición de este producto (AGENTS.md §2).

- [x] **14.6 corregida.** `zoneIds` sigue plural y sigue combinándose con O — **no se revierte nada de la 14.6**. Lo que cambia es **dónde se puede pedir**: `src/modules/listing-search/domain/search-location.ts` (nuevo) contesta, para cada forma de ruta, qué zonas busca la dirección, con qué query se componen sus enlaces y qué hay que avisar. 12 casos en `search-location.test.ts`; el eje es *"la zona de la ruta es la única, aunque la query traiga otras"*.
- [x] **14.23b cerrada del lado que quedaba.** *"Un parámetro inválido se ignora con un aviso en vez de romper la página"*: un `?zona=` en la ruta de zona es exactamente esa clase, y no se aplica a medias. `resolveSearchLocation` **saca el parámetro de la query** —dejarlo lo arrastraría a la paginación, al filtro de la pastilla y al `callbackUrl` de «Entrar»— y devuelve el aviso. Probado en *"lo saca de la query, así ningún enlace de la página lo arrastra"*, *"lo dice en vez de aplicarlo a medias: el parámetro se ignora CON aviso"* y *"un «zona» vacío no es un parámetro puesto: se saca igual, pero no avisa"*. La otra mitad —un `?filtros=` que nombra un grupo que ya no existe— la cubre `STALE_FILTER_GROUP_NOTICE` en `search-accordion.ts`: el panel abre igual y lo explica, porque `?filtros=zona` es un enlace que era válido cuando alguien lo compartió.
- [x] **14.14 sigue siendo verdad, y su prueba no se tocó.** La salida `add-zone` **sigue sumando** y sigue diciendo cuántos: *"sumar la zona vecina promete los que hay MÁS los que suma"* pasa sin un solo cambio, porque afirma la etiqueta y la etiqueta no cambió. Lo que cambió es **adónde lleva**: desde `/alquiler/maracaibo/bella-vista` la salida ahora es `/alquiler/maracaibo?zona=bella-vista,la-lago`, o sea **cambia de forma de ruta**. Eso **no lo ataba ninguna prueba** —el `href` no se afirmaba en ninguna parte—, así que se agregó una al lado: *"y esa suma cambia de forma de ruta: de la zona a la ciudad con la lista"*.
- [x] **14.36 completada del lado del código.** El texto del fundador dice que la 14.36 *"ya sacó ciudad y zona del panel"*; **verificado, y era falso**: `SEARCH_STEPS` seguía siendo `["ciudad","zona","precio","habitaciones"]` y `SearchPanel.tsx` seguía dibujando `CityStep` y `ZoneStep`. La 14.36 estaba marcada porque **la decisión** estaba tomada, no porque el código la reflejara. Ahora sí: los cuatro grupos son precio, habitaciones, quién publica y atributos, y `SearchPanel.test.tsx` lo ata por el negativo — *"la ubicación no está en ninguna parte del panel (14.36)"* comprueba que no aparezcan «¿En qué ciudad?», «¿Qué zonas?», «Maracaibo» ni «Buscar una zona».

**Lo que ya estaba construido y el plan daba por hacer** (AGENTS.md §5, verificado archivo por archivo):

- **`zoneHref` ya implementaba la tabla entera.** Una zona → su ruta canónica; dos o más → la ciudad con `?zona=a,b`. Está en `search-panel.ts` desde la 14.6, con la razón escrita al lado. La resolución no lo cambia: lo confirma.
- **El `noindex` de la forma combinada ya estaba.** `zona` está en `FILTER_KEYS` (`listing-discovery/domain/zone-route.ts`) desde la 14.6, así que `/alquiler/<ciudad>?zona=…` ya respondía `robots: { index: false, follow: true }`. Lo que se agregó es la tabla del fundador como prueba con nombre sobre esa función, para que sacar `zona` de la lista no pueda pasar en silencio.
- **`zone_aliases` y `suggestFilters` ya resuelven el topónimo compuesto.** `schema.ts` lo dice: sin los alias, *"Bella Vista encuentra dos de sus siete apariciones reales"*. Y `suggestFilters` ya emite sugerencias `city` y `zone` con su alcance. Nada de eso hacía falta construir.

**Desvío de forma, y es del texto de la resolución.** La resolución escribe `?zona=a&zona=b` (parámetro repetido); el código usa **`?zona=a,b`** (lista separada por comas), que es lo que `readZoneList` lee, lo que `zoneParam` escribe y lo que la F12 fijó — *"`?zona=chacao,altamira` y no dos hashes de treinta y seis caracteres"*. Se conservó la coma: la diferencia no es de producto, y cambiarla tocaría `buildSearchHref`, `resolveZoneTokens` y el tipo `searchParams` de las dos páginas, que colapsa los repetidos a uno solo. **Requerida la confirmación del fundador**, aunque lo que la tabla decide —cuál forma de ruta y cuál se indexa— vale igual con las dos grafías.

- [x] 14.54 **El encabezado queda con UNA sola forma, y la vuelta se va adentro del contenido.** (nueva — decisión del fundador el 2026-09-03, tomada sobre las láminas y no sobre la 14.43.) La 14.43 buscaba 11 px para que «Inmobiliaria» entrara en una barra de cuatro hijos. La decisión los borra: **la barra deja de tener cuatro hijos.**

  **Lo que la decisión fija, en las dos anchuras:**

  ```
  móvil 360      [ ¿En qué zona buscás?  🔍 ]  Entrar   /   (MF)
  escritorio     rentas.  [ pastilla 420 ]  Publicar gratis  Entrar
                 rentas.  [ pastilla 420 ]  Publicar  Mis avisos (MF)
  ```

  **La placa del publicador sale del encabezado, y el dato NO se pierde.** Lo destapó el fundador preguntando si la ficha ya lo decía adentro: **sí lo dice**, `components/molecules/ContactBlock.tsx:149` dibuja «publica como dueño» / «publica como inmobiliaria» al lado del nombre y del teléfono, construido y sirviéndose hoy. **La lámina móvil dibuja las DOS** —placa arriba, palabras abajo— así que la repetición era del diseño y no un descuido; lo que la decisión elige es quedarse con la de abajo, que es la que el inquilino lee justo antes de escribir. Lo que se pierde es verlo sin bajar; en la cuadrícula esa señal se queda (14.53 la subió a la portada), así que sólo cambia dentro de la ficha. **Esto revierte lo que el PR #218 construyó anteayer**: `NavProps` pierde su tercera forma `NavWithListing`, y con ella se van las dos reglas de hoja que apagaban una placa a cada ancho y la medición que las contaba.

  **La vuelta se va del encabezado y entra al contenido, y arregla una torcedura que ya existía.** Verificado: `/alquiler/[ciudad]` y `/alquiler/[ciudad]/[zona]` dibujan `<nav aria-label="Miga de pan">` dentro del contenido; **la ficha es la única del camino de lectura donde la vuelta vive en la barra**. Y hay un dato que lo empuja más fuerte: ese enlace **no siempre dice «← Resultados»**. `resultsLink` (`src/modules/listing-discovery/domain/return-to-results.ts:145`) es una regla de dominio — con origen dice «← Resultados», sin origen dice «Ver avisos en Chacao», que es una etiqueta de veinte caracteres. Eso no entra en una barra de 60 px y sí entra en una fila de miga de pan. **La regla no se mueve**: sigue en el dominio, se mueve el marcado.

  **`/reportar` se lleva la vuelta adentro también** (decisión del fundador en la misma conversación). Son las dos únicas pantallas que le pasan `back` al `Nav` —`app/alquiler/[ciudad]/[zona]/[slug]/page.tsx:302` y `.../reportar/page.tsx:86`—, así que con las dos adentro **`NavProps` queda con una sola forma** y se borran `NavWithReturn` y `NavWithListing`. Si sólo se moviera la ficha, `NavWithReturn` sobreviviría para una pantalla y el encabezado no quedaría uniforme, que es justo lo que la decisión compra.

  **Presupuesto y rebanadas** (el techo de 400 líneas revisables de AGENTS.md §6 no admite esto en un PR): (a) la vuelta sale del encabezado y entra a la ficha y a reportar, `NavProps` colapsa a una forma; (b) el logo en móvil, que es la 14.55; (c) `hasListings`, que es la 14.56.

  **HECHA el 2026-09-03.** `NavProps` pasó de tres formas a **una interfaz**: se borraron `NavWithSearch`, `NavWithReturn`, `NavWithListing`, `NavBackAction` y la unión, y quedaron `account`, `publish`, `signInHref` y `pill` opcional. Con ellas se fueron `.back`, `.brandCentre` y `.publisher` de `Nav.module.css` —83 líneas de hoja— y la segunda copia de la marca que el marcado dibujaba en el slot del medio. Lo ata `components/organisms/Nav.test.tsx` → *"Nav — una sola forma (14.54)"*, con dos `@ts-expect-error` que ponen `pnpm typecheck` en rojo el día que la unión vuelva, y con el conteo de `>rentas.<` en 1 con pastilla y sin ella.

  **La vuelta entró al contenido en las dos pantallas.** La ficha y `/reportar` dibujan su enlace dentro del `<main>`, arriba del contenido, con los mismos tokens que la miga de pan de la zona (`--meta`, `--meta-fs-sm`, `--accent`, piso táctil `--target-min`). Medido sobre los bytes servidos y no sobre el fuente, que es lo único que distingue un enlace del `<header>` de uno del `<main>`: `ficha-servida.test.tsx` → *"la vuelta vive dentro del contenido, no en la barra (14.54)"* y `reportar.test.ts` → *"la vuelta al aviso vive dentro del contenido (14.54)"*. `resultsLink` no se movió ni se duplicó: el destino y la etiqueta siguen saliendo del dominio, y la prueba comprueba **las dos respuestas** — «← Resultados» con origen y «Ver avisos en Tierra Negra» sin él.

  **DECISIÓN, y va escrita en el código: es la fila de vuelta y NO la miga completa.** El fundador dejó la elección abierta. Se eligió la fila porque una miga dice **jerarquía** —cada paso es un ancestro— y el destino de acá, con origen, es *la búsqueda de la que se vino* (`/alquiler/maracaibo?min=200&hab=2`), que no es ancestro de nada: ponerlo como último paso diría algo falso justo en el caso que la 16.9 existe para distinguir. Y la miga completa tendría que componer las rutas de ciudad y zona en la página, que es la clase de regla que AGENTS.md §1 saca del frente, duplicando además la ruta de zona que `resultsLink` ya devuelve como respaldo.

  **Lo del PR #218 se revirtió entero**: la tercera forma `NavWithListing`, las dos reglas de hoja que apagaban una placa a cada ancho (`Nav.module.css` y `.summaryPublisher` de `ficha.module.css`), el envoltorio del marcado, las dos barras del arnés de `/measure` y las 258 líneas de conteo que la 14.43 había dejado en `tests/measure/ficha.spec.ts`. El átomo `PublisherBadge` no se tocó —lo siguen usando `ListingCard` y `ResultRow`— y la placa volvió a encabezar la columna de datos, sin envoltorio.

  **HALLAZGO, y es de los caros: la premisa de esta decisión no tenía UNA sola prueba.** La 14.54 saca la placa del encabezado porque «la ficha ya lo dice adentro», y eso es `ContactBlock.tsx:149`. Medido con una mutación antes de escribir nada: **borrar ese renglón dejaba los gates enteros en verde, cero rojos**. O sea que el argumento que sostiene la decisión se apoyaba en una línea que cualquiera podía borrar sin que nada se enterara. Se ancló acá mismo (`ContactBlock.test.tsx` → *"quién publica se dice con palabras, no sólo con un tono (14.54)"*), con las dos palabras porque el ternario tiene dos ramas y la mitad de los avisos son de inmobiliaria; la mutación ahora da 1 / 0.

  **HALLAZGO no resuelto — la placa de la columna de datos de la ficha sigue sin quien la mire.** Borrar `<PublisherBadge publisherType={detail.publisherType} />` de `page.tsx` da **0 rojos en unidad y 0 en tipos**, con las pruebas nuevas y sin ellas. No es de esta tarea —es anterior, y la 14.43 ya lo había anotado al pasar («`ficha.test.ts` ordena `gallery → summary → body → contact` y tampoco la ancla»)—, pero ahora importa más: es el único sitio donde la placa se dibuja en la ficha. Queda como **14.57**.

  **CONSECUENCIA para la 14.55, y contradice lo que esa tarea supone.** Su nota dice *«La miga de pan de la ficha arranca en «Inicio», así que probablemente ya esté cubierto»*: **no arranca en «Inicio»**, porque no hay miga completa. Hoy el camino a `/` desde la ficha en móvil es **la marca del encabezado**, que la 14.54 puso ahí (antes, con `back` en el primer slot, a 360 no se dibujaba ninguna) — atado en `tests/measure/layout.spec.ts` → *"14.54: a 360 la ficha dibuja la marca, porque la vuelta ya no le toma el lugar"*. O sea que esconder el logo en móvil deja la ficha **sin camino a `/`**, y esa prueba se pondrá roja el día que la 14.55 lo intente, diciéndolo.

  **Diez mutaciones, cada una con el texto objetivo presente exactamente una vez, sha256 antes ≠ después, restauración por copia de bytes con sha256 idéntica y `git status` limpio al final; cada una corrida DOS veces —con las pruebas nuevas en el árbol y con ellas aparcadas fuera, con la suite acotada a los archivos afectados**: la barra dibuja la marca dos veces (1 unidad / 0), `NavProps` vuelve a admitir la vuelta (1 tipos / 0), vuelve a admitir el publicador (1 tipos / 0), la hoja conserva la regla huérfana `.back` (1 unidad / 0), el destino de la vuelta deja de salir del dominio (2 unidad / 1), la ficha vuelve a pasarle la vuelta al encabezado (1 unidad + 1 tipos / 0 + 1 tipos), la ficha deja de dibujar la vuelta adentro (3 unidad / 1), `/reportar` deja de dibujarla adentro (1 unidad / 0), la ficha deja de dibujar la placa (0 / 0, el hallazgo de arriba) y `ContactBlock` deja de decir quién publica (1 / 0, después de anclarla; **0 / 0 antes**). Las dos columnas «sin» que no dan cero son `volver-a-resultados.test.ts`, que ya exigía `back.href` y `back.label` en el fuente, y el tipo, que no es una prueba.

  **Costo en el paquete**: `pnpm budget:bundle` antes → después, la ficha **109.31 → 109.26 KB** gzip, `/reportar` 107.74 → 107.66 y `/` 108.13 → 108.09. Bajan las trece rutas; la peor sigue siendo `/importar`, 110.60 → **110.51** contra un tope de 130.

  **Evidencia.** `typecheck`, `lint`, `lint:tokens` en verde; `pnpm test` 218 archivos / **2625** pruebas; `build` en verde; `test:measure` **69 pasadas** (la primera corrida después de `build` falló por una carrera del servidor de Playwright y no por el código — la repetición dio 69/69). `test:integration` y `test:e2e` saltados a propósito: no se tocó ninguna consulta ni el camino sin JavaScript, y los dos enlaces mudados siguen siendo `<a href>` reales.

- [x] 14.55 **CERRADA SIN CONSTRUIRSE el 2026-09-03: el fundador decidió dejar el logo.** El logo se queda en móvil. La tarea se anota igual, entera, porque lo que la cerró no fue arrepentimiento sino una medición que la contradijo, y eso es lo que hay que poder releer.

  **La tarea era**: esconder `rentas.` en móvil para que la pastilla ganara ~70 px. Desvío de lámina decidido —la vigente (`Rentas - Cuenta e Importar.dc.html`, 25 ago) sí lo dibuja en los dos estados de 360— y por eso se anotaba con la forma de la 20.4 y la 14.36.

  **Lo que la cerró, y es un error mío escrito acá para que no se repita.** Esta tarea decía que «la miga de pan de la ficha arranca en Inicio, así que probablemente ya esté cubierto». **Es falso: la ficha no tiene miga completa** — la tienen `/alquiler/[ciudad]` y `/alquiler/[ciudad]/[zona]`, y la ficha no. Lo supuse en vez de mirarlo; quedó marcado como «se afirma midiendo, no suponiendo» y midiendo salió que no. La consecuencia es concreta: con la vuelta ya mudada al contenido por la 14.54, **la marca del encabezado es el ÚNICO camino a `/` desde la ficha en móvil**, y esconderla deja la pantalla más visitada sin salida al inicio.

  **La razón de producto, además de la técnica**: los ~70 px que se ganaban se los lleva la pastilla de búsqueda, que en la ficha es justo donde menos importa — quien está leyendo un aviso ya encontró lo que buscaba. Se paga un ancho de búsqueda más chico en la pantalla donde nadie busca, a cambio de perder la única salida al inicio.

  **Si alguna vez se reabre**, lo que hay que resolver primero es de dónde sale el camino a `/` en la ficha en móvil, y no cuántos píxeles gana la pastilla.

- [x] 14.56 **«Mis avisos» sólo se dice si de verdad hay avisos; si no, sólo el ícono del perfil.** (nueva — regla del fundador el 2026-09-03, con la salida 1 elegida por él después de ver el costo.) Prometerle «Mis avisos» a quien acaba de crear la cuenta lo manda a una página vacía, y ésa es la casilla que miente de §5.

  **Va al dominio y no al componente**, con la forma que `canImportListings` ya dejó: `NavAccountAuthenticated` gana `hasListings: boolean`, resuelto afuera y sólo leído por la barra. Un `if` sobre datos en el componente sería una regla de negocio en el frente, que es la regla permanente del fundador.

  **Sólo tiene efecto en escritorio.** En móvil la lámina ya es **sólo el círculo de 44 px** con `aria-label="Mis avisos"`, sin palabras; las palabras existen únicamente a 1280. **Y no se resuelve con CSS**: esto no es «dónde cabe», que es geometría — es una afirmación sobre datos, y una hoja de estilos no puede saber si alguien publicó.

  **El costo, medido antes de elegir, y no es la base sino el viaje.** `bulkImportEnabled` es **una columna de `user`** (`src/shared/db/schema.ts:82`) y viaja con la fila. `hasListings` no existe como columna: hay que preguntarle a `listing` por publicador. Y **la mayoría de las pantallas hoy no consultan nada** — `app/page.tsx:190` llama `resolveNavAccount(session)` sin banderas, con la sesión saliendo de la cookie y cero viajes. O sea que esto agrega **un viaje donde hoy hay ninguno** en `/`, resultados y ficha, y este proyecto midió 6→5 viajes como una victoria. `/mis-avisos` y `/editar` ya pagan una consulta de usuario (`DrizzleBulkImportAccounts.findAccount`), así que ahí el `EXISTS` se pliega en la misma sentencia y no cuesta nada.

  **Las tres salidas, con lo que cada una contradice** (misma forma que la 14.53 y la 14.43): (1) **`EXISTS` por carga** — nunca miente, +1 viaje para quien tiene sesión; (2) **en el token de sesión** — cero viajes, pero se queda viejo: quien publica su primer aviso sigue viendo sólo el ícono hasta que el token se renueve; (3) **no validar** — cero costo y la promesa falsa se queda.

  **El fundador eligió la 1 el 2026-09-03**, con la razón dicha: el viaje extra lo paga sólo quien tiene sesión, y en la ficha y en `/` ésa es la minoría porque el inquilino navega anónimo; la 2 cambia una mentira chica por una intermitente, que es peor de depurar. **Queda pedido explícitamente dejar el número medido**, antes y después, y no declararlo.

  **Es `EXISTS` y no `COUNT`**: la pregunta es si hay al menos uno, y contar todos para comparar contra cero paga filas que nadie mira. El puerto va **al lado** del que ya existe (`PublisherListingsPort` en `listing-publication` lista, no cuenta) y no se ensancha, que es AGENTS.md §3.

  **Hecho 2026-09-03, con el número medido y no declarado.**

  **En el dominio, con la forma que `canImportListings` dejó.** `src/modules/identity/domain/nav-account.ts`: `NavAccountFlags` gana `hasListings`, `NavAccountAuthenticated` lo expone y `resolveNavAccount` lo copia con `?? false`. Dos casos nuevos en `nav-account.test.ts` — «sin banderas, no se afirma que haya avisos» y «con la cartera consultada, `hasListings` viaja tal cual llegó».

  **Las dos banderas quedaron OPCIONALES, y es una corrección con razón** (AGENTS.md §5). El texto de la tarea sólo pedía sumar un campo; `bulkImportEnabled` pasó de requerido a opcional porque **ninguna pantalla paga las dos consultas**: `/` y la ficha ahora consultan la cartera y NO la columna del importador, y obligarlas a escribir `bulkImportEnabled: false` sería hacerles afirmar algo que no consultaron. Ausente se resuelve a `false`, o sea al estado que no promete nada (AGENTS.md §7).

  **El puerto nuevo, al lado y no adentro.** `PublisherHasListingsPort.hasAnyListing(publisherId)` (`listing-publication/application/ports/publisher-has-listings.port.ts`) con `DrizzlePublisherHasListings` y cinco casos de integración contra Postgres real (`tests/integration/publisher-has-listings.test.ts`), incluido «un borrador sin fotos cuenta igual que un aviso activo» — la cartera importada nace en `draft` (9.15) y es la cuenta que MÁS necesita el enlace.

  **Desvío anotado: se emite `select 1 … limit 1`, no `select exists (…)`.** Es el plan al que Postgres compila `EXISTS` —un `Limit`, sin nodo `Aggregate`— y evita `db.execute`, cuyo envoltorio difiere entre el driver de Neon (producción) y `pg` (la prueba), que es justo la diferencia que la prueba no podría atrapar. Lo afirma «le pide a Postgres UNA fila como mucho, nunca un conteo», midiendo el SQL que el adaptador MANDA por el registro de Drizzle y no una sentencia reescrita a mano.

  **La barra lo lee, no lo decide.** `AccountMenu` gana `triggerLabelVisible` y un `aria-label={triggerLabel}` que **siempre** viaja: sin palabras visibles el control se sigue anunciando «Mis avisos» y sigue siendo el mismo `<a href="/mis-avisos">`. `Nav.tsx` pasa `account.hasListings` tal cual — no hay un `if` sobre datos en el componente.

  **DISCREPANCIA ENCONTRADA MIDIENDO, no supuesta.** La tarea decía «sólo tiene efecto en escritorio, porque en móvil la lámina ya es sólo el círculo». **La lámina sí; el código no**: `.triggerLabel` no tiene ningún `@media` en `AccountMenu.module.css`, así que hoy las palabras se dibujan también a 360. O sea que este cambio tiene efecto en los dos anchos. **No se resolvió acá** —esconderlas en móvil es geometría y merece su propia casilla— y queda anotado en vez de cerrarse en silencio.

  **Los viajes, medidos con el arnés de la 11.22 —cada `POST /sql` es un viaje— contra la aplicación compilada y la semilla de e2e:**

  | Pantalla | Antes | Después |
  |---|---|---|
  | `/` sin sesión | **4** | **4** |
  | `/` con sesión | **5** | **6** |
  | ficha sin sesión | **2** | **2** |
  | ficha con sesión | **4** | **5** |
  | `/alquiler/[ciudad]` sin sesión | **5** | **5** |
  | `/alquiler/[ciudad]` con sesión | **6** | **7** |
  | `/mis-avisos` | **4** | **4** |
  | `/mis-avisos/[id]/editar` | **9** | **9** |
  | `/importar` | **2** | **3** |

  Es exactamente lo que el fundador aceptó: **+1 sólo con sesión, 0 sin ella**, y el visitante anónimo —casi todo el tráfico de `/`, resultados y ficha— no paga nada. **Y las dos pantallas que ya consultaban no pagan nada tampoco**, aunque no por donde la tarea suponía: en `/mis-avisos` la respuesta ya está en memoria (`board.total > 0`, y ese total cuenta el tablero entero, nunca lo filtrado), y en `/editar` haber pasado el `notFound()` de `loadListingForEdit` **es** la respuesta del `EXISTS` — con la fila delante. En ninguna de las dos hizo falta plegar nada dentro de `findAccount`, que habría sido ensanchar un puerto de otro módulo para que leyera `listing`.

  **Mutaciones: 8, cada una dos veces.** Con las pruebas nuevas en el árbol las 8 ponen roja la prueba que les corresponde; con esas pruebas aparcadas fuera, **7 de 8 pasan en verde** — o sea que lo que protege esta conducta son las pruebas nuevas y nada más. La excepción es M1 (`hasListings: true` siempre), que también rompe la aserción de forma que ya existía en `nav-account.test.ts`.

## Phase 15: Entrar — the two doors, and the magic link (founder, 2026-08-21)

**The single most expensive item in the founder's flows document, and it is not a screen.** Login by email link is infrastructure this project deliberately does not have, and the schema says so out loud: the note above `users` records that no `verificationToken` table exists *because* "this app has exactly one provider (Google OAuth) — no magic-link email provider". That justification expires the moment 15.1 lands, and the comment has to be corrected rather than left contradicting the schema beside it.

**There is no email sender in this repository at all** — not Resend, not nodemailer, nothing. `design.md` names Resend as the plan; nothing is installed. Part of that work is not the assistant's to do: sending from `rentas.com.ve` without landing in spam needs SPF and DKIM records in the domain's DNS, which is the founder's.

**Deliverability is a product risk here, not an ops detail.** The founder's own flow calls step 11 "el punto de fuga principal" — the only moment a tenant is asked for anything. A magic link that lands in the spam folder does not cost one visit; that person can never sign in at all. Google's one tap has no equivalent failure mode, which is why F16 goes first and above, and why F18's waiting screen with a live countdown is load-bearing rather than decorative.

### 15a. Infrastructure

- [x] 15.1 `verificationToken` table + migration, and **correct the now-false comment** in `src/shared/db/schema.ts` that explains its absence. **Hecho.** `verificationTokens` table (composite PK `(identifier, token)`, mirrors `@auth/drizzle-adapter`'s own default shape) + `drizzle/0015_melted_stellaris.sql`; the stale comment above `users`/`sessions` now says why the table exists instead of why it didn't
- [x] 15.2 An email sender behind a port, so the domain never learns who delivers mail. **Hecho.** Decision recorded in `mailer.port.ts`: `identity` gets its OWN `MailerPort`/`ResendMailer` (`resend-mailer.ts`), not `listing-lifecycle`'s `LifecycleMailerPort` — narrow ports per module, same idiom AGENTS.md already states for read/write ports. New env var `AUTH_MAIL_FROM` (documented in `.env.example`), reusing `RESEND_API_KEY`. Founder-owned SPF/DKIM prerequisite confirmed satisfied (verified domain in Resend, real send landed in Gmail Primary). Fails closed via `readAuthMailerConfig` — proven in `resend-mailer.test.ts`
- [x] 15.3 Auth.js email provider wired with `maxAge` set to **15 minutes** (F17). **Hecho.** `MAGIC_LINK_MAX_AGE_SECONDS` (`domain/magic-link.ts`) wired in `email-provider.ts`'s `buildEmailProvider`, assembled into `auth.ts`. `email-provider.test.ts`'s `maxAge` describe block fails if the pin is removed (mutation-checked)

### 15b. The rules of the link

- [x] 15.4 Single use (F17). Auth.js does this; assert it rather than assume it. **Hecho**, against real Postgres: `tests/integration/verification-token.test.ts` drives the exact same `@auth/drizzle-adapter` `createVerificationToken`/`useVerificationToken` contract `@auth/core`'s callback handler calls (verified by reading its source), proving a second use finds nothing
- [x] 15.5 **Fifteen minutes** (F17). **Hecho.** Boundary mirrored in `isVerificationLinkExpired` (`domain/magic-link.ts`, pure, boundary-tested like `expiry.ts`'s `isExpired`) and proven again against a real persisted `expires` row in `tests/integration/verification-token.test.ts` — which is also where a `timestamp`-without-timezone round-trip timezone hazard in `node-postgres` was found and worked around (see that file's comment)
- [ ] 15.6 ~~Must open on the same device~~ — **REMOVED by the founder, 2026-08-21, and the reasoning is recorded so nobody restores it as a hardening.** The rule was in F17 and it contradicted the founder's own "Decisión pendiente" two sections later: on a desktop the mail is read on the phone, so the link opens on another device **in the normal case, not an edge case**. Enforced as written, signing in by email from a computer could never succeed. What the rule buys is protection against a leaked link — forwarded mail, a shared inbox, a mail scanner that pre-fetches URLs; what it guards is access to a phone number behind a free account. Single use plus fifteen minutes covers the real exposure. It is also custom work: Auth.js is not same-device by default, so keeping it meant building a mechanism to buy a problem

### 15c. The two doors, same mechanism

- [x] 15.7 **Publicar → a page with its own URL** (both viewports). It has to be a return destination from Google and from a mail client, and a sheet has no URL. **Hecha, y sin una línea de cliente.** `/signin` era un marcador de posición de 41 líneas **en inglés y con Google solo**, y su copia era falsa de dos maneras comprobadas: decía *«There is no other way to sign in»* cuando la puerta del enlace por correo ya está montada en `auth.ts` (`buildEmailProvider()`, 15.1–15.5), y estaba en inglés cuando todo lo que ve un usuario en este producto está en español. **La regla**: `signInPageFor` (`src/modules/identity/domain/sign-in-page.ts`), el mismo molde que `contactDoorFor` — la misma ruta atiende cuatro destinos, y prometerle los pasos de publicar a quien viene de `/mis-avisos` es otra vez copia falsa. **No se agregó una segunda lista de destinos legales**: `signInDoorOf` sale de `safe-return-destination.ts` y comparte `ACCOUNT_DOORS` con `safeSignInReturn`, que es lo que la 15.10 dejó montado en `callbacks.redirect`. **Pruebas nombradas**: la regla en `sign-in-page.test.ts` → *«la puerta de publicar dice para qué es y trae los tres pasos de la lámina»*, *«sobre un aviso dice la misma razón que la hoja de la ficha, palabra por palabra»* (pinea por valor la frase de `contactDoorFor` para que las dos formas de una sola puerta no se separen, sin acoplar los dos módulos) y *«descarta … y entra a la cuenta sin destino»*; **la instalación**, que es la trampa 4, en `app/(auth)/signin/entrar-servida.test.tsx` → *«por la puerta de un aviso cambia la copia y la salida vuelve a ese aviso»* y *«un destino que la regla no admite no aparece en los bytes servidos»*, sobre `renderToStaticMarkup` de la ruta. **Los dos tamaños son medidos y no declarados** (`tests/measure/entrar.spec.ts`, contra `/signin` de verdad y no contra el arnés): a 1280 la columna pinta **420 px en x=90 y los pasos arrancan en x=590 a la misma altura** —al costado, que es la nota de la 9a—; a 360 pinta **328 de ancho** y los pasos bajan a y=398,75 debajo de una columna de 277,75 de alto. **Mutaciones, todas restauradas y verificadas por sha256**: que la puerta del aviso deje de decir la frase de la hoja pone roja 1 de 16; que el destino no pase por `safeSignInReturn`, 7; que la salida ignore la del dominio, 1; que la puerta de cuenta herede los pasos de publicar, 1; que la pantalla deje de leer el destino, 2 de las 3 de bytes servidos; que la salida se escriba a mano, 1; que los pasos no se dibujen, 1; apuntar la columna al 460 del diálogo vecino o quitar la cuadrícula de escritorio, la medición de 1280. **Correcciones registradas**: (a) **el campo de correo y su botón «Enviarme el enlace» NO se dibujaron**, y es la mitad de la lámina 8a/9a que falta — desemboca en la pantalla de espera de la 15.9, que no existe, y un botón que no lleva a ninguna parte es peor que no dibujarlo; queda con número propio en la 22.22; (b) **sin la marca de Google y en el nivel 1**, igual que la hoja de la 15.8 — ver 22.20; (c) la línea legal va **sin enlaces** porque `/terminos` y `/privacidad` no existen (22.24); (d) el `<h1>` usa `--title-fs` y no el 22/28 de estas dos láminas, y la barra usa `--nav-h`/`--nav-h-desktop` y no el 56/72 que dibujan (22.25); (e) el enlace de vuelta va en `--ink` como el «← Resultados» de la ficha, no en `--accent`. **El «funciona sin JavaScript» se mide y no se afirma**: `tests/e2e/entrar-sin-javascript.spec.ts`, que el proyecto `crawlability` corre con el script apagado contra la compilación de producción — `renderToStaticMarkup` no sirve para eso acá, porque fuera del compilador de Next React inyecta su propio `<script>` de reenvío de formularios (ver 22.23). **No cierra la 15.11**: sigue faltando una de las tres puertas
- [x] 15.8 **Ver WhatsApp → a sheet on mobile, a 460 px dialog on desktop.** The tenant must not be taken out of the listing they are reading. **Hecha, y sin una línea de cliente.** La puerta es **un estado de la ficha que decide la dirección** (`?entrar=si`), el mismo mecanismo que el panel de filtros (`resolveFilterPanel`/`PANEL_OPEN_TOKEN`) y por lo que `AGENTS.md` §2 obliga: una hoja que sólo existiera adentro de un componente de cliente deja afuera a quien no recibió el paquete, y el navegador de WhatsApp —de donde llegan estos enlaces— es justo donde no llega. **La regla**: `contactDoorFor` y `doorHrefFor` (`src/modules/contact-reveal/domain/sign-in-door.ts`), que responden juntas «hay puerta» y «qué dice» — el token lo escribe cualquiera en la barra, así que sobre un contacto ya revelado o un aviso vencido devuelve `null` en vez de un muro delante de algo abierto. **El panel**: `components/organisms/SignInDoor.tsx` + su hoja, una sola pieza con un punto de quiebre: en el servidor no se sabe el ancho. **El cable**: `revealListingContact` ya no manda a `/signin` — redirige a la misma ficha con la puerta abierta, y el destino lo juzga `safeReturnPath`, que es la regla que ya existía (no se agregó una segunda). **Pruebas nombradas**: la regla en `sign-in-door.test.ts` → *«está cerrada mientras el parámetro no traiga el token exacto»* y *«no se abre sobre un contacto que ya no tiene llave»*; **la instalación**, que es la trampa 4 de este archivo, en `ficha-servida.test.tsx` → *«con el token abierto sale entera en el HTML, sin un solo script»* y *«deja el aviso completo detrás, no una pantalla en su lugar»*, sobre `renderToStaticMarkup` de la ruta; el cable en `reveal-actions.test.ts` → *«abre la puerta sobre la misma ficha en vez de mandar a otra pantalla»*. **Los dos tamaños son medidos y no declarados** (`tests/measure/puerta.spec.ts`): a 1280 el panel pinta **460 px de ancho en x=410**, o sea centrado, y no toca el piso; a 360 pinta **360 de ancho, 383 de alto, con el borde inferior en 800 y el superior en 417** — queda pantalla arriba de la hoja, que es la 15.8 dicha en píxeles. **Mutaciones, todas restauradas y verificadas por sha256**: anular la comprobación de estado pone roja 1 de 5; aflojar la del token, 2; desmontar la puerta de la ficha, 3 de la prueba de bytes; mandar `stayHref` o `callbackUrl` al inicio, 1 cada una; apuntar `--door-w` al token vecino `--card-w-desktop`, la medición de escritorio; y cambiar `align-items: flex-end`, la de móvil. **Correcciones registradas**: (a) el prop de `ContactBlock` se llamaba `signInHref` y apuntaba a `/signin` —el nombre decía la verdad de antes—, ahora es `doorHref`; (b) la puerta **no repite el teléfono tapado** que la nota de la 9b pide, y eso se difiere a la 15.11 junto al resto de la copia de F20, porque la lámina lo dibuja con «teléfono verificado por WhatsApp» y `phone_verified_at` no existe (16.12): certificar lo que nadie comprobó es peor que callarse; (c) **sin la marca de Google** — ver 22.20
- [x] 15.9 F18's waiting screen: the typed address shown back (so a typo is caught without going back), why it might not arrive, resend **with a countdown rather than a dead button**, and an exit to Google so nobody is trapped waiting. **Hecha, y sin una línea de cliente — el sondeo NO está y es a propósito.** La pantalla vive en `/signin/revisa-tu-correo` (`app/(auth)/signin/revisa-tu-correo/page.tsx`), **colgada de la puerta y no como ruta de primer nivel**: la relación queda escrita en la dirección y la cookie se puede acotar a un solo prefijo. **La regla**: `magic-link-request.ts` (`resendStateFor`, `magicLinkRequestFor`, `magicLinkAddressOf`, `magicLinkTicketOf`, `magicLinkWaitFor`) — **la cara del control la calcula la MISMA función que autoriza el envío**, así que «Volver a enviar en 0:42» no puede mentir sobre lo que pasa si alguien arma el POST sin pantalla. **La pregunta de diseño, contestada**: *«reenviar con cuenta regresiva, no un botón muerto»* se resuelve **en el servidor**, que es quien sabe cuándo salió el enlace — sirve el control ya habilitado (un `<form>` de verdad) o ya negado con el número puesto, que es exactamente lo que la lámina dibuja (un `<span>`, no un ancla). Con el script apagado el número es una foto del instante en que se sirvió la página y no se mueve solo; **el tic es la 15.12 y deliberadamente no se pre-construyó**. Nadie queda encerrado: las dos salidas —«Mejor entro con Google» y «← Cambiar de correo»— son un formulario y un ancla reales. **La ventana es de 60 s y se cuenta por buzón, no por pestaña**: quien escribe `gmial.com` y se corrige al segundo tiene que poder mandar ya, y bloquearlo un minuto en «el punto de fuga principal» no ahorra un correo, cuesta la cuenta. **La dirección tecleada NO viaja en la barra**: va en una cookie `httpOnly` (`enlace.ts`) con alcance `/signin` y `maxAge` derivado de `MAGIC_LINK_MAX_AGE_SECONDS` — muere con el enlace. Sin comprobante la ruta **falla cerrado** y vuelve a `/signin`. Eso deja además el camino de la 15.14 abierto sin construirlo: el secreto que el sondeo va a necesitar cabe en ese mismo comprobante. **Pruebas nombradas**: la regla en `magic-link-request.test.ts` → *«dentro de la ventana dice cuánto falta, en la cara del botón»*, *«en el instante exacto del límite ya se puede»*, *«corregir un tipeo manda de inmediato: la ventana cuida un buzón, no una pestaña»*, *«un envío fechado en el futuro no autoriza nada, y no dibuja un número absurdo»* y *«un destino que la regla de vuelta no admite se cae del comprobante»*; **la instalación**, que es la trampa 4, en `app/(auth)/signin/revisa-tu-correo/espera-servida.test.tsx` → *«muestra de vuelta la dirección tecleada, que es como se caza el tipeo sin volver»*, *«dentro de la ventana el reenvío es la cuenta, y no un formulario»*, *«pasada la ventana el reenvío es un formulario de verdad, con su dirección»* y *«sin comprobante no hay pantalla: vuelve a la puerta»*; **el cable** en `app/(auth)/signin/actions.test.ts` → *«repetir la misma dirección adentro de la ventana no gasta un correo»* y *«si el envío no llegó a hacerse, no se promete un enlace que no salió»*. **Los dos tamaños son medidos y no declarados** (`tests/measure/entrar.spec.ts`): a 1280 la columna pinta **520 px en x=380** —centrada dentro del contenedor de 1100— y las dos salidas quedan **lado a lado**, que es la nota de la 9c; a 360 pinta **328 de ancho** y las salidas se apilan. **Y el «anda sin JavaScript» se mide**: `tests/e2e/entrar-sin-javascript.spec.ts` → *«la espera del enlace se lee entera y sus dos salidas funcionan sin JavaScript»*, que el proyecto `crawlability` corre con el script apagado contra la compilación de producción. **Mutaciones, todas restauradas y verificadas por sha256**: que el reenvío ignore el comprobante pone roja la de `crawlability` y 4 de las unitarias; quitar el techo del reloj futuro, 1; quitar la comprobación de despacho, 1; quitar el `redirect` sin comprobante, 1; apuntar la columna al `--door-w` vecino (460) o quitar la cuadrícula de escritorio, la medición de 1280. **Correcciones registradas**: (a) **no se dibuja «Abrí el enlace en este mismo teléfono» (8c) ni «te avisamos acá cuando pase» (9c)** — la primera es la regla que la 15.6 quitó (ver 15.15) y la segunda la cumple el sondeo de la 15.12, que no existe: prometerlas sería la casilla que miente de §5; (b) el verbo de la línea principal no es ni «Tocalo» ni «Hacé clic», que son falsos cada uno en el otro ancho — ver 22.27; (c) el sobre `✉` sí se dibuja porque es un carácter de texto, y el disco de Google sigue sin dibujarse (22.20); (d) el `<h1>` usa `--title-fs` y no el 21/26 de estas dos láminas, misma decisión que la 15.7 (22.25)
- [x] 15.10 F19 — return to the exact screen and listing, never the home. This is criterion 11 and it is what makes the contact reveal worth attempting at all. **Hecho, y la mitad que faltaba no era la que la casilla sugería.** Las puertas ya emitían `callbackUrl` —verificadas las siete—, así que lo que quedaba era el otro extremo. **El destino SÍ sobrevive el viaje por el correo, y quedó establecido conduciendo la librería de verdad, no leyendo su fuente**: `magic-link-ida-y-vuelta.test.ts` → *«abierto en otro navegador, sin una sola cookie nuestra, vuelve a la ficha»* pide el enlace, lo saca del correo redactado y lo canjea con una petición desnuda — Auth.js mete el `callbackUrl` **adentro del enlace**, así que llega al teléfono donde se lee el correo pedido en la computadora, que es el caso normal de escritorio (lámina 9c). **Lo que faltaba era la regla**: `safeReturnPath` NO guardaba este camino, y ponerla tal cual habría roto las puertas de cuenta — su prefijo es `/alquiler/` y `require-session` manda `/publicar`, `/mis-avisos` e `/importar`. Corrección registrada: la regla nueva es `safeSignInReturn` (`domain/safe-return-destination.ts`), que **comparte el parseo** con las otras dos —una sola comprobación, tres listas de destino— y agrega la lista de puertas. Va en `callbacks.redirect` (`infrastructure/redirect-callback.ts`, montada en `auth.ts`) y **no en la pantalla**, porque ése es el único paso por donde cruzan las dos puertas y los dos momentos del enlace. **Es más estrecha que la de Auth.js a propósito, y el par lo demuestra**: *«sin la regla del producto, la librería conserva cualquier ruta interna»* contra *«con la regla, esa misma ruta no llega al correo»* — la librería sólo exige «mismo origen», y el inicio es del mismo origen, que es justo lo que la F19 prohíbe. **Mutaciones, todas restauradas y verificadas por sha256**: quitarle la barra al prefijo de puerta pone roja 1 de 44; anular la lista, 7; que el proveedor le recorte la consulta al enlace, las 4 del arnés; que el correo deje de llevar el enlace, las 4; y que `auth.ts` deje de instalar la regla, exactamente la prueba que afirma la instalación. **No cierra la puerta del correo de punta a punta**: `/signin` sigue sin campo de correo, así que hoy el enlace sólo se pide por HTTP — eso es la 15.7/15.9, y la regla ya está esperándolas
- [x] 15.11 F20 — every door shows a visible way out. The listing is public; only the phone number is behind the account, and the copy says so. **No se intentó en esta entrega, y por una razón de orden antes que de tamaño.** Dice **«cada puerta»**, y de las tres que las láminas dibujan sólo existe una: la hoja de móvil y el diálogo de 460px (15.8, artboards 8b/9b, donde viven *«Seguir mirando sin entrar»* y la × que *«cierra sin entrar: no es un muro»*) y la pantalla de espera (15.9, con su *«Mejor entro con Google»*) están sin construir. Cerrarla hoy marcaría una casilla que sólo una de tres puertas cumple, que es exactamente la mentira que §5 describe. La copia que le falta a la única puerta que existe es de producto —*«Pedimos la cuenta para frenar avisos falsos. Es gratis y es un clic»* junto al teléfono tapado y su *«verificado por WhatsApp»*—, así que va en un módulo de dominio con su prueba, no en la pantalla. **Queda como seguimiento inmediato de la 15.7/15.8/15.9** **Actualización 2026-08-29 (15.9): el bloqueo de orden se levantó, y la tarea sigue abierta por otra razón.** Las tres puertas existen y las tres tienen su salida visible medida —«← Volver a los avisos»/«← Volver al aviso» (15.7), la × y «Seguir mirando sin entrar» (15.8), «← Cambiar de correo» y «Mejor entro con Google» (15.9)—, así que **ya se puede construir**. Lo que la frena ahora es lo otro que la propia tarea nombra: el teléfono tapado con su *«verificado por WhatsApp»*, que certifica algo que `phone_verified_at` (16.12) no puede sostener. **Se deja para su propia entrega y no se pliega acá**, que es lo que el techo de revisión y §5 piden **Actualización 2026-08-29 (16.12/16.34): el dato existe, y la puerta sigue sin poder leerlo.** El instante llegó —`verified_contact.verified_at`, cableado a la ficha— y **qué va a llamar es `DrizzleContactVerificationEvidence.findEvidence({ userId: publisherId, contact })`**, la misma consulta que la 16.12 ya usa, con la frase saliendo de un dominio como salió `contactVerificationNotice`. Lo que la frena ahora es **estructural y es la garantía del estado bloqueado**: `findEvidence` pregunta por el TRIPLE, o sea necesita el VALOR del contacto, y el camino bloqueado no lo lee nunca — `ListingDetail` no trae `contactValue` a propósito, para que el número no salga de Postgres hacia el render de un visitante anónimo. Dibujar «teléfono verificado por WhatsApp» al lado del teléfono TAPADO exige una lectura nueva que conteste sí/no sin devolver el valor. Va con número propio en la **22.32** **Cierra el 2026-09-02, y las dos razones anteriores estaban vencidas — la segunda por medida, no por opinión.** **Las tres puertas existen y las tres dibujan su salida, verificado y no supuesto**: la de `/signin` sale por `DoorBar` con `wayOut` de `signInPageFor` —«← Volver al aviso» cuando se vino de un aviso, «← Volver a los avisos» si no, y no hay rama donde sea `null`—; la hoja/diálogo de la ficha sale por dos sitios a la vez, la × (`aria-label="Cerrar sin entrar"`, un ancla de `--target-min` con borde, no un glifo suelto) y «Seguir mirando sin entrar», las dos apuntando al `stayHref` sin el token; y la espera sale por «← Cambiar de correo» en la barra y «Mejor entro con Google», **ninguna de las dos colgada de `resend.allowed`**, así que la cuenta regresiva no se lleva una salida por delante. **Lo que de verdad faltaba no era la salida sino la otra mitad de la F20**, que dice *«entrar no es un muro: el contenido del aviso es público y solo el teléfono está detrás de la cuenta»* — y esa frase **existía, escrita a mano adentro de `ContactBlock.tsx`**: producto en una capa a la que el suelo del 90 % no llega, sin una sola prueba que la nombrara, y duplicando la afirmación que `contactDoorFor` ya decía desde el dominio. **La regla**: `lockedContactNotice` (`src/modules/contact-reveal/domain/sign-in-door.ts`), al lado de la puerta y no en un módulo nuevo, porque es la misma afirmación en la otra de sus dos formas dibujadas. **Se conservan las dos frases y no se unifican**: la hoja la dice entera y con punto (8b/9b), el bloque la encadena con dos puntos a qué falta para ver el número (Ficha 136/99); unificarlas sería inventar una tercera. Lo que no puede pasar es que se separen, y de eso se encarga la prueba que las pinea por valor, el mismo recurso que `sign-in-page.test.ts` ya usa entre módulos. **Pruebas nombradas**: la regla en `sign-in-door.test.ts` → *«dice qué falta, por qué, y que no cuesta nada»*, *«nombra el canal que el aviso realmente guarda, y no siempre WhatsApp»* y *«hace la misma afirmación que la hoja, palabra por palabra»*; **el dibujo**, que es donde no había nada, en `ContactBlock.test.tsx` → *«dice qué falta para ver el número y que la cuenta no cuesta nada»*. **Mutaciones, restauradas y verificadas por sha256**: quitarle el canal a la frase pone roja 1 de las nuevas; cambiar la afirmación de la hoja sin cambiar la del bloque, 1; que el componente vuelva a escribir la frase a mano, la de `ContactBlock`. **Corrección registrada, y es la que separa esta tarea de la 22.32**: *«teléfono verificado por WhatsApp»* **no es F20** — la F20 escrita dice literalmente *«Todas las puertas tienen salida visible. Entrar no es un muro: el contenido del aviso es público y solo el teléfono está detrás de la cuenta»*, y no menciona la verificación. Esa línea salió de las láminas 8b/9b, no del requisito, y sigue bloqueada por lo estructural que la 22.32 describe: `findEvidence` pregunta por el triple y el camino bloqueado no lee el valor a propósito. **Se cierra la F20 y se deja la 22.32 abierta con su razón**, en vez de dejar abierta la F20 por algo que la F20 no pide. **`ContactBlock` sigue siendo el único sitio del bloque bloqueado con `contact_value` ausente**: no se agregó ni un dato al camino anónimo

### 15d. Still open after 15.6

- [x] 15.12 **RESOLVED 2026-08-22 — the waiting tab polls, as a progressive enhancement.** The desktop design (`Rentas - Entrar - Desktop.dc.html`, screen 9c) commits to it in copy: "Si lo abris en el telefono, entras ahi y esta pestana sigue esperando: te avisamos aca cuando pase." The founder confirmed after review.
  >
  > **Why this does NOT break F14, correcting my own earlier reading.** I had argued it made the desktop the one exception to the no-JS read path. That was the wrong scope: F14 protects "buscar, filtrar, paginar y navegar" — the surfaces a crawler indexes and a cheap phone pays for. The "revisa tu correo" screen is neither. It is not indexed, not shared, and only reached by someone who just pressed a button on purpose.
  >
  > **Mechanism: polling, not a held connection.** The tab must learn about a session created in another browser on another device, so nothing same-browser (BroadcastChannel, storage events) can work — only the server can tell it. SSE/WebSocket is the elegant answer and the wrong one here: Vercel runs functions with execution limits, not long-lived connections, so it would mean a second service. A ~20-line inline script (no React, no bundle weight on the pages that matter) polling every few seconds, **stopping by itself at 15 minutes** because that is the link TTL.
  >
  > **Degrades cleanly:** with JavaScript off the screen still works — the resend countdown and the exit to Google are there, it simply does not update itself. Enhancement, never mechanism.
- [x] 15.14 **The poll endpoint must be keyed by a browser-held secret, not by the email address.** "Has maria.f@gmail.com signed in yet?" is a question anyone could ask about anyone, which turns the waiting screen into a way to probe when a given person is online. The cookie dropped when the link was requested is what the poll carries. **Hecha, y la premisa de la tarea era falsa por el lado bueno: no había fuga que cerrar porque no había sondeo.** `app/api/` no tenía ninguna ruta de estado y `rg` no encontraba un solo `fetch` de espera — la 15.9 dejó escrito que «el tic es la 15.12 y deliberadamente no se pre-construyó». Así que esto no corrigió un endpoint filtrando: **construyó el mecanismo que la 15.12 resolvió el 2026-08-22, con la llave puesta desde el primer minuto**, que es lo que esta casilla existe para exigir. **La cookie existía y NO traía lo que hacía falta**: `rentas_enlace` (15.9) es `httpOnly` con alcance `/signin`, pero su contenido es JSON sin firmar —el propio dominio ya lo decía, «una cookie la escribe cualquiera»—, así que un sondeo que leyera la dirección de adentro contestaría sobre la dirección que le pongan: la fuga entera, mudada de la barra a una cabecera. **Dos piezas, y ninguna sirve sola.** La HUELLA (`linkFingerprint`) es el sha256 del token que Auth.js acaba de escribir en `verificationToken`: la pregunta pasa a ser «¿sigue vivo MI enlace?» y no «¿entró esta persona?». El SELLO (`seal`, HMAC de `dirección|huella` con `AUTH_SECRET`, en `app/(auth)/signin/enlace.ts`) es lo que hace que la cookie sea un secreto y no un formulario: sin él, cambiarle la dirección a un comprobante legítimo es la misma pregunta ajena con un paso más. **Se guarda la huella y jamás el token**: un token fuera del adaptador es un enlace de entrada. **La lectura**: `PendingMagicLinkPort` + `DrizzlePendingMagicLinks`, un puerto de lectura AL LADO del `Adapter` de Auth.js y no adentro (AGENTS.md §3), que devuelve las huellas vivas de la más nueva a la más vieja — el orden es contrato, porque un reenvío deja dos enlaces y el comprobante tiene que apuntar al último. **La ruta**: `/signin/revisa-tu-correo/estado`, que **no recibe nada** —`GET()` sin parámetro, afirmado por la firma— y cuelga de la pantalla para que el alcance `/signin` de la cookie la cubra sin ensancharla. **El caso sin cookie, contestado**: la pantalla YA fallaba cerrado antes de esto (sin comprobante redirige a `/signin`), así que quien llega en otro navegador, de incógnito o con el almacenamiento limpio no ve una espera colgada: ve la puerta. Lo que puede faltar es la HUELLA —un comprobante viejo, o `AUTH_SECRET` ausente, o un fallo de base al leerla—, y ahí la ruta contesta **204 y la pantalla queda entera**: cuenta regresiva, reenvío y las dos salidas. **Se pierde el aviso, nunca la espera**, que es la única forma de fallar cerrado sin dejar una pantalla que no resuelve nunca. **Pruebas nombradas**: la regla en `magic-link-request.test.ts` → *«mientras su enlace siga entre los pendientes, todavía no entró»*, *«otro enlace del mismo buzón no es el suyo»* (el caso del reenvío), *«sin huella no hay respuesta que dar, y «no entró» sería inventarla»*, *«una huella sin sello no viaja, y la espera sigue en pie»* y *«pregunta mientras el enlace pueda abrirse, y ni un segundo más»*; **la ruta**, que es la trampa 4, en `estado/estado-servido.test.ts` → *«mientras su enlace sigue vivo, dice que todavía no»*, *«cuando el enlace se abrió, se lo dice»*, *«un comprobante armado sobre la dirección de otra persona no recibe nada»* y *«no acepta nada de quien pregunta: la firma no recibe petición»* — **las dos direcciones se afirman juntas a propósito**: sólo la negativa pasa con la ruta entera borrada, y sólo la positiva pasa con la comprobación del sello borrada, que es exactamente la fuga; **la señal** contra Postgres de verdad en `tests/integration/verification-token.test.ts` → *«canjear el enlace hace desaparecer su huella, que es la señal de que entró»*, *«devuelve la huella y nunca el token»*, *«las ordena de la más nueva a la más vieja, y deja fuera la vencida»* y *«no mezcla buzones»*; **la pantalla** en `espera-servida.test.tsx` → *«el aviso … va servido y oculto»* y *«el sondeo va en línea y no escribe la dirección en ninguna parte»*. **Mutaciones, doce, todas restauradas y verificadas por sha256, y cada corrida contra la suite entera dos veces —con las pruebas nuevas en el árbol y con ellas afuera—: las doce ponen rojo con ellas y NINGUNA sin ellas**, que es lo que dice que nada de esto estaba cubierto antes. Que la ruta deje de comprobar el sello pone rojas 2; que el sello deje de cubrir la dirección, 1 —las dos capas se rompen por separado, que es la trampa (b)—; que el sondeo ignore las huellas pendientes, 3; que conteste sin huella, 1; que no se apague con el enlace, 1; que la huella viaje sin sello, 1; que el aviso salga visible, 1; que el guion se lleve la dirección en la ruta, 1; que la firma acepte petición, 1; que el adaptador devuelva el token en vez de la huella, 2; que deje pasar los vencidos, 1; que pierda el orden, 1. **Correcciones registradas**: (a) **esta entrega trae también la mitad de navegador de la 15.12**, porque un endpoint que nadie llama no cierra nada — el guion en línea son veinte líneas sin React y la pantalla sigue completa con el script apagado, medido por `crawlability`; (b) por eso mismo la 9c ya se puede dibujar: `signedInNotice` sale SERVIDO y escondido y el guion sólo le quita el `hidden`, así que la copia no se muda al frente; (c) queda una exposición residual y se escribe en vez de callarse — quien pida un enlace HACIA el buzón de otra persona obtiene un comprobante legítimo, y lo que puede sondear con él es **si SU propio enlace se usó**, nunca si esa persona entró por otra puerta; cerrarlo del todo pediría atar el comprobante a la fila de Auth.js con estado propio, y el precio es una tabla para una pregunta que ya no filtra nada
- [x] 15.15 **Fix the mobile design copy.** `Rentas - Entrar - Mobile.dc.html` screen 8c still reads "Abri el enlace en este mismo telefono. Si lo abris en otro, te pedimos el correo de nuevo" — the same-device rule removed in 15.6. Mobile and desktop currently state opposite behaviours, and mobile states a rule that no longer exists. **Hecha, y lo primero que se comprobó es dónde estaba de verdad la mentira.** **En el producto NO está**: ni `app/`, ni `components/`, ni `src/` tienen una sola cadena que diga la regla, y la pantalla de espera la omite a propósito desde la 15.9 —con una prueba que lo exige, `«no promete la regla que se quitó ni el aviso que todavía nadie manda»`, que afirma `not.toContain("este mismo teléfono")`—. Así que la lámina no es el eco de una pantalla rota: **la pantalla ya estaba bien y la lámina se quedó atrás**. **Y la lámina tampoco era el único sitio, que es la corrección que esta tarea no nombraba**: `design/especificaciones/Rentas - Flujos y funcionalidades.md` seguía enunciando la regla como requisito VIVO de la F17 —*«Debe abrirse en el mismo dispositivo. Si se abre en otro, se pide el correo de nuevo»*—, y ése es el sitio del que la lámina se dibujó: la 15.6 dice textualmente «The rule was in F17». Corregir sólo el 8c dejaba la especificación lista para que alguien la vuelva a derivar. **Dos ediciones deliberadas, y las dos se dicen enteras.** (1) `Rentas - Entrar - Mobile.dc.html`, lámina 8c, línea 141: la frase pasa a **«Podés abrirlo en cualquier dispositivo: no hace falta que sea este teléfono.»** — un `<p>` con estilos en línea, sin `id` ni clase, así que es un reemplazo de una sola línea. **Se eligió una redacción cierta a los dos anchos y no la de la 9c**, aplicando la regla que la 22.26 dejó dicha: la 9c abre con «Abrí el enlace en esta misma computadora», que es la misma preferencia de dispositivo al revés, y cierra con «te avisamos acá cuando pase», que es promesa del sondeo y no de esta lámina. (2) `Flujos y funcionalidades.md` F17: la viñeta queda **tachada y no borrada**, con la fecha de la decisión (2026-08-21) y su razón, para que la regla siga visible como algo que se quitó y no reaparezca como endurecimiento. **La lámina de escritorio 9c NO se tocó**: su caso —el enlace abierto en otro dispositivo— es real y es justamente el que la 15.6 protege; con la 15.14 shipeada su promesa además dejó de ser falsa. **Comprobado después de editar**: `rg` no encuentra la regla en ninguna lámina ni en ninguna especificación, y las pruebas que exigen que el producto no la diga siguen verdes. **Queda anotado**: los comentarios de `magic-link-request.ts` y `espera-servida.test.tsx` que dicen «la 15.15 manda corregir en la lámina» quedan en pasado y no se tocaron acá para no chocar con la entrega de la 15.14, que edita ese mismo bloque
### 15e. Phone verification is a different thing

- [ ] 15.13 F21 — Google or the mail link confirm **who you are**; the WhatsApp code confirms **that the listing's number works**. Two steps, two moments, never merged into one screen. **Entirely unbuilt today**: `PhoneVerificationPort` exists with a `DisabledPhoneVerificationAdapter` and a `createPhoneVerificationAdapter` that throws `PhoneVerificationNotImplementedError`. It is a third channel with a per-message cost, and it belongs to the publish flow, not to signing in. **Decisión del fundador, 2026-08-29: el canal de WhatsApp queda diferido al final del proyecto.** Tiene costo por mensaje y necesita una decisión de proveedor que todavía no está tomada. Esta casilla vacía es entonces una postergación deliberada y con fecha, no un descuido. Lo que sí se construyó mientras tanto es la mitad que no depende del canal (19.9/19.10): la tabla `verified_contact` y el camino del correo propio. Hasta que el canal exista, un teléfono sin fila **se comporta como no verificado**, que es lo que impide que su ausencia se vuelva un agujero. **Ratificado y fechado por el fundador el 2026-09-02: la verificación por WhatsApp se difiere al FINAL del proyecto, junto a la fase 10 (el riel de pago).** No se construye, ni se construye a medias. Esta casilla vacía es una postergación decidida y con fecha, no un descuido — y es la razón por la que la fase 15 puede darse por cerrada sin ella

## Phase 16: La ficha — F22 to F31 (founder, 2026-08-22)

The listing detail, specified by the founder with mobile and desktop designs delivered. This is the screen that closes the product loop: today a visitor can search and can publish, and **cannot open a listing at all** — `app/page.tsx` renders every result without an `href` and no `/alquiler/...` route exists.

Five questions were put to the founder before writing this and all five are answered here. Nothing below is inference.

### 16a. The photo pipeline no longer fits the design (founder, 2026-08-22: "tenemos que manejar ahora estos tamaños")

**The largest technical item in this phase, and it is not on any screen.** Exactly two derivatives ship per photo — a 128×96 thumbnail capped at 10 KB and a detail of up to 1280 px capped at 200 KB — and they were sized for the old row layout, whose thumbnail was 44×34. The new design needs four sizes:

| Surface | Needed | What exists today |
|---|---|---|
| Result card, mobile | 158 px | 128 px thumbnail — soft |
| Result card, desktop | 254 px | 128 px thumbnail — visibly blurred |
| Ficha strip, mobile | ~360 px at ~40 KB (F26) | the 200 KB detail — **six photos = 1.2 MB** |
| Ficha main, desktop | 640×360 | the detail serves |

The third row is a budget failure, not a quality one: F26 promises ~240 KB for all six and F15 caps the ficha at 500 KB, while six of today's detail derivative come to 1.2 MB.

- [x] 16.1 Re-derive from the 1280 px detail, **not from the original**. D12 discards originals after hashing and they are unrecoverable; 1280 px is enough to produce every smaller size, and saying so here stops someone concluding the photos must be re-uploaded. **Hecho.**
- [x] 16.2 `listing_photo` stores exactly two keys (`thumbnail_key`, `detail_key`) and its comment says "Two derivatives per photo and nothing else". Adding sizes changes that model — decide between more columns and a derivative table, and record which, because the comment currently forbids what this task requires. **Hecho, migración `0010`.** Tabla `listing_photo_derivative`, con `INSERT … SELECT` antes de borrar columnas para no destruir las claves de R2 ya subidas
- [x] 16.3 A backfill for photos already stored, since the new sizes do not exist for them. **Hecho.**

### 16b. URLs — the founder kept the ids (2026-08-22)

**Decision: our own ids stay. No incremental numeric id.** F27's example wrote `/aviso/84512/foto/2`, which implied a short numeric public id and a new route base. The founder rejected the incremental id outright ("un id incremental no va… debes poner el id que nosotros manejamos"), so **task 11.1's scheme survives unchanged** and the viewer nests inside it.

- [x] 16.4 The ficha at `/alquiler/<ciudad>/<zona>/<slug>-<id>` — `buildListingPath` and `listingIdFromSlug` already ship (11.1) and are unaffected. **Hecho, PR #74/#78.**
- [x] 16.5 The photo viewer at `/alquiler/<ciudad>/<zona>/<slug>-<id>/foto/<n>` (F27). One photo, one URL, shareable, indexable. **Hecho, PR #82.**
- [x] 16.6 **The duty 11.1 created, now due:** every path ending in the same id resolves to the same listing, so the page rebuilds the canonical path and redirects anything that differs. Without it one advert publishes unbounded duplicate URLs. **Hecho.** `resolveListingRoute`, y el visor lo reusa en vez de tener dos definiciones de canónico
- [x] 16.7 Previous and next are **real links**, not client state (F27). The browser's back button then steps back one photo rather than leaving the listing — that behaviour is a consequence of using links, not something to implement. **Hecho, PR #82.** Y por eso «atrás» retrocede una foto en vez de salir del aviso — eso sale del historial, no se programa
- [x] 16.8 Pin whether `/foto/<n>` is 1-based while `listing_photo.position` is 0-based. F27 writes `/foto/2` for the second photo. **Resuelto: 1-based en la URL, 0-based en la columna**, con la traducción en una sola función (`photoNumberOf` / `photoPositionOf`), PR #82

### 16c. Carrying the search back (F22, founder: "hay que arrastrarlo")

- [x] 16.9 "← Resultados" must return **with the filters intact**. The ficha's URL carries no search state, so the state has to travel with the link out of the results and back. Whatever shape it takes, it is a link and works with JavaScript off (F14) **Cerrada 2026-08-29, y el hallazgo es que el mecanismo entero estaba escrito y NO estaba conectado.** `src/modules/listing-discovery/domain/return-to-results.ts` ya exportaba `RETURN_PARAM`, `safeResultsOrigin`, `withResultsOrigin` y `resultsLink`; la ficha ya los leía; `listing-grid.ts` ya aceptaba un cuarto argumento con el origen y tenía su prueba. **Lo que faltaba era el único llamador que importa: ninguna de las dos pantallas de resultados le pasaba ese cuarto argumento.** O sea que en producción «← Resultados» no ocurría nunca desde una búsqueda — la ficha caía siempre en su respaldo («Ver avisos en Tierra Negra»), que es una salida honesta y NO una vuelta. Medido antes de tocar nada, sobre el cuerpo servido: el `href` de la tarjeta en `/alquiler/maracaibo/tierra-negra?max=500` traía `volver = null`. **Nada se veía roto**, que es exactamente el modo de fallo que el enunciado nombra. Lo que se agregó es la regla que faltaba y sus dos cables: **`resultsOriginHref(basePath, query)`** en `src/modules/listing-search/domain/search-query.ts`, porque QUÉ viaja en la vuelta es producto y no plantilla — y porque la zona y la ciudad tienen que componer el MISMO origen, y dos copias de la expresión escritas en dos páginas dejan de coincidir en el próximo parámetro. **Viaja la búsqueda, no el panel**: los filtros y `pag` sí (la página es dónde la persona estaba parada), `filtros` y `busca` no — no recortan un solo aviso, y arrastrarlos devolvería a los resultados con el modal encima. Es `NON_FILTER_FIELDS` menos `page`, no una cuarta lista. **No hizo falta tocar ninguna de las tres guardas** (`safeResultsOrigin`, `safeReturnPath`, `safeSignInReturn`) ni agregar un cuarto parser: `withResultsOrigin` valida antes de emitir, así que el origen nuevo pasa por la comprobación que ya existía, y una `?zona=` ajena ya la descarta `resolveSearchLocation` antes (14.23b). Pruebas: `src/modules/listing-search/domain/search-query.test.ts` → **«se lleva los filtros puestos, no sólo la ruta»**, **«se lleva la página, que es dónde estaba parado»**, **«no se lleva el estado del panel, que no filtra nada»** y **«lo que escribe, la ficha lo acepta»** (la invariante que ata al emisor con `safeResultsOrigin`). Que la PANTALLA lo instale es otra afirmación y por eso se lee sobre el cuerpo servido: `zona-sin-javascript.test.tsx` y `busqueda-sin-javascript.test.tsx` → **«cuelga de cada tarjeta la búsqueda entera, para que la vuelta la traiga (16.9)»**, con el valor esperado escrito como literal y no derivado de las mismas funciones que compone la página. Y el F14 medido y no afirmado: `tests/e2e/camino-de-lectura-sin-javascript.spec.ts` → **«volver de un aviso devuelve a la búsqueda filtrada, no a la zona pelada (16.9)»**, que en el proyecto `crawlability` (`javaScriptEnabled: false`) filtra a 2 avisos, abre uno, aprieta «← Resultados» y aterriza en `/alquiler/maracaibo?max=500` con los mismos 2. **Mutaciones, las cuatro medidas.** (a) `{ step: null, zoneSearch: null }` → `{}`: 1 de 35 en rojo, la del panel. (b) el `query` del origen → `{}`: 3 de 35. (c) el cuarto argumento de la zona → `undefined`: **exactamente 1 de 708** unitarias, la de su propio cuerpo servido. (d) el texto de la vuelta lo cubre `Nav.test.tsx` desde la 14.38 y no se volvió a tocar. **CORRECCIÓN a la prueba de navegador, encontrada corriéndola**: leer `page.url()` justo después del clic falla sólo en el proyecto `chromium` — con el script encendido el enrutador de Next navega en el cliente y la lectura llega antes. `crawlability` pasaba y `chromium` no, o sea la carrera y no el producto; se usa `expect(page).toHaveURL(...)`, que reintenta.

### 16d. The contact block (F29, F30)

**The mask is a fixed string, and that is what keeps the guarantee.** The founder chose to reveal only `+58` and mask the rest. Since the mask is drawn without reading the stored value, **not one character leaks** — the shipped rule that the contact value is unrepresentable in the locked state (contact-reveal domain) is untouched, and F29's "the number is always visibly there" is satisfied.

- [x] 16.10 Locked masks, per method: phone → `+58 ••• ••• ••••`, email → `•••••@•••••.•••`. Literal strings, never derived. **Hecho, PR #81.** Literales por método en `ContactBlock`
- [x] 16.11 **Three methods remain correct — no fourth value, and no migration.** The founder listed four cases (landline, mobile with WhatsApp, mobile without, email) but they collapse to three actions: `wa.me`, `tel:`, `mailto:`. A mobile without WhatsApp and a landline both dial. The only thing merged is a label nuance ("Llamar al celular" vs "Llamar"), which can be split later without breaking anything. **Hecho, PR #81.** Un `Record` por método, no un `if`: un cuarto rompe la compilación
- [x] 16.12 The revealed state needs **`phone_verified_at`** — F29 shows "desde cuándo está verificado". The column does not exist, and phone verification itself is a stub (`PhoneVerificationNotImplementedError`, see 15.13) **Entregado 2026-08-29, y SIN la columna que la tarea nombra.** **Corrección al texto, con su razón (AGENTS.md §5)**: la 19.9 resolvió el mismo problema con otra forma —`verified_contact (user_id, method, value, verified_at)`, clave primaria natural sobre el triple— porque **la verificación pertenece al valor y no a la persona**; una columna en `users` diría que María tiene «un teléfono verificado» cuando lo que hace falta saber es si ESTE número lo está. `verified_contact.verified_at` ES el instante que la F29 pide, así que no se generó migración ni se tocó el esquema. **Lo que faltaba no era el dato sino el cable**, y era la trampa del `resultsOrigin` otra vez: la ficha pasaba `verifiedAt={null}` escrito a mano en `page.tsx`, con `ContactBlock` ya listo para dibujar la línea. **La lectura**: `viewListingContact` (`src/modules/contact-reveal/application/view-listing-contact.ts`) recibe el `ContactVerificationEvidencePort` de la 19.9 y llama a `findEvidence({ userId: listing.publisherId, contact: { method, value } })` con la fila que `findRevealable` acaba de traer — una sola consulta, sin segunda regla, y **sólo en la rama revelada**, así que ninguna visita anónima la paga. La cuenta es la del AVISO y no la de quien mira: con `session.userId` la ficha diría que el teléfono del dueño está verificado porque el inquilino verificó el suyo. **Pruebas nombradas**: la regla del cable en `view-listing-contact.test.ts` → *«pregunta por el triple del aviso, no por la persona ni por quien mira»*, *«no lee `verified_contact` mientras nadie haya revelado»* y *«dice desde cuándo está verificado cuando el triple tiene fila»*; **la instalación**, que es lo que faltaba, en `app/alquiler/[ciudad]/[zona]/[slug]/ficha-verificacion.test.tsx` sobre `renderToStaticMarkup` de la RUTA → *«dice desde cuándo está verificado cuando el triple tiene fila»* y *«pregunta por la cuenta que publica y por el contacto que el aviso copió»*. **Mutaciones, todas restauradas y verificadas por sha256**: devolver `verificationNotice={null}` en la página pone roja 1 de las 3 de bytes servidos; preguntar con `session.userId`, la del triple; leer `accountEmailVerifiedAt` en vez de `verifiedAt`, 2. **La 15.13 no se desbloquea con esto**: el canal de WhatsApp sigue diferido al final del proyecto (fundador, 2026-08-29), así que hoy la única fila que existe es la del correo propio (19.10) y la línea nombra el canal de la fila, nunca «WhatsApp» por defecto
- [x] 16.13 The expired state: no contact, an explanation, and an exit to active listings **in the same zone** — that is the suggestion engine of 11.12, not a simple query **ENTRADA RANCIA, CORREGIDA 2026-08-28: no es trabajo aparte, es la 11.8, cerrada el 2026-08-27.** Estaba pendiente por duplicación de plan y no por código faltante. Lo cierra `app/alquiler/[ciudad]/[zona]/[slug]/ficha-vencida.test.tsx` → «responde con la pantalla del aviso y dice que venció» y «trae los avisos activos de la misma zona en el cuerpo de la respuesta», sobre el motor de sugerencias que la 11.12 construyó (`suggest-active-listings.ts`: zona primero, luego ciudad, nunca más allá). Las tres partes que esta tarea pide están: sin contacto y con explicación las dibuja `ContactBlock` en su estado `expired`, y la salida son los avisos vivos DIBUJADOS en la ficha, no un enlace a otra pantalla. **Y desde entonces se endureció dos veces**: la 11.23 sacó el ternario `status === "active"` de la página y puso la decisión en `resolveListingAvailability(listing, now)`, así que **decide el reloj y no el rótulo** —en la ventana de hasta 24 h en que el trabajo diario todavía no corrió, un aviso vencido ya no ofrece revelar el contacto—; y la 21.1 sumó `expires_at > now()` a los tres lectores del catálogo. Se marca cerrada POR la 11.8 y no como trabajo propio, que es la diferencia que evita construirla dos veces
- [x] 16.14 F30's negotiation warning sits with the contact, not in the footer. **Hecho, PR #81.**

### 16e. Content rules that need no new data

- [x] 16.15 F24 — four cells always drawn, a zero rendered as "0". This already matches the schema's own note that the strip "has no empty state for one of them". **Hecho.** `StatStrip`
- [x] 16.16 F25 — **list only what was declared, never the absence.** An unticked `amoblado` means "not declared", not "not furnished", and rendering the negative would state something the system does not know. Worth carrying into the column semantics: these booleans record a declaration, not a fact about the property. **Hecho.** `DeclaredFeatures`
- [x] 16.17 F23 — property type goes beside the location ("Apartamento · Chacao · Distrito Capital"), never inside the numeric strip, because it is a category and not a number. **Hecho.**
- [x] 16.18 F28 — alt text composed at render, **position first**: "Foto 2 de 6 — Apartamento 2 habitaciones, Chacao". There is no `alt_text` column and that is deliberate (see `listing_photo`'s note). **Hecho.** `photoAltText`, posición primero
- [x] 16.19 F26 — the strip is native `scroll-snap`, so it works with JavaScript off. First photo eager, the other five lazy. **Hecho.** `PhotoStrip`, `scroll-snap` nativo

### 16f. Not found (founder, 2026-08-22: "esto se tiene que manejar como notfound")

- [x] 16.20 **The case the founder's document did not cover and I raised: a ficha that never existed.** The expired state is specified; a deleted listing or a mistyped link is not, and that arrives constantly because links travel by WhatsApp. Handled as a real 404 — which is task 11b.3, still blocked on a design. **Hecho.** `notFound()` indistinguible de un aviso oculto: quien sondea URLs no puede averiguar si fue dado de baja o nunca existió
- [x] 16.21 ~~Reports (F31) pass a listing to `hidden` on acceptance. **There is no report table at all** — Phase 8's work, and the ficha's report link has nowhere to write until it exists~~ **ENTRADA RANCIA: la afirmación tachada ERA FALSA cuando se escribió y hoy lo es dos veces.** Corregida 2026-08-28 con lo que sí es cierto: (a) **la tabla existe** — `listing_report` está en `src/shared/db/schema.ts` con su `UNIQUE(listing_id, reporter_id)`, migración `drizzle/0013_giant_shriek.sql`, junto con `moderation_action` (8.1); (b) **la regla existe y es del dominio** — `resolveReportOutcome` pasa el aviso a `hidden` al tercer REPORTANTE DISTINTO (`src/modules/listing-trust/domain/report-threshold.ts`), con el caso de uso en `application/report-listing.ts` (8.4) y el conteo distinto probado contra Postgres real en `tests/integration/listing-trust.test.ts` — es la restricción única y no código de aplicación lo que hace que el segundo reporte de la misma cuenta inserte cero filas; (c) **la puerta existe desde ayer** — la 8.7 encontró que `reportListing` no lo llamaba ninguna ruta (el enlace de la ficha era un ancla `#reportar` a una sección que la página nunca dibujó) y la cerró con `app/alquiler/[ciudad]/[zona]/[slug]/reportar/` (`page.tsx`, `actions.ts`, `reportar.test.ts`), y el pie de la ficha ahora enlaza `${listingPath}/reportar`. **Lo que QUEDA, y es una tarea abierta y no ésta: la 8.8** — un aviso ocultado por reportes sólo se restaura con `curl` contra `app/api/operator/restore-listing/route.ts` detrás de un `OPERATOR_SECRET` de portador, sin pantalla y sin modelo de cuenta de operador. Mientras nadie podía reportar eso era teoría; desde la 8.7 tres cuentas cualesquiera pueden ocultar un aviso legítimo

### 16g. Tokens and measurements (founder's ficha spec, 2026-08-22)

**Measured before assuming: the palette did not change.** All eleven colour and radius values in the ficha specification are byte-identical to what `[data-theme="menta"]` already ships — `--surface`, `--bg`, `--line`, `--ink`, `--soft`, `--accent`, `--tint`, `--r`, `--rs`, plus `--rule`/`--acc-ink` under different names. The redesign is **layout, not colour**, and the restyle risk this phase was braced for does not exist for the ficha.

- [x] 16.22 Two names to reconcile, not two values: the spec's `--rule` is the shipped `--strong` (#788189) and its `--acc-ink` is `--accent-ink` (#ffffff). Pick one spelling and use it in both places — two names for one colour is how a palette starts drifting **Cerrada 2026-08-28. Ya estaba hecha y el plan no lo decía**, que es la cuarta vez en este archivo. Gana la grafía que ship*a* —`--strong` y `--accent-ink`—, y por una razón y no por costumbre: es la del archivo de referencia del diseño (`design/reference/sistema/tokens.css`) y la que usan las 169 hojas que `lint:tokens` escanea; `SISTEMA.md` no nombra `--rule` ni `--acc-ink` en ninguna parte, así que esas dos grafías viven sólo en los `.dc.html` y en las especificaciones del fundador. Lo que la cierra no es la elección sino el gate: `scripts/lint-tokens.mjs` lleva un `TOKEN_ALIASES` que RECHAZA `var(--rule)` y `var(--acc-ink)` en `components/` y `app/`, y `components/design-contract.test.tsx` → **«un solo nombre por color: la spec escribe --rule y --acc-ink, y ships --strong y --accent-ink»** afirma los dos valores y que ninguna de las dos grafías esté declarada en `tokens.css`. Hace falta un gate y no una revisión porque el síntoma es invisible: `var(--rule)` sin declarar no pinta nada raro, hereda
- [x] 16.23 **Type sizes the token set does not carry**, and D16 forbids writing them as literals: price 28/700 mobile and 34/700 desktop (today `--fpb` is 26 and has no desktop step); title 17/600 mobile and 19/600 desktop (today `--title-fs` is 20/700, which means the page title); description 15/1.6. **Do not reach for a token that merely has the right number** — that exact mistake is already recorded in `tokens.css`: the home's `<h1>` grabbed `--fpb` ("precio en ficha") because no page-title token existed, and `lint:tokens` passed, because it verifies a value IS a custom property and never that it is the RIGHT one. A missing token does not fail a gate; it produces a plausible wrong answer **Cerrada 2026-08-28.** Los siete tokens están en `src/styles/tokens.css` (`--ficha-price-fs`, `--ficha-price-fs-desktop`, `--ficha-price-fw`, `--ficha-title-fs`, `--ficha-title-fs-desktop`, `--ficha-title-fw`, `--ficha-body-fs`/`--ficha-body-lh`) y los usa `app/alquiler/[ciudad]/[zona]/[slug]/ficha.module.css`. **CORRECCIÓN AL TEXTO DE LA TAREA: el precio móvil es 30 y no 28.** La especificación lo escribe 28 dos veces (§4 R1 y la tabla de §8) y la lámina móvil dibuja `font-size:30px`; manda la lámina (AGENTS.md §2, «el sistema de diseño es la fuente visual de verdad»), y queda anotado dentro de `tokens.css` para que el próximo lector no lo tome por un error de tipeo. **Lo que faltaba de verdad era la prueba, no el token**: la afirmación «este token es el CORRECTO y no el que se le parece» es indistinguible de su contraria leyendo la hoja, así que se mide dibujada en `tests/measure/ficha.spec.ts` → **«16.23: a 360 el precio pinta 30/700 y el título 17/600»**, **«16.23: a 1280 el precio sube a 34 y el título a 19 — hay paso de escritorio»** y **«16.23: la descripción pinta 15px con interlineado 1.6 en las dos pantallas»**. **Mutación, y es la del propio enunciado**: apuntar `.price` a `var(--fpb)` (26) pone en rojo exactamente 1 de las 6 — y `pnpm lint:tokens` sigue diciendo «no literal values found», exit 0, con la ficha dibujando el precio equivocado. Ese exit 0 es la demostración del punto: el gate de tokens no puede cerrar esta tarea y por eso hay una medición
- [x] 16.24 **DECIDIDA POR EL FUNDADOR EL 2026-08-27 Y APLICADA: 44.** De los tres candidatos sólo uno se puede comprobar contra una norma — WCAG 2.2 SC 2.5.8 (AA) pide 24×24 y los tres lo pasaban, SC 2.5.5 (AAA) pide 44×44 y **sólo 44 lo alcanza**; 40 era «mejor que 36» sin ser suficiente. `--target-min-desktop: 44px` en `src/styles/tokens.css`, con su comentario reescrito: el que había («36 y no 40, y es una decisión pendiente del fundador») quedó obsoleto y ahora registra el razonamiento de accesibilidad que la decidió. **El token NO se colapsa en un alias de `--target-min` aunque valgan lo mismo**: el nombre registra la intención y quien mañana quiera bajar el mínimo de escritorio tiene que ver cuál de los dos toca. Probado por `tests/measure/layout.spec.ts` → **«1b.14/16.24: interactive targets measure exactly 44px on mobile and on desktop»**, con `toBe(44)` y no `toBeGreaterThanOrEqual`: la cota inferior anterior aceptaba las TRES respuestas posibles, o sea que no preguntaba nada. Y por `design-contract.test.tsx` → «--target-min-desktop es 44: el único valor que alcanza WCAG 2.2 SC 2.5.5 (16.24)» más «sigue siendo un token propio y no un alias de --target-min», que es la aserción que faltaba. **Mutación, una sola y las dos suites en rojo**: `--target-min-desktop` de vuelta a 36 pone rojas las dos pruebas de `design-contract.test.tsx` y la medición.

  **Dos hallazgos que la mutación destapó y el texto de esta tarea tenía mal:**

  1. **La 1b.14 registraba «desktop smallest dimension 36px» y era falso.** Con el token en 36 el botón dibujado mide **39,25 px**, no 36: el `padding: 8px 16px` y el `line-height` ganan sobre el `min-height`. La cota `>= 36` tenía tres píxeles y cuarto de holgura que nadie sabía que estaban ahí, así que ni siquiera estaba midiendo el token. A 44 el `min-height` sí manda y la medida es 44,00 exacto en las dos pantallas.
  2. **El cuadrado de 44×44 de `photo-uploader` líneas 322-323: medido en un navegador, NO tapa la fotografía, pero pasa de ocupar un doceavo a un octavo de la baldosa.** A 1280 el ancho útil del subidor es **600 px** (lo fija `FormShell`), así que la rejilla 4-up con `gap: 12` da baldosas de **141 × 105,75 px**. El botón de quitar cubre **31,2 % del ancho, 41,6 % del alto y 13,0 % del área**; a 36×36 era 25,5 % / 34,0 % / **8,7 %**. Es una consecuencia de diseño que se reporta y no se resuelve en silencio: **41,6 % del alto de la miniatura es mucho**, y si el fundador quiere recuperarlo la salida no es bajar el token —es que ese control deje de estar encima de la imagen (la lámina 2g del teléfono ya le da columna propia). Nada más cambia composición: las otras quince declaraciones son `min-block-size`/`min-height` y sólo crecen los controles que quedaban por debajo de 44.

  ~~**A conflict with a shipped token, not a gap:**~~ the spec sets the desktop minimum touch target at 40 px; `--target-min-desktop` ships as 36 px. One of them is wrong and the change is global — every control on every screen **DECISIÓN DEL FUNDADOR, PREPARADA 2026-08-28 — no se toca hasta que la tome.** Las consecuencias, contadas y no estimadas: (a) **alcance**: 16 declaraciones en 12 hojas — `components/atoms/Button.module.css` (los tres niveles de botón, que es todo botón del producto), `Field`, `FilterChips` (×2), `SearchFilters`, `CityZoneSelect`, `SearchPanel`, `app/home`, `app/alquiler/[ciudad]`, `app/alquiler/[ciudad]/[zona]`, `app/publicar/publish-page`, `app/publicar/publish-steps` (×2) y `app/publicar/fotos/photo-uploader` (×3). (b) **Qué se ve distinto a 40**: quince de las dieciséis son `min-block-size`/`min-height`, así que sólo crecen los controles que hoy quedan por debajo de 40 y ninguno se mueve de sitio; **la excepción es `photo-uploader` líneas 322-323**, un cuadrado FIJO de 36×36 (el `×` de quitar una foto, posicionado sobre la miniatura) que pasaría a 40×40 y sí cambia composición — es el único punto que hay que mirar con los ojos. (c) **Qué prueba se rompe**: exactamente una, y a propósito — `components/design-contract.test.tsx` → «--target-min-desktop NO se toca: 36 ships y la spec dice 40, y decide el fundador», que es el ancla puesta para esto. **`tests/measure/layout.spec.ts` → «1b.14» NO se rompe**: afirma `>= 36` en escritorio, y 40 lo cumple — o sea que la suite de medición dejaría pasar el cambio en silencio, y ése es un dato de la decisión, no un consuelo. (d) **Accesibilidad**: no la decide. WCAG 2.2 SC 2.5.8 (AA) pide 24×24 y los dos valores lo pasan; SC 2.5.5 (AAA) pide 44×44 y **ninguno de los dos lo alcanza** — si el criterio fuera accesibilidad la respuesta sería 44, no 40. Las guías de plataforma (Apple 44pt, Material 48dp) empujan hacia arriba, así que 40 es «mejor que 36» y sigue sin ser suficiente para AAA. La pregunta para el fundador es entonces de diseño y no de norma: **40 como dice su especificación, o 44 igual que el móvil, o 36 como está**
- [x] 16.25 Action buttons are 46 px tall, which is neither `--target-min` (44) nor the desktop value. Needs its own token **Cerrada 2026-08-28. El token ya estaba** (`--action-h: 46px` en `src/styles/tokens.css`, con su comentario explicando por qué no se ata a `--target-min`) y lo usan `ContactBlock.module.css`, `SearchPanel.module.css` y `SearchOutcome.module.css`; lo que no estaba era una prueba capaz de verlo. La que había —`ContactBlock.test.tsx`, «da área táctil a los dos controles del bloque»— es `expect(css).toContain("var(--action-h)")`: una aserción sobre el contenido de la hoja que **nunca vio el número 46**. Ahora `tests/measure/ficha.spec.ts` → **«16.25: el botón mide 46px de alto en las dos pantallas — ni 44 ni 36»**, con `toBe(46)` y no `toBeGreaterThanOrEqual(44)` a propósito: una cota inferior aceptaría las dos respuestas posibles, y una aserción que acepta las dos respuestas válidas no está preguntando nada. **Dos mutaciones, y la segunda es la que importa.** (a) `min-height: var(--action-h)` → `var(--target-min)` en `ContactBlock.module.css`: pone en rojo 1 de las 6 medidas, y **también** la aserción de hoja de estilos — o sea que ahí las dos sirven. (b) `--action-h: 46px` → `44px` en `tokens.css`, que es el mismo defecto un nivel más adentro: **59 pruebas unitarias en verde** (`ContactBlock.test.tsx` + `design-contract.test.tsx`, las 59) y la medición en rojo. Ésa es la demostración de por qué hacía falta medir: el nombre del token puede ser el correcto y el valor estar mal, y una aserción sobre el texto de la hoja no puede ver la diferencia
- [x] 16.26 Geometry tokens the ficha introduces: main photo 640×360, thumbnail 120×90, viewer thumbnail 84×56, mobile photo height 180, container 1100 (already shipped as `Container`). **Hecho, PR #82.**
- [x] 16.27 **The viewer's palette is deliberately outside the theme** — `#131517` background, `#F2F3F3` text, `rgba(242,243,243,.62)` secondary, `rgba(242,243,243,.24)` borders. The spec's reason is sound and worth keeping in the code: any tint shifts the temperature of the photograph. It still may not be written as literals in a component (D16), so it needs named tokens of its own. Note `scripts/lint-tokens.mjs` checks the contract across `menta` and `oscuro`; a third, screen-scoped set is a case it has never seen. **Hecho, PR #82.** En `:root` y no en `[data-theme]`, con una mutación que lo prueba
- [x] 16.28 `--soft` is the shipped answer to "never dim text with `opacity`" (criterion 15). Already true in the token set; assert it rather than assume it. **Hecho.**

### 16h. Two contradictions in the specification itself

- [x] 16.29 **The right column is 420 px or 328 px — the document says both.** §3 gives `grid-template-columns: 640px 1fr; gap: 40px` inside the 1100 container, which computes to 420. §8's measurement table says "Columna de contacto: 328px, pegada". The shipped `DetailSplit.module.css` already uses `640px 420px`, so the grid formula is what exists. 328 may be the block inside a 420 column, but that is a reading, not a decision **RESUELTA POR MEDICIÓN 2026-08-28: son 420, y 328 es la medida MÓVIL puesta en la celda de escritorio.** No se eligió el número que coincidía con el código; se fue a la lámina. Tres evidencias, en orden de peso: (1) **la lámina de escritorio no dibuja ningún bloque de 328** — `rg` sobre `design/pantallas/Rentas - Ficha - Desktop.dc.html` devuelve cero coincidencias de «328», y los hijos de la columna pegada (`position:sticky;top:24px`) son todos de nivel de bloque sin ancho propio, o sea que ocupan la pista entera; (2) **la lámina MÓVIL dibuja 328 seis veces**, y siempre es el ancho de la foto de la tira — 360 − 16 − 16 de margen, que es el ancho útil del teléfono y lo que `--ficha-photo-w: 328px` ya nombra; (3) **medido en un navegador**, `tests/measure/ficha.spec.ts` → **«16.29: a 1280 la columna derecha dibuja 420px dentro del contenedor de 1100»** imprime `media=640px contacto=420px separación=40px total=1100px`, y su par **«16.29: a 360 la columna de contacto ocupa el ancho útil y no 420»** imprime `328px` — el mismo 328, en el ancho donde la tabla dice «ancho completo». La fila de §8 tiene entonces las dos celdas corridas: lo que está bajo «Escritorio» es el valor de «Móvil». **No es una contradicción del diseño y no hace falta molestar al fundador**; es un error de transcripción de una tabla, y las láminas —que AGENTS.md §2 hace fuente visual de verdad— nunca dijeron 328 en escritorio. **Mutación**: `grid-template-columns: 640px 420px` → `640px 328px` pone en rojo 1 de las 6 medidas y también la prueba de hoja de estilos de `DetailSplit.test.tsx`
- [x] 16.30 **The URL base: RESOLVED by the founder, 2026-08-22 — `/alquiler/<ciudad>/<zona>/<slug>-<id>`, chosen for SEO.** The ficha specification still writes `/aviso/84512` and the design files show only an id; the founder was explicit that this does not govern ("si ves en el diseño solo el id no le pares, solo para esto, ya que es la estructura nueva"). So task 11.1's shipped scheme stands unchanged, the viewer nests as `.../foto/<n>` (16.5), and the canonical redirect duty (16.6) is what keeps one advert from publishing unbounded duplicate URLs. Recorded because the design files will keep showing the short form and the next reader will otherwise think they are authoritative. ~~The founder rejected the incremental numeric id (2026-08-22) and said to use "el id que nosotros manejamos" — which settles the ID and leaves the BASE open.~~
- [x] 16.31 The WhatsApp action opens `wa.me` **with a drafted message that names the listing** (§2 step 9). New: nothing composes that text today, and it is the last thing the product does before the conversation leaves for WhatsApp. **Hecho, PR #81.** Y el número se normaliza a dígitos internacionales: sin eso `wa.me` abre igual con un número que no existe, y la conversación simplemente no ocurre
- [x] 16.32 "Copiar el número" in the revealed state — a clipboard action, so JavaScript, on a screen whose read path ships none. It is an enhancement and must degrade to the number being selectable. **Hecho, PR #81.** Degrada por ausencia, no por inercia: arranca invisible y sólo lo levanta un efecto que comprueba `navigator.clipboard`. La salida sin JS es `user-select: all`
- [x] 16.33 Keyboard navigation in the desktop viewer: ← → change photo, Escape closes (criterion 9). JavaScript on top of links that already work without it (criterion 8) — the two criteria are compatible only in that order. **Hecho, PR #82.** Con un test que ata los selectores del componente cliente a los atributos de la página — sin él el teclado podía apagarse en silencio
- [x] 16.34 "verificado por WhatsApp el 19 ago" needs `phone_verified_at` (see 16.12) and a date format; the expired state needs the expiry date rendered ("Venció el 12 de septiembre"), which `expires_at` already carries **Entregado 2026-08-29, en dos mitades con edades distintas.** **La mitad vencida ya estaba viva** y se marca por lo que hay, no por trabajo nuevo: `ContactBlock` dibuja «Venció el 12 de septiembre y no fue renovado» desde la 11.8, probado por *«explica qué pasó y da una salida a la zona»* (`components/molecules/ContactBlock.test.tsx`). **La mitad de la verificación es nueva, y la frase se mudó al dominio**: lo que la pantalla AFIRMA sobre un contacto —quién lo comprobó y cuándo— es producto, así que salió del componente y vive en `src/modules/contact-reveal/domain/contact-verification-notice.ts`, donde el piso del 90 % llega y `app/` no. Devuelve `null` cuando no hay instante, y **`null` significa que no se dibuja NADA**: las tres láminas del bloque —sin cuenta, con cuenta y vencido— no dibujan ningún estado negativo, así que «sin verificar» sería una pantalla inventada además de una afirmación que nadie hizo. **El canal sale de la fila y no de un literal**: escribir «por WhatsApp» fijo sería falso para toda fila que existe hoy, porque ese canal está diferido (fundador, 2026-08-29) y la única que se escribe es la del correo (19.10). **El formato es UNO y está en un solo sitio**: `src/shared/format/spanish-date.ts` reemplaza las CUATRO copias que había —`formatDate` en `page.tsx`, `shortDate` y `longDate` adentro de `ContactBlock`— y de paso les pone el `timeZone: "UTC"` que `lifecycle-notice.ts` y `/mis-avisos` ya escribían («en UTC para no mentir») y que las cortas no tenían: Venezuela es UTC-4, así que un instante de las 02:00 Z se leía «18 ago» o «19 ago» según dónde corriera el render. **Pruebas nombradas**: la frase en `contact-verification-notice.test.ts` → *«no dice nada cuando la verificación no ocurrió»*, *«dice por qué canal y desde cuándo»* y *«nombra el canal que se verificó, y hoy el que existe es el correo»*; el formato en `src/shared/format/spanish-date.test.ts` → *«escribe el día y el mes abreviado, como el pie de la ficha»* y *«no corre el día cuando el proceso vive al oeste de Greenwich»*; el dibujo en `ContactBlock.test.tsx` → *«dibuja la frase de verificación que le llega, y nada cuando no le llega ninguna»*, que además comprueba que sin frase **no sale ni el envase vacío** (`data-testid="contact-verification"` ausente). **Mutaciones, todas restauradas y verificadas por sha256**: quitar `timeZone: "UTC"` pone rojas exactamente las 2 pruebas de zona horaria; borrar la guarda de `null`, 1; fijar «WhatsApp» como literal, 1; dibujar la línea siempre (`{true ? …}`), 1. **Correcciones registradas**: (a) la frase pasa a **minúscula inicial** —«verificado por WhatsApp el 19 ago»— porque es lo que dibujan las dos láminas de la Ficha y la §6 de la especificación; shipeaba con «Verificado»; (b) `Intl` en `es-VE` escribe «19 ago.» y «12 sept.» con punto, y las láminas dibujan «19 ago» y «12 sep» — **no se resolvió inventando una tabla de doce literales** (sería inventar un valor que el sistema no define, §2) y queda con número propio en la **16.40**
- [x] 16.35 The footer carries the listing ID and its expiry date beside the report link (§3 item 12) — the first surface that shows an id to a visitor, which is an argument in 16.30's decision. **Hecho.**

### 16i. Lo que salió de reconciliar la especificación contra el conjunto que ship*a* (2026-08-28)

- [x] 16.36 **(nueva — el arnés que las 16.23/16.25/16.29 necesitaban para poder cerrarse.)** Las tres afirmaban tamaños, y ninguna prueba del repositorio podía ver un tamaño: `lint:tokens` verifica que un valor SEA una propiedad personalizada, y una aserción sobre el texto de una hoja no distingue `--ficha-price-fs` (30) de `--fpb` (26). `app/measure/page.tsx` monta ahora la hoja REAL de la ficha (`ficha.module.css`) dentro de `Container` + `DetailSplit`, con el `ContactBlock` real, y `tests/measure/ficha.spec.ts` lee `getComputedStyle` y `getBoundingClientRect` en un navegador de verdad — seis medidas. La atadura entre las dos pantallas la fija `app/alquiler/[ciudad]/[zona]/[slug]/ficha-medida.test.ts` («la ficha usa styles.price», «el arnés mide fichaStyles.price», y once más), porque un arnés que mide una clase huérfana queda verde para siempre: es el defecto que ya dejó a este repositorio midiendo un formulario de publicar que no existía. **Esa atadura falló su propia mutación la primera vez y hubo que arreglarla**: `expect(page).toContain("styles.price")` sigue verde si alguien renombra a `styles.priceUsd`, porque una cadena es prefijo de la otra — la aserción escrita para atrapar ese renombre lo dejaba pasar. Ahora el límite lo pone la llave de cierre (`{styles.price}`), y la mutación sí la pone en rojo. **Trampa 1 del propio plan, encontrada dentro de la prueba escrita para evitarla**
- [x] 16.37 **(nueva — segunda instancia de la 14.48, encontrada barriendo por la 16.23.)** **`--fpb` ya no lo usa nadie.** Era el token que el `<h1>` del inicio agarró por error —el defecto fundacional que `tokens.css` documenta cuatro veces—, y la corrección fue darle a la home su `--title-fs`: correcta, y dejó a `--fpb` declarado en `[data-layout="compacto"]` sin un solo `var(--fpb)` en `app/` ni en `components/`. **La buena noticia primero: el mal uso documentado ya NO está vivo**; la home usa `--title-fs`. La que queda es la forma de defecto de la 14.48: un token que nadie usa es invisible para `lint:tokens`, que no puede ver lo que no aparece. **No se resuelve igual que `--searchbar-*`, y por eso es tarea aparte**: aquéllos son medidas que este proyecto inventó y puede tirar, mientras que `--fpb` es del sistema —está en `design/reference/sistema/tokens.css` para las cuatro estructuras y `SISTEMA.md` lo nombra «Precio en ficha»—, así que borrarlo del conjunto que ship*a* es un cambio al sistema. Decidir entre las dos salidas honestas: retirarlo del subconjunto que ship*a* con su nota, o que la ficha vuelva a usarlo (y entonces `--ficha-price-fs` sobra). Hoy conviven un token del sistema sin uso y uno propio con el papel que aquél nombra **Cerrada 2026-08-29 — el fundador eligió la salida A: retirarlo del subconjunto que ship*a*, con su nota.** `src/styles/tokens.css` ya no declara `--fpb` en `[data-layout="compacto"]`, y en su lugar queda escrito POR QUÉ falta: el papel que nombra —«Precio en ficha»— lo cumple `--ficha-price-fs` (30/34) desde la 16.23, así que convivían un token del sistema sin un solo uso y uno propio haciendo su trabajo, que es exactamente cómo el `<h1>` del inicio terminó agarrando `--fpb`. **No se borró de `design/reference/sistema/tokens.css` y eso es deliberado**: la referencia describe el SISTEMA (las cuatro estructuras lo declaran) y el archivo que ship*a* describe lo que ESTE producto usa. **Como es un token del sistema, la retirada es un cambio al sistema y quedó escrita en `SISTEMA.md`**: la fila «Precio en ficha» de la tabla de tipografía dice ahora qué lo dibuja y que `--fpb` queda fuera, y la tabla «Lo que este documento decía y por qué cambió» lleva su fila propia con la decisión del fundador y su fecha. **No lleva prueba, y se dice en vez de fingirla** (mismo criterio que la 16.39): quitar una declaración que NADIE lee no cambia ningún píxel, así que no hay número dibujado que medir — la única aserción posible sería sobre el texto de un archivo, que es la trampa 1 de este plan. La evidencia es la suite entera en verde (2057 unitarias, 47 medidas, 60 e2e) más `lint:tokens` en 0 ANTES y DESPUÉS, que es justamente lo que prueba el punto: **el gate no vio ni que sobraba ni que se fue**. **La causa común sigue abierta**: `lint:tokens` verifica que un valor SEA una propiedad personalizada y que los dos temas la declaren, nunca que alguien la USE. La 14.48 (`--searchbar-*`) **NO queda cerrada por esta decisión y sigue distinta**, por tres razones: aquéllos son medidas que este proyecto inventó, así que sacarlos no toca el sistema ni necesita fila en `SISTEMA.md`; `design-contract.test.tsx` todavía afirma dos cosas sobre ellos (`--searchbar-h >= --target-min` y `--searchbar-fs != --control-fs`) que tienen que irse con los tokens y no antes; y la 22.13 (`--ft`/`--tclamp`) es la tercera instancia. **Lo que esta tarea sí deja es el precedente y la forma**: un token del sistema se retira del subconjunto con su nota en `SISTEMA.md`, uno propio se tira
- [x] 16.38 **(nueva — tercera discrepancia lámina/especificación de la ficha, después de las dos que las 16.23 y 16.29 ya corrigieron.)** **La descripción: 15/1.6 o 16/1.65.** La tabla de §8 da `15px / 1.6` para las DOS pantallas, la lámina móvil dibuja exactamente eso, y la lámina de escritorio dibuja `font-size:16px;line-height:1.65`. El conjunto ship*a* un solo par (`--ficha-body-fs: 15px`, `--ficha-body-lh: 1.6`) y lo usan además otras cuatro pantallas (importar, mis-avisos, los pasos de publicar y reportar), así que darle paso de escritorio a la ficha no es agregar una `@media`: es decidir si el par pasa a ser `--ficha-body-fs`/`--ficha-body-fs-desktop` para todas ellas o si la ficha se lleva un token propio. **No se decide de paso** — a diferencia de la 16.29, acá los dos documentos son internamente consistentes y lo que difiere es lámina contra tabla en un valor que ya está compartido. `tests/measure/ficha.spec.ts` ancla hoy 15/24px en los dos anchos, así que el cambio llega con su medida ya escrita y en rojo **Cerrada 2026-08-29 — el fundador eligió la salida A: el par gana paso de escritorio y crecen TODAS las pantallas que lo comparten.** `src/styles/tokens.css` declara `--ficha-body-fs-desktop: 16px` y `--ficha-body-lh-desktop: 1.65` al lado del par de móvil, y las hojas los aplican en el punto de quiebre único del proyecto (768) — el mismo idioma que ya usan `--ficha-price-fs-desktop` y `--card-price-fs-desktop`, no uno tercero. **El recuento se rehízo y da 20 en 8 hojas**, que es el número con el que el fundador decidió: la descripción de la ficha (1), los pasos de publicar (9), importar (4), entrar (2), la espera del correo (1), reportar (1), `/mis-avisos` (1) y la pantalla de fallo (1). **Prueba nombrada, y son DOS a propósito**: `tests/measure/ficha.spec.ts` → *«16.38: a 1280 la descripción sube a 16px con interlineado 1.65 — el par tiene paso de escritorio»* y *«16.38: a 1280 el cuerpo de publicar sube igual que el de la ficha — el paso es de las ocho»*. La segunda existe porque medir sólo la ficha dejaba SIETE hojas creciendo sin que nada las viera: se podía revertir `publish-steps.module.css` entero con la suite en verde, que es la trampa 4 de este archivo. **Mutaciones, las dos restauradas y verificadas por sha256**: apuntar `.text` de la ficha a `--ficha-body-fs` pone en rojo exactamente 1 —la de la ficha— y deja verde la de publicar; sacar `.help` de la regla de escritorio de publicar pone en rojo exactamente 1 —la de publicar— y deja verde la de la ficha. **CORRECCIÓN AL TEXTO DE ESTA TAREA: la medida NO llegaba «ya escrita y en rojo».** Llegaba escrita y en VERDE, porque anclaba 15/24 en los dos anchos, que es el valor de ANTES de la decisión: el `it` afirmaba lo contrario de lo que la lámina de escritorio dibuja. El rojo hubo que escribirlo (se partió en dos `it`, uno por ancho) y se vio fallar —1 rojo, 45 verdes— antes de tocar un token. Una medida que ancla el valor viejo no es una medida pendiente: es una que hay que cambiar. **Y el recuento del par no es simétrico, que es un hallazgo**: `--ficha-body-fs` aparece 20 veces en 8 hojas, pero `--ficha-body-lh` aparece 35 veces en 11 — las 5 declaraciones de más (en `ContactBlock`, `DeclaredFeatures` y `SignInDoor`) usan el interlineado del cuerpo con `--meta-fs` o `--control-fs`, o sea media pareja. **Ésas NO crecieron**: estirar un metadato de 12px que ninguna lámina dibuja distinto excedería la decisión del fundador, que fue sobre el PAR. Queda con número propio en la **22.33**. **Y tres declaraciones de `--ficha-body-fs` llevan interlineado literal propio** (1.5, 1.55, 1.45 en entrar y en la espera): ahí crece sólo el tamaño, porque el interlineado ya lo decidió esa hoja. **Dos correcciones de puntería que la hoja habría dejado pasar en silencio**: (a) en `importar.module.css` el bloque de escritorio nuevo va al FINAL del archivo y no dentro del `@media` que ya existía a mitad de hoja, porque `.problemaTitulo` se declara DESPUÉS de aquél y con la misma especificidad gana la declaración posterior — es el orden de fuente que la 22.2 ya dejó escrito; (b) `.railItem` y `.railExit` de publicar viven DENTRO del `@media` de 768 y leían `--ficha-body-fs`: el riel sólo existe en escritorio, así que ahora leen el token de escritorio en vez de quedarse congelados en 15 mientras la columna de al lado sube a 16. **Ninguna otra medida se puso roja**: las cuatro specs (`ficha`, `layout`, `entrar`, `puerta`) pasan 47 de 47
- [x] 16.39 **(nueva — defecto de rotulado, barato y confuso.)** `src/styles/tokens.css` encabeza el bloque de `--searchbar-h`/`--searchbar-fs` con «La barra de búsqueda del inicio (16.23)», y la 16.23 es la tipografía de la FICHA. El bloque de la ficha, doce líneas más abajo, no lleva número. Un comentario que apunta a la tarea equivocada es peor que ninguno: manda al próximo lector a leer una tarea que no habla de lo que tiene delante. Corregir la referencia (la barra del inicio sale de la 14.21/14.42, no de la 16.23) y numerar el bloque de la ficha **Hecho 2026-08-29, plegado en la PR de la 16.9 por caber holgado bajo el techo.** `src/styles/tokens.css`: el bloque de `--searchbar-h`/`--searchbar-fs` ahora encabeza «La barra de búsqueda del inicio (14.21/14.42)» y dice en voz alta cuál era la referencia equivocada y por qué; el bloque de la ficha lleva su «(16.23)». **No lleva prueba a propósito y se dice en vez de fingirla**: es prosa, y `lint:tokens` lee valores. La única guarda posible sería una aserción sobre el texto del archivo, que es justo la trampa 1 de este plan
- [x] 16.40 **(nueva — cuarta discrepancia lámina/código de la ficha, encontrada cerrando la 16.34.)** **El mes abreviado: «12 sep» o «12 sept.».** Las dos láminas de la Ficha escriben «verificado por WhatsApp el 19 ago» y «vence 12 sep», sin punto y con tres letras. `Intl.DateTimeFormat("es-VE", { month: "short" })` —que es lo que el código shipe*a* desde que existe el pie de la ficha— devuelve «19 ago.» y «12 sept.»: el punto es del español abreviado y «sept.» es lo que el CLDR da para septiembre. **No se resolvió de paso y por eso es tarea aparte**: la única salida que produce exactamente lo dibujado es una tabla de doce literales escrita a mano, y eso es inventar un valor que el sistema no define (§2) además de congelar un idioma adentro del código. Las dos salidas honestas: aceptar la abreviatura del CLDR y corregir las láminas, o declarar los doce meses en un solo sitio con su razón escrita. **Hoy conviven una lámina que dice «sep» y una respuesta que dice «sept.»**, en la pantalla más visitada del sitio y en dos lugares distintos de ella (el pie y la línea de verificación) **RESUELTA por el fundador el 2026-08-29 — se queda lo que da el idioma, y las láminas divergen a propósito.** El producto escribe «19 ago.» y «12 sept.», que es lo que `Intl.DateTimeFormat("es-VE", { month: "short" })` devuelve; las láminas dibujan «19 ago» y «12 sep». **No hay cambio de código y se dice en vez de fingir uno.** La razón de la decisión es la que la propia tarea nombra: la única salida que produce exactamente lo dibujado es una tabla de doce literales escrita a mano, y eso es inventar un valor que el sistema no define (AGENTS.md §2) además de congelar un idioma dentro del código. **Se verificó que el código NO escribe ningún mes a mano**: no hay tabla de meses, ni arreglo `MESES`, ni literal «ago»/«sep» en `src/`, `app/`, `components/` ni `tests/`; los cuatro sitios que formatean una fecha usan `Intl` (`src/shared/format/spanish-date.ts`, `lifecycle-notice.ts`, `/mis-avisos` y `/renovar/[token]`). **Queda escrito en `SISTEMA.md`** —lista «Lo que queda abierto», punto 4— que la divergencia lámina/producto es deliberada, para que nadie la «arregle» más adelante creyendo que es un defecto
- [x] 16.41 **(nueva — quinta discrepancia lámina/código de la ficha, encontrada en la misma barrida.)** **Quién no renovó el aviso.** La lámina y la §6 escriben «Venció el 12 de septiembre y **el dueño no lo renovó**»; `ContactBlock` dibuja «Venció el 12 de septiembre y **no fue renovado**». La voz pasiva no es un capricho de redacción: el aviso puede ser de una inmobiliaria, y «el dueño» sería falso en ese caso — que es probablemente la razón por la que alguien la cambió, sin dejarlo escrito. **Se registra en vez de corregirse en cualquiera de las dos direcciones**: o la lámina dice «quien publica», o el código vuelve a la lámina y acepta que miente para el 100 % de los avisos de inmobiliaria. Es copia de producto y la decide el fundador **RESUELTA por el fundador el 2026-08-29 — manda el código, y la lámina es la que hay que corregir.** El recuadro vencido sigue diciendo «Venció el 12 de septiembre y **no fue renovado**» (`components/molecules/ContactBlock.tsx`), y la razón queda escrita en vez de suponerse: el aviso puede ser de una inmobiliaria, y «el dueño no lo renovó» es falso para el 100 % de esos avisos. La voz pasiva no afirma quién dejó de renovar, que es exactamente lo que el sistema sabe. **No hay cambio de código y no se toca ningún archivo de diseño en silencio**: la corrección de las dos láminas de la Ficha y de la §6 de la especificación va con número propio en la **22.34**. **Queda escrito en `SISTEMA.md`** —lista «Lo que queda abierto», punto 5— con la decisión, su fecha y su razón

## Phase 17: La taxonomía de zonas (founder, 2026-08-22)

**Triggered by real data, not by design.** The founder supplied Maracaibo's official breakdown: 14 parroquias and several hundred barrios and sectores. Two things broke immediately.

**1. That list cannot be imported into today's schema.** `zone` carries `UNIQUE (city_id, name)`, and the list repeats names constantly across parroquias — *San José* appears in Bolívar, Cacique Mara, Chiquinquirá and Cristo de Aranza; *La Consolación* in four; *Indio Mara*, *Rafael Urdaneta*, *Santa Clara*, *Los Andes*, *Nueva Vía*, *Manzana de Oro*, *Sabaneta*, *La Florida*, *La Pastora*, *5 de Julio*, *Los Planazos*, *Los Pinos*, *Los Olivos* likewise. The founder's own notes concede it: "la fuente contiene algunos nombres repetidos". A flat insert fails on the second *San José*. **A barrio is unique inside its parroquia, never inside a city** — that is the constraint the current model does not express.

**2. The seeded taxonomy is already geographically wrong.** `src/shared/db/seed.ts` files Chacao, Altamira, La Castellana, Los Palos Grandes, El Rosal and Las Mercedes under the city "Distrito Capital". **All six are in the state of Miranda** — the first five in Municipio Chacao, Las Mercedes in Baruta. Distrito Capital is Municipio Libertador alone. The founder anticipated this ("imaginate si agregamos el Distrito Capital que hay varias ciudades") without knowing it had already shipped.

### The model: search hierarchy, not administrative hierarchy

Venezuela's real hierarchy is `Estado (o Distrito Capital) → Municipio → Parroquia → barrio/sector` — four levels, and what people call "Caracas" spans **two federal entities**. Modelling that faithfully produces a four-step filter, and nobody rents that way: a tenant says "busco en Tierra Negra", never "en la parroquia Cacique Mara del municipio Maracaibo del estado Zulia".

Two navigable levels, plus one that is **a label and not a step**:

| Level | Example | Purpose |
|---|---|---|
| Área | Maracaibo · Caracas | The first choice. What the city-isolation rule protects |
| Zona | Tierra Negra · Chacao · San José | The real unit of search |
| *Padre* | Cacique Mara · Municipio Chacao | **Never navigated.** Disambiguates a suggestion and makes the zone unique |

The parent being a label is what keeps the filter exactly as shallow as it is today while letting *San José* exist four times. It also resolves Caracas honestly: one área holding zones from both Distrito Capital and Miranda, each showing its municipality.

- [x] 17.1 Add the parent level. `UNIQUE (city_id, name)` becomes uniqueness within the parent; the composite FK that guarantees D5 (`listing_zone_city_fk`) must keep working — it is the constraint that makes a Maracaibo listing physically unable to hold a Caracas zone, and it is not negotiable. **Hecho.** `UNIQUE (city_id, parent_id, category, name) NULLS NOT DISTINCT` — la `city_id` es la que permite que «Centro» exista en Maracaibo Y en Caracas
- [x] 17.2 **Correct the seed.** Six zones are filed under the wrong federal entity. Decide the área's name first: "Distrito Capital" is factually wrong for five of the six, and "Caracas" is the name people actually use and search for. This is a product decision, not a data fix. **Hecho, por el mismo commit que cierra 17.1/17.10/17.11/17.12 (`c005a3b`, 2026-08-22).** El `seed()` real ya no arma la taxonomía a mano: lee `docs/territorio/` a través de `buildTerritoryRows` y agrupa cada municipio en su área con `areaForMunicipality` (`listing-catalogue/infrastructure/territorio-areas.ts`), donde Chacao, Altamira, La Castellana, Los Palos Grandes, El Rosal y Las Mercedes caen bajo `Caracas` — nombrada así y no `Distrito Capital`, que es incorrecto para cinco de los seis — y ese nombre agrupa los cinco municipios del extinto Distrito Metropolitano (Bolivariano Libertador, Baruta, Chacao, El Hatillo, Sucre), Miranda y Distrito Capital a la vez. Lo que quedaba mal era código MUERTO y no la siembra real: `PROVISIONAL_TAXONOMY` en `src/shared/db/seed.ts`, un array sin ningún lector (ni `seed()`, ni `CityZoneSelect.tsx`, que ya no existe, ni ningún test) que todavía repetía el defecto de origen. Se borró en esta pasada — la corrección de la siembra ya vivía en `buildTerritoryRows`/`AREAS`, y dejar el array viejo era un dato incorrecto disponible para que alguien lo reintrodujera por accidente.
- [x] 17.3 Import Maracaibo's parroquias and zones. **The founder decided to load the full list** (2026-08-22) rather than a curated shortlist, which is only viable because of 17.4. **Hecho.** Lista completa: 5.796 lugares, 10 municipios, 81 parroquias
- [x] 17.4 **The zone search box, and it is now mandatory rather than a convenience.** A `<select>` of several hundred options is unusable on a phone; a box that suggests is not. Same mechanism as 14.18: a suggestion is a (filter, value) pair carrying its kind and scope — `San José · Cacique Mara`, exactly as the "Centro" collision between Maracaibo and Distrito Capital already required. Pure domain, no external service, no per-query cost, and it reuses the NFD accent strip shipped in `listing-discovery/domain/listing-url.ts` so "san jose" finds "San José". Works with JavaScript off as a `GET` whose results are links **Reconciliado 2026-09-04 — ya estaba construido, y el hueco real era 17.5/17.7.** El mecanismo de 14.18/14.19/14.20 (`suggestFilters`, `DrizzleSearchVocabulary`, `search-destination.ts`) ya es esta caja: reusa `slugify` (el despojo NFD), emite pares (filtro, valor) con `scope` — probado contra la colisión de "Centro" — y su piso es un `<form method="get">` cuyo servidor traduce y, cuando hay más de un lugar, dibuja `SearchChoice[]` como enlaces (`kind: "choices"`). No hacía falta una segunda caja: el `<select>` de cientos de opciones que el texto de esta tarea describe es exactamente el que `SearchFilters.tsx` dejó de usar en producción cuando `SearchPanel`/`SearchPill` tomaron el filtro de zona (14.30b/14.39-42) — no queda ningún `<select>` de zona sirviendo una pantalla real. Lo que faltaba de verdad es la 17.5/17.7
- [x] 17.5 **Order suggestions by listing count, descending.** This is what makes loading the full list safe. **A correction of my own earlier advice, recorded because the reasoning matters:** I argued against importing hundreds of zones because ~93% would show a count of zero, and the founder's own document rejects free-text search for exactly that reason ("devuelve vacío casi siempre y el sitio parece vacío"). With a search box that argument mostly collapses — nobody sees all of them at once, only what they typed toward. Ranking by real supply finishes the job: the zones where renting actually happens rise on their own, and the empty ones sink without disappearing. The catalogue decides, not a guess about which neighbourhoods matter **Hecho 2026-09-04.** `searchChoices` (`listing-catalogue/domain/search-destination.ts`) ordena sus sugerencias de zona por `count` descendente antes de convertirlas en opciones — orden estable, así que dos zonas sin conteo conocido conservan el orden del vocabulario. Para que el camino de servidor tuviera ese número, `DrizzleSearchVocabulary` (`listing-catalogue/infrastructure/drizzle-search-vocabulary.ts`) ahora agrega un `GROUP BY` de avisos activos por zona con el MISMO predicado que `DrizzleActiveZones` (`status = 'active'` y `expires_at > now()`) — mismo predicado a propósito, regla transversal 3. Probado en dominio (`search-destination.test.ts`, "ordena las zonas por avisos, de mayor a menor") y contra Postgres real (`tests/integration/catalogue-search.test.ts`, describe "la oferta real (17.5/17.7)": cuenta correctamente, excluye un vencido, y el tipo del conteo es `number` y no el string del `bigint`). El vocabulario acotado del panel (`boundedVocabularyOf`, 14.51) no necesitó tocarse: ya sólo trae zonas con avisos, así que la nueva ordenación de `searchChoices` alcanza igual
- [x] 17.6 **RESOLVED by the founder, 2026-08-22: never render a zero.** Zones with listings come first, showing their count; a zone with none shows no number at all — not "0", nothing. This supersedes F4's "una zona con 0 avisos se muestra atenuada con su conteo en cero", and it is the better rule: a column of zeroes is a catalogue advertising its own emptiness
- [x] 17.7 **Search and publish need opposite behaviour here, and conflating them breaks one of them.** In SEARCH, a zone with no listings must not be offered at all — transversal rule 4 says no option may lead to a void, and offering a zone that returns nothing is exactly that. In PUBLISH, every zone must remain selectable including the empty ones, because otherwise **a zone can never receive its first listing** and the taxonomy freezes at whatever it launched with. Same table, two different reads: the search side filters by supply, the publish side does not **Hecho 2026-09-04, y el lado de publicar ya estaba correcto.** El lado de publicar (`searchPublicationZones`, `listing-publication/domain/zone-search.ts` + `DrizzleZoneVocabulary`) nunca miró la oferta — ni siquiera llevaba un campo `count` — así que ya ofrecía toda zona, vacía incluida; se afirmó con una prueba nueva ("ofrece una zona sin avisos igual que una con oferta") en vez de darlo por sentado. El lado de búsqueda sí tenía el hueco: `searchChoices` ofrecía una zona en cero sin su número mas SIN excluirla — la prueba vieja "no escribe un cero al lado de una zona vacía" lo pedía así, y quedó corregida ("no ofrece una zona en cero — la oferta real decide, no sólo el nombre"). Ahora `searchChoices` descarta una zona cuyo `count` es exactamente `0` antes de convertirla en opción; `undefined` (nadie contó) sigue pasando, porque "no sé" no es "no hay". Probado en dominio y, contra Postgres real, con una búsqueda que sólo nombra la zona vacía y contesta `unknown` en vez de aterrizar en una ficha sin salida — la prueba directa de la regla transversal 4
- [x] 17.8 **The área "Caracas" spans several federal entities** (founder, 2026-08-22: "toda la capital cambia porque son varios estados que están dentro de Caracas"). Named `Caracas` because that is what people type, not `Distrito Capital`, which is factually wrong for five of the six seeded zones. Taxonomies for Caracas, Miranda and La Guaira are being prepared by the founder. **Hecho, `c005a3b`.** `AREAS` en `listing-catalogue/infrastructure/territorio-areas.ts` define `Caracas` como los cinco municipios del extinto Distrito Metropolitano — `Bolivariano Libertador` (Distrito Capital) más `Baruta`, `Chacao`, `El Hatillo` y `Sucre` (Miranda) — citando la Gaceta Oficial 36.906 y su derogación en la 41.308 como la razón por la que ningún nivel oficial puede expresar esa área. `tests/integration/seed.test.ts` prueba el resultado contra Postgres real: exactamente 5 filas en `city` (Caracas, La Guaira, Maracaibo, Cabimas, Santa Rita) y 5.796 en `zone`
- [x] 17.9 **Open: does La Guaira belong inside the Caracas área or is it its own?** It is a separate state and, for long-stay rental, a different market — someone searching Caracas is unlikely to want Catia La Mar or Macuto, which is a motorway away. Folding it in would make the city-isolation rule return results 40 minutes from what was asked for; keeping it separate costs nothing and can be merged later. **Not to be settled by whoever imports the list first**. **RESUELTO por el fundador, 2026-09-04: La Guaira es su PROPIA área, no parte de Caracas.** Es una entidad federal separada y, para alquiler de larga estadía, otro mercado — quien busca en Caracas difícilmente quiera Catia La Mar o Macuto, que quedan a una autopista de distancia. Meterla adentro haría que la regla de aislamiento por ciudad (D5) devolviera resultados a 40 minutos de lo que se pidió. Mantenerla separada no cuesta nada y se puede fusionar más adelante; fusionar primero y separar después sí cuesta. **Ya implementado, `c005a3b` (2026-08-22), antes incluso de que esta decisión se registrara por escrito**: `AREAS` en `territorio-areas.ts` define `La Guaira` como su propia área (municipio `Vargas` solo), con la misma razón documentada en su `rationale`. `tests/integration/seed.test.ts` fija el conteo en 5 áreas exactas — "Cinco areas, no dos: Caracas, La Guaira, Maracaibo, Cabimas y Santa Rita" — así que un futuro cambio que las fusionara rompería esa prueba antes de llegar a producción

### 17e. El dato real llegó, y cambia el modelo (2026-08-22)

The founder had another agent build `docs/territorio/` — 11,017 lines of curated territory, and it is rigorous: hierarchy from INE's DPT, postal codes from IPOSTEL, geometric containment from OSM, provenance tagged per entry, **categories never inferred**, duplicates never merged.

| | |
|---|---|
| Entidades federales | 4 — Distrito Capital, Miranda, La Guaira, Zulia |
| Municipios | 10 |
| Parroquias | 81 |
| Elementos sub-parroquiales | **5,705** |

**It independently confirms the seed defect found in 17.2**, and cites the Gaceta for it: *"Cuatro de los cinco no son Distrito Capital"* — Baruta, Chacao, El Hatillo and Sucre are Miranda.

**The collision numbers settle the schema question.** `Buena Vista` appears **12 times**, `San José` **11**, `El Carmen` **10**, `Los Pinos` and `Santa Ana` **9** each. `UNIQUE (city_id, name)` is not merely tight — it is unsatisfiable.

**And the finding that only real data could produce: the searchable unit is not one level of the hierarchy.** *Chacao* is a municipio AND a parroquia; *Altamira* is an urbanización inside that parroquia; *Sabana Grande* sits in parroquia El Recreo and nobody names the parroquia. People name places **across** levels. Four tables — one per level — would force search to UNION four queries and force `listing` to carry four nullable foreign keys.

- [x] 17.10 **One self-referencing `territory` table**, not four: `id`, `parent_id`, `kind` (estado|municipio|parroquia|elemento), `category` (barrio|sector|urbanizacion|conjunto|parcelamiento|caserio|localidad|otro), `name`, `ubigeo`, `postal_code`, `source`. ~5,800 rows, which is nothing for Postgres. **Hecho, `c005a3b`, evolucionando la tabla en vez de crear una nueva.** No se llama `territory`: se quedó llamándose `zone`, y la razón está escrita en `schema.ts` — renombrar bajo una clave foránea viva (`listing_zone_city_fk`) compra una palabra mejor al costo de una migración de datos. Todas las columnas pedidas están: `id`, `parent_id` (self-FK `zone_parent_fk`), `kind`, `category`, `name`, `ubigeo`, `postal_code`, `source`. 5.796 filas — 10 municipios, 81 parroquias, 5.705 elementos —, generadas por `territorio-parser.ts`/`territorio-import.ts` desde `docs/territorio/`, no transcritas
- [x] 17.11 **The `area` (Caracas, Maracaibo) is the product's invention and stays separate from the official hierarchy.** Caracas metropolitana crosses two federal entities — the document proves it with Gaceta Oficial 36.906 and its repeal in 41.308 — so no official level can express it. Keeping it apart is what allows the INE data to be re-imported later without overwriting a product decision. **Hecho, `c005a3b`.** No se llama `area`: la tabla `city` pasó a cumplir ese rol, con el mismo razonamiento anotado en su comentario de esquema. La agrupación en sí vive aparte de la jerarquía oficial, en `AREAS` (`territorio-areas.ts`) — un array de producto, no un dato del INE — y `areaForMunicipality` es la única función que cruza ambos mundos. Reimportar `docs/territorio/` no toca `AREAS` ni viceversa
- [x] 17.12 **D5 survives unchanged, with the same mechanism already in use**: `territory` carries `UNIQUE (id, area_id)` and `listing` keeps its composite foreign key. A Maracaibo listing remains physically unable to hold a Caracas zone — the guarantee is not weakened by the new depth. **Hecho, `c005a3b`/`55ed1b5`.** `zone_id_city_id_unique` — `UNIQUE (id, city_id)` sobre `zone` — no se tocó, y `listing_zone_city_fk` sigue siendo la clave foránea compuesta `(zone_id, city_id) -> zone(id, city_id)`. Probado contra Postgres real, no contra un mock: `tests/integration/zone.test.ts`, "rejects a listing whose city does not match its zone's real city" — intenta insertar un aviso con una zona real de un área pareada con el id de otra área y Postgres la rechaza con `23503` (foreign_key_violation); el mismo par correcto sí inserta, lo que prueba que el rechazo es por el cruce y no por una sentencia mal formada. `tests/integration/seed.test.ts` añade la prueba de extremo a extremo sobre las 10 filas sembradas: `SELECT count(*) FROM listing l JOIN zone z ON z.id = l.zone_id WHERE z.city_id <> l.city_id` da 0
- [x] 17.13 **Keep the provenance columns.** `ubigeo`, `postal_code` and `source` are what make a future re-import possible without losing curation. Dropping them because "the app does not render them" is how a dataset becomes unmaintainable. **Hecho.** `ubigeo`, `postal_code` y `source` viven en `zone`
- [x] 17.14 A parser from `docs/territorio/**.md` into the seed, so the taxonomy is regenerated rather than retyped. The source files carry a stable format — `### Parroquia <name>`, `#### <Category> (n)`, `- <Prefix> <Name>  \`[CP nnnn — SOURCE]\`` — and the seed must stay idempotent (task 2.3). **Hecho.** `listing-catalogue/infrastructure/territorio-parser.ts`

- [x] 17.15 **(nueva, 2026-09-05 — la trae el fundador desde producción, y NINGÚN agente la reprodujo todavía.)** **El paso 2 de publicar no ofrece ni una zona en `https://rentoru.com`, así que el fundador no puede pasar de ahí, y el flujo de publicar es el único camino que el producto tiene para crear oferta.** Sin paso 2 no hay aviso, y sin avisos no hay búsqueda, ni portada, ni ficha, ni contacto: la falla no degrada una pantalla, apaga la mitad del producto que produce el dato del que viven todas las demás. **Lo que hace que ésta sea una tarea sobre las PRUEBAS y no sobre una zona faltante, en palabras del fundador: la suite está verde y la funcionalidad no sirve, así que hay una prueba que no está haciendo su trabajo.** El 2026-09-05 `pnpm test` dio **2839/2839 sobre 245 archivos** en `dev`, con `typecheck`, `lint`, `lint:tokens`, `build` y `budget:bundle` en verde, y producción corre `origin/main`, hoy en `034fdb4` (PR #244, mergeado el mismo día) — o sea que **producción y `dev` están sincronizadas**, y el verde describe exactamente los bytes que fallan. **Y esta área se dio por cerrada hace dos semanas**: la Fase 17 existe para resolver la taxonomía de zonas, PR #231 cerró la 17.2, 17.8, 17.9, 17.10, 17.11 y 17.12, PR #232 cerró la 17.4, 17.5 y 17.7 — la 17.7 dice textualmente que *«el lado de publicar nunca miró la oferta, así que ya ofrecía toda zona, vacía incluida»* y la 17.8 fija **5.796 filas en `zone`** —, y sin embargo el paso 2 no ofrece ninguna. **Las cuatro pruebas que cubren este camino, y contra qué afirma cada una, medido leyendo los archivos y no de memoria: (1)** `app/publicar/paso/[paso]/atributos-y-zona.test.tsx` — 8 `it(`, cuatro de ellos del buscador de zona — renderiza la RUTA de verdad, pero `vi.mock("@/shared/db/client", () => ({ db: {} }))` en la línea **64** y `vi.mock(".../drizzle-zone-vocabulary")` en la **65-69** la desconectan de Postgres, y el `lookup` de `vi.hoisted` (líneas **53-62**) devuelve **dos zonas escritas a mano** (`altamira`, `chacao`) con `aliases: []`; **afirma contra un doble, y un doble siempre tiene zonas**. **(2)** `src/modules/listing-publication/domain/zone-search.test.ts` — **17** `it(` de dominio puro sobre un `SuggestionVocabulary` literal, sin base y sin red por diseño. **(3)** `tests/integration/publish-steps.test.ts` — **9** `it(` contra un Postgres REAL, y es la única que ejercita el SQL de `DrizzleZoneVocabulary`, pero **no la corre `pnpm test`**: el `include` de `vitest.config.ts` (líneas **34-42**) nombra `src/**`, `components/**`, `app/**` y tres archivos sueltos, y de `tests/integration/` sólo entra `tests/integration/support/**` (línea **59**) — el resto corre con `pnpm test:integration` (`.github/workflows/ci.yml:244`) contra un contenedor efímero; y además **inserta sus propias fixtures** (`INSERT INTO "zone"`, línea **101**) y las busca por un marcador `randomUUID` (líneas **45-64**) justamente para **no** ver lo que hay en la tabla, así que por construcción nunca puede afirmar que la taxonomía sembrada esté presente. **(4)** `tests/e2e/publish-access.spec.ts` — **3** `test(`, y ninguno pasa de la puerta: *«publishing is refused without a session»*. El navegador nunca llega al paso 2. **Ninguna de las cuatro puede ponerse roja porque una base real no tenga zonas, y ése es el hueco exacto.** **Causas candidatas, con la comprobación que confirma o elimina cada una — sin veredicto, porque ninguna está probada: (a) `zone` vacía o sin sembrar en producción.** La taxonomía **no llega por migración** — `rg -li "insert into .?zone" drizzle` no devuelve un solo archivo — sino por `pnpm db:seed` (`package.json:39` → `tsx src/shared/db/seed.ts`), un script manual que lee `docs/territorio/` del disco; ni el despliegue ni CI lo corren (CI sólo corre `pnpm db:test:seed:e2e`, `ci.yml:377`, contra el contenedor de e2e) y `docs/going-live.md` no menciona la siembra en ninguna de sus cuatro secciones. Comprueba o elimina: `SELECT count(*) FROM zone` contra la base de producción — 0 confirma, 5.796 elimina. **(b) `city` vacía, o zonas cuya ciudad no existe.** `searchPublicationZones` (`src/modules/listing-publication/domain/zone-search.ts:100-118`) **descarta toda zona cuya `cityId` no esté en `vocabulary.cities`**, y ese arreglo sale de un `SELECT id, name FROM city` sin filtro (`drizzle-zone-vocabulary.ts:118-122`): con `zone` llena y `city` vacía la lista sale vacía **sin un solo error en ningún registro**. Comprueba: `SELECT count(*) FROM city`, más `SELECT 1 FROM zone z LEFT JOIN city c ON c.id = z.city_id WHERE c.id IS NULL LIMIT 1`. **(c) No se buscó nada, y la pantalla en blanco es el comportamiento diseñado.** El paso 2 sólo consulta cuando hay `q` (`app/publicar/paso/[paso]/page.tsx:75-78`) y sin resultados `PublishStep.tsx:458` no dibuja lista: queda la caja y el «¿No está la tuya?». Comprueba: si la dirección llevaba `?q=` cuando el fundador lo vio. **(d) Una búsqueda de una sola letra.** `wordsOf` (`drizzle-zone-vocabulary.ts:40-47`) descarta toda palabra de menos de **2** caracteres, y con `words` vacío la consulta queda reducida a `eq(zones.id, raw)`, que no coincide con ningún nombre. Comprueba: reproducir con una letra y con tres. **(e) Un fallo del lado del servidor que no se ve como fallo.** `src/shared/db/client.ts:8-14` lanza si falta `DATABASE_URL` y `assertPooledConnectionString` rechaza una cadena no agrupada — los dos fallan RUIDOSAMENTE, así que este camino debería dar una pantalla de error y no una lista vacía. Comprueba: los registros de la función de Vercel para `/publicar/paso/zona`; una excepción confirma (e) y elimina (a) y (b). **Dos candidatas ya ELIMINADAS por lectura, anotadas para que nadie las vuelva a mirar: `zone_alias`** sólo **suma** resultados y nunca los quita (`drizzle-zone-vocabulary.ts:84-92`), así que una tabla de alias vacía no puede vaciar la lista; **y el formulario anidado**, porque el GET del buscador es HERMANO del POST que guarda (`PublishStep.tsx:240` lo dibuja fuera, y `atributos-y-zona.test.tsx:268` lo afirma comparando posiciones y no clases). **Esto bloquea también la 26.19**, cuya única verificación válida es una subida real de foto a través de este flujo: el paso de fotos es el 8 y está detrás del 2, así que mientras el paso 2 no ofrezca zona no hay forma de comprobar la política CORS del balde — el defecto se encontró justamente intentando verificarla. **Qué significa «hecho» acá, y son cuatro cosas, no tres:** (1) el defecto **reproducido** por un agente contra un entorno real, con la comprobación que lo reprodujo escrita; (2) la causa **probada**, no elegida de la lista de arriba; (3) el arreglo; y (4) **una prueba que habría estado ROJA antes del arreglo** — sin ella el hueco queda idéntico y el próximo despliegue lo repite con la suite en verde. **El costo de esa cuarta parte hay que decirlo entero y no es barato**: ninguna prueba de dominio ni ninguna prueba de ruta con doble puede cubrirla, porque el defecto no está en la lógica sino en el DATO del entorno; y ni siquiera un e2e que recorra el flujo alcanza tal como está hoy la tubería, porque `scripts/seed-e2e.ts` **borra la tabla** (`delete from zone`, línea 179) y siembra **4 zonas** fijas (líneas 38-41), así que probaría el código y no la presencia de la taxonomía. Las salidas honestas son dos y hay que elegir una a la vista: una afirmación de arranque sobre la base de producción del estilo de la 3.19 (`SELECT count(*) FROM zone` como comprobación de despliegue, que es lo que `docs/going-live.md` hoy no pide), o sembrar la taxonomía completa en el contenedor de e2e y hacer que un recorrido real del paso 2 sea el que se pone rojo. **CAUSA PROBADA el 2026-09-05, por consulta de SOLO LECTURA contra la base de producción, con el fundador confirmando que la `DATABASE_URL` del `.env` apunta ahí y autorizando la lectura. La candidata (a) queda confirmada y las otras cuatro descartadas.** Medido: `zone` = **10** filas contra las **5.796** que fija la 17.3, todas `kind = parroquia`, **todas con `ubigeo` NULL**; `city` = **2**, llamadas **«Distrito Capital»** y **«Maracaibo»**; `zone_alias` = **0**; `listing` = **22**, todos `active`; `user` = **3**. Las diez zonas son, textualmente, la lista PROVISIONAL que la 2.3 describe (*«ships PROVISIONAL Distrito Capital + Maracaibo lists»*): Altamira, Chacao, El Rosal, La Castellana, Las Mercedes y Los Palos Grandes bajo «Distrito Capital», más Bella Vista, Indio Mara, La Lago y Tierra Negra bajo «Maracaibo». **Esas seis son EXACTAMENTE las seis que la 17.2 llama «filed under the wrong federal entity»**, y la 17.2 está cerrada `[x]` con su commit `c005a3b` renombrando el área a «Caracas» — o sea que **el arreglo de código se entregó y la base nunca se enteró**. El buscador del paso 2 no está roto: existen diez parroquias en dos ciudades y ninguna coincide con el nombre de una zona real que alguien teclee. **Matiz del fundador, y corrige el encuadre de esta tarea: ese dato se insertó como data FICTICIA a propósito en su momento, así que esto no es necesariamente «alguien olvidó volver a correr el seed».** Es que **nadie definió nunca el traspaso** de la carga provisional de demostración a la taxonomía real — ninguna tarea, en ninguna fase, se hizo dueña de «la taxonomía llega a producción», y por eso el hueco no tiene culpable sino ausencia de dueño. **Sigue SIN verificarse, y se escribe como tal:** si el seed posterior a `c005a3b` llegó a correr contra producción y no hizo nada, o si nunca corrió, no está establecido — lo único probado es el estado actual de las filas. **Y una advertencia medida antes de que alguien la aprenda cara: NO correr `pnpm db:seed` contra producción tal como está.** Tres trampas, las tres leídas en `src/shared/db/seed.ts`: (1) el upsert de ciudades apunta a `name` (línea **309**), así que «Caracas» **no colisiona** con «Distrito Capital» — inserta una ciudad nueva y deja seis zonas huérfanas colgando de la vieja; (2) el upsert de zonas apunta a `id` (línea **320**) y el id es `stableId(key)`, así que si la clave cambió entran filas nuevas y **las diez viejas se quedan**; (3) los 22 avisos sostienen la clave foránea compuesta `(zone_id, city_id)` contra esas zonas viejas, así que borrarlas sin más los rompe. Sumado a que `seed()` (línea **283**) es una sola función sin bandera que **también** inserta los usuarios (línea **357**) y los avisos de demostración (línea **429**), partir la taxonomía de la demo es requisito previo a cablear nada al despliegue. **Lo que sigue sin verificarse:** nadie reprodujo la pantalla en un navegador, y los 2839 verdes y el estado de `origin/main` vienen de la medición del fundador el 2026-09-05, no de una corrida de esta sesión. **HECHA el 2026-09-06, y las cuatro partes se cumplieron.** **(4) La prueba que habría estado ROJA**: `scripts/taxonomy-smoke.ts` (PR #245), enganchada a `deploy-migrate.mjs` dentro de `vercel-build`, que **falla el build** en vez de advertir. Es **direccional y no de igualdad** a propósito — falta una zona que el formulario ofrece, publicar está roto; sobra una fila que la taxonomía no nombra, no es asunto del gate —, porque exigir igualdad bloquearía todo despliegue hasta borrar datos de gente, y porque el contenedor de integración llega legítimamente a 5.813 zonas cuando otras suites insertan las suyas. Compara **ids derivados de `territoryId`** contra `buildTerritoryRows(readTerritoryDocuments())` leído en el momento, nunca contra un `5796` escrito a mano. **Se descartó por evidencia la otra «salida honesta» que esta misma tarea proponía**: sembrar la taxonomía en el contenedor de e2e y recorrer el paso 2 habría estado VERDE, porque prueba el código —que ya funcionaba, la 17.7 lo probó— y no la presencia del dato en el entorno; encima `publish-access.spec.ts` no pasa de la puerta por falta de sesión. **(3) El arreglo**: `seed()` se partió en `seedTaxonomy` y `seedDemo` (el requisito previo que esta tarea nombra), y `scripts/reseed-taxonomy.ts` (PR #246) borra lo provisional y siembra lo real, con ensayo por defecto y **una guardia que se niega si hay datos de gente**. **Esa guardia disparó en la primera corrida real contra producción y evitó un desastre**: `contact_reveal_event` tenía 1 fila —un clic de prueba del propio fundador del 2026-08-23—, y como el orden de borrado es derivadas → fotos → avisos y **no hay transacción** (el driver HTTP de Neon no soporta transacciones interactivas), sin ella se habrían borrado las 36 fotos y sus 180 derivadas para después reventar en `delete from "listing"` por el RESTRICT, dejando producción rota a la mitad y sin vuelta atrás. Se borró esa única fila por id, con su contenido respaldado, **sin aflojar la guardia**. Tres tablas que este inventario NO había mirado la sostienen: `contact_reveal_event` (`schema.ts:761`), `listing_report` (`:909`) y `moderation_action` (`:948`). **(1) y (2) Reproducción y causa**: ejecutado el 2026-09-06 con `pnpm db:reseed:taxonomy --confirm`. Producción pasó de `city`=2 / `zone`=10 / `zone_alias`=0 / `listing`=22 a **`city`=5** (Cabimas, Caracas, La Guaira, Maracaibo, Santa Rita), **`zone`=5796**, **`zone_alias`=3547**, `listing`=0, `user`=3 intactos, y **0 violaciones de aislamiento por ciudad (D5)** — medido por consulta independiente, no por el script. **Con esto el defecto de la 17.2 quedó arreglado EN LA BASE**, dos semanas después de que su commit `c005a3b` se entregara: «Distrito Capital» desapareció y «Caracas» ocupó su lugar. **El fundador confirmó en el navegador el 2026-09-06 que puede publicar y que subir fotos funciona**, que es la parte que ningún agente podía firmar. **180 objetos de R2 quedaron HUÉRFANOS y no borrados**, bajo `photos/b7034a05-…/`: la herramienta los nombra a propósito y nunca toca el bucket, porque un objeto de más cuesta centavos y uno borrado por error no vuelve. Falta limpiarlos a mano. **Y sale un defecto NUEVO que sólo se ve con la taxonomía real cargada, anotado en la 17.16**: buscar «chacao» en el paso 2 devuelve 5 opciones pero **sólo 4 distinguibles**.
- [x] 17.16 **(nueva, 2026-09-06 — sale de verificar la 17.15 contra producción, y NO se pudo ver antes porque hacía falta la taxonomía real cargada.)** **El paso 2 de publicar ofrece dos opciones IDÉNTICAS, y quien publica no puede elegir entre ellas.** Medido llamando `searchPublicationZones` con `DrizzleZoneVocabulary` contra la base de producción ya sembrada: `"chacao"` devuelve **5 opciones pero sólo 4 distinguibles** — `Chacao · Chacao · Caracas` aparece **dos veces**, con el mismo `label` Y el mismo `scope`. El mecanismo en sí funciona: `"bella vista"` devuelve **8 de 8 distinguibles**, y el `scope` separa correctamente Macuto · La Guaira de Catia La Mar · La Guaira de Coquivacoa · Maracaibo. El hueco es de casos específicos, no del diseño. **Por qué importa y no es cosmético**: `PublicationZoneOption.scope` está documentado en `src/modules/listing-publication/domain/zone-search.ts:51-52` como *«"Municipio Chacao · Distrito Capital". Es lo unico que desambigua»*, y acá no desambigua — elegir la fila equivocada manda el aviso a una zona distinta de la que el publicante quiso, en silencio y sin forma de notarlo. La 17.4 pide textualmente pares que carguen *«its kind and scope»*, y el `kind` es justo lo que falta: la hipótesis a comprobar es que hay una **parroquia** «Chacao» y un **elemento** «Chacao» bajo esa misma parroquia, con lo cual `parroquia · ciudad` no puede separarlos. **Qué significa «hecho»**: (1) la causa probada leyendo las filas reales —no elegida de esta hipótesis—; (2) la regla de desambiguación decidida por el fundador, porque **qué se muestra es una decisión de producto y no del que arregla** (¿se agrega el `kind`? ¿se colapsan las filas duplicadas? ¿se prefiere la más específica?); y (3) una prueba de dominio sobre un vocabulario que contenga el choque, que habría estado ROJA — el caso es reproducible sin base, así que acá sí alcanza el dominio, a diferencia de la 17.15. **Cuidado con el atajo**: deduplicar por `label + scope` haría desaparecer una zona REAL de la lista, y entonces esa zona no podría recibir nunca su primer aviso — exactamente lo que la 17.7 prohíbe para el lado de publicar. **HECHA el 2026-09-11. La hipótesis original quedó DESCARTADA por lectura de las filas reales**: no hay una parroquia «Chacao» y un elemento «Chacao» bajo la misma parroquia con `parroquia · ciudad` sin poder separarlos — `scopeOf` ya distingue una zona con padre de una sin padre. **La causa real, medida contra `rentas_test` con la consulta `SELECT a.alias, z.name, p.name FROM zone_alias a JOIN zone z ON z.id = a.zone_id LEFT JOIN zone p ON p.id = z.parent_id WHERE a.alias ILIKE '%chacao%'`**: existe la zona **8530d288…**, nombrada «Chacao», con padre «Chacao» (`label` "Chacao", `scope` "Chacao · Caracas"), y el alias **«Chacao» → «Oficina Postal Telegráfica Chacao»** (zona **c23fdfa8…**), cuyo padre es TAMBIÉN «Chacao». Como `label` mostraba el alias con el que se encontró la fila y no el nombre real, las dos zonas —reales y distintas— salían con el mismo `label` ("Chacao") y el mismo `scope` ("Chacao · Caracas"): indistinguibles. **La decisión del fundador, ya tomada y aplicada tal cual**: el `label` nombra SIEMPRE la zona real, aunque el match haya venido de un alias — el alias sigue encontrando la zona, deja de ser lo que se muestra. **El arreglo**: en `searchPublicationZones` (`zone-search.ts`), `consider(zoneId)` ya no recibe un `label` desde el llamador — arma el label con `zone.name` siempre, tanto si el match vino del loop de alias como del loop de zonas. El dedup por `zoneId` (el `Set seen`) YA EXISTÍA antes de esta tarea y no se tocó: ninguna zona real desaparece, cada `zoneId` distinto sigue apareciendo una sola vez. El doc-comment de `PublicationZoneOption.label` (línea 49-53) se corrigió: ya no dice "el nombre o el alias por el que se la encontró", dice que siempre es el nombre real. Se revisaron los dos consumidores de `.label` (`app/publicar/PublishStep.tsx:471`, `app/measure/page.tsx`) y ninguno depende de que sea el alias — sólo lo muestran. **`DEFAULT_LIMIT = 8`**: como el dedup por `zoneId` ya evitaba que una misma zona ocupara dos cupos, este arreglo NO cambia cuántas zonas entran en el límite de 8 — sólo cambia el `label` de las que ya entraban. **La prueba de dominio pedida, RED antes del arreglo**: `zone-search.test.ts`, caso *"el label nombra siempre la zona real, aunque se la haya encontrado por alias (17.16)"`, con un vocabulario mínimo que reproduce el choque medido (zona "Chacao" con padre "Chacao", y alias "Chacao" sobre "Oficina Postal Telegrafica Chacao" con padre "Chacao"); falló con `expected 1 to be 2` sobre la cantidad de firmas `label|scope` distintas, confirmando el choque sin necesitar base. Además se corrigió la prueba preexistente *"encuentra una zona por su alias"*, que afirmaba el contrato viejo (`label` = alias); ahora afirma `label` = "La Lago", el nombre real. **Verificado contra `rentas_test` con `DrizzleZoneVocabulary` real, antes y después**: ANTES, `"chacao"` traía la zona 8530d288 y la c23fdfa8 con `label` "Chacao" y `scope` "Chacao · Caracas" LAS DOS (7 de 8 firmas distintas). DESPUÉS, c23fdfa8 pasa a `label` "Oficina Postal Telegrafica Chacao" y 8530d288 se queda con "Chacao" — 8 de 8 firmas distintas, mismos 8 `zoneId`, ninguna zona perdida. `pnpm test:unit` 2950/2950, `pnpm test:integration` 410/410, `pnpm tsc --noEmit` sin errores, `pnpm biome check` limpio sobre los dos archivos tocados.

- [x] 17.17 **(nueva, 2026-09-06 — sale de un fallo de CI en un PR que sólo tocaba `tasks.md`, o sea que el código no había cambiado.)** **`tests/integration/catalogue-search.test.ts` pasa o falla según si la taxonomía real está sembrada cuando le toca correr, y eso lo decide el ORDEN de los archivos, no el código.** Falló en el PR #248 con `expected 'choices' to be 'route'` (línea **212**, «pega a la ruta los filtros que la misma frase trae») y pasó al relanzar el mismo commit; el mismo código había pasado en el #246, en el #247 y en `dev` (`ab2af30`). **El mecanismo está PROBADO, no es azar: (1)** `wordsOf` (`drizzle-search-vocabulary.ts:57-63`) conserva toda palabra de **2 caracteres o más**, así que **`en` entra como término de búsqueda**; **(2)** la consulta es `ilike('%en%')` sobre `zone.name` (`:95`), subcadena en cualquier posición, y **1.052 de las 5.796 zonas** contienen «en» — medido contra producción el 2026-09-06: «23 de Enero», «Alimentador San Bernardino», «18 de Octubre Sector Norte Independencia»…; **(3)** con `LOOKUP_LIMIT = 60` (`:35`) y `ORDER BY name ASC`, la consulta devuelve 60 zonas reales que no tienen nada que ver con la frase, se suman al candidato legítimo que el alias trajo, y el destino cae en `choices` en vez de `route`. Con la base casi vacía sólo existe la fixture propia del test y resuelve a `route`; con la taxonomía sembrada, nunca. **La prueba está bien escrita en lo suyo** —inserta su ciudad, sus zonas y su alias con `randomUUID()`, y los borra en su `afterAll`—, pero `vocabulary.lookup()` consulta la base ENTERA, así que su resultado depende de filas que ella no puso ni controla. **Y esto empeoró por mi culpa el 2026-09-06**: `taxonomy-smoke.test.ts` y `reseed-taxonomy.test.ts` siembran la taxonomía completa y la dejan puesta, así que hay dos archivos más que pueden correr antes y volcar el dado del lado malo. **Por qué importa más que un rojo ocasional**: una suite que falla de a ratos enseña a apretar «relanzar» sin leer, y eso es exactamente lo que la 17.15 vino a combatir — el verde dejó de significar algo y la funcionalidad no servía. Un test que se arregla solo al reintentarlo es peor que uno rojo, porque el rojo al menos se mira. **Qué significa «hecho»**: la prueba resuelve a `route` de forma determinista, corra en el orden que corra y esté la taxonomía o no. **Ojo con los dos atajos malos**: fijar el orden de los archivos esconde el problema en vez de arreglarlo y se rompe solo el día que alguien agregue un archivo; y bajar `LOOKUP_LIMIT` cambia el comportamiento del producto para acomodar una prueba. **Anotado adentro, y es una sospecha SIN probar, no un defecto**: que `en` sea una palabra buscable puede estar ensuciando también las búsquedas de gente real, porque una frase natural como «apartamento **en** Altamira» arrastra 60 zonas irrelevantes. **Hoy NO se puede afirmar y se escribe así a propósito**: producción tiene **1 aviso vivo** al 2026-09-06, así que 5.795 zonas están en cero y la 17.7 las excluye de la búsqueda por diseño — para no mandar a nadie a una pantalla vacía. Ese efecto tapa por completo cualquier efecto de `en`, y los dos no se pueden separar hasta que haya oferta repartida. Se revisa cuando haya diez o veinte avisos en varias zonas, y recién ahí se decide si hacen falta palabras vacías, un mínimo de 3 caracteres, o nada.

  **HECHA el 2026-09-11, confirmando el mecanismo por medición directa contra `rentas-pg` con la taxonomía real ya sembrada (5.796 zonas, 3.547 alias) — no por lectura.** Con el fixture de la prueba (`ZONE_ALIAS = "Bellavistona <uuid>"`), la frase con «en» encontraba su propio alias en la mayoría de las corridas porque «Bellavistona» ordena temprano en el alfabeto — pero era suerte de nombre, no una garantía: reemplazando el alias por uno que ordena tarde (`"Zetavistona <uuid>"`), la MISMA consulta, contra la MISMA base, con el MISMO código, deja el alias fuera de `vocabulary.lookup()` — `aliasFound: false`, `zoneFound: false` — y `resolveSearchDestination` cae a `choices`, reproduciendo exactamente `expected 'choices' to be 'route'` del PR #248. Confirmado también en la otra dirección: la MISMA frase con «en» removida encuentra su alias siempre, con cualquiera de los dos nombres. **Causa exacta, medida y no supuesta:** no son sugerencias adicionales de zonas irrelevantes las que ganan — el dominio ya descarta esas por comparación de subcadena estricta, sin coincidencia difusa (`suggest-filters.ts`) —, sino que el propio alias de la prueba puede quedar EXCLUIDO de la fila 60 de `LOOKUP_LIMIT` cuando bastantes de las ~1.052 zonas reales que contienen «en» ordenan alfabéticamente antes que él; cuántas ordenan antes depende de qué otras filas existan en ese momento, que es justamente lo que el ORDEN de archivos decide.

  **El arreglo, y por qué no es un atajo de los dos prohibidos.** `tests/integration/catalogue-search.test.ts`, la prueba "pega a la ruta los filtros que la misma frase trae": se quitó la palabra «en» de la frase (`apartamento amoblado en ${ZONE_ALIAS} hasta 400` → `apartamento amoblado ${ZONE_ALIAS} hasta 400`). No es fijar el orden de los archivos (ningún archivo se reordenó ni se le puso una dependencia) y no es bajar `LOOKUP_LIMIT` (el producto no cambió). Es la tercera salida que la tarea pedía sopesar: la prueba pasa a afirmar sobre una vocabulario que ella controla, porque «en» es precisamente la única palabra de esa frase que es simultáneamente (a) un `STOPWORDS` del propio dominio (`suggest-filters.ts:91`) — no aporta nada a la decisión de a dónde ir, `resolveSearchDestination` la ignora igual con o sin ella — y (b) una subcadena de ~18% de la taxonomía real, la única palabra de la frase capaz de disparar la competencia de `LOOKUP_LIMIT`. Ninguna otra palabra de la frase («apartamento», «amoblado», «hasta», el alias con su UUID) colisiona con nombres reales de lugares. Quitarla no reduce lo que se prueba: sigue siendo texto libre con dos filtros y una zona por alias, resuelto contra Postgres real y no contra un doble; sólo deja de depender de cuántas filas ordenan antes de un conector que el dominio de todas formas descarta. Probado 8 corridas seguidas con UUID nuevo cada vez, taxonomía real puesta: 9/9 verde en las 8. `pnpm test:integration` completo: 44/44 archivos, 410/410 pruebas. `pnpm test:unit`: 2949/2949. `pnpm tsc --noEmit` y `pnpm biome check` sobre el archivo: limpios (el único hallazgo de `biome check` a nivel de repo es preexistente, en `photo-derivatives.test.ts`, no tocado acá).

  **No se tocó producción.** `src/modules/listing-catalogue/infrastructure/drizzle-search-vocabulary.ts` queda igual — `wordsOf`, `LOOKUP_LIMIT` y el `ILIKE` sobre `zone`/`zone_alias` siguen resolviendo igual para cualquier visitante real; sólo cambió el texto que ESTA prueba envía. La opción de acotar `lookup()` por ciudad —la primera que la tarea pedía sopesar— se descartó por lectura directa y no por conveniencia: el único llamador de producción es `app/page.tsx`, y su propio comentario dice **"Va sin ciudad a propósito: en `/` no hay ninguna elegida"** — el buscador del inicio es deliberadamente global, así que acotar por ciudad ahí habría sido un cambio de comportamiento del producto disfrazado de arreglo de prueba, no una opción legítima.

  **La sospecha del texto sobre `en` ensuciando búsquedas reales sigue SIN confirmarse ni descartarse aquí, a propósito** — la tarea la deja para cuando haya oferta repartida en varias zonas, y ésta no es esa tarea. Queda anotado para quien la retome: el mecanismo que hoy la oculta (5.795 de 5.796 zonas en cero, excluidas por la 17.7 antes de llegar a la caja) es el mismo `LOOKUP_LIMIT`/`ORDER BY name ASC` que acá se demostró capaz de excluir una fila legítima — vale la pena mirar los dos juntos cuando llegue el momento, no por separado.

## Phase 18: Publicar — nueve pasos (founder, 2026-08-22)

**La regla del fundador sobre qué se puede corregir después de publicado (2026-09-01), en sus palabras y anotada acá porque REEMPLAZA a la tabla campo por campo de la 18.14 (2026-08-29):**

> Se puede corregir cualquier dato menos el de la zona, y eso porque va con la URL del SEO y eso no puede cambiar. Si quiere cambiar la zona después de publicado hay que desactivar ese anuncio y crear otro y ya.

Aquella tabla enumeraba lo permitido, así que cada campo que no llegó a nombrar quedó cerrado por omisión y no por decisión; ésta enumera lo cerrado, que hoy es **la zona, la ciudad —que la zona determina— y `publisherType`**, este último por la razón de integridad del 2026-08-29 que la regla nueva no discute (ver la tensión anotada en la 18.27). Lo que un futuro lector necesita es esta frase, no el delta: la regla vive aplicada en el jsdoc de `src/modules/listing-publication/domain/listing-edit.ts`.

The founder delivered the publish specification with mobile and desktop designs, and it **replaces the two-step form that ships today**. The brief's own "es un formulario, no un embudo de cinco pasos" is explicitly overturned: nine steps, one question per screen, with a review screen before publishing.

**A correction of my own earlier framing, recorded because it changed the estimate.** I told the founder publicar was "already built, so this is a reskin plus new fields". That was true of the two-step form. It is not true of this: nine screens, per-step server persistence, a review screen, and back-navigation that preserves forward steps is a **rebuild of the delivery layer**. What survives untouched is everything that matters most — `validatePublishableListing`, the photo pipeline, duplicate detection, and the publish use case. The domain was built for this; the screens were not.

### 18a. Decisions the founder made here that close open questions

- [x] 18.1 **A failed photo upload publishes with the rest.** "Se publica con las que subieron y se avisa cuál faltó" — this answers the edge case raised on 2026-08-21 and left open
- [x] 18.2 **Phone verification happens AFTER the listing is saved**, not before. The listing exists as *pending verification*, so abandoning at the code loses nothing and closing the browser mid-flow is no longer an edge case. Asking for a code before the listing exists is asking for effort with nothing yet earned
- [x] 18.3 **The city is never asked — it is derived from the zone.** Sound, and stronger than what ships: `zone` already carries `city_id` under `UNIQUE (id, city_id)`, which is the constraint `listing_zone_city_fk` depends on. Deriving cannot produce a mismatched pair, whereas asking twice can

### 18b. What the new flow needs that does not exist

- [x] 18.4 **A new listing status: `pending_verification`.** The enum ships as `active | expired | hidden`; F1 additionally needs `pending_moderation`, so it grows to five. Verify what already holds: `DrizzleListingSearch` filters `status = 'active'` unconditionally, so a pending listing cannot leak into search by accident — assert it rather than assume it. ~~**Hecho, PR #94.** `pending_verification` en el enum~~ — **corrección (2026-08-29, verificado al construir la 18.16):** `schema.ts:496` sigue diciendo `"active" | "expired" | "hidden" | "draft"` y la cadena `pending_verification` no aparece en ninguna parte del código. Lo que sí quedó hecho es la otra mitad de la tarea: el aviso se guarda ANTES de pedir el código.
- [x] 18.5 **Server-side draft, one row per step, keyed by session.** The shipped draft is a 10-minute cookie of ~4 KB, and the founder is right that it cannot carry nine steps plus photo state. New storage, and with it a new question the project has not had: abandoned drafts accumulate, and "no podemos borrar data real" (11b.6) has to be reconciled with a draft that nobody will ever finish. **Parcial, PR #94.** Cookie de 30 minutos y DOS cookies, no una: la descripción sola a 1.200 caracteres acentuados roza los 4 KB, y hay un test que mide el peor caso. El borrador servidor pedía una tabla nueva, y las migraciones estaban fuera del alcance del agente. Es el mínimo que la propia spec acepta.
- [x] 18.6 **`title` has no maximum today.** The spec sets 90 characters; the domain has `title.required` and nothing else. A new violation code plus its Spanish copy. **Hecho, PR #94.** Tope de 90 agregado a `publishable-listing.ts`, extendido y no reemplazado.
- [x] 18.7 **`referencia`** — free text, optional, shown only on the ficha. Never filtered, never indexed. This is the field that replaces Google Places, and the founder's reasoning for rejecting it is worth keeping: a formatted address is not the product's taxonomy, and four shipped things depend on the zone being a closed list — the search filter, the per-zone counts, the `/alquiler/<ciudad>/<zona>/…` URL, and the zone landing pages **CIERRA el 2026-08-30.** La regla, la columna, el camino de escritura, la ficha y —lo que importaba— la prueba de la negativa.

  **Medido antes de escribir una línea, y la mitad del enunciado ya shipeaba.** El campo existía en el formulario desde la 18.16: `PublishStep.tsx` dibujaba el control, `step-values.ts` lo leía, `draft.ts` lo llevaba en la cookie y `CHANGE_FIELD_LABEL` ya tenía su nombre en castellano. **Lo que faltaba era el otro extremo**: `listing` no tenía columna, así que `publishFromReview` no la mandaba y ningún aviso publicado la conservaba. Es la séptima vez que este cambio encuentra la forma «pieza construida, sin llamador» —el anti-fraude de fotos, la pastilla, el importador, `canImportListings`, las dos de la 9.28, la pantalla de editar de la 18.20— y la primera en que el hueco estaba del lado de la base.

  **La migración: `drizzle/0017_skinny_the_order.sql`, una sentencia.** `ALTER TABLE "listing" ADD COLUMN "reference" text;`. **Nulable, y ése es el estado final y no un paso intermedio**, dicho en voz alta porque el precedente de la 14.2 (`drizzle/0008`) es el contrario: aquel patrón —nulable, rellenar, exigir `NOT NULL`— existe para columnas que TIENEN que terminar en `NOT NULL` sobre filas vivas. Ésta no: el campo es opcional por decisión del fundador, «sin referencia» es un hecho verdadero de todos los avisos publicados hasta hoy, y rellenarla inventaría una seña que nadie escribió. **Los dos guardianes del despliegue la aceptan y se comprobó en vez de suponerse**: `deploy-migrate.mjs` no encuentra ninguna sentencia destructiva (no lleva ni necesita el marcador `allow-destructive`), y el chequeo de humo de la 11b.5 la conoce **sin tocarlo**, porque `expectedTableShapes()` deriva las columnas de `schema.ts` en vez de una lista escrita a mano — verificado contra el contenedor: `schema.ts espera listing.reference: true` y cero deriva. **El SQL lleva un comentario escrito a mano** por una razón: en el diff este `ADD COLUMN "reference"` aparece al lado de un `external_reference` que ya existe, y son dos cosas distintas —aquélla es la llave de idempotencia de la importación en lote, protegida por un índice único; ésta la teclea una persona y no la lee ningún índice.

  **La regla, y por qué el campo libre necesitaba una.** `MAX_REFERENCE_CHARACTERS = 120` en `publishable-listing.ts`, con su código `reference.tooLong`. La especificación no da número, así que se eligió y se justificó: `text` no tiene techo en Postgres y esta columna se dibuja siempre que existe, o sea el mismo defecto que `MAX_DESCRIPTION_CHARACTERS` se creó para cerrar. **120 es la longitud mínima que este producto acepta como descripción**: pasado ese punto lo escrito dejó de ser una seña y es una descripción, y la ficha ya tiene una. **El número se escribe entero y NO se deriva de `MIN_DESCRIPTION_CHARACTERS`** aunque coincidan: si mañana el mínimo de la descripción baja, la referencia no tiene por qué encogerse con él. Lo prueban *«acepta una referencia de 120 caracteres y rechaza la de 121»*, *«cuenta la referencia en puntos de codigo, igual que el titulo y la descripcion»* y *«una referencia en blanco no es una referencia, y no se rechaza»* (`publishable-listing.test.ts`), con la afirmación sobre la constante en una prueba aparte para que ninguna derive su valor esperado del mismo número que el sujeto usa. **Mutación**: `> MAX_REFERENCE_CHARACTERS` → `+ 1` pone rojas 2 de 67, las suyas; restaurada por copia y sha256 comparado.

  **Nada se reescribió: la unión, el `Record` y la tabla de campos hicieron su trabajo.** Agregar el código dejó de compilar en los cuatro lugares que la garantía nombra —`STEP_FOR_VIOLATION`, `PUBLISH_VIOLATION_COPY`, `LISTING_VIOLATION_FIELD` y el `PUBLISH_COPY` del importador— y `tsc` los nombró uno por uno, que es exactamente para lo que están escritos como `Record` sobre la unión. **En el importador la frase es inalcanzable hoy y se escribió igual**, por el mismo criterio con que la 18.13 dejó vivos `cityId.required` y `cityId.unknown`: el archivo no tiene columna de referencia, pero el importador llama al MISMO validador.

  **El campo se mudó al borrador en vez de quedar al lado.** `reference` colgaba de `PublicationDraft` con la razón escrita adentro —«`listing` no tiene columna para él»—, y esa razón se venció con la migración. Ahora vive en `DraftListingValues`, o sea que lo ve el validador (que es lo único que hace de su tope una regla del producto y no una línea dentro de una pantalla, AGENTS.md §1) y llega solo hasta `publishListing`. **La mudanza BORRA código en vez de agregarlo**: se fueron el caso especial de `applyStepAnswers`, el `| "reference"` de `ChangedField`, su entrada suelta en `CHANGE_FIELDS` y los dos ternarios de `describeDraftChanges`.

  **Lo que prueba que llega hasta la fila es una prueba de integración y no un doble** (`tests/integration/listing-reference.test.ts`): un repositorio en memoria devuelve lo que se le guardó porque está escrito para devolverlo, y habría pasado igual el día anterior a la migración. *«la columna existe y el valor vuelve entero, con acentos y todo»* —con «í» y un emoji, porque el valor viaja por una cookie en base64url— y su par *«un aviso sin seña guarda NULL, no una cadena vacía»*, que existe porque una sola afirmación aceptaría las dos respuestas y porque guardar `''` haría que la ficha dibujara un párrafo vacío. **Mutación**: quitar `reference: listing.reference ?? null` del adaptador pone rojas 2 de 5; restaurada por sha256.

  **La negativa, que es la garantía de la tarea y la que nadie escribe.** «Nunca se filtra, nunca se indexa» se probó en tres frentes, y en los tres la prueba es sobre el conjunto y no sobre el nombre `reference` —una prueba que dijera «`SEARCH_QUERY_NAMES` no tiene `reference`» sólo cazaría a quien eligiera ESE nombre:

  1. **El vocabulario de filtros es cerrado.** *«son catorce y ninguno más: un filtro nuevo tiene que pasar por acá»* (`search-query.test.ts`) fija los catorce nombres por valor. Cae con cualquier parámetro nuevo, incluido uno llamado `cerca`. Su par de comportamiento es *«una seña colgada de la dirección no cambia la búsqueda»*, que arma la misma dirección con la seña bajo cuatro nombres y afirma que produce el mismo criterio.
  2. **El SQL real no la distingue.** *«con todos los filtros puestos, los dos avisos vuelven igual»* (integración) publica dos avisos idénticos SALVO la seña y les pide la búsqueda con los ocho filtros a la vez; los dos vuelven. Su par, *«ningún resultado transporta la seña, ni siquiera escondida en una propiedad»*, cierra el `select`: una tarjeta que la cargara la publicaría en la lista, en las sugerencias de la ficha y en la portada, o sea en tres páginas que un rastreador sí lee.
  3. **No entra en el JSON-LD, y las dos mitades se afirman sobre los MISMOS bytes.** *«no la emite en el JSON-LD, que es el canal que un buscador cita sin abrir la pagina»* (`ficha-servida.test.tsx`) comprueba que la respuesta lleva la seña en el cuerpo y NO en el documento estructurado, y que la zona sí sigue ahí. Afirmar sólo lo segundo pasaría también si la ficha hubiera dejado de dibujarla.

  **Las mutaciones de la negativa son la parte que vale**, porque cada una es el cambio que alguien haría de verdad: agregar `reference: "referencia"` a `SEARCH_QUERY_NAMES` y leerlo en `buildSearchCriteria` pone rojas las 2 del vocabulario; poner `is null` en el `WHERE` de la búsqueda pone rojas las 2 de integración; agregar `reference: listings.reference` a su `select` pone roja 1; y enriquecer la descripción del JSON-LD con la seña —`[description, reference].join(" ")`, que es como esto se rompería de verdad— pone roja exactamente 1, la del JSON-LD, y ninguna otra. Todas restauradas por copia con sha256 comparado.

  **La decisión sobre el documento estructurado, dicha y no supuesta: NO se emite.** `StructuredDataListing` no la lleva, y es deliberado en vez de omitido. Emitir una seña en el JSON-LD la entrega a un buscador como un dato de ubicación al lado de la zona: `PostalAddress` no tiene campo para un punto de referencia, y meterla en `description` o en `containedInPlace` afirmaría que es taxonomía. **Eso es una forma de indexar aunque no exista ningún filtro**, y es exactamente lo que se rechazó de Google Places — no por costo, sino porque una dirección formateada no es la taxonomía del producto.

  **La pantalla de editar NO la ofrece, y esto es lo que dice la tabla del fundador.** La 18.14 nombra campo por campo lo que una edición puede tocar —título, descripción, precio, habitaciones, baños, metros², fotos, contacto, y el `NO` del tipo de publicador— **y la referencia no está en esa tabla**, porque el campo no existía el día que se decidió. La tabla no la permite ni la prohíbe, y AGENTS.md §7 dice qué hacer con eso: fallar cerrado. Ofrecerla sería tomar por el fundador una decisión de producto y dejar falsa la frase de la 18.14 («hoy la tabla entera del fundador se toca desde el navegador»). **Queda como 18.27, con las dos caras escritas.** Lo que sí se hizo es que la negativa no se pierda: `reference.tooLong` está en `LISTING_VIOLATION_FIELD` y NO en `EDIT_REQUEST_FIELD`, así que cae en `elsewhere` y se dice igual — *«la negativa de la referencia no tiene control en editar, y se dice igual»* (`violation-field.test.ts`), con la mutación de agregarla a `EDIT_REQUEST_FIELD` poniéndola roja.

  **La pantalla de revisar sigue mostrando sólo la zona en el paso 2, y no es un hueco de esta tarea.** El resumen dibuja UN valor por paso —el paso 4 dice «2 hab · 78 m²» y calla baños y puestos—, así que omitir la seña es lo que esa pantalla ya hace con todo lo secundario, no algo que este campo estrene.

  **Dónde quedó cada cosa.** Regla y constante: `src/modules/listing-publication/domain/publishable-listing.ts` (más `referenceOrNone`, para que «en blanco es ninguna» se decida una sola vez). Paso y campo: `publication-steps.ts` y `violation-field.ts`. Copia: `app/publicar/violation-copy.ts` y `app/importar/import-copy.ts`. Escritura: `listing-repository.port.ts`, `publish-listing.ts`, `drizzle-listing-repository.ts`. Lectura: `listing-detail.port.ts`, `drizzle-listing-detail.ts`. Pantallas: `app/publicar/PublishStep.tsx` (su `FieldError` propio, con `aria-invalid` y `aria-describedby`) y `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` con `.reference` en `ficha.module.css` — **sin un token nuevo y sin un valor literal**: misma familia y mismo color que la ubicación que completa.
- [x] 18.8 **The review screen** and **back-navigation that preserves forward steps.** Rule: correcting step 4 from review leaves steps 5–9 intact, with their values. Steps already answered are links; steps ahead are not. The button changes to "Guardar y volver a revisar" when entered from review, and the change is stated back ("Cambiaste habitaciones de 2 a 3"). **Hecho, PR #94.** `/publicar/revisar` + `/publicar/paso/[paso]`. Volver no borra los pasos de adelante; el botón dice «Guardar y volver a revisar» al venir de revisar. `isDraftReadyForReview` bajó al dominio: decidir quién ve el resumen es una regla de producto y en la página no la alcanzaba ninguna prueba.
- [x] 18.9 **The price histogram in step 3 is the same faceting engine as F5.** This raises 14.11's priority: counting is no longer only a search feature, it is on the publish path too. **Avance: el motor ya es UNO SOLO y ya está — rebanada A de la 14.12, PR #178. La casilla NO se marca**: el paso 3 no dibuja todavía ni una barra. `listing-search/domain/price-histogram.ts` es el único histograma del repositorio y publicar lo importa desde ahí en vez de tener el suyo; **por eso vive en `listing-search`**, aunque el consumidor de esta tarea sea `listing-publication`: dos motores darían dos «la mayoría está entre» distintos para la misma zona, que es exactamente lo que esta tarea existe para impedir. **Lo propio de publicar, y es lo único que la lista no usa**: `pricePositionInBand(band, precio)` contesta `below | within | above` — la lámina lo convierte en «**Tu precio está en el medio**», y se compara contra la franja de la mayoría y **no contra el eje entero**, porque quien pide $300 donde va de $200 a $1000 está adentro del eje y muy por debajo de lo que pide la gente; los dos bordes cuentan como adentro (el aviso que fija el borde ES uno de los que la franja cuenta) y un precio que todavía no es número devuelve `null`, que es lo honesto a mitad de tipear. **El español NO está en el dominio**: la frase la escribe la pantalla, misma partición que `app/publicar/violation-copy.ts` hace con los códigos del validador — y no la de `STEP_COPY` en `search-accordion.ts`, que es el rótulo fijo de un vocabulario cerrado y no un juicio sobre datos. **Falta todo el cableado**: la faceta del puerto y su consulta (rebanada B), y el paso 3 dibujando el eje, las seis u ocho barras, la frase y la posición (rebanada D). **Ojo con la lámina al construir la D**: `Rentas - Publicar - Mobile` dibuja **seis** barras mientras `Publicar - Desktop` y las dos de Lista dibujan **ocho** — se unificó en ocho, ver la 14.12. **Avance: la faceta y su consulta ya están — rebanada B, PR #180. La casilla SIGUE ABIERTA**: el paso 3 no dibuja todavía ni una barra. Publicar va a leer `FacetCounts.byPriceBucket` del MISMO `FacetedSearchPort` que la lista —no un puerto propio— por la misma razón por la que el motor es uno solo: dos consultas darían dos reparticiones para la misma zona. **Y `pricePositionInBand` ya tiene con qué**: la franja que recibe sale de `priceHistogram(counts.byPriceBucket)`, o sea de las filas reales. **Lo que la D todavía tiene que resolver**: con qué criterio se pide el conteo mientras alguien escribe el precio —el histograma ignora el filtro de precio a propósito, así que el número que se está tecleando NO angosta las barras, que es lo que hace que el dibujo siga sirviendo mientras se mueve— y qué se muestra por debajo del piso de doce, donde el dominio devuelve `insufficient` con el total real y la pantalla tiene que decir algo que no sea un histograma vacío **Avance: la lista ya dibuja el histograma — rebanada C, PR #181 y PR #182. La casilla SIGUE ABIERTA**: el paso 3 no dibuja todavía ni una barra. **Lo que la D hereda hecho y no tiene que volver a decidir**: `listing-search/domain/price-histogram-panel.ts` ya resuelve las tres preguntas que la lámina de publicar también hace — qué barra queda dentro del rango (con los precios REALES del cubo y no con el borde nominal, ver la contradicción registrada en la 14.12), cómo se nombra el lugar de la frase, y qué se dice por debajo del piso de doce, que es justamente lo que esta tarea dejaba sin resolver. **Y lo que la D todavía tiene que resolver, que sigue siendo suyo**: en publicar no hay un rango elegido sino UN precio que se está tecleando, así que el marcado dentro/fuera no aplica igual — lo que la lámina de publicar dibuja es `pricePositionInBand`, «Tu precio está en el medio», contra la franja de la mayoría; y con qué criterio se pide el conteo mientras alguien escribe, dado que el histograma ignora el filtro de precio a propósito. **Rebanada D de cuatro, hecha — PR de cierre. La casilla SE MARCA**: el paso 3 dibuja las ocho barras, el eje, el encabezado con la zona y la frase, servidas desde el servidor. **Qué puerto llama el paso 3, y por qué no es el gordo**: un puerto de lectura angosto propio, `ZonePriceTallyPort.tallyForZone(cityId, zoneId)`, cuyo adaptador **compone `DrizzleFacetedSearch` y no escribe una línea de SQL** — reusar el motor es que haya UN `width_bucket` en el repositorio, no que haya una sola interfaz. `FacetedSearchPort` devuelve además el total de la búsqueda, el conteo por zona, por habitaciones, por atributo, por tipo, las nueve relajaciones y el total de la ciudad: números de un panel, calculados contra criterios que quien publica nunca expresó, y entregárselos a un formulario lo deja a una línea de escribir «47 avisos en Chacao». **Lo que eso cuesta hoy, dicho y no escondido**: por debajo se evalúan las agregaciones del panel entero — un solo viaje y un solo recorrido de las mismas filas, que es el costo dominante, pero no es gratis; desprender el reparto a una función compartida queda como 18.28 en vez de partir acá un archivo recién embarcado. **La contradicción de los tokens, verificada y resuelta**: las dos láminas de Publicar pintan las barras no marcadas en `--line` y las de Lista en `--tint`, y el acento significa cosas distintas —allá «tu rango cubre esto», acá «acá se concentra la oferta»—. **No es un desliz: son dos preguntas distintas**, porque en el paso 3 no hay rango que elegir sino un número que se escribe. Se decide que **el color no cargue el significado en ninguna de las dos**: las dos dicen el dibujo en palabras en el `aria-label` de la fila y las dos escriben la frase debajo, así que el acento señala y la frase nombra. Y **`--tint` NO se reusa acá**: en el panel el par tinte + borde significa EXCLUIDO, y en el paso 3 nada lo está —las ocho barras son precios de vecinos reales—, así que las no marcadas quedan en el token neutro de la lámina. **La única desviación de la lámina es el borde de la barra no marcada**, y es la misma que tomó la rebanada C: el relleno solo es «invisible para quien no distingue colores y para el modo de alto contraste». **Los cuatro estados de la frase**: sin precio, «La mayoría pide entre $380 y $620.» sola —que es el momento en que el histograma más sirve, alguien que no sabe qué pedir—; con basura tecleada, **la misma frase sola aunque el borrador guarde el precio anterior**, porque el campo está mostrando `raw` y juzgar el guardado sería una frase sobre un número que la pantalla ya reemplazó (llega a `pricePositionInBand` como `NaN` y contesta `null`, que es la negativa del dominio y no una segunda escrita en la pantalla); con un precio, la franja más «Tu precio está por debajo / en el medio / por encima», **contra la franja y no contra el eje**; y por debajo de doce avisos **no se dibuja nada y se dice distinto que en la lista** —«Con 3 avisos en Chacao no alcanza para decir cuánto se pide: el precio lo ponés vos.», y «Todavía no hay avisos en Chacao: el precio lo ponés vos.» con cero—, porque allá falta una decoración y acá el riesgo es que tres vecinos le fijen el precio a alguien; el campo de precio sigue entero debajo en los dos casos. **Qué avisos cuenta: los de la zona del paso 2, y el dominio ya lo garantizaba** — `isStepNavigable("precio")` exige que el paso 2 esté completo, así que aterrizar en el 3 sin zona redirige al 2 antes de consultar; se verificó con una prueba sobre la ruta (*«aterrizar en precio sin el paso 2 contestado devuelve al paso que falta»*) en vez de asumirlo, y la página igual falla cerrado: sin `cityId`, `zoneId` o nombre no pregunta. **El criterio no lleva precio**, así que el número que se está tecleando no angosta las barras que se miran para elegirlo. **Se sirve sin una línea de JavaScript** y se prueba sobre la RUTA y no sobre el componente (`precio-sin-javascript.test.tsx`): props escritas a mano medirían una pantalla que nadie sirve, que es el defecto que la rebanada A encontró con `draft.violations`. **16 mutaciones, todas restauradas por copia de bytes y verificadas por sha256**, cada una corrida contra la suite entera con y sin las pruebas nuevas en el árbol. **Contradicción menor que queda anotada**: `Publicar - Mobile` dibuja seis barras sin eje y con el rótulo «En Chacao», `Publicar - Desktop` ocho con eje `$200`–`$1000` y el rótulo «Precios en Altamira» — se verificó leyendo las dos láminas y se unificó en las ocho del escritorio con su eje y su rótulo, por la misma razón que la 14.12: el número de cubos se decide antes de la consulta. La altura de la fila queda en los 44 px del móvil en los dos anchos; los 52 px del escritorio no se copiaron.

### 18c. Contradictions to resolve before building

- [x] 18.10 **RESUELTO por el fundador, 2026-08-29: se queda lo que hay.** Gana el par que ship*a* —`--warn` acento y `--warn-bg` menta— y la razon que `tokens.css` ya guardaba sigue en pie: el mostaza era el unico color de esa pantalla que no pertenecia a ningun tema, y «vence pronto» tiene que leerse como parte del producto y no como una advertencia prestada del navegador. **Y la medicion lo respalda por un segundo motivo que la spec no tuvo en cuenta:** #8a5a00 pasa AA sobre `--surface` (5.93:1) y `--bg` (5.40:1) pero da **2.90:1 en el tema oscuro**, que falla — asi que volver al mostaza obligaria ademas a fijar a mano su contraparte oscura. La especificacion de Publicar es la que necesita correccion, no el codigo. ~~`--warn: #8a5a00` reverses a decision the founder made six days earlier.** `tokens.css` records it verbatim: "The mustard pair was the only colour on this screen belonging to no theme; `--warn` is now the accent navy and `--warn-bg` the mint, so 'vence pronto' reads as part of the product rather than a borrowed browser warning." **Measured with the project's own `contrastRatio`:** #8a5a00 gives 5.93:1 on `--surface` and 5.40:1 on `--bg` — both pass AA — but **2.90:1 on the dark background, which fails.** The dark theme carries its own `--dark-warn`, so this is survivable, but `design-contract.test.tsx` asserts AA across BOTH themes and will need the dark counterpart set deliberately rather than inherited
- [x] 18.11 **CERRADA 2026-08-29: no era una decision pendiente, ya estaba tomada el mismo dia.** La 17.3 registra la eleccion del fundador de cargar la taxonomia **completa** de Maracaibo —cientos de zonas—, viable gracias al buscador ordenado por conteo. La especificacion de Publicar quedo vieja ese mismo dia. Su propia frase lo cierra: **«doce zonas y varios cientos son productos distintos»**, y el producto elegido es el de varios cientos. Lo que necesita correccion es la especificacion, no el codigo. ~~The zone list size contradicts Phase 17, decided the same day.~~ This document says "lista cerrada, ~40 en Distrito Capital y ~12 en Maracaibo". Phase 17.3 records the founder's decision to load the **full** Maracaibo taxonomy — hundreds of zones — made viable by the ranked search box. Twelve zones and several hundred are different products: with twelve, a plain `<select>` works and 17.4's search box is optional again
- [x] 18.12 **CERRADA 2026-08-29 por medicion, no por decision: la cifra de la especificacion se equivoca por un factor de quince.** La 14.35 corrio el parser real sobre `docs/territorio/` (888 KB, 9 archivos): **4.710 toponimos = 89,8 KB gzip** enteros, **30,1 KB** recortados a alias mas parroquia, contra **~21 KB** de espacio disponible en el camino de lectura. Los «~2 KB» pertenecen a la lectura de doce zonas que la 18.11 acaba de descartar — la propia tarea lo admitia. Y la forma minima tampoco serviria: sin id de zona y sin ciudad, una sugerencia deja de ser un par (filtro, valor) con su alcance, que es la regla 14.18. **El camino de servidor ya existe y no es opcional**: `SearchVocabularyPort` recorta con `ILIKE` a 60 filas por texto tecleado, con esa razon escrita adentro. La forma que si entra esta propuesta en la 14.51 (vocabulario acotado a zonas con avisos activos) y espera una decision de producto, no de rendimiento. ~~The zone autocomplete is specified as client-side filtering.~~ F14 allows mandatory JavaScript only for photo compression, so the picker needs a working server path as well. The 2 KB figure also belongs to the twelve-zone reading of 18.11; the full taxonomy with parent labels is an order of magnitude larger
- [x] 18.13 **CERRADA 2026-08-29: el codigo ya eligio, y eligio el criterio 7.** «La ciudad nunca se pregunta» es lo que ship*a*: ningun paso del formulario la pide, porque **la ubicacion vive en la ruta** (decision del fundador, 2026-08-27, registrada en `search-location.ts`). La tabla de §6 quedo vieja. **Lo que NO cambia, y la tarea ya lo dejaba escrito**: `cityId.required` y `cityId.unknown` se quedan en el dominio, porque la importacion de cartera llama al mismo validador y ahi la ciudad SI llega como dato. Quedan inalcanzables desde el formulario, que no es lo mismo que muertas — borrarlas abriria un hueco en el importador. ~~§6's validation table still lists Ciudad~~ with "Por ahora publicamos en Distrito Capital y Maracaibo", while criterion 7 says the city is never asked. Both cannot hold. The shipped `cityId.required` and `cityId.unknown` violations become unreachable from the form — they must stay in the domain regardless, because the bulk import calls the same validator
- [x] 18.14 **RESUELTO por el fundador, 2026-08-29 — lo que una edicion puede tocar, campo por campo.** ~~Falta construirlo; la decision ya no.~~ **CIERRA ENTERA: la regla, el camino de escritura y la pantalla.** La 18.20 construyo la mitad que faltaba —`app/mis-avisos/[id]/editar/page.tsx`, `guardarEdicion` y el enlace «Editar» en la fila de un aviso activo—, asi que hoy un dueño corrige su precio desde el navegador y con el script apagado. ~~Lo unico que la tabla del fundador promete y todavia no se puede tocar son las fotos, que son la 18.21 por su dependencia con la 18.15.~~ **Las fotos cerraron con la 18.21 el 2026-08-30**: hoy la tabla entera del fundador se toca desde el navegador.

  | Campo | Editable | Por que |
  |---|---|---|
  | titulo, descripcion, precio | **si** | son la oferta, y cambian con el mercado |
  | habitaciones, banos, metros² | **si** | corregir un dato mal cargado no es publicar otro aviso |
  | fotos | **si**, respetando el limite | el tope que ya rige al publicar sigue rigiendo al editar |
  | contacto | **si** | *«el que revelo, revelo. Si entra de nuevo que vea el contacto nuevo»* |
  | tipo de publicador | **NO** | cambiarlo invalidaria hacia atras el filtro «solo de duenos» |

  **El contacto editable NO contradice la copia al publicar, y conviene no confundirlas**: la copia protege que editar el contacto por defecto de tu CUENTA no reescriba avisos que alguien ya contesto. Editar el contacto de ESTE aviso es un acto deliberado sobre ese aviso, que es otra cosa.

  **Y la evidencia del revelado sobrevive**, verificado el 2026-08-29: `contact_reveal_event` **no guarda el valor del contacto** —su puerto lo dice, «este puerto nunca escribe ese valor»— sino que hubo un revelado, de que aviso, quien publico y quien pregunto. Asi que cambiar el contacto no reescribe una sola fila de evidencia, y quien vuelve ve el numero vigente en vez de uno viejo, que seria peor.

  **Hecho: la regla y la escritura.** `planListingEdit` en `src/modules/listing-publication/domain/listing-edit.ts` (los ocho campos editables y la negativa), `editListing` en `application/edit-listing.ts`, `ListingEditPort` y `DrizzleListingEdit` en `infrastructure/drizzle-listing-repository.ts`. **Ninguna migracion**: las ocho columnas ya existen y `listing` no tiene `updated_at`.

  **Como se refusa el tipo de publicador, y donde.** En el dominio, con un codigo propio: `ListingEditViolation = PublishViolation | "publisherType.immutable"`, con la misma forma que `ImportRowViolation` ya usa para el suyo. **No se ensancho `PublishViolation`** porque `STEP_FOR_VIOLATION` es un `Record` sobre esa union y habria habido que inventarle un paso de publicar a un codigo que ninguno de los nueve puede mostrar. Lo prueban «rechaza un tipo de publicador distinto del que el aviso ya declaro, y no escribe nada» y su par «repetir el MISMO tipo de publicador no es un cambio, y no se rechaza» (`listing-edit.test.ts`) — el segundo existe porque sin el la afirmacion aceptaria las dos respuestas — y en la base «mandar un publisher_type distinto se refusa y la fila no se toca» y «despues de editar precio y contacto, publisher_type sigue diciendo lo mismo» (`tests/integration/listing-edit.test.ts`). **Mutacion**: `if (edit.publisherType !== undefined && ...)` a `if (false)` pone roja 1 de 12, la suya; restaurada por sha256.

  **Vencido y oculto: no son editables, decidido a proposito.** `findEditableById` y `applyEdit` llevan `status = 'active'` EN el `WHERE`, y el `set` del `UPDATE` no incluye `status`. Editar no puede ser el camino por el que un aviso apagado por tres denunciantes vuelve solo — el mismo agujero que el `WHERE status = 'active'` de `markExpired` cierra. El camino de vuelta de un vencido es renovar, que es su propia decision con su propio ciclo. Lo prueba «un aviso vencido y uno oculto por reportes no son editables, y siguen apagados».

  **La evidencia del revelado si sobrevive, y la tarea citaba mal su prueba.** La frase «este puerto nunca escribe ese valor» de `contact-reveal-event.port.ts` habla del `NULL` de `message`, no del valor del contacto. Lo que de verdad lo establece es la FORMA de la tabla: `contact_reveal_event` tiene `listing_id`, `publisher_id`, `tenant_user_id`, `city_id`, `revealed_at` y `message`, y ninguna columna para el contacto. Verificado ademas contra Postgres por «cambiar el contacto no borra ni reescribe la evidencia del revelado».

  **Dos mutaciones quedaron VERDES y por eso hay dos pruebas mas.** Quitarle `publisher_id` y `status` al `WHERE` de `applyEdit` no rompia nada, porque la lectura ya habia refusado antes: el guarda de la escritura esta para la ventana ENTRE las dos, y se prueba llamando al adaptador directo («el UPDATE mismo refusa una fila ajena o apagada, no solo la lectura que lo precede»). Quitarselos a `findEditableById` tampoco rompia nada, porque el caso de uso revalida al dueno: lo que si cambia es que un vencido sin fotos contestaria `photos.required` en vez de «no existe», o sea contaria que existe («un aviso que no podes editar no te cuenta POR QUE seria rechazado», AGENTS.md §7).

  **Validadores reusados, ninguno reescrito**: `validatePublishableListing` en etapa `"activation"` — la misma que corre al activar un borrador — asi que `MIN_PHOTOS_FOR_ACTIVATION`, `MAX_PHOTOS_PER_LISTING`, `MAX_TITLE_CHARACTERS` y las veinte reglas restantes rigen igual al editar. Ningun numero se reescribio aca.

  ~~Editing a published listing (§12.6) collides with two shipped guarantees.~~ Publisher type cannot change after publishing. And the contact is *copied* at publish time precisely so that editing an account default never rewrites an advert somebody already acted on — the ficha's own rule is that a tenant who wrote to a number needs that advert to keep saying that number. What an edit may and may not touch is a product decision, not an implementation detail

### 18b. Lo que la especificación de Publicar pide y ninguna tarea recogía (2026-08-25)

- [x] 18.20 **La pantalla de editar, que es la mitad que falta de la 18.14 y NO es pulido.** La regla y la escritura shipean sin un solo llamador: `editListing` no lo invoca ninguna ruta, y `/mis-avisos` no tiene enlace a editar. **Es la forma de defecto que este cambio ya encontro cinco veces** —el anti-fraude de fotos, la pastilla de busqueda, el motor de importacion, `canImportListings` y las dos llamadas de la 9.28— asi que queda escrito: hasta que esta tarea cierre, un dueno no puede corregir su precio aunque el dominio sepa como. **Por que se partio**: el trabajo medido dio 1.105 lineas revisables solo con la regla, el caso de uso, el adaptador y las dos pruebas (14 unitarias, 8 de integracion contra Postgres), contra un techo de 1.200 del propio arnes; la pantalla son ~350 mas. Se partio diciendolo, no recortando una prueba. **Lo que necesita**: `app/mis-avisos/[id]/editar/page.tsx` con un `<form>` de verdad, la accion de servidor al lado de `activarBorrador`, el enlace «Editar» en la fila de un aviso activo de 14d, y la copia de `publisherType.immutable` — que se resuelve delegando en `publishViolationMessage` para los codigos compartidos, sin abrir una segunda tabla de espanol al lado de `CHANGE_FIELD_LABEL`. **Y un texto que ya esta prometido**: el paso 9 de publicar dice «Aparece siempre en tu aviso y no se puede cambiar despues», asi que la pantalla de editar tiene que decir lo mismo donde deberia haber estado el campo, no callarlo

  **Hecha.** `app/mis-avisos/[id]/editar/page.tsx` (la pantalla), `guardarEdicion` en `app/mis-avisos/actions.ts` (la accion, al lado de `activarBorrador`), `card.editable` en `publisher-listing-board.ts` (quien ofrece «Editar») y `loadListingForEdit` en `application/edit-listing.ts` (la lectura que precarga). **Ninguna migracion y ninguna regla nueva.**

  **La prueba central es que la ruta llega al dominio**, que es de lo que se trataba la tarea: «llama a editListing con el id del formulario y los ocho campos editables, y vuelve a la lista» (`app/mis-avisos/actions.test.ts`). **Mutacion**: cambiar `await editListing(` por `await Promise.resolve(` pone rojas las 5 de ese bloque y ninguna otra; restaurada por copia y sha256 comparado. Los bytes servidos los prueba `app/mis-avisos/[id]/editar/editar-servido.test.tsx` con `renderToStaticMarkup(await Page(...))` — necesario y no suficiente, por eso las dos.

  **Cinco correcciones y hallazgos sobre lo que decia el enunciado.**

  Primera, **quien ofrece «Editar» lo decide el dominio y no la fila**: `PublisherListingCard.editable`, probado por «un aviso activo y uno que vence pronto ofrecen editar» y su par «un borrador, uno vencido y uno oculto no ofrecen editar» (`publisher-listing-board.test.ts`). El par existe porque una sola afirmacion aceptaria las dos respuestas. Y es **mas estricto que el `WHERE` del puerto a proposito**: una fila que el cron todavia no marco vencida pasaria `status = 'active'` aunque la pantalla ya la dibuje como vencida, asi que ofrecerle «Editar» seria contradecir en la misma fila lo que la fila dice («una fila que la base todavia llama activa pero cuya fecha paso no ofrece editar», AGENTS.md §7).

  Segunda, **la lectura que precarga la pantalla no existia** y se agrego al lado de la escritura, no en un archivo propio: `loadListingForEdit` comparte puerto, puertas y —lo que importa— el MISMO `EditListingNotFoundError`. Separarlas invitaria a que la pantalla refusara con un error distinto del que refusa el guardado, y entonces un aviso ajeno seria distinguible de uno inexistente en exactamente una de las dos. Lo prueba «un aviso ajeno y uno inexistente dan el MISMO error, con el mismo texto», que compara los textos y no solo las clases. **Mutacion**: quitarle la revalidacion de dueño pone roja 1 de 11, la suya.

  Tercera, **un codigo de violacion inventado en la URL tumbaba la pantalla, y eso se encontro escribiendo esta tarea.** Los codigos vuelven en la direccion porque sin JavaScript no hay otro lugar donde devolverle la negativa a la pantalla, asi que una direccion escrita a mano es dato de afuera: indexar `PUBLISH_VIOLATION_COPY` con lo que trajera daba `undefined.message`, o sea un 500 donde correspondia una frase — la misma forma de «Cambiaste undefined» de la 18.16. `listingEditViolationMessage` toma `string` y cae al codigo, el mismo `?? reason` que `importRowReasonText` ya usaba. La garantia de que ningun codigo REAL se quede sin copia la sigue dando el `Record` sobre la union, que no compila si el dominio agrega uno.

  Cuarta, **la accion SI le manda `publisherType` al dominio aunque la pantalla no lo dibuje**, y las dos mitades hacen falta. Una accion de servidor es un endpoint HTTP publico: descartar el campo aceptaria en silencio un pedido que el producto refusa, y la garantia pasaria a depender de que ningun formulario lo dibuje nunca. Lo prueban «un tipo de publicador que llegue en el POST viaja al dominio para que lo refuse» y, del otro lado, «no dibuja ningun control de tipo de publicador» sobre los bytes servidos.

  Quinta, **ninguna segunda lista de castellano.** Las etiquetas del formulario salen de `CHANGE_FIELD_LABEL` —la unica lista de nombres de campo del repositorio, ahora exportada— con la primera letra en mayuscula, que es presentacion y no un nombre nuevo; los dos tipos de publicador y los tres metodos de contacto, de `PUBLISHER_TYPE_LABEL` y `CONTACT_METHOD_LABEL`; las veinticinco violaciones compartidas se delegan en `publishViolationMessage` («delegar conserva el contador, no solo la frase»); y la promesa del paso 9 quedo escrita UNA vez en `violation-copy.ts`, con `PublishStep` armando su negrita desde las mismas dos constantes. La hoja de estilos tambien se reusa: la pantalla dibuja con `publish-steps.module.css`, sin una hoja nueva.

  **Una contradiccion entre el enunciado y la lamina, resuelta a favor de la lamina.** La tarea dice «el enlace «Editar» en la fila de un aviso activo **de 14d**», pero 14d NO lo dibuja: su columna de accion lleva «Subir fotos» en un borrador, «Renovar 30 dias» en uno que vence pronto y «Volver a publicar» en uno vencido, y en la fila activa va **vacia** —«la columna de accion se reserva incluso vacia: las filas no bailan»—. Asi que «Editar» no se copio de la lamina: se DERIVO del sistema en el hueco que la lamina ya reservaba, con el papel de nivel 3 (borde neutro, texto de tinta) y `--target-min`, que es lo que AGENTS.md §2 pide para las dieciseis superficies que las laminas no cubren. Y ocupa tambien la fila de «vence pronto», que la lamina llena con «Renovar 30 dias» y que el dominio si considera editable — las dos acciones conviven porque son decisiones distintas sobre el mismo aviso.

  **Lo que NO trae, dicho en vez de a medias.** ~~Las fotos son la 18.21 y dependen del menu `⋯` de la 18.15, que hoy no existe ni al publicar~~ (el menú existe desde el 2026-08-30; la 18.21 sigue abierta por el camino de escritura, no por el menú); ~~el tope y el piso SI rigen esta edicion, lo que falta es agregar y quitar («no ofrece editar las fotos, que son otra tarea»)~~ — **cerrado por la 18.21 el 2026-08-30**: agregar y quitar viven en la misma pantalla, cada foto con su propio `<form>`; lo que queda sin dibujar es la miniatura, que es la 18.26. ~~Y los mensajes de negativa salen en un solo bloque arriba del formulario, como los de activar en `/mis-avisos`, en vez de al lado del campo que los produjo como hace publicar~~ — **cerrado por la 18.22 el 2026-08-30**: van al lado del campo, con el mismo `FieldError` de publicar; el bloque quedó sólo para las negativas sobre algo que una edición no manda.
- [x] 18.21 **Editar las fotos de un aviso publicado.** La tabla del fundador dice «fotos: si, respetando el limite», y **el limite ya rige**: `planListingEdit` valida en etapa `"activation"`, asi que una edicion no puede dejar el aviso por debajo de `MIN_PHOTOS_FOR_ACTIVATION` ni por encima de `MAX_PHOTOS_PER_LISTING` — lo prueban dos afirmaciones de `listing-edit.test.ts`. Lo que falta es poder AGREGAR y QUITAR: `ListingEdit` no lleva fotos a proposito, porque `photoCount` sale de contar y nunca de un campo del pedido (dos fuentes para el mismo numero es como una pantalla dice «3 fotos» sobre un aviso que tiene dos). ~~Depende de la 18.15, que es donde vive el menu de fotos y hoy no existe ni al publicar~~ — **DESBLOQUEADA 2026-08-30**: la 18.15 cerró, y con ella `draft-photo-actions.ts`, que decide agregar, quitar, reordenar y portada sin tocar el componente. Lo que queda para esta tarea es el camino de escritura, no la mecánica: quién adjunta y quién desprende una foto de un aviso YA publicado

  **MEDIDA Y DIFERIDA el 2026-08-30, con las tres decisiones tomadas para que la próxima tanda no las vuelva a derivar.** Se midió antes de escribir una línea: el camino de escritura necesita un caso de uso para adjuntar y otro para desprender, una lectura de las fotos del aviso al lado de `findEditableById`, un puerto de escritura nuevo para desprender, dos métodos de adaptador, dos acciones de servidor, la sección de la pantalla, y las pruebas unitarias más las de integración contra Postgres. El trabajo comparable ya shipeado —la 18.20, con UN caso de uso, su adaptador y dos suites— midió 1.105 líneas revisables; éste es más. Junto con la 18.22 pasa el techo del arnés, así que se partió diciéndolo en vez de recortar una prueba, y se ordenó al revés: **la 18.22 primero porque es la más chica y la más visible**.

  **Primera: quién adjunta y quién desprende, y por qué NO es `attachPhotoToDraft`.** Su propio docstring ya lo dejó escrito —«an `active` listing cannot reach this function at all»— porque su puerta es `findDraftById`, cuyo `WHERE` lleva `status = 'draft'` adentro. Un aviso publicado es otro sujeto y ensanchar ese `WHERE` le abriría a `activateListing` una fila que ya está activa. Va **un caso de uso al lado, no uno más ancho**: `attachPhotoToListing`, con la misma tubería pero con `findEditableById` de puerta —la misma que ya refusa un aviso ajeno, un borrador, un vencido y uno oculto como el mismo `null`—, y `detachPhotoFromListing` para el otro sentido. Del lado de los puertos: adjuntar **reusa `ListingPhotoAttachmentPort.attachPhoto` tal cual**, que ya escribe una foto sobre una fila que existe; desprender pide un puerto NUEVO al lado del angosto, no un método más en él (AGENTS.md §3), y leer las fotos del aviso pide una lectura al lado de la escritura, como la 18.20 ya hizo con `loadListingForEdit`.

  **Segunda: `processUploadedPhoto` sigue siendo el único punto de paso.** Es el que `publishListing` y `attachPhotoToDraft` ya llaman los dos, y el que aplica el rechazo por hash perceptual entre cuentas de la 4.7. Cualquier camino de adjuntar que no lo llame VERBATIM vuelve a abrir el hueco que esa tarea cerró, así que la regla queda escrita antes de construir: el adjuntar de un aviso publicado lo llama igual, con `session.userId` como `publisherId` —que es también la exención del mismo dueño— y grabando el hash sólo después de que `attachPhoto` devuelva el id.

  **Tercera: al desprender, el objeto de R2 se queda, y SÍ empeora la 18.23.** Borrarlo pediría permiso de borrado sobre el bucket, que es exactamente el ensanche que la 18.23 ya decidió no hacer. Pero no es el mismo huérfano: el de la 18.23 vive bajo el prefijo de subidas sin adjuntar, que es donde una regla de ciclo de vida del bucket lo barre sin mirar la base; las derivadas de una foto desprendida viven bajo el prefijo PROMOVIDO, donde también viven las fotos de todos los avisos activos, así que la misma regla de ciclo de vida no las alcanza sin arrasar con las que sí se muestran. **La salida barata de la 18.23 no cubre a las de la 18.21**: éstas sólo son distinguibles por la ausencia de su fila en `listing_photo`, o sea un barrido contra la tabla y no una regla del bucket. Dicho antes de construir, no después.

  **Y un hallazgo que la tarea no traía: desprender obliga a renumerar.** `listing_photo_position_unique` es sobre `(listing_id, position)` y `attachPhotoToDraft` escribe `position = photoCount`. Quitar la foto de la posición 1 de tres deja las posiciones {0, 2} con dos filas, así que el siguiente adjuntar reclama la 2 y choca contra la restricción. Renumerar tiene que pasar dentro de la MISMA transacción del desprender — y es también lo que hace verdadero en la base el «quitar la portada asciende a la siguiente» que `planPhotoRemoval` ya decide en memoria. Eso no lo puede probar un doble: es una prueba de integración contra Postgres
  **CIERRA el 2026-08-30, con las tres decisiones de la tanda anterior verificadas en vez de vueltas a derivar.** El camino de escritura es `src/modules/listing-publication/application/edit-listing-photos.ts` (17 pruebas), sus puertos `application/ports/listing-photo-set.port.ts`, el adaptador `DrizzleListingPhotoSet` en `infrastructure/drizzle-listing-repository.ts`, las tres acciones de `app/mis-avisos/actions.ts` (6 pruebas) y la sección de fotos de `app/mis-avisos/[id]/editar/page.tsx` (9 pruebas sobre los bytes servidos). Contra Postgres de verdad: `tests/integration/listing-photo-editing.test.ts` (9 pruebas). **Ninguna migración y ningún cambio de esquema.**

  **La puerta no se copió: se reusó `loadListingForEdit` entera.** Los tres casos de uso —firmar, adjuntar, desprender— entran por la MISMA función que ya usa la pantalla de editar, así que un aviso ajeno, uno inexistente, un borrador, un vencido y uno oculto salen todos por el mismo `EditListingNotFoundError`. Copiar su `WHERE` habría dejado dos lugares donde equivocarse y uno solo probado. Lo prueban «el aviso de otra cuenta y uno que no existe se contestan con el mismo error, y ninguna fila se escribe» (integración, que además vuelve a leer la tabla del dueño después) y «un aviso vencido no desprende fotos». **Mutación**: quitar el guarda del tope pone rojas 2 de 17 y ninguna otra; restaurada por copia y sha256 comparado.

  **`processUploadedPhoto` siguió siendo el único punto de paso.** `attachPhotoToListing` lo llama verbatim con el `publisherId` del aviso —que `loadListingForEdit` ya comparó dos veces contra la sesión— y graba el hash sólo después de que `attachPhoto` devolvió el id. Lo prueban «pasa por processUploadedPhoto: una foto de otra cuenta se rechaza y no se escribe ninguna fila» y «graba el hash sólo después de que attachPhoto devolvió el id que le tocó», más «adjuntar pasa por el caso de uso del aviso, no por el del borrador» en las acciones. **Mutación**: apuntar la acción a `attachPhotoToDraft` pone roja exactamente una.

  **El puerto de desprender fue AL LADO del angosto, no adentro.** `ListingPhotoAttachmentPort` sigue con su único `attachPhoto`; `ListingPhotoOrderPort` (leer el orden) y `ListingPhotoDetachmentPort` (quitar) son dos interfaces nuevas en su propio archivo. Adjuntar reusó `attachPhoto` tal cual y no se le tocó una línea.

  **La renumeración pasa en el adaptador, en UNA transacción y en tres sentencias de conjunto — nunca en una lectura seguida de una escritura.** Y no es `position = position - 1`: `listing_photo_position_unique` no es `DEFERRABLE`, así que Postgres lo comprueba fila por fila dentro de la sentencia — medido contra este mismo contenedor, `UPDATE ... SET position = position + 1` sobre `{0,1,2}` falla con `duplicate key`. Restar uno sobrevive sólo si el plan visita las filas de menor a mayor, y ningún `UPDATE` promete un orden de visita. Va un rodeo por los negativos: `k → -k-1` y después `-k-1 → k-1`, dos mapas cuyos destinos no pisan ningún origen bajo ningún orden. Lo prueba **«quitar la del medio renumera, así que el siguiente adjuntar NO choca contra listing_photo_position_unique»**, que quita la 1 de tres y después adjunta — un doble no tiene índice único y no podría verlo. **Dos mutaciones**: borrar sin renumerar pone rojas 2 de 9; renumerar de más (`-2` por `-1`) pone rojas las mismas 2 y ninguna otra.

  **Quitar la portada asciende a la siguiente, y ahora es verdad EN LA BASE**: lo prueba «quitar la portada asciende a la siguiente EN LA BASE, y es la que el plan nombró», que compara `position` real contra el id que `planPhotoRemoval` nombró. Su par —«quitar la que no es portada no mueve la portada»— existe porque una sola afirmación aceptaría las dos respuestas. **Mutación**: devolver siempre `coverChangedTo: null` pone roja exactamente una unitaria.

  **Ninguna regla nueva y ningún número repetido.** El piso, el ascenso de la portada y la negativa de quitar la última los sigue contestando `planPhotoRemoval` (18.15); el tope es `MAX_PHOTOS_PER_LISTING`, la misma constante que `planListingEdit` aplica en etapa `"activation"`; la copia sale de `PHOTO_ACTION_COPY` y `PHOTO_REMOVAL_REFUSAL_COPY`, con un solo agregado —`photoRemovalRefusalMessage`, el mismo `?? codigo` que `listingEditViolationMessage` ya usaba para una dirección escrita a mano—. `ListingEdit` NO ganó fotos.

  **Y no se construyó un segundo mecanismo de subir.** `FotosDelBorrador.tsx` se renombró a `SubirFoto.tsx` y recibe sus dos acciones por props: el borrador importado y el aviso publicado se fotografían igual —comprimir, firmar, subir, adjuntar— y lo único que cambia es la puerta del servidor. Un segundo componente habría sido la segunda manera de subir una foto. Quitar, en cambio, es un `<form method="post">` de verdad y funciona con el script apagado; agregar no puede, porque comprime en el teléfono (AGENTS.md §2 exime a `/mis-avisos`).

  **El objeto de R2 se queda, y esta tarea NO cambia la forma del problema de la 18.23**: las derivadas de una foto desprendida siguen bajo el prefijo promovido, junto a las de todos los avisos activos, así que la regla de ciclo de vida barata no las alcanza sin arrasar con las que sí se muestran. Lo único que cambia es el volumen —ahora también se generan huérfanos al quitar de un aviso publicado, no sólo al descartar en el paso 8—, así que la 18.23 pasó a nombrarlo.

  **Lo que NO trae, dicho en vez de a medias.** La sección dibuja las fotos por su posición —«Foto 1 (portada)», «Foto 2»— y **no dibuja la miniatura**: la imagen pide leer la derivada `thumb` de cada foto y componer su URL, que es maquinaria que `listing-discovery` ya tiene y esta pantalla no cablea. Queda como **18.26**. Tampoco trae reordenar ni elegir portada a mano: `photoActionsFor` las nombra, pero no existe camino de escritura para ellas y ofrecer un botón que no puede ejecutarse es peor que no ofrecerlo.

  **Medida: 1.814 líneas revisables de código y pruebas** (1.783 agregadas, 31 borradas; 1.846 contando este plan), contra un techo de 1.400 del arnés y los 400 de AGENTS.md §6. Se dice en vez de recortar una prueba. La estimación anterior —«más que las 1.105 de la 18.20»— se quedó corta porque hicieron falta TRES casos de uso y no dos (firmar un presignado para un aviso activo no lo podía hacer `requestDraftPhotoUpload`, cuya puerta es `findDraftById`), una lectura más para la pantalla, y cuatro suites en vez de dos.

- [x] 18.22 **La negativa de editar, al lado del campo que la produjo.** Hoy `app/mis-avisos/[id]/editar/page.tsx` dibuja los motivos en un solo bloque arriba del formulario, que es la forma que `/mis-avisos` ya usaba para las negativas de activar. **Publicar hace otra cosa y su razon esta escrita**: `violation-copy.ts` dice que el mensaje va ANTES del campo que lo produjo y se anuncia, con `aria-invalid` y `aria-describedby` — «un borde rojo es invisible para quien no distingue colores y para el modo de alto contraste». Editar deberia decirlo igual. **Lo que falta para poder hacerlo** es a que campo pertenece `publisherType.immutable`: `PUBLISH_VIOLATION_COPY` es un `Record` sobre la union de publicar y ese codigo no esta adentro, asi que hace falta la contraparte de `ViolationCopy.field` para la union de editar, no una segunda tabla

  **CIERRA.** La regla es `src/modules/listing-publication/domain/violation-field.ts` (8 pruebas) y la pantalla `app/mis-avisos/[id]/editar/page.tsx`. **Ninguna migración y ninguna segunda tabla de castellano.**

  **La contraparte se resolvió mudando `field`, no duplicándolo.** `ViolationCopy` llevaba `field` en `app/publicar/violation-copy.ts`; ahora esa tabla lleva el español y nada más, y a qué campo pertenece cada código lo contesta `LISTING_VIOLATION_FIELD`, un `Record` sobre la unión de EDITAR —que contiene entera la de publicar más `publisherType.immutable`—, en el dominio y bajo el piso del 90 %. Es una sola tabla que leen las dos pantallas: agregar un código al dominio deja de compilar hasta que alguien diga a qué campo pertenece, la misma garantía que la copia ya daba para la frase. Lo prueban «no se puede cambiar quién publica pertenece a quién publica» y «los códigos de publicar conservan el campo que ya tenían» (`violation-field.test.ts`). **Mutación**: apuntar `publisherType.immutable` a `title` pone rojas 2 de 8 y ninguna otra; restaurada por copia y sha256 comparado.

  **Y una decisión que la tarea no traía: qué pasa con una negativa que esta pantalla no tiene dónde poner.** Una edición manda nueve campos —los ocho que escribe más `publisherType`, que viaja para ser rechazado—, así que `photos.required`, `zoneId.notInCity`, `cityId.unknown`, `propertyType.invalid` y `parkingSpots.invalid` no tienen control al lado del cual leerse. **Se dicen igual, en el bloque**, en vez de tragarse: un formulario que se niega a guardar sin decir por qué es peor que un bloque arriba (AGENTS.md §7). El reparto lo hace `placeListingEditViolations` y no la pantalla, y el conjunto de los nueve sale de `Record<keyof ListingEdit, true>`, que no compila si mañana `ListingEdit` gana un campo. Lo prueban el par «una negativa sobre un campo que la edición manda se coloca al lado de ese campo» / «una negativa sobre algo que la edición no manda no tiene campo, y no se pierde» —el par existe porque una sola afirmación aceptaría las dos respuestas— más «un código inventado no se coloca en ningún campo, y tampoco se traga». **Mutación**: quitar el guarda de colocabilidad pone rojas 2 de 8, las suyas.

  **En los bytes servidos**, `app/mis-avisos/[id]/editar/editar-servido.test.tsx` (16 pruebas): «pone la negativa antes del campo que la produjo y anuncia el control», su par «sin negativas ningún control se anuncia inválido», «marca sólo el campo de la negativa, no todos», «la negativa de quién publica se lee donde debería haber estado el campo» y «la negativa colocada no se repite en el bloque». **Tres mutaciones**: dejar `invalido()` sin `aria-invalid` ni `aria-describedby` pone rojas 2; dibujar el bloque con `motivos` en vez de con `elsewhere` pone roja 1; quitar el `FieldError` de quién publica pone roja 1. Ninguna otra.

  **Se reusó la anatomía de `PublishStep`, no se construyó una segunda.** `FieldError` vivía adentro de `PublishStep.tsx` y se movió a `app/publicar/FieldError.tsx`; las dos pantallas lo importan, con las mismas clases de `publish-steps.module.css` (`error`, `controlInvalid`), el mismo `id` de `{campo}-error` y el mismo par `aria-invalid` / `aria-describedby`. **Con una diferencia dicha en vez de improvisada**: `publisherType` no lleva `aria-invalid` porque no hay control que invalidar —que es justamente lo que la negativa dice—, y el grupo de radios de contacto se describe entero en el `<fieldset>` en vez de marcar el primero, que diría que ése es el problema

- [x] 18.24 **`PublishStep.tsx` no tenía una sola prueba, y así se encontró.** Al mudar `field` al dominio se corrió la mutación obligatoria —apuntar TODAS las violaciones de publicar al campo `title`— y **no puso roja ninguna prueba de `app/publicar`**: 638 líneas, los nueve pasos del formulario, sin archivo de pruebas. Lo único que se afirmaba era que la tabla NOMBRA un campo (`violation-copy.test.ts`), nunca que el mensaje aterrice bajo ese control, así que la razón que la 18.22 cita —«el mensaje va ANTES del campo que lo produjo»— estaba escrita en un comentario y en ninguna aserción. **Cerrado sólo el mecanismo compartido**: `app/publicar/paso-servido.test.tsx` (3 pruebas) prueba el paso 3 y con eso la mutación sí pone roja exactamente una. **Lo que queda abierto** son los otros ocho pasos y todo lo que `PublishStep` dibuja que no es el error de campo: el riel, el contador, el botón contextual, los cinco atributos, el buscador de zona. Es la misma forma de la 18.15 —reglas y marcado sin una prueba— sobre un archivo cuatro veces más grande. **Avance parcial el 2026-08-30 (18.25), anotado para que la casilla no mienta**: `paso-servido.test.tsx` sumó dos pruebas sobre el paso 7 —el contador de la descripción, dicho y callado—, así que ya son dos de los nueve pasos. Los otros siete y todo lo que `PublishStep` dibuja que no es el error de campo siguen abiertos. **Segundo avance parcial el 2026-08-30 (18.17)**: el contador de la barra —uno de los seis que este enunciado nombra— quedó cubierto por `app/publicar/paso/[paso]/mapa-de-pasos.test.tsx`, que afirma en los bytes servidos que dice «1 / 9» y que sin ningún salto posible no se vuelve un desplegable. El riel, el botón contextual, los cinco atributos y el buscador de zona siguen sin una sola aserción. **Tercer avance parcial el 2026-08-30 (18.7)**: `paso-servido.test.tsx` sumó dos pruebas sobre el paso 2 —la negativa de la seña leída antes de su control, y su par: la negativa de la zona no marca el campo de la seña—, así que ya son tres de los nueve pasos. El riel, el botón contextual, los cinco atributos y el buscador de zona siguen sin una sola aserción. **Cuarto avance parcial el 2026-08-31**: `app/publicar/paso/[paso]/riel-y-boton.test.tsx` (7 pruebas) cierra **el riel y el botón contextual**, los dos primeros de los cuatro que el tercer avance dejó nombrados. El riel se afirma en los bytes servidos y acotado a `<nav aria-label="Progreso">`, porque el mapa de móvil dibuja las MISMAS direcciones desde el mismo arreglo: un `href` buscado en el documento entero da verde con el riel roto. Las cuatro mitades: un paso contestado es enlace; uno de adelante no lo es **y su fila se dibuja igual** —sin esa segunda mitad, un riel que esconde lo que falta pasaría—; cada fila enlazada abre y cada fila sin enlace redirige; y una fila contestada dibuja su valor («Altamira», «$450 al mes») y no su nombre corto. El botón se afirma con sus dos frases sobre el mismo paso y el mismo borrador —«Seguir» y «Guardar y volver a revisar»— más el paso 9, que dice «Revisar el aviso» salvo que se venga de revisar. **Lo que el riel NO duplica, verificado y no supuesto**: `page.tsx:63` arma `navigable` con `isStepNavigable`, la misma puerta que la línea 52 vuelve a aplicar al aterrizar. Se prueba por el único caso donde una copia razonable discreparía —el paso actual, sin contestar y aun así abrible—, así que cambiar esa línea a `isStepComplete` pone roja exactamente una prueba. **Seis mutaciones corridas, ninguna en blanco** (sha256 comparado antes y después, restauración por copia). Y tres de ellas, corridas contra la suite entera SIN este archivo, no ponían roja ninguna de las 2.285 pruebas: el riel dibujando el nombre en vez del valor, y el riel preguntando `isStepComplete`. Es la medida del hueco que este enunciado nombra, en vez de la afirmación. **Hallazgo anotado, no arreglado**: la prueba «no dibuja el paso sin contestar» de `mapa-de-pasos.test.tsx` (18.17) busca el `href` en el documento entero, así que hoy se pone roja por el riel y no por el mapa que nombra. Es la forma de la trampa 4 —un título que promete lo que su cuerpo no acota—, y acotarla es trabajo de la 18.17, no de acá. **Quinto avance parcial el 2026-08-31**: `app/publicar/paso/[paso]/atributos-y-zona.test.tsx` (8 pruebas) cierra **los cinco atributos y el buscador de zona**, los dos últimos que el cuarto avance dejó nombrados. Los atributos se afirman acotados al `<fieldset>` del paso 5 —el paso 1 dibuja otro con la misma clase—: los cinco nombres apareados con sus cinco etiquetas y que son exactamente cinco; lo declarado vuelve marcado y lo demás vuelve DIBUJADO y sin marcar (pedido por un ayudante que falla si el control no está, porque un `not.toContain("checked")` sobre un control borrado pasa solo); y la mitad que contesta la 14.9: **una casilla no distingue `false` de «no lo declaró» —los dos borradores dibujan el MISMO byte de `<fieldset>`— y lo que los separa se lee en el riel**, «Ninguno» contra el nombre corto, que es por qué el borrador lleva `featuresDeclared` aparte. Con su cuarta: «No tiene ninguna» es un POST propio fuera del formulario de las casillas —sin él «Ninguno» es inalcanzable con el script apagado, porque nada destilda cinco casillas al enviar— y no aparece en ningún otro paso. El buscador se afirma **con `renderToStaticMarkup`, que es la 18.12 medida y no supuesta**: es un GET que CIERRA antes del formulario que guarda (comprobado por posición y no por la clase, que es un hash del CSS) y no lleva `step`; devuelve lo tecleado, y nada cuando no se buscó; **la lista ya viene servida con el alcance que desambigua dos nombres iguales, y sin buscar no se consulta el vocabulario**; y la zona ya elegida se sigue enviando cuando la búsqueda no la muestra, con su par —cuando sí la muestra hay un solo control y no dos. **Diez mutaciones corridas, ninguna en blanco** (una ocurrencia exacta verificada antes, sha256 comparado después, restauración por copia y `git diff --stat` limpio), y cada una pone roja exactamente la prueba cuyo título la nombra. **La medida del hueco, en vez de la afirmación: nueve de las diez no ponían roja NINGUNA de las 2.292 pruebas restantes** con este archivo fuera — la etiqueta de un atributo cruzada con la columna de otro, la casilla invertida, «No tiene ninguna» guardando el paso 1, buscar posteando, el campo sin devolver lo tecleado, el alcance de un resultado dibujando su etiqueta, la consulta al puerto sin lo tecleado, y la zona guardada dejando de enviarse. La décima —quitar la guarda de `featuresDeclared` en `stepSummary`— ponía roja 1. **Lo que esta tarea NO cierra, y por eso la casilla sigue abierta.** Su enunciado abre dos conjuntos y sólo uno queda cerrado: cerrado «todo lo que `PublishStep` dibuja que no es el error de campo» —riel, contador, botón contextual, cinco atributos y buscador de zona, los cinco—; **abierto el mensaje de campo de seis de los nueve pasos** (1, 4, 5, 6, 8 y 9), porque los pasos 3, 7 y 2 los cerraron la 18.25, la 18.17 y la 18.7 y nadie tocó los otros seis. **Contradicción anotada, no arreglada**: el encargo llama «paso 1» al buscador de zona y el código dice paso 2 —`STEP_COPY.zona.number` es 2 y el comentario de `PublishStep.tsx` dice «el buscador del paso 2»—; se siguió el código. Y el hallazgo del cuarto avance sobre `mapa-de-pasos.test.tsx` **no tocó este trabajo**: ninguna de las diez mutaciones puso roja esa prueba, y las aserciones de acá van acotadas al `<fieldset>`, al `<nav aria-label="Progreso">` o al formulario `method="get"` **CIERRA el 2026-08-31.** `app/publicar/paso/[paso]/negativa-de-campo.test.tsx` (15 pruebas) cierra **el mensaje de campo de los seis pasos que faltaban** —1, 4, 5, 6, 8 y 9—, o sea el conjunto que el quinto avance dejó abierto. Con eso los nueve pasos y las cinco piezas que `PublishStep` dibuja fuera del mensaje quedan afirmados, y esta casilla ya no tiene nada suyo sin aserción. **Se sirve la ruta y no el componente, y la razón es una decisión que ninguna prueba nombraba**: `page.tsx` pasa `draft.violations` —lo que `actions.ts` guardó del ÚLTIMO INTENTO— y no las violaciones recién calculadas, así que un paso al que se llega sin haber enviado nada NO grita. Escribirle `violations` a mano al componente mediría una pantalla que nadie sirve. **Diez mutaciones corridas, ninguna en blanco** (una ocurrencia exacta verificada antes, sha256 comparado después, restauración por copia y `git diff --stat` limpio), y cada una pone roja exactamente la prueba cuyo título la nombra. **La medida del hueco: nueve de las diez no ponían roja NINGUNA de las 2.300 pruebas restantes** con este archivo fuera —el paso 1 leyendo el error del título, el paso 4 diciendo primero los metros, los controles del paso 4 anunciando el mensaje, el título perdiendo `aria-describedby`, el contador del paso 6 contando la descripción, el paso 8 leyendo el error del título, el contacto perdiendo el respaldo de `contactValue`, y —la que más pesa— `page.tsx` leyendo las violaciones frescas, que pone roja las cinco pantallas abiertas sin intento previo y ninguna otra. La décima —mudar `parkingSpots.invalid` al paso 5— ya la cubría `publication-steps.test.ts`. **Tres desviaciones de la regla de la 18.22 halladas y NO arregladas, afirmadas tal como se sirven** (cada una necesita su propia tarea): el paso 4 dibuja UN solo mensaje para sus cuatro controles, así que la negativa de los metros cuadrados se lee antes del campo de habitaciones y ninguno de los cuatro lleva `aria-invalid` ni `aria-describedby`; el campo del dato de contacto del paso 9 se marca **sólo con una clase —un borde—**, que es exactamente lo que `violation-copy.ts` llama «invisible para quien no distingue colores y para el modo de alto contraste»; y las opciones del paso 1 y del paso 9 tampoco nombran su mensaje, que se lee por posición dentro del `<fieldset>`. **Cuarta contradicción anotada**: la frase de `photos.required` manda a «subir al menos una foto en el paso 2» y las fotos son el paso 8 (`STEP_COPY.fotos.number`). Y el hallazgo del cuarto avance sobre `mapa-de-pasos.test.tsx` tampoco tocó este trabajo: ninguna de las diez mutaciones puso roja esa prueba.

- [x] 18.26 **La sección de fotos de editar no dibuja la foto.** La 18.21 la lista por su posición —«Foto 1 (portada)», «Foto 2»— porque `listing_photo` no guarda ni nombre de archivo ni `alt_text` a propósito, así que la posición es el único nombre que una foto tiene. **El costo, dicho en vez de escondido**: quien tiene seis fotos parecidas elige por un ordinal y puede quitar la que no era. **Lo que falta no es una decisión sino un cable**: `listing-discovery` ya tiene `photoUrl` y `photoAltText` puros y probados, y `listing_photo_derivative` ya guarda la clave de la `thumb`; falta la lectura que las una —una lectura al lado de `listPhotoIdsInOrder`, no un ensanche de la escritura— y el `<img>` con su `alt` compuesto. Es la sexta vez que este cambio encuentra la forma «componente listo, dato presente, nadie los une», y por eso queda escrita como tarea en vez de como comentario — **CIERRA el 2026-08-30, y era un cable: ninguna de las tres piezas hubo que construirla.** `photoUrl` y `photoAltText` estaban puros y probados en `listing-discovery`, `listing_photo_derivative` guardaba la clave de la `thumb`, y `DrizzleListingPhotoSet` ya leía el orden. Lo que faltaba era la lectura que las une y el `<img>`.

  **Dónde fue cada cosa.** La lectura es `ListingPhotoThumbnailPort` en `listing-photo-set.port.ts`, **una interfaz al lado de `ListingPhotoOrderPort`, no un método más adentro** (AGENTS.md §3): aquél contesta una sola pregunta —en qué orden van— y `planPhotoRemoval` recibe su arreglo tal cual, así que agregarle las claves de R2 le pondría a `detachPhotoFromListing` un join contra `listing_photo_derivative` que nunca pidió. **`ListingPhotoAttachmentPort` quedó intacto**, con su único `attachPhoto`. El adaptador es `DrizzleListingPhotoSet.listPhotoThumbnailsInOrder`, y `loadListingPhotosForEdit` cambió su dependencia de `order` a `thumbnails` en vez de sumar un segundo caso de uso.

  **Tres decisiones que la tarea no traía.** Primera, **`leftJoin` y no `innerJoin`, con `name = 'thumb'` en el `ON` y nunca en el `WHERE`**: la cuadrícula puede permitirse el `innerJoin` porque un aviso sin derivadas simplemente no se muestra, pero acá el renglón es el ÚNICO camino para quitar una foto, así que dejarla afuera produciría una fila que el aviso sigue mostrando y su dueño ya no puede sacar. Vuelve con `thumbKey` en `null`. Segunda, **el `alt` se compone sin zona**: `photoAltText` la emite opcional a propósito, el dueño ya sabe dónde está su aviso, y traerla pediría una segunda lectura para una coma. Tercera, **`readPhotoPublicBaseUrl()` se lee DESPUÉS del `notFound()`**: al revés, un despliegue sin `R2_BUCKET_PUBLIC_URL` distinguiría un id ajeno (500) de uno inventado (404), que es justo lo que `loadListingForEdit` cierra. Sin esa variable la pantalla se niega entera en vez de volver al ordinal en silencio (§7): un despliegue mal configurado no debe verse igual que uno correcto.

  **Lo prueban**, en `app/mis-avisos/[id]/editar/editar-servido.test.tsx` (6 pruebas nuevas sobre los bytes servidos), «el alt de cada foto es la frase compuesta del dominio, con su posición y el total» y su par «no le pone a la segunda foto el nombre de la primera»; en `tests/integration/listing-photo-editing.test.ts` (3 contra Postgres), «una foto sin derivadas sigue en la lista, con la miniatura en null» y «devuelve la clave de la thumb de cada foto, y no la de ningún otro tamaño»; y en `edit-listing-photos.test.ts` (2), «una foto sin miniatura vuelve igual, no se filtra de la lista». **Las cuatro mutaciones obligatorias pusieron roja exactamente la prueba que les tocaba**: `leftJoin`→`innerJoin` (1), `'thumb'`→`'card'` (2), el `alt` compuesto→`alt="foto"` (2), y el `src` a la miniatura→al id (3).

  **Medida: 486 líneas revisables** (469 agregadas, 17 borradas), sin renombrar ningún archivo. Sobre las 400 de AGENTS.md §6 y bajo el techo de 600 del arnés; se dice en vez de recortar una prueba. **El paquete**: `/mis-avisos/[id]/editar` 107,39 → 107,42 KB gzip (+0,03), el peor del camino de lectura 109,25 → 109,28 contra 130. Las imágenes no son JavaScript, y apuntan a la `thumb` de 160×120 —no a la foto entera— que es para lo que las cuatro derivadas existen.

  **Publicar NO tiene el mismo defecto, y la razón importa.** El paso 8 ya dibuja la miniatura desde un `blob:` local y su renglón lleva el nombre del archivo al lado, así que su `alt=""` es correcto: la fila está etiquetada. Lo que se pierde al publicar es justamente ese nombre —`listing_photo` no lo guarda— y por eso la pantalla de editar necesitaba componer el suyo

- [x] 18.25 **El contador de una negativa de editar cuenta lo GUARDADO, no lo que se envió.** `description.tooShort` dice «Vas N» y esa N sale de `PublishCopyContext`, que en la pantalla de editar sólo recibe `aviso.title`: la descripción que la persona acaba de escribir no viaja de vuelta, porque lo único que vuelve en la dirección son los códigos. O sea que hoy la frase diría «Vas 0» sobre una descripción de 24 caracteres, y pasarle `aviso.description` la haría decir el largo de la que está en la base, que tampoco es la que se rechazó — **las dos son un número que nadie escribió**. La 18.22 no lo empeoró; lo puso a la vista al mover el mensaje al lado del campo. **Lo que falta para poder arreglarlo** es dónde devolver el valor ofensivo sin volver a meter la descripción entera en la URL, que es exactamente el problema que la 18.19 ya midió en 7 KB de dirección — **CIERRA el 2026-08-30 con la 18.19, y la respuesta a las dos es una asimetría**: un largo son cuatro dígitos y una descripción son 1.200 caracteres, así que **vuelve la medida y jamás el texto**. `guardarEdicion` cuenta lo que se posteó y lo manda como `largoDescripcion`; la pantalla lo lee con `readCarriedMeasure`, que sólo acepta dígitos.

  **La forma es la garantía, no el cuidado.** `PublishCopyContext` **ya no tiene** `description` ni `title`: lleva `descriptionLength` y `titleLength` y nada más. Con el campo de texto borrado, ninguna de las dos versiones equivocadas —«Vas 0» o el largo de la guardada— se puede volver a escribir por accidente; el compilador las rechaza. Es la misma forma que el estado de contacto bloqueado, que no tiene propiedad `value` para no poder filtrarla (AGENTS.md §7).

  **Sin medida, silencio.** `counted()` devuelve cadena vacía cuando no llegó número, así que la frase dice «✱ Mínimo 120 caracteres.» y se calla el contador. Un parámetro ausente, vacío, negativo, decimal, con espacios, con `1e3` o con doce dígitos no dibuja nada: **un número ausente es preferible a uno inventado**, que es la misma decisión que `import-copy.ts` ya había tomado cuando la fila no viaja hasta la copia. El `?? codigo` de `listingEditViolationMessage` y el de `photoRemovalRefusalMessage` quedaron intactos y sus pruebas siguen verdes.

  **CORRECCIÓN al alcance, dicha y no escondida: el título tenía el MISMO defecto en la MISMA pantalla.** `title.tooLong` contaba `aviso.title` —el guardado— y el `<input>` acepta el doble del máximo, así que era alcanzable. Se arregló con el mismo mecanismo (`largoTitulo`) en vez de dejar «arreglado en un campo, roto en su gemelo».

  **Publicar NO tiene este defecto, y ahora se prueba en vez de afirmarse.** El borrador que el paso dibuja es el que se acaba de guardar en la cookie, así que su descripción es la que se escribió; lo único que cambió es que ahora se mide antes de entregarla. Lo afirman *«dice los caracteres escritos, en puntos de código»* y *«sin descripción en el borrador no dibuja un cero que nadie escribió»* (`app/publicar/paso-servido.test.tsx`), sobre los bytes servidos del paso 7.

  **Un solo contador para los tres lugares**: `measureOf` en el dominio, usado por `PublishStep`, por `revisar/page.tsx` y por `guardarEdicion`. Puntos de código, igual que el validador —con `String.length` un emoji contaría doble y la pantalla rechazaría una descripción que su propio contador llamó suficiente—, y `undefined` para lo no contestado, que no es lo mismo que cero.

  **Lo prueban**, en `app/mis-avisos/[id]/editar/editar-servido.test.tsx`, *«dice el largo de la descripción que se acaba de rechazar»*, *«no dice el largo de la que está guardada, que es otra»*, *«sin medida dice el mínimo y se calla el contador, en vez de dibujar un cero»* y *«una medida inventada en la dirección no dibuja ningún número»*; en `app/mis-avisos/actions.test.ts`, *«la negativa lleva la medida de lo que se envió, jamás el texto»* y *«un campo vacío no manda una medida, porque no se envió ninguno»*; y en `app/publicar/violation-copy.test.ts`, *«sin medida no inventa un cero: la frase va sin contador»*. **La mutación que importa es la corrección equivocada**: cambiar la pantalla a `measureOf(aviso.description)` —el arreglo obvio que el enunciado descarta— pone rojas cuatro, entre ellas la que lo dice por su nombre.

  **Medida del trabajo conjunto (18.19 + 18.25): 610 líneas revisables** (550 agregadas, 60 borradas) en 15 archivos de código, sin contar este plan. Sobre las 400 de AGENTS.md §6, y se dice en vez de recortar una prueba: partirlo habría dejado el contador de editar pidiendo un valor que la otra mitad todavía no sabía transportar. **El paquete no se movió**: `/mis-avisos/[id]/editar` 107,42 KB gzip y el peor del camino de lectura 109,28 contra 130, los dos idénticos a antes — lo que cambió es lo que viaja en una dirección, que no es JavaScript.
- [x] 18.15 **El menú `⋯` de las fotos, y por qué cambia según el dispositivo.** Móvil: acciones nombradas — "Mover arriba" · "Mover abajo" · "Hacer portada" · "Quitar del aviso". Escritorio: arrastrar para reordenar, el resto en el mismo menú. La razón está escrita y no es preferencia: *"arrastrar con el pulgar en un teléfono lento no es confiable. Con mouse sí."* **Dos textos que la spec marca como NO decorativos** y que hay que emitir literalmente: *"Quitar del aviso — no borra la foto de tu teléfono"* (sin eso un dueño duda antes de tocar) y *"Hacer portada — se ve en la lista y arriba del aviso"* (la palabra portada no significa nada sola). ~~Hoy `PhotoUploader.tsx` no tiene menú, ni reordenar, ni portada~~ — **corrección (2026-08-30, medida antes de escribir una línea): esa frase era falsa en las tres cosas.** Reordenar, portada y quitar YA shipeaban, como botones de glifo (`◆ ↑ ↓ ×`) con `aria-label`. **Lo que faltaba era otra cosa y era peor**: las dos frases no decorativas existían SÓLO dentro de `aria-label` y `title`, o sea invisibles justo para el dueño vidente al que la spec quiere tranquilizar, y un `title` no aparece nunca en un teléfono, que es donde se publica; «Quitar del aviso» ni existía como frase —se emitía partida en `Quitar ${nombre} del aviso`—; no había menú `⋯`; no había arrastre; y las reglas (qué acciones ofrece una foto, qué es la portada, cuándo se rechaza quitar) vivían dentro de un componente de cliente, o sea fuera del piso de 90%, sin una sola prueba: `PhotoUploader.tsx` no tenía archivo de pruebas. **CIERRA.** Las reglas son `src/modules/listing-publication/domain/draft-photo-actions.ts` (21 pruebas), la copia `app/publicar/photo-action-copy.ts` (11) y la pantalla `app/publicar/fotos/PhotoUploader.tsx` (10). Lo prueban ««Quitar del aviso» sale entero, no partido por el nombre de la foto» y «la negativa de la única foto dice la salida, no sólo el no» (`photo-action-copy.test.ts`), «las dos frases no decorativas se leen, no se adivinan» y «la única cosa que el puntero decide es el atributo `draggable`» (`PhotoUploader.test.tsx`), y «con el pulgar no se ofrece, aunque haya seis fotos» más «quitar la portada asciende a la siguiente, y la nombra en vez de dejarlo al orden» (`draft-photo-actions.test.ts`).

  **Tres decisiones que la tarea no traía y hubo que tomar.** Primera, **quitar la portada asciende a la siguiente y se anuncia con nombre** en la región viva («Ahora la portada es «Cocina».»): quien quita la primera foto cambió la cara del aviso sin pedirlo, y «la portada cambió» sobre seis miniaturas no dice cuál quedó. Segunda, **quitar la última foto se RECHAZA**, no se avisa: el piso es `MIN_PHOTOS_FOR_ACTIVATION`, la misma constante que `activateListing` revalida en etapa `"activation"`, y enterarse cuatro pasos después es peor que enterarse al tocar (AGENTS.md §7: la forma preferida es la negativa). La negativa dice la salida —agregá otra y después quitá ésta— así que cuesta un toque, no un camino cerrado. Tercera, **el objeto ya subido a R2 se queda huérfano**: su clave sale de los campos ocultos, así que ningún borrador lo referencia y ningún aviso lo muestra; borrarlo pediría permiso de borrado sobre el bucket, que es ensanchar un puerto de escritura angosto por un huérfano — queda como 18.23.

  **Y el arrastre es mejora, no mecanismo, y eso está medido y no prometido.** Los cuatro renglones nombrados salen en los dos anchos, dentro de un `<details>` que abre con clic y con Enter sin una línea de script; lo único que decide `(pointer: fine)` es el atributo `draggable`, y una prueba cuenta que `dragEnabled` aparece exactamente dos veces en el archivo — si mañana condiciona un botón, cae. Un reordenar que desaparece para quien usa teclado o lector de pantalla sería una regresión, no un refinamiento
- [x] 18.16 **Al volver desde revisar, decir QUÉ cambió.** *"Cambiaste habitaciones de 2 a 3. El resto del aviso quedó como estaba."* La 18.8 cubre que volver no borre los pasos posteriores; **esto es lo otro**: que la persona lo SEPA. Sin el mensaje, nadie distingue "se guardó" de "se perdió", y el diseño lo lista entre las tres cosas que compensan los nueve momentos de abandono. **Hecho.** `describeDraftChanges` y `parseDraftChanges` en `src/modules/listing-publication/domain/publication-steps.ts`, la frase en `app/publicar/step-copy.ts` y la vuelta en `app/publicar/change-notice-url.ts`. Lo prueban «nombra TODOS los campos que cambiaron, no solo el primero» y «no cuenta como cambio un valor identico al anterior» (`publication-steps.test.ts`), «nombra los tres campos que cambio un mismo paso, no solo uno» y «sin cambios no dice nada, porque el silencio no miente» (`step-copy.test.ts`), y en los bytes servidos «nombra los dos campos cuando el paso escribió dos» y «calla cuando la URL anuncia un cambio entre dos valores iguales» (`app/publicar/revisar/aviso-de-cambio.test.tsx`).

  **Tres correcciones sobre lo que decía la tarea.** Primera: el mensaje de UN campo ya existía desde la PR #94 y estaba conectado — lo que faltaba era que dijera la verdad. El paso 4 escribe cuatro campos de una sola vez y sólo se nombraba el primero, así que «el resto del aviso quedó como estaba» era falso sobre los otros tres; ahora se nombran todos, con dos verbos separados («Cambiaste X de A a B. Pusiste Y en C.») porque mezclarlos deja un hueco donde nunca hubo valor anterior, y los cinco atributos comparten etiqueta y se dicen una sola vez. Segunda: sin cambios no se dice nada, y eso ahora incluye una URL escrita a mano que anuncie un cambio entre dos valores iguales o un campo que no es del borrador — antes salía «Cambiaste habitaciones de 2 a 2» y «Cambiaste undefined» (§7, fallar cerrado). Tercera: **la frase vive en `app/publicar/`, no en `src/modules/`**, contra lo que pedía el enunciado. Las reglas —qué cambió y qué se le cree a la URL— sí bajaron al dominio y están bajo el piso del 90 %; el español se quedó donde ya vivía el de las violaciones y el de los pasos, con su propia prueba. Bajarlo habría creado una segunda lista de nombres de campo al lado de `CHANGE_FIELD_LABEL`, que es exactamente la que después nadie mantiene
- [x] 18.17 **El equivalente al riel en móvil, para volver atrás.** §12 lo nombra entre lo que falta diseñar. En escritorio el riel de nueve pasos es navegable y lleva "Volver a revisar" fijo al pie; en móvil sólo hay la flecha ← y el botón contextual. **Falta el mapa**: en un teléfono se sabe cuánto falta pero no se puede saltar a un paso hecho salvo desde revisar — **CIERRA el 2026-08-30, y con ella la lista entera de §12.** La regla es `jumpableStepsFrom` en `src/modules/listing-publication/domain/publication-steps.ts`, la copia `STEP_MAP_TRIGGER_LABEL` en `app/publicar/step-copy.ts`, y el cable el `jumpable` de `app/publicar/PublishStep.tsx` con su hoja en `publish-steps.module.css`. Lo prueban «ofrece los pasos ya contestados que quedaron atrás, que es el salto que en un teléfono no existía» y «no ofrece un paso sin contestar, que es el criterio 10 dibujado en el teléfono» (`publication-steps.test.ts`), en los bytes servidos «dibuja un enlace a cada paso ya contestado, sin ningún formulario ni script detrás» y «no dibuja el paso sin contestar, que es adonde el enlace no aterrizaría» (`app/publicar/paso/[paso]/mapa-de-pasos.test.tsx`), y la geometría «a 360 el mapa abre encima y no le cuesta un solo píxel de alto a la columna» (`tests/measure/layout.spec.ts`)

  **De dónde salió la pantalla, ya que ninguna lámina la dibuja.** Se derivó de tres cosas que el sistema ya tenía escritas, no de una preferencia. Primera, el encabezado de `publish-steps.module.css`: *«en 360 hay una barra de 3px y hay que confiar en un porcentaje; en 1280 se ve el riel entero… es la diferencia entre saber cuánto falta y poder hacer algo al respecto»* — o sea que el hueco ya estaba nombrado en la hoja de estilos y lo que faltaba era lo segundo. Segunda, el mecanismo: **el mismo `<details>` + hoja del menú `⋯` de las fotos que cerró la 18.15 el día anterior**, que abre con un toque y con Enter sin una línea de JavaScript. Tercera, la anatomía de fila del riel —✓ o número, y el VALOR del paso y no su nombre— y `--target-min`, que son 44 px por decisión del fundador (16.24). Ningún token nuevo, ninguna hoja de estilos nueva, ningún glifo nuevo.

  **El disparador es el contador «4 / 9» que ya estaba, y por eso el mapa costó CERO píxeles de alto.** Nueve renglones apilados en el flujo habrían sido una segunda lista para desplazar, y el punto es saltar: la hoja se dibuja encima, anclada a `.bar` —que ya es `sticky` y por lo tanto su bloque contenedor— y no al disparador, porque anclada al disparador se salía de costado en 360. Medido: disparador 44×44, hoja x=16 y ancho 328 dentro de una ventana de 360, y el desplazamiento de la columna dentro del paso queda en 100 antes y después de abrir. La mutación que pasa `.mapSheet` a `position: static` lo empuja a 274 y pone roja exactamente esa prueba.

  **Ninguna segunda regla de alcance, que era el riesgo real.** `jumpableStepsFrom` pregunta por `isStepNavigable` —la misma puerta que `page.tsx` vuelve a aplicar al aterrizar— y lo único que agrega es sacarse a sí mismo. Un mapa con su propia copia habría ofrecido un salto que el destino rechaza, que es el defecto que la 18.18 ya pagó una vez con `volver=revisar`. La prueba «ofrece exactamente lo que el riel enlaza, sin una segunda regla al lado» es la anti-deriva: la mutación que le saca `isStepNavigable` pone rojas tres, la que le saca `candidate !== stepId` pone rojas otras tres.

  **Dos consecuencias de esa decisión, dichas en vez de escondidas.** Primera: **el mapa también ofrece el paso actual sin contestar** —desde el paso 4 con el 5 en blanco, el renglón «Qué tiene» está—, porque `isStepNavigable` deja pasar el paso actual y **el riel de escritorio ya lo enlazaba**. Es navegable de verdad: la página no redirige. Corregirlo sería escribir el `if` que esta tarea existe para no escribir, y si algún día se decide que no, se cambia en `isStepNavigable` y cambia en los dos anchos a la vez. Segunda: **de 768 para arriba el contador «4 / 9» desaparece junto con el mapa**, por la misma frase que ya rige la barra de 3 px —*el riel reemplaza la barra, no la acompaña*—: los nueve pasos con su ✓ y su valor dicen todo lo que el contador decía, y más. Ninguna prueba afirmaba el contador en escritorio; se verificó antes de sacarlo.

  **Prop requerida y no opcional, y encontró a los dos.** `jumpable` entra como `readonly` requerida en `PublishStepProps`: `tsc` nombró en el acto a `app/measure/page.tsx` y a `app/publicar/paso-servido.test.tsx`, que es la misma técnica con la que la 18.19 encontró dos renderers. Opcional, el arnés de medición habría seguido midiendo una pantalla sin mapa y en verde.

  **Medida: 355 líneas revisables** (349 agregadas, 6 borradas) en 9 archivos, sin contar este plan. Bajo las 400 de AGENTS.md §6. **El paquete: `/publicar/paso/[paso]` pasó de 109,23 a 109,35 KB gzip**, medido construyendo `ee2f905` en un árbol desechable y no citando un número viejo. Los 0,12 KB son la hoja de estilos, que Next cuenta en la primera carga de la ruta; **no shipeó una sola línea de JavaScript nueva** —`PublishStep` es un componente de servidor y el mapa es `<details>` más enlaces—. Sigue siendo el peor del camino medido, contra un techo de 130.

  Las otras cuatro cosas que §12 daba por diseñar **ya se resolvieron el 2026-08-25**: mis publicaciones e importar cartera están en `Rentas - Cuenta e Importar.dc.html`, y el correo de vencimiento y los errores de validación quedaron cubiertos por el trabajo de la fase 7 y por los cuatro rechazos del importador. Queda ésta, y hoy tampoco: **§12 no tiene nada abierto.** **La 18.14 (editar un aviso publicado) cerró entera con la 18.20**, salvo las fotos, que son la 18.21

- [x] 18.18 **«Descartar el cambio» está dibujado y no existe.** La lámina de escritorio lo pone como enlace secundario al lado de «Guardar y volver a revisar», en el marco rotulado *«volviendo al paso 4 desde revisar»*. Hoy ese paso sólo ofrece guardar: quien abrió el paso, tocó un campo y se arrepintió no tiene salida que no escriba. Con el borrador en cookie no es un `undo` —los valores anteriores viven en la cookie hasta que se guarda—, así que es un enlace a `/publicar/revisar` sin postear, y por eso es barato. Sin él, la única forma de deshacer es acordarse del valor viejo y volver a escribirlo — **CIERRA el 2026-08-30, y el borrador SÍ está en cookie: la premisa se verificó antes de construir.** `readPublicationContext` lee `DRAFT_COOKIE` y `DRAFT_TEXT_COOKIE` y nada más; durante los nueve pasos no existe fila. La regla es `offersDiscardToReview` en `src/modules/listing-publication/domain/publication-steps.ts`, la copia `DISCARD_CHANGE_LABEL` en `app/publicar/step-copy.ts`, y el cable el `discardHref` de `app/publicar/PublishStep.tsx`. Lo prueban «se ofrece al paso abierto desde revisar, que es donde la lamina lo dibuja», «no se ofrece en el recorrido hacia adelante, que no viene de ninguna revision» y «no se ofrece cuando revisar todavia redirige, aunque la URL diga que viene de ahi» (`publication-steps.test.ts`), y en los bytes servidos «dibuja la salida hacia revisar con su dirección, sin ningún formulario detrás» y «no la dibuja en el recorrido hacia adelante, que no viene de ninguna revisión» (`app/publicar/paso/[paso]/descartar-el-cambio.test.tsx`)

  **La regla no es `returningToReview` a secas, y la diferencia es una salida que no sale.** `volver=revisar` es una afirmación de la barra de direcciones, no un hecho: escrita a mano sobre un borrador con un paso sin contestar, el paso dibuja —`isStepNavigable` deja pasar el paso actual— pero `/publicar/revisar` redirige por `isDraftReadyForReview`, así que «Descartar el cambio» aterrizaría donde no dijo. Por eso la función pregunta por **la misma puerta que revisar aplica**, no por una copia suya que pueda discrepar (AGENTS.md §7). La mutación que deja `return returningToReview;` pone roja exactamente una prueba, que es la que dice eso.

  **Se dibuja con `styles.secondary`, el mismo nivel que «Atrás», y no se agregó una hoja de estilos.** La lámina lo pinta como texto `--soft` sin borde, pero **la jerarquía de botones de la especificación nombra «Descartar» en la fila Neutro**, junto a «Atrás» y «Publicar otra» — y AGENTS.md §2 dice que las láminas son referencia y no código a copiar, con sus estilos en línea que no llegan a producción. Se siguió la tabla, que es la que el sistema define.

  **`discardHref` es obligatorio y no opcional a propósito.** Un prop opcional habría dejado que un renderizador se olvidara en silencio, que es la forma de defecto —«componente listo, dato presente, nadie los une»— que este cambio ya encontró seis veces. Al hacerlo obligatorio, `tsc` señaló los dos renderizadores que faltaban: el arnés de `app/measure/page.tsx` y `app/publicar/paso-servido.test.tsx`.

  **Contradicción de plan, dicha y no arreglada acá.** La 18.5 se titula «Server-side draft, one row per step, keyed by session» y su casilla está marcada; su propio texto aclara «**Parcial**: cookie de 30 minutos y DOS cookies», porque la tabla nueva pedía migración. O sea que el título de la 18.5 describe algo que no shipeó y el código coincide con el texto de la 18.18, no con el título de la 18.5. Nada de esta tarea depende de eso: descartar es irse sin postear, y eso vale igual con cookie o con fila.
- [x] 18.23 **La foto que se subió y nadie adjuntó.** El paso 8 comprime, firma y sube ANTES de que se guarde el paso, así que quitar una foto del aviso deja el objeto en R2 sin que ninguna clave lo nombre: el borrador no lo referencia y ningún aviso lo muestra. Hoy se queda ahí a propósito —borrarlo desde el navegador pediría una acción de servidor con permiso de borrado sobre el bucket, que es ensanchar un puerto de escritura deliberadamente angosto (AGENTS.md §3) por un huérfano—, pero *a propósito* no es *gratis*: el plan gratuito de R2 se dimensionó contra las derivadas guardadas (~7.900 avisos), y un huérfano por foto descartada come de esa misma cuenta sin aparecer en ninguna. La salida barata es una regla de ciclo de vida del bucket sobre el prefijo de subidas sin adjuntar, no un puerto nuevo. **Lo que falta para poder hacerlo** es saber cuánto pesa: nadie midió todavía cuántos objetos sin adjuntar hay por aviso publicado. **Y desde la 18.21 hay una segunda fuente de huérfanos, que la 18.21 dejó dicha antes de construir**: quitar una foto de un aviso publicado deja sus derivadas bajo el prefijo PROMOVIDO, donde también viven las de todos los avisos activos, así que la regla de ciclo de vida barata no las alcanza sin arrasar con las que sí se muestran — sólo son distinguibles por la ausencia de su fila en `listing_photo`, o sea un barrido contra la tabla y no una regla del bucket — **la 18.29 cierra la mitad que faltaba y agranda el enunciado, sin cerrar la casilla**: la fuente MÁS GRANDE de huérfanos no era ninguna de las dos nombradas acá, sino que el borrador vivía en dos cookies de treinta minutos. Cada publicación abandonada pasado ese plazo dejaba hasta seis fotos ya promovidas a cinco derivadas WebP **que nada podía volver a nombrar**, porque el único registro de esas claves se moría en el navegador de quien se fue: ni la regla de ciclo de vida del bucket ni el barrido contra `listing_photo` las alcanzan, porque el objeto está bajo el prefijo promovido y no tiene fila. Con `publish_draft` esas claves ahora existen en la base durante 24 horas, que es lo que hace POSIBLE el barrido; construirlo es la 18.32 — **avance del 2026-09-01 (18.30), y la casilla sigue abierta a propósito**: las claves ya no viven en el navegador de quien se fue, viven en la fila. El flujo de publicar dejó las dos cookies y escribe `publish_draft` en cada paso, así que a partir de hoy un abandono deja las hasta seis claves nombrables durante 24 horas en vez de irreversiblemente huérfanas. **Lo que da el valor sigue sin construirse**: nada borra todavía esas derivadas de R2, que es la 18.32 **CIERRA el 2026-09-01, y las tres fuentes que este enunciado nombra se cerraron por caminos distintos.** **Primera — configuración del fundador, anotada acá para que no sea infraestructura invisible.** El 2026-09-01 el fundador creó en las reglas de ciclo de vida del bucket de R2 la regla `barrer-subidas-sin-adjuntar`: alcance por prefijo `incoming/` —**con la barra final a propósito**, porque el prefijo se compara literal y `incoming` a secas también agarraría algo como `incoming-backup/`—, acción borrar los objetos subidos **al día**, y abortar las subidas multiparte incompletas también al día. La transición a Acceso Infrecuente quedó **sin marcar a propósito**: ahí los objetos se mueren dentro del día y esa clase cobra una duración mínima, así que transicionar antes de borrar costaría plata para nada. Un día es la granularidad mínima de R2 y sobra de lejos: un objeto `incoming/` vive segundos en el camino normal, porque `processUploadedPhoto` lo borra él mismo apenas las cinco derivadas están en `photos/`; lo que sobreviva un día ahí es, por definición, una subida que nadie terminó. **Segunda — las derivadas de una foto desprendida de un aviso publicado: código, la 18.32.** `detachPhotoFromListing` lee las cinco claves con `listDerivativeKeys` ANTES de borrar la fila y las quita de R2 DESPUÉS. Éstas sí viven bajo el prefijo promovido y ninguna regla del bucket las alcanza sin arrasar con las de todos los avisos activos, que es exactamente lo que este enunciado decía. **Tercera — el borrador abandonado: la 18.32 le borra la fila, y sus objetos son `incoming/`, así que la regla de arriba también los alcanza.** Y acá va la **corrección de este enunciado**, medida: dice que el abandono deja derivadas ya promovidas, y no. El paso 8 guarda la clave `incoming/` que firmó `requestUploadTargets`; la promoción a `photos/` ocurre recién al publicar. Lo prueba `app/publicar/actions.ts:166` (`incomingKey: photo.key`) junto con `process-uploaded-photo.ts:137`, que rechaza toda clave que no esté bajo `incoming/`. **Segunda corrección: la premisa que este enunciado y la 18.21 compartían era falsa.** Los dos dicen que borrar de R2 «pediría ensanchar un puerto de escritura deliberadamente angosto». `PhotoStoragePort.remove` existe desde D12, `purgeExpiredPhotos` (19.4) ya lo usa contra este mismo bucket y `processUploadedPhoto` lo llama en cada publicación: las credenciales ya necesitan `DeleteObject`. No hubo puerto que ensanchar y no se agregó `list` — el barrido trabaja desde filas, nunca enumerando el bucket. **Lo que este enunciado NO nombra y quedó abierto como tarea propia, dicho antes de que alguien lo descubra pagándolo**: la 18.35 (publicación fallida a mitad de camino) y la 18.36 (la regla del bucket puede matar las fotos de un borrador que todavía se puede retomar). Ninguna de las dos es de las tres que esta casilla contaba

- [x] 18.19 **El aviso de cambio viaja en la URL, y la descripción mide 1.200 caracteres.** `reviewPathFor` manda `campo`, `antes` y `ahora` por campo cambiado; corregir el paso 7 manda la descripción entera dos veces, que codificada pasa de 7 KB de dirección. Es anterior a la 18.16 y no empeoró con ella —ningún paso escribe la descripción junto a otro campo—, pero el día que un proxy corte por largo de URL, el paso se guarda y la vuelta a revisar falla. La descripción es de los campos que no dicen su valor en voz alta (`OPAQUE_CHANGE_FIELDS`), así que la salida probablemente sea no transportar lo que no se dibuja — **CIERRA el 2026-08-30, junto con la 18.25 y en un solo trabajo, porque eran la misma pregunta hecha dos veces.**

  **`OPAQUE_CHANGE_FIELDS` existía y se comportaba como la tarea decía**, verificado antes de escribir una línea: era una `Set` privada de `app/publicar/step-copy.ts` con los cinco atributos y la descripción, y `changeNoticeMessage` la consulta ANTES de mirar el valor, así que de esos campos nombra la etiqueta y calla el resto.

  **Corrección de la cifra, medida en vez de repetida** (AGENTS.md §5). «Pasa de 7 KB» es el peor caso y no el típico: con una descripción de 1.200 caracteres en castellano llano la dirección medía **1.438**; con acentos —que es como se escribe acá, y cada uno son seis caracteres codificados— pasa de **7.000**; con una de puros acentos llega a **14.449**. Las tres exceden el límite de cualquier proxy conservador, así que el defecto era real: lo que no era uno solo era el número.

  **Y la salida NO fue «no transportar lo que no se dibuja», que es lo que el enunciado proponía.** Mandar el campo con los valores vacíos habría roto dos cosas de una vez: `parseDraftChanges` descarta un cambio cuyo `antes` y `ahora` son iguales, así que la descripción habría desaparecido del aviso que la 18.16 construyó —«nombra TODOS los campos que cambiaron»—, y `""` es siempre igual a `""`, así que esa misma puerta se habría vuelto un pase libre para una dirección escrita a mano. **Lo que viaja es la MEDIDA**, en `carriedChangeValue` (`src/modules/listing-publication/domain/carried-value.ts`): un largo son cuatro dígitos, sigue distinguiendo dos descripciones distintas, y es exactamente lo único que la pantalla dibuja de ese campo. La dirección quedó en **55 caracteres** y deja de crecer con la descripción.

  **`OPAQUE_CHANGE_FIELDS` bajó al dominio entera, sin abrir una segunda** (AGENTS.md §1): quien decide que un campo no se dibuja es quien tiene que decidir que no viaja, y separadas una dejaría de dibujarlo mientras la otra lo sigue cargando. `step-copy.ts` la importa; no hay una lista nueva al lado de `CHANGE_FIELD_LABEL`.

  **Un hallazgo que sacó la mutación obligatoria, y no es cosmético.** Sacar `description` de la lista no ponía roja NINGUNA prueba del aviso de cambio: se afirmaba que un booleano no se lee en voz alta y nunca que la descripción tampoco. Desde que lo que llega en `antes` y `ahora` es un número, esa omisión habría dejado dibujar «Cambiaste la descripción de 67 a 1200» —dos números que se leen como si fueran el texto—. Lo cierra *«tampoco lee la descripción: nombra el campo y calla el número que la mide»* (`app/publicar/step-copy.test.ts`), y esa mutación ahora pone roja exactamente una.

  **Lo prueban**, en `app/publicar/change-notice-url.test.ts`, *«manda la medida de la descripción, nunca la descripción»*, *«la dirección mide 55 caracteres donde medía 1.438»*, *«y con acentos, que es como se escribe en castellano, medía más de 7 KB»*, *«la dirección no crece con la descripción, que es lo que se arregló»* y *«y revisar sigue sabiendo que la descripción cambió, que es lo que dibuja»*; y en el dominio `src/modules/listing-publication/domain/carried-value.test.ts` (12 pruebas). La mutación de volver `reviewPathFor` al valor crudo pone rojas las seis.

> **Ojo al leer esta fase**: las tareas 18.1 a 18.8 están marcadas `[x]` como **hallazgos registrados**, no como trabajo entregado. Son el análisis de esta especificación contra el código. Verificado el 2026-08-25: `listing.status` sigue siendo `active | expired | hidden | draft` — **`pending_verification` (18.4) NO existe**, y el borrador por paso del lado del servidor (18.5) tampoco. Un tilde acá significa "lo sabemos", no "está construido"

- [x] 18.27 **(nueva — la decisión que la 18.7 dejó escrita en vez de tomar por su cuenta.) ¿Se puede corregir la referencia de un aviso publicado?** La tabla del fundador de la 18.14 nombra campo por campo lo que una edición toca y **la referencia no está en ella**, porque el campo no existía el 2026-08-29, que es el día en que se decidió. La 18.7 falló cerrado (AGENTS.md §7): la pantalla de editar no la ofrece, `EDIT_REQUEST_FIELD` no la lleva, y su negativa cae en `elsewhere` y se dice igual. **Las dos caras, para que la decisión no haya que reconstruirla:** a favor de permitirla, es exactamente la clase de «dato mal cargado» que la propia tabla admite para habitaciones y metros² —«corregir un dato mal cargado no es publicar otro aviso»—, la panadería puede cerrar, y hoy quien se equivoca de seña convive con ella treinta días; en contra, no hay ninguna razón de integridad como la del tipo de publicador (nada la filtra, nada la cuenta, no está en ninguna URL), así que el único costo de permitirla es el de otro campo en una pantalla que ya tiene ocho. **Lo que cuesta si se aprueba** es corto y está medido: un campo en `ListingEdit`, su fila en `EDIT_REQUEST_FIELD`, el `set` de `applyEdit`, el control en `app/mis-avisos/[id]/editar/page.tsx` —la copia ya existe, `CHANGE_FIELD_LABEL` la nombra «la referencia»— y ninguna migración. **Lo que NO se puede hacer sin decidir** es dejarlo como está y llamarlo cerrado: hoy la frase de la 18.14 —«hoy la tabla entera del fundador se toca desde el navegador»— sigue siendo verdadera sólo porque la tabla no la nombra **CIERRA el 2026-09-01, y la que decide NO es esta pregunta: es una regla general del fundador, dicha ese día y más ancha que esta tarea.** «**Se puede corregir cualquier dato menos el de la zona, y eso porque va con la URL del SEO y eso no puede cambiar. Si quiere cambiar la zona después de publicado hay que desactivar ese anuncio y crear otro y ya.**» **Reemplaza a la tabla campo por campo del 2026-08-29** (18.14), y el reemplazo es lo que importa: aquella tabla enumeraba lo PERMITIDO, así que dejaba cerrado todo lo que no llegó a nombrar — la referencia no existía ese día, y el tipo de inmueble y los puestos quedaron afuera sin ninguna razón de integridad. Hoy la lista es al revés y lo que se enumera es lo cerrado: la zona, la ciudad (que la zona determina) y `publisherType`. La tabla nueva vive en el jsdoc de `listing-edit.ts`, que es donde la regla se aplica. **Los tres campos que abre, verificados contra `ListingEdit` y contra el validador de publicar y no contra el enunciado**: la referencia, el tipo de inmueble y los puestos. **Y el precio que esta tarea midió se confirmó entero: ninguna migración.** `listing.reference`, `property_type` y `parking_spots` ya son columnas, y `CHANGE_FIELD_LABEL` ya nombraba a las tres. **Por qué el título sí, si también va en la URL, y la zona no** —la asimetría que nadie debería tener que volver a derivar—: el último segmento de `/alquiler/<ciudad>/<zona>/<slug>-<id>` termina en el id y `listingIdFromSlug` resuelve POR ID, así que una dirección con el título viejo sigue encontrando el aviso; la zona es su propio segmento y no lleva ningún id adentro, o sea que no tiene ese rescate. Queda escrito al lado de la regla, en `listing-edit.ts` y en `EDIT_REQUEST_FIELD`. **`EDIT_REQUEST_FIELD` sigue siendo `Record<keyof ListingEdit, true>`**: agregar un campo editable sin decir dónde se lee su negativa sigue sin compilar. **La referencia es el único campo con dos ausencias distintas**, y hubo que decidirlo: mandarla en blanco es «no tengo ninguna» y BORRA la que había; no mandarla es «no la contesté» y deja la de ayer. Sin esa diferencia una seña equivocada se podría corregir pero no sacar, que es media corrección. El `?? current` de `writeFor` la resuelve componiendo con `referenceOrNone`, la MISMA función que decide «en blanco es ninguna» al publicar. **CORRECCIÓN de lo que la 18.7 y la 18.22 dejaron escrito**: «la negativa de la referencia no tiene control en editar, y se dice igual» dejó de ser verdad, y su prueba en `violation-field.test.ts` se reemplazó por la contraria. **Lo que la regla abre y este cambio NO abrió, dicho antes de que alguien lo descubra pagándolo: queda como 18.37.** Y **la tensión con `publisherType`, que no se resuelve sola**: la regla dice «cualquier dato» y `publisherType` es un dato, pero el `NO` del 2026-08-29 tenía una razón de integridad —cambiarlo invalida hacia atrás el filtro «solo de dueños»— que la regla nueva no discute, porque habla de la URL y no de eso. **Se dejó cerrado y se anota en vez de decidirlo por el fundador** (AGENTS.md §7). Pruebas: `listing-edit.test.ts` (5 nuevas, incluida la del candado — un pedido con zona ajena entra por un `as never` y NO llega al aviso ni lo rechaza), `violation-field.test.ts` (1), `actions.test.ts` (2), `editar-servido.test.tsx` (3, sobre los BYTES SERVIDOS) y `tests/integration/listing-edit.test.ts` (2, contra Postgres real: la fila releída, y la zona y la ciudad donde estaban). **Once mutaciones (M6–M16)**, con ocurrencia única verificada antes, sha256 comparado después, restauración por copia de bytes y `git diff --stat` limpio. Cada una pone roja exactamente la prueba que la nombra. **Diez de las once, con las pruebas nuevas fuera del árbol, no ponen roja ninguna de las 2.404 unitarias ni de las 326 de integración restantes; la undécima —M10, abrir el candado de la zona— pone rojas TRES**, y eso es un hallazgo y no un ruido: la zona no depende de la prueba nueva, la protegen además la lista de claves de `writeOf` y la de la edición vacía, que ya existían y este cambio sólo actualizó de ocho a once.

## Phase 19: Fotos, retención y capacidad (founder, 2026-08-22)

Three decisions taken while importing the designs, each with the measurement that produced it.

### 19a. Photo captions: removed before they were built

- [x] 19.1 **The mobile ficha design labels every photo** — "Sala", "Habitación principal", "Cocina", "Baño", "Estacionamiento", "Fachada". `listing_photo` has no caption column, and more importantly the Publicar specification never asks for one, so six captions would have had to be typed with a thumb in step 8. The founder removed it on sight: "no debe llevar título, solo subir la foto". **Nothing is lost**: R7/F28 already composes the alt text at render time from position, title and zone ("Foto 2 de 6 — Apartamento 2 habitaciones, Chacao"), which is what a screen reader actually needs. What stays from step 8 is what matters — choosing the cover photo and ordering the rest
- [x] 19.2 **"publica como dueña · desde 2025" → neutral, and the date dropped.** "dueña" is gendered and the product neither stores nor should store anyone's gender. The date needed a creation timestamp on `user`, which comes from the Auth.js adapter and does not carry one — a column avoided for a fact the founder judged not worth showing

### 19b. What the new sizes cost, measured

At 1x, because the design itself states that 4K is not served double-density ("chocaría con el presupuesto de bytes").

| Derivative set | Per listing (6 photos) | Listings inside R2's free 10 GB |
|---|---|---|
| Today — thumbnail 128 + detail 1280 | 1.23 MB | **8,322** |
| New design, 5 sizes (160·256·360·640·1280) | 1.79 MB | **5,711** |
| New, viewer at 1024 instead of 1280 | 1.44 MB | 7,104 |
| New, without the small thumb | 1.39 MB | 7,342 |

Meeting the new design costs **31% of capacity**. Operations do not bind: 30 Class A writes per listing against 1,000,000 free monthly is ~33,000 listings/month, and R2 egress is free — the reason R2 was chosen over S3.

- [x] 19.3 **The biggest single lever is the viewer derivative**, not the number of sizes: dropping it from 1280 to 1024 returns 1,393 listings, because it is 59% of the weight **Reconciliada el 2026-09-02 — construida, la casilla estaba vieja** (AGENTS.md §5: «antes de construir una tarea, comprobá si ya existe»). `photo-derivatives.ts` declara `FULL_MAX_EDGE = 1024` con la decisión del fundador del 2026-08-22 escrita al lado, y la prueba que lo fija es **«acota el visor a 1024 sin deformarlo»** en `photo-derivatives.test.ts` — afirma el lado mayor Y la proporción, así que recortar a 1024 deformando también pondría rojo. Shipeó en `731fb03` («feat(fotos): cinco tamanos, y el visor baja a 1024»). **Y la corrección que este enunciado necesitaba, medida y no supuesta: los 1.393 avisos son holgura QUE LLEGA, no capacidad recuperada.** Las derivadas se generan al subir (D12): `deriveListingPhoto` se llama desde `processUploadedPhoto`, `app/publicar/fotos/publication.ts`, `app/mis-avisos/actions.ts` y `scripts/seed-demo.ts`, y de ninguna otra parte. **No existe ningún barrido que vuelva a derivar lo ya guardado**, así que cada aviso publicado antes del 2026-08-22 conserva su `full` de 1280 hasta que sus fotos se purguen o alguien las vuelva a subir. La frase «devuelve 1.393 avisos» describe el techo para los avisos que vengan, no bytes que se hayan liberado: a los ~47 avisos de hoy la diferencia práctica es cero y crece con cada publicación nueva. **Si hiciera falta cobrarla de una vez, eso es un rederivado masivo sobre R2 y es su propia tarea, no ésta** — leer cada `full` guardado, redimensionarlo y reescribir la clave, con el orden de la 18.32 (leer antes de borrar) y sin ensanchar ningún puerto, porque `PhotoStoragePort` ya tiene `remove` y las credenciales ya necesitan `DeleteObject`.

### 19c. Retention — delete the photos, never the listing

The founder proposed deleting expired listings after a 15-day grace period. **The listing row cannot be deleted, and that is a decision already written into the schema rather than an obstacle.** `contact_reveal_event` carries `ON DELETE restrict` on all four foreign keys, and its comment says why: cascading "would silently delete the go/pivot evidence exactly when a listing is taken down — the deletions most correlated with the months a reveal happened". A `DELETE FROM listing` fails for any listing anyone ever revealed, on purpose.

Deleting only the photos gives up nothing: they are effectively 100% of the R2 weight, while a listing row is ~600 bytes — a hundred thousand expired listings is 57 MB **in Postgres, not R2**. Keeping the row also keeps the URL resolving into the expired state the design already draws, keeps Google from meeting a wall of 404s on indexed pages, and keeps the north-star metric intact.

| Retention | Sustainable new listings per month, indefinitely |
|---|---|
| **45 days** (30 active + 15 grace) | **3,807** |
| 60 days | 2,855 |
| 90 days | 1,903 |

At ~47 listings today, 45 days puts the ceiling 81× above the current catalogue.

- [x] 19.4 A job that deletes the **photos** of listings expired more than 15 days, leaving the listing row and its status untouched. **Reconciled 2026-08-24 — built, plan was stale.** `listing-lifecycle/application/purge-expired-photos.ts` plus the route at `app/api/jobs/photo-purge/route.ts`. Six tests: "corta exactamente 15 días después del vencimiento", "borra las filas de foto y los objetos del bucket", and the structural guarantee **"no tiene con qué tocar la fila del aviso"** — the port has no path to the listing row, so leaving it untouched is not discipline. It deletes from the bucket BEFORE the database, so a crash leaves an orphaned row rather than an orphaned object nobody can find. ~~**The cron is still deliberately absent from `vercel.json`** — see the note on that route.~~ **LA CASILLA MENTÍA, y se corrige el 2026-09-02 en vez de taparse.** Esta entrada quedó marcada `[x]` el 2026-08-24 con el caso de uso y la ruta construidos y **sin una sola línea que los llamara**: `vercel.json` programaba `expiry-reminders` y `draft-sweep` y nada más. Un trabajo que nadie dispara no es un trabajo, y lo que la 19.5 y la 19.6 shipearon encima lo volvió peor que una omisión: el correo del día 40 anunciaba la fecha, el conteo de `/mis-avisos` la contaba, y **nada borraba nada**. Quien recibía el aviso veía el conteo llegar a cero con sus fotos intactas. **CIERRA de verdad el 2026-09-02.** El cron son las `0 17 * * *`, su propia ranura: cuatro horas después de los recordatorios, que con la deriva de ±59 minutos del plan Hobby sobra para que `markExpired` y el correo del día 40 hayan salido antes de que se borre nada, y para que una caída de uno no se lea como la del otro en `job_run`. **Ninguna variable nueva**: `CRON_SECRET` es la misma de los otros dos y R2 ya necesita `DeleteObject` desde D12. **Y el defecto era el CABLEADO, así que lo que se afirma es el cableado**: `app/api/jobs/cron-wiring.test.ts` lee `vercel.json`, lista las carpetas bajo `app/api/jobs/` que tienen `route.ts` y exige que los dos conjuntos sean iguales — sobre el DIRECTORIO y no sobre una lista escrita a mano, porque acordarse de actualizar una lista es exactamente lo que falló. Afirma además que no compartan horario y que ninguna corra más de una vez por día, que en Hobby **no se ignora: falla el despliegue** (verificado el 2026-09-02 contra `vercel.com/docs/cron-jobs/usage-and-pricing`, que también desmiente el mito de las 2 crons — son 100 por proyecto en todos los planes). El trabajo número cuatro se delata solo el día que alguien lo cree. **Y la ruta recibe por fin sus tres pruebas de puerta cerrada** (`app/api/jobs/photo-purge/route.test.ts`), las mismas que `draft-sweep` tenía desde el primer día: sin portador, con el portador equivocado y sin `CRON_SECRET` en el servidor. **Medido, y es el hallazgo**: invertir el `if` de autorización de esa ruta —abrir un borrado irreversible de R2 a un `GET` anónimo— **no ponía roja ni una de las 2.824 pruebas del repositorio**. La diferencia con su hermana era que aquélla estaba cableada y ésta no. **Lo que el fundador tiene que hacer antes de desplegar: nada de configuración, y una medición.** El primer disparo purga DE UNA todo lo que lleva más de 45 días acumulado sin que nada lo borrara — ver el bloque de la 19.16 para la consulta que lo cuenta antes de merge. **Decisión del fundador, 2026-09-02: el atraso no necesita mitigación y `dev → main` no espera por él.** La razón es que **los datos de producción son ficticios**: los avisos que hoy pasan de 45 días son de siembra, no de nadie, así que el primer disparo del cron llevándose las fotos de avisos que nunca recibieron el aviso del día 40 es aceptable y no le quita nada a ninguna persona. Queda escrito con su fecha porque dentro de seis meses «lo mergeamos y borramos fotos que nadie avisó» necesita su porqué al lado: no fue un descuido ni una excepción a la 19.8, fue una decisión tomada sobre un conjunto de datos que no tenía dueño. **Lo que NO cubre**, dicho para que no se estire: la regla vale para este primer disparo y para estos datos. Con avisos de personas de verdad adentro, la 19.8 vuelve a mandar entera —dos canales antes de borrar— y un atraso acumulado volvería a ser una decisión que hay que tomar de nuevo.
- [x] 19.5 **Two emails, not one.** The plan carries one notice *before* expiry, to renew. The purge happens 15 days *after*, so a publisher who ignored the first email loses their photos with no second warning. Day 27: "vence en 3 días". Day 40: "tus fotos se borran el 27 de octubre — renová antes y el aviso vuelve con ellas". **Reconciled 2026-08-24 — built, plan was stale.** `listing-lifecycle/domain/lifecycle-notice.ts` decides which of the two notices is due, and the calendar is pinned day by day: "el día 27 toca el aviso de vencimiento", "el día 40 toca el aviso de purga", nothing between them and nothing long before. **"el mismo aviso recibe DOS avisos distintos a lo largo de su ciclo"** is this task in one assertion. Each says what it must: the first counts the days left and offers renewal, the second names the photos and the day they are deleted.
- [x] 19.6 **The countdown must also live in Mis publicaciones**, because an irreversible deletion may not depend on an email arriving. This is the same deliverability risk that makes the magic link fragile (15.2), except the cost here is not a failed sign-in — it is a publisher's photographs, permanently **CIERRA el 2026-09-02.** `listing-lifecycle/domain/retention-notice.ts` decide el aviso y escribe la frase, **al lado de `lifecycle-notice.ts` y bajo el piso del 90%**, no en la pantalla (AGENTS.md §1): es el mismo reparto que los dos correos ya eligieron, y de las dos frases posibles la equivocada manda a alguien a tocar un botón esperando su aviso de vuelta. `publisher-listing-board.ts` cuelga el aviso de la ficha (`PublisherListingCard.retention`) y `app/mis-avisos/page.tsx` lo dibuja; ningún `if` de esa pantalla mira `photoCount` ni la fecha de la purga. **Texto servido y no montado en el navegador**: se afirma con `renderToStaticMarkup` sobre la ruta de verdad en «un aviso vencido con fotos sirve el conteo regresivo de la purga».
  **La decisión que este enunciado no traía, tomada y escrita: la cuenta de fotos manda sobre el reloj.** El reloj dice cuándo DEBERÍAN borrarse; la fila dice si se borraron, y el cron de la purga sigue deliberadamente ausente de `vercel.json` (19.4), así que los dos discrepan durante días. Decidir por el reloj le diría a quien todavía tiene sus seis fotos que las perdió. Lo prueba «con la fecha pasada pero las fotos todavía ahí, el aviso sigue siendo el conteo». Y por la misma razón, pasado el día el conteo **deja de nombrar una fecha**: escribir «se borran el 16 de septiembre» un 20 de septiembre es prometer un día que ya pasó.
  **La zona horaria se afirma con un instante donde de verdad se nota.** Las demás pruebas fijan el vencimiento al mediodía, donde ninguna zona cambia el día y una mutación de `timeZone` no mediría nada; «no corre la fecha un día por leerla fuera de UTC» usa las 02:00 Z, que en UTC-4 cae el día anterior — el día que este proyecto ya perdió una vez (16.34). La pantalla además dejó su copia corta de formateo y toma `longSpanishDate`, byte a byte la misma salida.
  **Nueve mutaciones, ninguna en blanco** (una ocurrencia exacta verificada antes, sha256 comparado después, restauración por copia de bytes con sha256 idéntico al original y `git diff --stat` limpio en las nueve). Cada una pone roja la prueba cuyo título la nombra: la fecha de la purga por la del vencimiento → «nombra el día en que las fotos se borran y cuántos faltan»; el singular → «dice «1 día» y no «1 días» la víspera»; `remaining === 0` → `< 0` → «pasado el día deja de prometer una fecha que ya pasó»; `photoCount === 0` → `< 0` → «sin fotos el conteo se acabó, y lo dice en pasado»; la frase de purgado por la de renovación → «promete el aviso con sus fotos antes de la purga, y subirlas de nuevo después»; `state === "expired"` → `!== "draft"` en el tablero → «ni la activa, ni la que vence pronto, ni el borrador llevan aviso de retención»; y **cada mitad de la pantalla por separado** —borrar `deadline` y borrar `republish` en dos corridas distintas—, porque las dos juntas en una sola mutación no distinguen qué mitad se sirve. **La medida del hueco: las ocho primeras, con estas pruebas fuera del árbol, no ponen roja NINGUNA de las 2.473 unitarias restantes.** La novena —quitar `timeZone: "UTC"` de `spanish-date.ts`— es la excepción esperada y por eso se corrió: sin estas pruebas sigue muriendo en las dos de `spanish-date.test.ts` (16.34), y con ellas muere además en la del conteo, que es el nivel donde el día importa.
- [x] 19.7 **"Volver a publicar" changes meaning after the purge.** A listing older than 45 days can be republished but its photos are gone, so it means re-uploading. The copy in Mis publicaciones has to say which of the two it is **CIERRA el 2026-09-02, en el mismo aviso que la 19.6.** Las dos ramas, literales: con fotos todavía, «Renovalo antes de esa fecha y el aviso vuelve con sus fotos.»; purgadas, «Volver a publicarlo significa subir las fotos de nuevo.». **Las dos se afirman juntas y a propósito** en «promete el aviso con sus fotos antes de la purga, y subirlas de nuevo después»: cualquiera de las dos sola pasa con la decisión entera borrada, porque una función que devuelve siempre la misma frase cumple la mitad que se afirme. La prueba compara además que sean DISTINTAS. En los bytes servidos van por separado, y la del lado purgado comprueba que la otra NO salió — con el control de que la ficha se sigue dibujando entera, sin el cual una pantalla que no dibujara nada pasaría las dos negativas.
  **DESVÍO respecto del enunciado, dicho antes de que alguien lo descubra: la frase NO es la etiqueta de un botón, porque el botón todavía no existe.** «Volver a publicar» y «Renovar 30 días» son la 9.31, que sigue abierta y que el fundador ya dejó decidida entera (~400 líneas por sí sola). Lo que esta tarea pedía es que la copia «diga cuál de las dos es», y eso es lo que ship*a*: la frase va en el aviso de retención de la ficha vencida. Cuando la 9.31 construya los botones, la etiqueta sale de `RetentionNotice` y no de un `if` nuevo — su tabla de decisión y ésta parten el estado vencido por el mismo dato, `photoCount`.
- [x] 19.8 Relationship to "no podemos borrar data real" (11b.6): that rule is about migrations destroying data silently. This is a deliberate, announced retention policy through two channels. Different thing — but the announcement is what makes it different, so 19.5 and 19.6 are not optional extras **Revisada el 2026-09-02: no es trabajo, es una condición sobre otras tareas, y hoy NO se cumple.** El portón de la 11b.6 es `scripts/deploy-migrate.mjs`, que sólo lee `drizzle/*.sql`; el borrado de la retención vive en código de aplicación (`purge-expired-photos.ts`), así que el portón ni se entera —ni debería—, y la distinción que esta entrada traza es real. Pero su condicional está a medias: la 19.5 shipeó con sus dos avisos y **la 19.6 —el conteo regresivo en Mis publicaciones, el segundo canal anunciado— sigue sin construirse**. Hoy la política se anuncia por UN canal, no por dos. **Se marca el 2026-09-02: la 19.6 shipeó y el segundo canal existe.** El conteo regresivo vive en `/mis-avisos`, servido en los bytes de la ruta, y nombra el día en que las fotos se borran. La condición que esta entrada pone —anunciada por DOS canales, no por uno— se cumple para los avisos `active` y `expired`, que son los que 19c describe. **Y desde el 2026-09-02 se cumple sin la salvedad**: la 19.16 sacó `hidden` y `draft` de la purga, así que el conjunto que el borrado alcanza y el conjunto al que se le anuncia son la misma lista (`ANNOUNCED_LIFECYCLE_STATUSES`) y no dos `WHERE` que coinciden. La condición de esta entrada pasa a ser estructural: ampliar el borrado sin ampliar el anuncio ya no se escribe en un solo lado.
  **Y con la condición cumplida aparece un hueco que nadie había medido, escrito acá para que no se descubra pagándolo: hay dos estados a los que la purga llega y ningún canal anuncia.** `DrizzleLifecycleListings.candidates` filtra **sólo** por `expires_at` (`.where(lt(listings.expiresAt, purgeBefore))`, sin un solo predicado de estado), así que también se lleva las fotos de un `hidden` y de un `draft` cuyo reloj pasó los 45 días. Del otro lado, `noticeCandidates` filtra `inArray(status, ['active','expired'])`, así que ninguno de los dos recibe correo; y el conteo de la 19.6 tampoco los alcanza, por una razón deliberada y no por olvido: renovar deja `hidden` como está (9.31), así que «renovalo y el aviso vuelve con sus fotos» sería falsa justamente en la ficha que la leería. **Para esos dos estados la política se anuncia por CERO canales**, que es exactamente lo que esta entrada dice que no puede pasar. No se tapa acá porque no es de esta entrada: queda como 19.16.

### 19d. Verification is reused, and it belongs to the value, not the person

The founder asked whether a publisher who verified their WhatsApp should have to verify again for a second listing. No — and the schema half-anticipated it: `users.contact_method`/`contact_value` already exist as the account default, and `listing` **copies** them at publish time. What is missing is the verification state.

**The unit is the (user, method, value) triple, not the user.** If María verifies +58 412 555 0134 and later publishes with a different number, that number is not verified. Treating it as verified because she once verified something is what makes verification stop meaning anything — and telling a real listing from a fake one is the only thing it is for. A small `verified_contact` table also serves an inmobiliaria with two numbers without inventing anything.

- [x] 19.9 `verified_contact (user_id, method, value, verified_at)`. Publishing with a value that has a live row asks for nothing; a new value is verified and recorded. **Entregado 2026-08-29.** Tabla en `src/shared/db/schema.ts` (`verifiedContacts`) con migración `0016_even_boomerang.sql`; la regla en `identity/domain/contact-verification.ts` y el caso de uso en `identity/application/resolve-contact-verification.ts`, llamado por `publishListing`. La prueba que la sostiene corre contra Postgres real: **«la agencia que sube cincuenta avisos verifica una vez: una escritura, una fila, y cuarenta y nueve que no piden nada»** (`tests/integration/contact-verification.test.ts`), que es a la vez la 19.13. La segunda, **«verificar un número no verifica el otro número de la misma cuenta, ni el mismo valor por otro método»**, es la que hace cierto el encabezado de la 19d — se agregó DESPUÉS de que una mutación que borraba `value` del `JOIN` dejara la suite entera en verde.

  **Corrección al texto de la tarea, con su razón (AGENTS.md §5).** «Un valor nuevo se verifica» supone un canal, y el de WhatsApp está diferido al final del proyecto (fundador, 2026-08-29: tiene costo por mensaje y necesita una decisión de proveedor que no está tomada). Lo entregado es la mitad que no lo necesita: la tabla, la regla, y el registro del correo propio (19.10). Publicar **no se corta** — cerrar la publicación por teléfono para todo el mundo sería un retroceso de producto, no un cierre en falso. El cierre en falso es el otro y es estructural: **sin fila no hay fecha**, y «verificado el …» no tiene de dónde salir. `resolveContactVerification` no escribe una sola fila para un método que no sea `email`, probado por **«no deja ninguna fila cuando el contacto es un teléfono, y publica igual»** (`publish-listing.test.ts`) y por la cuenta de filas de Carla en `tests/integration/photo-duplicate-rejection.test.ts`, que atraviesa el `publishListing` de verdad.

  **La tabla guarda ESTADO y no es append-only**, al revés que `contact_reveal_event`, `listing_report` y `moderation_action`. Aquéllas registran que algo ocurrió; ésta contesta una pregunta con una sola respuesta útil, y la 19.9 nombra cuatro columnas sin id de evento. La clave primaria natural `(user_id, method, value)` ES la garantía de la 19.13, y volver a verificar mueve el instante con `ON CONFLICT DO UPDATE` — **«volver a verificar mueve el instante en la misma fila en vez de dejar dos»**. Para la 19.11 esto es un cambio de consulta y no una migración: los doce meses entran como un `WHERE verified_at > $desde` en la condición del `LEFT JOIN` de `DrizzleContactVerificationEvidence`, y el dominio no compara fechas. La 19.12 no pide nada acá: el aviso ya copia `contact_method`/`contact_value` al publicar y la ficha escribe la FECHA, nunca un estado.

  **Qué van a llamar la 16.12, la 16.34 y la 15.11 cuando les toque:** `DrizzleContactVerificationEvidence.findEvidence({ userId, contact })` con `userId = listing.publisher_id` y el `contact` que el aviso ya copió — una sola consulta, sin segunda regla, y `verifiedAt` es el `phone_verified_at` que la 16.12 pide. `null` significa no verificado y no hay nada que dibujar.
- [x] 19.10 **Email needs no WhatsApp code at all.** If the chosen contact is an email and it matches the account's own address, Auth.js already set `emailVerified` at sign-in — through Google or through the magic link. Sending a WhatsApp code to verify an email address never made sense; this removes the step rather than implementing it. **Entregado 2026-08-29** en `identity/domain/contact-verification.ts`: el atajo exige `method === "email"` ANTES de mirar el valor, y esa es la razón estructural de que un teléfono no pueda llegar nunca a la rama del correo. Prueba: **«da por verificado el correo de la propia cuenta, y con el instante en que Auth.js lo verificó»**, y su contraparte **«no verifica un teléfono aunque coincida con el correo verificado de la cuenta»** (`contact-verification.test.ts`). El instante que se registra es `user.emailVerified` y no `now()`: la verificación ocurrió al entrar, y usar el reloj de publicar le regalaría un año a la caducidad de la 19.11.

  **La premisa de esta tarea es cierta a medias, y la mitad falsa es la que más pesa** — ver la 19.14. `@auth/core` 0.41.3 escribe `emailVerified` sólo por la puerta del enlace por correo; por Google crea la cuenta con `emailVerified: null` **explícito** (`lib/actions/callback/handle-login.js:260`), sea cual sea lo que devuelva `profile()`. La regla cierra en falso: sin instante no verifica, probado por **«no verifica el correo de la cuenta mientras Auth.js no haya dejado el instante»**
- [x] 19.11 **Verification expires after 12 months** (founder, 2026-08-22). Venezuelan numbers get recycled, and a two-year-old verification may belong to somebody else by now. A publish whose verification has lapsed re-verifies **PELIGRO ANOTADO EL 2026-09-02, antes de construir — leelo antes de escribir el `WHERE`.** `DrizzleContactVerificationEvidence.findEvidence` lo llaman LAS DOS: `resolveContactVerification` al publicar **y** `viewListingContact` al dibujar la ficha. La 19.9, el comentario del puerto y el del adaptador prescriben los doce meses ahí, como `WHERE verified_at > $desde` — y **eso borra la frase de un aviso ya publicado y todavía activo el día que su verificación caduca**, que es exactamente la invalidación que la 19.12 prohíbe y que desde hoy tiene prueba. **La ventana no puede aplicarse en ese puerto compartido sin romper la 19.12**: o se separa la lectura de publicar de la de la ficha, o la ventana vive en el llamador de publicar y no en el adaptador. **CIERRA el 2026-09-02, y NO donde los tres comentarios la mandaban.** Se verificaron los dos llamadores de `findEvidence` antes de escribir nada: `resolveContactVerification` (publicar) y `viewListingContact` (ficha), y ningún otro. El `WHERE verified_at > $desde` del adaptador se probó **como mutación** y el resultado es peor que lo anotado: con las pruebas de esta entrega fuera del árbol, la suite entera queda **VERDE** —2460 unitarias, 334 de integración, cero rojas— mientras un aviso activo pierde su frase. No la delataba nadie porque `ficha-verificacion.test.tsx` reemplaza ese puerto por un falso y `infrastructure/` no tiene piso de cobertura. **La ventana vive entonces en `decideContactVerification`** (`identity/domain/contact-verification.ts`), con `now` de parámetro —la forma de `isVerificationLinkExpired`, `isExpired` y `resolveListingAvailability`—, y es estructuralmente incapaz de tocar la ficha: la ficha no llama a esa función. La otra salida anotada —separar la lectura de publicar de la de la ficha— se descartó porque pone la regla en SQL, en la capa sin piso de cobertura y falseada por todas las pruebas de arriba, y deja las dos consultas a un error de cableado de distancia. **Caducan los dos instantes**, la fila y el `emailVerified` de la cuenta: la 19.10 ya lo había decidido al registrar el instante de Auth.js en vez de `now()`, y si el atajo no caducara se escribiría una fila ya vencida que la publicación siguiente reescribiría —la 19.13 al revés—. **Sin migración**: `verified_contact.verified_at` ya existía. **Pruebas nombradas** (`contact-verification.test.ts`, bloque «los doce meses de la 19.11»): *«una fila de once meses todavía contesta»* / *«una fila de trece meses ya no contesta»*, *«el corte es a los doce meses exactos»*, *«la fila caducada de un correo propio se vuelve a verificar con el instante de la cuenta»*, *«el atajo del correo también caduca»* / *«un `emailVerified` de once meses sí verifica»*; la escritura en `resolve-contact-verification.test.ts` → *«vuelve a escribir la fila caducada, moviéndole el instante hacia adelante»*; las costuras en `publish-listing.test.ts` y `activate-listing.test.ts`; y el guardián que faltaba, contra Postgres real, en `tests/integration/contact-verification.test.ts` → *«una fila de tres años vuelve entera: la ficha de un aviso activo no pierde su fecha»*. **La 19.12 quedó verde en todo momento**, y se comprobó por los dos lados: la misma ventana metida en `viewListingContact` pone roja su prueba de ruta en las dos columnas.
- [x] 19.12 **An active listing is not invalidated when its verification lapses mid-flight.** A listing lives 30 days and can be published on a verification's last day, so the two clocks cross. The ficha states the date rather than a status — "verificado por WhatsApp el 19 ago" says *when*, never "still valid today" — which is already what the design draws, so nothing has to change to stay honest **CIERRA el 2026-09-02.** Era cierta y no la guardaba nada: `ficha-verificacion.test.tsx` fijaba la fecha en los bytes servidos, pero **nadie afirmaba el caso del cruce de relojes** — que es justo lo que este enunciado promete. Ahora sí, por las dos puertas: la ruta afirma que una verificación caducada a mitad de vuelo no invalida el aviso ni afirma vigencia, y el dominio afirma que **dice lo mismo cinco años después, porque no tiene reloj con qué decidir si sigue vigente**. La frase se fija por igualdad exacta, así que agregarle «· vigente» pone rojo. **Medido por mutación, y la segunda columna es el hallazgo**: meterle a `contactVerificationNotice` una caducidad de doce meses, y por separado hacer que una verificación vieja tape el contacto en `viewListingContact`, pone roja exactamente la prueba nueva que las nombra — y **con las pruebas nuevas fuera del árbol las 2.455 restantes siguen en verde las dos veces**. O sea que hasta hoy esta tarea no la sostenía nada: todas las pruebas que tocan la frase fijan un instante reciente.
- [x] 19.13 **This is what makes the broker bulk import (Phase 9) usable at all.** An agency uploading fifty listings verifies once, not fifty times. **Cae sola de la forma, y está medido (2026-08-29):** la clave primaria `(user_id, method, value)` no deja escribir una segunda fila, y `resolveContactVerification` ni siquiera llega a escribir — del segundo aviso en adelante la fila viva ya contesta. Las cincuenta resoluciones de la prueba de integración dejan **una** fila y **una** escritura. No se marca todavía porque la importación de cartera activa avisos por `activateListing`, que no pasa por `publishListing` y por lo tanto todavía no resuelve la verificación — ver la 19.15 **Avance el 2026-09-02, sin cerrar**: `tests/integration/contact-verification.test.ts` afirma contra Postgres real que **la segunda fila del mismo triple la rechaza Postgres, no un `if` de la aplicación** — la prueba que ya existía sólo recorría el camino del upsert (`ON CONFLICT DO UPDATE`), así que nunca tocaba la restricción. **Sigue abierta porque su propio cuerpo la difiere a la 19.15**: `activateListing` —por donde la Fase 9 saca un `draft` a `active`— todavía no resuelve verificación, así que la inmobiliaria que importa cincuenta filas no deja ni una. **CIERRA el 2026-09-02 junto con la 19.15**, que era lo único que la difería. `activateListing` ya resuelve la verificación, así que la inmobiliaria que importa deja fila — y una sola: `tests/integration/listing-activation.test.ts` → *«dos borradores activados de la misma cuenta dejan UNA fila verificada, no dos ni ninguna»*, contra Postgres real, con el 0 de antes de activar medido para que el 1 signifique algo. Las tres afirmaciones de la tarea quedan entonces cubiertas por las tres pruebas que les corresponden: que la primaria rechaza la segunda fila (la escritura desnuda), que las cincuenta resoluciones dejan una (el camino de publicar) y que activar cincuenta borradores tampoco deja dos (el camino de importar).
- [x] 19.14 **El enlace magico SI deja `emailVerified`; Google no. La asimetria es al reves de lo que uno esperaria.** Verificado contra `@auth/core` 0.41.3 el 2026-08-29: `lib/actions/callback/handle-login.js:260` crea la cuenta de OAuth con `emailVerified: null` escrito a mano, así que `toMinimalGoogleProfile` no tiene cómo cambiarlo aunque ya **rechace** un correo de Google sin verificar (`UnverifiedGoogleEmailError`). La evidencia existe en el momento de entrar y no se guarda. Consecuencia hoy: quien entra por Google y publica con su propio correo queda **no verificado**, y la regla cierra en falso en vez de inventar un instante. Cerrarlo es su propia entrega porque toca la creación de cuentas — el asiento es el hook `events.createUser` o el callback `signIn` de `auth.ts`, y las cuentas de Google que ya existen no se pueden rellenar honestamente porque nadie sabe cuándo se verificaron. Mientras tanto el impacto práctico es nulo: lo único que se dibujaría con esa fila es «verificado por WhatsApp el …», y ese canal está diferido (15.13) **Verificado de nuevo el 2026-09-02 y sigue exacto**: la versión instalada es `@auth/core` **0.41.3**, la misma que este enunciado nombra, y las dos mitades se leyeron en el paquete instalado — `handle-login.js:260` crea la cuenta de OAuth con `emailVerified: null` escrito a mano, y las líneas 70 y 76 escriben la fecha en las dos ramas del enlace mágico. `emailverified-de-auth-js.test.ts` lo afirma con un proveedor de mentira, sin JWKS ni red, y **con la `profile()` más optimista posible** —devuelve el instante ya puesto— para dejar probado que no hay `profile()` que pueda ganarle. **Las dos mitades se conducen, no se leen** (misma disciplina que `magic-link-ida-y-vuelta.test.ts`: una nota al pie sobre el fuente de una dependencia envejece en la primera actualización): la del enlace mágico con el proveedor de correo de verdad, y la de OAuth con un proveedor de mentira —sin JWKS, sin `id_token` y sin red— porque el `null` lo escribe la rama genérica de cualquier proveedor OAuth y no algo de Google. **Verificado por mutación sobre el paquete instalado**: cambiar cada una de las tres líneas de `handle-login.js` pone roja exactamente la prueba que la nombra, y **con el archivo nuevo fuera del árbol no se pone roja ninguna de las 2.455 restantes** — o sea que el día que una actualización deje de escribir el instante, la 19.10 se apaga en silencio y hoy nada avisa. **Sigue abierta porque lo que nombra sin construir es escribir la fecha en el camino de Google**, que el fundador decidió que va en su propio PR. **CIERRA el 2026-09-02, y lo primero que hay que corregir es la razón por la que estaba abierta sin urgencia.** Este enunciado decía que «el impacto práctico es nulo: lo único que se dibujaría con esa fila es «verificado por WhatsApp el …», y ese canal está diferido (15.13)». **Eso es falso, y ya lo era cuando se escribió.** `contactVerificationNotice` no lleva un texto fijo: saca el canal del MÉTODO que se verificó (`contactChannelNoun`, con `email: "email"`), y su propio comentario lo dice —«la única fila que se escribe es la del correo propio (19.10), así que un texto fijo «por WhatsApp» certificaría un mensaje que nunca se mandó»—. El paso de contacto de Publicar ofrece «Correo» como una de sus tres opciones (`PublishStep.tsx`), así que el camino existe entero y está a la vista: quien entra por ENLACE MÁGICO y publica con su propio correo ve «verificado por email el 2 sep» en la ficha, y quien entra por GOOGLE —el camino más común— no ve nada. Y desde la 19.15 (2026-09-02) la asimetría alcanza los dos caminos de escritura, no uno: `resolveContactVerification` corre al publicar **y** al activar un borrador de cartera. O sea que lo que faltaba no era una fila que nadie dibujaría: era una frase que la mitad de las fichas no dice porque su dueña entró por la puerta que verifica mejor. **El asiento es `events.signIn`, y no es ninguno de los dos que este enunciado nombraba** — se evaluaron los tres contra `@auth/core` 0.41.3 leído, no supuesto. *El callback `signIn` pierde por orden*: corre en `lib/actions/callback/index.js:63`, ANTES de `handleLoginOrRegister` (`:70`), así que en el caso que importa —la primera entrada por Google— todavía no hay fila que actualizar y lo que se escribiera lo pisaría el `createUser({ ...profile, emailVerified: null })` de `handle-login.js:260`. Y es la compuerta de acceso: `handleAuthorized` envuelve en `AccessDenied` todo lo que no sea un `AuthError`, así que un fallo de base convertiría «no se pudo anotar» en «no podés entrar». *`events.createUser` pierde por evidencia*: recibe `{ user }` y nada más (`handle-login.js:77` para el enlace, `:262` para OAuth), sin `account` ni `profile`. No tiene con qué saber por qué puerta entró ni qué afirmó el proveedor, así que sólo podría adivinar «vino `null`, luego fue OAuth, luego Google lo verificó» — que es inventar el instante, y el día que exista un segundo proveedor OAuth le escribiría fecha a uno que no verifica nada. Además corre una sola vez, al crear. *`events.signIn` gana por las dos*: corre en `index.js:114`, después de que `handleLoginOrRegister` devolvió la fila persistida, y lleva `user` con su id real, `account` —de donde sale el proveedor— y `profile`, que es el perfil CRUDO del proveedor (`oauth/callback.js:210`) y por lo tanto la afirmación `email_verified` misma, la que `toMinimalGoogleProfile` ya usa para dejar entrar. **Qué pasa con las cuentas de Google que ya existen, que era la restricción de este enunciado.** No se rellenan hacia atrás y no hizo falta: `events.signIn` corre en TODAS las entradas, no sólo en la primera, así que la cuenta vieja recibe su instante la próxima vez que entra — el de ESE día, que es cuando Google lo afirmó. No es un pasado inventado, es una verificación nueva. Prueba: *«le deja su fecha a la cuenta de Google que ya existía, la próxima vez que entra»*. **El instante es el de la entrada y no `now()` de publicar**, que es la misma disciplina de la 19.10: se registra cuándo el proveedor lo AFIRMÓ, exactamente lo que hace el enlace mágico al canjearse (`emailVerified: new Date()`), y es un instante que la ventana de doce meses de la 19.11 puede juzgar. **Sin migración**: `user.emailVerified` ya existía. **La regla vive en el dominio y el asiento no tiene ni un `if` de producto** (AGENTS.md §1): `identity/domain/provider-email-verification.ts` decide qué proveedor cuenta —`PROVEEDORES_QUE_VERIFICAN_EL_CORREO = ['google']`, una lista y no «cualquier OAuth», porque de Google se SABE que verifica y del próximo no—, que la afirmación esté presente, y que la dirección afirmada sea la de la fila. Ese último guardián no es decorativo: `handle-login.js` devuelve `userByAccount` tal cual, sin refrescarle el correo, así que quien cambió su dirección en Google tiene una fila con la vieja mientras Google acaba de verificar la nueva. `providerClaimsVerifiedEmail` es UNA función que leen los dos lados —`toMinimalGoogleProfile` para dejar entrar y esta regla para registrar—, así que admitir a alguien por verificado y negarle la fecha dejó de ser posible; se midió: la mutación que la abre pone roja también *«rejects an unverified email instead of creating an account from it»*, que ya existía. **Un `try`/`catch` propio se puso y se SACÓ, medido**: `@auth/core` ya envuelve todos los eventos en el suyo (`lib/init.js:138`, `eventsErrorHandler`) y sigue devolviendo la cookie de sesión, así que el `catch` de más no agregaba una garantía sino que tapaba la de la librería — y la prueba que lo «afirmaba» pasaba por coincidencia, porque el `console.error` que buscaba lo emite también el logger de Auth.js. Ahora la garantía se conduce contra la librería de verdad —302 y cookie de sesión con la escritura rota— y **se midió mutando el paquete instalado**: quitarle el `try` a `eventsErrorHandler` pone roja esa prueba y sólo esa. **Anotado de paso, medido y no supuesto, porque es una trampa para el próximo**: `user."emailVerified"` es la ÚNICA marca de tiempo del esquema sin `withTimezone` —viene del esquema por omisión del adaptador de Auth.js, como `image`—, así que Postgres guarda un reloj de pared pelado. Drizzle lo escribe y lo lee en UTC, de modo que el camino de producción (escribir por `adapter.updateUser`, leer por `DrizzleContactVerificationEvidence`) devuelve el instante exacto en cualquier zona; `node-postgres` crudo lo interpreta en la zona DEL PROCESO y acusa un desvío de horas que no existe. La primera versión de la prueba de integración se leyó así y falló contra un defecto imaginario. **No hay nada que arreglar en producción** (Vercel corre en UTC y el camino nunca mezcla los dos lectores), y cambiarle el tipo a la columna no es aditivo, así que no se toca acá. **Pruebas nombradas.** Dominio (`provider-email-verification.test.ts`): *«escribe el instante de AHORA cuando Google afirma que el correo está verificado»* con sus cinco negativas —proveedor desconocido, sin afirmación, sin perfil, dirección distinta, fila sin correo— y *«sí escribe cuando la dirección es la misma con otras mayúsculas»*. Costura (`record-provider-email-verification.test.ts`). Arnés contra Auth.js de verdad (`emailverified-de-auth-js.test.ts`, bloque «la fecha de Google se escribe al entrar (19.14)»), con el proveedor de mentira llamado `"google"` a propósito —el asiento decide por ese nombre, así que uno llamado de otro modo no produciría nunca la entrada que se quiere medir—: *«anota el instante sobre la cuenta recién creada, que Auth.js dejó sin fecha»* (que afirma las DOS mitades: el `null` de la librería sigue ahí y la fila queda con la fecha), *«le deja su fecha a la cuenta de Google que ya existía…»*, *«no anota nada cuando Google no afirma…»*, *«no anota nada por un proveedor OAuth que no es Google, aunque afirme lo mismo»* y *«entra igual cuando la escritura de la fecha falla»*. Y contra Postgres real (`tests/integration/provider-email-verification.test.ts`): *«la entrada por Google le deja el instante a la cuenta, y con eso publicar deja fila»*, que es la única que prueba que la escritura LLEGA —el arnés usa un adaptador que anota en vez de escribir, y el asiento actualiza por `adapter.updateUser` del `DrizzleAdapter` de verdad con un objeto parcial: la forma exacta del defecto de `resultsOrigin`— más su negativa. **Mutación, las dos columnas** (una ocurrencia exacta verificada antes, sha256 comparado después, restauración por copia de bytes con sha256 idéntico al original y `git diff --stat` limpio en las siete). Con las pruebas de esta entrega EN el árbol, cada mutación pone roja exactamente la que la nombra: *el asiento no hace nada* → 3 unitarias + 1 de integración; *cualquier proveedor OAuth cuenta* → 2; *la afirmación se da por hecha* → 4 (una de ellas la de `google-profile`, que es la medida de que el predicado es de verdad compartido); *la dirección afirmada no importa* → 2; *el instante deja de ser el de la entrada* → 5; *`@auth/core` deja de envolver los eventos* → 1. **Con las pruebas de esta entrega FUERA del árbol, ninguna de las 2.495 unitarias ni de las 338 de integración se pone roja por ninguna de las siete, salvo una sola** —la de `google-profile` que ya existía—, o sea que hasta hoy nada en el repositorio sabía que Google entraba sin dejar fecha.

  **Corregida por el fundador, 2026-08-29 — la mitad que faltaba, y cambia el encuadre.** El apunte original decia que «la evidencia existe al entrar y no se guarda», y eso vale **solo para Google**. El mismo archivo, cuarenta lineas mas arriba, muestra que el proveedor de correo SI la escribe: `handle-login.js:70` pone `emailVerified: new Date()` sobre una cuenta que ya existia, y `:76` lo pone al crearla. **Las dos ramas del enlace magico guardan la fecha.** El `null` de `:260` es exclusivo de OAuth.

  Asi que hoy, sin tocar nada: **quien entra por enlace magico y publica con ese mismo correo queda verificado**, y la 19.10 funciona entera para el. Quien entra por Google, no — **y es el camino mas comun**.

  Lo raro, dicho por el fundador y vale dejarlo escrito: **Google verifica el correo MEJOR que el enlace magico** —`toMinimalGoogleProfile` ya rechaza uno que Google no verifico— y sin embargo es el unico camino que queda sin fecha. No es un defecto del producto: Auth.js no distingue entre un proveedor OAuth que verifica y uno que miente, asi que trata a todos como si mintieran.

  **El arreglo va en su propio PR** (decision del fundador, 2026-08-29): escribir la fecha en el camino de Google cuando `toMinimalGoogleProfile` ya confirmo que Google la verifico. Sigue sin poder rellenarse para las cuentas de Google que ya existen, porque nadie sabe cuando se verificaron.
- [x] 19.15 **La importación de cartera todavía no verifica nada.** `publishListing` resuelve la verificación desde el 2026-08-29, pero `activateListing` —por donde la Fase 9 saca un `draft` a `active`— no, así que una inmobiliaria que importa cincuenta filas no deja ni una fila en `verified_contact`. No se plegó acá para no cruzar el techo de revisión con un segundo camino de escritura, y no abre ningún agujero: sin fila, la ficha no dibuja nada. Es una línea en `activateListing` más su prueba **CIERRA el 2026-09-02.** Una línea en `activate-listing.ts`, como decía, más sus dependencias: `resolveContactVerification` con el triple del BORRADOR —la fila que está por volverse activa y que la ficha va a leer—, después de la validación para que un borrador nunca activable no haga trabajo, y antes de la escritura por la misma razón que en `publishListing` (es un hecho de la CUENTA, cierto se gane o no la carrera del compare-and-swap). **Los dos puertos son obligatorios y no opcionales**, y eso es lo que obligó a completar los nueve sitios de llamada incluida la acción de servidor de `/mis-avisos`: con dependencias opcionales, el llamador que las olvide reabre este mismo agujero en silencio; así lo abre en `tsc`. **Pruebas nombradas** en `activate-listing.test.ts`, bloque «contact verification on activation (19.15)»: *«asks for the DRAFT's own contact triple, and records the account's email»*, *«activates a draft whose contact cannot be verified, and leaves no row»* —activar NO se corta, el canal de WhatsApp sigue diferido—, *«never reads verification for a draft the rules refuse, or for a stranger's»* y *«records an account verification of eleven months, and refuses one of thirteen»*, que es la 19.11 en esta costura. **Mutación**: borrar la llamada entera pone rojas 2 unitarias y 1 de integración con las pruebas de esta entrega, y ninguna sin ellas.
- [x] 19.16 **(nueva — medida al cerrar la 19.6, no supuesta.) La purga alcanza dos estados que ningún canal anuncia.** `DrizzleLifecycleListings.candidates` selecciona por `expires_at` y nada más, así que un `hidden` y un `draft` con 45 días encima pierden sus fotos igual que un vencido; `noticeCandidates` filtra `status IN ('active','expired')` y no les manda correo, y el conteo de la 19.6 los deja afuera porque renovar no devuelve un `hidden` a la búsqueda (9.31) y prometerlo sería mentir en la ficha. **Son dos problemas distintos y conviene no resolverlos con la misma línea.** Para el `hidden` la salida honesta es un aviso propio que cuente los días SIN prometer renovación, o dejar de purgarlo hasta que 9.31 defina qué hace renovar con un oculto. Para el `draft` la pregunta es anterior: un borrador que nunca se publicó probablemente DEBE perder sus fotos —es basura, y la 18.32 ya barre los borradores de publicación por esa misma razón—, pero entonces que lo haga el barrido que le corresponde y con su propio criterio, no de rebote por una fecha que para un borrador no significa lo mismo. **Lo que no puede quedar es lo de hoy: el borrado ocurre y nadie lo anunció**, que es la distinción entera de la 19.8. **CIERRA el 2026-09-02, y los dos salen de la purga — no ganan un canal.** El conjunto que la retención anuncia pasa a ser UNA lista en el dominio, `ANNOUNCED_LIFECYCLE_STATUSES` en `expiry.ts` (`['active','expired']`), que consumen las DOS consultas de `drizzle-lifecycle.ts`: `candidates` y `noticeCandidates`. Que el conjunto alcanzado y el conjunto avisado sean el mismo deja de ser una coincidencia entre dos `WHERE` que nadie compara y pasa a ser una sola línea que el día que cambie los mueve juntos. La otra salida —darles un canal propio— pierde por estado y por razones distintas, como la entrada pedía. **`hidden`: un conteo regresivo que no se puede evitar es una notificación, no un anuncio.** No hay ninguna forma de renovar un aviso oculto: `noticeCandidates` no le manda el correo del que sale el token de `/renovar/[token]`, y el botón de `/mis-avisos` es la 9.31, sin construir. Así que la frase honesta sería «tus fotos se borran el 27 y no podés hacer nada», y la de la 19.7 —«renovalo y el aviso vuelve con sus fotos»— sería **falsa justo en la ficha que la leería**, porque `renew` deja `hidden` como está. Lo que se paga por esperar son los bytes de R2 de un puñado de avisos que tres personas distintas reportaron, y se paga en una dirección reversible: el día que la 9.31 decida qué hace renovar con un oculto, volver a purgarlo es agregar un elemento a esa lista. Borrarlos hoy no se deshace. **`draft`: la fecha por la que se los purgaba no significa eso.** `confirm-import.ts` escribe `expiresAt: publishedAt` con su propio comentario al lado —«Placeholder until activation… carries no meaning before that… A draft is excluded from every expiry-driven query by its `status` alone»—, y **esa última frase era falsa exactamente para esta consulta**: la purga era la única lectura por vencimiento que no filtraba por estado, así que un borrador importado quedaba a 45 días de perder sus fotos contados desde el instante de la importación. Purgar por un número que nadie fijó no es una política de retención, es un error de coincidencia. Si un borrador tiene que perder sus fotos, lo hace el barrido que le corresponde y con su criterio propio — la forma que la 18.32 ya usa para los borradores de publicación. **Hueco que esto abre y no se tapa acá, dicho antes de que se descubra pagándolo**: con `draft` fuera, las fotos de un borrador de cartera no las purga nada. Hoy el agujero está VACÍO —`confirm-import.ts` crea las filas con `photos: []` y la activación las mueve a `active` antes de que exista foto—, así que no hay bytes colgando; pasa a ser real el día que un borrador pueda ganar fotos sin activarse. Queda como **19.17**. **Corrección del 2026-09-02, medida al ir a cerrar la 19.17: esta frase —«hoy el agujero está VACÍO»— es falsa, y lo era al escribirse.** `attachPhotoToDraft` (9.20/9.21) le pone fotos a un borrador importado desde la Fase 9, cableado a `/mis-avisos`, y ya lo afirma contra Postgres real `listing-photo-attachment.test.ts` → *«the legitimate owner attaches a photo: the row lands, at position 0»*. La decisión de esta tarea —sacar `draft` de la purga porque `expires_at` no significa nada antes de activar— sigue siendo la correcta; lo mal medido fue el tamaño del hueco. Ver la 19.17. **Pruebas nombradas** (`tests/integration/lifecycle.test.ts`, bloque «la purga alcanza lo mismo que se anuncia (19.16)»), contra Postgres real y **cada negativa con su positiva**: *«no se lleva las fotos de un oculto ni de un borrador, y sí las del vencido»* —el `toContain` del vencido va primero a propósito: sin él, una purga que dejara de alcanzar TODO pasaría las dos negativas, y está medido: mutar el corte a `new Date(0)` pone roja esa mitad— y *«y tampoco les manda el correo que sí le manda al vencido»*, que es la otra mitad del conjunto y se prueba en su propia ventana porque las dos son disjuntas por diseño (la purga mira antes del día 45, el aviso después). De esa segunda, la mitad de `draft` ya la guardaba la 9.19 en `listing-activation.test.ts`; la de `hidden` no la guardaba nada. **Mutación, las dos columnas.** Con las pruebas nuevas en el árbol, cada una pone roja exactamente la que la nombra: quitarle el predicado de estado a `candidates` → la primera; ensanchar el `inArray` de `noticeCandidates` → la segunda; **ensanchar la lista compartida a `['active','expired','hidden','draft']` → LAS DOS**, que es la medida de que la lista única es lo que las ata. **Con las pruebas nuevas fuera del árbol, quitarle el predicado a `candidates` no pone roja ninguna de las 2.824** — o sea que hasta hoy nada en el repositorio sabía a qué llega la purga.
- [ ] 19.17 **(nueva — abierta por la 19.16, con el agujero hoy vacío y medido.)** Con `draft` fuera de la purga, las fotos de un borrador de cartera no las purga nada. **Hoy no cuelga un solo byte** y eso está comprobado, no supuesto: `confirm-import.ts` crea las filas con `photos: []` y la única salida de `draft` es `activateListing`, que las pasa a `active` antes de que exista foto alguna. Se vuelve real el día que un borrador pueda ganar fotos sin activarse — la edición de un borrador importado, o una importación que traiga URLs. Cuando eso exista, el barrido es el suyo y con su propio criterio (la forma de la 18.32: leer las claves ANTES de borrar la fila, sin ensanchar `PhotoStoragePort`, que sigue sin `list` a propósito), **nunca reabriendo `ANNOUNCED_LIFECYCLE_STATUSES`**: esa lista es el conjunto que se anuncia, y un borrador no recibe ninguno de los dos avisos. **SIGUE ABIERTA, y su premisa era falsa — comprobado el 2026-09-02 antes de escribir el centinela que iba a cerrarla.** Se pidió cerrarla como guardián: una prueba que se ponga roja el día que un borrador pueda ganar fotos sin activarse. Se verificó la premisa en vez de darla por buena, y **ese día ya pasó: fue la Fase 9.** Las dos mitades de la premisa se miden por separado y sólo una se sostiene. *Cierto*: `confirm-import.ts:139` crea las filas con `photos: []`, y es el ÚNICO sitio del repositorio que escribe `status: "draft"`. *Falso*: «la única salida de `draft` es `activateListing`». Ésa es la única transición de ESTADO, pero las fotos no esperan a la transición — **`attachPhotoToDraft` existe justamente para ponerle fotos a un borrador importado** (9.20/9.21, y su propio encabezado lo dice: «an imported draft is created with zero photos (tasks.md 9.15), and until this file existed there was no way to put one on it»). Su puerta es `findDraftById`, con `status = 'draft'` adentro del `WHERE`, así que sólo acepta borradores. **Y está cableado a una pantalla viva, no a un puerto suelto**: `/mis-avisos` dibuja `<SubirFoto>` para toda tarjeta con `card.state === "draft"` (`app/mis-avisos/page.tsx`), que llama `adjuntarFotoAlBorrador` (`app/mis-avisos/actions.ts`) → `attachPhotoToDraft` → `processUploadedPhoto`, que **promueve los bytes en R2** y escribe la fila de `listing_photo`. No hace falta creerle a un comentario: ya lo afirma contra Postgres real una prueba que shipeó hace fases — `tests/integration/listing-photo-attachment.test.ts` → *«the legitimate owner attaches a photo: the row lands, at position 0»*. **Así que el agujero no está vacío: está abierto y en producción.** Un borrador de cartera que recibió fotos por `/mis-avisos` y nunca se activó conserva sus filas de `listing_photo` y sus objetos promovidos de R2 **para siempre**, porque la 19.16 sacó `draft` de `candidates` y el barrido de la 18.32 no lo alcanza —barre `publication_draft`, la sesión de publicación a medias, que es OTRA tabla—. No lo purga nada. **Escribir el guardián que se pidió habría sido mentir.** Un centinela que afirme «ninguna fila `draft` lleva fotos» o se pone rojo el mismo día que se escribe, o —si se lo apunta estrecho, contra `confirm-import.ts`— pasa certificando lo contrario de lo que ocurre. Una casilla marcada acá le diría al próximo lector que las fotos de un borrador se borran, y no se borran. **Lo que la 19.16 decidió sigue en pie y no se toca**: purgar un borrador por `expires_at` era purgar por un número que nadie fijó (`confirm-import.ts` escribe `expiresAt: publishedAt` como marcador, con su comentario al lado). Lo que cambia es el TAMAÑO del hueco que dejó, que se midió mal: no era «hoy vacío, real el día que…», era real desde el primer borrador con foto. **Lo que falta sigue siendo lo que la 19.17 ya nombraba**, y ahora con urgencia en vez de con hipótesis: un barrido propio para el borrador, con su propio reloj —porque `expires_at` no significa nada antes de activar— y con la forma de la 18.32 (leer las claves ANTES de borrar la fila, sin ensanchar `PhotoStoragePort`). **Nunca reabriendo `ANNOUNCED_LIFECYCLE_STATUSES`**: esa lista es el conjunto que se ANUNCIA, y un borrador no recibe ninguno de los dos avisos, así que meterlo ahí volvería a borrar sin avisar. Falta además la decisión de producto que el reloj necesita y que nadie tomó: cuántos días vive un borrador de cartera con fotos antes de que se le borren, y por qué canal se le dice. **Cuánto cuelga hoy**: nada medible, y por la misma razón que la 19.4 — los datos de producción son ficticios (fundador, 2026-09-02). Eso lo hace urgente antes de que haya inmobiliarias de verdad, no después.

- [ ] 19.18 **(nueva — abierta por la 14.28, y es la tarea que la 19.3 ya había descrito sin número.) El rederivado masivo de lo ya guardado.** La migración `0010` lo dice dentro de sí misma: *«Las otras tres —card, strip y detail— NO existen para esas fotos y hay que re-derivarlas desde la de 1280, que es su propia tarea»*, y la 19.3 lo repitió desde el otro lado — *«no existe ningún barrido que vuelva a derivar lo ya guardado»*, verificado contando llamadores: `deriveListingPhoto` sale de `processUploadedPhoto`, `app/publicar/fotos/publication.ts`, `app/mis-avisos/actions.ts` y `scripts/seed-demo.ts`, y de ninguna otra parte.

  **Qué desbloquea, dicho con precisión para que nadie lo agrande.** **No** es prerrequisito de la cuadrícula: `buildListingGrid` ya descarta al aviso cuya portada no tenga `thumb` y `card`, así que una foto a medio derivar nunca dibuja una tarjeta rota — sólo no dibuja tarjeta. Lo que esto desbloquea es que **los avisos publicados antes del 2026-08-22 vuelvan a aparecer**, que es inventario. Con ~47 avisos hoy, cuántos son exactamente es lo primero que hay que contar: un `select` sobre `listing_photo_derivative` agrupando por `photo_id` dice cuántas portadas tienen menos de los cinco tamaños, y ese número decide si esto vale un barrido o se paga solo con las publicaciones nuevas.

  **La forma, ya acotada por la 19.3**: leer cada `full` guardado, redimensionar y reescribir la clave, **con el orden de la 18.32 —leer antes de borrar—** y **sin ensanchar ningún puerto**: `PhotoStoragePort` ya tiene `remove` y las credenciales ya necesitan `DeleteObject`. Cobra además la holgura del visor a 1024 (19.3) sobre lo viejo, que hoy conserva su `full` de 1280
## Phase 12: Cleanup

- [ ] 12.1 README: setup, env vars, deploy steps
- [ ] 12.2 Confirm `pnpm test`, `test:unit`, `test:integration`, `test:e2e` all pass end to end

---

## Fase 20 — Lo que las láminas revelaron (2026-08-22)

> **Qué significa una casilla marcada acá.** Que se entregó y está en `main`, verificado por **tests**: unitarios, integración contra Postgres real, build, y verificación por mutación de las reglas que cargan peso.
>
> **No significa que el fundador lo haya usado.** Son dos verificaciones distintas: los tests prueban que el código hace lo que los tests dicen; nadie probó todavía que la pantalla se vea bien ni que el flujo tenga sentido con las manos encima.
>
> **Marcar no congela.** Una casilla se reabre, se corrige y se le agregan tareas — pasó hoy mismo con la 14.22b, la 14.23b y la decisión D14. Lo que no se hace es marcar algo que no se entregó.


**Las nueve pantallas y las cuatro especificaciones se importaron hoy** (`design/pantallas/`, `design/especificaciones/`). Hasta esta mañana el repositorio tenía tres pantallas de nueve, y las fases 14 a 19 se escribieron leyendo texto en vez de mirando dibujos.

Esta sección es el resultado de auditar el plan **contra los archivos** ahora que están. No son ideas nuevas: son cosas que el fundador ya había dibujado o escrito y que el plan no nombraba.

### 20a. El inicio, que resultó tener tres piezas más

La lámina del artboard `inicio` dibuja, arriba de las cuatro tiras, dos elementos que ninguna tarea mencionaba. El error de lectura está identificado: la F1 sólo nombra la barra **en el caso vacío** (*«sin ningún aviso activo, el inicio muestra la barra de búsqueda y una invitación a publicar»*), y de ahí salió la conclusión —equivocada— de que la barra aparecía sólo entonces.

- [x] 20.1 **La barra de búsqueda «¿En qué zona buscás?», SIEMPRE visible.** No sólo en el estado vacío. Es lo primero que se ve al entrar y hoy no existe. **Hecho, PR #92.** `homeSearchBar` en el dominio. Apunta a `/alquiler/<ciudad>` y no al acordeón, que no existía como ruta al construirla.
- [x] 20.2 **Las fichas de ciudad en el inicio** (F2). Al elegir una, las colecciones se recalculan sólo con esa ciudad, **la tira de la otra desaparece**, y la URL pasa a `/?ciudad=maracaibo`. Es la regla de aislamiento de ciudad aplicada al inicio, y no estaba escrita en ninguna tarea. **Hecho, PR #92.** `homeCityChips`. Con ciudad elegida, las tres colecciones llevan `cityId`; un `?ciudad=` desconocido no cae a la primera. Mutación: romper la llamada en `page.tsx` dejaba TODO el dominio verde con el aislamiento roto — el hueco que el piso del 90 % deja por no llegar a `app/`.
- [x] 20.3 **El subtítulo con conteo de cada tira**: «23 avisos activos en cuatro zonas.» La lámina lo dibuja bajo el título de la tira de ciudad. **Hecho, PR #92.** `zoneCount` en el puerto. Sigue siendo una sola consulta: obligó a cambiar la ventana por un CTE con `group by`, porque Postgres no admite `count(distinct …) over (…)`.
- [x] 20.4 **Los tres estados de la barra** — la lámina les dedica un artboard propio (`los tres estados de la barra`), así que son tres y hay que mirarlos antes de construir. **Hecho.** `src/modules/identity/domain/nav-account.ts` (`resolveNavAccount`, `resolveNavPublish`) decide sin sesión / con sesión / agencia — 8 casos en `nav-account.test.ts`, incluida la prueba explícita de que sesión y agencia producen el MISMO estado salvo `canImportListings`. `src/modules/identity/domain/account-initials.ts` degrada las iniciales del nombre al correo (cuenta de enlace mágico, sin nombre) — 9 casos. `components/organisms/Nav.tsx` + `.module.css` traducen esos tres estados a marcado — grid `250px 1fr 250px` en escritorio (14a), un solo renglón con `flex-wrap`/`order` en móvil, 10 casos en `Nav.test.tsx` incluida la prueba byte-a-byte de que sesión y agencia dibujan la MISMA barra. **Cableado en las cuatro pantallas del camino de lectura por la 14.39/14.40/14.41**; cuando se marcó esta casilla el nav existía y sólo lo dibujaba `/mis-avisos`, que es la deuda que la 14g nombró y saldó. En el camino se corrigió el `back` (ver 14.40): dibujaba un `←` mudo con la etiqueta escondida en un `aria-label`, y `resultsLink` devuelve dos acciones distintas que así se veían iguales. **DEFECTO CORREGIDO, encontrado midiendo mientras se cableaba la ficha (14.40).** Los tres slots de escritorio se colocan por `order`, y esta tarea declaró sólo los del teléfono —marca 1, acciones 2, pastilla 3—, que son los del `flex-wrap` de dos renglones. En la grilla de `250px 1fr 250px` esos mismos valores dejaban **la pastilla en la columna derecha de 250 px y las acciones en el centro flexible**: el encabezado al revés de como lo dibujan esta misma lámina y la 7b/7c. Medido en un navegador real: centro de la barra 640, centro de la pastilla **1065**. La prueba que había —`grid-template-columns: 250px 1fr 250px`— era cierta y no decía **nada** sobre qué cae en cada columna, o sea un gate que no afirmaba nada sobre lo que existía para proteger. Corregido declarando los tres `order` en el bloque de escritorio, y **medido** en `tests/measure/layout.spec.ts` ("a 1280 la pastilla va en el centro y las acciones contra el borde") — el arnés que la 1b.10 creó exactamente para esto: *"declarado en la hoja no es renderizado"*. Mutación: volver los `order` a los del teléfono pone roja esa prueba y sólo esa. **Discrepancia anotada, no resuelta en silencio:** el artboard "360 · sin sesión" no dibuja "Publicar gratis" en esa fila, pero el texto de la 14.38 ("sin sesión queda afuera y en acento, que es cuando hay que provocar") es explícito y más reciente (RESUELTO 2026-08-25) — se siguió el texto. Requerida al fundador la confirmación, igual que 20.10/20.8 ya quedan anotadas.

### 20b. Reglas de la búsqueda que estaban en el texto y no en el plan

- [x] 20.5 **Con 1 resultado, el botón lleva DIRECTO a la ficha** en vez de a una lista de uno (F7). Regla explícita del documento y sin tarea. **Hecho, PR #93.** `onlyListingHref` en `search-confirm.ts`.
- [x] 20.6 **Cambiar de ciudad borra las zonas elegidas, y se avisa ANTES** (F3). El descarte estaba implícito en el aislamiento de ciudad; el aviso previo no estaba en ninguna parte. **Hecho, PR #93.** `planCityChange` en `search-query.ts`.

  **⚠️ CORREGIDA 2026-09-02 por la 14.50: el aviso previo ya no existe, y la mitad que sí sobrevive es el descarte.** `planCityChange` se borró porque su único llamador de producción era `toCityChoice`, que armaba el paso de ciudad del panel — el paso que la 14.36 sacó por decisión del fundador. Desde entonces la función corría en cada carga y **su aviso no tenía dónde salir**.

  **Lo que NO se perdió**, y por eso esto es una anotación y no una regresión silenciosa: el descarte de la zona al cambiar de ciudad sigue garantizado dos veces. En la dirección, `homeCityChips` —el único selector de ciudad que el producto sirve hoy— compone `/?ciudad=…` **desde cero y no encima de la que había**, con la razón escrita al lado de la función; en el criterio, `buildSearchCriteria` descarta la zona que no pertenece a la ciudad. Lo que falta es **decirlo antes de que pase**, que era la mitad nueva que esta tarea aportó.

  **Qué haría falta para volver a tenerlo**, si el fundador lo quiere: una pantalla que ofrezca cambiar de ciudad **con zonas ya elegidas**. Hoy no existe — al inicio se llega sin zonas — así que el aviso no tenía visitante posible. Reabrir esta tarea es decidir esa pantalla primero; el texto y la regla se recuperan de `git show b5918e3:src/modules/listing-search/domain/search-query.ts`.
- [x] 20.7 **Un atributo que ningún resultado actual cumple aparece deshabilitado con conteo 0** (F6). No es lo mismo que «se muestra en cero»: hay que impedir elegirlo, porque llevaría a un vacío — la regla transversal 4 dice que ninguna opción puede llevar a un vacío. **Hecho, PR #93.** `search-options.ts`. No es «se muestra en cero»: se impide elegirlo, porque ninguna opción puede llevar a un vacío.

### 20c. Dos rutas que el maestro fija y el código no cumple

`design/especificaciones/Rentoru - Flujos y funcionalidades.md` §7ter es una tabla de URLs para **todo el sitio**. Dos filas no coinciden con lo que ships.

- [ ] 20.8 **`/entrar`, no `/signin`.** El maestro fija `/entrar`; el código sirve `/signin`. **No es cosmético**: `signin-return.test.ts` ata la ficha con esa pantalla por el nombre del parámetro, `safe-return-destination.ts` valida contra el prefijo `/signin?callbackUrl=`, y Auth.js tiene su propia configuración de ruta. Cambiarla toca cuatro lugares y hay que decidir si vale — o si el maestro debería decir `/signin`. **Decisión del fundador**
- [x] 20.9 **`/mis-avisos` es el nombre de la ruta de Mis publicaciones.** La pantalla no existe todavía; la tarea es que cuando se construya, se llame así y no `/mis-publicaciones`. **Hecho, parcial a propósito.** `app/mis-avisos/page.tsx`: protegida por sesión (`requireSession`), dibuja el `Nav` completo y una carcasa — "3 avisos · 1 vence pronto" y las tres filas de 14c NO están, porque necesitan una consulta que no existe todavía (mismo motivo que ya deja escrito la nota de la 9.26/9.27). Existe ya porque el control de cuenta del nav apunta acá en las dos rutas de escape (con y sin JavaScript) y un enlace roto no es aceptable (AGENTS.md §2). Probado en `tests/e2e/nav-account-control.spec.ts`: sin sesión cae en `/signin?callbackUrl=%2Fmis-avisos`, corre en las dos proyecciones de Playwright (`chromium`, `crawlability`) sin necesitar una vista previa desplegada — el mismo mecanismo que ya prueba `/publicar` en `publish-access.spec.ts` (sin cookie, `auth()` nunca toca la base). El caso CON sesión ("el control de cuenta lleva a `/mis-avisos` con JavaScript apagado") no se pudo probar con un navegador real — no hay forma de fabricar una cookie de sesión de estrategia `database` sin escribir la fila en el Postgres real de una vista previa, y este entorno no tiene esas credenciales — así que queda probado de forma determinista y más fuerte (corre en cada `push`, no sólo en PR con vista previa) en `AccountMenu.test.tsx`/`Nav.test.tsx` vía `renderToStaticMarkup`. Registrado como riesgo en el reporte de aplicación de este trabajo.

### 20d. Lo que las láminas NO dibujan, y ahora se puede afirmar

Con los nueve archivos en la mano, esto se verifica en vez de suponerse.

- [ ] 20.10 **La franja de 768 a 1099 px no está dibujada en ningún archivo.** Los nueve artboards son 360 o 1280. `design/README.md` la marca «sin diseñar» y el propio pie de las láminas la repite. Es una tableta en vertical o una ventana angosta, y **hoy un desarrollador tiene que decidirla por su cuenta — que es exactamente donde se rompe la cuadrícula**. Requerida al fundador
- [ ] 20.11 **Los errores de validación de publicar están especificados pero no dibujados** (Publicar §12.5). La tabla de la §6 da el texto de cada mensaje; el aspecto no está en ninguna lámina
- [x] 20.12 **El riel de nueve pasos de escritorio** (Publicar §7) no tenía tarea propia. No es la barra de progreso agrandada: en 1280 muestra **el valor de cada paso hecho** («Altamira», «$450 al mes») y permite saltar. La diferencia es entre saber cuánto falta y poder hacer algo al respecto. **Hecho, PR #94.** El riel muestra el valor de cada paso hecho, no sólo el progreso.

### 20e. Defectos del propio plan, corregidos hoy

- [x] 20.13 **Había DOS tareas 14.22 y DOS 14.23.** Renumeradas a `14.22b` y `14.23b`. Dos tareas con el mismo número es una que alguien da por hecha mirando la otra
- [x] 20.14 **La tarea 14.23 seguía escribiendo `/buscar?ciudad=…`**, que la 14.24 ya había descartado. Corregida, y la deuda que la 14.24 dejaba abierta —«pendiente de corregir en los documentos del fundador»— **está saldada por el fundador**: el §7ter del maestro dice textualmente que reemplaza `/buscar?ciudad=…` y `/aviso/84512`
- [x] 20.15 **El encabezado de la fase 14 seguía diciendo que los resultados se mudan a `/buscar`.** Corregido

## Fase 21 — La frescura son dos condiciones en TODOS los lectores (2026-08-28)

**FASE CERRADA el 2026-09-02 con la 21.7, la última de las siete.** Y cierra corrigiendo su propio recuento: los lectores del catálogo eran **cinco** y no cuatro — el quinto escribe el predicado en SQL crudo, que es por lo que nadie lo había contado.

**Número nuevo, por encima del más alto en uso.** La fase 20 era la última; ésta no es una tarea de la 5, la 14 ni la 16 metida a presión, porque cruza tres módulos y su razón es una sola. Este archivo ya arregló cinco colisiones de numeración (20.13 documenta dos de ellas); no se crea la sexta.

**El quinto lugar que la 11.23 dejó anotado y no construyó.** Esa tarea cerró la ficha, el JSON-LD, robots y el sitemap, y terminó nombrando esto como decisión pendiente del fundador por una razón concreta: sumarle `expires_at > now()` a la consulta más caliente del producto parecía una decisión de índice, y el índice es esquema. **Medido, no lo es** — ver abajo.

**La ventana está medida y no supuesta.** `vercel.json` agenda `/api/jobs/expiry-reminders` con `0 13 * * *`, una vez al día; `markExpired` corre adentro de ese trabajo (`send-lifecycle-notices.ts`) con `WHERE status = 'active' AND expires_at < now()`, y nada más mueve el rótulo. Como un aviso vence a los 30 días de la **hora** en que se publicó, el hueco entre «vencido por reloj» y «vencido en la base» va de 0 a casi 24 horas. Es la misma regla que la 11.13 ya había escrito para el sitemap: *«el filtro de frescura son **dos condiciones, no una**: `status = 'active'` AND `expires_at > now()`»*.

- [x] 21.1 **Los tres lectores del catálogo que leían sólo el rótulo ahora leen también el reloj.** `listing-search/infrastructure/drizzle-listing-search.ts` (los resultados), `listing-search/infrastructure/drizzle-faceted-search.ts` (los conteos) y `contact-reveal/infrastructure/drizzle-contact-reveal.ts` (`findRevealable`). Los tres suman `gt(listings.expiresAt, sql\`now()\`)` al mismo `WHERE` donde ya vivía el estado — en SQL y no en un `if` posterior, porque en el revelado la garantía es que **el contacto no sale de Postgres**. Las pruebas, contra Postgres real y no contra un puerto falso: `tests/integration/listing-search.test.ts` → **«no devuelve el aviso cuya fecha ya pasó, aunque su rótulo siga diciendo active»** (más «sigue fuera del alcance del filtro más específico que lo describe»); `tests/integration/faceted-search.test.ts` → **«no cuenta el aviso cuya fecha ya pasó, aunque su rótulo siga diciendo active»** y **«el total y las filas siguen de acuerdo con la fila vencida por reloj adentro»**; `tests/integration/contact-reveal.test.ts` → **«no devuelve nada de un aviso cuya fecha ya pasó, aunque su rótulo siga diciendo active»**. Cada bloque abre con una aserción de precondición —`SELECT status, expires_at < now()`— porque una prueba verde sobre una fixture que quedó `expired` no prueba nada; y las fixtures se siembran con `now() + make_interval(mins => …)` y **nunca con una fecha literal**, que es el defecto de la trampa 3 (ver 21.3). **Mutación**: quitar el reloj de la búsqueda pone en rojo 15 pruebas —las 2 suyas y 13 de paridad conteo/filas—; quitarlo de las facetas, 35; quitarlo del revelado, **exactamente 1**, la suya. El texto se verificó presente una sola vez y el sha256 del archivo antes y después, porque `sd` hace no-op silencioso sobre literales multilínea y sale 0.
- [x] 21.2 **Los conteos y las filas se arreglan JUNTOS o no se arregla ninguno.** La 14.11 dejó una sola consulta que devuelve las filas y cada faceta, y `faceted-search.test.ts` compara cada total contra las filas de `DrizzleListingSearch`. Si el reloj entrara sólo en las filas, la pantalla diría «9 avisos en Chacao» encima de una lista de ocho — **un conteo que discrepa de su propia lista es peor que uno viejo**, porque rompe lo único para lo que ese botón existe (regla transversal 3). Por eso la condición va en el `WHERE` compartido de `countFacets` y no columna por columna: de ahí salen el total, cada faceta, `cityTotal` y las nueve relajaciones, así que ningún número puede quedarse con la mitad de la regla. Medido: arreglar una sola de las dos consultas deja 13 pruebas de paridad en rojo.
- [x] 21.3 **DOS pruebas que ya existían estaban verdes midiendo otra cosa, y el reloj las delató.** `listing-activation.test.ts` («activates a draft with a photo…») activaba con `now = 2026-02-10` y `bulk-import-to-search.test.ts` («la cadena entera…») con `now = 2026-04-10`; el `expires_at` de las dos —treinta días después— ya había quedado en el pasado. Mientras la búsqueda miraba sólo el rótulo, las dos afirmaban «y aparece en la búsqueda» sobre un aviso **activado y ya vencido**. Corregidas a `new Date(Date.now() - 24h)`, que sigue siendo «bastante después de la importación» y no puede envejecer. Es la tercera vez que este proyecto encuentra una fixture que cambió de sujeto sola: **una fecha de prueba se escribe relativa a `now()`**.
- [x] 21.4 **`listing-lifecycle/infrastructure/drizzle-lifecycle.ts` es LA excepción y se deja como está, con la razón escrita al lado.** `markExpired` existe justamente para encontrar las filas cuyo rótulo quedó viejo: `status = 'active'` no es un descuido sino el sujeto de su consulta, y agregarle `expires_at > now()` la dejaría sin una sola fila que actualizar — el rótulo no volvería a moverse nunca. Confirmado, no «arreglado».
- [x] 21.5 **`listing-publication/infrastructure/drizzle-listing-repository.ts` no entra**, y tampoco es un olvido: sus dos `eq(listings.status, …)` son sobre `'draft'`, no sobre `'active'`. Un borrador no tiene ciclo de vigencia que leer —`activate` es lo que le calcula `published_at` y `expires_at`—, así que la frescura no significa nada ahí. `drizzle-sitemap.ts` ya estaba bien desde la 11.13 y no se tocó.
- [x] 21.6 **El índice se midió, no se supuso, y NO hace falta cambiarlo.** Con 40.000 avisos sembrados en un Postgres 18 dockerizado —70 % vigentes, 0,5 % vencidos por reloj con rótulo viejo (que es a lo sumo el día que el cron todavía no barrió), el resto `expired`/`hidden`/`draft`— el `EXPLAIN ANALYZE` del SQL que el adaptador realmente genera da **el mismo plan antes y después**: `Bitmap Index Scan on listing_city_status_idx` → `Bitmap Heap Scan`, `Buffers: shared hit=1532` en los dos casos, y `expires_at > now()` entra como `Filter` que descarta 80 filas de 14.080. Tiempo de ejecución 5,57 ms → 4,78 ms en los resultados y 6,57 ms → 6,40 ms en las facetas; el revelado sigue siendo un `Index Scan using listing_pkey` de 3 buffers. **El índice `(city_id, status)` sigue sirviendo**: acota a las filas activas de esa ciudad y la fecha filtra sobre un conjunto ya chico. Los dos planes completos están en el cuerpo del PR. Si algún día degradara, el arreglo sería `(city_id, status, expires_at)` — **una migración y una decisión de esquema, que es del fundador**; no se generó ninguna acá.
- [x] 21.7 **Riesgo de deriva anotado, no resuelto: el predicado está escrito CUATRO veces.** Sitemap, resultados, facetas y revelado repiten `gt(listings.expiresAt, sql\`now()\`)` cada uno en su módulo. Se dejó así a propósito —extraerlo a un helper compartido cruzaría el límite entre capacidades que la arquitectura mantiene, y el predicado es de una línea—, pero el que llegue con un quinto lector no tiene nada que se lo recuerde salvo este renglón y los comentarios de los cuatro archivos. **Lo que hoy lo sostiene son las pruebas de integración**, no el tipo. Si aparece un quinto lector del catálogo, la decisión se reabre. **CIERRA el 2026-09-02, y la decisión YA se había reabierto: los lectores son CINCO.** Contados archivo por archivo y no de memoria, sobre `src/` entero: `listing-discovery/infrastructure/drizzle-sitemap.ts:51`, `listing-search/infrastructure/drizzle-listing-search.ts:81`, `listing-search/infrastructure/drizzle-faceted-search.ts:123`, `contact-reveal/infrastructure/drizzle-contact-reveal.ts:143` y —el que este renglón no contaba— `listing-discovery/infrastructure/drizzle-home-collections.ts:139`, que lo escribe `and l.expires_at > now()` **en SQL crudo adentro de una plantilla**. `listing-lifecycle/infrastructure/drizzle-lifecycle.ts` sigue sin contar y no por descuido: es la excepción que la 21.4 confirmó, su comparación es al revés (`lt`) y sus otros tres usos acotan una ventana contra fechas calculadas, no contra el reloj.

  **Decisión del fundador, 2026-09-01: no se unifica, se declara.** El límite entre capacidades vale más que evitar cinco copias de una línea — el mismo criterio con el que se dejó cerrada la zona. Lo que faltaba nunca fue la unificación: era que **nada se pusiera rojo**.

  **La prueba: `src/vigencia-del-catalogo.test.ts`**, cinco afirmaciones nombradas. La que cuenta es *«son exactamente los declarados: un lector más reabre la decisión»*; las otras cuatro existen para que ésa mida algo — *«la guarda: el recorrido encuentra el árbol de src»*, *«la guarda: quitar los comentarios no se lleva el código»*, *«el ciclo de vida no entra, y entra sólo si se leen los comentarios»* y *«las dos formas del predicado están ejercidas por código vivo»*.

  **Sobre qué se afirma, y por qué una lista escrita a mano pierde.** El inventario se cuenta **recorriendo `src/`**, por la razón que `app/api/jobs/cron-wiring.test.ts` escribió el 2026-09-01: una lista hay que acordarse de actualizarla, y acordarse es justo lo que falla — un barrido que sólo mirara las cinco rutas conocidas jamás podría encontrar la sexta, que es lo único que esta prueba existe para encontrar. La lista de cinco vive del lado **esperado**, que es otra cosa: no es una segunda fuente sobre el código, es la decisión del fundador puesta por escrito, y que discrepe del árbol es exactamente el evento que la reabre. Tampoco se afirma sobre un NÚMERO: un número no dice cuál, y el diff de una lista ordenada nombra el archivo intruso.

  **Y las DOS formas del predicado, que es la mitad del valor.** Escrita con la de Drizzle sola —que es como se escribe sola— esta prueba decía **CUATRO habiendo cinco**: se corrió así y salió roja señalando `drizzle-home-collections.ts`. Ningún renombre de `expiresAt` toca ese SQL crudo y ninguna búsqueda del ayudante de Drizzle lo encuentra; habría sido exactamente el verde que no mide nada. **Y hay que quitar los comentarios antes de contar**, o la cuenta da seis: `drizzle-lifecycle.ts` lleva `expires_at > now()` escrito en su comentario justamente para explicar por qué NO lo usa. Es el mismo despojo que `app/inicio-contract.test.ts` ya hacía por la misma razón.

  **Por qué una prueba que lee el código y no una de comportamiento.** Las de integración de la 21.1 cubren los cinco lectores que existen; ninguna puede ponerse roja por uno que todavía no se escribió. Y `infrastructure/` no tiene piso de cobertura (`vitest.config.ts`), así que un sexto adaptador se entrega sin una sola prueba y el informe no cambia de color — el mismo hueco que la 19.11 nombró en `verified-contact.port.ts`.

  **Mutaciones, cada una con el texto presente una sola vez, sha256 antes ≠ después, restauración byte a byte y `git diff --stat` limpio; y cada una corrida DOS veces, con las pruebas nuevas en el árbol y con ellas fuera.** Sexto lector en `drizzle-listing-detail.ts` **en forma Drizzle**: 1 roja con las pruebas nuevas, **0 sin ellas** (2510/2510 verdes). El mismo sexto lector **en forma de SQL crudo**: 1 roja / **0 sin ellas** — la que un barrido de una sola forma se habría comido entera. Quitarle el predicado a un lector VIVO (el inicio): 3 rojas / **0 sin ellas**. Volver lector de verdad —en código, no en comentario— a la excepción de la 21.4: 2 rojas / **0 sin ellas**, que es lo que prueba que el despojo de comentarios no ciega la cuenta. Y vaciar el propio lector de archivos de la prueba deja **3 de sus 5 en rojo** en vez de un verde hueco.

## Fase 22 — El sistema de registro describía una hipótesis, y llegaron las láminas (2026-08-28)

**Número nuevo, por encima del más alto en uso (21.7).** Este archivo ya arregló **cinco** colisiones de numeración —la 20.13 documenta dos, la 14.30b, la 14.24b y la 14.22b/14.23b las otras— y no se crea la sexta. Se numera desde la 22.1.

**El origen, y reencuadra todo lo de abajo.** Cuando arrancó el proyecto **no había pantallas**: la anatomía de componentes de `SISTEMA.md` se escribió como **hipótesis**. Las nueve láminas llegaron el **2026-08-25** y contestaron distinto en seis puntos. Nadie violó el sistema — el sistema se escribió primero, y esta fase es reemplazar la hipótesis por lo decidido **dejando escrito por qué cambió**. Dentro de tres meses alguien va a leer «nunca una cuadrícula» y va a creer que hubo una violación.

**El orden de autoridad, fijado por el fundador:** manda la lámina, y donde una decisión posterior del fundador corrigió una lámina, manda la decisión. Las dos direcciones ya ocurrieron en este archivo — *«la lámina de móvil tenía el bug»* (14.36) y *«seguí el diseño, que fue lo que se decidió acá»* (14.40).

**AMPLIADO Y FIJADO POR EL FUNDADOR EL 2026-09-05.** La línea de arriba dice «manda la lámina» a secas y eso deja sin contestar tres de los cuatro casos que esta fase encontró. **Por qué la regla existe, y es la mitad que hay que leer primero:** `SISTEMA.md` se escribió **al arrancar el proyecto**, cuando no había ni una pantalla, así que su anatomía de componentes era una hipótesis. Las láminas llegaron el **2026-08-25**, **con el proyecto ya avanzado**, y llegaron **como una corrección deliberada de UI/UX** — no como una ilustración de lo que el sistema ya decía. Las ramas, en orden de precedencia:

0. **Un texto ya shipeado y decidido en una tarea anterior gana sobre todo lo demás.** El fundador: *«si son textos que ya están hechos en código y todo, que decidimos en tareas anteriores, tenemos que tomar esos textos»*. Una lámina no vuelve a litigar una redacción que ya se resolvió.
1. **Donde una lámina y `SISTEMA.md` discrepan en UI/UX —disposición, tipografía, componentes, color, tamaños— manda la lámina, y el sistema se actualiza para coincidir.** El fundador: *«el UI y UX es la lámina»*. Precedente: la regla dura *«Lista de filas, nunca grilla de tarjetas, en ningún ancho»* quedó anulada a favor de la cuadrícula de tarjetas que las láminas dibujan (14.25/14.33, y su fila en la tabla de cierre de la 22.1).
2. **Donde la discrepancia es sólo de redacción, manda el sistema o el texto shipeado, y la lámina NO se edita: se registra la divergencia.** El fundador: *«si es solo un texto, dejamos el de sistema»*. Precedente ya escrito en este archivo: la **16.40** — el producto escribe «12 sept.», las láminas escriben «12 sep», las láminas se dejaron como estaban y la divergencia quedó anotada en `SISTEMA.md` para que nadie la «arregle» más adelante creyéndola un defecto.
3. **Donde ninguna lámina lo dibuja, gobierna `SISTEMA.md`: se deriva, no se inventa.** Precedente: la **11b.2**, que levantó un bloqueo idéntico *«derivando, no inventando»*, sacando cada valor de un papel que el sistema ya declaraba y sin aportar uno propio.
4. **Una diferencia de redacción entre la lámina de móvil y la de escritorio no es un defecto.** El fundador: *«yo no estoy desarrollando la aplicación móvil, estoy desarrollando una aplicación web que funciona en el móvil y en un desktop, más nada»* — en el móvil también hay un navegador, y un toque ahí es un clic en el navegador.

**Lo que esta regla NO desbloquea, dicho para que no se cuente de más.** La rama 3 sigue gobernando la **20.10** (la franja de 768 a 1099 px), la **20.11** (los errores de validación de publicar) y la **22.29** (la dirección de correo que no sirve): **nadie las dibujó**, así que no hay lámina que mande y se derivan del sistema como hizo la 11b.2. No son tareas que el ruling del 2026-09-05 haya destrabado.

- [x] 22.1 **`SISTEMA.md` vuelve a describir lo dibujado.** Seis apartados resincronizados, cada uno con su fecha y la decisión que lo cambió, más una tabla de cierre («Lo que este documento decía y por qué cambió») que registra las nueve entradas para que la contradicción no se lea como una violación: la fila → la cuadrícula (14.25/14.33, láminas 6c/7c); la barra lateral de 240px → sin barra lateral (fundador, 2026-08-26); la miniatura de 44×34 → fuera del camino de lectura; `/mis-avisos` de cuatro badges → **cinco estados y una línea de metadato con color**; la altura mínima de control 36 → 44 (fundador, 2026-08-27); el precio de la ficha 26 → 30/34 (16.23); el precio de lista 15 → 16/17 en tarjeta. **Y una sección que faltaba entera: la pastilla de búsqueda**, con sus tres estados, su regla de móvil, por qué su conteo no es el del engranaje y por qué la ficha no la lleva (14.30b, 14.31, 14.38, 14.40; artboard 14i). El criterio de evaluación 1 pasa de «la fila no supera 96px» —que medía una fila que ya no está en el camino de lectura— a los cuatro/seis avisos sobre el pliegue de la 14.29
- [x] 22.2 **El precio de la tarjeta pintaba 15px, el token declaraba 17 y la lámina dibuja 16.** Tres números y ninguno era el que alguien pidió, con `lint:tokens` en 0 todo el tiempo. **La causa es estructural y vale más que el número:** `ListingCard.module.css` declaraba el tamaño en el `<p>` que envuelve y `Price.module.css` lo declaraba en el `<span>` que lleva el texto — **el hijo gana**, así que la declaración de la tarjeta no dibujaba nada. Y la prueba que existía —`expect(block(cardCss, "price")).toContain("var(--card-price-fs)")`— era cierta y ciega, que es la trampa 1 de este archivo en su forma más limpia.

  **Resuelto siguiendo la lámina (AGENTS.md §2), y las dos tienen razón porque describen anchos distintos** — la misma resolución que la 14.40 usó para el nav y la 16.23 para el precio de la ficha: `--card-price-fs: 16px` (artboard 6c, cuadrícula de `158px 158px`) y `--card-price-fs-desktop: 17px` (artboard 7c, `repeat(4,254px)`). El tamaño llega al átomo por `--price-fs`, que es un **enganche y no un valor**: la superficie elige qué token lo alimenta y `lint:tokens` sigue rechazando el literal.

  **Medido, no declarado.** `tests/measure/layout.spec.ts` → *«22.2: a 360 el precio de la tarjeta mide 16px, que es --card-price-fs»* y *«…a 1280 … 17px, que es --card-price-fs-desktop»*, que comparan el `getComputedStyle` renderizado **contra el token resuelto en la misma página** — así se rompen tanto si el átomo vuelve a pisar el tamaño como si alguien cambia el token creyendo que eso cambia la pantalla. **RED capturado en vivo antes de tocar producción**: `[22.2] 360px: dibujado 15px · --card-price-fs=17px`. **Mutación**: devolver `var(--price-fs, var(--fp))` a `var(--fp)` pone rojas exactamente esas 2 de 37 medidas; restaurado y verificado por sha256 del archivo.
- [x] 22.3 **El metadato deja de estar escrito tres veces.** `ListingCard.module.css:72` y `ResultRow.module.css:58` eran idénticas byte a byte salvo un comentario, y `mis-avisos.module.css:126` era una tercera copia que **ya había perdido** `font-family`, `font-weight` y `line-height`. Esa divergencia es el argumento, no un detalle: la misma frase se dibujaba en dos pesos según la pantalla y ninguna prueba podía verlo, porque cada hoja era coherente consigo misma. **Hecho:** `components/atoms/ListingMeta.tsx` + `.module.css`, un solo sitio donde se declara.

  **Probado midiendo, con el arnés extendido para poder comparar.** `app/measure/page.tsx` monta ahora la carcasa de la fila de `/mis-avisos` al lado de la cuadrícula —sin el borrador, su formulario ni su Server Action— y `tests/measure/layout.spec.ts` → *«22.3: el metadato se dibuja igual en la tarjeta y en /mis-avisos»* compara los cinco valores calculados. **RED en vivo**: `tarjeta 600 / 16.8px` contra `mis-avisos 400 / 18.6px`. **Mutación**: `--meta-fw` → `--ftw` en el átomo pone roja exactamente 1 de 37; restaurada por sha256.

  **Se convergió al papel que `SISTEMA.md` declara (12/600/1.4/`--sans`) y NO al de las láminas nuevas, a propósito**: los artboards dibujan el metadato de la tarjeta en 10,5–11px con `--meta` (mono) y peso 400. Bajarlo es un cambio visible en dos pantallas que ninguna tarea pidió y ninguna decisión cubre — queda como **22.9**. Lo que esta tarea compra es que el día que se decida haya **un solo sitio** donde cambiarlo
- [x] 22.4 **El título de lista, y sólo ese papel.** Dos consumidores vivos que **ya discrepaban**: `ListingCard` en `--ftw` (400) recortado a dos líneas y `/mis-avisos` en `--ficha-title-fw` (600) sin recortar. **NO se plegaron el `<h1>` de la ficha ni el pie del visor**: son otros papeles tipográficos con sus propios tokens, y fundirlos es exactamente cómo el `<h1>` del inicio terminó agarrando `--fpb`. **Hecho:** `components/atoms/ListingTitle.tsx`.

  **El nivel de encabezado y el recorte viajan como props cerradas, y cada una tiene su razón.** `level: 2 | 3` porque el nivel es del **documento** —en la cuadrícula el título cuelga del `<h1>` de la pantalla y en `/mis-avisos` del `<h1>` de la página—, cerrado a dos valores para que un `as` libre no deje dibujarlo como `<div>`. `clamp` porque el recorte es del **contenedor** y no del tipo: la cuadrícula lo necesita para que dos tarjetas vecinas alineen y la lámina 14c dibuja `/mis-avisos` sin recortar.

  **Medido**: *«22.4: el título de lista se dibuja igual en la tarjeta y en /mis-avisos»*. **RED en vivo**: `400 / 17.55px` contra `600 / 20.8px`. **Mutación**: quitarle la clase de tipografía al caso con recorte pone roja exactamente esa prueba.

  **`ResultRow` entró en la promoción aunque ningún visitante la vea**, y la razón se dice: sus únicos consumidores son el arnés de medición y su propia prueba —la cuadrícula la reemplazó en la 14.25—, pero es la fila que 1b.10/1b.11/1b.12 miden, y **un arnés que mide una copia privada no mide el producto**. Costó ocho líneas. Consecuencia verificada: al irse `max-height: var(--tclamp)` la fila mide lo mismo — 87,94px medidos antes y después con el árbol anterior, o sea cero regresión. *(De paso: el «85.89px» que la 1b.10 registra es un número viejo; hoy son 87,94 y la cota de 96 sigue holgada.)*
- [x] 22.5 **La ficha de selección se pinta con UN idioma, el del inicio. Decidido por el fundador el 2026-08-28.** Dos pantallas dibujaban el mismo componente —un enlace que elige una opción de un conjunto, con una marcada— y lo pintaban distinto: `app/home.module.css:69` con relleno `--tint`, borde `--accent` y texto `--accent`, que es el **nivel 2 de la jerarquía de botones** de `SISTEMA.md`; `app/mis-avisos/mis-avisos.module.css:52` con el mismo relleno pero borde `--strong` y texto `--ink`.

  **Verificado antes de aplicar, y el conteo cambia el argumento: son tres fichas y dos papeles, no dos idiomas empatados.** `FilterChips.module.css:14` —las fichas de filtro puesto de la lámina 7c— **ya usaba el idioma del inicio**, así que la resolución es dos contra uno y además la que coincide con el sistema. Pero `FilterChips` **no se pliega en el mismo componente**: es otro papel — siempre representa un filtro *aplicado*, siempre lleva su × de quitar y no tiene estado «no elegida». Forzarlas juntas fundiría dos roles, que es el error que este archivo ya documenta para el título.

  **Medido**: `tests/measure/layout.spec.ts` → *«22.5: la ficha elegida se pinta igual en el inicio y en /mis-avisos»*, comparando **las tres propiedades a la vez** — comparar sólo el relleno habría estado verde desde antes, porque el relleno era la única que ya coincidía. **RED en vivo**: borde `rgb(120,129,137)` (`--strong`) y texto `rgb(30,32,34)` (`--ink`) contra `rgb(39,35,67)` (`--accent`) en las dos. **Mutación**: `border-color: var(--accent)` → `var(--strong)` pone roja exactamente 1 de 38. *(Primer intento de mutación fallido y anotado: `sd` con un `\n` en el patrón hizo **no-op silencioso** y salió 0 — la trampa 4 de este archivo, atrapada por comparar el sha256 antes y después.)*
- [x] 22.6 **El encabezado de resultados, extraído.** `app/alquiler/[ciudad]/ciudad.module.css` y `app/alquiler/[ciudad]/[zona]/zona.module.css` no diferían en una sola regla de CSS —sólo dos comentarios— y los dos `page.tsx` repetían el mismo bloque de JSX bastante más grande que la estimación original (~140 líneas cada uno, no ~30: cubre miga de pan, título, avisos, conteo con el orden, fichas de filtro, la página que ya no existe, el vacío, la cuadrícula y la paginación).

  **Se extrajo el encabezado ENTERO más la carcasa de resultados, no un átomo de miga de pan**: `components/organisms/SearchResultsHeader.tsx` (+`.module.css`) dibuja miga de pan/título/avisos/conteo+orden/fichas de filtro, y `components/organisms/SearchResultsList.tsx` (+`.module.css`) dibuja la página inexistente/vacío/cuadrícula/paginación/cierre. Las dos son organismos —componen moléculas completas (`FilterChips`, `OrderMenu`, `ListingGrid`/`ListingCard`, `SearchOutcome`), no átomos sueltos— y ninguna entra en `SISTEMA.md`: es una corrección de duplicación de código, no anatomía nueva del sistema de diseño (verificado: ninguna lámina dibuja una miga de pan). `ciudad.module.css` y `zona.module.css` se borran enteros; los dos `page.tsx` quedan reducidos a resolver sus datos y montar los dos organismos.

  **`app/measure/lista/page.tsx` (el arnés de la 14.29) monta ahora el mismo `SearchResultsHeader`** en vez de duplicar su marcado una tercera vez, y su cuadrícula lee `.results` de la hoja real de `SearchResultsList`. `app/measure/lista-medida.test.ts` se reescribió: en vez de comparar nombres de clase (`styles.breadcrumb`, etc., que dejaron de estar en `page.tsx`), ata las tres pantallas a que monten el MISMO componente y a que la hoja del arnés sea la real y no una copia.

  **No cambia un pixel: probado por medición, no por texto de archivo.** `tests/measure/lista.spec.ts` (14.29, la cota exacta que este mismo enunciado protegía) sigue midiendo 2 avisos completos a 360×640 y 4 a 1280×800 después del cambio, igual que antes. `pnpm test` 2880/2880, `pnpm typecheck` limpio, `pnpm lint` 0 errores, `pnpm test:integration` 394/394, `pnpm test:measure -- --workers=2` 71/72 (el rojo es `sugerencias.spec.ts` 14.51, intermitente y ya conocido en `dev`, ajeno a esta tarea).

  **Tamaño, dicho y no escondido: `size:exception` aceptado por el fundador.** Este enunciado ya había proyectado ~380 líneas revisables y por eso se dejó fuera una vez; medido con `git diff --numstat` (ambas columnas) más las líneas de los archivos nuevos, el cambio real es ~1384 —sobre todo por las ~640 líneas de CSS/JSX 100 % duplicado que había que borrar de verdad—. Un primer intento de esta rebanada podó comentarios para acercarse al presupuesto original de 500 y eso mismo se revirtió: `work-unit-commits` prohíbe achicar un diff sacando comentarios, docs o pruebas para caber, y en este repositorio el comentario ES la documentación —`SISTEMA.md` y este mismo archivo remiten a él—, así que la prosa completa quedó restaurada en los cuatro archivos nuevos y en cada archivo tocado. Es el mismo patrón que 1b.a (734 sobre 200–300), 1b.b (526 sobre 250–350) y 23.6+23.7 (1.227 sobre 800) ya dejaron anotado en este archivo: reportar la excedencia en vez de recortar para caber, y pedir la excepción
- [x] 22.7 **`.srOnly` está definida CUATRO veces, con el mismo cuerpo.** ~~Contadas: `app/home.module.css:9`, `app/publicar/publish-steps.module.css:24`, `components/molecules/SearchPill.module.css:120` y `app/alquiler/[ciudad]/[zona]/[slug]/foto/[n]/visor.module.css:119`.~~ **CORRECCIÓN AL TEXTO DE ESTA TAREA, verificada contra el árbol de trabajo el 2026-09-04: son CINCO, no cuatro, y una ya había divergido.** `app/home.module.css:9`, `app/publicar/publish-steps.module.css:24`, `components/molecules/SearchPill.module.css:125` (no 120), `components/client/SearchSuggestions.module.css:82` (que esta tarea no nombraba) y `app/alquiler/[ciudad]/[zona]/[slug]/foto/[n]/visor.module.css:119` — ésta última con sólo SEIS de las nueve propiedades del cuerpo (le faltaban `margin: -1px`, `padding: 0` y `border: 0`), que es justamente la deriva que la tarea predijo antes de que ocurriera. Es una utilidad de accesibilidad, no un papel tipográfico, y las cinco usan `clip-path: inset(50%)` en vez de `display:none` por la misma razón escrita —sacarlo del árbol de accesibilidad sería justo lo contrario de lo que se busca—. **El riesgo no es el peso sino la deriva**: la copia que un día se «arregle» con `display:none` deja de ser accesible y ninguna prueba lo ve. **Decisión del fundador, 2026-09-04: `.srOnly` se muda a un módulo compartido y se consume con `composes`.** Nuevo módulo `src/styles/a11y.module.css` (CSS Module, no una hoja global — el repositorio no tiene `globals.css` en ninguna parte) con el cuerpo completo de nueve propiedades; vive en `src/styles/` porque es donde ya vive la infraestructura de diseño compartida (`tokens.css`, `base.css`) y ninguna de las dos áreas (`app/`, `components/`) es dueña de una utilidad que las dos consumen. Las cinco hojas pasan a `.srOnly { composes: srOnly from "…/src/styles/a11y.module.css"; }` con ruta relativa (los alias de TypeScript como `@/` no resuelven dentro de `composes` de css-loader), y `visor.module.css` unifica al cuerpo completo, cerrando su divergencia. **Prueba**: `src/styles/a11y.test.ts` — *«src/styles/a11y.module.css declara el cuerpo completo una sola vez»* y, por cada una de las cinco hojas, *«compone srOnly del módulo compartido y no repite el cuerpo»* (con su par de la negativa: falla si el `composes` falta O si el cuerpo viejo sigue ahí). Roja antes del cambio (las cinco hojas no tenían `composes`), verde después. **Es un movimiento de CSS puro y ninguna prueba de comportamiento lo distingue** —las cinco pintan igual en pantalla, con o sin la unificación, porque `clip-path: inset(50%)` ya recorta el elemento a nada visible—; la evidencia es el texto de las hojas, el mismo criterio que ya usa `components/design-contract.test.tsx` para leer `scripts/lint-tokens.mjs` y `tokens.css`. Suite de seguridad corrida sobre las cinco hojas y sus consumidores: verde (191 + 29 pruebas)
- [x] 22.8 **El contador de fotos sobre la portada, que las láminas dibujan y no existe.** `Rentoru - Lista y Filtros - Desktop.dc.html` artboard 7c lo dibuja en cada tarjeta: `position:absolute; right:9px; bottom:9px`, `--meta` a 10px, pastilla de fondo `--surface` y texto `--soft`, diciendo `1 / 6`. **Ninguna tarea lo nombraba**; se destapó leyendo la lámina para la 22.1. El dato existe —`photoCount` ya viaja en `/mis-avisos`— pero la consulta de la cuadrícula no lo trae, así que el trabajo es el dato antes que el pixel
- [x] 22.9 **La tipografía de la tarjeta: las láminas la dibujan más chica que el sistema. ~~Decisión del fundador.~~ Decidida por el fundador el 2026-09-05: manda la lámina.** Medido contra los artboards: título **12,5px** a 360 y 13 a 1280 contra los 13 planos que declara `SISTEMA.md`; metadato **10,5px** a 360 y 11 a 1280, en `--meta` (mono) y peso 400, contra los 12 / 600 / `--sans` de la fila «Metadato». El precio ya se resolvió en la 22.2 porque su defecto era una contradicción interna del código; **éste no lo es** — el código es coherente y lo que difiere es lámina contra sistema en un papel que el sistema declara para todo el producto. Bajarlo cambia dos pantallas a la vista. Desde la 22.3/22.4 el cambio es de **una línea en un archivo** **DESBLOQUEADA 2026-09-05 — manda la lámina (rama 1 del encabezado de esta fase), y sigue abierta porque queda el trabajo, no la decisión.** El fundador: *«el UI y UX es la lámina»*, y esto es tipografía, o sea UI/UX y no redacción. La tarjeta baja a lo que dibujan los artboards —título 12,5 a 360 y 13 a 1280; metadato 10,5 y 11, en `--meta` (mono) y peso 400— y **`SISTEMA.md` se actualiza para coincidir**, que es la dirección de la rama 1: acá no se registra una divergencia, se corrige el sistema. Con eso queda contestada la pregunta que la 22.3 dejó abierta cuando convergió a propósito al papel del sistema (12/600/1.4/`--sans`) «para que el día que se decida haya un solo sitio donde cambiarlo»: ese día es hoy. Lo que falta es el cambio y su medición con el criterio de la 22.2/22.3 — `getComputedStyle` contra el token resuelto en la misma página, no una aserción sobre el texto de la hoja. **RETIRADA de este recorte, fundador 2026-09-06, después de medir.** El cambio es real —las láminas sí dibujan la tarjeta más chica—, pero queda **inseparable** de la reescritura pendiente de la prueba `14.29` (`tests/measure/layout.spec.ts`), que sólo puede firmar el fundador: esa prueba supone que la MISMA frase de metadato tiene el MISMO ancho natural a 360 y a 1280, premisa que el par de tamaños por punto de quiebre de esta tarea retira por diseño —10,5px a 360, 11px a 1280, dos anchos donde la prueba medía uno solo—. Y la propia medición de este cierre mostró que lo que de verdad frena la frase no es el tamaño: es la FAMILIA. Queda retirada de este recorte hasta que las dos —la reescritura firmada de la 14.29 y esta tarea— se cierren juntas **CERRADA 2026-09-07, con las dos piezas firmadas por el fundador entrando juntas.** Tamaño: `--card-title-fs`/`--card-title-fs-desktop` bajan a 12,5px/13px (`ListingTitle.module.css`, un solo componente con un `@media (min-width: 768px)`, el mismo patrón que ya usa `--card-price-fs`). Familia: **se queda en `--sans` y NO se toca `--meta`** — el metadato de la tarjeta pasa a leer un papel propio, `--card-meta-fs`/`--card-meta-fs-desktop`/`--card-meta-fw` (10,5px/11px/400), declarado y consumido sólo por `ListingMeta.module.css`; las diez superficies que leen `--meta-fs`/`--meta-fw` (`SearchPanel`, `AccountMenu`, `SiteFooter`, `SearchSuggestions`, el inicio, entre otras) no se tocan. `pnpm test` → `lint:tokens` verde: los cinco tokens nuevos tienen lector. **Divergencia registrada en `SISTEMA.md`** (fila «Metadato de tarjeta (lista)» de la tabla de tipografía y punto 6 de «Lo que queda abierto»), con el mismo formato que la 16.40 usó para el mes abreviado — la lámina sigue dibujando mono y no se edita. **La reescritura firmada de la 14.29 cierra junto con ésta**: ver `tests/measure/lista.spec.ts`, prueba renombrada a *«el metadato entra con margen en el teléfono, y sobra en el escritorio»*
- [x] 22.10 **CERRADA 2026-09-02 por la 14.53, que es donde el fundador la decidió: la placa se subió a la portada.** `ListingCard` la dibuja dentro de `.cover`, absoluta a 8 px, sobre un piso opaco de `--surface` que es lo que sostiene la garantía de la 14.25 encima de una foto de verdad — probado en Chromium sobre una portada clara (L=0,902) y una oscura (L=0,006) leídas de un `<canvas>`, con separación de luminancia 0,986. **La 14.43 NO se cierra con ésta**: es la ficha y es otra superficie —la placa se muda al encabezado de 56 px, no encima de una foto—, y darla por hecha mirando ésta es la deuda de numeración que la 14.24b ya vino a pagar una vez. **La placa de publicador va ENCIMA de la foto en las dos láminas, y el código la pone en el cuerpo.** Verificado: 6c dibuja `position:absolute;left:8px;top:8px` dentro del recuadro de la portada y 7c `left:9px;top:9px`; `ListingCard.tsx:66` la dibuja como primer hijo de `.body`. **No es un defecto de la placa** —`design-contract.test.tsx` fija su construcción, relleno contra borde, y nunca dónde se dibuja—, es composición de la tarjeta. Hermana de la 14.43, que es el mismo problema en la ficha: la placa se mueve de sitio según el ancho
- [x] 22.11 **El control segmentado y el interruptor, que la lámina 7b dibuja y el sistema no nombra.** Los dos están en el panel de filtros de escritorio y ninguno existe como átomo: el **segmentado** (`flex:1; min-height:40px; border-radius: var(--r); border: 1px solid --strong`) para Habitaciones 1/2/3/4+ y Baños, y el **interruptor** (`44×26`, `border-radius:99px`, fondo `--line`, perilla de 20×20) para cada atributo, con su conteo debajo. Hoy el panel dibuja **enlaces**, que es lo que sostiene el piso sin JavaScript de la 14.33 — así que esto no es «cambiar el marcado», es decidir si el aspecto de esos controles se consigue **sin dejar de ser enlaces**. Ojo con el `min-height:40px` de la lámina: el fundador fijó 44 el 2026-08-27 (16.24), así que ahí la lámina ya está corregida por una decisión posterior. Los baños además no tienen conteo detrás (14.45) **DESBLOQUEADA 2026-09-05 — manda la lámina (rama 1 del encabezado de esta fase), y sigue abierta porque queda el trabajo, no la decisión.** El fundador: *«el UI y UX es la lámina»*, y un componente que la lámina dibuja y el sistema no nombra es exactamente el caso que la rama 1 resuelve. **El segmentado y el interruptor de la 7b entran a `SISTEMA.md` como anatomía nueva**, en vez de corregir la lámina por dibujar algo que el sistema todavía no tenía — es la misma dirección con la que la 22.1 registró la cuadrícula. Lo que la regla NO toca, y sigue en pie tal como esta tarea ya lo dejaba escrito: el `min-height:40px` queda corregido a **44** por la decisión del fundador del 2026-08-27 (16.24) —una decisión posterior manda sobre la lámina, que es la línea original de este encabezado—, y el aspecto tiene que conseguirse **sin dejar de ser enlaces**, que es el piso sin JavaScript de la 14.33

  **CONSTRUIDA, rebanada C.** La decisión ya estaba tomada por la rama 1 del encabezado de la fase: el aspecto se consigue sin dejar de ser enlaces. Lo que faltaba era el trabajo. Dos átomos nuevos, ninguno existía: `components/atoms/SegmentedControl.tsx` (+`.module.css`, +test) dibuja habitaciones y baños como una tira de `<a>` de ancho igual —`grid-auto-columns: 1fr`, la misma razón por la que ya era grid y no un número de columnas escrito a mano— con borde `1px solid var(--strong)` (corregido desde `--line`, que es lo que esta hoja declaraba antes de leer la lámina) y `min-block-size: var(--target-min)` (44, la 16.24). Una opción deshabilitada se dibuja `<span aria-disabled="true">`, la misma regla que el resto del panel. `components/atoms/Switch.tsx` (+`.module.css`, +test) es el interruptor: un `<span aria-hidden="true">` puramente decorativo —44×26 con perilla de 20×20, radio `var(--rs)` y no un literal, el mismo token de pastilla que `PhotoCounter`/`SearchPill`— que vive DENTRO del enlace de cada atributo (`AttributeOptionItem`, `SearchPanel.tsx`) y no lo reemplaza: el enlace sigue siendo el que decide y lleva el foco, y lo elegido sigue viajando en `aria-current`, nunca en el interruptor. Apagado/encendido se distingue por posición de la perilla y relleno, nunca sólo por color. El prefijo «✓ » que dibujaba el atributo elegido se retira: la perilla lo reemplaza, y el conteo («9 de 16») pasa a su propia línea debajo, tal como pide el enunciado.

  **`SearchPanel.tsx`/`.module.css` dejan de declarar su propia copia.** `RoomsStep` arma `model.rooms`/`model.bathrooms` en la forma que el átomo pide y le entrega el dibujo; `.steps`/`.roomOption`/`.stepNumber` se retiran de `SearchPanel.module.css` porque su anatomía se mudó. `AttributesStep` gana `.attribute`/`.attributeHead` —distintas de `.option`, que Quién publica sigue usando en una sola fila sin que esta tarea la toque— para que el interruptor y el conteo se apilen en vez de ir en fila. `SISTEMA.md` registra las dos anatomías nuevas en la sección de Resultados de búsqueda, con la corrección de 40 a 44 dicha explícita.

  **Los baños ya tienen conteo detrás.** La mención de arriba es residuo de antes de que la 14.45 cerrara (rebanada B, 2026-09-04): `model.bathrooms` llega con `count` real desde `FacetedSearchPort`, y `SegmentedControl` lo dibuja igual que el de habitaciones.

  **Probado por comportamiento, no por el texto de la hoja** (evita la trampa 1 de este archivo): `SegmentedControl.test.tsx` (6/6) afirma sobre marcado renderizado —un enlace por opción y no un `<input>`/`<button>`, `aria-current` en la elegida, un `<span aria-disabled>` en la deshabilitada, ningún `aria-pressed`— y sobre el bloque `.segment` extraído del CSS real, no sobre una subcadena cualquiera del archivo. `Switch.test.tsx` (4/4) afirma `aria-hidden`, la ausencia de `role="switch"`/`<button>`/`<input>`, que `data-on` sólo aparece encendido, y los dos bloques `.track`/`.knob` extraídos del CSS real. `SearchPanel.test.tsx` (33/33) sigue en verde sin tocarse: ninguna de sus aserciones dependía de las clases que se retiraron. Evidencia completa: `pnpm test` 2897/2897, `pnpm typecheck` limpio, `pnpm lint` 0 errores, `pnpm lint:tokens` 0 violaciones (268 hojas — el radio del interruptor lee `var(--rs)`, nunca `99px`/`50%` literales), `pnpm test:integration` 394/394, `pnpm test:measure -- --workers=2` 71/71 sin nueva medición dedicada porque ninguna cota de píxeles cambió (el alto ya era 44, el borde no se mide en ningún `.spec.ts`)
- [x] 22.12 **La ficha de selección como átomo, que la 22.5 dejó a medio camino.** La 22.5 unificó el **idioma** y no el componente: `home.module.css` y `mis-avisos.module.css` siguen declarando cada una su pastilla, con tipografías distintas (`--meta-fs-sm` contra `--control-fs`) y alturas mínimas distintas (`--target-min-desktop` contra `--target-min`, que hoy valen lo mismo). Es la misma forma de deuda que la 22.3 acaba de cerrar para el metadato, un escalón más arriba: dos declaraciones coherentes consigo mismas que ya empezaron a separarse
- [x] 22.13 **`--ft` y `--tclamp` quedaron sin un solo uso**, y es la tercera instancia de la 14.48 (`--searchbar-*`) y la 16.37 (`--fpb`). Los destapó la 22.4 al mudar el título de `ResultRow` al átomo: `rg 'var\(--ft\)|var\(--tclamp\)'` sobre `components/`, `app/` y `src/` vuelve vacío. **La causa común ya estaba resuelta cuando esta tarea se retomó**: la 14.48 (commit `8d510b6`, 2026-09-02) extendió `lint:tokens` para que cuente usos —no sólo que un valor SEA una propiedad personalizada y que los dos temas la declaren—, con prueba propia en `scripts/lint-tokens.test.ts` (roja sin la regla: *«rechaza un token declarado que ninguna hoja lee»*). Esa misma corrida dejó `--ft`/`--tclamp` con una excepción NOMBRADA en `UNUSED_TOKENS_ALLOWED` en vez de retirarlos. **Decisión del fundador, 2026-09-04: se retiran del subconjunto que ship*a*, la misma salida que la 16.37 tomó para `--fpb`, y no la excepción.** El sistema los conserva —siguen en `design/reference/sistema/tokens.css` para las cuatro estructuras y `SISTEMA.md` los nombra «Título de aviso (lista)»— pero `src/styles/tokens.css` ya no los declara, con la nota de por qué faltan en su lugar (mismo patrón que la nota de `--fpb`). `UNUSED_TOKENS_ALLOWED` queda vacío: una excepción para un token que ya no se declara sería un perdón sin motivo vivo. `SISTEMA.md` lleva su fila en «Lo que este documento decía y por qué cambió», con el mismo formato que la fila de `--fpb`. **No lleva prueba nueva de comportamiento** (mismo criterio que la 16.37): retirar una declaración que nadie lee no cambia ningún píxel; la evidencia es `pnpm lint:tokens` en 0 antes y después (con la excepción, y sin ella y sin la declaración) más `scripts/lint-tokens.test.ts` (3/3) y `components/design-contract.test.tsx` (56/56) en verde
- [x] 22.14 **«Recientes ▾» sigue sin construirse, y ahora está en el sistema.** Es la **14.47** y no se renumera — se nombra acá sólo porque la 22.1 la agregó a `SISTEMA.md` como parte de la anatomía de la pantalla de resultados, y un lector que abra el sistema va a ver dibujado un control que el producto no tiene

  **CERRADA sin código, rebanada C: el enunciado mismo ya estaba desactualizado.** Verificado contra el código, no contra el enunciado: `components/molecules/OrderMenu.tsx` está construido desde la 14.47 (2026-09-03) y cableado en las dos pantallas reales — `app/alquiler/[ciudad]/page.tsx:342` y `app/alquiler/[ciudad]/[zona]/page.tsx:371`, los dos con `<OrderMenu model={buildOrderMenu(...)} />` —, con su prueba propia (`OrderMenu.test.tsx`), la del dominio (`search-order.test.ts`) y la de punta a punta con el script apagado (`tests/e2e/camino-de-lectura-sin-javascript.spec.ts`). «Sigue sin construirse» era cierto cuando la 22.1 escribió la línea de `SISTEMA.md`, y dejó de serlo el 2026-09-03 sin que nadie volviera a corregir el documento — la 22.1 se completó ANTES de que la 14.47 cerrara, y ésa es la causa, no una excepción a la 22.1. Lo que esta tarea cierra es exactamente eso: `SISTEMA.md`, «Orden de la lista», deja de decir «No está construido» y pasa a describir las tres opciones reales, el componente que las dibuja y por qué «Recientes» viaja como ausencia del parámetro (indexación). **No hace falta código nuevo**: el control ya existe, ya está servido, y lo único que mentía era la documentación. Evidencia: `rg buildOrderMenu` sobre `app/` confirma el cableado en las dos rutas; `pnpm test` 2897/2897 sin un solo archivo tocado fuera de `SISTEMA.md`

- [x] 22.15 **`/mis-avisos` viste la miniatura de una fila que ya no existe, y no tiene escritorio.** Dos defectos en la misma hoja, verificados los dos. **Uno**: `app/mis-avisos/mis-avisos.module.css:87,108,109` dimensiona la miniatura con `var(--tw)`/`var(--th)` — 44 × 34, que son los tokens de la fila de resultados que la cuadrícula reemplazó en la 14.25 — mientras los artboards 14c y 14d de «Cuenta e Importar» la dibujan **74 × 56** (`width:74px;height:56px`, ocho veces en la lámina). **Dos**: esa hoja contiene **cero reglas `@media`** —es la única de las once hojas de `app/` que no tiene ninguna; las otras diez sí—, así que `/mis-avisos` **no tiene disposición de escritorio en absoluto** aunque la lámina 14d dibuje la fila dentro del contenedor de 1100px que `SISTEMA.md` fija. ~~Los dos son cambios visibles que ninguna tarea pidió, así que van a decisión del fundador y no se cambian de oficio.~~ **Este hallazgo se había atribuido por error a la 22.11, que es el control segmentado y el interruptor de la lámina 7b — otra cosa**; queda con número propio para que no se dé por cerrado mirando aquélla, que es el defecto que la 20.13 ya corrigió una vez para la 14.22/14.23. Emparenta con la contradicción abierta que `SISTEMA.md` ya registra en «La miniatura de 44 × 34, y por qué sigue declarada», que no cubre la mitad del `@media` **DESBLOQUEADA 2026-09-05 — manda la lámina en las DOS mitades (rama 1 del encabezado de esta fase), y sigue abierta porque queda el trabajo, no la decisión.** El fundador: *«el UI y UX es la lámina»*. **Uno**: la miniatura pasa a **74 × 56**, que es lo que dibujan los artboards 14c y 14d, y deja de vestirse con `var(--tw)`/`var(--th)` —los tokens de la fila que la cuadrícula reemplazó en la 14.25—; `SISTEMA.md` actualiza en consecuencia su contradicción abierta «La miniatura de 44 × 34, y por qué sigue declarada», que es la dirección de la rama 1: el sistema se corrige, no la lámina. **Dos, y también queda decidida en vez de quedar colgando**: esos mismos artboards **14c y 14d dibujan 360 y 1280**, así que sí hay lámina de escritorio para `/mis-avisos` y por lo tanto manda. Que la hoja tenga hoy **cero reglas `@media`** —única entre las once de `app/`— deja de ser una pregunta abierta y pasa a ser trabajo pendiente: la fila va dentro del contenedor de 1100px que `SISTEMA.md` fija

- [x] 22.16 **El `notFound()` de una ruta dinámica queda en blanco con el script apagado: lo dibuja Next del lado del cliente.** Medido el 2026-08-28 al cerrar la 11b.3, contra la compilación de producción. `/alquiler/maracaibo/tierra-negra/apartamento-que-nunca-existio` contesta **404** y su cuerpo servido son **234 bytes** —`<div hidden><!--$?--><template id="B:0">` más un `<div hidden id="S:0">` VACÍO—, con la pantalla entera únicamente dentro de la carga de Flight. La misma pantalla pedida por una dirección que no coincide con ninguna ruta (`/una-direccion-que-nadie-publico`) sale **entera en el HTML**, 526 bytes con su `<h1>`, y ésa es la que la prueba con el script apagado camina hoy. Pasa igual en los tres `notFound()` dinámicos medidos: ciudad, zona y ficha. **No lo causó `app/not-found.tsx`**: antes era la pantalla por defecto de Next por exactamente el mismo camino, así que es un agujero que la 11b.3 destapó y no uno que abrió. **Choca de frente con `AGENTS.md` §2** —el camino de lectura funciona con JavaScript apagado— y con el criterio del fundador de que ninguna pantalla termina en un vacío sin salida; y el enlace mal copiado llega casi siempre desde el navegador de WhatsApp, que es donde el script no llega. Next 15.5.23. **Candidatos de arreglo, escritos para que sea decisión y no descubrimiento**: que la ficha resuelva el 404 antes de suspender, o que la ruta redirija a una dirección de 404 que sí se pre-renderiza — las dos cambian el comportamiento de la ficha y por eso no se hicieron dentro de la 11b.3. Reproducción: `pnpm build && pnpm start`, y `curl` a las dos direcciones de arriba comparando el cuerpo sin `<script>`. **DECIDIDO POR EL FUNDADOR el 2026-09-07: se arregla en el middleware, y NO se toca la ficha — ni ninguno de los otros dos `notFound()` dinámicos (ciudad, zona), que sólo se resuelven con una consulta y por eso quedan fuera de este arreglo.** De los cuatro `notFound()` que la ficha puede lanzar, sólo uno es decidible SIN Postgres: el slug MALFORMADO, donde `listingIdFromSlug` no encuentra un id al final del segmento — eso es una regex pura, y es justo el caso del `apartamento-que-nunca-existio` medido arriba. Los otros tres —id con forma válida pero sin fila, ciudad inexistente, zona inexistente— necesitan una consulta y ese trabajo no puede vivir en el Edge; quedan exactamente como estaban. `middleware.ts` (`matcher` ya cubre `/alquiler/:ciudad/:zona/:slug` y su variante `/foto/:n`) reconoce el slug malformado ANTES de que la ruta dinámica se ejecute y hace `NextResponse.rewrite` con `status: 404` hacia `/slug-de-aviso-malformado` — una dirección que **no existe como página en `app/`**, así que Next la resuelve por el mismo camino ya medido y funcional del 404 genérico: prerenderizado en build, sin límite de Suspense, con el `<meta name="robots" content="noindex, follow">` que `app/not-found.tsx` ya trae. No es una superficie indexable nueva: nada la enlaza, no tiene `page.tsx` propio y por eso nunca entra a `app/sitemap.ts`. Verificado que `listingIdFromSlug` es segura para el runtime Edge donde corre este archivo: es una función pura sobre `string` (una expresión regular y `.toLowerCase()`), sin I/O y sin ningún import transitivo de una API de Node. **Medido con el mismo arnés de arriba, antes y después**: antes del arreglo, `/alquiler/maracaibo/tierra-negra/apartamento-que-nunca-existio` seguía siendo **404, 14.236 bytes, sin `<h1>`**; después, **404, 19.931 bytes, con `<h1>No encontramos esa página</h1>`** — el mismo tamaño que ya servía la dirección de control, `/una-direccion-que-nadie-publico` (19.889 bytes). El visor de fotos con el mismo slug malformado (`/foto/1`) mide igual: 404 con el `<h1>` servido. Las rutas con un id sintácticamente válido, y las tres causas que necesitan consulta, no cambiaron: se comprobó contra el mismo arnés que una dirección con id nunca entra al rewrite y que una ciudad inexistente sigue devolviendo 500 contra la `DATABASE_URL` inalcanzable del arnés (nunca 404), que es el límite ya conocido de reproducir sin Postgres real y no una regresión de este arreglo. **Pruebas nombradas**: `middleware.test.ts` → *«rewrites a malformed listing slug to the pre-rendered 404, keeping the 404 status»* y *«does not rewrite a listing slug that carries a syntactically valid id»*, sobre la DECISIÓN del rewrite y no sobre lo que un navegador recibe; y — siguiendo la convención que la 22.31 registra abajo — `tests/e2e/aviso-malformado-sin-javascript.spec.ts` → *«la ficha con un slug sin id responde 404 y sirve la pantalla completa»* y *«el visor de fotos con el mismo slug también responde 404 servido entero»*, sobre el **HTML servido de verdad** contra la compilación de producción, en los dos proyectos de Playwright (`chromium` y `crawlability`) y **sin necesitar el arnés de la 11.22**: el slug malformado se rechaza antes de cualquier consulta, así que corre igual sin `TEST_DATABASE_URL` — se corrió localmente, roja contra el `middleware.ts` de antes (el `<h1>` no aparecía) y verde contra el de después, las dos veces contra el build real. Los dos casos que sí necesitan el arnés —ciudad inexistente, zona inexistente— siguen sin poder medirse con el script apagado, y siguen fuera de alcance a propósito

- [x] 22.18 **`safeSignInDestination` se quedó sin llamador.** Era el único guardián del `signInHref` que la acción de revelar recibía, y la 15.8 cambió ese campo por `doorHref`, que juzga `safeReturnPath`. Sigue exportada y con sus pruebas, sin nadie que la use. No se borró dentro de la 15.8 por el techo de revisión; la 15.7 vuelve a tocar `/signin?callbackUrl=…` y es ahí donde se decide si revive o se va **Decidido en la 15.7: se va.** La pantalla nueva recibe el `callbackUrl` pelado y lo juzga `safeSignInReturn`, así que nadie vuelve a necesitar el guardián de la dirección `/signin?callbackUrl=…` entera. No se borró adentro de la 15.7 por el techo de revisión —son 23 líneas de fuente y 80 de prueba, que es un quinto del presupuesto gastado en un borrado— y queda como tarea propia con la decisión ya tomada. **Hecho**: la función, su import y sus 12 pruebas se borraron de `identity/domain/safe-return-destination.ts`/`.test.ts`; `safeReturnPath` y `safeSignInReturn` quedan como las dos reglas vivas, y ningún archivo del repositorio vuelve a nombrarla
- [x] 22.19 **Cerrada — decisión del fundador del 2026-09-07: no se pide el mensaje hasta que entre.** El formulario de revelar exigía un mensaje (6.11-6.13) aunque no hubiera sesión; se escribía, se perdía en la vuelta desde la puerta y se pedía de nuevo. Guardarlo obligaba a decidir dónde, y la dirección es justo el sitio que publica texto ajeno en el historial y en los registros. La salida elegida no guarda nada en ningún lado: **sin sesión, `ContactBlock` no dibuja el campo**, sólo un enlace nativo (`ActionLink`) a la misma ficha con la puerta abierta; quien entra vuelve y RECIÉN entonces ve el formulario con el campo vacío, todavía antes de ver el contacto — 6.11-6.13 no se tocan. `ContactBlock.tsx` recibe un booleano nuevo, `hasSession`, que decide entre las dos ramas; lo manda `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` desde la MISMA lectura de sesión que ya usaba la barra (sin consulta nueva), y `app/measure/page.tsx` fija `hasSession={true}` porque mide la geometría del formulario, que ya tiene su propia puerta montada aparte. **Pruebas nombradas**, `ContactBlock.test.tsx`: «no dibuja el campo de mensaje ni el formulario que lo pediría», «ofrece un enlace directo a la puerta sobre esta misma ficha», «sigue diciendo qué falta y por qué, igual que con sesión», «nombra la acción con el mismo rótulo que el formulario usaría», «con sesión, el bloque no cambia: sigue pidiendo el mensaje». Nada del mensaje viaja por la URL, por una cookie ni por la sesión: la persona simplemente no lo escribe todavía
- [x] 22.20 **La lámina dibuja la marca de Google y el sistema no la admite.** Las cuatro puertas (8a, 8b, 9a, 9b) ponen el disco de cuatro colores con un `conic-gradient` y el botón blanco con borde, que es lo que las reglas de marca de Google piden. `SISTEMA.md` cierra los glifos en caracteres de texto **más exactamente dos SVG en línea**, y dice que un tercero es un cambio del sistema y no un uso de él. La 15.8 shipeó el botón **sin marca y en el nivel 1 (relleno `--accent`)**, porque sin el logo un botón blanco con borde se lee como «Cancelar» en la jerarquía de tres niveles. ~~Es decisión del fundador: o entra una excepción de marca al sistema, o la lámina se corrige~~ **DESBLOQUEADA 2026-09-05 — manda la lámina (rama 1 del encabezado de esta fase), y sigue abierta porque queda el trabajo, no la decisión.** El fundador: *«el UI y UX es la lámina»*. De las dos salidas que esta tarea nombraba se toma la primera: **entra la excepción de marca al sistema** y las cuatro puertas se quedan como están. `SISTEMA.md` admite el disco de Google como tercer SVG en línea y lo nombra **como excepción, con su razón escrita** —las reglas de marca de Google no admiten una versión propia, así que acá el sistema no puede derivar nada—, con la misma forma con la que la 22.1 registra lo que cambió y por qué. Con la marca puesta, el botón blanco con borde deja de leerse como «Cancelar» y el argumento con el que la 15.8 lo shipeó en nivel 1 se cae solo: lo que falta es el SVG, su entrada al sistema y ese cambio de nivel. **Hecho**: `GoogleMark` en `components/atoms/icons.tsx` (los cuatro `fill` literales, no `currentColor`), montado en `app/(auth)/signin/page.tsx` y en `SignInDoor.tsx` dentro de `NeutralButton` (nivel 3, no `ActionButton`); `SISTEMA.md` documenta la excepción en la tabla de Assets y en la sección 8
- [x] 22.21 **Las dos láminas de la puerta escriben la misma frase distinta.** La 8b (móvil) dice *«Es gratis y es un toque»* y la 9b (escritorio) *«Es gratis y es un clic»*. La copia sale del dominio y no puede cambiar con el ancho sin duplicarse, así que se shipeó «un toque», que es además la que `ContactBlock` ya usaba. Una de las dos láminas hay que corregirla **CERRADA 2026-09-05 SIN DEFECTO — no hay lámina que corregir.** Ruling del fundador de ese día, ramas **4** y **0** del encabezado de esta fase: una diferencia de redacción entre la lámina de móvil y la de escritorio **no es un defecto** —*«yo no estoy desarrollando la aplicación móvil, estoy desarrollando una aplicación web que funciona en el móvil y en un desktop»*, y un toque en el teléfono es un clic en el navegador—, y por encima de eso el texto ya shipeado y decidido en una tarea anterior gana. Queda «un toque», que es lo que `ContactBlock` ya usaba, y **no se edita ninguna de las dos láminas**. La frase «Una de las dos láminas hay que corregirla» queda sin efecto
- [x] 22.22 **La puerta de entrar todavía no pide el enlace por correo.** Las láminas 8a y 9a dibujan, debajo del botón de Google, el separador *«o con tu correo»*, el campo `Correo` y el botón «Enviarme el enlace»/«Enviar enlace». **Hecho, junto con la 15.9 como decía esta misma tarea, y sin una línea de cliente.** La copia sale de `signInPageFor` (`email: SignInEmailDoor`), **igual para las cuatro puertas**: entrar por correo no cambia porque se venga de un aviso o de publicar. El campo es un `<input type="email" required>` con `<label for>` de verdad —no un `placeholder`, que se borra al escribir— y su formulario es un POST nativo con Server Action (`requestMagicLink`). El destino cruza los dos formularios en un campo oculto y lo vuelve a juzgar `safeSignInReturn`: no se agregó una segunda regla. **Pruebas nombradas**: la copia en `sign-in-page.test.ts` → *«debajo de Google pide el enlace por correo, con la misma copia en las cuatro puertas»*; **la instalación** en `app/(auth)/signin/entrar-servida.test.tsx` → *«debajo de Google dibuja el separador, el campo de correo y su botón»* y *«el formulario del correo se lleva el destino en un campo oculto»*; **el envío** en `actions.test.ts` → *«manda el correo, guarda el comprobante y lleva a la pantalla de espera»*, *«con una dirección … no manda nada y devuelve a la puerta»* y *«un destino que la regla no admite no viaja adentro del enlace»*; **sin script** en `tests/e2e/entrar-sin-javascript.spec.ts` → *«la puerta pide el enlace por correo con un campo y una etiqueta de verdad»*. **El seam de Auth.js es `pages.verifyRequest`**, no un redirect: `@auth/core` manda a `/api/auth/verify-request` después de despachar el correo **venga de donde venga la petición**, así que apuntarlo a nuestra pantalla es lo único que vuelve inalcanzable la página en inglés de la librería. Se afirma sobre la configuración que `auth.ts` entrega, en `redirect-callback.test.ts` → *«apunta `pages.verifyRequest` a la pantalla de espera y no a la de Auth.js»* — un `toContain` sobre el texto del archivo quedaría verde con la línea en un comentario. **Mutaciones**: quitar `required` pone roja 1; quitar el campo oculto del destino, 1; quitar la línea de `pages`, exactamente la prueba que afirma la instalación. **Correcciones registradas**: (a) la dirección se normaliza en el dominio **igual que `defaultNormalizer` de Auth.js** y se rechazan la coma y la comilla, que la librería recorta — si no, la pantalla mostraría una dirección y el correo iría a otra; (b) el alto del campo sale de `--target-min` (44) y no de los 50 de la 8a, misma resolución que la 22.25; (c) el botón dice «Enviarme el enlace» y las dos láminas escriben tres cosas distintas — ver 22.27; (d) **no se dibujó el campo en la hoja/diálogo (8b/9b)**, que esta tarea no nombra — ver 22.28
- [x] 22.23 **Una prueba de la 15.8 promete en su nombre algo que su cuerpo no afirma.** `ficha-servida.test.tsx` → *«con el token abierto sale entera en el HTML, sin un solo script»* nunca comprueba la ausencia de `<script>`: afirma tres cadenas y la ausencia del teléfono. Es la trampa 1 de este archivo movida al título. Y la comprobación que promete **no se puede hacer ahí**: medido al construir la 15.7, `renderToStaticMarkup` de un formulario con Server Action inyecta el `<script>` de reenvío de React, porque fuera del compilador de Next la acción es una función común — o sea que la afirmación literal habría sido roja por el arnés y no por la pantalla. El lugar correcto es el proyecto `crawlability`, como en `tests/e2e/entrar-sin-javascript.spec.ts`. Falta renombrar la prueba de la ficha y darle su medición con el script apagado. **HECHA el 2026-09-06**: la prueba de `ficha-servida.test.tsx` se renombró a lo que su cuerpo afirma de verdad (título, motivo, promesa de vuelta, ausencia del teléfono). La medición del script apagado quedó en `tests/e2e/puerta-de-whatsapp-sin-javascript.spec.ts` (proyecto `crawlability`), siguiendo el patrón de `entrar-sin-javascript.spec.ts`: **no** se afirma "cero `<script>` en el documento" —la ficha ya sirve su propio JSON-LD siempre (11.14), así que esa afirmación literal sería falsa hasta con la puerta cerrada—; se afirma lo que sí puede ser cierto y lo que un navegador sin script necesita: los dos formularios de la puerta tienen un `action` real (no la reescritura `javascript:` que produce `renderToStaticMarkup` fuera de Next) y `method="post"` nativo. **No se pudo correr `pnpm test:e2e` en esta máquina** — el build falla en cierre sin `SITE_URL` y el job de CI necesita un Postgres sembrado más el proxy local de pg —, así que el archivo se validó con `playwright test --list` (parsea y lista en los dos proyectos, `chromium` y `crawlability`) y con `pnpm typecheck`; su primera corrida real será en CI
- [x] 22.24 **La línea legal nombra dos pantallas que no existen.** Las cuatro láminas de entrar escriben *«Al entrar aceptás los términos y la privacidad»* con las dos palabras enlazadas. No hay ruta `/terminos` ni `/privacidad` en `app/`, así que la 15.7 shipeó la frase **sin enlaces**: mandar a un 404 desde la pantalla que pide una cuenta es peor que decirlo sin enlazar. O se construyen las dos pantallas, o la lámina deja de prometer el enlace **DESBLOQUEADA 2026-09-05, y NO por una decisión: por la Fase 23, que construyó las dos pantallas.** De las dos salidas que esta tarea nombra, la primera —*«o se construyen las dos pantallas»*— está **cumplida**: `/legal/terminos` y `/legal/privacidad` existen (PR #238, mergeado el 2026-09-05, tarea 23.5) y las dos son **indexables** —ninguna lleva `noindex`, decidido en la 23.9 y verificado ahí—. Ya no hay 404 al que mandar a nadie, así que el argumento con el que la 15.7 shipeó la frase sin enlaces se cae, y **la lámina no tiene que dejar de prometer nada**. Sigue abierta por el trabajo —enlazar las dos palabras de la línea legal— y no por una decisión. **Las rutas viven bajo `/legal/` y eso no es un problema**: la lámina promete un enlace, no una dirección; `/terminos` y `/privacidad` a secas nunca fueron el requisito. Ojo con el alcance real: las cinco Legales están publicadas **como borrador con su banner visible** hasta que el fundador las ratifique (23.5, todavía abierta), y eso condiciona cuándo enlazarlas, no si se puede. **Hecho**: `LEGAL` en `identity/domain/sign-in-page.ts` pasó de `string` a `readonly LegalFragment[]` — un arreglo de fragmentos de texto y de enlace, con el marcado decidido en el dominio y no en `app/`; «términos» enlaza a `/legal/terminos` y «privacidad» a `/legal/privacidad`, y `app/(auth)/signin/page.tsx` sólo recorre la lista
- [x] 22.25 **Las láminas de entrar dibujan un encabezado y una barra que ningún otro tamaño del producto usa.** La 8a pone el `<h1>` en 22 px y la barra en 56; la 9a, 28 y 72. Todas las pantallas que shipean —inicio, ciudad, zona, publicar, mis avisos, importar— dibujan su `<h1>` con `--title-fs` (20) a los dos anchos, y la barra con `--nav-h`/`--nav-h-desktop` (60/68). La 15.7 usó los tokens del sistema y no los números de la lámina, porque un tamaño de encabezado propio de una pantalla es exactamente la deriva que `SISTEMA.md` prohíbe. ~~Falta decidir si el sistema sube su escala de título o si estas dos láminas se corrigen~~ **DESBLOQUEADA 2026-09-05 — manda la lámina (rama 1 del encabezado de esta fase), y sigue abierta porque queda el trabajo, no la decisión.** El fundador: *«el UI y UX es la lámina»*, y un tamaño de encabezado y un alto de barra son UI/UX. De las dos salidas que esta tarea nombraba se toma la primera: **la 8a y la 9a se quedan como están** —`<h1>` de 22 y 28, barra de 56 y 72— y **`SISTEMA.md` se actualiza para admitirlas**. Lo que queda por resolver es de alcance y no de autoridad, y hay que decidirlo con el sistema delante: mover `--title-fs` y `--nav-h`/`--nav-h-desktop` cambia **todas** las pantallas que ya shipean —inicio, ciudad, zona, publicar, mis avisos, importar—, así que si lo que las cuatro puertas piden es un papel propio de la puerta y no una escala nueva para el producto entero, el sistema tiene que **declararlo como papel propio**; es lo único que actualiza el sistema sin abrir la deriva que él mismo prohíbe. Precedente de la misma forma en la 22.22, corrección (b): el alto del campo salió de `--target-min` (44) y no de los 50 de la 8a. **Hecho**: `--door-title-fs`/`--door-title-fs-desktop` (22/28) y `--door-bar-h`/`--door-bar-h-desktop` (56/72) en `tokens.css`, como papel propio de esta pantalla; `signin.module.css` los usa en `.title` y `.barInner` en vez de `--title-fs`/`--nav-h`, verificado por `signin-door-role.test.ts`; `SISTEMA.md` §8 documenta la excepción
- [x] 22.26 **Los tres pasos de publicar están escritos distinto en las dos láminas, y una de las dos versiones es falsa.** La 8a dice *«Subís las fotos, que comprimimos en tu teléfono»* y la 9a *«…en tu navegador antes de mandarlas»*; el paso 1 también difiere («los datos de la propiedad» contra «…: zona, precio, habitaciones»). Es la misma clase de contradicción que la 22.21 y se resolvió con una regla dicha en voz alta: **se toma la redacción que sigue siendo cierta en los dos anchos**, o sea la de la 9a, porque «en tu teléfono» es falso en una computadora. Falta corregir la lámina móvil **CERRADA 2026-09-05 SIN DEFECTO — la lámina móvil no se corrige.** Ruling del fundador de ese día, ramas **4** y **0** del encabezado de esta fase: lo que separa a la 8a de la 9a es redacción entre dos anchos y eso **no es un defecto** —en el móvil también hay un navegador—, y el texto shipeado, el de la 9a («en tu navegador antes de mandarlas», el único cierto a los dos anchos), ya estaba decidido en esta misma tarea. La frase «Falta corregir la lámina móvil» queda sin efecto: **no se edita ninguna lámina** y la divergencia se registra, exactamente como la 16.40
- [x] 22.27 **Las dos láminas de entrar escriben tres frases distintas, y ninguna regla anterior elige entre ellas.** Tres pares: el botón del correo es *«Enviarme el enlace»* (8a), *«Enviar enlace»* (9a) y *«Enviar»* (8b/9b); la línea principal de la espera es *«Tocalo y entrás sin escribir nada más»* (8c) contra *«Hacé clic y entrás…»* (9c); y la salida de la barra es *«← Cambiar correo»* (8c) contra *«← Cambiar de correo»* (9c). La regla que la 22.26 dejó dicha —**se toma la redacción que sigue siendo cierta en los dos anchos**— resuelve el segundo par en contra de las dos: «tocalo» es falso en una computadora y «hacé clic» en un teléfono. Se shipeó **«Abrilo y entrás sin escribir nada más»**, que es el mismo verbo neutro para los dos, y queda registrado como corrección y no como invención. Los otros dos pares son ciertos a los dos anchos, así que la regla no elige: se shipeó **«Enviarme el enlace»** (dice qué se recibe, no sólo qué se aprieta) y **«← Cambiar de correo»**. Es la misma clase de contradicción que la 22.21 y la 22.26: hay que corregir una lámina de cada par **CERRADA 2026-09-05 SIN DEFECTO — no se corrige ninguna lámina de ningún par.** Ruling del fundador de ese día, ramas **4** y **0** del encabezado de esta fase: los tres pares son diferencias de redacción entre la lámina de móvil y la de escritorio y eso **no es un defecto** —*«en el móvil también hay un navegador»*—, y lo ya shipeado y decidido gana. Quedan las tres como están: **«Enviarme el enlace»**, **«Abrilo y entrás sin escribir nada más»** —la del segundo par, que ya se había resuelto en contra de los DOS dibujos y por eso es la que la rama 0 protege— y **«← Cambiar de correo»**. La frase «hay que corregir una lámina de cada par» queda sin efecto: **no se edita ninguna lámina**, y las tres divergencias se registran
- [x] 22.28 **La hoja y el diálogo de la ficha siguen sin campo de correo.** Las láminas 8b y 9b lo dibujan igual que 8a/9a —separador, campo y un botón «Enviar» corto—, y la 22.22 sólo nombraba 8a y 9a, así que `components/organisms/SignInDoor.tsx` sigue ofreciendo únicamente Google. La infraestructura entera ya está (15.3, 22.22): lo que falta es el marcado y su medición. **Hoy no es un muro** —la hoja tiene su × y su «Seguir mirando sin entrar»— pero sí es una puerta con la mitad de las opciones que la de al lado. **Hecho**: `ContactDoorCopy` gana el campo `email` (`ContactEmailDoor`, misma forma que `SignInEmailDoor` de la puerta de página, pineados por valor); `SignInDoor.tsx` dibuja el separador, el campo y el botón, reutilizando `requestMagicLink` de `app/(auth)/signin/actions.ts` —la misma acción que ya sirve la puerta de página, sin una segunda copia—; verificado en `SignInDoor.test.tsx`
- [x] 22.29 **Cerrada — decisión del fundador del 2026-09-07: se deriva del sistema.** Ninguna de las cuatro láminas dibuja qué pasa cuando el servidor rechaza la dirección —una coma, una comilla, un dominio sin punto—, y la premisa original de esta tarea (que dibujarlo era lo que §2 prohíbe) quedó desactualizada por la rama 3 del encabezado de esta fase, que nombra la 22.29 explícita: donde ninguna lámina lo dibuja, gobierna `SISTEMA.md`, con la 11b.2 de precedente. `SISTEMA.md` línea 225 ya declara el idioma verbatim: «Campo en error: borde de 2px `--err` y mensaje propio debajo, además del texto de ayuda neutro». Eso es lo que se aplicó, sin inventar nada más. **Mecánica**: `requestMagicLink` (`app/(auth)/signin/actions.ts`) ya distinguía las dos negativas; a la del servidor (`reason: "sin-direccion"`) se le agregó una bandera en la vuelta —`signInPathFor(returnTo, { emailRejected: true })`, nuevo segundo parámetro construido con `URLSearchParams` y no a mano, la misma advertencia que ya deja escrita `doorHrefFor`—. La bandera es un booleano puro (`?correo=invalido`) y NUNCA la dirección tecleada: `signInPageFor` la recibe y sólo elige la frase (`emailError`, dominio en `sign-in-page.ts`), nunca la guarda ni la repite. `app/(auth)/signin/page.tsx` dibuja el borde de 2px `--err` (`.fieldInvalid`), el mensaje propio ANTES del texto de ayuda (`.emailError`, que se queda), y lo anuncia con `aria-invalid`/`aria-describedby` — misma disciplina que `Field.tsx` ya declara para el otro formulario del producto. Sigue funcionando sin script: todo el mecanismo es una redirección de servidor con un parámetro de consulta. **Fuera de alcance a propósito**: el fallo de *envío* (`!despachado(destino)`, cuando Auth.js no llega a mandar el correo) no lleva la bandera — es un fallo de mensajería, no de formato de la dirección, y la tarea habla del segundo. **Pruebas nombradas**: `sign-in-page.test.ts` («sin la bandera, no hay nada que afirmar», «con la bandera, trae el mensaje propio…», «la bandera es la única entrada: la dirección tecleada nunca viaja de vuelta», y las tres de `signInPathFor`); `actions.test.ts` (la vuelta con dirección inválida ahora lleva `&correo=invalido`); `entrar-servida.test.tsx` («dibuja el mensaje propio y conserva el texto de ayuda», «sin la bandera, no dibuja ningún mensaje de error», «anuncia el error al lector de pantalla, no sólo lo dibuja»)
- [x] 22.17 **La prueba que cuida la F19 en la ficha es de las que este archivo llama trampa 1.** `app/alquiler/[ciudad]/[zona]/[slug]/signin-return.test.ts` lee el **texto** de dos archivos y compara subcadenas: `expect(SIGNIN).toContain(emitted)` queda verde con el parámetro adentro de un comentario, y `toContain("/alquiler/${ciudad}/${zona}/${slug}")` queda verde con el `href` en una rama muerta. Su comentario explica bien por qué nació así —lo que falló fue una **relación entre dos archivos**, no un render—, y esa razón ya no aplica: desde la 15.10 el destino lo juzga `safeSignInReturn` y la relación se puede afirmar sobre comportamiento. Se deja fuera de la 15.10 por el techo de revisión, no por desacuerdo. **HECHA el 2026-09-06**: la prueba renderiza las dos pantallas de verdad (`FichaPage` y `SignInPage`) y pasa el `href` que una sirve como entrada de la otra, sin asumir el nombre del parámetro — se extraen genéricamente los `searchParams` del `href` servido. Confirmado que atrapa el defecto original: reintroducido temporalmente `volver=` en vez de `callbackUrl=` en `page.tsx`, la prueba se puso roja, y se revirtió
- [x] 22.30 **(nueva — encontrada cableando la 16.9.)** **Las sugerencias de la ficha no arrastran el origen, y no está decidido si deberían.** `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` llama a `buildListingGrid(suggestions.listings, suggestionCovers, photoBase)` sin cuarto argumento, así que quien llega desde una búsqueda filtrada, abre un aviso y toca uno de los sugeridos **pierde la búsqueda ahí**: la segunda ficha dibuja el respaldo. El `volver` ya está en el alcance de esa función (`returnTo`), así que cuesta un argumento. **No se hizo porque es una decisión de producto y no un olvido**: un aviso sugerido no salió de esa lista de resultados, y prometerle «← Resultados» a quien llegó por la cadena de sugerencias puede ser tan mentiroso como el defecto que la 16.9 corrigió. Hay que elegir entre las dos, no ampliarlo de paso **CERRADA 2026-09-06. DECIDIDO POR EL FUNDADOR: las sugerencias ARRASTRAN el origen.** Entre las dos salidas que esta tarea planteó se eligió la primera, y la razón es que no es el caso de la 16.9: aquélla le prometía resultados a quien **nunca vino de resultados**, y acá la persona sí vino de una búsqueda — perderla en el segundo clic es un costo concreto y frecuente contra una confusión rara. `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` le pasa ahora el cuarto argumento a `buildListingGrid`, el mismo que la ficha ya usaba para su propio enlace. **Y salió una corrección de tipo que no estaba prevista**: el cuarto parámetro declaraba `string` mientras `withResultsOrigin` —a quien se lo reenvía entero— declara `string | readonly string[] | undefined`; estrecharlo obligaba a cada llamador a normalizar lo que el validador de abajo normaliza igual, así que se ensanchó para coincidir con lo que de verdad consume. La garantía se afirma sobre el HTML **servido** y no sobre la llamada: un espía sobre el cuarto argumento quedaría verde aunque el enlace saliera pelado, que es la trampa 1 que la 22.17 acaba de sacar de este mismo directorio.
- [x] 22.31 **(nueva — segunda instancia de la 8.7, encontrada por la 16.9.)** **Un dominio completo, probado y sin llamador es indistinguible de uno que funciona.** `return-to-results.ts` shipeó con sus cuatro funciones, sus pruebas al 100 % y un llamador —`buildListingGrid`— que también tenía su prueba con el origen puesto… y ninguna de las dos pantallas de resultados le pasaba ese origen. La cobertura estaba verde, la 8.7 barrió `application/` buscando casos de uso sin llamador y ésta no era ésa: es un **argumento opcional que nadie pasa**, que la barrida de la 8.7 no podía ver. Decidir si eso se puede detectar mecánicamente (un parámetro exportado que ningún llamador de producción llena) o si la respuesta es que toda regla nueva llegue con su prueba de cuerpo servido, que es lo que la 16.9 terminó usando. **CERRADA 2026-09-07. DECIDIDO POR EL FUNDADOR: la convención, no la detección mecánica. Cero código.** La detección mecánica se descartó por escrito: un argumento opcional legítimamente puede quedar sin llenar —`home-collections` no le pasa un origen a `buildListingGrid` a propósito, porque el inicio no es una búsqueda filtrada—, así que un barredor que marcara todo argumento opcional sin llamador produciría falsos positivos reales, y sería además una herramienta entera con presupuesto propio para un problema que una convención de prueba ya resuelve. La regla que gana: **toda regla nueva llega con su prueba sobre el HTML SERVIDO, y no sobre la llamada.** La razón por la que gana no es que detecte el síntoma — es que hace la causa imposible: una prueba sobre el cuerpo servido no puede quedar verde mientras el argumento nunca viaja, porque lo que afirma es lo que una persona ve, no lo que un espía sobre la llamada reportaría. **Las dos instancias del mismo patrón, la primera de las cuales dio nombre a esta tarea**: (1) `return-to-results.ts`, arriba, shipeó con sus cuatro funciones al 100 % de cobertura y un llamador (`buildListingGrid`) que ninguna pantalla de resultados alimentaba — cobertura verde, cero garantía sobre lo que salía en pantalla; (2) **la 22.30, segunda instancia exacta del mismo patrón**: `buildListingGrid`'s cuarto argumento tampoco lo pasaba la ficha al dibujar sus sugerencias, con la misma forma —función probada, llamador real, argumento opcional que nadie llenaba— hasta que la 16.9 lo encontró cableando la vuelta filtrada. La 22.30 ya cerró afirmando el HTML servido y no la llamada, dejándolo escrito ahí mismo: *"un espía sobre el cuarto argumento quedaría verde aunque el enlace saliera pelado, que es la trampa 1 que la 22.17 acaba de sacar de este mismo directorio"*. **La regla queda escrita donde este repositorio guarda sus reglas no negociables**: `AGENTS.md` §1, junto a TDD estricto y mutation-checking — porque es la misma familia de disciplina (una prueba tiene que poder fallar por la razón correcta) y no una convención de un módulo. Esta misma tarea (22.16) es la primera en seguir la regla recién escrita: su guardián de verdad es `tests/e2e/aviso-malformado-sin-javascript.spec.ts`, sobre el cuerpo servido y no sobre el código fuente de `middleware.ts`
- [x] 22.32 **(nueva — encontrada cerrando la 16.12, y es el bloqueo real de la 15.11.)** **El estado bloqueado necesita saber si el contacto está verificado sin poder saber cuál es.** Las láminas 8b/9b dibujan «teléfono verificado por WhatsApp» **al lado del teléfono tapado**, o sea en el estado en el que el valor no se lee: `ListingDetail` no trae `contactValue` a propósito, y ésa es la mitad de la garantía de la 11.11 —el número no sale de Postgres hacia el render de un anónimo—. Pero `findEvidence` pregunta por el triple `(cuenta, método, valor)`, así que **hoy no hay forma de contestar la pregunta sin leer justo lo que el estado bloqueado existe para no leer**. Las dos salidas y la asimetría entre ellas: **(a)** un puerto de lectura nuevo y angosto al lado del que ya hay —AGENTS.md §3, «cuando necesites leer de una tabla cuyo puerto de escritura es deliberadamente angosto, agregá un puerto de lectura al lado»— que resuelva el `JOIN` en SQL contra `listing.contact_method`/`contact_value` y devuelva un booleano o un instante, **sin que el valor cruce nunca el límite del proceso de render**; **(b)** ampliar `ListingDetail` con el contacto, que es barato de escribir y **tira la garantía**: el valor pasaría a viajar en la carga de todo render anónimo, y esa fuga es silenciosa. La (a) es la que este repositorio ya eligió tres veces; se deja escrito para que la 15.11 no la vuelva a decidir de cero **Cerrada 2026-09-04 — se construyó la (a), y sólo la (a).** `ListingContactVerificationPort.findVerifiedAt(listingId)` (`src/modules/identity/application/ports/verified-contact.port.ts`), implementado por `DrizzleListingContactVerification` (`src/modules/identity/infrastructure/drizzle-verified-contact.ts`): un `INNER JOIN` entre `listing` y `verified_contact` sobre el triple completo —`user_id = publisher_id`, `method = contact_method`, `value = contact_value`— corrido enteramente en SQL, cuyo `select` sólo nombra `verified_at`. El tipo de retorno es `Date | null`, así que la garantía no depende de que nadie recuerde no leer el valor: no hay campo por el que `contact_method`/`contact_value` puedan volver a JavaScript desde este método. `listingContactIsVerified(verifiedAt, now)` (`src/modules/identity/domain/contact-verification.ts`) decide vigencia con el mismo borde de doce meses que `contactVerificationIsLive` ya prueba, con `now` por parámetro y no del sistema — la misma razón por la que la ventana de la 19.11 no vive en el `WHERE` de ningún puerto compartido. **Ninguna capa de render se tocó**: esto es sólo el puerto y su adaptador, para que la 15.11 (o quien retome el dibujo de «verificado por WhatsApp» junto al teléfono tapado) lo componga sin volver a decidir la estructura. **Pruebas nombradas**: la vigencia en `contact-verification.test.ts` → *«sin instante no hay nada que envejecer: no verificado»*, *«una fila de once meses todavía contesta que sí»*, *«una fila de trece meses ya no contesta que sí»*; el `JOIN` de verdad, contra Postgres, en `tests/integration/listing-contact-verification.test.ts` → *«no contesta que sí para otro valor de la misma cuenta verificada»* y *«no contesta que sí para el mismo valor por otro método»* — las dos que habrían pasado con un `JOIN` que sólo comparara `user_id`, que es justo el agujero que la 22.32 describe. **Mutaciones, restauradas y verificadas por sha256 (AGENTS.md §17)**: quitar `eq(verifiedContacts.value, listings.contactValue)` del `JOIN` pone roja exactamente *«no contesta que sí para otro valor de la misma cuenta verificada»*; quitar `eq(verifiedContacts.method, listings.contactMethod)` pone roja exactamente *«no contesta que sí para el mismo valor por otro método»*; que `listingContactIsVerified` deje de honrar la vigencia (contestar por la sola presencia del instante) pone roja exactamente *«una fila de trece meses ya no contesta que sí»*. **Hallazgo, y se reporta tal cual salió**: quitar `eq(verifiedContacts.userId, listings.publisherId)` no pone roja NINGUNA de las cinco — ningún fixture de la suite cruza dos publicadores distintos compartiendo método y valor contra una fila verificada de otra cuenta, así que ese predicado corre hoy sin una prueba propia que lo sostenga. Queda como seguimiento de esta misma tarea, no de la 15.11 **Seguimiento cerrado 2026-09-04**: se agregó *«no contesta que sí para el mismo método y valor de una cuenta ajena»* a `tests/integration/listing-contact-verification.test.ts`, con un aviso de OTHER_PUBLISHER cuyo `(contact_method, contact_value)` coincide letra por letra con la fila viva de PUBLISHER. Quitar `eq(verifiedContacts.userId, listings.publisherId)` pone roja exactamente esa prueba nueva y ninguna otra de las seis — restaurado y verificado por sha256, `6ff0cb78...ac83ba5` antes y después, `git status` limpio sobre el adaptador.
- [x] 22.33 **(nueva — encontrada rehaciendo el recuento de la 16.38.)** **`--ficha-body-lh` se usa sin `--ficha-body-fs` en cinco declaraciones: es media pareja.** ~~Las tres hojas de más son `ContactBlock.module.css` (`.why`, `.expiredText`, y el recuadro de las 154), `DeclaredFeatures.module.css` (`.nota`) y `SignInDoor.module.css` (`.reason`)~~ **CORRECCIÓN AL TEXTO DE ESTA TAREA, verificada contra el árbol de trabajo el 2026-09-04: son OCHO, no cinco.** Revisando cada declaración de `var(--ficha-body-lh)` móvil (16 en total) contra su `font-size` en la misma regla aparecieron TRES más con la forma exacta que esta tarea describe y que su lista original no nombraba: `.lead` en `app/(auth)/signin/revisa-tu-correo/espera.module.css:36` (con `--control-fs`), `.reason` en `app/(auth)/signin/signin.module.css:62` (con `--control-fs`) y `.textarea` en `app/publicar/publish-steps.module.css:325` (sin `font-size` propio — lo hereda de `.control`, la clase con la que siempre se combina en el marcado, que sí declara `--control-fs`). Las ocho declaraciones reales: `ContactBlock.module.css` `.why` (66), `.expiredText` (141) y `.warning` (154, «el recuadro de las 154»), `DeclaredFeatures.module.css` `.note` (51), `SignInDoor.module.css` `.reason` (55), y las tres nuevas de arriba. Verificado también: las 8 restantes que SÍ llevan `--ficha-body-fs` (`FailureScreen`, `/mis-avisos`, tres en publicar, `importar`, la ficha, `reportar`) suman exactamente 8, cerrando las 16 declaraciones móviles de `--ficha-body-lh` sin resto. **No crecieron con la 16.38 a propósito**: la decisión del fundador fue sobre el par, y estirar a 1.65 un metadato de 12px o un campo de 15px que ninguna lámina dibuja distinto habría excedido lo que se decidió. **Decisión del fundador, 2026-09-04: las ocho ganan su propio token de interlineado, `--secondary-lh: 1.6`** — el papel real es «texto secundario cómodo de leer», que no es el cuerpo. Rechazada la otra salida honesta (declarar en `SISTEMA.md` que el interlineado del cuerpo se comparte a propósito): habría sido la misma forma de nota que la 16.38 ya advirtió que es cómo un token con dos significados empieza a discrepar. Declarado una sola vez en `[data-layout="compacto"]` de `src/styles/tokens.css`, junto al par del cuerpo — no por tema (`--secondary-lh` no es un color, y `lint:tokens` sólo exige el contrato de tema para lo que sí lo es) — y sin variante de escritorio: ninguna de las ocho hojas dibuja un `@media` que toque estas ocho reglas, así que darle paso habría sido decidir algo que nadie pidió. Nombrado en `SISTEMA.md`, fila «Texto secundario cómodo de leer» en la tabla de tipografía. El par `--ficha-body-fs`/`--ficha-body-lh` queda contando parejo: cada `--ficha-body-lh` que sobrevive tiene un `--ficha-body-fs` en la misma regla. **Pruebas nombradas**: `src/styles/secondary-lh.test.ts` → *«tokens.css declara --secondary-lh»*, una por cada una de las ocho ubicaciones (*«lee --secondary-lh y no --ficha-body-lh»*, roja antes del cambio) y una guarda permanente contra la deriva — *«ninguna hoja de componente combina --meta-fs o --control-fs con --ficha-body-lh»*, que barre `app/`/`components/` en vez de enumerar y habría atrapado las tres que la lista original no vio. `pnpm lint:tokens` en 0 antes y después. Suite de seguridad sobre los cinco componentes tocados: verde (235 pruebas) — la misma lección que ya está escrita sobre `--fp`/`--control-fs` y sobre `--brandbar-h`/`--nav-h`
- [x] 22.34 **HECHA el 2026-09-06.** **(nueva — la mitad de la 16.41 que sí necesita cambiar, y va en un archivo de diseño.)** **Corregir el recuadro del aviso vencido ~~en las dos láminas de la Ficha y~~ en la §6 de la especificación.** Dibujan «Venció el 12 de septiembre y **el dueño no lo renovó**»; el producto escribe «y **no fue renovado**», y el fundador ratificó el código el 2026-08-29 porque «el dueño» es falso para todo aviso de inmobiliaria. **Se registra en vez de editarse de paso**: tocar una lámina es un cambio a la fuente visual de verdad (AGENTS.md §2) y esta entrega no venía a eso. Alcance exacto: `design/pantallas/Rentoru - Ficha - Mobile.dc.html` (línea 191), `design/pantallas/Rentoru - Ficha - Desktop.dc.html` (línea 150) y `design/especificaciones/Rentoru - Ficha - Especificacion.md` (línea 182). ~~**La divergencia del mes abreviado (16.40) NO va acá**: aquélla se decidió al revés —el producto escribe «12 sept.» y las láminas «12 sep», y las láminas se quedan como están— y ya está anotada en `SISTEMA.md`~~ **ALCANCE REDUCIDO POR EL RULING DEL FUNDADOR DEL 2026-09-05 (rama 2 del encabezado de esta fase), y la casilla sigue abierta porque el trabajo escrito no está hecho.** El contenido ya estaba resuelto y no se reabre: el producto se queda con «no fue renovado», ratificado por el fundador el 2026-08-29 porque «el dueño» es falso para todo aviso de inmobiliaria. Lo que queda es sólo la constancia, y son dos cosas distintas y no tres. **SÍ se corrige** `design/especificaciones/Rentoru - Ficha - Especificacion.md:182`: una especificación desactualizada se lee como la verdad para quien tome la ficha después. **NO se editan las dos láminas** —`design/pantallas/Rentoru - Ficha - Mobile.dc.html:191` y `design/pantallas/Rentoru - Ficha - Desktop.dc.html:150`—: la discrepancia es sólo de redacción, así que manda el texto shipeado y la divergencia **se registra en `SISTEMA.md`**, exactamente como hizo la 16.40. **Corregida entonces la última frase de esta tarea, que hoy es falsa**: la 16.40 no se decidió «al revés» — se decidió igual, y esta tarea es su mismo caso y no su contrario. **El trabajo escrito que faltaba ya está**: `Especificacion.md:182` dice «y no fue renovado», las dos láminas quedaron intactas, y el punto 5 de la lista de `SISTEMA.md` registra la divergencia con la misma forma que el punto 4 (16.40)
- [x] 22.35 **(nueva — de una revisión de fiabilidad de Gentle AI que quemó sus artefactos al reconocerse; del hallazgo `R3-afterall-pool-leak` (WARNING) sólo sobrevivieron el id, la ubicación y la severidad.)** **El `afterAll` de la suite nueva cierra el pool en la última línea, así que un `DELETE` que falle se lleva el cierre con él.** `tests/integration/listing-contact-verification.test.ts:115-122` corre tres `DELETE` y recién entonces `pool.end()`, sin `try`/`finally`: si cualquiera de los tres revienta —el caso exacto que `tests/integration/listing-report.test.ts:169-181` documenta, una violación de clave foránea por filas que otra suite dejó en la base compartida— el pool queda abierto, Vitest no cierra limpio y el error de teardown tapa el `DELETE` que falló de verdad. **Es real y no es de este archivo**: `tests/integration/listing-detail.test.ts:87-92` y `tests/integration/contact-verification.test.ts:101-111` tienen exactamente la misma forma, y `tests/integration/zone.test.ts:82-84` sólo cierra. Las salidas son dos: o un helper compartido que garantice el cierre en `finally` para todas las suites de integración, o dejar escrito que la convención se acepta como está — parchear este archivo solo deja los otros treinta y cinco igual y borra el hallazgo sin resolverlo. **Corrección registrada sobre esta misma marcación (fundador, 2026-09-04): la 22.39 SÍ es real, no es la única — la frase de la 22.39 que decía lo contrario queda corregida ahí mismo, con su razón.** **Cerrada 2026-09-04 — se construyó el helper compartido, aplicado a las tres suites que el propio hallazgo ya identificó con la forma exacta.** `withPoolCleanup` (`tests/integration/support/pool-cleanup.ts`) envuelve la limpieza en `try`/`finally`: el pool cierra pase lo que pase, y el error que se propaga es siempre el de la limpieza real, nunca uno de teardown tapando al otro. Se aplicó a `listing-contact-verification.test.ts` (el archivo del hallazgo) y a los otros dos que el propio texto señaló como «exactamente la misma forma» — `listing-detail.test.ts` y `contact-verification.test.ts`. **Decisión de alcance, tomada deliberadamente y no por omisión**: de las dos salidas que el hallazgo nombra, ninguna se tomó entera. No se dejó la convención como está —eso habría sido no construir nada—, y tampoco se barrieron las treinta y cinco suites restantes en esta misma entrega: ese barrido es mecánico pero no gratis (cada archivo cambia su `afterAll` y hay que releer cada uno para no romper una limpieza que ya dependía del orden), y siendo tres de cinco tareas de un mismo lote de revisión, no es proporcional. Las tres suites corregidas eliminan el defecto donde ya estaba probado que existía —el hallazgo, más las dos citas textuales—; las treinta y cinco restantes quedan en la convención vieja, registradas como seguimiento explícito y no silencioso, con el helper ya escrito y listo para que ese barrido sea mecánico cuando se decida hacerlo. **Pruebas nombradas**: `tests/integration/support/pool-cleanup.test.ts` → *«cierra el pool después de que la limpieza termina bien»* y *«cierra el pool aunque la limpieza reviente, y deja pasar el error real»*, sin Postgres real porque es control de flujo puro. Las tres suites reescritas corren contra Postgres real por `pnpm test:integration` y siguen pasando sin cambiar ninguna aserción de producto.
- [x] 22.36 **(nueva — de la misma revisión quemada; hallazgo `R3-limit-without-order` (SUGGESTION), ubicación `tests/integration/listing-contact-verification.test.ts:32-34`.)** **El id y la ubicación no se corresponden: en las líneas señaladas no hay `limit` ni `order`.** Esas tres líneas son el armado del archivo —`new Pool(...)`, `drizzle(pool, { schema })` y `new DrizzleListingContactVerification(db)`—, y `rg 'limit|order'` sobre el archivo entero no devuelve una sola coincidencia: no hay consulta que ordenar en ninguna de sus 161 líneas. El único `.limit(1)` sin `ORDER BY` de la entrega vive en `src/modules/identity/infrastructure/drizzle-verified-contact.ts:93` y está tratado en la 22.38. **Se registra como no sustanciado, con su razón**, porque el texto del hallazgo se quemó y no se puede recuperar: o era una copia mal ubicada del de la 22.38, o hablaba de algo que ya nadie puede leer. No hay nada que corregir acá, y ésta es la anotación para que no se reabra desde cero
- [x] 22.37 **(nueva — de la misma revisión quemada; hallazgo `R3-malformed-listing-id` (SUGGESTION).)** **Un `listingId` con forma inválida no revienta, porque `listing.id` es `text` y no `uuid`.** `src/modules/identity/infrastructure/drizzle-verified-contact.ts:80` recibe `listingId: string` crudo y lo lleva sin validar a `eq(listings.id, listingId)`; el hallazgo tendría cuerpo sólo si la columna forzara un casteo que Postgres pudiera rechazar, y `src/shared/db/schema.ts:405` la declara `id: text("id").primaryKey()`. Contra una columna de texto, cualquier cadena —vacía, con espacios, no-UUID— simplemente no empareja: cero filas, y `rows[0]?.verifiedAt ?? null` contesta `null`. La suite ya lo prueba, además, en `tests/integration/listing-contact-verification.test.ts:158-160` → *«un aviso que no existe no revienta: devuelve null»*. **No sustanciado.** Reviviría sólo si `listing.id` pasara alguna vez a `uuid`, y ese día el trabajo sería de la migración y no de este método
- [x] 22.38 **(nueva — de la misma revisión quemada; hallazgo `R3-nondeterministic-row-pick` (SUGGESTION), y un revisor anterior ya lo había discutido.)** **El `.limit(1)` sin `ORDER BY` es determinista por la primaria, no por suerte, y un `ORDER BY` explícito mentiría sobre la cardinalidad.** `src/modules/identity/infrastructure/drizzle-verified-contact.ts:93` cierra con `.limit(1)` un `INNER JOIN` cuyo `WHERE` entra por `listing.id` —primaria, a lo sumo un aviso— y cuya condición de join iguala las TRES columnas de la primaria de `verified_contact`: `src/shared/db/schema.ts:199` la declara `primaryKey({ columns: [verified.userId, verified.method, verified.value] })`. Con el triple entero fijado, a lo sumo una fila puede emparejar; el resultado ya tiene cardinalidad máxima uno ANTES del `limit`, así que no hay fila que elegir ni orden que decidir. **La lectura del revisor anterior es la que se sostiene, y se deja escrita.** Ordenar «por intención» empeoraría el código: un `ORDER BY verified_at DESC` diría «si hay varias, gana la más nueva», que es falso, y contradice la disciplina que el mismo adaptador ya escribe sobre su `upsert` —la garantía es la restricción y nunca un `if`—. **No sustanciado**; lo único defendible sería borrar el `.limit(1)` por redundante, y eso no paga un cambio de código
- [x] 22.39 **(nueva — de la misma revisión quemada; hallazgo `R3-uncomposed-halves` (SUGGESTION).)** **Corrección de la marcación original (fundador, 2026-09-04): la frase «y es el único real de los cinco» queda tachada acá y no reescrita, porque la 22.35 también es real** — la marcación vigente es la que la 22.35 trae. **Las dos mitades de la 22.32 existen, están probadas por separado, y nada en producción las compone.** `src/modules/identity/domain/contact-verification.ts:119-121` decide vigencia sobre un `Date | null`, y `DrizzleListingContactVerification.findVerifiedAt` produce exactamente ese `Date | null`; sin embargo `rg` sobre el repositorio devuelve como llamadores de `listingContactIsVerified` sólo `src/modules/identity/domain/contact-verification.test.ts:246-257`, y de `findVerifiedAt` sólo la suite de integración. **Ninguna capa de render llama a ninguna de las dos**, así que hoy ningún instante leído de Postgres llega jamás al predicado que lo juzga, y el borde de doce meses nunca se ejerce sobre un dato real. **Es deuda declarada y no un descuido**: la propia 22.32 dice «Ninguna capa de render se tocó» y deja la composición a la 15.11. Se anota igual porque es el mismo patrón que la 22.31 nombró como segunda instancia de la 8.7 —un dominio completo, probado y sin llamador es indistinguible de uno que funciona—: la salida es que la 15.11 componga las dos mitades y traiga la prueba que las cruza, y que hasta entonces esto no se cuente como cubierto. **Cerrada 2026-09-04, y no por la 15.11 — la 15.11 ya cerró el 2026-09-02 sin tocar esto, así que no hay a quién más diferirle la deuda.** `isListingContactVerified` (`src/modules/identity/application/is-listing-contact-verified.ts`) es la composición: llama a `ListingContactVerificationPort.findVerifiedAt` y le pasa el instante a `listingContactIsVerified` con el mismo reloj obligatorio en forma de función (`now: () => Date`) que `resolveContactVerification` ya exige, y por la misma razón — un valor por omisión dejaría compilar un llamador que se olvida de la ventana de doce meses. **El llamador de producción es `contactDoorFor`** (`src/modules/contact-reveal/domain/sign-in-door.ts`): la puerta que pide la cuenta (láminas Ficha 8b/9b) recibe el booleano ya decidido — nunca el instante, nunca el valor del contacto, la misma garantía del estado bloqueado que la 22.32 ya defendía del lado del puerto — y dibuja «verificado por {canal}» al lado de la razón cuando corresponde, `null` si no. **Costo: sólo cuando la puerta se va a dibujar de verdad.** `app/alquiler/[ciudad]/[zona]/[slug]/page.tsx` sólo llama a `isListingContactVerified` cuando el contacto está `locked` Y el parámetro `entrar=si` ya llegó — la misma disciplina que ya aplica `ContactVerificationEvidencePort` en este archivo (sólo se paga en la rama que lo necesita), y no una consulta nueva en cada visita anónima de la pantalla más visitada del sitio. **Alcance, decidido y no freelanceado**: el bloque de contacto de la ficha (`ContactBlock`) NO se tocó — su lámina «sin cuenta» (`Rentas - Ficha - Desktop.dc.html:120-130`) deliberadamente no dibuja nada sobre verificación en el estado bloqueado, así que agregarlo ahí habría contradicho la fuente visual de verdad (AGENTS.md §2). La lámina que SÍ dibuja «teléfono verificado por WhatsApp» junto al contacto tapado es la puerta de entrar (`Rentas - Entrar - Desktop/Mobile.dc.html`, 9b/8b), y es exactamente donde se compuso. El texto se generalizó con el mismo patrón que `contactVerificationNotice` ya usa (`verificado por ${canal}`, sin fecha porque el estado bloqueado nunca recibe el instante) en vez de copiar literalmente «teléfono verificado por WhatsApp», que sería falso hoy para el único canal con verificación realmente viva (`email`, vía el atajo de cuenta de la 19.10 — el canal de WhatsApp sigue diferido). `app/measure/page.tsx` pasa `verified: false` sin cambiar su geometría medida, a propósito: ese arnés no corre contra Postgres. **Pruebas nombradas**: `is-listing-contact-verified.test.ts` → *«contesta que sí cuando el puerto trae un instante vigente»*, *«contesta que no cuando el puerto no trae fila»*, *«contesta que no cuando el instante ya venció»*, *«pregunta al puerto por el listingId exacto que se le pasó»*; `sign-in-door.test.ts` → *«dice que el contacto está verificado cuando la respuesta ya viene en sí»*, *«nombra el canal del aviso y no siempre WhatsApp»*, *«no afirma nada cuando la respuesta ya viene en no»*; `ficha-servida.test.tsx` → *«dice que el contacto está verificado cuando el puerto trae un instante vigente»* (contra la ruta real, con el adaptador reemplazado por un doble) y *«no pregunta por la verificación mientras la puerta está cerrada»*, que es la que prueba la disciplina de costo. `pnpm test` — 2702/2702 verdes, sin ninguna prueba pre-existente rota.
- [x] 22.40 **(nueva — de una segunda revisión de fiabilidad de Gentle AI, linaje `review-851a60f17d7aac50`, objetivo `sha256:624de9f8…` contra `dev`, riesgo medio, 17 archivos / 388 líneas, una sola lente `review-reliability`: aprobada y reconocida, con la autoridad y los artefactos quemados en el reconocimiento. De los siete hallazgos sólo sobrevivieron el id, la severidad y la ubicación; el texto que sigue en la 22.40 a la 22.46 es la única constancia que queda de ellos, y se escribió leyendo el código, no recordando el hallazgo. Riega el trabajo de las tareas 22.7, 22.13 y 22.33.)** **Hallazgo `R3-srOnly-enumerated-not-swept` (WARNING), ubicación `src/styles/a11y.test.ts:32-50`.** **La guarda de la 22.7 no barre el árbol: enumera cinco hojas a mano, así que una sexta que vuelva a declarar el cuerpo entra sin que nada la vea.** `CONSUMERS` (líneas 32-50) es una lista escrita a mano de cinco rutas, y `rg 'readdir|walk|glob' src/styles/a11y.test.ts` no devuelve nada: la negativa de la línea 71 —`expect(css).not.toMatch(/\.srOnly\s*\{[^}]*position:\s*absolute/)`, que es la que impide que el `composes` se agregue dejando vivo el cuerpo viejo— corre **sólo sobre esas cinco rutas**. **La lista está completa hoy, y eso se midió, no se supuso**: `rg --glob '*.css' '\.srOnly' app components src` devuelve seis declaraciones —las cinco de `CONSUMERS` más `src/styles/a11y.module.css:20`—, y `rg -U '\.srOnly\s*\{[^}]*position:\s*absolute'` sobre `app`, `components` y `src` devuelve **un solo archivo**, el módulo compartido. La deriva que la 22.7 cerró está cerrada. Lo que no existe es el mecanismo que la mantenga cerrada mañana. **El criterio ya está escrito en este repositorio, por dos vías**: su hermana del mismo lote, `src/styles/secondary-lh.test.ts:74-91`, sí recorre `app` y `components` con `readdirSync`; y `scripts/lint-tokens.test.ts:81` nombra la regla textualmente en el título de una prueba — *«recorre los archivos en vez de mirar una lista escrita a mano»*. La 22.7 no la cumple. Cerrarlo pide que la guarda camine `app`, `components` y `src` y falle ante cualquier bloque `.srOnly` con cuerpo fuera de `src/styles/a11y.module.css` —la forma exacta que la hoja hermana ya usa—, o que quede escrito que la enumeración se acepta y por qué **Cerrada 2026-09-04 — se agregó la guarda de barrido.** `src/styles/a11y.test.ts` ahora tiene una prueba adicional (*«ninguna hoja fuera del módulo compartido declara su propio cuerpo de .srOnly»*) que recorre `app`, `components` y `src` con `readdirSync` — la misma forma que ya usa `secondary-lh.test.ts` — y falla ante cualquier bloque `.srOnly { … position: absolute … }` fuera de `src/styles/a11y.module.css`. `CONSUMERS` se conserva, pero deja de ser la única defensa: ahora sólo fija qué ruta relativa espera el `composes` de cada consumidor conocido. **Probado con RED real, no supuesto**: se creó `components/molecules/__scratch-22-40/Scratch.module.css` con un `.srOnly` local; la guarda enumerada vieja (`it.each(CONSUMERS)`) siguió en verde con la mentira presente en el árbol — confirmando el hallazgo tal cual lo describe—; la guarda de barrido nueva la detectó de inmediato (roja); se borró el archivo y la suite volvió a verde (7/7, antes 6/6)
- [x] 22.41 **(nueva — de la misma revisión quemada; hallazgo `R3-secondary-lh-guard-misses-desktop` (WARNING), ubicación `src/styles/secondary-lh.test.ts:82-91`.)** **La guarda contra la deriva compara contra el literal `var(--ficha-body-lh)` y por eso no ve `var(--ficha-body-lh-desktop)`, que es exactamente la forma que el defecto tomaría en escritorio.** La línea 87 hace `block.includes("var(--ficha-body-lh)")`, con el paréntesis de cierre dentro de la cadena; **verificado ejecutándolo**: `"var(--ficha-body-lh-desktop)".includes("var(--ficha-body-lh)")` es `false`, porque después de `lh` viene un guion y no el paréntesis. El paso de escritorio existe y se usa: `src/styles/tokens.css:193` declara `--ficha-body-lh-desktop: 1.65` y **seis hojas lo leen dentro de un `@media`** —`FailureScreen.module.css:64`, `publish-steps.module.css:774`, `mis-avisos.module.css:262`, `importar.module.css:263`, `ficha.module.css:238` y `reportar.module.css:93`—, las seis apareadas con `--ficha-body-fs-desktop`, que es el par legítimo del cuerpo. Una séptima que dentro de un `@media` escribiera `font-size: var(--meta-fs); line-height: var(--ficha-body-lh-desktop);` sería la media pareja que esta tarea entera existe para matar, y la guarda la dejaría pasar en silencio. **Dos revisores ciegos llegaron a este punto por separado**, y eso pesa más que uno: un validador de contexto fresco, corrido antes de esta revisión y sin conocerla, ya había señalado que `--secondary-lh` no tiene paso de escritorio y que hoy es correcto sólo porque ninguna de las ocho hojas dibuja un `@media` que las toque — lo que la guarda no cubre es ese futuro. **La premisa se verificó**: de las ocho hojas, cinco tienen `@media` (`ContactBlock`, `SignInDoor`, `espera`, `signin`, `publish-steps`) y **ninguno de esos bloques toca ninguno de los ocho selectores** — tocan `.block`, `.door`, `.panel`, `.column`, `.troubles`, `.actions`, `.troubleList`, `.barInner`, `.brand`, `.back`, `.grid`, `.emailRow`, `.steps`, `.layout`, `.review`, `.rail`, `.progress`, `.counter`, `.map`. La afirmación de `src/styles/tokens.css:205` —«ninguna de las ocho hojas dibuja un `@media` que las toque»— es cierta hoy y es prosa: nada la asevera. Cerrarlo pide que `hasBodyLh` también reconozca la variante de escritorio, o que `--secondary-lh` reciba su propio paso y la guarda cubra el par entero **Cerrada 2026-09-04 — `hasBodyLh` reconoce ahora las dos formas.** `src/styles/secondary-lh.test.ts` compara contra `var(--ficha-body-lh)` O `var(--ficha-body-lh-desktop)`; no se le dio un paso propio a `--secondary-lh` porque, como la propia tarea deja escrito, ninguna de las ocho hojas dibuja hoy un `@media` que toque esos ocho selectores — inventar un paso de escritorio que nadie usa habría sido decidir algo que nadie pidió. **Probado con RED real**: se agregó temporalmente `app/__scratch-22-41/scratch.module.css` con un bloque `@media` que combinaba `var(--meta-fs)` y `var(--ficha-body-lh-desktop)` — la guarda vieja quedó en verde con la mentira presente; tras el fix la misma hoja puso la guarda en rojo; se borró el archivo y la suite volvió a verde
- [x] 22.42 **(nueva — de la misma revisión quemada; hallazgo `R3-visor-body-change-unasserted` (WARNING), ubicación `app/alquiler/[ciudad]/[zona]/[slug]/foto/[n]/visor.module.css:125`.)** **El visor es la única de las cinco hojas cuyo CSS pintado cambió de verdad, y ninguna prueba asevera ese cambio: sólo una prosa lo declara inocuo.** La línea 125 es el `composes: srOnly from "../../../../../../../src/styles/a11y.module.css";` que reemplazó al cuerpo local. **La asimetría se midió contra `dev`**: `git show dev:…/visor.module.css` traía un `.srOnly` de **seis** propiedades (`position`, `inline-size`, `block-size`, `overflow`, `clip-path`, `white-space`), mientras que las otras cuatro hojas —`app/home.module.css`, `app/publicar/publish-steps.module.css`, `components/molecules/SearchPill.module.css`, `components/client/SearchSuggestions.module.css`— ya traían en `dev` el cuerpo de **nueve** idéntico al que hoy declara `src/styles/a11y.module.css:20-31`. Componer, entonces, es un no-op textual para cuatro y **agrega tres declaraciones nuevas a una sola**: `margin: -1px`, `padding: 0` y `border: 0` sobre el visor. La única cosa que dice que eso no cambia nada en pantalla es el comentario de cabecera de `src/styles/a11y.test.ts` —«las cinco pintan lo mismo en pantalla, completa o incompleta, porque `clip-path: inset(50%)` ya recorta el elemento a nada visible»—, que es un argumento y no una aserción; la prueba de la línea 68 sólo mira que el texto del `composes` esté, y la de la 71 que el cuerpo viejo no esté. **El argumento es plausible y no está probado**, y es el mismo patrón que la 22.35 castigó: una convención defendida en prosa no es una guarda. Cerrarlo pide o una aserción sobre el visor renderizado —la única de las cinco donde el cambio es real—, o dejar escrito que el argumento del recorte se acepta como suficiente y por qué las tres propiedades agregadas no pueden alterar nada visible **Cerrada 2026-09-04, con la segunda salida — el argumento se acepta, y se registra por qué, verificado propiedad por propiedad y no en bloque.** El repositorio tiene un arnés real para esto (`tests/measure/` + `playwright.measure.config.ts`, contra un servidor Next real), y se descartó a propósito: las tres declaraciones agregadas cierran en geometría CSS pura, sin ninguna dependencia de layout real que sólo un navegador pudiera revelar, así que levantar el arnés habría medido lo que ya se puede demostrar por las reglas de la cascada. **`padding: 0` y `border: 0` no son un cambio**: son exactamente los valores iniciales que el navegador ya aplicaba sin ellos —`padding` inicial es `0`, y `border-style` inicial es `none`, que computa el ancho del borde a `0` sin importar qué `border-width` se declare—, así que el `.srOnly` viejo del visor (sin esas dos líneas) y el nuevo (con ellas) pintan idéntico por definición, no por argumento. **`margin: -1px` es el único cambio real, y es de LAYOUT, no de pintura**: los cuatro `<span className={styles.srOnly}>` del visor son hijos de contenedores `display: flex` (`.close`, `.previous`, `.next`, `.thumb`/`.thumbCurrent`) pero el propio `.srOnly` es `position: absolute` — y un hijo posicionado de forma absoluta queda fuera del algoritmo de flexbox por especificación: no participa en el tamaño ni en la posición de sus hermanos, en el visor ni en las otras cuatro hojas, con o sin el `margin` nuevo. Lo único que el `margin: -1px` puede mover es la posición final de la propia caja del `.srOnly` respecto de su bloque contenedor — y esa caja, con `clip-path: inset(50%)` recortando un elemento de `1px × 1px` al 50% desde cada lado, pinta CERO área desde antes de que el `margin` importe: `inset(50%)` dejaba la región visible en `0 × 0` ya con el cuerpo viejo de seis propiedades que traía `dev`, así que desplazar una caja que no pinta nada no puede producir un píxel que antes no existía. La combinación de las dos razones —dos propiedades que ya eran no-operativas y una tercera que actúa sobre una caja sin área pintada— es lo que cierra el argumento sin necesitar un render real
- [x] 22.43 **(nueva — de la misma revisión quemada; hallazgo `R3-composes-path-not-resolved` (SUGGESTION), ubicación `src/styles/a11y.test.ts:63-73`, y un segundo revisor ciego lo levantó por su cuenta.)** **La prueba asevera la ruta relativa como CADENA y nunca la resuelve: una errata pasa en verde y sólo revienta en `pnpm build`.** La línea 68 hace `expect(css).toContain(\`composes: srOnly from "${from}"\`)` — comparación textual contra el literal que `CONSUMERS` guarda; `rg 'existsSync|resolve|join|node:path' src/styles/a11y.test.ts` **no devuelve nada**, así que en ningún punto se comprueba que el destino exista. Las rutas son relativas y no con el alias `@/` por una razón real —**el alias `@/` no resuelve dentro de `composes` de css-loader**—, y la más profunda son **siete** «../» (la del visor, `app/alquiler/[ciudad]/[zona]/[slug]/foto/[n]/visor.module.css:125`): un «../» de más o de menos es un fallo de compilación que ninguna prueba y ningún gate reportan. **Corroborado por dos revisores independientes**: un validador de contexto fresco, corrido antes de esta revisión y ciego a ella, nombró el mismo agujero desde el otro lado — **ningún gate del plan compila CSS Modules**, así que la 22.7 entera descansa en que un `composes` con ruta relativa resuelva, y lo único que lo prueba es `pnpm build`. Cerrarlo pide o que la prueba resuelva cada `from` contra el directorio de su hoja y asevere que el destino existe, o un gate que compile los CSS Modules — la segunda salida cubre además a toda hoja futura y no sólo a estas cinco **Cerrada 2026-09-04 — la prueba resuelve la ruta.** `src/styles/a11y.test.ts` ahora calcula `join(dirname(path), from)` y asevera `existsSync` sobre el resultado, para cada una de las cinco entradas de `CONSUMERS`, incluida la de siete «../» del visor. **Probado con RED real**: se corrigieron a la vez `app/home.module.css` y su entrada en `CONSUMERS` con un «../» de más, idéntico en los dos lados — con eso la aserción textual (`toContain`) seguía en verde, reproduciendo exactamente el escenario que el hallazgo describe; con `existsSync` agregado la prueba se puso roja en esa entrada y sólo en ésa. Se restauró `app/home.module.css` byte a byte (`git diff --stat` vacío tras revertir) y la entrada de `CONSUMERS`, y la suite volvió a verde (7/7). No se agregó un gate que compile CSS Modules aparte: la resolución de ruta ya cierra el agujero para las cinco hojas conocidas, y una sexta futura entra por el mismo `it.each` que ya la exige en `CONSUMERS`
- [x] 22.44 **(nueva — de la misma revisión quemada; hallazgo `R3-empty-allowlist-unexercised` (SUGGESTION), ubicación `scripts/lint-tokens.mjs:123-128`.)** **La 22.13 dejó el mapa de excepciones vacío, y con eso las dos ramas que dependen de él quedaron como código muerto que ninguna prueba ejecuta jamás.** La línea 128 es `const UNUSED_TOKENS_ALLOWED = new Map();`, sin entradas, porque `--ft` y `--tclamp` se retiraron de `tokens.css` en vez de exceptuarse. `rg 'UNUSED_TOKENS_ALLOWED'` devuelve **dos consumidores**: `scripts/lint-tokens.mjs:445` (`const excuse = UNUSED_TOKENS_ALLOWED.get(token); if (excuse) continue;`, el perdón) y el bucle de caducidad de las líneas 454-459, que es el que emite *«ya se usa, así que su excepción caducó»*. Con cero entradas, `.get()` devuelve siempre `undefined` y el bucle **no itera ni una vez**. `scripts/lint-tokens.test.ts` tiene **tres** pruebas —líneas 57, 70 y 81— y `rg 'UNUSED_TOKENS_ALLOWED|caduc|excuse|excepción'` sobre ese archivo **no devuelve nada**: la caducidad automática que el comentario de las líneas 115-122 defiende con tanto cuidado —«una lista de perdones que nadie limpia es la forma en que un gate deja de significar algo»— **nunca se ejecutó**. **No es un defecto de la 22.13**, que hizo lo correcto al retirar los tokens en vez de perdonarlos; es que al vaciar el mapa se volvió invisible que la maquinaria no tiene prueba. Cerrarlo pide una prueba que arme un espacio de trabajo con un token exceptuado y demuestre **las dos** ramas —perdonado mientras nadie lo usa, y fallando en cuanto alguien lo usa—, o retirar la maquinaria junto con su comentario el día que se decida que ese perdón no vuelve **Cerrada 2026-09-04, con la segunda salida: se retiró la maquinaria junto con su comentario.** Se quitaron el JSDoc que explicaba la excepción (líneas 106-122), la nota de la 22.13 (123-127) y `const UNUSED_TOKENS_ALLOWED = new Map();` (128), además de las dos ramas de `checkTokenUsage` que dependían de ella —el perdón (`excuse`) y el bucle de caducidad—. **Por qué esta salida y no la prueba**: `UNUSED_TOKENS_ALLOWED` es una constante de módulo del propio script, no un dato del workspace de mentira que `scripts/lint-tokens.test.ts` ya arma (ese arnés escribe `tokens.css` y una hoja de componente por prueba, pero no tiene forma de inyectar una excepción sin mutar el script en ejecución); construir un arnés nuevo sólo para ejercitar una excepción que hoy no tiene un solo caso real habría sido la misma clase de máquina sin motivo vivo que el hallazgo señala. Si un futuro token DEL SISTEMA necesita la salida que ya usó `--fpb` (16.37), se reintroduce entonces junto con la prueba que la ejercite — no antes. **Evidencia**: `pnpm lint:tokens` en 0 antes y después; `scripts/lint-tokens.test.ts` 3/3; `components/design-contract.test.tsx` 48/48; `rg 'UNUSED_TOKENS_ALLOWED'` sobre `scripts/` ya no devuelve nada
- [x] 22.45 **(nueva — de la misma revisión quemada; hallazgo `R3-secondary-lh-first-block-only` (SUGGESTION), ubicación `src/styles/secondary-lh.test.ts:42-47`.)** **`extractBlock` devuelve el PRIMER bloque del selector y nada más — su propio comentario lo dice —, así que una redeclaración posterior en la misma hoja queda sin leer.** La línea 45 arma un `RegExp` sin anclar y la 46 hace `cssText.match(pattern)?.[1]`, que es la primera coincidencia; el comentario de la línea 42 lo declara en mayúsculas (*«the FIRST `selector { ... }` block»*). **Hoy no muerde, y eso se midió**: contando bloques por par (archivo, selector) sobre las ocho entradas de `SECONDARY_LOCATIONS`, **las ocho devuelven exactamente 1** — `.why`, `.expiredText`, `.warning`, `.note`, las dos `.reason` (en hojas distintas), `.lead` y `.textarea`. **La hermandad con la guarda de barrido acota el daño y no lo elimina**: una redeclaración posterior que volviera a `var(--ficha-body-lh)` sí caería en el barrido de las líneas 74-91, así que esa mitad está cubierta por otro lado. Quedan dos huecos que ninguna de las dos pruebas ve: una redeclaración que use `var(--ficha-body-lh-desktop)` —el punto ciego que la 22.41 documenta— y la mitad **positiva**, un segundo bloque que quitara `var(--secondary-lh)` mientras el primero lo conserva, porque la aserción positiva de la línea 65 se satisface con el primer bloque y el barrido sólo busca infractores, no ausencias. Cerrarlo pide que `extractBlock` use `matchAll` y que la aserción corra sobre **todos** los bloques del selector, no sobre el primero **Cerrada 2026-09-04 — `extractBlock` se reemplazó por `extractBlocks`.** Usa `matchAll` con la bandera `g` en vez de `match` sin ancla, y `it.each(SECONDARY_LOCATIONS)` ahora corre las dos aserciones sobre TODOS los bloques que devuelve, no sobre el primero. **Probado con RED real** (verificación manual con `node -e`, sin arnés permanente, porque la función vieja es una expresión de módulo del propio archivo de prueba y no algo con lo que valga la pena montar un workspace): sobre el string `.foo { line-height: var(--secondary-lh); } .bar {} .foo { line-height: var(--ficha-body-lh); }`, la función vieja (`match` sin `g`) devolvió sólo el primer bloque —limpio— y nunca vio la redeclaración; la versión con `matchAll` devolvió los dos bloques, incluido el que regresa a `--ficha-body-lh`. Suite verde (11/11) tras el cambio, con las ocho ubicaciones reales de `SECONDARY_LOCATIONS` (que hoy siguen teniendo un solo bloque cada una) sin ningún falso positivo
- [x] 22.46 **(nueva — de la misma revisión quemada; hallazgo `R3-textarea-pairing-unproved` (SUGGESTION), ubicación `app/publicar/publish-steps.module.css:325-329`.)** **`.textarea` lleva el interlineado pero no el tamaño: el par depende de que el marcado la combine siempre con `.control`, y lo único que lo afirma es un comentario.** Las líneas 325-328 dicen «El tamaño lo trae `.control` (`font-size: var(--control-fs)`), con la que esta clase siempre se combina en el marcado», y la 329 pone `line-height: var(--secondary-lh);`. **La afirmación es cierta hoy, medida y no supuesta**: `rg --glob '*.tsx' 'styles\.textarea' app components` devuelve **exactamente dos** llamadores —`app/publicar/PublishStep.tsx:679` y `app/mis-avisos/[id]/editar/page.tsx:420`— y **los dos** escriben `${styles.control} ${styles.textarea}`; el segundo además importa la misma hoja (`import styles from "../../../publicar/publish-steps.module.css"`, línea 44), y `rg --glob '*.css' '^\.textarea'` confirma que el bloque existe en un solo lugar. **El comentario no está equivocado: está sin probar**, que es otra cosa. Y `.textarea` es la **única** de las ocho entradas de `SECONDARY_LOCATIONS` cuyo tamaño vive en otra clase, así que es la única donde el par puede romperse desde el lado del marcado y no desde el de la hoja — un tercer llamador que escribiera `styles.textarea` solo dejaría un interlineado secundario sobre un tamaño heredado de nadie, en verde y sin síntoma. Cerrarlo pide o una aserción de que todo uso de `styles.textarea` viaja con `styles.control` —el mismo criterio textual que las dos pruebas nuevas de este lote ya aplican sobre las hojas—, o que `.textarea` declare su propio `font-size: var(--control-fs)` y deje de depender de la combinación **Cerrada 2026-09-04, con la segunda salida: `.textarea` declara su propio `font-size`.** `app/publicar/publish-steps.module.css` agrega `font-size: var(--control-fs);` al bloque de `.textarea`, con el comentario corregido para no seguir prometiendo una herencia que ya no hace falta. **Probado con RED real**: se agregó a `src/styles/secondary-lh.test.ts` la prueba *«.textarea declara su propio font-size (22.46)»*, que falló antes del cambio — y falló contra la aserción correcta, no una que el propio comentario de la hoja hace pasar por accidente: la hoja MENCIONA `var(--control-fs)` entre backticks dentro de su comentario, así que un `toContain` ingenuo sobre el bloque crudo habría leído la prosa y pasado en verde sin que el CSS cambiara; la prueba usa `toMatch(/font-size:\s*var\(--control-fs\);/)`, que exige el punto y coma de una declaración real y no la coincide con el comentario. Tras agregar la línea, la prueba pasó y la suite quedó en verde (11/11)
- [x] 22.47 **(nueva — medida al retirar la 22.9 de este cierre, 2026-09-06.)** **La frase del metadato se compone como texto plano, así que el navegador puede partirla en CUALQUIER espacio.** Verificado hoy: `components/molecules/ListingCard.tsx:109` dibuja `{zone} · {rooms} hab · {areaM2} m²`; `components/molecules/ResultRow.tsx:71-75` agrega ciudad y antigüedad, cada una en su propio `<span className={styles.wide}>`; `app/mis-avisos/page.tsx:245-248` le suma `· ref. ${card.externalReference}`. El átomo `ListingMeta` sólo lleva el papel tipográfico —deliberadamente no compone la frase, «no decide qué dice, sólo cómo se ve»—, así que ninguno de los tres consumidores hereda una regla de dónde puede partirse. **Consecuencia**: un corte puede caer entre `78` y `m²`, o adentro de `Los Palos Grandes`. **Esto es PRE-EXISTENTE, no lo causó la Fase 22.** Medido a 360px, el cuerpo de la tarjeta deja **136px** utilizables. La frase de muestra «Chacao · 2 hab · 78 m²» mide **113,4px** con la tipografía base actual (12px/600/`--sans` da 131,3px, holgura +4,7px = 3,5%). Una zona real de la semilla, **«Los Palos Grandes» (17 caracteres contra los 6 de «Chacao»)**, vuelve la frase de 22 a 33 caracteres —estimado en ~170px al tamaño de lámina y ~197px al tamaño base—. **Ninguna de las dos entra en 136px.** Ninguna familia tipográfica ni ningún tamaño lo arregla. **Decisión del fundador, 2026-09-06**: cada parte del metadato es una unidad que nunca se parte por dentro, y **si la tercera parte no entra, cae entera a la línea de abajo**. Los cuatro consumidores que tienen que cambiar juntos: la hoja del átomo, `ListingCard`, `ResultRow` (ciudad + antigüedad) y `/mis-avisos` (`· ref.`). Tiene que entrar junto con la 22.9 y la reescritura firmada por el fundador de la 14.29 **CERRADA 2026-09-07.** La regla es sólo `white-space: nowrap`, declarada una vez en `ListingMeta.module.css` (`.part`) y expuesta por un componente nuevo, `ListingMetaPart` (`ListingMeta.tsx`), que envuelve el contenido en un `<span>`. El átomo `ListingMeta` sigue sin componer la frase — no sabe qué es una "parte"; cada consumidor decide qué texto envolver, exactamente como ya decidía qué texto mostrar. Las cuatro hojas cambiaron juntas: `ListingMeta.module.css`/`.tsx` (la regla y el envoltorio), `ListingCard.tsx` (zona, habitaciones, metros), `ResultRow.tsx` (además ciudad y antigüedad, cada una dentro de su `<span className={styles.wide}>` existente) y `app/mis-avisos/page.tsx` (además `ref. LC-0912` como una sola unidad — "ref." nunca se separa de su código). El separador `· ` sigue siendo texto plano entre unidades, sin tocar: es el único lugar donde el navegador puede cortar ahora. **Pruebas**: `ListingMeta.test.tsx` prueba `ListingMetaPart` (envoltorio, `<span>` y no `<p>`, la regla CSS exacta); `ListingCard.test.tsx` y `ResultRow.test.tsx` se actualizaron para leer el texto visible (etiquetas quitadas) en vez del HTML crudo, porque el marcado ya no es la subcadena literal aunque la frase leída no cambió

## Fase 23 — El pie del sitio promete diez páginas que no existen (2026-08-31)

El fundador trajo hoy una lámina que nunca había estado en el repositorio: `design/pantallas/Rentas - Footer.dc.html`, bajada del proyecto de diseño y ya versionada acá. Dibuja un pie mínimo en los dos anchos —escritorio 1280 (`#9a`) y móvil 390 (`#9b`)— y su propia nota de diseño explica la decisión: *"Fuera las zonas y las ciudades: eso ya lo resuelve el buscador y la lista, y repetirlo abajo solo alargaba la página. Queda lo que un pie sí tiene que sostener: cómo pedir ayuda y dónde están los términos."*

**Esta fase se registra, no se ataca todavía** (decisión del fundador el 2026-08-31). Existe para que el trabajo esté nombrado con su alcance real cuando le toque, en vez de reconstruirse desde la lámina.

**Lo que la hace distinta de las otras fases:** el pie nombra diez destinos y **ninguno de los diez existe**. No es una pantalla que le falta un detalle; es una superficie nueva que aparece en TODAS las páginas y que hoy produciría diez enlaces muertos multiplicados por cada página del sitio, rastreados por Google desde todas ellas. Y cinco de esos diez son texto legal que sólo puede redactar el fundador, así que la fase no puede cerrarse sola.

- [x] 23.1 **El armazón, que es lo único embarcable sin una sola página nueva.** La marca «rentas.», la bajada —«Alquileres en Venezuela sin comisión. El dueño publica, el inquilino escribe directo.»— y la franja inferior con sus dos líneas: «© 2026 rentas.com.ve · Publicar y contactar no cuesta nada» y «rentas.com.ve no interviene en el contrato entre las partes». **Es la promesa que el pie existe para repetir**, y no necesita ningún enlace para decirla. Monta en `app/layout.tsx`, que hoy es deliberadamente plano: sin componente de cliente, sin webfont, sin JavaScript. El pie no puede ser lo primero que rompa eso **Cerrada 2026-09-04.** `components/organisms/SiteFooter.tsx` dibuja la marca, la bajada y la franja SIEMPRE, sin condicionar nada a que haya enlaces; las columnas de ayuda/legal son estrictamente condicionales a `linkGroups` (23.2) y hoy no dibujan nada. El componente no lleva `"use client"` ni un solo `useState`/`useEffect` — es HTML servido, igual que el resto del camino de lectura — y `app/layout.tsx` lo monta una sola vez dentro de `<body>`, después de `{children}`. La marca «rentas.» es HOY una tercera copia retipeada (Nav.tsx ya tenía dos) y se deja anotado por qué, con el mismo riesgo que Nav.tsx ya documenta. Probado con RED real: `components/organisms/SiteFooter.test.tsx` y `app/layout.test.tsx` no existían para este componente antes de escribirse (el segundo SÍ existía y su prueba `renders its children inside <body>` se puso roja al agregar el pie — una aserción `toContain("<body><p>content</p></body>")` demasiado literal, corregida para afirmar orden en vez de adyacencia exacta, y se agregó una prueba nueva que verifica que el pie monta). Ambos en verde: 4/4 y 3/3. Diez mutaciones corridas sobre `footer-links.ts` y `SiteFooter.tsx` (ver 23.2), ninguna en blanco al cerrar — dos salieron en blanco la primera vez (un `toContain("rentas.")` que la propia «rentas.com.ve» del pie de página satisfacía, y la ausencia de una fixture con categoría «legal») y las dos se cerraron fortaleciendo la aserción, no descartando la mutación
- [x] 23.2 **Un enlace cuyo destino no existe NO se dibuja.** Un registro en el dominio donde cada entrada declara su destino, y la pantalla dibuja sólo las que resuelven. Es AGENTS.md §7 —fallar cerrado— y es lo que permite que las diez páginas lleguen de a una sin volver a tocar el pie ni una vez. La alternativa —dibujar los diez y que nueve den 404— no es una etapa intermedia aceptable: un 404 indexado desde todas las páginas del sitio cuesta más que un pie sin enlaces. **La lista de entradas es una regla de producto, así que vive en `src/modules/`, no en el componente**: el piso de cobertura del 90 % no llega a `app/` ni a `components/` **Cerrada 2026-09-04.** `src/modules/site-footer/domain/footer-links.ts` declara `FOOTER_LINK_CATALOGUE`: las diez entradas reales que nombra la lámina, cada una con `href: null` y un comentario que apunta a la tarea que lo llena (23.4/23.6/23.7 para Ayuda, 23.5 para Legal). `resolveFooterLinks` es una función pura parametrizada sobre el catálogo — filtra `href !== null` — y `groupResolvedFooterLinks` agrupa lo resuelto por categoría en un orden fijo, descartando una categoría entera si no le queda ningún enlace (una cabecera sin nada debajo es peor que ninguna cabecera). `app/layout.tsx` resuelve UNA vez, a nivel de módulo, y pasa el resultado ya agrupado a `SiteFooter` por props — el mismo contrato que `Nav` ya usa para su cuenta: el componente no decide qué existe. **Medido, no supuesto**: `resolveFooterLinks(FOOTER_LINK_CATALOGUE)` da hoy `[]` — ninguna de las diez páginas existe —, y es una prueba explícita (`footer-links.test.ts`) pensada para ponerse roja el día que la primera exista, que es la forma correcta de que falle. Pruebas: `src/modules/site-footer/domain/footer-links.test.ts` (6, con triangulación: catálogo mixto resuelto/no-resuelto, catálogo real vacío, agrupación con 0/1/2 categorías, orden fijo ayuda-antes-que-legal) y `components/organisms/SiteFooter.test.tsx` (4). **Diez mutaciones corridas sobre las dos piezas, ninguna en blanco al cerrar** (restauración por copia + sha256 comparado antes/después, `git diff --stat` vacío tras cada una): invertir el filtro de `href`, quitar el descarte de categorías vacías, invertir `CATEGORY_ORDER`, aflojar el predicado de agrupación a `true`, resolver a mano la primera entrada del catálogo real — las cinco del dominio, todas rojas a la primera. Del lado del componente, dos salieron en blanco la primera vez y se corrigieron fortaleciendo la aserción en vez de descartar la mutación: un `toContain("rentas.")` que «rentas.com.ve» de la franja ya satisfacía (se cambió a `toContain(">rentas.</a>")`, específico del ancla de marca) y una mutación que cruzaba la etiqueta «Legal» hacia «Ayuda» sin que ninguna prueba tuviera una fixture de categoría legal (se agregó una)
- [x] 23.3 **Ya existen dos `<footer>` y ninguno es el del sitio. Hay que decidir qué pasa en esas dos rutas.** La ficha del aviso dibuja uno con el ID y la fecha de vencimiento al lado del enlace de reportar (tarea 16.35 — la primera superficie que le muestra un ID a un visitante), y el visor de fotos dibuja otro. **Dos pies apilados en la ficha es un defecto, no una suma.** Las tres salidas son: que el pie del sitio conviva debajo, que absorba lo que esos dos dicen, o que se calle en esas rutas. Ninguna es obviamente correcta y la lámina no las contempla porque dibuja el pie solo **DECIDIDA 2026-09-04, cerrada.** Se calla en las dos rutas. Las otras dos salidas no aplicaban por igual: convivir debajo apila un pie del sitio contra un `<footer>` que hoy dice el ID y el vencimiento del aviso —**eso es un dato del AVISO, no del sitio**, y ponerlo junto a «Ayuda»/«Legal» lo disuelve—, y el pie del visor de fotos **no es un pie**: es la barra de control de una pantalla inmersiva a pantalla completa, con el precio y «Ver el aviso», y su propio comentario ya dice «el precio no desaparece: se sigue decidiendo mientras se mira» — meterle debajo el pie del sitio rompe esa pantalla. **Costo aceptado**: desde esas dos rutas no hay camino a Ayuda ni a Legales a través del pie. `app/layout.tsx` es el único layout del proyecto y envuelve TODA ruta sin excepción, así que ningún descendiente puede apagar lo que un ancestro ya dibuja — `usePathname` está fuera de la mesa (sin componente de cliente, design.md D13/D14) y no había middleware. La salida elegida: `middleware.ts` (nuevo, raíz del repo) acota su `matcher` a exactamente `/alquiler/:ciudad/:zona/:slug` y `.../:slug/foto/:n` y estampa `x-hide-site-footer: 1`; `app/layout.tsx` (ahora `async`, porque `headers()` de Next 15 es asíncrona) lo lee y sólo entonces omite `<SiteFooter>` — la ausencia del header es «mostrar el pie», el default seguro para el resto del sitio. **Costo medido, no supuesto**: leer `headers()` en el layout raíz saca TODA ruta del prerenderizado estático — `/_not-found`, `/measure` y `/measure/lista` pasaron de estático (○) a dinámico (ƒ) en la tabla de build el día que esto se integró; `pnpm budget:bundle` no se mueve (110,67 KB gzip, igual que antes) porque mide el bundle de cliente, no el modo de renderizado. **Sigue de acá para la tarea 23.9**: las diez páginas de Ayuda/Legales se declaran «no tienen por qué costar una consulta» — mientras este layout lea `headers()`, ninguna de las diez puede prerenderizarse tampoco; queda anotado en `app/layout.tsx` como seguimiento conocido, no resuelto acá. Probado: `middleware.test.ts` (3, matcher exacto y estampado del header contra `NextRequest` real en las dos formas de ruta, verificado contra `x-middleware-request-x-hide-site-footer`, el mecanismo real de `NextResponse.next({request:{headers}})`) y `app/layout.test.tsx` (4, reescrito para invocar `RootLayout` directo — `await RootLayout({children})`, el mismo patrón que `app/publicar/borrador-en-la-tabla.test.tsx` ya usa para un server component async — con `next/headers` simulado igual que ese archivo simula `cookies()`; ambas direcciones probadas: el pie se dibuja en una ruta ordinaria y se calla cuando el header viene estampado, con su par positivo de que los hijos siguen dibujándose). Cinco mutaciones corridas (matriz del `matcher` recortada, valor del header invertido, condición del pie invertida, comparación del header aflojada), las cinco en rojo, restauradas por copia con sha256 verificado y `git diff --stat` vacío después de cada una. `pnpm test` 2737/2737, `pnpm typecheck` limpio, `pnpm lint` 0 errores, `pnpm lint:tokens` 0 violaciones
- [x] 23.4 **Las cinco de Ayuda: derivables del producto.** «Preguntas frecuentes», «Cómo publicar un aviso», «Cómo contactar al dueño», «Reportar un aviso» y «Escribinos». Las tres primeras salen de lo que ya está construido —los nueve pasos de publicar, la revelación del contacto con llave, el flujo de reporte— y se pueden escribir sin decidir nada nuevo. **Se listan como una sola entrada a propósito**: son contenido estático de la misma naturaleza y partirlas en cinco tareas invitaría a cinco PR por algo que es un solo cuerpo de texto **CERRADA 2026-09-05, en dos rebanadas de la cadena.** PR23a (rebanada 1 de 4) escribió las tres derivables del producto; PR23c (rebanada 3 de 4, `size:exception` aceptado por el fundador — ver el cierre de 23.7) escribió las dos que faltaban. `app/ayuda/preguntas-frecuentes/page.tsx`, `app/ayuda/como-publicar-un-aviso/page.tsx` y `app/ayuda/como-contactar-al-dueno/page.tsx` — cada una un componente de servidor sin `"use client"`, sin `readSession()` y sin consulta, montado bajo `app/ayuda/layout.tsx` (nuevo: `Nav` con `resolveNavAccount(null)` siempre anónimo — costo aceptado y anotado en el propio archivo, no un descuido — más `Container`/`FormShell`, los mismos átomos que `reportar/page.tsx` ya usa para una columna de texto). El contenido de las tres sale de código real y no de una plantilla de clasificado: los nueve pasos vienen de `app/publicar/step-copy.ts` en su mismo orden, la duración de 30 días de `listing-lifecycle/domain/expiry.ts::LISTING_LIFETIME_DAYS`, y el contacto con llave —mensaje obligatorio antes de revelar, entrar con Google o con enlace por correo, la advertencia de que rentas.com.ve no participa en la negociación— de `components/molecules/ContactBlock.tsx` y `contact-reveal/application/reveal-contact.ts`. `FOOTER_LINK_CATALOGUE` (23.2) gana sus primeros tres `href` reales: `resolveFooterLinks(FOOTER_LINK_CATALOGUE)` pasó de `[]` a las tres entradas, con `footer-links.test.ts` reescrito ANTES de tocar el catálogo para afirmar el nuevo estado real (rojo contra el catálogo viejo, verde después) — la forma correcta de que la prueba fallara, tal como la 23.2 la dejó escrita. **«Reportar un aviso» se renombró a «Cómo reportar un aviso» y «Escribinos» ganó su ruta, las dos en PR23c** (23.6/23.7 — ver esas dos entradas para el detalle, el módulo nuevo y la evidencia de esa rebanada). `FOOTER_LINK_CATALOGUE` (23.2) tiene ahora sus cinco `href` de Ayuda completos, y con los cinco de Legal (23.5) las diez entradas de la lámina resuelven. **Las cinco páginas de Ayuda existen, así que 23.4 cierra.** Evidencia de esta parte (PR23a, sin cambios): `pnpm test` 2752/2752 (RED→GREEN documentado para cada página nueva y para `footer-links.test.ts`), `pnpm typecheck` limpio, `pnpm lint` 0 errores, `pnpm lint:tokens` 0 violaciones (230 hojas, la nueva `ayuda.module.css` sin un solo literal), `pnpm build` compila las tres rutas nuevas (`ƒ`, dinámicas por el mismo costo de `headers()` que 23.3 ya aceptó y que 23.9 hereda) y `pnpm budget:bundle` sigue en **PASS** — el peor camino sigue siendo `/importar/page` en 110,67 KB gzip, sin moverse, y las tres páginas nuevas son las rutas más livianas del sitio (100,62–103,92 KB). 504 líneas autoría (493 inserciones + 11 eliminaciones), dentro del presupuesto de 400–600 pronosticado para esta rebanada
- [ ] 23.5 **Las cinco Legales. DECIDIDO el 2026-09-04: el borrador lo escribe el agente, y el fundador lo ratifica.** «Términos y condiciones», «Política de privacidad», «Uso de cookies», «Normas de publicación» y «Tratamiento de datos». **Esta tarea decía que el texto lo redactaba el fundador y que no se generaba; el fundador cambió las dos cosas al decir que no tiene abogado**, y el argumento que lo resolvió es que el borrador se escribe con el comportamiento real del producto delante —qué datos pide publicar, qué guarda `listing_report`, qué cookies existen, qué valida hoy el publicar—, mientras que un texto genérico de clasificado describiría otro producto. **Lo que NO cambia**: es exposición legal real de un clasificado que opera en Venezuela, un placeholder publicado es peor que una página ausente porque parece un compromiso, y por eso lo que se genera es un **borrador para revisión del fundador**, nunca texto final publicado sin que él lo ratifique. La tarea no se cierra con el borrador escrito: se cierra con los cinco textos ratificados. Lo que sí es trabajo de acá es la ruta, el marcado y la indexabilidad; el cuerpo llega de afuera. **«Normas de publicación» es el caso de borde**: es política de producto más que texto legal, y buena parte ya vive en el validador de publicar —lo que se puede y no se puede subir—, así que puede empezar derivada y ser ratificada, en vez de escrita de cero. **PARCIAL — PR23b (rebanada 2 de 4 de la cadena, 2026-09-05). Trabajo de esta rebanada: la ruta, el marcado y la indexabilidad de las cinco páginas, con las cinco escritas como BORRADOR pendiente de ratificación — la tarea sigue sin cerrarse.** `app/legal/layout.tsx` (nuevo, mismo dibujo que `app/ayuda/layout.tsx` pero espejado a propósito en vez de compartido — categorías distintas del pie, dueñas distintas de la copia) y `app/legal/legal.module.css`, más `app/legal/terminos/page.tsx`, `.../privacidad/page.tsx`, `.../cookies/page.tsx`, `.../normas/page.tsx` y `.../datos/page.tsx` — cada uno con su banner visible «Borrador en revisión» (`app/legal/draft-notice.tsx`) para que ningún texto se lea como un compromiso final. Cada hecho citado se verificó contra el código antes de escribirse: `publishable-listing.ts` y `uploaded-photo.ts` para «Normas de publicación», `google-profile.ts`/`enlace.ts`/`auth.ts` (cookie `authjs.session-token` por defecto de `next-auth@5.0.0-beta.32`, sin override) para «Uso de cookies» y «Política de privacidad», y `resend-lifecycle-mailer.ts`/`r2-photo-storage.ts`/Neon como los encargados reales nombrados en «Tratamiento de datos». La retención de `contact_reveal_event` se deja explícitamente sin fecha porque design.md la nombra como pregunta abierta. `FOOTER_LINK_CATALOGUE` (23.2) gana sus cinco `href` de «legal» (`footer-links.test.ts` reescrito ANTES de tocar el catálogo, rojo contra el viejo `null`, verde después). **Riesgo de publicación, dicho explícito**: fusionar sólo esta rebanada a `main` publicaría las cinco páginas con su banner de borrador visible — nunca como texto ratificado silencioso —, así que no hay exposición de un compromiso legal no revisado; el paso que sí lo cerraría es que el fundador ratifique y alguien retire `DraftNotice` de las cinco. Evidencia: `pnpm test` 2787/2787, `pnpm typecheck` limpio, `pnpm lint` 0 errores, `pnpm lint:tokens` 0 violaciones (244 hojas), `pnpm build` compila las cinco rutas nuevas y `pnpm budget:bundle` PASS (peor camino sigue en `/importar/page`, 110,67 KB gzip, sin moverse; las cinco Legales están entre las rutas más livianas, 100,63–103,93 KB). 798 líneas de autoría (775 inserciones + 23 eliminaciones), por encima del pronóstico de 500–700 pero bajo el techo duro de 800
- [x] 23.6 **«Reportar un aviso» no tiene destino posible tal como está pensada — contradicción entre la lámina y el código.** En el producto reportar es **por aviso**: `app/alquiler/[ciudad]/[zona]/[slug]/reportar/page.tsx` necesita el aviso en la dirección. **Un pie no tiene aviso.** Las salidas son: que sea una página que EXPLICA cómo se reporta y desde dónde (que es distinto de reportar), que apunte a un canal de contacto general, o que se caiga del pie. Es la misma forma que las contradicciones que la fase 20 registró: la lámina describe una intención que el código resolvió en otro nivel **DECIDIDA 2026-09-04, no construida.** Es una página estática que EXPLICA cómo se reporta y desde dónde, y la etiqueta del pie cambia a «Cómo reportar un aviso». El argumento: la etiqueta vieja prometía un VERBO imposible desde el pie, porque reportar necesita a la vez el aviso en la dirección (`.../[slug]/reportar`) y una cuenta (un reporte por cuenta, `report-threshold.ts`) — ninguna de las dos existe parada en el pie. Renombrar corrige la promesa, no sólo el enlace muerto. **Esto colapsa dentro de 23.4**: «Reportar un aviso» ya era una de sus cinco páginas de Ayuda, así que no abre una tarea nueva. Salidas rechazadas: apuntar a un canal de contacto general la hubiera dejado dependiendo de 23.7 sin necesidad; caerse del pie dejaba a quien vio una estafa y cerró la pestaña sin ningún camino. **CONSTRUIDA 2026-09-05 — PR23c (rebanada 3 de 4 de la cadena).** `app/ayuda/como-reportar-un-aviso/page.tsx` (+test) explica, y no ejecuta: de dónde sale el enlace real (`«Reportar este aviso»`, al pie de cada ficha), que exige entrar con cuenta (`reportar/actions.ts` redirige a `/signin` sin sesión), que cuenta una vez por cuenta (unicidad de `listing_report_listing_reporter_unique`) y que el umbral de ocultamiento automático es de tres cuentas distintas (`AUTO_HIDE_REPORT_THRESHOLD` en `listing-trust/domain/report-threshold.ts`) — el mismo número que la página de Términos (23.5) ya hace público, así que decirlo acá no revela nada nuevo. Sin prometer que se va a decir si un reporte puntual ocultó el aviso, la misma reserva que `reportarAviso` ya guarda. El pie gana el nuevo `href`: `/ayuda/como-reportar-un-aviso`. Evidencia combinada con 23.7 — ver el cierre de esa tarea
- [x] 23.7 **«Escribinos» necesita un canal que exista hoy.** El canal de WhatsApp lo difirió el fundador al final del proyecto (fase 18, decisión del 2026-08-29), así que no está. El correo sí: Resend está cableado y es lo que manda el enlace mágico. **Falta decidir qué pasa con un mensaje que llega** —a qué dirección, quién lo lee, si se contesta— porque un formulario de contacto que cae en un buzón que nadie abre es la clase de promesa rota que este producto no puede pagar en confianza **DECIDIDA 2026-09-04, no construida.** «Escribinos» es un FORMULARIO que envía por Resend, no un `mailto:` ni una dirección publicada. El hecho que lo decidió: el producto ya ENVÍA correo en dos flujos —`identity/infrastructure/email-provider.ts` para el enlace mágico, `listing-lifecycle/infrastructure/resend-lifecycle-mailer.ts` para los recordatorios de vencimiento— y nunca RECIBIÓ uno: no hay buzón, no hay tabla de mensajes, no hay camino de entrada. El formulario gana sobre `mailto:` porque no expone una dirección a los scrapers y funciona para quien no tiene cliente de correo configurado. Lo que obliga: validación de entrada, protección contra spam y un camino de salida nuevo. **La dirección de destino es configuración, no código**: una variable de entorno nueva junto a `AUTH_MAIL_FROM`, que falla cerrado si falta (AGENTS.md §7), la misma forma que ya usa `AuthMailerNotConfiguredError` **CONSTRUIDA 2026-09-05 — PR23c (rebanada 3 de 4 de la cadena), `size:exception` aceptado por el fundador.** Módulo nuevo `src/modules/site-contact/`: `domain/contact-message.ts` (+test) evalúa la entrada de forma pura — la trampa (`honeypot`) se revisa primero y corta corto, reportada aparte de los errores reales para que un script no aprenda qué otro campo también rompió; guarda contra inyección de encabezados rechazando `\r`/`\n` en nombre y correo, porque ambos terminan en el asunto y el `replyTo` del correo compuesto — y `domain/contact-screen.ts` (+test) espeja `report-screen.ts`: POST → redirección → GET, `?enviado` dibuja el acuse, `?error` sólo es alcanzable posteando directo, sin pasar por la validación HTML5 nativa del formulario. `application/ports/contact-mailer.port.ts` y `send-contact-message.ts` (+test) conectan las dos mitades. `infrastructure/resend-contact-mailer.ts` (+test) es la variable nueva: **`CONTACT_MAIL_TO`**, la única que este módulo agrega — **reusa `RESEND_API_KEY` y `AUTH_MAIL_FROM`** en vez de declarar un cuarto remitente propio, una desviación deliberada del patrón «cada mailer tiene su `_FROM`» que `resend-mailer.ts`/`resend-lifecycle-mailer.ts` siguen, porque la tarea en sí nombra una sola variable nueva. Falla cerrado exactamente como `AuthMailerNotConfiguredError`: sin `RESEND_API_KEY`/`AUTH_MAIL_FROM`/`CONTACT_MAIL_TO`, `ResendContactMailer` tira al construirse — construido en `actions.ts`, en el envío y no al cargar el módulo, y ANTES de evaluar la entrada, así que una falla de configuración es igual de cerrada para un mensaje válido, uno inválido o uno de un bot. `app/ayuda/escribinos/page.tsx` (+test) dibuja un `<form action={sendContactMessageAction}>` nativo (sin `method="post"` propio — React lo infiere de una `action` función, igual que `reportar/page.tsx`), tres campos `Field` con validación HTML5 real (`required`, `type="email"`, `minLength`/`maxLength`, sin una línea de JavaScript de cliente) y la trampa de spam: un campo `sitioWeb` oculto con `clip`/`aria-hidden`/`tabIndex={-1}` — nunca `display:none`, que algunos rastreadores de formularios ignoran a propósito. `actions.ts` (+test) es el cable hacia `sendContactMessage`. El pie gana el `href`: `/ayuda/escribinos`. **El presupuesto de revisión, dicho explícito**: esta rebanada (23.6+23.7 juntas, como el fundador pidió entregarlas) midió **1.227 líneas de autoría (1.211 inserciones + 16 eliminaciones)** contra el techo duro de 800 pronosticado — 52 % por encima. El sobrecosto se registra en vez de absorberse en silencio, igual que PR1b-a lo hizo con sus 734 líneas contra 400. Su forma: 23.6 sola mide ~150 líneas (una página estática); el resto es 23.7 — un módulo de correo nuevo construido con el mismo rigor hexagonal de tres capas que `identity` y `listing-lifecycle` ya tienen, cada capa con su propia prueba, más la pantalla del formulario y su acción de servidor, también con las suyas. Ningún split cohesivo lo baja de 800: 23.7 sola ya lo supera. `size:exception` aceptado por el fundador el 2026-09-05, después de ver el conteo exacto y las alternativas — la sesión de apply se detuvo primero y reportó en vez de entregar una rebanada sobredimensionada sin avisar, tal como este cambio exige. Evidencia (23.6+23.7 juntas, verificada después de commitear): `pnpm test` 2834/2834, `pnpm typecheck` limpio, `pnpm lint` 0 errores, `pnpm lint:tokens` 0 violaciones (251 hojas), `pnpm build` compila las dos rutas nuevas, `pnpm budget:bundle` PASS (peor camino sigue en `/importar/page`, 110,67 KB gzip, sin moverse; las dos rutas nuevas entre las más livianas, 103,93–104,18 KB)
- [x] 23.8 **El presupuesto de bytes, medido y no supuesto.** El peor camino de lectura está hoy en **109,34 KB gzip contra un techo de 130** (`pnpm budget:bundle`), o sea unos 20 KB de aire. Un pie en TODAS las páginas se come parte de ese aire en todas a la vez, no en una. Se mide antes y después, y el número va en el PR: la lámina de escritorio declara que el pie mide 210 px de alto y «no compite con el contenido», que es una afirmación de geometría verificable con `tests/measure/` **Cerrada 2026-09-04.** **Antes**: 109,34 KB gzip (`/importar/page`, cifra ya citada arriba). **Después de montar `SiteFooter` en `app/layout.tsx`** (`pnpm build && pnpm budget:bundle`, corrido de verdad sobre esta rama): el peor camino sigue siendo `/importar/page`, ahora en **110,67 KB gzip contra el mismo techo de 130** — **+1,33 KB**, y quedan **19,33 KB de aire** contra los ~20 que había. `SiteFooter` no lleva `"use client"` ni estado, así que el delta no es JavaScript de este componente; no se investigó más a fondo porque el gate sigue en verde con margen de sobra y esa investigación no es parte de esta tarea — queda anotado para quien lo mire después. `pnpm budget:bundle` da **PASS** con las 13 rutas del camino de lectura. **La geometría, medida y no asumida**: la producción real hoy resuelve a CERO grupos de enlaces (23.2), así que el pie que se sirve HOY es más chico que los 210 px de la lámina — no hay nada de qué preocuparse ahí. Para poder medir la afirmación de 210 px de la lámina ANTES de que exista un solo destino real, `app/measure/page.tsx` monta el mismo `SiteFooter` con las diez entradas reales ya resueltas (fixture, con destinos que ningún archivo del repositorio sirve). `tests/measure/footer.spec.ts` mide esa versión con los diez enlaces: **243,7 px de alto a 1280px** contra el techo generoso de 260px que la prueba fija (no un match exacto: la lámina se dibujó con la métrica de fuente de la herramienta de diseño, y esto lee la pila de sistema real, design.md D13). 243,7 no es 210, pero **la promesa que la afirmación protege —que el pie no compite con el contenido— se sostiene igual**: es una franja angosta contra cualquier alto de página real. Evidencia: `pnpm test:measure` con `tests/measure/footer.spec.ts`, 2/2, y las 71 pruebas de medición existentes siguen en verde tras tocar `app/measure/page.tsx`
- [x] 23.9 **Diez páginas nuevas indexables, en un sitio cuyo camino de lectura entero se renderiza en servidor.** Son contenido estático y público: no llevan sesión, no llevan JavaScript y no tienen por qué costar una consulta. Falta decidir si entran al `sitemap`, con qué frecuencia declarada, y si alguna debe quedar fuera del índice. **Y hay una interacción con el pie**: enlazar diez páginas desde todas las páginas del sitio reparte autoridad de enlace hacia contenido que no es catálogo, que es exactamente lo contrario de lo que las páginas de zona buscan **DECIDIDA 2026-09-04, no construida.** Las cinco de Ayuda entran al `sitemap` con frecuencia **mensual**; las cinco Legales quedan **fuera del `sitemap` pero siguen indexables**; **ninguna de las diez lleva `noindex`**. Razón: Ayuda contesta búsquedas reales y es superficie de adquisición, Legales es superficie de confianza. Por qué se rechazó `noindex` en Legales: esconder términos y privacidad del índice se lee como evasivo para un clasificado con exposición legal real en Venezuela — el mismo argumento que 23.5 usa contra un placeholder. **Costo de implementación registrado**: `changeFrequency` en `src/modules/listing-discovery/domain/sitemap.ts:46` es hoy la unión `"daily" | "weekly"`, así que las cinco entradas mensuales de Ayuda necesitan ensanchar esa unión, con su propia prueba — un cambio de dominio, no un literal suelto en una página **CONSTRUIDA 2026-09-05 — PR23d (rebanada 4 de 4 de la cadena, la última de la fase 23).** El costo registrado se verificó contra el código antes de tocarlo: la línea era exactamente esa, y seguía siendo la unión de dos miembros. `changeFrequency` amplía a `"daily" | "weekly" | "monthly"`, con su propio comentario que explica por qué `monthly` es la elección — páginas de contenido estático sin fecha de modificación natural, no una `"weekly"`/`"daily"` prestada que exagere cuánto cambian. El resto de la tarea ya estaba en verde sin escribir una línea: **las diez páginas de Ayuda/Legales ya declaraban `metadata.robots` ausente** desde que cada una se escribió (23.4/23.5/23.6/23.7), cada una con su propia prueba `"is indexable — the page carries no noindex directive"` — no había ningún `noindex` que quitar, la ratificación pendiente de 23.5 nunca se jugó con ese mecanismo. Lo que sí faltaba era la mitad `sitemap`: `ayudaSitemapPaths` (nueva, en el mismo archivo de dominio, con su propia prueba) es la función completa que decide qué entra — lee `FOOTER_LINK_CATALOGUE` (23.2) y se queda sólo con la categoría `"ayuda"` con `href` resuelto, así que una página de Ayuda que aún no exista (`href: null`) no puede aparecer en el sitemap antes de existir, la misma garantía que este archivo ya le da a las páginas de zona. Las Legales nunca entran a esa función porque su categoría no es `"ayuda"` — no hay una excepción que codear, la ausencia es estructural. `buildSitemap` gana un tercer parámetro opcional, `ayudaPaths` (default `[]`, así que la firma vieja de dos argumentos sigue compilando sin tocar ningún llamador existente), y las emite sin `lastModified` — mismo argumento que ya usa el inicio del sitio cuando no hay avisos: inventar `new Date()` volvería la función impura y le mentiría a Google sobre cuánto cambian. **La decisión entera queda en dominio, no en `app/sitemap.ts`**: ese archivo pasa `FOOTER_LINK_CATALOGUE` sin filtrarlo ni decidir nada — es la misma regla permanente del fundador que el propio `sitemap.ts` ya cita en su comentario de cabecera, y que este archivo de tests confirma en la práctica: dos pruebas nuevas prueban `ayudaSitemapPaths` sola —conserva una entrada de Ayuda resuelta, descarta una de Legal resuelta, y descarta una de Ayuda con `href: null`— y tres prueban `buildSitemap` con el tercer argumento: una ruta de Ayuda publica una entrada mensual sin fecha, las entradas de avisos/zonas siguen intactas cuando también hay rutas de Ayuda presentes, y sin ese argumento el comportamiento viejo no cambia (sólo el inicio, como antes). **Sobre la interacción con el pie que la tarea nombra** (repartir autoridad de enlace hacia contenido que no es catálogo): sigue siendo contexto de la decisión ya tomada, no una decisión nueva — el pie de 23.1/23.2 no se tocó, y esta rebanada no le agrega `nofollow` ni cambia su enlazado; si el pie construido contradice esa lectura, queda anotado como observación, no resuelto acá. RED→GREEN real: las cinco pruebas nuevas de `sitemap.test.ts` referenciaban `ayudaSitemapPaths` (que no existía) y una firma de tres argumentos de `buildSitemap` (que tampoco existía) antes de escribir el código — las 4 fallaron en rojo con `TypeError: ayudaSitemapPaths is not a function` y una comparación de arreglo incompleta, confirmado antes de implementar. Evidencia: `pnpm test` 2839/2839, `pnpm typecheck` limpio, `pnpm lint` 0 errores (los mismos 11 avisos/2 infos preexistentes, no tocados por esta rebanada — verificado comparando antes/después del cambio), `pnpm lint:tokens` 0 violaciones, `pnpm build` compila las 37 rutas sin una nueva, `pnpm budget:bundle` **PASS** sin moverse — el peor camino sigue en `/importar/page`, 110,67 KB gzip contra el techo de 130, el mismo número que 23.4/23.8 ya midieron, porque `sitemap.ts` no forma parte del bundle de cliente. **114 líneas de autoría (110 inserciones + 4 eliminaciones)**, dentro del pronóstico de 150–250 para esta rebanada y muy por debajo del techo duro de 800. **Con esta tarea cerrada, la fase 23 queda con una sola tarea abierta: 23.5, bloqueada en la ratificación del fundador sobre los cinco textos legales — nada más en esta fase depende de trabajo de implementación.**
- [x] 23.10 **La geometría táctil del móvil, que la lámina dice explícitamente.** Su nota declara «filas de 44 px, la misma altura tocable del resto del móvil» y «sin acordeón: diez enlaces se leen de una». Los 44 px son `--target-min`, ya tokenizado. **La ausencia de acordeón es una decisión, no un descuido** —la lámina la argumenta: esconder diez enlaces detrás de un toque no ahorra nada real— y merece quedar afirmada, porque es justo la clase de cosa que un rediseño posterior «mejora» sin saber que fue elegida **Cerrada 2026-09-04.** `components/organisms/SiteFooter.module.css` da a `.link` `min-block-size: var(--target-min)` en móvil, y lo saca (`min-block-size: auto`) sólo dentro de `@media (min-width: 768px)` — la lámina de escritorio no declara alto mínimo en sus anclas, sólo la de móvil lo hace. Ningún `<details>` ni acordeón en ningún lado del archivo. **Declarado no es dibujado**, así que se midió con Playwright y no se afirmó por la hoja: `tests/measure/footer.spec.ts` monta el pie con las diez entradas reales (fixture, `app/measure/page.tsx`, ver 23.8) a 360px y lee `getBoundingClientRect()` de cada una de las diez filas — las diez miden **exactamente 44px**, con `toBe(44)` y no una cota inferior, la misma lección que 16.24 ya dejó escrita en `layout.spec.ts` para un valor ya decidido: una cota inferior deja pasar cualquier respuesta que la cumpla, y acá el número ya está elegido. Y la ausencia de acordeón se verificó dos veces: `footer.querySelector("details")` da cero, y las diez filas están en el DOM y con caja medible A LA VEZ — no una detrás de otra que un `count()` conjunto podría confundir con una lista colapsada. Evidencia: `tests/measure/footer.spec.ts`, prueba «23.10», 1/1, con las diez alturas impresas en el log
- [x] 23.11 **Ninguna hoja de este pie escribe un valor literal.** `SISTEMA.md` lo pide sin excepción: ningún componente escribe un hex, un radio ni un tamaño; todo lee variables CSS, de modo que cambiar `data-theme` y `data-layout` en el `<html>` repinte la aplicación entera. La lámina viene con estilos en línea porque es un prototipo de la herramienta de diseño —su propio README lo dice: «nada de eso debe llegar a producción»—, así que **portarla es traducir cada valor a su token, no copiar el marcado**. `pnpm lint:tokens` es la red, y ya cazó un token equivocado antes **Cerrada 2026-09-04.** Ocho tokens nuevos en `src/styles/tokens.css` — `--footer-brand-fs`/`-desktop`, `--footer-tagline-fs`/`-desktop`, `--footer-heading-fs`, `--footer-link-fs`/`-desktop`, `--footer-legal-fs` —, uno por cada tamaño de letra que la lámina 9a/9b dibuja (`lint:tokens` trata `font-size` como regla ciega: CUALQUIER literal se rechaza). Ninguno reutiliza `--nav-brand-fs-desktop` aunque coincida en 19px con el de escritorio: son componentes distintos y un valor que coincide no es el mismo token — la lección que el propio archivo ya deja escrita para `--brandbar-h`/`--nav-h`. Sí se reutilizan a propósito dos tokens existentes que SON el mismo papel: `--brand-ls` (la marca «rentas.» ya usa esa letra-espaciada en `Nav`, y es la misma palabra) y `--target-min` (44px, ya decidido para todo el móvil — 23.10). Colores por `var(--ink)`/`var(--soft)`/`var(--bg)`/`var(--line)`, ninguno literal. `gap`/`padding`/`letter-spacing` de cabeceras quedaron en px literal a propósito: `lint:tokens` es una regla ciega sólo para color/radio/tamaño-de-letra y una regla dirigida para ancho/alto de miniatura — `Container.module.css` y `Nav.module.css` ya escriben esos mismos literales sin token, y tokenizar ahí no habría cerrado nada que el gate proteja. Evidencia: `pnpm lint:tokens` da **0 violaciones** — «221 hojas de componente escaneadas… ningún valor literal encontrado; el contrato de tema se sostiene… y cada token declarado tiene un lector» —, corrido después de agregar `SiteFooter.module.css` a las 220 hojas que ya existían
- [x] 18.29 **(nueva — la dependencia que `app/publicar/draft.ts` dejó anotada el día que se escribió.) El borrador de publicar tiene tabla, con la sesión como llave.** `publish_draft` (migración `0018_mixed_sunset_bain.sql`, aditiva: un `CREATE TABLE` y el alta de su propia clave foránea, ni un `DROP` ni una fila viva tocada), el puerto `PublicationDraftStorePort` de tres preguntas —`load`, `save`, `discard`— y su adaptador `DrizzlePublicationDraftStore`. **Cuatro decisiones, todas argumentadas donde viven:** (1) **la primaria es `publisher_id` sola**, así que «empezar una publicación nueva descarta la anterior» lo garantiza Postgres y no un `if` que alguien recuerde escribir — probado violándolo, no leyendo el DDL; (2) **dos `jsonb` y `expires_at` como columna de verdad**, porque el barrido de la 18.32 tiene que poder preguntar cuáles vencieron y leer sus claves de foto sin traer la descripción de 1.200 caracteres de cada fila; (3) **el vencimiento es un instante absoluto y no `updated_at + 24h`**, y la ventana vive en `domain/draft-expiry.ts` para que cambiarla sea código y no una migración que además vencería hacia atrás filas ya escritas — `timestamptz`, como todo instante de este esquema; (4) **el puerto no tiene variante sin filtro** y el barrido no entra en él (AGENTS.md §3). **Sin llamador a propósito**: cambiar el flujo de la cookie a la tabla es la 18.30 y se hizo aparte. Pruebas: `src/modules/listing-publication/domain/draft-expiry.test.ts` (5) y `tests/integration/publication-draft-store.test.ts` (5, contra Postgres real). **Diez mutaciones corridas, ninguna en blanco** (ocurrencia única verificada antes, sha256 comparado después, restauración por copia y `git diff --stat` limpio); cada una pone roja exactamente la prueba que la nombra y **las diez, con estos dos archivos fuera del árbol, no ponen roja NINGUNA de las 2.382 unitarias ni de las 315 de integración restantes** — o sea que nada de esto estaba cubierto por otra cosa. **Lo que quedó fuera del techo de 400 líneas, dicho antes de abrir el PR y no después**: la prueba de integración que afirma que la consulta del barrido (`jsonb_path_query_array(photos, '$[*].key')` filtrando por `expires_at`) devuelve las claves sin traer `answers`. Se corrió en verde antes de sacarla; le toca a la 18.32, que es donde esa consulta va a vivir
- [x] 18.30 **(nueva — la rebanada peligrosa que la 18.29 dejó preparada.) Cambiar el flujo de publicar de las dos cookies a `publish_draft`.** `readDraft`/`writeDraft` de `app/publicar/actions.ts`, más `app/publicar/fotos/actions.ts` y `publication-context.ts`, dejan de leer `cookies()` y pasan por el puerto con `session.userId`. **Lo que hay que decidir y no está decidido**: qué le pasa a quien está a mitad de publicar con una cookie viva el día que esto shipea. La cookie tiene el borrador entero y la tabla está vacía, así que un corte seco le vacía el formulario; la salida barata es que el primer `load` que devuelva `null` lea la cookie como respaldo, la guarde en la tabla y la borre — un puente de una entrega que se saca en la siguiente. **Y hay que borrar las dos cookies al terminar**, o quedan como una segunda fuente del mismo borrador. Con la tabla, `DRAFT_TTL_SECONDS` (30 min) deja de gobernar y pasa a hacerlo `DRAFT_LIFETIME_MS` (24 h) — **CIERRA el 2026-09-01, y la decisión que faltaba se tomó: el puente existe y borra.** `readPublicationDraft` / `savePublicationDraft` / `discardPublicationDraft` (`src/modules/listing-publication/application/publication-draft-session.ts`) son las tres únicas puertas al borrador, y las cuatro rutas de `app/publicar` pasan por ellas con `session.userId`: `submitStep`, `publishFromReview`, `requestUploadTargets` y `readPublicationContext`, que **ahora exige `publisherId` en la firma** — dibujar el flujo sin saber de quién es el borrador dejó de ser posible, que era justo lo que la cookie permitía. **Tres decisiones, argumentadas donde viven:** (1) **la tabla gana y la cookie es sólo respaldo de LECTURA**, porque al revés una cookie vieja del mismo navegador pisaría en silencio lo que la persona acaba de guardar — y porque migrar en la lectura es imposible: `readPublicationContext` corre en un componente de servidor, donde Next no deja tocar cookies, así que la migración ocurre sola en el primer guardado; (2) **toda escritura de la tabla borra LAS DOS cookies, y sólo DESPUÉS de que la fila existe**: borrarlas primero deja a quien publica sin ninguna de las dos fuentes por una falla de la base en la que no tuvo parte; (3) **la fila se normaliza igual que la cookie** — `normaliseStoredDraft` se mudó de `app/publicar/draft.ts` a `domain/stored-draft.ts` y hoy es UNA regla con dos puertas, porque una fila no viene de un atacante sino del formulario de ayer, y falla cerrado POR CAMPO (AGENTS.md §7): se pierde un paso, no los nueve. **Defecto pre-existente encontrado, no introducido**: el `publishFromReview` anterior hacía `store.delete(DRAFT_COOKIE)` sin `path`, y las dos cookies se escribieron bajo `/publicar` — un borrado sin `path` pone una cookie vencida en `/` y deja viva la de `/publicar`, o sea exactamente la segunda fuente que esta rebanada existe para cerrar, ya en el código. Ahora se borran con `{ name, path: "/publicar" }`. **`DRAFT_TTL_SECONDS` se fue con su escritor**: nada vuelve a escribir una cookie, así que `serialiseStoredDraft` y `DRAFT_COOKIE_OPTIONS` se borraron con él — una constante de vencimiento sin nadie que la aplique es una regla que miente. Pruebas: `domain/stored-draft.test.ts` (13), `application/publication-draft-session.test.ts` (10), `app/publicar/borrador-en-la-tabla.test.tsx` (6, sobre los BYTES SERVIDOS de la ruta y no sobre el componente) y `tests/integration/publication-draft-session.test.ts` (3, contra Postgres real, porque «una fila con la forma de ayer» con un doble es un objeto que escribe la prueba y contra Postgres es un `jsonb` que volvió de una columna). **Diez mutaciones corridas, ninguna en blanco** (ocurrencia única verificada antes, sha256 comparado después, restauración por copia y `git diff --stat` limpio), y cada una pone roja exactamente la prueba que la nombra; **nueve de las diez, con estos archivos fuera del árbol, no ponen roja NINGUNA de las 2.377 unitarias ni de las 320 de integración restantes** — la décima (la descripción vacía) pone roja una, la de la puerta de la cookie, que es la prueba de que la regla es una sola. **Lo que NO cierra y queda como 18.34**: qué ve quien vuelve pasadas las 24 horas. **Corregido el 2026-09-01, antes de mergear: el puente NUNCA EXISTIÓ en el árbol que se entrega.** El fundador avisó que el sitio no está publicado y nadie publica nada, así que no había a quién proteger, y la 18.33 lo sacó en esta misma rama. Lo que shipea es la mitad sin puente: la tabla es la única fuente, ninguna de las cuatro rutas toca `cookies()`, y las decisiones (1) y (2) de arriba —el orden tabla-primero y el borrado con `path`— quedaron sin objeto junto con él. La decisión (3) sí shipea, y es la que sobrevive: `normaliseStoredDraft` sobre cada fila
- [x] 18.31 **(nueva — la decisión que la 18.29 devolvió al fundador en vez de tomar.) ¿Sigue el tope de la descripción en 1.200 caracteres?** `app/publicar/draft.ts` lo decía textual: con el borrador en una cookie, 1.200 caracteres acentuados son ~3.200 en base64url y no entran al lado del resto en los ~4 KB que un navegador acepta, así que el tope pasó a ser **«una restricción técnica, no una preferencia de producto»**. Con `publish_draft` esa restricción desapareció: una columna `jsonb` no tiene ese techo. **No se cambió nada**, porque volver a ser una pregunta de producto significa que la contesta el fundador. Si se sube, lo que se toca es `MAX_DESCRIPTION_CHARACTERS` en `publishable-listing.ts`, el contador del paso 7 y la copia de `description.tooLong`; ninguna migración, porque `listing.description` es `text` — **CONTESTADA el 2026-09-01: el tope SE QUEDA en 1.200.** Se le preguntó al fundador el mismo día en que la 18.30 quitó la restricción técnica, y la respuesta fue que no sube. `MAX_DESCRIPTION_CHARACTERS` no se toca, el contador del paso 7 tampoco y la copia de `description.tooLong` tampoco. **La pregunta está contestada, no diferida**: con la columna `jsonb` el tope volvió a ser una preferencia de producto, y la preferencia es 1.200
- [x] 18.32 **(nueva — lo que el fundador pidió el 2026-09-01 y para lo que la 18.29 era el prerequisito.) El barrido de las 24 horas.** Un trabajo programado que borra los borradores vencidos y **sus fotos de R2**, que es la mitad que da valor: hoy esas derivadas son huérfanas que la 18.23 no puede alcanzar. Lo que necesita y esta rebanada no construyó: un puerto de lectura propio al lado del de la 18.29 —«cuáles vencieron, de quien sea», que es exactamente la variante sin filtro que aquél no tiene—, el índice sobre `expires_at` junto con ese llamador, el permiso de borrado sobre el bucket que la 18.23 evitó pedir, y la ruta con `CRON_SECRET` que falla cerrada (AGENTS.md §7). **Orden obligatorio: leer las claves ANTES de borrar la fila**, porque la fila es lo único que las nombra. **Y un agujero conocido**: `publish_draft` es `ON DELETE cascade`, así que borrar una cuenta se lleva la fila y con ella las claves de sus fotos, que el barrido nunca verá. **Y dos cosas más que la 18.30 dejó medidas para acá, antes de que nadie las descubra construyendo:** (a) **el barrido sólo alcanza el ÚLTIMO estado guardado de un borrador**, porque `save` es un upsert que PISA la columna `photos` — quien sube una foto en el paso 8, la quita y guarda otra deja la primera huérfana en el acto, sin fila que la nombre, igual que antes de la 18.29; (b) **publicar borra la fila sin tocar R2** (`discardPublicationDraft`), que es correcto para las promovidas —`listing_photo` las referencia— y deja igual que antes las `incoming` que nadie adjuntó, o sea la mitad original de la 18.23 **CIERRA el 2026-09-01.** Lo que esta tarea nombraba, pieza por pieza: el puerto de lectura propio es `ExpiredPublicationDraftsPort.listExpired(now)` en `application/ports/expired-publication-drafts.port.ts` —**al lado del de la 18.29 y no adentro** (AGENTS.md §3): aquél tiene por invariante que ningún método suyo lee la fila de otra cuenta y éste pregunta justamente lo contrario, así que también su adaptador es una clase aparte, `DrizzleExpiredPublicationDrafts`—; el índice es `publish_draft_expires_at_idx` (migración `0019_minor_violations.sql`, **aditiva: un solo `CREATE INDEX` sobre una columna que ya existe**, sin `DROP`, `TRUNCATE`, `DELETE`, `ALTER … TYPE` ni `NOT NULL`, así que no lleva ni necesita el marcador `allow-destructive`); el caso de uso es `sweepExpiredDrafts`; y la ruta es `app/api/jobs/draft-sweep/route.ts`, con la MISMA puerta que los otros dos trabajos —`isAuthorizedJobRequest`, tiempo constante, cerrada sin `CRON_SECRET`— y `GET` además de `POST` porque el cron de Vercel dispara `GET`. **El permiso de borrado sobre el bucket NO hubo que pedirlo, y la premisa era falsa**: `PhotoStoragePort.remove` existe desde D12, `purgeExpiredPhotos` (19.4) ya lo usa contra este mismo bucket y `processUploadedPhoto` lo llama en cada publicación para tirar el original. Las credenciales de R2 ya necesitan `DeleteObject` desde entonces; lo que sigue sin existir —y sigue sin hacer falta— es `list`: el barrido trabaja desde filas, jamás enumerando el bucket. **El orden se AFIRMA, no se comenta**: `sweep-expired-drafts.test.ts` (5 pruebas) pasa todo por un registro de llamadas y compara el arreglo literal `["listExpired", "remove:…", "remove:…", "discard:…"]`, porque «se llamó a las dos cosas» no distingue leer las claves antes de borrar la fila de leerlas después, que es cuando ya no existen. **Y qué pasa cuando R2 rechaza, decidido y probado: la fila SOBREVIVE.** Se desvía a propósito de `purgeExpiredPhotos`, que borra las filas aunque falle el objeto: allá la clave también vive en `listing_photo_derivative` de un aviso que la pantalla dibuja, acá la fila es el único registro que existe (AGENTS.md §7). Una fila vencida es invisible —`load` filtra por `expires_at > $ahora`— y pesa unos cientos de bytes, así que conservarla cuesta eso y la corrida de mañana la vuelve a nombrar; borrarla cuesta los bytes para siempre. Se prueba con su par positivo —el segundo borrador de la misma corrida sí se barre entero—, sin el cual un barrido MUERTO pasaría las dos. Integración contra Postgres real en `tests/integration/publication-draft-store.test.ts` (2 nuevas): que `jsonb_path_query_array(photos, '$[*].key')` saque las claves de un `jsonb` de verdad **sin traer `answers`** —se afirma con `toEqual` sobre el objeto entero, así que empezar a traer la descripción de 1.200 caracteres pone rojo— y que el borde sea el complemento EXACTO del de `load`: en el instante justo el barrido ve la fila y quien vuelve ya no. **La ventana, dicha y no supuesta**: cron propio en `vercel.json` a las `0 5 * * *`, aparte del de recordatorios (`0 13 * * *`) para que una falla no tape a la otra. No monta el mismo horario y no puede correr más seguido: el plan Hobby de Vercel corre cada cron una vez por día, así que un borrador abandonado se limpia entre 24 y 48 horas después. Si esa cola importa, la salida es más frecuencia y nunca acortar `DRAFT_LIFETIME_MS`, que vencería hacia atrás borradores escritos con la promesa de veinticuatro horas. **Corrección al enunciado, medida y no supuesta — lo que el barrido borra son objetos `incoming/`, NO derivadas promovidas.** La 18.29 y la 18.30 dicen que un abandono deja «hasta seis fotos ya promovidas a cinco derivadas WebP»; el código dice otra cosa. `app/publicar/fotos/actions.ts` firma un PUT contra `incoming/<cuenta>/<token>`, el paso 8 guarda ESA clave en la fila, y la promoción a `photos/` ocurre recién al publicar, dentro de `publishListing → processUploadedPhoto`. La prueba es doble: `app/publicar/actions.ts:166` le pasa `incomingKey: photo.key` a publicar, y `process-uploaded-photo.ts:137` RECHAZA cualquier clave que no esté bajo `incoming/`, así que si la fila guardara una promovida publicar fallaría con `key.notOwnedByPublisher`. Consecuencia práctica: el valor único de este barrido es **la fila** —que ninguna regla del bucket puede borrar— y la prontitud; los objetos también los alcanza la regla de ciclo de vida de la 18.23. Y `remove` sobre una clave que la regla ya se llevó no lanza, porque el `DeleteObject` de S3 es idempotente, así que la negativa de arriba no se traba. **Las tres limitaciones del enunciado siguen ciertas y ninguna se tapó**: (a) el barrido sólo alcanza el ÚLTIMO estado guardado, porque `save` pisa `photos` entera; (b) `ON DELETE cascade` se lleva la fila al borrar la cuenta; (c) publicar borra la fila sin tocar R2. Las tres dejan objetos bajo `incoming/`, así que la regla de ciclo de vida que el fundador configuró hoy las cubre — dicho con la evidencia del párrafo anterior, no por optimismo. **Diez mutaciones corridas, ninguna en blanco** (una ocurrencia exacta verificada antes, sha256 comparado después, restauración por copia de bytes con sha256 idéntico al original y `git diff --stat` limpio), y cada una pone roja la prueba cuyo título la nombra. **La medida del hueco: las diez, con estos archivos fuera del árbol, no ponen roja NINGUNA de las 2.375 unitarias ni de las 306 de integración restantes**
- [x] 18.33 **(nueva — el puente que la 18.30 construyó para sacarlo, escrito acá para que no se vuelva permanente por olvido.) Borrar las dos cookies del borrador y todo lo que las sostiene.** Se van enteros `app/publicar/draft.ts`, `app/publicar/legacy-draft-cookies.ts`, `LegacyPublicationDraftPort` y el parámetro `legacy` de `PublicationDraftDependencies`; `readPublicationDraft` queda en `normaliseStoredDraft(await store.load(...))` y `save`/`discard` dejan de tener una segunda mitad. **Cuándo**: una entrega después de que la 18.30 esté en producción, que es cuando el borrador de cookie más nuevo posible ya venció — la cookie duraba 30 minutos, así que el plazo real es de horas y no de días, y el criterio para disparar es que haya pasado un despliegue completo. **Por qué está escrito como tarea y no como comentario**: un puente que nadie saca deja para siempre dos fuentes del mismo borrador, y el orden actual —tabla primero— es lo único que hoy impide que la vieja gane. Lo que se conserva es `normaliseStoredDraft`, que ya no es del puente: es la regla que valida la fila — **CIERRA el 2026-09-01, y cierra POR SUSTRACCIÓN: el puente se sacó sin haber servido a nadie.** La razón la dio el fundador el mismo día: **el sitio no está publicado y nadie publica nada**, así que el día del despliegue no hay ningún borrador de cookie vivo que rescatar y el puente era código muerto desde su primera línea. Se fueron `app/publicar/legacy-draft-cookies.ts` entero, `LegacyPublicationDraftPort` entero, el campo `legacy` de `PublicationDraftDependencies`, el respaldo de lectura de `readPublicationDraft`, los dos `legacy.clear()` de `save`/`discard`, y lo que quedó varado con ellos: `DRAFT_COOKIE`, `DRAFT_TEXT_COOKIE`, `DRAFT_COOKIE_PATH`, `parseStoredDraft`, su `decode` de base64url y `app/publicar/draft.test.ts` completo. **Corrección al texto de arriba**: `app/publicar/draft.ts` NO se fue entero. Le quedaban dos cosas que no eran del puente —`emptyDraft()` y el alias `StoredDraft`— y ahí se mudó `publicationDraftDependencies()`, que era lo único de `legacy-draft-cookies.ts` que no hablaba de cookies: el archivo pasó de 70 líneas a 33 y hoy es la raíz de composición del borrador. **Lo que se conserva, y por una razón que nunca fue del puente**: `normaliseStoredDraft` corre sobre CADA fila que vuelve de la tabla, porque el tipo del puerto promete una forma y la columna `jsonb` no la garantiza — una fila la escribió el formulario de ayer. **El defecto pre-existente del `delete` sin `path` quedó SIN OBJETO, no sin arreglar**: ya no hay ninguna llamada a `cookies()` en todo `app/publicar`, así que no queda borrado que pueda olvidar su `path`. Lo que lo sostiene es una aserción y no una ausencia: `app/publicar/borrador-en-la-tabla.test.tsx` espía el frasco de cookies y afirma `get`, `set` y `delete` **nunca llamados** en las tres puertas —servir un paso, guardar un paso, publicar—, cada una pareada con su positivo (los bytes servidos dibujan el título de la fila y `name="title"`; `save` recibió lo tecleado; `discard` recibió la cuenta), porque un negativo sobre una pantalla rota pasa solo. Pruebas: `application/publication-draft-session.test.ts` (6, eran 10), `app/publicar/borrador-en-la-tabla.test.tsx` (4, eran 6), `tests/integration/publication-draft-session.test.ts` (3, sin el doble del puente) y `domain/stored-draft.test.ts` (13, intactas). **Ocho mutaciones corridas** —las cinco que sobreviven de la rebanada anterior más tres del código nuevo— (ocurrencia única verificada antes, sha256 comparado después, restauración por copia y `git diff --stat` limpio), cada una en rojo por la prueba que la nombra y ninguna que dependiera sólo de una prueba del puente: **ninguna quedó sin cazar por la sustracción**
- [x] 18.34 **(nueva — lo que la 18.30 midió en vez de improvisar.) Qué ve quien vuelve con el borrador vencido.** Hoy el modo de falla de las 24 horas es el paso 1 con el formulario en blanco, sin una frase que lo explique — **el MISMO modo de falla que la cookie de treinta minutos tenía, 48 veces menos frecuente**, así que la 18.30 no lo empeoró: lo hizo raro. **Por qué no se arregló ahí**: `load` filtra en el `WHERE` con `expires_at > $ahora`, una decisión que la 18.29 argumentó («un borrador vencido nunca llega a existir en memoria, así que no hay rama donde alguien lo devuelva por error») y que no se da vuelta dentro de la rebanada que ya carga con el riesgo de cambiar el almacenamiento. Con ese filtro, **vencido y nunca empezado son el mismo observable**. **Las dos salidas, con su costo:** (a) un puerto de lectura angosto al lado del de la 18.29 —`hasExpiredDraft(publisherId, now)`— consultado SÓLO cuando `load` devolvió `null`, o sea coste cero en el caso normal, más el aviso en `/publicar` y su prueba de ruta: ~80 líneas; (b) sacar el filtro del `WHERE` y darle su primer llamador a `hasDraftExpired`, que **hoy es una función de dominio probada y sin nadie que la use** —la novena aparición en este cambio de la forma «pieza construida, sin llamador»—, más barato en líneas pero deshace un argumento embarcado. **Y un plazo**: la 18.32 borra la fila vencida, así que la señal de (a) sólo existe entre el vencimiento y el barrido siguiente; decidir la ventana del barrido y esta pantalla es la misma decisión **La ventana quedó decidida el 2026-09-01 (18.32)**: el barrido corre a las `0 5 * * *`, una vez por día, así que la señal de la salida (a) existe entre el vencimiento y la corrida siguiente — hasta 24 horas, no un instante. **CIERRA el 2026-09-01, con la salida (a) — y con una corrección del enunciado que la 18.34 no podía ver desde acá.** **Por qué gana (a) y por qué pierde (b).** La (b) —sacar el `expires_at > $ahora` del `WHERE` y dejar que el dominio juzgue la fila devuelta— es más barata en líneas y le daba a `hasDraftExpired` su primer llamador, pero **deshace una garantía embarcada justo el día en que empezó a costar**: desde la 18.32 el barrido borra de R2 las fotos de la fila vencida, así que un borrador vencido vivo en memoria es exactamente el camino por el que alguien llega a revisar un aviso al que le faltan imágenes. Con el filtro adentro del `WHERE` eso es estructuralmente imposible; con la (b) depende de un `if` que no puede olvidarse hoy y sí mañana (AGENTS.md §7). **Y la (b) tampoco era más barata de verdad**: cambia la firma del puerto, que arrastra `readPublicationDraft`, sus diez pruebas unitarias y las tres de integración. **CORRECCIÓN de la salida (a) tal como esta tarea la escribió**, y es la que hace que el noveno «pieza sin llamador» SÍ se cierre: la (a) pedía `hasExpiredDraft(publisherId, now)`, un puerto que devuelve un booleano — y un booleano obliga a escribir el `expires_at <= $ahora` adentro de la consulta, o sea **el borde del vencimiento escrito por TERCERA vez en SQL** (`load` lo tiene con `>`, `listExpired` con `<=`) y `hasDraftExpired` seguiría sin llamador. Se construyó `ExpiredDraftSignalPort.findExpiry(publisherId)`, que devuelve el INSTANTE y no lleva `now`: la consulta no juzga nada y el borde lo sigue razonando el dominio en un solo lugar. **`hasDraftExpired` tiene hoy su primer llamador** —`readPublicationDraftOrExpiry`, embarcó en el #186 sin ninguno— así que la novena aparición de «pieza construida, sin llamador» queda cerrada. **Coste cero en el camino normal, y es una prueba y no una promesa**: la segunda consulta sólo ocurre cuando `load` devolvió `null`, y esa condición vive en el módulo y no en la pantalla (AGENTS.md §1). **La frase la escribe `app/`**, `DRAFT_EXPIRED_NOTICE` en `step-copy.ts`, misma partición que `PUBLISH_VIOLATION_COPY`: dice el plazo —sin él «venció» no enseña a evitarlo— y dice las fotos, porque el barrido se las llevó y callarlo dejaría a quien subió seis creyendo que las reencuentra en el paso 8. **Dónde se lee**: la prop `draftExpired` es requerida en `PublishStep`, como `jumpable` y `discardHref`, y no hace falta preguntar en qué paso estamos — sólo puede ser `true` cuando no hay borrador, y sin borrador el único paso navegable es el 1. Pruebas: `publication-draft-session.test.ts` (4), `borrador-en-la-tabla.test.tsx` (3, sobre los BYTES SERVIDOS de la ruta) y `tests/integration/publication-draft-session.test.ts` (1, contra Postgres real, porque lo que se afirma es que el `timestamptz` vuelve como un `Date` que el dominio puede comparar). **Cinco mutaciones (M1–M5)**, cada una con ocurrencia única verificada antes, sha256 comparado después, restauración por copia de bytes y `git diff --stat` limpio; cada una pone roja exactamente la prueba que la nombra y **ninguna de las cinco, con las pruebas nuevas fuera del árbol, pone roja ninguna de las 2.404 unitarias ni de las 326 de integración restantes**.

- [x] 18.35 **(nueva — hallada verificando la 18.23 el 2026-09-01.) La publicación que falla a mitad de camino deja derivadas promovidas sin fila.** `publishListing` recorre las fotos y llama a `processUploadedPhoto` una por una ANTES de guardar el aviso, así que la que falla en la cuarta —un duplicado entre cuentas, una imagen que no decodifica— deja las derivadas de las tres primeras ya escritas bajo `photos/` y ningún `listing_photo` que las nombre. **Es la única fuente de huérfanos del prefijo PROMOVIDO que queda abierta**, y por eso no la alcanza la regla de ciclo de vida de la 18.23, que sólo mira `incoming/`. `discardQuietly` limpia el `incoming` de la foto que falló y nada más. La salida barata es la de la 18.32 aplicada acá: juntar las claves promovidas mientras se recorre y borrarlas en el `catch`, con el mismo orden y la misma negativa ruidosa. **CIERRA el 2026-09-01, y el enunciado se corrige dos veces.** **Primero: NO se disuelve con la 18.36 y tampoco se muda.** La 18.36 se resolvió por su segunda salida —atar el vencimiento del borrador a la retención de `incoming/`—, así que la promoción sigue ocurriendo al publicar: `processUploadedPhoto` sigue siendo el único que escribe bajo `photos/` y `publishListing` lo sigue llamando una por una dentro del bucle, verificado leyendo las dos funciones y no supuesto. La salida barata es la que el enunciado nombraba: `discardPromotedDerivatives` en `publish-listing.ts`, las claves juntadas MIENTRAS se recorre —después de la falla no hay de dónde sacarlas, porque la fila que las nombraría es la que no llegó a escribirse— y borradas en el `catch`, en fila y sin exigir, la misma disciplina de `discardQuietly`. **Se amplía a un segundo momento que el enunciado no nombraba y es el mismo huérfano un paso después**: el rechazo de `listings.save`. Ahí también se limpia, y se puede porque esa escritura es una transacción — si rechazó, no hay fila y esas claves no las nombra nadie. **Y se DETIENE donde empieza lo irreparable, que es la decisión de esta rebanada**: en cuanto `listings.save` resolvió no se borra nada más, falle lo que falle. Con la fila escrita las derivadas son las que la ficha dibuja y D12 ya tiró el original, así que borrarlas dejaría un aviso publicado apuntando a fotos que no se pueden volver a derivar; un hash sin registrar cuesta una comprobación de duplicados y una foto borrada cuesta el aviso entero (AGENTS.md §7). Se prueba con su par: `photoHashes.record` que revienta DESPUÉS de `save` no borra ni una clave. **El orden se afirma con el arreglo literal de llamadas** (`publish-listing.test.ts`, 5 nuevas), la técnica de la 18.32: `["read:incoming/…/0", "put:…/thumb.webp", …, "remove:incoming/…/0", "read:incoming/…/1", "remove:photos/…/thumb.webp", …]` — «se llamó a `remove`» no distingue borrar las cinco promovidas de borrar el original de la foto que falló, que es lo único que este código ya hacía. Con sus dos pares positivos, sin los cuales una limpieza que borrara SIEMPRE pasaría las dos negativas y se llevaría las fotos del aviso recién publicado. **Segunda corrección, medida y no supuesta: esto no era sólo un huérfano, dejaba el borrador INSERVIBLE, y esta rebanada NO lo arregla.** `processUploadedPhoto` borra el original `incoming/` en cuanto las cinco derivadas existen, así que las fotos que sí se promovieron perdieron su original: al reintentar publicar, la primera falla en `storage.read` con una clave que ya no está, y el paso 8 la sigue dibujando adjunta. Borrar las derivadas —lo que esta rebanada hace— quita el huérfano y deja el borrador igual de trabado. **Y tercera: «la única fuente de huérfanos del prefijo PROMOVIDO» eran DOS.** Dentro de una sola foto, las cinco derivadas se suben con `Promise.all`; si una rechaza, las otras cuatro quedan escritas bajo `photos/` y `promoted` nunca las ve, porque `processUploadedPhoto` lanzó antes de devolverlas. Las dos cosas son la misma costura —la atomicidad de UNA foto— y quedan como **18.39**

- [x] 18.36 **(nueva — el choque entre la regla del bucket de la 18.23 y el borrador que se retoma, medido el 2026-09-01.) La foto del paso 8 puede morirse antes que el borrador que la nombra.** `save` corre el vencimiento en CADA guardado, así que un borrador puede vivir mucho más que 24 horas desde la subida; la regla `barrer-subidas-sin-adjuntar` borra el objeto `incoming/` al día **contado desde la subida**. Quien sube fotos el lunes, guarda otro paso el martes de noche y publica el miércoles llega a `processUploadedPhoto` con claves que ya no existen, y publicar falla al leer el objeto. **No es un huérfano: es lo contrario**, una foto viva que se muere. Las dos salidas, con su costo: promover en el paso 8 en vez de al publicar —le da a cada foto de borrador su fila y su prefijo definitivo, y hace que el barrido de la 18.32 borre derivadas de verdad—, o atar `DRAFT_LIFETIME_MS` a la retención de `incoming/` para que la fila nunca prometa más de lo que el bucket conserva. La primera es la que además cierra la limitación (a) de la 18.32. **CIERRA el 2026-09-01 por la SEGUNDA salida, y la primera se descarta con su razón.** **Lo que se hizo**: el vencimiento que `save` corre en cada guardado ya no puede pasar del día en que el bucket se lleva la foto más vieja del borrador — `draftExpiresAt(now, photos)` devuelve `min(now + DRAFT_LIFETIME_MS, la más vieja + INCOMING_RETENTION_MS)`, y las dos constantes viven juntas en `domain/draft-expiry.ts` porque son la misma pregunta por los dos lados: cuánto puede prometer la fila y cuánto conserva el bucket. Manda la más vieja porque con una sola clave que falte publicar falla entero. El tope puede caer en el pasado y ahí ya venció (AGENTS.md §7): `load` deja de devolver la fila y `readPublicationDraftOrExpiry` de la 18.34 lo explica con una frase, que es una negativa dicha en vez de un fallo al publicar sin ninguna. **La mitad que hace que sirva es el SELLO**: `stampUploadInstants` conserva por clave el `uploadedAt` que ya estaba y sólo una clave que el servidor nunca vio estrena el instante de este guardado — resellar en cada guardado devolvería el defecto entero. **Y el sello no puede venir del formulario**: el paso 8 manda las fotos en campos ocultos y `applyStepAnswers` reemplaza el arreglo entero con ellos, así que un `uploadedAt` que viajara ahí sería el navegador decidiendo cuánto vive el borrador; lo que `current` traiga se descarta a propósito, y hay una prueba que lo afirma. **Por qué pierde promover en el paso 8**, que era la otra salida y la que el enunciado prefería: (1) le cambia la forma al camino de publicar entero — `tokenFromOwnedKey` rechaza por diseño cualquier clave que no esté bajo `incoming/`, así que la guarda de propiedad, `actions.ts:166` con su `incomingKey: photo.key`, el tipo `SubmittedPhoto`, y el `processUploadedPhoto` que `attachPhotoToDraft` comparte se mueven todos; (2) **no cierra la limitación (a) de la 18.32, la empeora**: `save` seguiría pisando `photos` entera, así que quitar una foto en el paso 8 seguiría dejándola sin fila que la nombre — sólo que bajo `photos/`, donde ninguna regla de ciclo de vida del bucket la alcanza, en vez de bajo `incoming/`, donde la del fundador sí; (3) obligaría a guardar en el `jsonb` las cinco claves derivadas y el hash perceptual de cada foto, y a decidir dónde se registra ese hash cuando la fila de `listing_photo` todavía no existe. La D4 se queda donde estaba, antes de derivar, que era el requisito. **La limitación (a) de la 18.32 sigue abierta y sigue siendo barata**, dicho con la evidencia y no por optimismo: el huérfano que deja queda bajo `incoming/`, que es exactamente lo que la regla del fundador barre. **La mitigación del fundador, con su fecha**: el 2026-09-01 subió `barrer-subidas-sin-adjuntar` de 1 día a 7. **Ensancha la ventana y no la cierra** —la regla cuenta desde la subida y el vencimiento corre en cada guardado, así que un borrador renovado a diario le gana a cualquier plazo fijo—, y por eso quedó anotada como mitigación en el propio `INCOMING_RETENTION_MS`. **Ninguna migración**: `publish_draft.photos` ya es `jsonb` y `uploadedAt` es un campo más adentro; `normaliseStoredDraft` lo acepta cuando es texto y lo descarta cuando no, como cualquier otro campo con la forma de ayer, y `draftExpiresAt` trata «sin sello» como «no se sabe» y no acorta — una fila anterior a esta regla no pierde el borrador por un despliegue que no vio. **La consulta del barrido no se toca**: `jsonb_path_query_array(photos, '$[*].key')` sigue trayendo sólo claves, y la prueba de integración que compara el objeto ENTERO lo sigue afirmando. **Lo que queda optimista, dicho**: el sello es «cuándo el servidor vio la clave por primera vez», que llega tarde por lo que la persona tardó entre subir y seguir, nunca temprano — el subidor hace el PUT al elegir el archivo y los campos ocultos viajan con el mismo formulario, así que el desfase se mide en minutos y jamás en días. Pruebas: `domain/draft-expiry.test.ts` (8 nuevas), `domain/stored-draft.test.ts` (1), `application/publication-draft-session.test.ts` (1), `app/publicar/borrador-en-la-tabla.test.tsx` (1, el llamador — la forma de defecto que este cambio ya encontró seis veces) y `tests/integration/publication-draft-session.test.ts` (1 nueva más el sello metido en el borrador compartido, para que el `timestamptz` recortado y el `jsonb` con el sello se prueben contra Postgres y no contra un doble)

- [x] 18.37 **(nueva — lo que la regla general del fundador abre y la 18.27 no cerró, medido el 2026-09-01.) Los cinco atributos de la F6 siguen sin poderse corregir.** `hasPowerPlant`, `hasRegularWater`, `isFurnished`, `hasSecurity` y `hasAppliances` son columnas de `listing`, los escribe el paso 5 de publicar, y **no aparecen en una sola línea del camino de editar**: ni en `ListingEdit`, ni en `EditableListingSnapshot`, ni en el `select` de `findEditableById`, ni en el `set` de `applyEdit`, ni en la pantalla. Verificado buscando los cinco nombres en los cinco archivos, no supuesto. **La regla del fundador del 2026-09-01 los abre** —«se puede corregir cualquier dato menos el de la zona»— y no hay ninguna razón de integridad para tenerlos cerrados: ninguno está en la URL. **Por qué duele más que los otros tres**: se filtran. La F6 los combina con AND y cada uno informa cuántos de los resultados actuales lo cumplen, así que un aviso que declaró «sin planta eléctrica» porque el edificio todavía no la tenía queda **fuera de la búsqueda de quien la pide**, y su dueño no tiene ninguna pantalla donde arreglarlo. **Lo que cuesta**: los cinco en `ListingEdit`, en `EditableListingSnapshot` y en `ListingEditWrite`, sus cinco `??` en `writeFor`, el `select` y el `set` del adaptador, y una casilla por atributo en la pantalla reusando `FEATURE_LABELS`. **Ninguna migración.** **Lo que hay que decidir antes**: un booleano no tiene «no contestado» en un `<form>` —una casilla sin marcar no viaja—, así que a diferencia de la referencia acá el formulario ES la única fuente y mandar el paso equivale a declarar los cinco; el paso 5 ya resolvió eso con `featuresDeclared`, y editar tiene que decidir si copia ese mecanismo o acepta que guardar declara los cinco cada vez. **CIERRA el 2026-09-01, y la decisión que el enunciado dejaba abierta se tomó copiando el mecanismo del paso 5 y no su forma.** El paso 5 resuelve «no tiene ninguna» con un POST aparte porque allá **mandar el paso ES la respuesta** y nada puede destildar cinco casillas al enviar sin JavaScript. Al editar los nueve pasos son uno solo, así que lo que se copió es la MARCA y no el botón: `FEATURES_DECLARED_FIELD` es un campo oculto que viaja DENTRO del formulario de guardar, y con él destildar las cinco y apretar «Guardar cambios» ya es «no tiene ninguna» con el script apagado — el segundo formulario del paso 5 no hace falta acá. **La marca es la respuesta, no las casillas**: sin ella los cinco llegan `undefined` y el `?? current` de `writeFor` deja lo de ayer, que es lo que impide que un pedido escrito a mano contra la acción de servidor —un endpoint HTTP público— se lleve por delante cinco declaraciones al corregir el precio. **Y no se aceptó «guardar declara los cinco cada vez»**, que era la otra salida que el enunciado ofrecía: desde la pantalla sí declara los cinco, y está bien porque las casillas vuelven cargadas; desde cualquier otro pedido, no. **Lo que `false` significa, dicho porque cambia cómo se dibuja**: `search-criteria.ts` documenta que en el esquema `false` es «no lo declaró» y jamás «no lo tiene» —las cinco columnas son `NOT NULL DEFAULT false`—, así que la búsqueda sólo puede pedir el `true`. La pantalla no puede distinguir los dos estados y no lo intenta: dibuja `defaultChecked` con la columna y la ayuda dice lo que una casilla MARCADA afirma y nada sobre la que no lo está, porque destildar saca el aviso de esa búsqueda y no lo mete en la contraria. **Un solo lugar donde viven los cinco nombres**: `ListingFeatures`, del que extienden `EditableListingSnapshot`, `ListingEditWrite` y —en `Partial`— `ListingEdit`; tres listas sueltas es como una termina con cuatro. **El precio medido se confirmó entero: ninguna migración**, y el `Record<keyof ListingEdit, true>` de `EDIT_REQUEST_FIELD` obligó a nombrarlos ahí aunque ninguno tenga código de violación —un booleano no puede ser inválido—, que es la garantía de que el día que alguno gane una regla que rechace, `violation-field.ts` no compile hasta que se diga dónde se lee su negativa. Pruebas: `listing-edit.test.ts` (3 nuevas), `actions.test.ts` (2, incluida la que afirma que sin la marca los cinco viajan AUSENTES y jamás en `false`), `editar-servido.test.tsx` (3, sobre los BYTES SERVIDOS de la ruta) y `tests/integration/listing-edit.test.ts` (2, contra Postgres real: los dos sentidos en las columnas, y la edición que no manda atributos y no toca ninguno). **Nueve mutaciones (M1–M9)**, con ocurrencia única verificada antes, sha256 comparado después, restauración por copia de bytes y `git diff --stat` limpio; cada una pone roja exactamente la prueba que la nombra. **Con las pruebas nuevas fuera del árbol, siete de las nueve no ponen roja ninguna de las 2.437 unitarias ni de las 330 de integración restantes.** Las dos que sí —M3 y M4, la guarda de la marca en `leerAtributos`— ponen roja una prueba que PREDATA este cambio (`guardarEdicion … los once campos editables`), y eso es un hallazgo y no ruido: la forma del pedido ya estaba protegida. **Y la línea de base del árbol podado es un hallazgo propio**: con las seis pruebas nuevas afuera, la prueba de la lista de claves de `writeOf` —que la 18.27 dejó en once— falla siempre, porque la edición ahora escribe dieciséis. Es la misma señal buena que la 18.27 encontró al abrir el candado de la zona: la forma de lo que se escribe la protege una prueba que este cambio no escribió

- [x] 18.38 **(nueva — la decisión del fundador del 2026-09-01, que no tenía tarea.) `publisherType` se puede corregir en UNA sola dirección: dueño → inmobiliaria, nunca al revés.** Hoy `planListingEdit` rechaza cualquier cambio (`listing-edit.ts:182`, `publisherType.immutable`) y la pantalla de editar **no dibuja el control en absoluto** — `leerEdicion` lee el campo del formulario a propósito, para que un pedido a mano se rechace en el dominio y no se cuele por un `undefined`. **La razón del fundador, y es asimétrica a propósito**: corregir hacia la honestidad no cuesta nada —quien se declaró dueño y en realidad es corredor arregla el aviso—, mientras que la dirección contraria es exactamente cómo alguien aprende que mentir sale barato: declararse inmobiliaria, quedar fuera del filtro de quien busca trato directo, y volver a dueño cuando no llegan mensajes. **Lo que cuesta**: la guarda de `listing-edit.ts:182` pasa de «cualquier cambio se rechaza» a «sólo `owner → broker`», con su par —repetir el valor vigente sigue sin ser un cambio, que es la razón por la que la guarda compara y no sólo mira si vino—; una segunda copia de negativa al lado de `publisherType.immutable` en `listingEditViolationMessage` (`app/publicar/violation-copy.ts:240`), porque «se declara una vez» deja de ser cierto y la frase tiene que decir cuál de las dos direcciones está cerrada; y el control en `app/mis-avisos/[id]/editar/page.tsx`, que hoy no existe y sólo debe dibujarse cuando el aviso es de un dueño — para una inmobiliaria no hay a dónde ir y ofrecerlo prometería algo que la guarda va a negar. **Ninguna migración**: la columna existe y la unión no cambia. **Lo que hay que decidir antes**: si el cambio es reversible dentro de la misma sesión de edición (no lo es: una vez guardado `broker`, la guarda cierra la vuelta) hay que decirlo EN la pantalla antes de guardar, no después en una negativa **CIERRA el 2026-09-01, y lo que había que decidir se decidió por la pantalla y no por la negativa.** No es reversible, y la advertencia se lee ANTES del botón, dentro del formulario que guarda: una frase que llega en el rechazo llega cuando ya no evita nada. **La guarda pasa de «cualquier cambio se rechaza» a «sólo `owner → broker`», y conserva su par**: repetir el valor vigente sigue sin ser un cambio, que es por qué compara en vez de mirar si vino. **Hacia dónde puede moverse lo contesta el dominio y no la pantalla**: `editablePublisherTypes` devuelve la lista —vacía para una inmobiliaria, porque no hay a dónde ir— y la pantalla sólo dibuja lo que esa lista trae. Un `if` de la pantalla habría puesto la mitad de la regla fuera del piso del 90 % (AGENTS.md §1). **La razón del fundador queda escrita donde la regla se aplica**, para que nadie la vuelva a derivar: corregir hacia la honestidad no cuesta nada, y la dirección contraria es exactamente cómo alguien APRENDE que mentir sale barato —declararse inmobiliaria, quedar fuera del filtro de trato directo, y volver a dueño cuando no llegan mensajes—; mentir en el paso 9 ya era posible, lo que la irreversibilidad impedía era aprenderlo. **La copia se amplía a un lugar que el enunciado no nombraba, y hacía falta**: el paso 9 prometía «Aparece siempre en tu aviso y no se puede cambiar después», que con esta rebanada deja de ser verdad para un dueño. La promesa se renombró a `PUBLISHER_TYPE_ONE_WAY_*` y ahora dice las dos direcciones —«Después podés corregirlo de dueño a inmobiliaria, nunca al revés»—, una sola vez y leída por el paso 9 y por editar; la segunda copia es la negativa, que nombra la dirección cerrada («Una inmobiliaria no puede volver a declararse dueño»). Dejar la vieja habría sido embarcar una mentira en el paso 9. **`publisherType` deja de ser una clave de `ListingEdit` que sólo se rechaza y pasa a `ListingEditWrite`**, que va de once campos a doce; el `set` del adaptador lo escribe, y eso no ensancha nada porque el plan sólo lo deja pasar hacia `broker`. **Ninguna migración**: la columna existe y la unión no cambia. **Verificado, no supuesto, lo que el encargo pedía verificar**: nada fuera del filtro F6 y la insignia agrega ni cachea `publisherType`. Las facetas se calculan vivas en UNA consulta por pedido y `drizzle-faceted-search.ts` dice adentro por qué no puede haber caché («F7 pide el número exacto»); no hay `unstable_cache`, ni `export const revalidate`, ni `generateStaticParams` en ninguna ruta, ni vista materializada; el `cache()` de React que usan `/alquiler/...` es memoización por pedido. Los otros lectores son en vivo y ninguno agrega: los datos estructurados de la ficha (`RealEstateAgent` contra `Person`, que ES lo que corresponde que cambie), la sugerencia «Solo de dueños», el tablero de `/mis-avisos` —que `guardarEdicion` ya revalida— y la importación en lote, que lo escribe. Pruebas: `listing-edit.test.ts` (4 nuevas, con las DOS direcciones y la lista de destinos), `edit-listing.test.ts` (1 invertida), `violation-copy.test.ts` (1, con el literal fijado), `editar-servido.test.tsx` (3 sobre los BYTES SERVIDOS: el dueño ve el control cargado, la inmobiliaria no ve ninguno y su valor se sigue diciendo, y la advertencia se lee dentro del formulario) y `tests/integration/listing-edit.test.ts` (2, las dos direcciones contra Postgres real). **Ocho mutaciones (N1–N8)**, con ocurrencia única verificada antes, sha256 comparado después, restauración por copia de bytes y `git diff --stat` limpio; cada una pone roja exactamente la prueba que la nombra. **Y la medida del hueco es la más grande que esta fase encontró**: con las pruebas nuevas fuera del árbol, la línea de base ya son 10 unitarias y 1 de integración rojas —las aserciones del candado viejo, que esta rebanada reemplaza a propósito— y NINGUNA de las ocho mutaciones agrega una sola roja por encima de esa línea. Borrar la guarda entera (N1), invertir la dirección (N2), ofrecerle a una inmobiliaria lo que la guarda le niega (N3) y no aplicar el cambio permitido (N4) son todas invisibles sin las pruebas de esta rebanada

- [ ] 18.39 **(nueva — las dos correcciones que la 18.35 midió al construirla, el 2026-09-01.) Una foto que se promueve a medias deja el borrador trabado, y el `Promise.all` de las cinco derivadas es una segunda fuente de huérfanos.** **La primera duele más y no es un huérfano**: `processUploadedPhoto` borra el original `incoming/` en cuanto las cinco derivadas existen (D12), así que cuando publicar falla en la cuarta foto, las tres primeras ya perdieron su original — reintentar publicar falla en `storage.read` con una clave que ya no está, el paso 8 las sigue dibujando adjuntas, y la única salida que hoy tiene quien publica es quitarlas y volver a subirlas sin que nada se lo diga. La 18.35 quita el huérfano y deja esto igual. **La segunda**: las cinco derivadas de UNA foto se suben con `Promise.all`; si una rechaza, las otras cuatro quedan escritas bajo `photos/` y el llamador nunca las ve, porque `processUploadedPhoto` lanza antes de devolverlas — así que la 18.35 no cerró «la única fuente de huérfanos del prefijo promovido», cerró una de dos. **Las dos son la misma costura: la atomicidad de UNA foto**, y por eso van juntas. **Las dos salidas, con su costo**: (a) `Promise.allSettled` adentro de `processUploadedPhoto`, borrando las que sí quedaron antes de relanzar — barato, cierra la segunda, no toca la primera; (b) sacar el `storage.remove(incomingKey)` de `processUploadedPhoto` y dárselo a sus DOS llamadores para que lo hagan después de escribir su fila —donde `publishListing` y `attachPhotoToDraft` ya registran el hash por la misma razón—, con lo que el original sobrevive a una publicación fallida y el reintento vuelve a derivar sobre las MISMAS claves, porque el token sale de la clave `incoming/` que el borrador conserva. La (b) cierra las dos y cambia un orden que `process-uploaded-photo.ts` documenta como decidido («Al final, y sólo cuando las cinco existen»); su razón sobrevive, sólo se muda una capa afuera **CIERRA A MEDIAS el 2026-09-01, y NO se tilda: la SEGUNDA mitad cierra por la salida (a), la PRIMERA se abre como 18.40, y el enunciado se corrige dos veces.** **Lo que se hizo (salida a)**: las cinco derivadas suben con `Promise.allSettled` en vez de `Promise.all` —siguen saliendo juntas, esperarlas a todas no serializa ningún PUT—, y si alguna rechaza se borran EN FILA las que sí quedaron escritas, con la disciplina de `discardQuietly` (se intenta y no se exige), antes de relanzar el motivo del PRIMER rechazo. **El original no se toca**: es lo único con lo que se puede volver a derivar, y borrarlo acá fabricaría el defecto de la 18.40; si nadie vuelve, lo alcanza la retención de `incoming/` (18.23, 18.36). Se prueba en `process-uploaded-photo.test.ts`, «las cinco derivadas de una foto son atómicas (18.39)» (5 nuevas, con el arreglo literal de llamadas: los cinco `put` primero y recién después los cuatro `remove`, cada uno nombrado, porque «se llamó a `remove`» no distingue borrar las supervivientes de borrar el original), y las dos capas se prueban por separado: `publish-listing.test.ts` afirma que COMPONEN con la 18.35 —«un PUT rechazado adentro de una foto no deja huérfanas ni las suyas ni las de la anterior»—, que es exactamente la costura que la 18.35 no podía alcanzar. **Primera corrección, contada y no supuesta: los llamadores son TRES, no dos.** `publishListing`, `attachPhotoToDraft` y —desde la 18.21— `attachPhotoToListing` en `edit-listing-photos.ts`. **Segunda, y es la que decide la elección: la (b) NO cierra las dos.** Sacar el `remove` del original no borra las cuatro supervivientes; sólo las deja pisables por un reintento sobre las MISMAS claves. Si quien publica no vuelve, siguen bajo `photos/` para siempre, junto a las de todos los avisos activos, donde ninguna regla del bucket las distingue — así que la (a) hacía falta igual y la (b) no la contiene. Y la (b) MUEVE una negativa: hoy `storage.remove` está fuera de todo `try` (D12) y revienta ANTES de `listings.save`; con la (b) reventaría DESPUÉS, o sea rechazándole a quien publica un aviso que ya se publicó. Eso es una decisión nueva que el enunciado no costeó, sobre tres llamadores, con el arreglo literal de la 18.35 entero por reescribir: las dos juntas pasan el techo de 400 líneas revisables
- [ ] 18.40 **(nueva — la PRIMERA mitad de la 18.39, medida al construir su segunda el 2026-09-01.) Una foto que se promovió sin que su aviso llegara a escribirse pierde el original, y el borrador la sigue dibujando adjunta.** `processUploadedPhoto` borra el objeto `incoming/` en cuanto las cinco derivadas existen (D12), así que cuando publicar falla en la cuarta foto las tres primeras ya perdieron el suyo: reintentar publicar revienta en `storage.read` con una clave que ya no está, el paso 8 las sigue mostrando adjuntas, y la única salida que hoy tiene quien publica es quitarlas y volver a subirlas sin que nada se lo diga. **La 18.35 quita el huérfano y la 18.39 cierra el `Promise.all`; las dos dejan esto igual.** **No es un huérfano: es lo contrario**, un borrador vivo que apunta a algo que ya no está — la misma forma que la 18.36. **La salida**: sacar `storage.remove(incomingKey)` de `processUploadedPhoto` y dárselo a sus TRES llamadores —`publishListing`, `attachPhotoToDraft` y `attachPhotoToListing`— para que lo hagan después de escribir su fila, donde los tres ya registran el hash por la misma razón; el original sobrevive a una publicación fallida y el reintento vuelve a derivar sobre las MISMAS claves, porque el token sale de la clave `incoming/` que el borrador conserva. Los borrados de los caminos de RECHAZO (`discardQuietly`) se quedan donde están: ahí la foto está refusada y no hay reintento posible. **Lo que hay que decidir antes de construir, y es por lo que no entró en la 18.39**: hoy ese `remove` está fuera de todo `try` (D12, la negativa ruidosa que la 18.23 pide) y revienta ANTES de `listings.save`; movido, reventaría DESPUÉS, rechazándole a quien publica un aviso que la base ya guardó. O se traga —y contradice a D12— o se grita —y miente sobre el resultado—; la tercera es no tirar el original y dejárselo a la retención del bucket, que es lo que la 18.36 ya ató con `INCOMING_RETENTION_MS`. **Su costo**: tres casos de uso, el arreglo literal de llamadas de la 18.35 entero por reescribir (el `remove:incoming/…` se corre al final), la prueba de D12 de `process-uploaded-photo.test.ts` invertida, y un par positivo nuevo por llamador que afirme que el reintento vuelve a leer la misma clave. **La ventana del reintento la acota la 18.36**: `draftExpiresAt` ya no promete más de lo que el bucket conserva, así que el original sobrevive hasta `INCOMING_RETENTION_MS` desde la subida y no más

## Fase 24 — Búsquedas guardadas, que nunca fue una casilla de la 14 (2026-09-03)

**Por qué existe esta fase.** La 14.16 decía, entera: *«Saved searches, which F11 promises ("avisarme cuando aparezca algo así")»*. Una línea, una casilla, al final de una fase sobre la cuadrícula y el filtro. Venía del documento original y **nadie la volvió a mirar en todo el trabajo**; sobrevivió a la 14.32, a la 14.36, a la 14.41 y a la 14.53 sin que ninguna la tocara, porque no comparte una línea con ellas. El fundador la sacó el 2026-09-03 al preguntar qué faltaba de la 14 y ver seis casillas donde había dos trabajos.

**Lo que en realidad pide, y por eso es una fase.** «Avisarme cuando aparezca algo así» son cinco piezas y ninguna existe: (a) **guardar el criterio** — una tabla por usuario con la búsqueda, que hoy vive sólo en la dirección; (b) **correrla sola** — un cron con su ruta bajo `app/api/jobs/`, que `cron-wiring.test.ts` va a exigir que esté en `vercel.json`; (c) **saber qué es nuevo desde la última vez**, que es una marca de agua por búsqueda y no una consulta más; (d) **el correo**, con su plantilla y su remitente, que ya tiene puerto (`MailerPort` en `identity`) pero no molde; (e) **desuscribirse sin entrar**, que es un enlace firmado y una decisión legal, no un botón.

**Decisiones del fundador que faltan antes de escribir una línea**: con qué frecuencia corre (¿diario? ¿al instante?), cuántas búsquedas puede guardar una persona, qué pasa con una búsqueda cuyo catálogo quedó vacío tres meses, y si esto es del MVP o de después. **Ninguna es técnica.**

- [x] 24.1 **La decisión de si esto entra al MVP. DECIDIDA el 2026-09-04: NO entra.** Es la primera y bloquea las otras cuatro. La fase 24 queda escrita con sus cinco piezas y no se toca una línea de código: la promesa de F11 pasa a ser deuda documentada **con su forma real medida**, que es exactamente lo que la 14.16 nunca dio cuando era una casilla suelta. Las 24.2 a 24.5 quedan cerradas por esta decisión, no construidas — si la fase se retoma, se retoman ellas también.

## Fase 25 — El administrador, que hoy es un `curl` (2026-09-03)

**Por qué existe.** El fundador la pidió por nombre —*«hay que agregar esto a la fase del admin que tenemos que crear sí o sí»*— al decidir la 14.4. No existía ninguna fase de administración y **el trabajo de operador ya está repartido en el proyecto sin dueño**: la fase 8 lo llama «operator restore», la 9 «operator-gated CSV import», y la 14.4 le acaba de agregar una cola de aprobación.

**Lo que HAY hoy, verificado.** Una sola ruta: `app/api/operator/restore-listing/route.ts`, autorizada con un secreto compartido en la cabecera `Authorization` (`isAuthorizedOperatorRequest` + `readOperatorSecret`, responde 401 sin él — **no está abierta**). `listing_report(listing_id, reporter_id, reported_at)` con unicidad por par, y `moderation_action(listing_id, action, created_at)` donde `action` **sólo conoce el valor `"restore"`**.

**Lo que NO hay.** Una pantalla. `app/` tiene ficha, publicar, mis avisos, importar y renovar, y nada más. Administrar hoy es armar un `curl` a mano con el secreto en la cabecera — sirve para una emergencia y no para un trabajo diario.

**La tensión que esta fase tiene que resolver antes de escribir código**: un secreto compartido en una cabecera es la autorización correcta para una ruta que se llama desde una terminal, y es la incorrecta para una pantalla con sesión. `bulkImportEnabled` (columna por usuario, `src/shared/db/schema.ts:82`) ya es el molde de un permiso por cuenta. **Que convivan dos formas de decir «sos operador» es la clase de duplicación que este proyecto viene cazando**, así que la fase empieza eligiendo una.

- [x] 25.1 **Quién es operador, y en una sola forma. DECIDIDA el 2026-09-04: una columna nueva por cuenta, y el secreto se borra.** Un segundo booleano en `users` —con la FORMA de `bulkImportEnabled` pero columna y guardia propias—, y la pantalla de moderación exige sesión más esa columna. `OPERATOR_SECRET`, `isAuthorizedOperatorRequest` (`listing-trust/domain/operator-authorization.ts`), `readOperatorSecret` y la ruta `POST /api/operator/restore-listing` se **borran** junto con sus pruebas: no sobrevive una salida de emergencia por `curl`, porque una segunda puerta al admin es una segunda puerta que rotar y cuidar. **La pregunta original de esta tarea estaba mal planteada y el fundador la corrigió**: ofrecía `bulkImportEnabled` como «molde de un permiso de operador», y operador NO es realtor — un realtor con la carga masiva habilitada no debe poder entrar al admin jamás. Verificado en el código antes de decidir: hoy ya son dos guardias que no se hablan (`authorizeBulkImport` gobierna sólo las cuatro puertas de importar; el secreto gobierna sólo la ruta de restaurar) y **`OPERATOR_SECRET` ni siquiera está atado a una cuenta: no sabe quién sos**. La separación de roles que había que conservar ya existía. Hoy son dos caminos posibles y hay que quedarse con uno: el secreto de cabecera (que ya existe y sirve para `curl`) o un permiso por cuenta con el molde de `bulkImportEnabled` (que es lo que una pantalla con sesión necesita). Si sobreviven los dos, decir por escrito cuál gobierna qué y por qué — un `curl` de emergencia que siga funcionando cuando la pantalla esté rota es un argumento válido, pero tiene que ser un argumento y no un accidente. **Bloquea a las otras cinco.**

- [ ] 25.2 **`pending_moderation` como quinto estado, y su exclusión de TODAS las colecciones.** No basta agregarlo al enum: hay que probar que no aparece en la búsqueda, ni en las facetas, ni en el sitemap, ni en las tiras del inicio, ni en el revelado de contacto. La fase 21 ya dejó el molde exacto para esto — una prueba que recorre `src/` y declara quiénes filtran, y se pone roja cuando aparece un lector nuevo.

- [ ] 25.3 **El publicador marcado, con el umbral de tres.** Un aviso entra a `pending_moderation` al publicarse **sólo si su publicador tiene al menos un aviso ocultado por reportes**. El dato existe (`listing_report` → `listing` → `publisher_id`); la regla es de dominio y va al lado de `report-threshold.ts`, que ya es donde vive el umbral.

- [x] 25.4 **La SALIDA, que sin ella la regla es una condena. DECIDIDA el 2026-09-04: el publicador queda limpio, con registro del bloqueo.** La pregunta era: si se restaura un aviso porque el reporte era falso, ¿el publicador queda limpio, o sigue en revisión? ¿Para siempre, o vence? **Respuesta: restaurar borra la marca** —vuelve a cero y sus próximos avisos se publican en automático, sin vencimiento ni limpieza manual—, **pero tiene que quedar rastro de que ese aviso estuvo bloqueado por reportes**. **La consecuencia técnica NO es la que esta tarea anticipaba.** Esta tarea suponía que cualquier respuesta agregaba al menos un valor a `moderation_action.action`; «queda limpio» por sí solo **no agrega ninguno**, porque el `restore` ya se registra hoy. Lo que falta es el **ocultamiento**: el auto-ocultado no escribe una sola fila —se deduce de que las filas de `listing_report` llegaron al umbral— y el puerto declara `action: "restore"` como lista cerrada de uno (`moderation-action.port.ts:12`, «closed list of one today»). El registro que el fundador pide obliga a escribir también el `hide`, y **ahí sí** `action` pasa a ser un enum con su migración.

- [ ] 25.5 **La cola y su pantalla.** Qué se ve de un aviso en espera, en qué orden salen, y qué se puede hacer con cada uno. Es la primera pantalla del producto que **no** es para un inquilino ni para un publicador, así que ninguna lámina la dibuja: es diseño nuevo, no lectura de tablero.

- [ ] 25.6 **Quién aprobó qué.** `moderation_action` no guarda actor: tiene `listing_id`, `action` y `created_at`. Con una sola persona operando da igual; con dos, la primera pregunta después de un error es quién lo hizo. Decidir si se agrega ahora o se anota la deuda con su razón.

## Fase 26 — El producto se llama `rentoru.com`, y el dominio viejo nunca sirvió una página (2026-09-05)

**Por qué existe.** El fundador decidió el 2026-09-05 renombrar el producto de **`rentas.com.ve` a `rentoru.com`**, dominio incluido. No es un cambio de copia suelto: `design/reference/sistema/SISTEMA.md:323` define la marca textualmente —*«No hay logotipo: la marca es la palabra "rentas." en el stack del sistema»*—, así que renombrar cambia **la definición de la marca misma**, no un literal que aparece por ahí.

**Lo que hace distinta a esta fase, y hay que dejarlo escrito para que nadie la reabra: `rentas.com.ve` NUNCA sirvió el sitio.** El fundador es dueño del dominio, pero el TLD `.ve` dio problemas con Vercel y con Cloudflare, así que el producto siempre se sirvió desde una dirección de Vercel. **Esto NO es una migración de dominio**: no hay un 301 que construir, no hay autoridad de enlace indexada que rescatar, no hay contenido duplicado entre dos anfitriones y no hay una sola dirección vieja en el índice de Google esperando una redirección. Es un **renombre de marca** más el **primer montaje real de un dominio** — dos trabajos que se parecen a una migración desde afuera y no comparten ni uno de sus riesgos. Quien retome esta fase y busque el plan de redirecciones no va a encontrarlo porque no hace falta, no porque falte.

**El segundo hecho que ordena la fase: las dos cosas que hoy sí funcionan viven en el dominio personal del fundador.** Resend envía desde `gianbarboza.com` —deuda registrada en este mismo archivo, `tasks.md:339`, con su razón escrita el 2026-08-24: *«the sending domain is the founder's personal `gianbarboza.com` (tracked debt — `rentas.com.ve` is not owned yet)»*, donde hoy sabemos que la razón real no era la propiedad sino el TLD— y el balde de fotos se sirve desde `fotos.gianbarboza.com`. **Esta fase les da casa propia por primera vez.** En el mismo sentido, las ~20 fixtures de prueba que afirman `https://fotos.rentas.com.ve` (medidas con `rg -n 'fotos\.rentas\.com\.ve'`, 20 apariciones) **describen un mundo que nunca existió**: nunca hubo un anfitrión ahí, y sin embargo están verdes desde el día que se escribieron.

**Pronóstico de carga de revisión de esta fase.** El presupuesto de la sesión es de **800 líneas de autoría por rebanada** y la estrategia sigue siendo cadena de PR, `stacked-to-main`. La estimación honesta, por rebanada natural: **PR26a — la marca en el código (26.2–26.9)**, ~400–550 líneas, casi todo texto y el endurecimiento de las tres pruebas que hoy mienten; **PR26b — el dominio, su respaldo y sus fixtures (26.10, 26.11, 26.13, 26.14)**, ~250–400, mayoría resta y no suma; **PR26c — canónicas, `metadataBase` y Open Graph (26.12)**, ~400–700, la única rebanada que es **capacidad nueva** y la más cerca del techo, porque toca los 28 archivos con metadata y ninguno tiene hoy dónde apoyarse; **PR26d — diseño y artefactos de planificación (26.21–26.26)**, ~300–500, casi nada de código. **Las costuras**: entre B y C, que B no cambia nada de lo que un rastreador ve y C sí; entre las fixtures y la capacidad de metadata, que una borra afirmaciones falsas y la otra escribe afirmaciones nuevas sobre **los mismos 28 archivos** — juntarlas pasa de 800 sin discusión, y ésa es la costura que no hay que cruzar. **El grupo D (26.15–26.20) es configuración del fundador: cuesta CERO líneas de autoría y bloquea a todas las demás**, porque el código puede renombrarse en verde mientras el correo, la entrada con Google y las fotos siguen apuntando a otro lado.

**Grupo A — el arreglo que no espera al código.**

- [x] 26.1 **`SITE_URL` no está puesta en Vercel, y eso le gana a cualquier línea de código de esta fase.** El fundador lo verificó el 2026-09-05. `src/modules/listing-discovery/infrastructure/site-base-url.ts:21-31` resuelve en cadena: `SITE_URL` (línea 21), si no `https://${VERCEL_URL}` (línea 27), si no el `PRODUCTION_ORIGIN` codificado (línea 31). **En Vercel `VERCEL_URL` SIEMPRE está puesta y es la dirección POR DESPLIEGUE**, así que hoy gana el segundo escalón y nunca el tercero — el respaldo que el archivo cree tener no se usa jamás en producción. La consecuencia se reparte en cinco superficies: el `sitemap`, la directiva `host:` de `robots.txt` (`app/robots.ts:37`), el `@id` y el `url` del JSON-LD de cada aviso (`src/modules/listing-discovery/domain/listing-structured-data.ts:312-313,338`) y el enlace de renovación dentro de los correos de ciclo de vida (`src/modules/listing-lifecycle/application/send-lifecycle-notices.ts:134`), que hoy viajan al buzón de alguien con un anfitrión que cambia en cada push. **Poner `SITE_URL = https://rentoru.com` en Vercel corrige las cinco sin tocar una línea de código.** Es acción del fundador y va primero: hacerla después de renombrar el código no cambia el resultado, pero hacerla antes deja el resto de la fase midiéndose contra el anfitrión definitivo. **HECHA, ejecutada el 2026-09-05** (§2 del runbook `docs/rentoru-cutover.md`) y verificada desde afuera. `SITE_URL` quedó en `https://rentoru.com` — ápice, sin `www` y sin barra final.

**Grupo B — código: la marca.**

- [x] 26.2 **La palabra «rentas.» está retipeada a propósito en seis lugares y no hay una sola constante que las gobierne.** `components/organisms/Nav.tsx:17` y `components/organisms/SiteFooter.tsx:10` la declaran como `WORDMARK`, y `app/publicar/PublishStep.tsx:167`, `app/publicar/listo/page.tsx:34`, `app/publicar/revisar/page.tsx:114` y `app/(auth)/signin/DoorBar.tsx:24` la escriben inline. **La duplicación es deliberada y está documentada** —el comentario de `Nav.tsx:73` y el de `SiteFooter.tsx:6` explican por qué cada superficie dibuja la suya—, así que el renombre no es «cambiar una constante»: son seis ediciones que tienen que quedar idénticas entre sí, y el único guardián que hay son las pruebas de cada componente. **Y hay un cambio de nivel escondido**: `SISTEMA.md:323` dice que la marca ES la palabra, así que ésta es la tarea que redefine el sistema de diseño y no la que ajusta un texto. Si se hace a medias, el sitio dibuja dos marcas distintas en la misma sesión de navegación, que es peor que no haber renombrado. **HECHA el 2026-09-06** (Grupo B, PR26a). El fundador eligió **`Rentoru`** — mayúscula inicial y SIN punto final —, así que la marca y el sufijo de títulos de la 26.3 quedan en una sola forma. Reemplaza a `rentas.` en los seis lugares, y **la duplicación se conservó a propósito**: no se refactorizó a una constante compartida, porque los comentarios de `Nav.tsx` y `SiteFooter.tsx` explican por qué cada superficie dibuja la suya y unificarlas es una decisión de diseño que esta tarea no autoriza. Las seis quedaron idénticas byte a byte, verificado. **Y apareció una séptima que este inventario NO tenía**: `src/modules/identity/domain/sign-in-page.ts:67` decía «Rentas no participa en el trato…» en la MISMA pantalla que `DoorBar.tsx:24`, así que embarcar sin ella habría dibujado `Rentoru` en la barra y `Rentas` dos líneas más abajo — el defecto exacto de «dos marcas en una sesión» que esta tarea advierte. También se corrigió `src/styles/tokens.css:342`, un comentario que definía la marca como «rentas.» y quedaba contradiciendo al código.

- [x] 26.3 **El sufijo `— Rentas` de los títulos, en veintiuna rutas, más la base.** Medido: `rg -n '— Rentas"' app/` da **21** apariciones, y `app/layout.tsx:24` declara aparte el título base `title: "Rentas"` — veintidós declaraciones en total. Es la marca que sale en la pestaña del navegador y en el resultado de Google, o sea la primera palabra que ve alguien que todavía no entró. No hay plantilla ni `title.template` que las junte: cada página escribe su propio literal, así que el conteo es el trabajo. **HECHA el 2026-09-06** (Grupo B, PR26a). Sufijo `— Rentoru` y `title: "Rentoru"` en `app/layout.tsx`. **El conteo de esta tarea estaba corto y queda corregido: son 23, no 21.** La medición original usaba `rg '— Rentas"'`, que se pierde las dos que viven dentro de literales de plantilla — `app/alquiler/[ciudad]/page.tsx:449` y `app/alquiler/[ciudad]/[zona]/page.tsx:472` —; con la base son **24 declaraciones**. Se agregaron además los `title:` de las cinco páginas legales, que la 26.5 no listaba porque son de ésta.

- [x] 26.4 **La franja del pie, que se dibuja en TODAS las páginas del sitio.** `components/organisms/SiteFooter.tsx:15` (`"© 2026 rentas.com.ve · Publicar y contactar no cuesta nada"`) y `:16` (`"rentas.com.ve no interviene en el contrato entre las partes"`). Son dos líneas y aparecen en cada ruta salvo la ficha y el visor de fotos, que la 23.3 dejó calladas. **La segunda no es decoración: es la advertencia legal que el producto repite en cada página**, así que dejarla con el nombre viejo mientras el resto del sitio dice `rentoru.com` deja una advertencia que nombra a un tercero. Las dos líneas tienen su prueba en `components/organisms/SiteFooter.test.tsx` y su copia literal en la lámina (ver 26.21). **HECHA el 2026-09-06** (Grupo B, PR26a). Las dos líneas de la franja pasan a `rentoru.com`, con su prueba en `SiteFooter.test.tsx` en rojo primero. **La lámina `Rentas - Footer.dc.html:71,72,110,111` sigue con el texto viejo a propósito**: es de la 26.21 y cruzarla acá mezclaba dos rebanadas.

- [x] 26.5 **Las cinco páginas de `/legal/*`, donde el nombre del sitio es UNA PARTE CONTRACTUAL y no una firma.** `app/legal/terminos/page.tsx:13,14,24,28,30` («rentas.com.ve es un sitio gratuito de avisos…», «Qué es rentas.com.ve y qué no es», «rentas.com.ve conecta a quien publica…»), `privacidad:16,70`, `datos:14,58`, `cookies:15,25` —más el nombre de cookie `rentas_enlace` que `cookies:38` publica y que es una decisión aparte, porque renombrar una cookie invalida las vivas—, `normas:13`, y el banner de borrador en `app/legal/draft-notice.tsx:15`. **Hay una colisión con la 23.5 que conviene aprovechar en vez de sufrir**: las cinco están **sin ratificar** (23.5 sigue abierta, bloqueada en la lectura del fundador), así que renombrar ANTES de que las ratifique hace que las lea una sola vez. Al revés —ratificar y después renombrar— le pide al fundador aprobar dos veces el mismo texto legal, y la segunda con el nombre cambiado en treinta lugares. **HECHA el 2026-09-06** (Grupo B, PR26a). Las cinco páginas legales más el banner de borrador. **La cookie `rentas_enlace` NO se tocó**, tal como esta tarea exige: renombrarla invalida las sesiones vivas y eso es un cambio de comportamiento, no de texto. Queda como decisión aparte.

- [x] 26.6 **Las cinco páginas de `/ayuda/*`, incluido un `<h2>` que es una pregunta con la marca adentro.** `app/ayuda/preguntas-frecuentes/page.tsx:19,20,30,59,61` —la 59 es el encabezado *«¿rentas.com.ve interviene en el contrato o en el pago?»*, que es texto de pantalla y no metadata—, `como-contactar-al-dueno/page.tsx:17,18,52`, `como-publicar-un-aviso/page.tsx:12,13`, `como-reportar-un-aviso/page.tsx:26,27` y `escribinos/page.tsx:34,35`. Son las cinco páginas que 23.4/23.6/23.7 acaban de escribir, así que el renombre llega sobre tinta fresca; el costo de dejarlas fuera es que la superficie de adquisición del producto —la que contesta búsquedas reales, según la decisión de 23.9— nombra a otro sitio. **HECHA el 2026-09-06** (Grupo B, PR26a). Las cinco páginas de ayuda, incluido el `<h2>` de `preguntas-frecuentes/page.tsx:59`, que es texto de pantalla y no metadata.

- [x] 26.7 **El asunto del correo de entrada.** `src/modules/identity/domain/magic-link.ts:26`: `subject: "Tu enlace para entrar a Rentas"`. Es el ÚNICO camino de entrada que no pasa por Google, y es la primera línea que ve alguien en su bandeja antes de decidir si el correo es legítimo. Un asunto que nombra a «Rentas» junto a un remitente que ya dice `rentoru.com` (26.17) se lee como suplantación, que es exactamente lo que un correo de autenticación no puede permitirse. **HECHA el 2026-09-06** (Grupo B, PR26a). `magic-link.ts:26` pasa a «Tu enlace para entrar a Rentoru». **No había NINGUNA prueba que afirmara el asunto**: la 26.7 se habría embarcado sin guardián. Se agregó una en `magic-link.test.ts`, roja primero contra el valor viejo para probar que ata de verdad.

- [x] 26.8 **El mensaje de WhatsApp por defecto, que viaja al teléfono de un desconocido y nunca falla.** `components/molecules/ContactBlock.tsx:94` compone `` `Hola, vi tu aviso «${listingTitle}» en rentas.com.ve y me interesa.` ``. **Es la superficie más lejana del renombre y la que ninguna prueba de humo va a mirar**: sale del sitio, entra a una aplicación que no controlamos y llega a quien publicó el aviso. No tiene modo de error — un nombre viejo ahí no rompe nada, sólo le dice a quien recibe el mensaje que el interesado viene de un sitio que no existe. Es la clase de cosa que se descubre seis meses después, por un publicador que pregunta. **HECHA el 2026-09-06** (Grupo B, PR26a). `ContactBlock.tsx:94` pasa a `rentoru.com`. **La prueba del respaldo afirmaba sólo el título del aviso dentro del `wa.me`, nunca el nombre del sitio** — o sea que la superficie que esta tarea llama «la que ninguna prueba de humo va a mirar» tampoco la miraba su propia prueba. Ahora afirma `encodeURIComponent("en rentoru.com y me interesa.")`.

- [x] 26.9 **Lo que queda suelto, junto porque es de la misma naturaleza y no da para cuatro tareas.** `components/molecules/ContactBlock.tsx:246` (*«Rentas no participa en la negociación…»*, la advertencia junto al botón de revelar contacto), `app/measure/page.tsx:198` (la fixture de la página de medición, que no se sirve a nadie pero es lo que `tests/measure/` mide), `app/importar/plantilla/route.ts:59` — el nombre del archivo descargable, `plantilla-rentas.csv`, **afirmado en `app/importar/plantilla/route.test.ts:55`**, así que ésta es la única del grupo que pone una prueba en rojo al cambiarla, y eso es correcto — y el `mailto:hola@rentas.com.ve` de `app/publicar/PublishStep.tsx:505`, que apunta a un buzón que hoy no existe y que la 26.15 recién crea. **HECHA el 2026-09-06** (Grupo B, PR26a). Los cuatro sueltos: la advertencia de `ContactBlock.tsx:246`, la fixture de `app/measure/page.tsx:198`, `plantilla-rentoru.csv` en `app/importar/plantilla/route.ts:59` — que puso su prueba en rojo, como esta tarea anticipaba, y eso era lo correcto — y el `mailto:` que ahora apunta a `hola@rentoru.com`, buzón que la 26.15 creó de verdad. Se actualizaron además los dos specs de Playwright que afirman el wordmark (`publish-access.spec.ts:68`, `measure/layout.spec.ts:558,559,575`), que habrían roto en el siguiente despliegue.

**Grupo C — código: el dominio, su respaldo y lo que todavía no existe.**

- [x] 26.10 **`PRODUCTION_ORIGIN` es un respaldo que decide NO fallar, y con un dominio real hay que decidir si sigue teniendo sentido.** `src/modules/listing-discovery/infrastructure/site-base-url.ts:16` declara `const PRODUCTION_ORIGIN = "https://rentas.com.ve"`, y su docblock argumenta la excepción: *«fallar dejaría el sitio entero sin sitemap por una variable de entorno faltante, cuando el dominio de producción es un dato conocido y estable. Se prefiere una cadena de respaldos explícita a un 500»* — una desviación deliberada del «fallar cerrado» de AGENTS.md §7, escrita como tal. Su prueba lo fija en dos direcciones: `site-base-url.test.ts:30` (sin ninguna variable) y `:36` (con `SITE_URL` en blanco y `VERCEL_URL` vacía). **La tarea no es reemplazar la cadena por `rentoru.com` y seguir**: es decidir, con el argumento a la vista, si un respaldo codificado sigue valiendo ahora que existe un dominio de verdad configurado en Vercel — porque hoy ese respaldo es inalcanzable en producción (26.1) y su única función real es servir a las pruebas y al desarrollo local. Las dos salidas son legítimas y hay que escribir cuál se eligió y por qué, no cambiar el literal en silencio. **HECHA el 2026-09-06** (Grupo C, primera rebanada). **Decisión del fundador: se BORRA el respaldo y se falla cerrado.** La razón, y es la que esta tarea pedía escribir: la 26.1 estableció que en Vercel `VERCEL_URL` SIEMPRE está puesta, así que el tercer escalón de la cadena era inalcanzable en producción y su única función real era servir a las pruebas y al desarrollo local. Borrarlo devuelve el módulo al «fallar cerrado» de AGENTS.md §7 a costo cero en producción, y convierte una `SITE_URL` faltante en un error ruidoso en vez de un origen equivocado servido en silencio. El mensaje sigue la forma de su hermano en la misma carpeta (`photo-public-base-url.ts:20`). El docblock viejo no se borró: se reescribió diciendo qué tarea derogó la excepción y por qué. **Y salió una consecuencia que esta tarea NO anticipaba y que habría puesto el CI en rojo al mergear**: `app/robots.ts` y `app/sitemap.ts` se PRERENDERIZAN, así que `readSiteBaseUrl` corre durante `next build` y no sólo al servir. Sin el respaldo, el export muere en `/robots.txt`. Tres jobs de CI corren `pnpm build` y ninguno declaraba `SITE_URL`; se les agregó un valor fijo e irrutable, con el comentario de por qué Vercel no necesita el equivalente.

- [x] 26.11 **`SITE_BASE_URL` no la lee NADIE en producción, y parece que sí.** Aparece en exactamente tres lugares (`rg -n 'SITE_BASE_URL'`): `.env.example:68`, que la publica como si fuera configuración real, y `app/api/jobs/expiry-reminders/route.test.ts:15,25`, que la declara y la escribe como fixture — mientras **la ruta que esa prueba ejercita lee `SITE_URL`**, que es la que `readSiteBaseUrl` consulta. **La trampa es de la peor especie**: alguien que configure el dominio nuevo va a encontrar `SITE_BASE_URL` en `.env.example`, la va a poner en Vercel, y no va a pasar absolutamente nada — ni un error, ni un aviso, ni una diferencia. Sacarla de `.env.example` y de la fixture cierra el hueco; dejarla cuesta una tarde de alguien que no va a entender por qué. **HECHA el 2026-09-06** (Grupo C, primera rebanada). `SITE_BASE_URL` fuera de `.env.example` y de la fixture de `expiry-reminders/route.test.ts`. **Y la trampa era peor de lo que esta tarea midió**: `SITE_URL` —la variable que el código SÍ lee— **no estaba en `.env.example` en absoluto**, y `SITE_BASE_URL` ocupaba su lugar con un comentario que describía lo que hace `SITE_URL`. O sea que no era sólo una clave muerta: era una clave muerta SUPLANTANDO a la viva, que es exactamente por qué nadie notó que la viva no estaba documentada. Borrarla y no poner nada habría dejado un checkout nuevo reventando en `/robots.txt` sin cura publicada. Se reemplazó en el mismo lugar por `SITE_URL`.

- [x] 26.12 **Ni etiquetas canónicas, ni `metadataBase`, ni Open Graph existen — y no hay `next.config`.** Verificado: `rg -n 'metadataBase|alternates|openGraph' app src` no devuelve **ni una línea**, sobre los **28** archivos de `app/` que declaran `metadata` o `generateMetadata`, y `fd '^next.config'` no encuentra nada en la raíz. **Esto es capacidad nueva y no una edición**, y por eso va en su propia tarea con su propia rebanada: agregar `metadataBase` en `app/layout.tsx` es la pieza que hace que toda dirección relativa de metadata se resuelva contra el dominio, y es también el único lugar donde una canónica puede apoyarse sin que cada página repita el anfitrión. **El renombre es lo que lo vuelve urgente y no lo que lo causa**: sin `metadataBase`, montar un dominio real deja las tarjetas sociales y las canónicas sin base, exactamente igual que las dejaba antes; la diferencia es que antes no había dominio del que valiera la pena hablar. Decidir si entra `next.config` también acá o si `metadataBase` alcanza sola es parte de la tarea. **HECHA el 2026-09-06**, y cierra la fase. **Decidido que `next.config` NO entra, que era parte explícita de esta tarea**: `metadataBase` alcanza —es lo que resuelve toda dirección relativa de metadata—, `next/og` es nativo por convención de archivo en Next 15 y no pide ni dependencia ni configuración de empaquetado (comprobado en el build: `/opengraph-image` prerenderiza como estático sin ningún config presente), y lo ÚNICO que un config podría guardar acá es el anfitrión, que es exactamente lo que la 26.10 prohíbe codificar. Un archivo agregado «porque la mayoría de los proyectos lo tiene» es un archivo vacío que alguien llena con lo equivocado más adelante. **`metadataBase` sale de `readSiteBaseUrl()` y nunca de un literal**, así que hereda el fallar-cerrado de la 26.10 — importar el layout ahora evalúa esa función, y se agregó una prueba que afirma que lanza, o sea que AGENTS.md §7 pasó de estar reclamado por un comentario a estarlo por una prueba. **Las canónicas son RELATIVAS**: absolutas obligarían a llamar a `readSiteBaseUrl()` en 27 archivos —la repetición del anfitrión que `metadataBase` existe para evitar— y harían que una vista previa de Vercel se canonizara a producción. **Sólo lleva canónica lo que pide ser indexado**: `noindex` y `canonical` son dos señales contradictorias, así que las rutas filtradas y el aviso vencido emiten `alternates: undefined` justo donde ya emiten `index: false`. Las cuatro rutas dinámicas reutilizan los constructores que ya existen y **no escriben una segunda construcción de URL**; derivan del nombre curado del catálogo y no del segmento que llegó, porque devolver la petición como canónica es la forma clásica del defecto. **El visor de fotos se canoniza a SÍ MISMO**, y no es una decisión nueva: la 16.5/16.7 ya había decidido «una foto, una URL», y canonizar al padre borraría del índice las seis direcciones que esa tarea creó a propósito. **La imagen de Open Graph se generó acá** (`app/opengraph-image.tsx` con `ImageResponse`, sin asset binario), derivada del sistema como AGENTS.md §2 exige: la placa `--accent` con la marca en `--accent-ink`, la bajada en `--ink`, la regla `--line` y las áreas en `--soft`. **Y sale un hallazgo sobre el gate de tokens que conviene no perder**: `lint:tokens` SÍ escanea ese archivo y NO puede verlo — su `DECLARATION_PATTERN` exige un `;` al final de línea y un objeto de JavaScript escribe `color: palette.ink,`, cosa que su propia cabecera declara fuera de alcance. Un `#272343` ahí habría pasado el build. En vez de aprovecharlo, la paleta vive en `social-card.ts` y una prueba la compara contra el bloque `[data-theme="menta"]` de `tokens.css`, que es lo que el gate haría si pudiera leerlo. **Defecto encontrado y corregido en la revisión, y vale anotarlo porque su clase es la peor**: la primera versión de la tarjeta decía «Distrito Capital · Maracaibo» escrito a mano — un área que la 17.2 renombró a «Caracas» por incorrecta y que la 17.15 borró de producción el mismo día, más la omisión de las otras cuatro. **Una tarjeta social no tiene modo de falla: nadie la ve romperse, sólo la comparte**, así que un nombre viejo ahí sobrevive años. Ahora se deriva de `AREAS` —el mismo arreglo que usa la siembra— con dos pruebas que lo atan: una exige que las cinco estén, otra prohíbe «Distrito Capital» por nombre. **Verificado mirando el PNG**, no sólo su tamaño: 1200×630, 49.082 bytes, `image/png`, y dice `Caracas · La Guaira · Maracaibo · Cabimas · Santa Rita`.

- [x] 26.13 **Las fixtures que se van a quedar verdes para siempre contra anfitriones que no van a existir, más tres pruebas que ya mienten hoy.** Medido: **20** archivos con `https://rentas.com.ve` y **20** apariciones de `fotos.rentas.com.ve` en el árbol. Las segundas nunca describieron nada real (el balde se sirve desde `fotos.gianbarboza.com`), y las primeras van a dejar de describirlo el día que `SITE_URL` apunte a `rentoru.com`. **En las dos mitades el modo de falla es el mismo y es el peor: verde permanente.** Aparte, tres pruebas ya están mal escritas y esta fase es el momento de arreglarlas porque las toca de todos modos: `app/layout.test.tsx:56` afirma `toContain("rentas.")` y **la línea de copyright del pie ya la satisface**, así que un error de tipeo en la marca misma pasa —`components/organisms/SiteFooter.test.tsx:15-19` ya corrigió exactamente este peligro en su propio archivo, con `toContain(">rentas.</a>")` y un comentario que lo explica, y `layout.test.tsx` no se actualizó—; y `app/ayuda/preguntas-frecuentes/page.test.tsx:34` y `app/legal/terminos/page.test.tsx:18` tienen NOMBRES que prometen afirmar la marca (*«states rentas.com.ve does not take part in the deal…»*) mientras sus aserciones no la miran, o sea que el renombre las deja verdes con el nombre viejo en el nombre de la prueba. **HECHA el 2026-09-06** (Grupo C, primera rebanada). Las fixtures de anfitrión pasan a `rentoru.com` y `fotos.rentoru.com`. **Las tres pruebas que mentían y que esta tarea también listaba ya estaban arregladas por el Grupo B**: se verificaron en vez de rehacerse. **Dos huecos del inventario que aparecieron al barrer y sí se arreglaron**: las fixtures de correo `ingresa@`, `avisos@` y `hola@rentas.com.ve` en cuatro archivos —no las contaba la medición porque le falta el prefijo `https://`, pero son idénticas a la mitad de `fotos.`: nunca describieron nada real— y `site-base-url.test.ts:21`, que usaba `rentas-abc123.vercel.app` como vista previa. **Y una decisión escrita, para que la próxima rebanada no la trate como olvido: los centinelas `@rentas.invalid` de 15 archivos se DEJAN.** Tres razones: `.invalid` es un TLD reservado por RFC 2606, así que esas fixtures son correctas JUSTAMENTE porque el anfitrión no puede existir, y el renombre no las hace empezar a mentir; nunca se renderizan, verificadas las cuatro formas; y renombrarlas **no es editar una fixture sino migrar datos** — `scripts/reseed-taxonomy.ts:99` compara `seed-%@rentas.invalid` con `LIKE` contra la base REAL, así que cambiar el escritor sin migrar las filas vivas deja la guardia sin reconocer a producción, o sea que dejaría de proteger en silencio en vez de fallar fuerte.

- [x] 26.14 **La fixture de ataque «un dominio que empieza igual que el nuestro» deja de modelar el ataque.** `src/modules/identity/infrastructure/redirect-callback.test.ts:28` usa `https://rentas.com.ve.evil.test/publicar` para probar que `signInRedirect` no acepta un destino cuyo texto arranca como el nuestro; la defensa real es `redirect-callback.ts:30`, `if (destino.origin !== new URL(baseUrl).origin) return ""`, que compara **orígenes** y no prefijos. Con el anfitrión real en `rentoru.com`, esa fixture pasa a ser un dominio que no se parece a nada nuestro y la prueba **sigue verde sin probar lo que dice probar**. Cambiarla a `https://rentoru.com.evil.test/publicar` la devuelve a su trabajo. Es un cambio de una línea y es el que más fácil se olvida, porque la prueba no se rompe. **HECHA el 2026-09-06** (Grupo C, primera rebanada). La fixture de ataque pasa a `https://rentoru.com.evil.test/publicar`. **No se cambió y ya: se PROBÓ por mutación que vuelve a hacer su trabajo.** Se debilitó a propósito la defensa de `redirect-callback.ts:30`, de comparar orígenes a comparar prefijos, y falló exactamente UNA prueba — la que esta tarea nombra. Defensa restaurada, 9 de 9 en verde. Sin esa comprobación, cambiar la fixture y verla verde no distingue entre «la defensa funciona» y «la prueba ya no prueba nada», que es el modo de falla exacto que esta tarea describe.

**Grupo D — configuración que sólo puede hacer el fundador, a mano.**

- [x] 26.15 **Cloudflare Email Routing para `hola@rentoru.com`, reenviado al correo personal del fundador.** La dirección está publicada en tres lugares de cara al usuario —`app/legal/privacidad/page.tsx:70`, `app/legal/datos/page.tsx:58` y el `mailto:` de `app/publicar/PublishStep.tsx:505`— y **el producto nunca recibió un correo**: no hay buzón, no hay tabla de mensajes, no hay camino de entrada, tal como la 23.7 lo dejó verificado antes de construir el formulario. **Si se salta**: las tres promesas de contacto de las páginas legales apuntan a una dirección que rebota, en páginas cuya única función es generar confianza. Dos advertencias para no descubrirlas después: Email Routing **reenvía pero no envía**, así que una respuesta sale desde la dirección personal salvo que se configure aparte un «enviar como»; y los registros MX de recepción **conviven sin conflicto** con el SPF/DKIM de envío de Resend (26.17), que son mecanismos distintos sobre la misma zona. **HECHA, ejecutada el 2026-09-05** (§4a del runbook). Cloudflare Email Routing reenvía `hola@rentoru.com` al correo personal del fundador, y el SPF del ápice quedó publicado: `v=spf1 include:_spf.mx.cloudflare.net ~all`, comprobado por DNS el 2026-09-06.

- [x] 26.16 **Google OAuth: origen autorizado y URI de redirección, más `AUTH_URL`/`NEXTAUTH_URL`.** Hay que agregar `https://rentoru.com` como origen de JavaScript autorizado y `https://rentoru.com/api/auth/callback/google` como URI de redirección. **Si se salta**: `redirect_uri_mismatch`, y nadie entra — o sea que nadie publica, nadie renueva, nadie revela un contacto y nadie reporta, porque las cuatro exigen sesión. **Y hay una segunda mitad que se olvida más seguido que la primera**: `AUTH_URL`/`NEXTAUTH_URL`, porque `src/modules/identity/infrastructure/redirect-callback.ts:30` compara el origen del destino contra el `baseUrl` que Auth.js le pasa; con un valor viejo, **todo** destino de vuelta después de entrar falla la comparación y cae al inicio — el comportamiento correcto de una defensa, aplicado a visitantes legítimos. **HECHA, ejecutada el 2026-09-05** (§3a y §3b del runbook) y confirmada por el fundador el 2026-09-06. Origen autorizado y URI de redirección cargados en Google Cloud Console, y `AUTH_URL` puesta en Vercel. **Queda dicho lo que no se observó**: el retorno de un inicio de sesión arrancado desde un deep link nunca se ejercitó, y ése es el único resultado que distingue un `AUTH_URL` correcto de uno ausente. El fundador da por buena la configuración; la observación puntual sigue sin hacerse.

- [x] 26.17 **Resend: verificar `rentoru.com`, publicar SPF/DKIM/DMARC, y RECIÉN DESPUÉS mover los remitentes.** Hoy `AUTH_MAIL_FROM` y `LIFECYCLE_MAIL_FROM` salen de `gianbarboza.com`, la deuda que `tasks.md:339` registró el 2026-08-24. **El orden importa y es lo único que puede romperlo**: cambiar las direcciones de origen antes de que el dominio esté verificado hace que **todo** envío rebote, incluido el enlace mágico — que es el único camino de entrada que no depende de Google, así que un rebote ahí y un `redirect_uri_mismatch` de la 26.16 al mismo tiempo dejan el producto sin ninguna puerta. Verificar primero, mover después, y comprobar con un envío real a una bandeja de verdad, como la 339 ya hizo una vez. Falta también el **nombre visible** del remitente, que hoy dice `Rentas`. **HECHA el 2026-09-06, con los tres registros verificados por DNS y no supuestos.** Resend verificó `rentoru.com`, los remitentes se movieron (§4b y §4c del runbook, ejecutados el 2026-09-05), el correo sale y el enlace mágico entra — el fundador lo confirma. **SPF y DKIM están publicados y correctos**: `send.rentoru.com` devuelve `v=spf1 include:amazonses.com ~all` y `resend._domainkey.rentoru.com` devuelve su clave pública, los dos comprobados por consulta DNS el 2026-09-06. **Y `_dmarc.rentoru.com` quedó publicado el 2026-09-06** como `v=DMARC1; p=none; rua=mailto:hola@rentoru.com`, comprobado contra Cloudflare `1.1.1.1`, Google `8.8.8.8` y Quad9 `9.9.9.9`. **Era el único de los tres que faltaba, y no por un olvido de Resend**: el proveedor automatiza exactamente tres registros al agregar un dominio —el SPF del subdominio de envío, el MX de rebotes y la clave DKIM—, y DMARC no está entre ellos porque `p=` es una decisión sobre TODO el correo del dominio y ningún proveedor se la puede tomar por su cuenta. **Se eligió `p=none` a propósito y NO protege todavía**: es un observatorio, no una defensa. Publicar la política dura de entrada es peor que no tenerla — `p=reject` sobre un dominio que recién empieza a enviar tira a la basura correo legítimo que ningún registro propio muestra, porque el que rebota es el servidor del que recibe. **Queda pendiente el endurecimiento y se anota acá para que no se pierda**: con un par de semanas de informes en la mano, y sólo cuando conste que todo el correo legítimo pasa, subir a `quarantine` y después a `reject`. Un `p=none` publicado y olvidado es una tarea marcada que no defiende de nada. El `rua` apunta a `hola@rentoru.com`, del MISMO dominio, a propósito: un `rua` hacia otro dominio exige que ese dominio publique un registro de autorización, y contra un buzón de un proveedor ajeno eso no se puede hacer, así que los informes se descartarían en silencio. **Los encabezados SPF/DKIM de un correo recibido tampoco se abrieron nunca** — están publicados, lo que hace probable que pasen, y probable no es comprobado. **Lo único que sigue sin observarse, y se deja escrito en vez de inferirse**: nadie abrió los encabezados de un correo recibido para ver los tres en verde. Los registros están publicados y comprobados en el DNS, que es la fuente de verdad de esta tarea; el correo llega desde `@rentoru.com` y el enlace mágico entra, así que la comprobación puntual es probable — y probable no es comprobado.

- [x] 26.18 **Cloudflare R2: dominio propio `fotos.rentoru.com`, su DNS, y después `R2_BUCKET_PUBLIC_URL`.** Es la otra mitad que vivía en el dominio personal (`fotos.gianbarboza.com`). **El balde NO se renombra** y conviene decir por qué antes de que alguien lo intente: hoy se llama `rentas-photos` (`src/modules/listing-publication/infrastructure/r2-photo-storage.ts:312`, `r2-photo-storage.test.ts:28`), R2 **no permite renombrar en el lugar**, hacerlo obliga a copiar cada objeto y a migrar las claves de `listing_photo_derivative` (`src/shared/db/schema.ts:680`), y **nada visible para el usuario lleva el nombre del balde** — el costo es alto y el beneficio es cero. **Si se salta el orden**: quitar el dominio propio viejo antes de cambiar la variable deja **todas** las fotos del sitio en 404, incluidas las del arreglo `image` del JSON-LD, o sea también en el resultado de Google. **HECHA, ejecutada el 2026-09-05** (§5b y §5c del runbook). Dominio propio `fotos.rentoru.com` con su DNS, y `R2_BUCKET_PUBLIC_URL` movida DESPUÉS, que es el orden que esta tarea exige. Verificada de la forma más dura posible el 2026-09-06: el fundador subió fotos reales por el flujo de publicar y se sirven.

- [x] 26.19 **CORS de R2: agregar `https://rentoru.com` a `AllowedOrigins`, conservando `AllowedMethods: ["PUT"]` y `AllowedHeaders: ["content-type"]`.** La política actual está transcrita en `docs/going-live.md:38-51` y sólo permite `https://rentascomve.vercel.app`, `https://*.vercel.app` y `http://localhost:3000`. **Ésta es la falla silenciosa insignia de este proyecto**: el navegador bloquea la subida **antes de que salga**, así que ningún servidor la registra porque la petición nunca llega a uno — y ya se embarcó a producción exactamente así el **2026-08-18**, con 42 pruebas verdes, hasta que el fundador intentó subir una foto. Esta tarea **se enlaza con la 3.17 y la 3.19, que siguen abiertas**: el hallazgo de la 3.19 es que lo único que lo caza es una aserción `GetBucketCors` contra el balde de producción, porque CI corre desde un origen que ya está permitido y por eso una prueba de subida real tampoco lo detectaría. **HECHA. La configuración se aplicó el 2026-09-05 y quedó verificada el 2026-09-06**, cuando el fundador subió una foto real a través del flujo de publicar — que es la única verificación que esta tarea acepta, y que estuvo bloqueada hasta que la 17.15 puso la taxonomía en producción y el paso 2 volvió a ofrecer zonas. **El origen se confirmó explícitamente y ése es el punto entero de la verificación: la subida salió desde `https://rentoru.com`, el ápice, NO desde una URL `*.vercel.app` de preview.** La distinción no es un tecnicismo: el comodín `https://*.vercel.app` sigue en la política a propósito, así que una subida desde un preview habría pasado por él y no habría probado nada sobre el ápice — el dominio del producto podría haber seguido bloqueado con la prueba en verde, que es la forma exacta del defecto del 2026-08-18. La política vigente es `https://rentoru.com`, `https://*.vercel.app` y `http://localhost:3000`; la entrada específica `https://rentascomve.vercel.app` se eliminó y el comodín se conservó deliberadamente. **Lo que esto NO cierra**: sigue sin haber una afirmación automática, y por eso **la 3.19 y la 3.17 quedan abiertas y ahora son el único guardián que falta de este camino**. Esta verificación es un par de ojos una vez, no un gate: nadie va a notar el día que alguien edite la política del balde, porque el navegador bloquea la subida ANTES de que salga y ningún servidor la registra.

- [x] 26.20 **Variables de entorno en Vercel, la lista completa y lo que NO va en ella.** `SITE_URL`, `AUTH_URL`, `AUTH_MAIL_FROM`, `LIFECYCLE_MAIL_FROM`, `CONTACT_MAIL_TO` y `R2_BUCKET_PUBLIC_URL`. Y explícito, porque es la trampa que la 26.11 documenta: **NO poner `SITE_BASE_URL`** — no la lee nadie, aparece sólo en `.env.example:68` y en una fixture de prueba, y ponerla da la sensación de haber configurado el dominio sin haber configurado nada. **HECHA, ejecutada el 2026-09-05** (§6 del runbook). La lista completa cargada en Vercel, y `SITE_BASE_URL` ELIMINADA — la trampa que la 26.11 documenta: no la lee nadie y ponerla da la sensación de haber configurado el dominio sin haber configurado nada.

**Grupo E — diseño y artefactos de planificación.**

- [x] 26.21 **Las once láminas de `design/pantallas/` llevan la marca, y `Rentas - Footer.dc.html` lleva las únicas cuatro cadenas literales `rentas.com.ve` de todo el árbol de diseño.** Están en las líneas **71, 72, 110, 111** de esa lámina — las dos líneas de la franja del pie, una vez en el artboard de escritorio (9a) y otra en el de móvil (9b) —, y son las mismas dos líneas que `components/organisms/SiteFooter.tsx:15,16` dibuja (26.4), así que si se toca una sola de las dos mitades la lámina y el código dejan de decir lo mismo. **Tocar una lámina es un cambio a la fuente de verdad visual (AGENTS.md §2)**, así que se registra como tarea propia y no se hace de paso mientras se edita otra cosa. **HECHA el 2026-09-06** (Grupo E). Las once láminas llevan `Rentoru` y las cuatro cadenas literales de `Rentoru - Footer.dc.html` pasan a `rentoru.com`. **Las dos mitades quedaron idénticas byte a byte** — la lámina y `SiteFooter.tsx:15,16` —, comprobado comparando bytes y no leyéndolos, que era el riesgo exacto que esta tarea nombra. **Y apareció algo que este inventario no tenía: el wordmark vive en DOS formas dentro de las láminas** — `>rentas.<` en el stack `--disp` y `>rentas<` sin punto en el `--mono` —, así que contar sólo la primera dejaba 19 apariciones atrás. Las dos colapsaron en `Rentoru`: 51 apariciones de `>Rentoru<`, ninguna con punto.

- [x] 26.22 **Los once nombres de archivo de las láminas empiezan por `Rentas - `, y renombrarlos rompe una prueba que resuelve una por ruta.** `app/alquiler/[ciudad]/[zona]/[slug]/ficha-escritorio.test.ts:15` construye `new URL("../../../../../design/pantallas/Rentas - Ficha - Desktop.dc.html", import.meta.url)` — es la única que **lee** un archivo de lámina y por lo tanto la única que se pone roja de verdad. Las otras ocho referencias son comentarios que quedarían apuntando a archivos inexistentes: `app/publicar/step-copy.ts:20`, `app/publicar/photo-action-copy.ts:8`, `app/publicar/paso/[paso]/mapa-de-pasos.test.tsx:12`, `app/publicar/paso/[paso]/descartar-el-cambio.test.tsx:6`, `app/publicar/revisar/aviso-de-cambio.test.tsx:7`, `app/measure/page.tsx:691`, `components/organisms/SiteFooter.tsx:35` y `src/modules/site-footer/domain/footer-links.ts:5` — más `components/organisms/SiteFooter.module.css:1` y `src/styles/tokens.css:148,252,439`, que también las nombran por archivo. **Si se renombran o no es una decisión que vale la pena nombrar en vez de tomar de paso**: un nombre de archivo no lo ve ningún usuario, viene del proyecto de diseño del fundador y renombrar cuesta trece referencias; dejarlos deja once archivos con el nombre viejo dentro de un repositorio que ya no se llama así. **HECHA el 2026-09-06** (Grupo E). Los once archivos renombrados con `git mv`, así que la historia los sigue. **La única que se puso roja de verdad fue `ficha-escritorio.test.ts`** —`ENOENT` sobre la ruta vieja—, y eso era lo correcto. **Y hubo una referencia que ningún reemplazo por nombre podía cazar**: `app/measure/page.tsx:691` parte la cadena a mitad, entre dos líneas de comentario (`Rentas -\n * Footer.dc.html`), así que un `sd 'Rentas - '` le pasa por al lado. La encontró un `rg -nU` multilínea. Vale anotarlo para cualquier renombre de rutas futuro. **Y una consecuencia que el fundador levantó y que esta tarea no tenía: el renombre alcanza a las TAREAS de este archivo, no sólo al código.** Medido el 2026-09-06: 23 referencias a los nombres viejos dentro de `tasks.md`. **La regla que las separa no es la antigüedad sino el uso: si alguien tiene que SEGUIR la referencia para hacer trabajo abierto, se actualiza; si sólo REGISTRA lo que pasó, se deja**, y ahí manda la 26.26. Repartición: **18 en tareas ya cerradas** (3.16, 14.12, 14.23, 14.40 ×2, 14.43, 14.45, 14.55, 15.12, 15.15, 16.29, 18.9 y otras) y **2 en prosa de fases sin tareas abiertas relacionadas** — la 14, que tiene 0 abiertas, y el relato de la 23 sobre cómo llegó la lámina del pie —, todas historia y todas intactas. **Y 3 que sí eran instrucciones vivas, corregidas**: la prosa de la Fase 20 que apunta a `Flujos y funcionalidades.md` §7ter (3 tareas abiertas la usan), la **22.8** y la **22.34**. Las dos de la 22 importan porque esa fase tiene **20 tareas abiertas** y es la próxima a atacar: la 22.34 además cita números de línea DENTRO de las láminas (191, 150, 182) — se verificaron y **siguen siendo correctos**, porque las sustituciones de la 26.21 fueron en el lugar y no corrieron ningún archivo.

- [x] 26.23 **El resto del árbol de diseño.** `design/reference/sistema/SISTEMA.md` en las líneas **1** (el título del sistema), **15** (la descripción del producto), **204** y **233** (dos textos de pantalla que la 26.9 también toca en el código: *«Rentas no participa en la negociación…»* y *«Rentas es gratis y sin comisión…»*, o sea que acá también las dos mitades tienen que moverse juntas), **323** (la definición de la marca, la línea que le da su peso a toda esta fase) y **401**; `design/reference/sistema/pantallas-compacto-menta.html`; y `design/reference/BRIEF-PANTALLAS.md`. Los cuatro archivos de `design/especificaciones/` —`Rentas - Ficha - Especificacion.md`, `Rentas - Flujos y funcionalidades.md`, `Rentas - Publicar - Especificacion.md` y `Rentas - UX movil.md`— llevan la marca **sólo en el nombre del archivo**, y le aplica la misma decisión de la 26.22, con una referencia más a considerar: `docs/publicar-funcionalidades.md:5` los cita por nombre. **HECHA el 2026-09-06** (Grupo E). `SISTEMA.md` en sus seis líneas, `pantallas-compacto-menta.html` y `BRIEF-PANTALLAS.md`. **La 323 cierra la contradicción que el Grupo B dejó abierta a propósito**: define la marca como `Rentoru`, con la mayúscula y la ausencia de punto escritas como razón, y los punteros que quedaron en los docblocks de `Nav.tsx` y `SiteFooter.tsx` apuntando acá se retiraron porque ya no apuntan a nada pendiente. **Tres correcciones al propio inventario**: los cuatro archivos de `design/especificaciones/` NO llevaban la marca «sólo en el nombre del archivo» como esta tarea afirma — la llevaban adentro también, en encabezados y en la copia de advertencia de F30; la línea **401** nombraba un archivo que NO EXISTE (`Rentas - Compacto Menta.dc.html` contra el real `pantallas-compacto-menta.html`), así que se la apuntó al archivo de verdad en vez de rebautizar un nombre colgado; y `Rentoru - UX movil.md:4` cita una lámina que no existe bajo ningún nombre, defecto PREEXISTENTE que se deja porque ese documento está explícitamente superado y se conserva por procedencia.

- [x] 26.24 **La identidad del repositorio, que es lo que ve quien llega de afuera.** `package.json:2` (`"name": "rentas.com.ve"`), `README.md:1`, `AGENTS.md:5`, `openspec/config.yaml:4`, los seis archivos bajo `docs/` que nombran la marca (`docs/caso-de-negocio.md`, `docs/propuesta-de-producto.md`, `docs/publicar-funcionalidades.md`, `docs/resumen-tecnico.md`, `docs/territorio/README.md`, `docs/territorio/zulia/README.md`) y el remoto de git, hoy `https://github.com/gianelo/rentas.com.ve.git`. **El remoto es el único de la lista que no es una edición de archivo**: renombrar el repositorio en GitHub cambia la dirección de todos los enlaces existentes —GitHub redirige, pero los clones locales siguen apuntando al nombre viejo hasta que alguien corra `git remote set-url`—, así que es una acción con coordinación y no un `sd` más. **HECHA el 2026-09-06.** Ediciones de archivo: `package.json:2` pasa a `"name": "rentoru.com"`, `README.md:1`, `AGENTS.md:5`, `openspec/config.yaml:4` y los seis archivos de `docs/`. **El lockfile NO cambia, y se comprobó en vez de suponerlo**: tras el renombre se corrió `pnpm install --lockfile-only` y `git diff --numstat pnpm-lock.yaml` volvió vacío — pnpm indexa sus `importers` por directorio (`.`) y nunca por el nombre del paquete raíz. **Y el remoto, que era lo único de esta lista que NO es una edición de archivo, lo cerró el fundador el mismo día**: el repositorio pasó a llamarse `gianelo/rentoru` en GitHub —**en minúscula, igual que el dominio**, no `Rentoru` con mayúscula como sí lleva el wordmark de la 26.2— y el clon local corrió `git remote set-url origin https://github.com/gianelo/rentoru.git`. Verificado después del cambio: `fetch` y `push` resuelven contra la dirección nueva, `origin/dev` sigue enlazada, y un `git fetch` real vuelve limpio. **Barrido de la dirección vieja: cero apariciones** de `github.com/gianelo/rentas` en el repositorio, y `package.json` no declara `repository`, `homepage` ni `bugs`, así que no había ningún metadato que corregir. **Lo que hay que recordar y no se ve desde acá: sólo se actualiza el clon donde se corrió el comando.** GitHub redirige el nombre viejo, así que nada se rompe de inmediato, pero cualquier otro clon —otra máquina, o un CI que clone por URL en vez de por la integración— sigue apuntando al viejo hasta que alguien lo actualice ahí también.

- [x] 26.25 **`docs/going-live.md` ya es una lista de verificación para exactamente esta clase de cambio, y hoy está doblemente vieja.** Se escribió para el salto de `rentascomve.vercel.app` a un dominio real, o sea el trabajo de esta fase escrito antes de tiempo y con el nombre equivocado; su sección de política CORS (`:38-51`) transcribe orígenes que ya no van a ser los correctos, y su relato del incidente del 2026-08-18 sigue siendo la mejor evidencia que tenemos de por qué la 26.19 importa. **Reescribirlo para `rentoru.com` es tarea propia y es el hogar natural de los pasos del grupo D**: hoy esos seis pasos viven sólo acá, en un archivo de tareas que se archiva cuando el cambio cierra, y alguien va a necesitarlos otra vez. **HECHA el 2026-09-06** (Grupo E). `docs/going-live.md` reescrito para `rentoru.com`, con la política CORS vigente (`https://rentoru.com`, `https://*.vercel.app`, `http://localhost:3000`). Se conservaron dos cosas a propósito: **el relato del incidente del 2026-08-18**, que sigue siendo la mejor evidencia de por qué la 26.19 importa, y **la sección de siembra** que la 17.15 agregó, que es actual y correcta.

- [x] 26.26 **NO reescribir las menciones históricas de este archivo.** `tasks.md` nombra `rentas.com.ve` en las líneas **98, 339, 1467, 2166, 2167 y 2169**, y las seis son **registros de trabajo cerrado**: lo que se decidió, con qué evidencia y en qué fecha. La convención de este archivo es **agregar la corrección abajo, no editar la historia** —es lo que la 18.30 hizo con su puente, lo que la 25.4 hizo con una consecuencia técnica mal anticipada y lo que la 25.1 hizo con una pregunta mal planteada—, y reescribirlas dejaría seis entradas afirmando cosas que en su fecha no eran ciertas. **La corrección de la 339 en particular ya está escrita en la prosa de esta fase**: dijo *«`rentas.com.ve` is not owned yet»* y hoy sabemos que la razón real era el TLD `.ve` y no la propiedad. Esa aclaración vive acá, en la 26, y la 339 se queda como está. **VERIFICADA el 2026-09-06** (Grupo E, PR26d). Las seis líneas se leyeron una por una y **ninguna se tocó**: siguen diciendo `rentas.com.ve` a propósito. **Tres de los seis números que esta tarea anotó ya no apuntan a lo que nombraban, y la corrección va acá y no arriba**: el archivo creció entre que se escribió la 26.26 y que se ejecutó —los registros del Grupo D, la 17.15, la 17.16 y la 17.17—, así que **2166, 2167 y 2169 son hoy 2181, 2182 y 2184**, o sea la 23.1, la 23.2 y la 23.4, que son exactamente las mismas tres entradas que la tarea quería proteger. Las otras tres —98 (la 0.7), 339 (la deuda de Resend) y 1467 (el diagnóstico de que no había remitente)— no se movieron. Medición: `rg -n 'rentas\.com\.ve' openspec/changes/mvp-rental-listings/tasks.md` devuelve esos seis números más los de la prosa y las tareas de esta misma fase, que **no** son historia cerrada y que sí nombran el dominio viejo porque es de lo que hablan. **Y la corrección de la 339 se confirmó escrita**: vive en el tercer párrafo de la prosa de esta fase, el que dice *«donde hoy sabemos que la razón real no era la propiedad sino el TLD»*, así que la 26.26 no tenía nada que escribir salvo esta constancia. **Su estado terminado es este párrafo**: la prueba de que se leyeron, de que se dejaron, y de dónde quedaron. **HECHA el 2026-09-06** (Grupo E). Las seis líneas se leyeron y se dejaron: el diff de `tasks.md` en esta rebanada es de una línea, y es el propio párrafo de esta tarea. **Tres de sus seis números habían derivado** — las 2166, 2167 y 2169 son hoy la 2181, 2182 y 2184, porque el archivo creció con los registros del Grupo D, la 17.15, la 17.16 y la 17.17; siguen siendo las mismas tres entradas (23.1, 23.2 y 23.4). Las 98, 339 y 1467 no se movieron. **Su estado terminado no era el silencio**: una tarea de «no hagas esto» cuyo inventario se desactualiza necesita el registro escrito, y la convención de este archivo manda agregar abajo y nunca encima.

**Grupo F — lo que esta fase deliberadamente NO hace.**

- [x] 26.27 **Los tres no-objetivos, escritos para que no se reabran.** **(1) No hay 301 desde `rentas.com.ve`**, porque el sitio nunca se sirvió desde ahí: no hay autoridad de enlace que transferir, no hay direcciones indexadas que preservar y no hay contenido duplicado entre dos anfitriones — quien vuelva a plantearlo tiene que traer primero evidencia de una dirección `rentas.com.ve` que alguna vez haya devuelto una página. **(2) No se renombra el balde de R2** (`rentas-photos`), por lo que la 26.18 argumenta: R2 no renombra en el lugar, hacerlo obliga a copiar cada objeto y a migrar las claves de `listing_photo_derivative`, y nada visible para el usuario lleva ese nombre. **(3) El logotipo y la identidad visual quedan para después**, decisión del fundador el 2026-09-05: esta fase cambia la palabra, no el sistema de color, la tipografía ni la ausencia de logotipo que `SISTEMA.md:323` declara — y esa ausencia sigue siendo la definición vigente de la marca hasta que el fundador diga otra cosa. **HECHA el 2026-09-06.** **Su estado terminado no era escribirlos —ya estaban escritos acá— sino ponerlos donde alguien que fuera a reabrirlos los encuentre, y comprobar que los tres se sostienen de verdad.** **Dónde viven ahora, y son dos lugares con trabajos distintos**: `docs/rentoru-cutover.md` §9 ya los registraba los tres citando esta tarea por número, pero ése es el registro de lo YA EJECUTADO del Grupo D y nadie lo abre para preguntarse si conviene redirigir; `docs/going-live.md` §*What is not part of a rename* es la lista mirando hacia adelante, la que alguien SÍ va a leer la próxima vez, **y tenía sólo dos de los tres** — se agregó el del logotipo y la identidad visual. **Comprobado que los tres se sostienen en el árbol, no supuesto: (1)** no existe `next.config` en absoluto y `middleware.ts` no menciona el host viejo, así que no hay redirección declarada en ninguna parte; **(2)** el balde sigue siendo `rentas-photos`, sin tocar; **(3)** `SISTEMA.md:323` sigue declarando que no hay logotipo — **la ausencia sobrevivió al renombre**, que es justamente lo que este no-objetivo protege: la fase cambió la palabra y dejó intacta la definición. **Y la carga de la prueba queda escrita en los dos lugares**: quien reabra el 301 tiene que traer primero evidencia de una dirección `rentas.com.ve` que alguna vez haya devuelto una página.

## Fase 27 — La cuota se agotó en un día, y el sitio entero se cayó con ella (2026-09-07)

**Por qué existe.** El 2026-09-07 producción devolvió 500 en **todo lo que consulta la base** —inicio, ciudad, zona, `sitemap.xml`— y el despliegue falló en `pnpm drizzle-kit migrate`. La causa no estaba en el código: **Neon había agotado la transferencia de red del mes, 13,48 de 5 GB**, y cortaba las conexiones. `Compute` iba en 9,35 de 100 y `Storage` en 0,09 de 0,5: **la única barra agotada era la transferencia**.

**Lo que HAY hoy, medido y no supuesto.** La medición se corrió **entera en local**, sin tocar producción ni gastar cuota, con el arnés que la 11.22 ya construyó: Postgres del `docker-compose.yml`, `scripts/neon-http-proxy.mjs` y `pnpm db:seed:taxonomy`. Los números:

- **5.813 zonas y 18 ciudades** tras la siembra real.
- **`DrizzleCatalogue.listZones()` devuelve 679 kB por llamada** — y son los bytes crudos de las seis columnas, **antes** del envoltorio JSON con el que Neon los manda por HTTP.
- Es un `SELECT ... FROM zone ORDER BY name` **sin `WHERE` y sin `LIMIT`**, y lo llaman las **tres** pantallas del camino de lectura, todas dinámicas: `app/alquiler/[ciudad]/page.tsx:56`, `app/alquiler/[ciudad]/[zona]/page.tsx` y `suggest-active-listings.ts`.
- Por ciudad: **Caracas 3.220 zonas / 316 kB**, Maracaibo 1.464 / 149 kB, La Guaira 791 / 75 kB.

**La cronología cierra el caso.** Hasta el 5 de septiembre producción tenía **10 zonas** y cada vista costaba ~1 KB. La resiembra de la **17.15**, el 6 de septiembre, las llevó a 5.813: **el coste por vista se multiplicó por ~580**, y la cuota murió al día siguiente. 13,48 GB ÷ 679 kB ≈ **20.000 vistas**, con **un solo aviso publicado**. Los avisos nunca fueron el peso.

**Lo que NO hay.** Ningún techo que impida que una consulta del camino de lectura devuelva un conjunto sin límite, ninguna regla escrita que diga que una decisión de diseño dependía del tamaño de una tabla, y **ninguna alarma**: el fundador se enteró del consumo con el sitio ya caído. Tampoco hay separación entre las vistas previas y producción — `scripts/deploy-migrate.mjs` lo documenta con todas las letras: *«a preview's `DATABASE_URL` is the production database»*.

**Dos hipótesis que se descartaron por el camino, anotadas para que nadie las repita.** La primera: Vercel había subido Node de 22 a 24 porque `engines` decía `">=22"`, un rango abierto. Se fijó en `22.x` y **falló idéntico** — no era. La segunda: el aviso de `pg` sobre `sslmode`; pero ese aviso sale en las dos versiones de Node y `pg` 8.23 está desde el 16 de agosto, o sea que ya estaba el día que funcionó. **El fijado de Node se conserva igual**, porque destapó un hueco real: el CI fijaba 22 y producción flotaba.

- [x] 27.1 **El dominio deja de filtrar en JavaScript lo que Postgres puede filtrar con un índice.** Es la causa, y la tarea que sola resuelve el problema.

    **Lo que hace hoy, verbatim.** `resolveZoneRoute` vive en `domain/` y decide así si una URL existe:

    ```js
    zones.find((c) => c.cityId === city.id && slugify(c.name) === zoneSlug)
    ```

    Eso es un `WHERE` de dos condiciones **hecho a mano en JavaScript, sobre 5.813 elementos, después de haberlos traído por la red**. Lo mismo `boundedVocabulary`, que —como su nombre dice— recibe el catálogo entero y lo acota acá.

    **Y el código NO está mal escrito: está escrito para un mundo que dejó de existir el 6 de septiembre.** La regla de `AGENTS.md` §3 es que el dominio no habla con la base: recibe datos planos y decide. Por eso la página consulta y le pasa el arreglo. **Con 10 zonas eso costaba ~1 KB**, y a ese precio mantener el dominio puro y probable sin base de datos ni dobles es un cambio excelente. Nadie se equivocó.

    **El defecto real es que la condición nunca se escribió.** «Esto funciona mientras el catálogo sea chico» no estaba en ningún lado, así que cuando la resiembra de la 17.15 lo llevó de 10 a 5.813 **nada se puso rojo**: el código siguió haciendo exactamente lo mismo, y lo mismo pasó a costar 580 veces más.

    **El arreglo no abandona la arquitectura; la respeta mejor.** El puerto expone la búsqueda —`findZoneBySlug(ciudad, zona)` y sus hermanas—, Postgres la resuelve con un índice y devuelve **una fila**. El dominio sigue decidiendo las REGLAS: qué hace válida una ruta, la ambigüedad de que «Centro» exista en Maracaibo y en Caracas, qué pasa si no coincide. La infraestructura hace la BÚSQUEDA. **Hoy el dominio está usurpando el trabajo del motor**, y eso es menos hexagonal, no más.

    **Lo que cambia, medido:** resolver la URL pasa de 5.813 filas y 679 kB a **una fila y ~120 bytes**; el panel de filtros pasa a pedir **sólo las zonas con avisos** —hoy, con un aviso publicado, **una**—; y el nombre de zona de cada tarjeta, sólo el de los avisos mostrados. **El mismo pedido, la misma pantalla, el mismo resultado en el navegador.** Lo único que cambia es quién filtra.

    **La única parte que necesita una decisión.** La coincidencia de la búsqueda —alguien escribe «chaca» y hay que encontrar «Chacao»— sí mira muchos nombres, y no se resuelve con una fila. Pero tampoco necesita 5.813 en cada carga de página: se resuelve **cuando alguien escribe, no cuando alguien mira**, con una consulta de coincidencia o con el índice de topónimos que ya existe (`toponym-resolve.ts`). Esa parte se decide aparte y con su medición.

    **Cuatro caminos que se descartaron, escritos para que no se reabran.** **(1) Caché con revalidación**: baja el consumo pero sigue leyendo, y hay que invalidarla a mano en cada resiembra. **(2) Generar el catálogo en tiempo de compilación desde `docs/territorio/`**: convertiría documentación en dependencia de ejecución, y `territorio-files.ts` lee con `readdirSync("docs/territorio")` —ruta relativa que **no sobrevive al empaquetado serverless**—. **(3) Acotar por ciudad y nada más**: no alcanza, Caracas sola son 3.220 zonas y 316 kB. **(4) Cambiar a una base NoSQL**: no resuelve nada —el proveedor cobra egreso, no consultas, y los mismos datos pesan lo mismo— y se perdería la clave foránea que hoy impide que un aviso apunte a una zona inexistente.

    **La base sigue siendo la única fuente de verdad**, y `docs/territorio/` sigue siendo lo que dice ser. Sólo deja de mandarse la tabla entera para contestar una pregunta de una fila. **Con la medición antes y después escrita acá.**

    **Rebanada A de cuatro, hecha — la columna `slug` que el puerto indexado va a necesitar. La casilla NO se marca**: sin la B (el puerto y su consulta), la C (`resolveZoneRoute` sobre una fila) y la D no hay un solo camino de lectura que deje de traer la taxonomía entera. `city.slug` y `zone.slug` (`src/shared/db/schema.ts`), llenados por la MISMA `slugify` que `resolveZoneRoute` y `buildListingPath` ya usan (`src/modules/listing-discovery/domain/listing-url.ts`) — importada, nunca reescrita, en los tres caminos que crean filas: `buildTerritoryRows` (`territorio-import.ts`, y de ahí `seed.ts` y `scripts/reseed-taxonomy.ts`, que llama a `seedTaxonomy`) y `scripts/seed-e2e.ts`. **Nullable a propósito, y la razón está en el comentario de `schema.ts`**: `slugify` hace NFD y un regex de JS que una migración SQL no puede reproducir sin reimplementar la regla — la misma duplicación que la 22.31 ya rechazó — así que la columna sólo puede AGREGARSE, nunca llenarse desde la migración (mismo patrón que drizzle/0006 y 0007 para una `NOT NULL` sin valor que darle a las filas existentes, sólo que acá ni siquiera hay un valor literal que poner). El backfill real vive en `taxonomy-slug-backfill.ts` y corre desde `scripts/taxonomy-smoke.ts` — que ya se ejecuta después de migrar en cada despliegue — y es un no-op verificado cuando no hay ningún `slug IS NULL`. El gate que falla cerrado, `findTaxonomySlugGapsIn`, no confía en el conteo que devuelve el backfill: vuelve a leer la base entera y compara cada fila contra `slugify(name)`. **Sin índice único** en `slug` ni en `(city_id, slug)` — la 27.7 mide 335 grupos de `(city_id, slug)` compartidos por 779 zonas reales y distintas, y un índice único rechazaría el backfill. Probado con RED→GREEN→REFACTOR: unitaria en `territorio-import.test.ts` (deriva el slug con la `slugify` real, no una copia) y en `taxonomy-slug.test.ts` (el gate de dominio), e integración contra el contenedor real en `taxonomy-slug-backfill.test.ts` — siembra la taxonomía real (5 áreas, 5.796 zonas) y afirma que ninguna fila queda con slug NULL o distinto de `slugify(name)`, que un backfill sobre filas puestas en NULL a mano las corrige a todas, y que un segundo backfill no toca nada. **Sin cambios de pantalla, de puerto ni de `resolveZoneRoute`**: eso es la rebanada B en adelante.

    **Rebanada B de cuatro, hecha — el puerto indexado y la página de zona. La casilla SIGUE ABIERTA**: falta la C (`boundedVocabulary` y el panel de filtros dejan de recibir las 5.813 zonas) y la D (`app/page.tsx` y `suggest-active-listings.ts`) — sin las dos, siguen quedando caminos de lectura que traen la taxonomía entera. `ZoneRoutePort.findZoneBySlug(citySlug, zoneSlug)` (`src/modules/listing-catalogue/application/ports/zone-route.port.ts`) resuelve la ciudad por `city.slug` y, dentro de ella, TODAS las zonas que comparten `zone.slug` — un arreglo, nunca una fila, porque la 27.7 (fundador, 2026-09-07) ya decidió que la ruta nombra un lugar y no una fila; diseñar el puerto para una sola fila hoy habría forzado un segundo cambio de firma el día que la 27.7 se implemente. **Puerto propio, y no un tercer método de `CataloguePort`**: cada consumidor de `CataloguePort` hoy —`broker-bulk-import`, `suggest-active-listings`— lo falsea como objeto plano y ninguno resuelve una ruta; sumarle un método obligatorio les habría exigido implementar una consulta que nunca llaman, y el primer intento de esta rebanada lo probó rompiendo 13 pruebas ajenas al cambio antes de separarlo. `DrizzleCatalogue` implementa los dos puertos en la misma clase. **Esta rebanada NO actúa sobre el plural**: `resolveZoneRoute` (`src/modules/listing-discovery/domain/zone-route.ts`) sigue siendo la misma función pura, con la misma firma, y sigue eligiendo una sola zona del arreglo que recibe — el mismo comportamiento y la misma ambigüedad latente que la 27.7 midió y va a corregir. Lo único que cambió es de dónde sale ese arreglo: antes, `app/alquiler/[ciudad]/[zona]/page.tsx` pedía `loadCatalogue()` —`listCities()` + `listZones()`, la taxonomía entera— para hacerle a `resolveZoneRoute` una pregunta de una fila; ahora le pregunta a `findZoneBySlug` con los dos segmentos exactos de la URL, y `loadCatalogue()` queda sólo para lo que la rebanada C todavía no resuelve: el panel de filtros y el vocabulario acotado. `generateMetadata` deja de pedir la taxonomía por completo, porque nunca necesitó más que el lugar. **Medido contra el contenedor real** (`rentas_test`, la siembra real de 5 áreas y 5.796 zonas, la misma que usa `taxonomy-slug-backfill.test.ts`), antes de escribir la consulta y no después: `listCities()` + `listZones()` devuelven 5.801 filas y ~1.207 KB (`pg_column_size`); el mismo lugar por `findZoneBySlug`, para una zona real sin nombre compartido (`Maracaibo` / `sector-san-rafael`), devuelve 2 filas y 286 bytes con `Index Scan using zone_slug_idx` (`EXPLAIN ANALYZE`, confirmado y no supuesto); un nombre real compartido entre parroquias (`Caracas` / `barrio-la-cruz`, 8 parroquias) sigue devolviendo apenas 9 filas. Probado con RED→GREEN→REFACTOR: `tests/integration/catalogue.test.ts` cubre que se devuelven la ciudad y TODAS las zonas que comparten su slug dentro de ella, que el slug de zona no cruza hacia la ciudad vecina que lo comparte, `null` cuando el segmento de ciudad no nombra una ciudad curada, la ciudad con un arreglo vacío cuando la zona no existe en ella, y que las filas que emite se consumen sin cambios por `resolveZoneRoute`; a nivel de página, `zona-sin-javascript.test.tsx` afirma que la ruta le pregunta a `findZoneBySlug` con los segmentos exactos de la URL y que una ciudad o zona que el índice no conoce sigue devolviendo 404. **Sin cambios de dominio**: la regla de qué hace válida una ruta, la ambigüedad de nombres compartidos y el 404 siguen exactamente iguales, sólo que alimentados por el índice en vez de la taxonomía entera. **Apretada tras la revisión de cuatro lentes, y la corrección vale escribirla porque cuatro revisores ciegos entre sí levantaron la MISMA línea**: la prueba del 404 aseveraba `rejects.toThrow()` a secas, es decir CUALQUIER excepción — un fallo de la consulta al índice habría pasado en verde informando un 404 que nunca ocurrió. Ahora afirma el `digest` exacto que `notFound()` emite en Next 15 (`NEXT_HTTP_ERROR_FALLBACK;404`), que es por donde el enrutador decide servir la página 404. **Probado con RED real**: sustituir `notFound()` por un `throw` cualquiera en `app/alquiler/[ciudad]/[zona]/page.tsx` pone roja esa prueba y sólo esa (1 de 10) — exactamente el escenario en el que la aserción vieja seguía verde—, y el archivo se restauró byte a byte. **Falta**: la C y la D.

    **Rebanada C de cuatro, hecha — `boundedVocabulary` y el panel de filtros dejan de recibir las 5.813 zonas. La casilla SIGUE ABIERTA**: falta la D (`app/page.tsx` y `suggest-active-listings.ts`) — sin ella sigue quedando un camino de lectura que trae la taxonomía entera. `ActiveCityZonesPort.listActiveZones(cityId)` (`src/modules/listing-catalogue/application/ports/active-city-zones.port.ts`) devuelve sólo las zonas de UNA ciudad con avisos activos, ya contadas — el mismo `GROUP BY` que `DrizzleActiveZones` (listing-discovery) ya corre para el inicio, acá recortado con `city_id` en el `WHERE`. **Puerto propio, y no un tercer método de `CataloguePort`**: la misma lección que la rebanada B ya pagó — `broker-bulk-import` y `suggest-active-listings` no llaman esta pregunta, y sumarles un método obligatorio les habría exigido implementar una consulta que nunca usan. `DrizzleCatalogue` implementa las tres interfaces en la misma clase. Las dos páginas de resultados dejan de llamar `loadCatalogue()` —`listCities()` + `listZones()`— para armar el panel y las sugerencias: piden `listCities()` (18 filas, sin cambios) y `listActiveZones(city.id)` por separado, y `boundedVocabulary` (que filtraba un `Record` de conteos sobre la taxonomía entera) deja de llamarse — las dos páginas pasan a usar `boundedVocabularyOf`, la misma función que el inicio ya usa para su vocabulario ya contado (14.52). El nombre de zona de cada tarjeta sale del mismo `activeZones`, nunca de la taxonomía: toda zona que un aviso mostrado pueda nombrar tiene avisos, así que está ahí. **`resolveZoneRoute` y el dominio no cambian**: la regla de qué hace válida una ruta y la ambigüedad de nombres compartidos siguen en `listing-discovery`, sin tocar. **La «consecuencia aceptada» de arriba se reabrió el 2026-09-08, no se sostuvo.** Una revisión de cuatro lentes marcó dos hallazgos CRÍTICOS, los dos corroborados por un refutador independiente. **`R4-zona-query-silent-widening`** (resiliencia): el caso real no era sólo una dirección vieja pegada a mano — es que el ÚLTIMO aviso vivo de una zona con tráfico real puede caducar entre dos pedidos, y ahí una dirección compartida HOY deja de nombrar esa zona en silencio y empieza a traer la ciudad entera bajo la misma URL, exactamente el daño que este mismo archivo ya rechaza con un 404 para el segmento de la ruta. El fundador decidió (2026-09-08): `?zona=` se resuelve contra la taxonomía CURADA, con una consulta acotada a los tokens que la petición trae —`ZoneRoutePort.findZonesByTokens(cityId, tokens)`, hermana plural de `findZoneBySlug`— y nunca recargando la taxonomía entera; `chosenZones` y el criterio de búsqueda pasan a usar esa consulta en vez de `activeZones`. **`R3-suggestion-count-scope-unproved`** (confiabilidad): cambiar de `boundedVocabulary`+`counts.byZone` a `boundedVocabularyOf`+`activeZones` no era sólo un cambio de origen del dato — `activeZones` cuenta la ciudad ENTERA por vigencia, y `counts.byZone` contaba el `criteria` de ESE pedido, así que con un filtro de precio o habitaciones puesto una sugerencia podía llevar a una zona que la propia búsqueda deja vacía. Las dos páginas vuelven a `boundedVocabulary(cities, activeZones, counts.byZone)`: `activeZones` aporta sólo la lista chica de nombres —barata, ya contada por zona— y `counts.byZone` sigue decidiendo cuáles entran. El panel de filtros conserva `activeZones` sin cambios: acotar por avisos vivos sigue siendo la regla correcta para SUS opciones. Las dos correcciones quedaron probadas con RED→GREEN antes del fix. En la página de zona, `loadActiveZones(place.city.id)` no puede correr en paralelo con `loadZoneRoute` —necesita el `cityId` que ésta resuelve— y se paga un viaje más en serie a propósito, la misma clase de costo que el archivo ya aceptaba para el panel. **Medido contra el contenedor real** (`rentas_test`, la siembra real de 17 ciudades y 5.810 zonas), antes de escribir la consulta y no después: `listCities()` + `listZones()` devuelven 5.827 filas y ~1.211 KB (`pg_column_size`) para armar el vocabulario de UNA ciudad; `listActiveZones` para Caracas (avisos reales sembrados) devuelve **6 filas y ~688 bytes**, con un `GroupAggregate` sobre las filas que el `WHERE` ya recortó (`EXPLAIN ANALYZE`, confirmado y no supuesto). Probado con RED→GREEN→REFACTOR: `tests/integration/active-city-zones.test.ts` cubre que una ciudad nunca trae la zona de la vecina que comparte nombre, que cada ciudad ve su propio conteo, que una zona sin avisos no aparece ni en cero, que la parroquia llega resuelta y que el conteo llega como número y no como el string del `bigint` del driver — confirmado quitando el `city_id` del `WHERE` a mano: sólo la prueba de aislamiento se pone roja, señalando la zona vecina filtrada de más, y el archivo se restauró byte a byte. `src/vigencia-del-catalogo.test.ts` (21.7) se actualizó con el octavo lector del predicado de vigencia: la lista que el fundador fijó se reabre por diseño cada vez que aparece uno, y esta rebanada agrega uno real. **Falta**: la D.

    **Rebanada D de cuatro, hecha — el inicio y `suggest-active-listings.ts` dejan de recibir la taxonomía entera. La casilla SE MARCA: con las cuatro rebanadas hechas no queda un solo camino de lectura que traiga las 5.813 zonas.** El inicio (`app/page.tsx`) resultó ya estar corregido, y no por esta rebanada: desde la 14.52 pide `DrizzleActiveZones.listActiveZones()` (`listing-discovery`, sin `city_id` porque en `/` no hay ciudad elegida) y nunca llamó `CataloguePort.listZones()` ni `loadCatalogue()` — se verificó leyendo el archivo entero, no se dio por sentado. El único camino de lectura que SÍ seguía trayendo la taxonomía entera era `suggest-active-listings.ts`: pedía `catalogue.listZones()` —las 5.813 zonas del país, sin `WHERE`— para nombrar la zona de cada aviso sugerido al ampliar de zona a ciudad, y descartaba en JavaScript las que no fueran de la ciudad del aviso vencido, la misma causa que las rebanadas B y C ya corrigieron en el resto de las pantallas. Ahora pide `ActiveCityZonesPort.listActiveZones(cityId)` —el mismo puerto que la rebanada C ya le dio al panel de filtros, reusado y no reabierto— así que Postgres acota con `WHERE city_id = $1` y un `GROUP BY`, y no queda un `.filter()` que hacerle a la respuesta. **Puerto propio, no un tercer método de `CataloguePort` reabierto**: `ActiveCityZonesPort` ya existía desde la C con `broker-bulk-import` y `suggest-active-listings` nombrados como sus dos consumidores pendientes; ésta es la mitad que faltaba, y `DrizzleCatalogue` ya la implementaba. `broker-bulk-import` (`generate-import-template.ts`, `run-import-validation.ts`) sigue llamando `catalogue.listZones()` a propósito: es una herramienta de carga masiva para corredores, no una pantalla del camino de lectura público, y necesita la taxonomía completa para resolver nombres ambiguos contra CUALQUIER zona del país — sacarla de acá habría sido tocar un archivo que esta tarea nunca nombró. **El fallo cerrado no cambió de forma**: un aviso cuya zona no resuelve nombre —antes, de otra ciudad; ahora, además, el borde teórico de una zona cuyo único aviso caduca en el instante entre las dos consultas— se sigue descartando con el mismo `flatMap`, nunca dibujado con un nombre inventado. **Medido contra el contenedor real** (`rentas_test`, 17 ciudades y 5.810 zonas, 23 avisos activos reales sembrados), antes de escribir la consulta y no después: `catalogue.listZones()` sin `WHERE` devolvía **5.810 filas y 701 kB**, sin importar qué ciudad se estuviera ampliando; `listActiveZones(cityId)` para Caracas (6 avisos activos) devuelve **6 filas y 688 bytes**, con `EXPLAIN ANALYZE` confirmando un `Index Scan` sobre `zone` por `zone_pkey`/`zone_id_city_id_unique` y ningún escaneo de la tabla entera. Probado con RED→GREEN→REFACTOR: `suggest-active-listings.test.ts` prueba con un RED real —revertido a `catalogue.listZones()`, 5 de 9 pruebas se ponen rojas exactamente en la rama ampliada, confirmado y no supuesto— y agrega una prueba que afirma que el puerto se llama con la ciudad exacta y una sola vez, nunca con la taxonomía completa; `ficha-servida.test.tsx` (render de la pantalla completa) se actualizó al mismo doble y sigue verde con las 29 pruebas, incluida la que afirma que ninguna sugerencia de la ciudad vecina se cuela aunque el puerto la devolviera. Suite completa corrida al cierre: 2.913 pruebas unitarias y 408 de integración, todas verdes; `tsc --noEmit` y `biome check` sin hallazgos nuevos.

    **Medición de una vista de página completa (2026-09-08), la que faltaba.** Las cuatro rebanadas midieron su propia consulta; ninguna sumó lo que UNA vista de UNA pantalla pública cuesta entera — el número que la 27.3 pide por escrito ("depende de la medición de la 27.1") y que la 27.4 necesita para su techo. `scripts/measure-page-view.ts` (re-corrible) instancia las clases reales del camino de lectura contra el contenedor real, intercepta el SQL que Drizzle emite de verdad y lo vuelve a correr envuelto en `pg_column_size`/`EXPLAIN ANALYZE` — nunca una consulta transcrita a mano. Datos: taxonomía real (5 áreas) más 24 avisos activos repartidos en 12 zonas reales de Caracas (llena `RESULTS_PER_PAGE`) y 75 avisos de relleno por cada otra ciudad real con estados mixtos, para que `listing_city_status_idx` tenga algo que descartar.

    | Página | Consultas (filas, bytes) | Total |
    | --- | --- | --- |
    | Inicio (`app/page.tsx`) | `listCities` (5, 349 B) · `listActiveZones` sin ciudad (16, 2.288 B) · `collectionsFor` (15, 2.608 B) · `coversFor`×2 (18, 1.977 B) | **54 filas, ~7,1 KB** |
    | Ciudad — Caracas (`app/alquiler/[ciudad]/page.tsx`) | `listCities` (reusada por `cache()`, 0) · `listActiveZones(cityId)` (12, 1.696 B) · `search` LIMIT 24 (24, 4.984 B) · `coversFor`×2 (72, 7.917 B) · `countFacets` (12, 8.640 B) | **120 filas, ~22,7 KB** |
    | Zona — Caracas/Altamira (`app/alquiler/[ciudad]/[zona]/page.tsx`) | `findZoneBySlug` (2, 191 B) · `listCities` (reusada, 0) · `listActiveZones(cityId)` (12, 1.696 B) · `search` (5, 1.000 B) · `coversFor`×2 (15, 1.647 B) · `countFacets` (12, 8.064 B) | **46 filas, ~12,3 KB** |

    `EXPLAIN ANALYZE` sobre la consulta real (con sus `$1`/`$2` parametrizados, no literales): `Bitmap Index Scan on listing_city_status_idx` en `search`/`listActiveZones(cityId)`/`countFacets`, `Index Scan using zone_slug_idx` en `findZoneBySlug`, `Bitmap Index Scan on listing_expires_at_idx` en `collectionsFor` — ninguna de las seis consultas del camino de lectura escanea una tabla entera contra datos a escala real.

    **La consulta más cara de una vista no es la que trae más filas: es `DrizzleFacetedSearch.countFacets`** —8.640 bytes en apenas 12 filas (una por zona con avisos), porque cada fila lleva más de treinta columnas `count(*) filter (...)` más el histograma de precio en `jsonb`. Es ancho de columna, no volumen de filas, y vale decirlo porque es distinto a la fuga que abrió esta fase.

    **Aritmética contra los 5 GB del plan Free**, asumiendo la página más cara (Ciudad, ~22,7 KB/vista, 24 avisos/página): 5.000.000.000 ÷ 23.237 ≈ **215.000 vistas/mes** de esa sola pantalla caben en la cuota. A los 3.000 visitas/día que la 27.3 ya asumía (90.000/mes), aun si TODAS fueran la pantalla más cara, son ~2,09 GB/mes — 58 % de margen debajo del techo, y las otras dos pantallas cuestan menos por vista.

    **Veredicto sobre la 27.3: NO hace falta cachear, con este número decidiéndolo.** 215.000 vistas/mes de la pantalla más cara ya caben en la cuota gratuita — más del doble del tráfico que la propia 27.3 asumía (90.000/mes) aun en el peor caso de que cada visita fuera esa pantalla. La 27.3 se cierra con este número y sin código, tal como su propio texto lo dejaba abierto. Revisar si el tráfico sostenido crece más de ~2-3× lo asumido acá.

    **Techo de filas para la 27.4: N = 300 filas por consulta.** La fila más alta medida en una sola consulta son 48 (`coversFor` de las 24 tarjetas de Caracas, dos derivadas por aviso — un número que no crece con el catálogo, sólo con `RESULTS_PER_PAGE`). 300 deja más de 6× de margen sobre eso y sigue estando muy por debajo de CUALQUIER instancia real de la fuga original: hasta la ciudad más chica que se filtró de más, La Guaira, devolvía 791 filas — 2,6× el techo propuesto. Un regresión que reintroduzca un escaneo sin acotar de `listing` o `zone` lo cruza de inmediato; el crecimiento legítimo del catálogo —más zonas con avisos dentro de una ciudad— no.

- [x] 27.2 **Las vistas previas dejan de compartir la base con producción.** La vista previa es `dev`, y hoy apunta a la MISMA base que producción.

    **Una corrección al primer borrador de esta tarea, medida y no supuesta.** Decía que cada despliegue de vista previa «prerenderiza páginas contra la base viva». **Es falso.** Las cuatro pantallas del camino de lectura son dinámicas —el inicio por `force-dynamic`, la ciudad, la zona y la ficha porque usan `searchParams`—, así que Next **no las prerenderiza**, y `pnpm build` corrió el 2026-09-07 con una `DATABASE_URL` inalcanzable y **pasó igual**. Los once PRs de ese día no se comieron la transferencia: **la fuga era la 27.1**. Esta tarea no es de costo.

    **Lo que sí es cierto, y es peor que el costo.** Primero: **una vista previa abierta consulta producción**, no en el build sino al visitarla, con datos de personas reales — y con las acciones de servidor disponibles, también puede escribirlos. Segundo: `scripts/deploy-migrate.mjs` **se salta las vistas previas a propósito**, y su propio comentario dice por qué —*«a preview build of an unmerged branch would apply that branch's migrations to live data — a NOT NULL column arriving before the code that fills it»*—. La protección existe, pero al precio de que la vista previa **renderice contra un esquema que puede no corresponder a su código**. Dos cosas rotas a la vez: puede tocar datos vivos, y puede mentir.

    **Y el 2026-09-07 lo demostró de la peor forma.** Cuando Neon agotó la cuota, no cayó sólo producción: **`dev` dejó de funcionar también**, porque comparten base. Un entorno de prueba que se cae con producción no es un entorno de prueba — es producción con otro nombre, y quita justamente el lugar donde se podría haber diagnosticado el incidente.

    **La mitad del trabajo ya está hecha y no la hicimos nosotros: la rama `preview` de Neon YA EXISTE**, vista en el panel el 2026-09-07 y marcada `Schema-only`. Lo que falta es conectar su cadena a la variable de entorno de **Preview** en Vercel, dejando la de **Production** intacta. Es configuración, no código.

    **Y entonces se puede cerrar la otra mitad**: con una base propia por entorno, `deploy-migrate` **deja de saltarse las vistas previas** y cada rama aplica sus migraciones a su propia base. Ahí la vista previa pasa a probar de verdad lo que va a desplegarse —incluido el paso de migración, que hoy no se prueba en ninguna parte (27.6)—, en vez de renderizar contra el esquema de otro.

    **Qué hay que decidir**: si la rama `preview` se siembra con la taxonomía real —que la 27.1 necesita para medirse— o con la semilla determinista de `scripts/seed-e2e.ts`; y si hay una rama por vista previa o una sola compartida entre todas.

    **La mitad de código, hecha; la casilla NO se marca porque la otra mitad sigue siendo del fundador.** `scripts/deploy-migrate.mjs` deja de saltarse TODA vista previa a ciegas: ahora migra cuando `VERCEL_ENV === "production"`, exactamente como antes, y también cuando `VERCEL_ENV === "preview"` **y** la variable nueva `PREVIEW_HAS_OWN_DATABASE` vale `"true"` — documentada en `.env.example`, junto al comentario que explica que se pone SÓLO en el dashboard de **Preview** de Vercel, nunca en el `.env` local ni en Production. Sin esa variable, o con cualquier otro valor, una vista previa se sigue saltando exactamente igual que hoy, y el log de build nombra la variable que la habilitaría — la misma propiedad de "avisa, no calla" que el script ya tenía. **A propósito, no se infiere nada del hostname, del id de rama de Neon ni de la cadena de conexión**: la 27.2 pidió una prueba explícita y no una adivinanza, porque exactamente esa clase de inferencia es la que falla en silencio el día que deja de ser cierta. Probado con RED→GREEN: `scripts/deploy-migrate.test.ts` corre el script real (no importa sus funciones — mismo motivo que `lint-tokens.test.ts`, `allowJs: false`) contra un `drizzle/` vacío y un `pnpm` de mentira en el PATH, y afirma las cuatro combinaciones — producción migra sin cambios, vista previa CON la variable migra, vista previa SIN la variable se salta nombrándola, y local/sin `VERCEL_ENV` se sigue saltando exactamente como antes. **Lo que sigue siendo del fundador, y sin lo cual el código de arriba no alcanza**: apuntar el `DATABASE_URL` de **Preview** en Vercel a la rama `preview` de Neon que ya existe, y sólo ENTONCES poner `PREVIEW_HAS_OWN_DATABASE=true` en ese mismo entorno. Hasta que eso pase, el código falla cerrado: toda vista previa se sigue saltando y sigue renderizando contra el esquema que tenía, ni un byte de esta rama toca la base de producción ni la comparte con `dev`. La tarea se cierra cuando la configuración también esté hecha y una vista previa real migre contra su propia base.
    **CERRADA el 2026-09-08: la configuración también está hecha y una vista previa real migró contra su propia base.** El registro de construcción de `dev` lo dice entero y en orden: `applying pending migrations to preview…`, después `schema-smoke: 19 tabla(s) del código presentes en la base`, después `taxonomy-smoke: 5 área(s) y 5796 zona(s) de docs/territorio/ presentes en la base`, y recién entonces `next build`. **Eso es la 27.2 entera funcionando**: la vista previa aplica sus propias migraciones, verifica su propio esquema y verifica su propia siembra, sin tocar producción en ningún punto.

    **Dos decisiones del fundador que este párrafo corrige respecto del anterior, porque el anterior las daba por otra cosa.** (a) **No se usó la rama `preview` del proyecto de producción: se creó un PROYECTO Neon aparte, «rentoru dev».** La razón está medida: en el plan Free **una rama comparte la cuota de su proyecto**, así que una rama aísla los datos pero no la transferencia, y las vistas previas seguirían gastando la cuota de producción — que es justamente lo que tumbó el sitio. Un proyecto aparte aísla las dos cosas. (b) **La base de vista previa se sembró con la TAXONOMÍA REAL** —5 áreas y 5.796 zonas—, no con la semilla determinista de `scripts/seed-e2e.ts`, porque es la única contra la que se pueden reproducir las mediciones de esta fase y ver un problema de escala ANTES de desplegarlo.

    **Una precisión sobre el párrafo anterior, que lo daba por hecho antes de tiempo**: `PREVIEW_HAS_OWN_DATABASE` sí quedó documentada en `.env.example`, pero **no la escribió el ejecutor**. Ese archivo está detrás de una barrera de permisos del entorno de agentes —ni el ejecutor ni el orquestador pueden leerlo ni escribirlo— así que el bloque lo pegó el fundador a mano el 2026-09-08. La variable está además documentada en el encabezado de `scripts/deploy-migrate.mjs`, que es lo único que el código puede garantizar por sí solo. **Queda anotado como convención: todo lo que viva en `.env.example` lo escribe una persona, no un agente.**

    **Lo que el camino de despliegue prueba ahora y antes no probaba en ninguna parte**: que la migración corre. Hasta hoy `deploy-migrate` se salteaba toda vista previa, así que el paso de migración sólo se ejercitaba en producción, con datos vivos y sin ensayo previo. Ahora cada rama lo ensaya contra su propia base antes de que le toque a producción — que es la mitad de lo que la 27.6 pedía, ya cobrada de arriba.

- [x] 27.3 **DEPENDE DE LA MEDICIÓN DE LA 27.1. No se empieza antes.** Cachear lo que el camino de lectura sigue pidiendo en cada visita, **si después de la 27.1 todavía hace falta**.

    **Por qué está condicionada.** Con los 679 kB del catálogo fuera, lo que queda por visita son las 18 ciudades (~2 kB), los avisos que se muestran y los conteos: unos pocos kilobytes. A 3.000 visitas diarias eso es **menos de 1 GB al mes**, cómodamente dentro de los 5 GB gratis. **Es muy posible que la 27.1 sola cierre el problema y esta tarea se cierre con el número y sin código** — que es la mejor forma de cerrar una tarea.

    **El `force-dynamic` NO es el defecto, y quitarlo rompe una protección deliberada.** Los dos archivos tienen su razón escrita y las dos son buenas. `app/page.tsx`: sin esa línea Next intenta exportar la página en compilación, y el build corre contra una `DATABASE_URL` **inalcanzable a propósito**, así que la compilación se cae en `listCities()` — *«se descubrió compilando, no en producción»*; y aparte, las cuatro tiras del inicio cambian cada vez que alguien publica. `app/sitemap.ts`: un sitemap horneado en el despliegue queda viejo con el primer aviso publicado después, y *«la página que más necesita estar al día sería la más desactualizada del sitio»*. **Esa `DATABASE_URL` inrouteable del job `build` es un guardián, no un accidente**: existe para que un cambio que consulte durante la compilación se caiga en CI en vez de en producción.

    **Por eso, si la tarea se hace, se cachea el DATO y no la ruta.** La ruta sigue siendo dinámica —el guardián del build sigue en pie— y lo que se cachea es el resultado de la consulta, entre peticiones. Quitar el `force-dynamic` sería cambiar un problema de cuota por uno de compilación, y de paso borrar una protección que ya atrapó un defecto real.

    **Y una pregunta que hay que contestar antes de escribir una línea**: cuánta desactualización tolera el sitemap. El comentario argumenta contra uno horneado en el DESPLIEGUE, y tiene razón — pero eso no es lo mismo que uno con **una hora**. Un aviso nuevo tarda días en ser rastreado. Si una hora sirve, la tarea es chica; si tiene que estar al segundo, **la respuesta correcta es no hacer esta tarea y dejarlo dicho**.


    **CERRADA el 2026-09-08 SIN escribir una línea de caché, que es el desenlace que esta tarea pedía.** Su enunciado condicionaba todo a una medición —*«si después de la 27.1 todavía hace falta»*—, y la medición se hizo: una vista de la página más cara cuesta **22,7 kB**, no los 679 kB de una sola consulta de antes.

    **La aritmética que la decide**, contra los 5 GB mensuales del plan Free y tomando la página más cara para TODAS las visitas, que es el peor caso y no el promedio: **caben unas 215.000 vistas al mes**. Las **3.000 visitas diarias que esta misma tarea suponía** son 90.000 al mes, o sea **2,09 GB — un 58 % de margen**.

    **No hace falta cachear.** Y la razón de no hacerlo no es pereza: una caché trae invalidación, y la invalidación es una fuente de defectos propia —datos viejos servidos como frescos— que sólo vale pagar cuando compra algo. Acá no compra nada: entra más del doble del tráfico asumido sin ella.

    **Cuándo reabrir esto, dicho con número para que no sea una corazonada**: si el tráfico sostenido pasa de **dos a tres veces** las 3.000 visitas diarias asumidas, o si una vista vuelve a costar mucho más que los 22,7 kB medidos. Lo segundo ya no depende de la vigilancia de nadie: la **27.4** puso un techo de 300 filas que revienta ruidosamente si el camino de lectura vuelve a pedir de más, y `scripts/measure-page-view.ts` deja la medición re-ejecutable para cuando el inventario crezca.

- [x] 27.4 **La otra mitad del par de presupuestos: un techo de FILAS sobre lo que el camino de lectura le pide a la base.** Es la tarea que impide que esto vuelva a pasar.

    **El nombre ya estaba escrito en el repositorio.** `scripts/budget-bundle.ts` pone un techo medido —`BUDGET_BYTES = 130 * 1024`— sobre el JavaScript que baja al navegador en el camino de lectura, y falla el CI si se pasa; su propio comentario se llama *«the fast half of the budget pair»*. **Ya existe el presupuesto de lo que BAJA. Falta el de lo que SUBE desde la base.** Va en el mismo job `budget`, para que las dos mitades se lean juntas y para que un fallo diga «budget» y cualquiera entienda qué significa sin abrir un archivo.

    **Se mide en FILAS y no en bytes** (decisión del fundador, 2026-09-07). Los bytes cambian entre máquinas —codificación, largo de los nombres— y una prueba que depende de eso parpadea. **Las filas son estables**: 5.813 son 5.813 en todos lados, y el defecto se expresa exacto en filas.

    **Y tiene que correr con la taxonomía REAL sembrada, o no prueba nada**: con 10 zonas habría pasado igual que pasó. Sembrar las 5.813 cuesta segundos de CI en cada PR, y se pagan: este defecto costó el sitio entero.

    **La prueba que hay que escribir es la que habría atrapado el incidente**: sembrar la taxonomía real, renderizar una pantalla del camino de lectura, y afirmar que **ninguna consulta devolvió más de N filas**. Con eso, la resiembra del 6 de septiembre se ponía **roja en CI antes de desplegar** — no al día siguiente con el sitio caído.

    **NO se hace con detección estática, y la razón es un precedente del propio fundador del mismo día.** En la 22.31 eligió la convención sobre el detector mecánico porque *«un argumento opcional sin llenar es a menudo legítimo: el falso positivo sería de diseño, no un accidente»*. Un detector de `SELECT` sin `WHERE` tiene la misma forma: marcaría `listCities()` —18 filas, acotado por diseño, perfectamente correcto— y se aprendería a ignorar. **Una prueba que mide lo que de verdad salió no puede dar ese falso positivo**, y es además la convención que la 22.31 dejó escrita en `AGENTS.md` §1, aplicada del lado de la base.

    **El CI no toca Neon y por eso esto es gratis**, verificado: todos los `DATABASE_URL` del workflow son o la falsa inrouteable `ep-ci-build-0000-pooler.invalid.neon.tech` (`build`, `e2e`, `budget`) o el contenedor en `localhost:5432` (`integration`, `e2e`). El job `budget` hoy no tiene base: **darle una es parte de esta tarea**.

    **Decidido: N = 300, uno solo para todo el camino de lectura.** Sale de la medición de una vista completa (27.1, párrafo de medición, 2026-09-08): la consulta más cara real son 48 filas (`coversFor`, acotada por `RESULTS_PER_PAGE` y no por el catálogo), y la fuga histórica más chica devolvió 791. Un techo por pantalla se descartó: las tres pantallas comparten las mismas seis consultas y un número distinto por pantalla sería tres cosas que mantener sincronizadas para decir lo mismo.

    **Dónde dispara, y por qué en los dos lugares y no en uno solo.** La regla vive pura en `src/modules/operability/domain/row-budget.ts` (`assertRowBudget`, `ROW_BUDGET = 300`) — el mismo suelo de cobertura del 90 % que protege el resto de `domain/`, y nunca en `app/`, que es exactamente la frontera de la que la 22.31 y `AGENTS.md` §3 hablan. Esa regla está **cableada dentro de los adaptadores de infraestructura** que sirven el camino de lectura (`DrizzleCatalogue.listCities/listActiveZones/findZoneBySlug/findZonesByTokens`, `DrizzleActiveZones.listActiveZones`, `DrizzleHomeCollections.collectionsFor`, `DrizzleListingPhotos.coversFor`, `DrizzleListingSearch.search`, `DrizzleFacetedSearch.countFacets`), así que corre **en PRODUCCIÓN, en cada visita real** — falla ruidosamente (lanza `RowBudgetExceededError`) el mismo momento en que una consulta cruza el techo, sin esperar a un despliegue. Eso es lo que un guardián de sólo-CI no puede prometer: el defecto original nunca pasó por un PR — fue una resiembra de datos corrida directo contra producción (17.15) — así que sólo algo que mira cada request de verdad lo habría atrapado el 6 de septiembre, antes de que el tráfico real agotara la cuota el 7. **`CataloguePort.listZones()` queda deliberadamente SIN el techo**: es la lectura completa que `broker-bulk-import` necesita de verdad (5.813+ filas), y ponerle 300 la rompería siempre, no sólo ante una regresión — verificado (27.1, rebanada D) que ningún `app/**/page.tsx` la llama hoy.

    **Y también en CI, contra la taxonomía real, antes de que una PR llegue a producción.** `scripts/budget-rows.ts` —la mitad de filas del mismo par que `scripts/budget-bundle.ts` abrió para bytes— corre en el job `budget`, que ahora tiene su propio contenedor Postgres 18 (igual que `integration`/`e2e`). Siembra la taxonomía real más los avisos de demostración (`seed()`, idempotente) y ejercita las seis consultas de arriba tal cual las llaman las tres pantallas; si alguna cruza 300, el mismo `assertRowBudget` ya cableado la hace lanzar y el job se pone rojo. Corrido a mano contra el contenedor local (`rentas-pg`, taxonomía real, 5.810 zonas): las tres pantallas pasan (0 filas sobre el techo) y una prueba de regresión en el mismo script —pasar `catalogue.listZones()` (5.810 filas) por `assertRowBudget`— confirma que el mismo mecanismo SÍ dispara sobre la forma exacta de la fuga original, sin revertir código para fabricar el fallo. RED antes del dominio: `row-budget.test.ts` fallaba por módulo inexistente antes de escribir `row-budget.ts`; GREEN después, y suite completa (2.922 unitarias, 408 de integración) verde con el guardián ya cableado en los seis adaptadores.

- [x] 27.5 **El latido: no se puede ver el consumo, así que se vigila la consecuencia.**

    **Esta tarea se llamaba «El consumo se ve antes de agotarse» y ese enunciado es falso en el plan que el proyecto usa.** Verificado contra la documentación de Neon el 2026-09-07, antes de escribir una línea:

    - Los avisos por correo —Neon los llama *spending notifications*, al 80% y al 100% de un umbral, revisados cada quince minutos— son *«available on the Launch and Scale plans»*. **El plan Free no los tiene.**
    - La API de métricas de consumo, que es la que un job periódico consultaría, está limitada a *«usage-based plans (Launch, Scale, Agent, and Enterprise)»*. **El plan Free tampoco la tiene.**
    - El límite que se agotó es el del propio plan Free: *«Data Transfer: 5 GB per project»* de egreso mensual. Coincide exacto con el panel: 13,48 sobre 5.

    **O sea que en el plan Free el consumo NO se puede ver antes de agotarse por ningún medio automático.** El único lugar donde se ve es el panel, a mano. Por eso el fundador se enteró de los 13,48 GB con el sitio ya caído: no había forma de enterarse antes. La alarma de verdad no es código, **es el plan** — comprar Launch compra el correo y la API de una sola vez.

    **El precedente que esta tarea invocaba tampoco aplica.** Citaba el `smoke:taxonomy` de la 17.15, que es un guardián en el despliegue. Pero **el consumo crece con las visitas, no con los despliegues**: entre la resiembra del 6 de septiembre y la muerte de la cuota el 7 no hubo despliegues, hubo tráfico. Un guardián de despliegue habría estado callado todo ese tiempo. **Un vigilante de cuota tiene que mirar cuando nadie está mirando.**

    **DECISIÓN DEL FUNDADOR, 2026-09-07: el latido.** Ya que no se puede medir el consumo, se vigila su consecuencia. Un workflow programado que le pregunte a producción «¿estás viva?» y falle ruidosamente si no. No dice «vas por el 80%»; dice «se cayó». Y eso convierte *enterarse horas después por casualidad* en *enterarse en minutos*, que es toda la diferencia que esta fase puede comprar gratis.

    **Sería el primer workflow programado del repositorio**, verificado: `.github/workflows/ci.yml` es el único que existe y no tiene ni un `schedule` ni un `workflow_dispatch`; todo se dispara por push o por PR.

    **Y acá está el límite que define la tarea: el latido consume la misma cuota que vigila.** Cada visita del vigilante es una visita de verdad, y hoy una vista del camino de lectura cuesta 679 kB por `listZones()` (medido en la 27.1). La aritmética, contra los 5 GB del plan:

    | Cadencia | Corridas al mes | Costo de cuota hoy | Porción de los 5 GB |
    | --- | --- | --- | --- |
    | cada 5 minutos | 8.640 | 5,9 GB | **más del 100% — la alarma sola agota la cuota** |
    | cada 15 minutos | 2.880 | 1,95 GB | 39% |
    | cada hora | 720 | 0,49 GB | 10% |

    **Un vigilante que tumba el sitio que vigila no es un vigilante.** Por eso esta tarea **depende de la 27.1**: mientras la taxonomía siga viajando entera en cada vista, la cadencia está atada de manos. Cuando la 27.1 saque esos 679 kB del camino, una vista pasa a costar una fracción y la cadencia se puede apretar sin pensar.

    **La tabla de arriba está VIEJA y no se puede dejar parada sin esta nota**: mide el costo de golpear una vista de página completa, que era la única candidata antes de decidir CUÁL dirección golpear. Con esa decisión tomada (abajo), la aritmética real es otra.

    **LAS TRES DECISIONES, CERRADAS:**

    1. **Cadencia: cada 5 minutos** (decisión del fundador, 2026-09-07).
    2. **Dirección: `/api/health`, que toca `listCities()`** — 5 filas, 349 bytes medidos (tasks.md 27.1, párrafo de medición) — y no una vista de página. Es 1/65 del costo de la vista de ciudad más barata (22,7 kB), y sigue siendo una consulta real contra una tabla real: muere si Neon está caído o sin cuota, igual que cualquier otra. Devuelve un status distinto de 200 —nunca 200 con la base muerta— y no filtra causa, host ni pila en el cuerpo.
    3. **Cómo grita: Resend, sólo en la falla.** Mismo mecanismo que `ResendLifecycleMailer` (tasks.md 7.11): un adaptador propio (`ResendHeartbeatMailer`), su propio remitente (`HEARTBEAT_MAIL_FROM`) y su propio destinatario (`HEARTBEAT_MAIL_TO`), sobre la misma cuenta de Resend (`RESEND_API_KEY`). Un job de GitHub Actions en rojo que nadie mira es el mismo silencio que esta tarea existe para romper (fundador) — por eso el correo, no sólo el rojo del job. Nunca manda en éxito: entrenaría a quien lo lee a ignorarlo y gastaría cuota de Resend por nada.

    **La aritmética recalculada, contra los 5 GB del plan, con la cadencia y la dirección ya decididas:**

    | Cadencia | Corridas al mes | Bytes por corrida | Costo de cuota al mes | Porción de los 5 GB |
    | --- | --- | --- | --- | --- |
    | cada 5 minutos | 8.640 | 349 B (`listCities()`, 27.1) | ≈ 3,0 MB | **0,06%** |

    Contra la tabla vieja (5,9 GB a esta misma cadencia, midiendo `listZones()`/una vista completa): sacar la dirección del camino de vista completa y ponerla sobre la consulta más chica del catálogo baja el costo casi 2.000×. La cadencia que antes agotaba la cuota sola hoy cuesta una fracción de una fracción.

    **Implementado**: `app/api/health/route.ts` (la ruta), `src/modules/operability/domain/heartbeat.ts` (el corte vivo/muerto y la redacción del aviso, puro), `src/modules/operability/application/run-heartbeat.ts` (la orquestación: comprobar, decidir, avisar sólo si hace falta), `src/modules/operability/infrastructure/resend-heartbeat-mailer.ts` (el envío) y `scripts/heartbeat.ts` + `.github/workflows/heartbeat.yml` (el vigilante programado, con `workflow_dispatch` para probarlo a mano). `app/robots.ts` excluye `/api` — una ruta JSON no rinde nada indexado.

    **Variables de entorno nuevas que el fundador tiene que agregar** (repository secrets de GitHub Actions): `HEARTBEAT_MAIL_FROM`, `HEARTBEAT_MAIL_TO`. `RESEND_API_KEY` ya existe (misma cuenta que el ciclo de vida).

- [x] 27.6 **DEPENDIENTE — la distancia entre lo que el CI prueba y lo que Vercel corre, medida pero no cerrada.**

    **La brecha, exacta.** El CI corre `pnpm build`. Vercel corre `pnpm vercel-build`, que es `node scripts/deploy-migrate.mjs && next build`. **No son el mismo comando**, y de ahí salen tres diferencias:

    1. **El CI nunca ejecuta `deploy-migrate.mjs`.** Ese script es el que corre `pnpm drizzle-kit migrate` contra la base de producción en cada despliegue, y no hay una sola prueba que lo toque.
    2. **El CI migra contra un Postgres en contenedor, sin SSL.** Producción migra contra Neon, con `sslmode=require`. Es una ruta de código distinta que nadie ejercita.
    3. **El CI fijaba `NODE_VERSION: "22"` mientras producción flotaba** — así fue como Vercel terminó en Node 24 sin que nada se pusiera rojo.

    **La tercera ya está cerrada, y gratis**: `package.json` fija `"engines": { "node": "22.x" }`. Quedan la primera y la segunda.

    **Lo que esta tarea NO es, y hay que dejarlo escrito porque su enunciado anterior lo insinuaba.** La caída del 7 de septiembre **no la causó esta brecha**: la causó la cuota de transferencia agotada, y **la cuota no es reproducible en el CI** — todos sus `DATABASE_URL` son la falsa inrouteable o el contenedor local, verificado. Ningún guardián de `vercel-build`, por completo que fuera, habría visto venir los 13,48 GB. **De ese incidente se ocupan la 27.1 y la 27.4.** Esta cubre un riesgo distinto y real, no el que quemó el sitio.

    **Por qué queda dependiente y no se ejecuta en esta fase.** Cerrarla de verdad exige una base con migraciones pendientes contra la cual correr `deploy-migrate.mjs` y afirmar su comportamiento: eso es un arnés propio, del peso de la 11.22. **La Fase 27 existe para apagar un incendio**, y meterle una tarea de peso completo que no toca ese incendio es exactamente la forma de que la fase no termine.

    **Queda escrita con la medición hecha** —los tres puntos de arriba, uno ya cerrado— para que el día que se retome no haya que volver a averiguar cuál era la distancia.


    **CERRADA el 2026-09-08, y no por haberla ejecutado: la 27.2 la cerró sin que nadie lo notara.** Los tres puntos que esta tarea midió están los tres atendidos, y conviene verificarlos uno por uno antes de creerlo.

    1. *«El CI nunca ejecuta `deploy-migrate.mjs` … no hay una sola prueba que lo toque»* — **ya no es cierto**. La 27.2 trajo `scripts/deploy-migrate.test.ts`, que corre el script REAL —no importa sus funciones, `allowJs: false`— contra un `drizzle/` vacío y un `pnpm` de mentira en el PATH, y asevera las cuatro combinaciones: producción migra, vista previa con la variable migra, vista previa sin ella se saltea nombrándola, y local se saltea igual que antes. Corre en el job `test` del CI.
    2. *«El CI migra contra un Postgres en contenedor, sin SSL. Producción migra contra Neon, con `sslmode=require`. Es una ruta de código distinta que nadie ejercita»* — **ya no es cierto tampoco, y es lo más valioso de la 27.2**. Desde que las vistas previas tienen base propia, **cada despliegue de vista previa migra contra Neon con `sslmode=require`**, o sea la ruta exacta de producción, antes de que le toque a producción. No es una prueba: es el ensayo real. El registro de construcción de `dev` del 2026-09-08 lo muestra, con el aviso de SSL de `pg` incluido.
    3. El Node flotante ya estaba cerrado por `"engines": { "node": "22.x" }`.

    **Lo que esto deja dicho sobre el orden de las tareas**: la 27.6 se había aparcado como «del peso de la 11.22, no cabe en una fase que apaga un incendio». Era una lectura correcta con la información de entonces. Lo que cambió es que **la 27.2 necesitaba, para su propio fin, exactamente el ensayo que la 27.6 pedía** — y al construirlo lo pagó de arriba. Vale registrarlo: no toda tarea cara hay que hacerla; a veces conviene mirar si otra ya la está pagando.

    **Lo que sigue sin cubrir, y se dice para no fingir que está todo**: no hay una prueba que corra `deploy-migrate.mjs` **contra una base con migraciones realmente pendientes** y afirme el resultado. Lo que hay es el ensayo en cada vista previa, que es evidencia de producción y no una aserción. Si algún día eso hace falta, es una tarea nueva y con su arnés, no el resto de ésta.

- [x] 27.7 **La ruta de zona nombra un LUGAR, no una fila: `/alquiler/<ciudad>/<zona>` busca en todas las zonas que se llaman así.**

    **Esta no es una tarea de consumo, y conviene decirlo de entrada.** Salió midiendo la 27.1 y vive en esta fase porque toca exactamente el mismo código; separarla de fase la haría redescubrir todo otra vez. Pero lo que arregla es un **agujero de producto**, no la cuota.

    **Lo que se midió** (2026-09-07, contenedor local con las 5.813 zonas reales y la `slugify` del repositorio, no una copia):

    | | |
    | --- | --- |
    | Colisiones de ciudad | **0** |
    | Slugs vacíos | **0** |
    | Slugs truncados por `MAX_SLUG_LENGTH` | **0** |
    | Grupos `(ciudad, slug)` con más de una zona | **335** |
    | Zonas involucradas | **779** |
    | **Zonas sin URL propia hoy** | **444** |

    **No son filas duplicadas.** Se verificó consultando el padre de cada una: son **lugares reales y distintos que comparten nombre en parroquias distintas de la misma ciudad**. En Maracaibo, *Barrio Nuevo* existe en Cristo de Aranza, Juana de Ávila **y** Olegario Villalobos; *Sector Santa Ana* en Antonio Borjas Romero, Domitila Flores y Manuel Dagnino. Duplicados de verdad —misma ciudad, mismo padre, mismo nombre— hay **3 en 5.813**: ruido, no el fenómeno.

    **El defecto, exacto.** `resolveZoneRoute` usa `zones.find(...)`, que devuelve **la primera coincidencia en silencio**. Las otras 444 zonas no tienen dirección: un aviso publicado en el *Barrio Nuevo* de Juana de Ávila vive en una zona que ninguna URL puede nombrar. **Nadie lo sabía porque nadie fue a contarlas.**

    **DECISIÓN DEL FUNDADOR, 2026-09-07**: *«como son parroquias diferentes, son diferentes lugares... Si buscamos por zona barrio nuevo, entonces deberían salir todos los avisos que están en barrio nuevo con cada parroquia, todos, no solo uno.»*

    O sea: **la ruta direcciona el NOMBRE, y el nombre cubre todos los lugares que se llaman así dentro de esa ciudad.** `/alquiler/maracaibo/barrio-nuevo` busca en las tres. Ninguna zona queda sin dirección, porque la dirección deja de apuntar a una fila.

    **El camino ya está construido, verificado.** `SearchLocation.zoneIds` ya es plural —su comentario dice *«Las zonas de la búsqueda, combinadas con O (F4)»*—, `SearchCriteria.zoneIds` ya es plural desde la 14.6, y la búsqueda, las facetas, los conteos y la paginación ya combinan varias. **El único punto singular del camino entero** es `SearchLocationInput.routeZoneId?: string` y su uso en `resolveSearchLocation`:

    ```js
    zoneIds: input.routeZoneId === undefined ? [] : [input.routeZoneId]
    ```

    El cambio es `routeZoneId: string` → `routeZoneIds: readonly string[]`, más que `resolveZoneRoute` devuelva el conjunto que comparte `(cityId, slug)` en vez de la primera.

    **No viola la regla de indexación de la 14.36, y hay que dejarlo escrito para que nadie lo lea como una violación.** Esa regla manda `noindex` a la forma **combinada** —`?zona=a,b`, «Chacao O Altamira»—, que son dos lugares distintos elegidos a mano. Acá es **un solo lugar nombrado** que resulta estar guardado en varias filas. La ruta sigue siendo canónica e indexable.

    **Por qué NO se implementa junto con la 27.1** (decisión del fundador): la 27.1 apaga el incendio y no cambia nada de lo que el visitante ve. Ésta **sí cambia lo que devuelve una pantalla**, y merece su propia rebanada, su propia prueba y su propio PR. Mezclarlas haría imposible saber cuál de las dos movió un resultado.

    **Qué queda por decidir**: si la pantalla dice algo cuando el nombre cubre varios lugares —«Barrio Nuevo» a secas, o nombrando las parroquias que agrupa—, y qué pasa con la miga de pan y el `<h1>`, que hoy hablan de una zona en singular.

    **Hecho (2026-09-08).** Exactamente el cambio que el párrafo de arriba anticipaba. `ZoneRoute.zones` reemplaza a `zone`: `resolveZoneRoute` (`zone-route.ts`) pasó de `.find(...)` a `.filter(...)` y devuelve el conjunto entero que comparte `(cityId, slug)`, tipado como `readonly [Z, ...Z[]]` — nunca vacío, y el tipo lo dice y no sólo el comentario, así que `zones[0]` no necesita un respaldo bajo `noUncheckedIndexedAccess`. `SearchLocationInput.routeZoneId?: string` se volvió `routeZoneIds?: readonly string[]`, y `resolveSearchLocation` las combina con O — la misma combinación que `?zona=` ya usa en la ruta de ciudad. La página (`app/alquiler/[ciudad]/[zona]/page.tsx`) pasa `place.zones.map((z) => z.id)` a la búsqueda: las tarjetas de las 444 zonas hoy inalcanzables empiezan a aparecer bajo la URL que comparten con su hermana.

    **La decisión pendiente, resuelta por el fundador (2026-09-08): la tarjeta nombra la parroquia, y sólo cuando el nombre está compartido.** «Barrio Nuevo» a secas cuando el nombre es único en su ciudad; «Barrio Nuevo, Juana de Ávila» cuando no lo es — para que alguien buscando vea a simple vista que son barrios distintos, sin cambiar la URL ni el `<h1>`. El `<h1>`, la miga de pan y los metadatos siguen nombrando el LUGAR (`place.zones[0].name`) y no la fila que lo resolvió: es la desambiguación de la tarjeta la que dice de cuál parroquia habla cada aviso, no el título de la pantalla.

    **Costo cero, medido y no supuesto — restricción que llegó a mitad de tarea.** El fundador pidió explícitamente que la parroquia no costara una consulta más, un join que ensanche la fila del aviso, ni una relectura de la taxonomía — exactamente la forma del defecto que abrió esta fase. La solución: un `Map<zoneId, parentName>` construido en la página a partir de `place.zones` — las filas que `findZoneBySlug` YA trajo, que ya cargan su propio `parentName` porque la consulta del puerto ya hace el `leftJoin` con el padre (27.1, rebanada B) — y sólo cuando `place.zones.length > 1`; con una sola zona resuelta el mapa queda vacío y ninguna tarjeta paga nada de más. `GridListing.zoneParentName` es opcional; `buildListingGrid` (`listing-grid.ts`) lo combina en el `zoneName` que la tarjeta DIBUJA, nunca en el que arma el enlace o el texto alternativo de la foto —los dos siguen usando el nombre de zona solo, porque son la dirección canónica de la ficha (`buildListingPath`) y esta tarea no la toca. **Medido de nuevo contra el contenedor real** (`scripts/measure-page-view.ts`, Caracas/Altamira, una zona sin nombre compartido): los mismos números que la 27.1 ya había dejado escritos — `findZoneBySlug` 2 filas/191 bytes, `listActiveZones` 12/1.696, `search` 5/1.000, `coversFor`×2 15/1.647, `countFacets` 12/8.064 —, byte por byte iguales a los de antes de esta tarea. El costo entero de la desambiguación es un `Map` armado en JavaScript sobre filas que la página ya pagó; cero SQL nuevo, cero bytes de más por vista.

    **Medido contra la taxonomía real de `rentas-pg`** (2026-09-08, la misma siembra de 5 áreas y 5.796 zonas que usa `taxonomy-slug-backfill.test.ts` — no una copia): la medición del fundador del 2026-09-07 se sostiene exacta, sin deriva pese a que el slug ya lleva un mes en producción — 335 grupos `(city_id, slug)` con más de una zona, 779 zonas involucradas, **444 zonas sin URL propia antes de esta tarea** (verificado con la misma consulta de agrupación, cero discrepancia). Duplicados de verdad —misma ciudad, mismo padre, mismo nombre— siguen siendo 3 grupos (6 filas), consistente con el "ruido, no el fenómeno" que el párrafo de arriba ya afirmaba. **Después de esta tarea, las 5.796 zonas del país son alcanzables por URL**: cada `(city_id, slug)` compartido pasa a resolver TODAS sus filas bajo la misma dirección, así que ninguna zona real queda sin una URL que la nombre — de 5.352 zonas alcanzables (5.796 − 444) a 5.796, el 100 %.

    **Probado con RED→GREEN→REFACTOR.** `zone-route.test.ts` afirma que `resolveZoneRoute` devuelve TODAS las zonas de un grupo compartido (`Barrio Nuevo` en dos parroquias de Maracaibo) y no la primera; `search-location.test.ts` afirma que `routeZoneIds` plural entra completo a `SearchLocation.zoneIds`. El RED real de la pantalla vive en `zona-sin-javascript.test.tsx` (`catalogo-de-prueba.ts` ganó dos zonas más — `BARRIO_NUEVO_CRISTO`/`BARRIO_NUEVO_JUANA`, mismo nombre, distinta parroquia, cada una con su aviso real): con el código de antes de esta tarea, `/alquiler/maracaibo/barrio-nuevo` sólo traía el aviso de la primera zona que `findZoneBySlug` devolvía y ninguna tarjeta mostraba una parroquia — confirmado revirtiendo `resolveZoneRoute` a `.find(...)` y viendo caer exactamente las pruebas de esta tarea, ninguna otra. Verde: 2.949 unitarias, 410 de integración, `tsc --noEmit` y `biome check` limpios. 425 líneas autoría en 13 archivos, dentro del presupuesto de 700.

- [x] 27.8 **El panel de filtros cuenta sobre TODAS las zonas de la ciudad, y recorta en JavaScript.** Es la misma forma del defecto que la 27.1, en otro archivo, y sobrevivió a las cuatro rebanadas porque la 27.1 miraba la taxonomía y esto mira los avisos.

    **Salió de la medición de una vista completa**, no de una sospecha: `DrizzleFacetedSearch.countFacets` es **la consulta más cara de una vista de página — 8.640 bytes en apenas 12 filas**. Y lo interesante no es el número de hoy sino de qué depende.

    **Lo que se verificó, archivo en mano** (`src/modules/listing-search/infrastructure/drizzle-faceted-search.ts`, 503 líneas):

    1. **No hay un solo `.limit()` en todo el archivo.** Ni uno.
    2. La línea 355 hace `.groupBy(listings.zoneId, priceFacet.tally)`, así que la consulta devuelve **una fila por cada zona de la ciudad que tenga al menos un aviso**, sin techo.
    3. **`offeredZoneIds` ya llega como parámetro** (línea 101) —las zonas que el panel efectivamente dibuja— **y no entra en el `WHERE`**: se usa sólo en la línea 400, para inicializar un objeto en memoria, DESPUÉS de que Postgres ya devolvió todas las filas.

    **Por qué importa, y por qué hoy no se ve.** Con el inventario actual son 12 filas. El límite real es *cuántas zonas de la ciudad tienen algún aviso*, y **Caracas tiene 3.220 zonas**. Cada una de esas filas además **es cara por ancho de columna**, no por cantidad: más de treinta agregados `count(*) filter` más un histograma `jsonb` por grupo. Doce filas ya pesan 8,6 kB; el mismo camino con inventario repartido pesa lo que pese multiplicado por zonas.

    **Es el mismo error que costó el sitio, dicho con precisión**: la base devuelve de más y el servidor recorta. La 27.1 lo arregló para la taxonomía; acá sigue vivo para los avisos.

    **Y el techo de filas de la 27.4 NO lo atrapa** — conviene dejarlo escrito para que nadie lo dé por cubierto. Dos razones: el costo dominante acá es el ancho de la fila y no su cantidad, y un techo a ciegas **haría mentir a los números del panel**, que es exactamente lo que la regla transversal 3 prohíbe («si una etiqueta dice 9, hay 9»). Un conteo recortado es peor que un conteo caro.

    **La dirección del arreglo, decidida por el fundador el 2026-09-08**: *«los límites tienen que ser con base de datos, no en el lado del server»*. O sea, **que `offeredZoneIds` acote la consulta en SQL**. Las zonas ofrecidas son las que el panel dibuja, así que contar sobre ésas y no sobre todas es a la vez más barato **y** más honesto: se cuenta exactamente lo que se muestra.

    **Lo que hay que cuidar al hacerlo, y no es menor**: los conteos de las relajaciones («sin el filtro de precio habría 31») y `cityTotal` viven en la misma consulta y **no** hablan de las zonas ofrecidas sino de la ciudad. Acotar sin separar esas dos preguntas rompería el botón de «Limpiar todo». `tests/integration/faceted-search.test.ts` compara cada total contra las filas de la búsqueda equivalente, así que un recorte mal hecho no puede pasar en verde — empezar por ahí.

    **No es una regresión hoy.** Es la deuda que la medición dejó a la vista, con su número y su archivo.

    **Hecho (2026-09-08).** `DrizzleFacetedSearch.countFacets` separó las dos preguntas que compartían una sola consulta: `citywide` agrega la ciudad entera SIN `GROUP BY` —`total`, `cityTotal`, las seis facetas de atributo/tipo/publicador, las nueve relajaciones y el escalón siguiente de precio, todos ajenos a qué zona se ofrece— y `zoneAgg` agrupa por zona con `WHERE zoneId IN (offeredZoneIds)`, así que el `GROUP BY` que antes corría sobre toda zona de la ciudad con avisos ahora corre sólo sobre las ofrecidas. Las dos se unen con `citywide CROSS JOIN priceFacet LEFT JOIN zoneAgg` en una sola sentencia, así que `queries === 1` (`tests/integration/faceted-search.test.ts`) sigue siendo cierto — un solo viaje de red, no dos. `build-filter-panel.ts` tenía que cambiar con ella: pasaba `chosenZoneIds` (casi siempre vacío) como `offeredZoneIds`, que con la consulta ya acotada habría dejado el panel sin una sola zona; ahora pasa `request.zones` —el catálogo ya acotado por la 27.1— unido a la elegida, y decide qué zona mostrar por si tiene avisos reales o fue elegida, no por si es una CLAVE del conteo (con la 27.8 toda zona ofrecida lo es, real o en cero).

    **Medido, con las 12 zonas reales de Caracas que siembra `scripts/measure-page-view.ts`.** Con las zonas ofrecidas exactamente iguales a las zonas con avisos (12 = 12), antes y después miden lo mismo — 12 filas, 8.640 bytes en la ciudad; 12 filas, 8.064 en la zona —, y hay que decirlo así de plano: sobre este dato la mejora es cero, porque el ancho de fila no bajó (`citywide` sigue viajando en cada fila junto a `zoneAgg`) y la cantidad tampoco (offered = discovered). El techo que baja es el de FILAS cuando offered ⊂ discovered, que esta siembra no reproduce pero una medición ad hoc contra los mismos datos sí: `countFacets({cityId: Caracas}, [Altamira])` —1 de las 12 zonas con avisos— devuelve **1 fila, 720 bytes** en vez de las 12 y 8.640 que la consulta vieja habría traído, con `total`/`cityTotal` intactos en 29 (la ciudad entera). Es exactamente el caso de Caracas real: 3.220 zonas en la taxonomía, un panel que sólo ofrece un puñado, y una consulta que hoy escala con las zonas ofrecidas y no con las 3.220.

    **Queries por el camino de facetas: 1**, verificado por `tests/integration/faceted-search.test.ts` («una sola consulta») y por `tests/integration/zone-price-tally.test.ts`, que reutiliza el mismo adaptador. No hizo falta el trade-off de una segunda consulta: separar `citywide` de `zoneAgg` y unirlas con `JOIN ... ON true` mantiene el viaje único porque Postgres resuelve las dos subconsultas dentro de la misma sentencia.

    **RED real** (`tests/integration/faceted-search.test.ts`, describe `"el conteo por zona se acota a las zonas ofrecidas, en SQL"`): con Norte (dos avisos reales, A4 y A5) fuera de `offeredZoneIds = [Centro]`, la consulta vieja —`GROUP BY zoneId` sin `WHERE` de zona— igual traía la fila de Norte y la sumaba a `byZone`; la nueva la excluye porque el `WHERE` de `zoneAgg` no la deja entrar. Confirmado leyendo el archivo antes de tocarlo: la fila de Norte pasaba el `if (row.withinPrice > 0) byZone[row.zoneId] = row.inZone` sin que `MCBO_NORTE` estuviera en `offeredZoneIds`. Una segunda prueba en el mismo describe confirma que `cityTotal` y `withoutFilter.zone` siguen contando los cinco avisos de la ciudad aunque sólo Centro esté ofrecida. Verde: 2.939 unitarias, 410 de integración, `tsc --noEmit` y `biome check` limpios.

## Fase 28 — La capa de entrega se comporta, y no sólo se dibuja (2026-09-12)

**De dónde sale.** El fundador recorrió el sitio en producción y en vista previa el 2026-09-11 y 12, con la lista de pruebas manuales de los issues #286 a #291, y anotó lo que veía. **No son treinta defectos sueltos: son ocho raíces**, y ninguna estaba tomada por otra fase.

**Por qué sobrevivieron a la Fase 22.** Aquélla cerró 47 de 47 el 2026-09-07 y trajo el sistema **visual** — tokens, átomos, contraste, geometría a 360 y 1280. **Nunca tocó el comportamiento.** Medido el 2026-09-09: buscando `outside|clickOutside|Escape|onKeyDown` en `components/` aparecen **tres archivos, y uno es un `.module.css` y otro un `.test.tsx`** — o sea **un solo componente de toda la capa de entrega** sabe de clic afuera o de teclado.

**Y el amplificador, que es lo que hay que romper.** El suelo de cobertura llega a `src/modules/*/domain` y `application` y **no llega a `app/` ni `components/`**. Ninguna prueba puede ponerse roja porque un menú no cierre. **El sistema produce exactamente lo que mide**, y por eso esto se embarcó sin que nadie lo notara.

### DECISIÓN DEL FUNDADOR (2026-09-12): las tres medidas canónicas

**Móvil 390 × 844 · Tablet 768 × 1024 · Escritorio 1440 × 900.** Toda pantalla se diseña y se prueba contra esas tres, y no contra otras. Las nueve láminas existentes son 360 y 1280, así que **ninguna de las tres coincide con lo que hay dibujado** — la de tablet directamente no existe, que es lo que la 20.10 ya había medido.

### DECISIÓN DEL FUNDADOR (2026-09-12): el dinamismo primero, y el sin-JavaScript como caída

Textual: *«entiendo que esto fue una de las peticiones que se hicieron al principio pero fue una mala petición: necesitamos dinamismo… pero también necesitamos que funcione sin javascript»*.

**Esto revierte una restricción fundacional y hay que decirlo sin suavizarlo.** El D13 —«sin sesión y sin JavaScript de cliente»— hizo que el camino de lectura entero se sirviera desde el servidor, y eso **se conserva y sigue siendo correcto** para el rastreador y para quien llega sin JavaScript. Lo que cambia es el orden: **con JavaScript disponible, la pantalla se comporta como una aplicación**; sin él, cae al camino servido que ya existe. Hoy el orden es el inverso, y por eso un modal es «una pantalla encima de otra» y un filtro exige recargar.

- [ ] 28.1 **Una primitiva de capa descartable, y todos los menús pasan a usarla.** Hoy no existe: cada menú resolvió su apertura a mano y **ninguno cierra con clic afuera ni con Escape**. El fundador lo dice de todos: *«ningún menú que abre se cierra, hay que darle de nuevo al botón, hay que buscarlas todas en toda la app»*. Instancias confirmadas: el menú de cuenta del encabezado, el de tres puntos de la carga de fotos, y el panel de filtros. **El arreglo BORRA código, no lo agrega**: una pieza con clic afuera, Escape y foco que vuelve al botón, y los toggles a mano desaparecen. **Hecho** significa: la primitiva existe, los tres menús la usan, y hay una prueba que se pone roja si uno deja de cerrar.

- [ ] 28.2 **El panel de filtros es un modal de verdad cuando hay JavaScript.** Hoy es «una pantalla encima de otra»: no cierra con clic afuera, sólo con la X; **«Limpiar todo» limpia y deja el modal abierto**; y mientras está abierto **el fondo se va filtrando solo**, que el fundador llama textualmente *«no tiene ningún sentido»*. Con la decisión de arriba: los filtros se eligen en el cliente, **se aplican al apretar el botón**, y el modal cierra al aplicar. Sin JavaScript cae al camino servido de hoy, que se conserva entero.

- [x] 28.3 **El conteo del filtro miente, y eso es la regla 3 rota.** Medido por el fundador en `dev`: filtrando por **dos habitaciones** en Maracaibo la pantalla dice **«siete propiedades»** y **se ven cuatro avisos**. La regla transversal 3 es «si una etiqueta dice 9, hay 9». **Esto no es diseño: es un defecto de conteo** y va primero que cualquier rediseño del panel.

    **La 27.8 se descartó primero, y quedó inocente.** Su cambio a `citywide`/`zoneAgg` movió el `total` a una subconsulta y le puso `.as(...)`, pero la EXPRESIÓN que lo calcula —`countWhere(...others())`— es literal, carácter por carácter, la misma de antes (`git show c759396` contra el archivo). `search()` y `countFacets()` comparten exactamente el mismo `WHERE`: ciudad, `status='active'`, `expiresAt>now()` (task 21.1, las dos condiciones en los dos lectores), y habitaciones como MÍNIMO (`gte`) en ambos — nunca "exacto" en uno y "o más" en el otro. Los tres caminos que la tarea pedía descartar quedaron descartados por lectura de código, no por sospecha.

    **La causa real: `buildListingGrid` descarta en JavaScript lo que `countFacets` ya había contado en SQL.** `app/alquiler/[ciudad]/page.tsx` llama a `search()` (SQL) y arma `cards` con `buildListingGrid`, que desde su origen implementa la regla F9 —«un aviso sin portada no se muestra»— descartando todo aviso cuya portada no tenga las DOS derivadas requeridas (`REQUIRED_SIZES`: `thumb` y `card`). Ese descarte ocurre DESPUÉS de que `countFacets` ya contó la fila: es la misma forma del defecto que la 27.1 y la 27.8 ya corrigieron para la taxonomía y las zonas — la base cuenta de más y el cliente recorta —, ahora en la fotografía. El propio código ya lo documentaba sin que nadie lo cerrara: el comentario de `page.tsx` decía textualmente "los avisos sin portada no se dibujan (F9) pero sí se cuentan, así que este número puede ser mayor que la cantidad de tarjetas — sigue sin cerrar". La importación de cartera de la Fase 9 no exige foto al importar, y aunque `activateListing` exige al menos una antes de marcar `active` (`bulk-import-to-search.test.ts`), el rellenado de derivadas de la 19a puede dejar una portada a medio derivar — con `thumb` y sin `card`, o viceversa — y esa fila sigue contando mientras la cuadrícula la descarta en silencio.

    **Por qué `tests/integration/faceted-search.test.ts` no lo veía.** Su describe "si una etiqueta dice 9, hay 9" compara `counts.total` contra `search().length` — las DOS consultas SQL, nunca contra lo que `buildListingGrid` puede dibujar. Ninguna de las dos sabe de fotos, así que la comparación siempre daba verde aunque las dos estuvieran de acuerdo en contar un aviso invisible. El hueco no era una combinación faltante: era que el oráculo de la prueba nunca alcanzaba la capa donde el descarte ocurre.

    **El arreglo, mínimo y sin tocar `search()`.** `DrizzleFacetedSearch.countFacets` suma una cuarta condición incondicional al `WHERE` compartido —`hasDrawableCover`, un `EXISTS` correlacionado contra `listing_photo`/`listing_photo_derivative` que exige la foto de posición 0 con las dos derivadas de `REQUIRED_SIZES` (importado de `listing-grid.ts`, nunca copiado)—, así que `total`, `cityTotal`, las nueve relajaciones y los conteos por zona dejan de prometer avisos que la cuadrícula no puede dibujar. `search()` NO cambia a propósito: tocarlo habría exigido que `lifecycle.test.ts`, `listing-activation.test.ts`, `listing-reference.test.ts`, `listing-search.test.ts` y `bulk-import-to-search.test.ts` — que crean avisos activos sin una sola foto para probar cosas que no son fotos — dejaran de pasar, muy por fuera del alcance de esta tarea. `buildListingGrid` sigue siendo la red de seguridad que de verdad dibuja.

    **Medido contra `rentas_test` real, con las clases de producción y no un doble.** Tres avisos activos, mismos filtros, una sola ciudad propia: uno con las dos derivadas, uno sin una sola foto (el caso que deja `broker-bulk-import`) y uno con la portada a medio derivar (el caso que deja la 19a). RED confirmado revirtiendo el `WHERE` de `countFacets`: `expected 3 to be 1`. GREEN con el `EXISTS` puesto: `total`, `cityTotal` y `byZone` de la zona dan 1, y `search()` — sin tocar — sigue trayendo los tres, documentado en la prueba misma como la asimetría que `buildListingGrid` todavía cierra. `tests/integration/faceted-search.test.ts` (83/83) y `tests/integration/zone-price-tally.test.ts` (5/5, que compone el mismo adaptador) necesitaron que su arnés compartido dejara de crear avisos sin portada por accidente: `insertListing` ahora inserta una portada completa por defecto (`cover: "full"`), así que ningún conteo que el archivo ya afirmaba cambió. Suite completa: 2.950 unitarias y 411 de integración, todas verdes; `tsc --noEmit` y `biome check` sin hallazgos nuevos.

- [ ] 28.4 **Las tres medidas canónicas, dibujadas y aseveradas.** Tres láminas por pantalla —390 × 844, 768 × 1024, 1440 × 900— y un contrato que las verifique, del mismo modo que `design-contract.test.tsx` ya verifica tokens y contraste. **La banda de tablet no existe hoy** y es donde el fundador encontró *«todo mal»*, incluido **un scroll horizontal** que no debería existir. Esta tarea es la que las demás consumen.

- [ ] 28.5 **El pie, dentro del contenedor, en las tres medidas.** `components/organisms/SiteFooter.tsx` **no envuelve nada en `Container`** —usa `styles.top` y `styles.strip` directos— mientras `Container.module.css` tiene `max-width: 1100px` y `margin-inline: auto`. A pantalla ancha el encabezado queda centrado y **el pie se desparrama de borde a borde**; en móvil **no tiene padding**. Y hay un comentario mintiendo en `SiteFooter.module.css:8`: *«Horizontal inset comes from `Container`»* — dice lo que el componente no hace. **El invariante se asevera una vez y cubre toda sección futura**, no sólo el pie.

- [ ] 28.6 **El encabezado en móvil, rediseñado.** *«Está horrible, no va bien»*, a 390 × 844. Y la pastilla de búsqueda **es más angosta que en el diseño**. Depende de la 28.4.

- [ ] 28.7 **El texto largo fluye; nada de altos ni anchos fijos.** Con la taxonomía real hay zonas como «Barrio Tierra Negra del Sector Bella Vista», y el metadato de la tarjeta **la corta**. El fundador: *«sé que decidimos que sea un espacio fijo, pero cuando hay texto grande debería hacer el salto de línea dinámicamente; no puede haber nada fijo para eso estamos trabajando responsive»*. Afecta tarjeta, ficha y pastillas de filtro.

- [ ] 28.8 **El panel de filtros pierde lo que sobra.** Cuatro cosas que el fundador nombró una por una: **(a)** los botones «usar este precio» y «usar esta superficie» **no deben existir** — poner el valor ya es querer usarlo; **(b)** los conteos por faceta tipo «1 de 71» **no tienen sentido** en un conjunto de filtros que se activan y desactivan; **(c)** el botón azul que cambia de texto —«quitar zonas y ver nueve»— **es confuso**: la sugerencia de relajación va arriba como texto, y el botón dice siempre lo mismo, «aplicar filtros»; **(d)** el conteo duplicado bajo el encabezado se va, porque «nueve propiedades activas» ya está arriba y **además no está alineado con el ancho del resto**.

- [ ] 28.9 **Ordenar y limpiar, donde se necesitan.** El botón dice **«Recientes»** y en realidad ordena: pasa a **«Ordenar por»**, con precio mayor/menor y publicación más nueva/más vieja. Y **en móvil no hay dónde limpiar los filtros**: hoy son **cuatro pasos** —abrir las tres rayitas, entrar, limpiar—. El botón de limpiar va a la pantalla, debajo del título o de la miga.

- [ ] 28.10 **Las sugerencias de la pastilla.** Tres cosas: **(a)** en el inicio **dejaron de aparecer al escribir** — el fundador dice *«no sé por qué antes las mostraba sin problema»*, **así que se trata como regresión y se busca cuándo se rompió**; **(b)** el desplegable **es más angosto que la pastilla** y debe medir lo mismo; **(c)** el conteo dentro de cada sugerencia **se quita** — nadie lo mira y cuesta.

- [ ] 28.11 **El menú de cuenta no tiene cómo cerrar sesión.** No es diseño, es una función que falta.

- [ ] 28.12 **Ayuda y legales, diseñadas.** Las diez páginas *«están feas y simples»*. Hoy no tienen lámina propia: heredan lo que haya. Depende de la 28.4.

- [ ] 28.13 **La fila de pastillas queda pegada a la cuadrícula.** El fundador lo vio en **tablet y en escritorio**: *«sale la pastilla del filtro agregada, está muy pegada al anuncio»* y *«no veo que haya un margen entre la pastilla del aviso y queda pegada a la parte de arriba también»*. Falta separación vertical entre la fila de filtros aplicados y los resultados. Chico, pero se nota en las dos medidas grandes.

- [ ] 28.14 **La imagen del aviso es demasiado ancha en escritorio.** A 1440 × 900: *«la veo muy grande, muy ancha; eso tenemos que rediseñar, tiene que ser un poquito más pequeña»*. Es la tarjeta de la cuadrícula, no la ficha. Depende de la 28.4, porque la medida sale de la lámina.

- [ ] 28.15 **En resultados móvil falta una salida hacia atrás.** *«Lo que sí me gustaría es un botón atrás o algo así»*. Hoy la única forma de volver es el gesto del navegador. Decidir con la lámina de 390 × 844 si es un botón propio o la miga de pan la que cumple ese papel — hoy la miga existe pero el fundador no la reconoció como salida, y eso ya es un dato.

- [ ] 28.16 **Los nombres de zona largos producen direcciones larguísimas, y hay que decidir si eso está bien.** Ejemplo real en vista previa: `/alquiler/maracaibo/barrio-tierra-negra-del-sector-bella-vista`. **No es un defecto del sembrador ni de la taxonomía**: «Barrio Tierra Negra del Sector Bella Vista» es el nombre real de la zona en `docs/territorio/`, y el sembrador de demos lo usa justamente porque «Tierra Negra» a secas dejó de existir con la resiembra de la 17.15. La pregunta que queda abierta es de producto: **¿una dirección así es aceptable, o el slug debería acortarse?** Acortarlo tiene un costo que hay que mirar antes: `slugify` es la misma función que resuelve la ruta, y cambiar la regla cambia **todas** las direcciones ya existentes. Se decide con medición, no de oficio.

- [ ] 28.17 **PENDIENTE DE ACLARACIÓN — el hallazgo 6 del issue #286.** El fundador adjuntó una captura de escritorio con el texto *«esto es en la version desktop»* y nada más. **No se puede derivar de ahí qué está mal**, y adivinarlo sería inventar una tarea. Queda escrita para no perderla: hay que preguntarle qué muestra esa imagen antes de darle número propio o plegarla a otra tarea.
